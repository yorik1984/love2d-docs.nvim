*love2d-docs.txt* *love2d-docs*      Documentation for the LÖVE game framework.

                               o  o                    ~
                      ╭─╮    ╭──────╮╭─╮    ╭─╮╭─────╮ ~
                      │ │    │ ╭──╮ ││ │    │ ││ ╭───╯ ~
                      │ │    │ │  │ │╰╮╰╮  ╭╯╭╯│ ╰───╮ ~
                      │ │    │ │  │ │ ╰╮╰╮╭╯╭╯ │ ╭───╯ ~
                      │ ╰───╮│ ╰──╯ │  ╰╮╰╯╭╯  │ ╰───╮ ~
                      ╰─────╯╰──────╯   ╰──╯   ╰─────╯ ~
                The complete solution for (Neo)Vim with LÖVE.
                   Includes documentation.

For LÖVE (http://love2d.org) version 11.5.

Generated from

    https://github.com/love2d-community/love-api

using

    https://github.com/yorik1984/love2d-docs.nvim

Original work by Davis Claiborne under the MIT license.

    https://github.com/davisdude/vim-love-docs

Modified and maintained by yorik1984 under the MIT license.

See LICENSE.md for more info.

===============================================================================
                                                             *love2d-docs-love*
|love|

The LÖVE framework

    |callbacks|                                    |love2d-docs-love-callbacks|
    |enums|                                            |love2d-docs-love-enums|
    |functions|                                    |love2d-docs-love-functions|
    |types|                                            |love2d-docs-love-types|

-------------------------------------------------------------------------------
                                                   *love2d-docs-love-callbacks*
The callbacks of |love|:

    |conf|                                              |love2d-docs-love.conf|
    |directorydropped|                      |love2d-docs-love.directorydropped|
    |displayrotated|                          |love2d-docs-love.displayrotated|
    |draw|                                              |love2d-docs-love.draw|
    |errorhandler|                              |love2d-docs-love.errorhandler|
    |filedropped|                                |love2d-docs-love.filedropped|
    |focus|                                            |love2d-docs-love.focus|
    |gamepadaxis|                                |love2d-docs-love.gamepadaxis|
    |gamepadpressed|                          |love2d-docs-love.gamepadpressed|
    |gamepadreleased|                        |love2d-docs-love.gamepadreleased|
    |joystickadded|                            |love2d-docs-love.joystickadded|
    |joystickaxis|                              |love2d-docs-love.joystickaxis|
    |joystickhat|                                |love2d-docs-love.joystickhat|
    |joystickpressed|                        |love2d-docs-love.joystickpressed|
    |joystickreleased|                      |love2d-docs-love.joystickreleased|
    |joystickremoved|                        |love2d-docs-love.joystickremoved|
    |keypressed|                                  |love2d-docs-love.keypressed|
    |keyreleased|                                |love2d-docs-love.keyreleased|
    |load|                                              |love2d-docs-love.load|
    |lowmemory|                                    |love2d-docs-love.lowmemory|
    |mousefocus|                                  |love2d-docs-love.mousefocus|
    |mousemoved|                                  |love2d-docs-love.mousemoved|
    |mousepressed|                              |love2d-docs-love.mousepressed|
    |mousereleased|                            |love2d-docs-love.mousereleased|
    |quit|                                              |love2d-docs-love.quit|
    |resize|                                          |love2d-docs-love.resize|
    |run|                                                |love2d-docs-love.run|
    |textedited|                                  |love2d-docs-love.textedited|
    |textinput|                                    |love2d-docs-love.textinput|
    |threaderror|                                |love2d-docs-love.threaderror|
    |touchmoved|                                  |love2d-docs-love.touchmoved|
    |touchpressed|                              |love2d-docs-love.touchpressed|
    |touchreleased|                            |love2d-docs-love.touchreleased|
    |update|                                          |love2d-docs-love.update|
    |visible|                                        |love2d-docs-love.visible|
    |wheelmoved|                                  |love2d-docs-love.wheelmoved|

-------------------------------------------------------------------------------
                                                        *love2d-docs-love.conf*
|love.conf|

If a file called conf.lua is present in your game folder (or .love file), it is
run before the LÖVE modules are loaded. You can use this file to overwrite the
love.conf function, which is later called by the LÖVE 'boot' script. Using the
love.conf function, you can set some configuration options, and change things
like the default size of the window, which modules are loaded, and other stuff.

Synopses: ~

        |love.conf|(`t`)

Variants: ~

        |love.conf|(`t`)

        See function description

        Parameters: ~
            • {t} (`table`)
                The love.conf function takes one argument: a table filled with
                all the default values which you can overwrite to your liking.
                If you want to change the default window size, for instance,
                do:

                function love.conf(t)
                    t.window.width = 1024
                    t.window.height = 768
                end

                If you don't need the physics module or joystick module, do the
                following.

                function love.conf(t)
                    t.modules.joystick = false
                    t.modules.physics = false
                end

                Setting unused modules to false is encouraged when you release
                your game. It reduces startup time slightly (especially if the
                joystick module is disabled) and reduces memory usage
                (slightly).

                Note that you can't disable love.filesystem; it's mandatory.
                The same goes for the love module itself. love.graphics needs
                love.window to be enabled.
                • {identity} (`string`) (default: `nil`)
                    This flag determines the name of the save directory for
                    your game. Note that you can only specify the name, not the
                    location where it will be created:
                    t.identity = "gabe_HL3" -- Correct

                    t.identity = "c:/Users/gabe/HL3" -- Incorrect
                    Alternatively love.filesystem.setIdentity can be used to
                    set the save directory outside of the config file.
                • {appendidentity} (`boolean`) (default: `false`)
                    This flag determines if game directory should be searched
                    first then save directory (true) or otherwise (false)
                • {version} (`string`) (default: `"11.5"`)
                    t.version should be a string, representing the version of
                    LÖVE for which your game was made. It should be formatted
                    as "X.Y.Z" where X is the major release number, Y the
                    minor, and Z the patch level. It allows LÖVE to display a
                    warning if it isn't compatible. Its default is the version
                    of LÖVE running.
                • {console} (`boolean`) (default: `false`)
                    Determines whether a console should be opened alongside the
                    game window (Windows only) or not. Note: On OSX you can get
                    console output by running LÖVE through the terminal.
                • {accelerometerjoystick} (`boolean`) (default: `true`)
                    Sets whether the device accelerometer on iOS and Android
                    should be exposed as a 3-axis Joystick. Disabling the
                    accelerometer when it's not used may reduce CPU usage.
                • {externalstorage} (`boolean`) (default: `false`)
                    Sets whether files are saved in external storage (true) or
                    internal storage (false) on Android.
                • {gammacorrect} (`boolean`) (default: `false`)
                    Determines whether gamma-correct rendering is enabled, when
                    the system supports it.
                • {audio} (`table`)
                    Audio options.
                    • {mic} (`boolean`) (default: `false`)
                        Request microphone permission from the user. When user
                        allows it, love.audio.getRecordingDevices will lists
                        recording devices available. Otherwise,
                        love.audio.getRecordingDevices returns empty table and
                        a message is shown to inform user when called.
                    • {mixwithsystem} (`boolean`) (default: `true`)
                        Sets whether background audio / music from other apps
                        should play while LÖVE is open. See
                        love.system.hasBackgroundMusic for more details.
                • {window} (`table`)
                    It is possible to defer window creation until
                    love.window.setMode is first called in your code. To do so,
                    set t.window = nil in love.conf (or t.screen = nil in older
                    versions.) If this is done, LÖVE may crash if any function
                    from love.graphics is called before the first
                    love.window.setMode in your code.

                    The t.window table was named t.screen in versions prior to
                    0.9.0. The t.screen table doesn't exist in love.conf in
                    0.9.0, and the t.window table doesn't exist in love.conf in
                    0.8.0. This means love.conf will fail to execute (therefore
                    it will fall back to default values) if care is not taken
                    to use the correct table for the LÖVE version being used.
                    • {title} (`string`) (default: `"Untitled"`)
                        Sets the title of the window the game is in.
                        Alternatively love.window.setTitle can be used to
                        change the window title outside of the config file.
                    • {icon} (`string`) (default: `nil`)
                        A filepath to an image to use as the window's icon. Not
                        all operating systems support very large icon images.
                        The icon can also be changed with love.window.setIcon.
                    • {width} (`number`) (default: `800`)
                        Sets the window's dimensions. If these flags are set to
                        0 LÖVE automatically uses the user's desktop
                        dimensions.
                    • {height} (`number`) (default: `600`)
                        Sets the window's dimensions. If these flags are set to
                        0 LÖVE automatically uses the user's desktop
                        dimensions.
                    • {borderless} (`boolean`) (default: `false`)
                        Removes all border visuals from the window. Note that
                        the effects may wary between operating systems.
                    • {resizable} (`boolean`) (default: `false`)
                        If set to true this allows the user to resize the
                        game's window.
                    • {minwidth} (`number`) (default: `1`)
                        Sets the minimum width and height for the game's window
                        if it can be resized by the user. If you set lower
                        values to window.width and window.height LÖVE will
                        always favor the minimum dimensions set via
                        window.minwidth and window.minheight.
                    • {minheight} (`number`) (default: `1`)
                        Sets the minimum width and height for the game's window
                        if it can be resized by the user. If you set lower
                        values to window.width and window.height LÖVE will
                        always favor the minimum dimensions set via
                        window.minwidth and window.minheight.
                    • {fullscreen} (`boolean`) (default: `false`)
                        Whether to run the game in fullscreen (true) or
                        windowed (false) mode. The fullscreen can also be
                        toggled via love.window.setFullscreen or
                        love.window.setMode.
                    • {fullscreentype} (`string`) (default: `"desktop"`)
                        Specifies the type of fullscreen mode to use (normal or
                        desktop). Generally the desktop is recommended, as it
                        is less restrictive than normal mode on some operating
                        systems.
                    • {usedpiscale} (`boolean`) (default: `true`)
                        Sets whetever to enable or disable automatic DPI
                        scaling.
                    • {vsync} (`number`) (default: `true`)
                        Enables or deactivates vertical synchronization. Vsync
                        tries to keep the game at a steady framerate and can
                        prevent issues like screen tearing. It is recommended
                        to keep vsync activated if you don't know about the
                        possible implications of turning it off. Before LÖVE
                        11.0, this value was boolean (true or false). Since
                        LÖVE 11.0, this value is number (1 to enable vsync, 0
                        to disable vsync, -1 to use adaptive vsync when
                        supported).

                        Note that in iOS, vertical synchronization is always
                        enabled and cannot be changed.
                    • {depth} (`number`) (default: `nil`)
                        The number of bits per sample in the depth buffer
                        (16/24/32, default nil)
                    • {stencil} (`number`) (default: `nil`)
                        Then number of bits per sample in the depth buffer
                        (generally 8, default nil)
                    • {msaa} (`number`) (default: `0`)
                        The number of samples to use with multi-sampled
                        antialiasing.
                    • {display} (`number`) (default: `1`)
                        The index of the display to show the window in, if
                        multiple monitors are available.
                    • {highdpi} (`boolean`) (default: `false`)
                        See love.window.getPixelScale, love.window.toPixels,
                        and love.window.fromPixels. It is recommended to keep
                        this option disabled if you can't test your game on a
                        Mac or iOS system with a Retina display, because code
                        will need tweaking to make sure things look correct.
                    • {x} (`number`) (default: `nil`)
                        Determines the position of the window on the user's
                        screen. Alternatively love.window.setPosition can be
                        used to change the position on the fly.
                    • {y} (`number`) (default: `nil`)
                        Determines the position of the window on the user's
                        screen. Alternatively love.window.setPosition can be
                        used to change the position on the fly.
                • {modules} (`table`)
                    Module options.
                    • {audio} (`boolean`) (default: `true`)
                        Enable the audio module.
                    • {event} (`boolean`) (default: `true`)
                        Enable the event module.
                    • {graphics} (`boolean`) (default: `true`)
                        Enable the graphics module.
                    • {image} (`boolean`) (default: `true`)
                        Enable the image module.
                    • {joystick} (`boolean`) (default: `true`)
                        Enable the joystick module.
                    • {keyboard} (`boolean`) (default: `true`)
                        Enable the keyboard module.
                    • {math} (`boolean`) (default: `true`)
                        Enable the math module.
                    • {mouse} (`boolean`) (default: `true`)
                        Enable the mouse module.
                    • {physics} (`boolean`) (default: `true`)
                        Enable the physics module.
                    • {sound} (`boolean`) (default: `true`)
                        Enable the sound module.
                    • {system} (`boolean`) (default: `true`)
                        Enable the system module.
                    • {timer} (`boolean`) (default: `true`)
                        Enable the timer module.
                    • {touch} (`boolean`) (default: `true`)
                        Enable the touch module.
                    • {video} (`boolean`) (default: `true`)
                        Enable the video module.
                    • {window} (`boolean`) (default: `true`)
                        Enable the window module.
                    • {thread} (`boolean`) (default: `true`)
                        Enable the thread module.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.directorydropped*
|love.directorydropped|

Callback function triggered when a directory is dragged and dropped onto the
window.

Synopses: ~

        |love.directorydropped|(`path`)

Variants: ~

        |love.directorydropped|(`path`)

        Paths passed into this callback are able to be used with
        love.filesystem.mount, which is the only way to get read access via
        love.filesystem to the dropped directory. love.filesystem.mount does
        not generally accept other full platform-dependent directory paths that
        haven't been dragged and dropped onto the window.

        Parameters: ~
            • {path} (`string`)
                The full platform-dependent path to the directory. It can be
                used as an argument to love.filesystem.mount, in order to gain
                read access to the directory with love.filesystem.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.displayrotated*
|love.displayrotated|

Called when the device display orientation changed, for example, user rotated
their phone 180 degrees.

Synopses: ~

        |love.displayrotated|(`index`, `orientation`)

Variants: ~

        |love.displayrotated|(`index`, `orientation`)

        See function description

        Parameters: ~
            • {index} (`number`)
                The index of the display that changed orientation.
            • {orientation} (`DisplayOrientation`) |love2d-docs-DisplayOrientation|
                The new orientation.


-------------------------------------------------------------------------------
                                                        *love2d-docs-love.draw*
|love.draw|

Callback function used to draw on the screen every frame.

Synopses: ~

        |love.draw|()

Variants: ~

        |love.draw|()

        See function description


-------------------------------------------------------------------------------
                                                *love2d-docs-love.errorhandler*
|love.errorhandler|

The error handler, used to display error messages.

Synopses: ~

        `mainLoop` = |love.errorhandler|(`msg`)

Variants: ~

        `mainLoop` = |love.errorhandler|(`msg`)

        See function description

        Parameters: ~
            • {msg} (`string`)
                The error message.

        Return: ~
            (`function`)
                Function which handles one frame, including events and
                rendering, when called. If this is nil then LÖVE exits
                immediately.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.filedropped*
|love.filedropped|

Callback function triggered when a file is dragged and dropped onto the window.

Synopses: ~

        |love.filedropped|(`file`)

Variants: ~

        |love.filedropped|(`file`)



        Parameters: ~
            • {file} (`DroppedFile`) |love2d-docs-DroppedFile|
                The unopened File object representing the file that was
                dropped.


-------------------------------------------------------------------------------
                                                       *love2d-docs-love.focus*
|love.focus|

Callback function triggered when window receives or loses focus.

Synopses: ~

        |love.focus|(`focus`)

Variants: ~

        |love.focus|(`focus`)

        See function description

        Parameters: ~
            • {focus} (`boolean`)
                True if the window gains focus, false if it loses focus.


-------------------------------------------------------------------------------
                                                 *love2d-docs-love.gamepadaxis*
|love.gamepadaxis|

Called when a Joystick's virtual gamepad axis is moved.

Synopses: ~

        |love.gamepadaxis|(`joystick`, `axis`, `value`)

Variants: ~

        |love.gamepadaxis|(`joystick`, `axis`, `value`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {axis} (`GamepadAxis`) |love2d-docs-GamepadAxis|
                The virtual gamepad axis.
            • {value} (`number`)
                The new axis value.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.gamepadpressed*
|love.gamepadpressed|

Called when a Joystick's virtual gamepad button is pressed.

Synopses: ~

        |love.gamepadpressed|(`joystick`, `button`)

Variants: ~

        |love.gamepadpressed|(`joystick`, `button`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {button} (`GamepadButton`) |love2d-docs-GamepadButton|
                The virtual gamepad button.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.gamepadreleased*
|love.gamepadreleased|

Called when a Joystick's virtual gamepad button is released.

Synopses: ~

        |love.gamepadreleased|(`joystick`, `button`)

Variants: ~

        |love.gamepadreleased|(`joystick`, `button`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {button} (`GamepadButton`) |love2d-docs-GamepadButton|
                The virtual gamepad button.


-------------------------------------------------------------------------------
                                               *love2d-docs-love.joystickadded*
|love.joystickadded|

Called when a Joystick is connected.

Synopses: ~

        |love.joystickadded|(`joystick`)

Variants: ~

        |love.joystickadded|(`joystick`)

        This callback is also triggered after love.load for every Joystick
        which was already connected when the game started up.

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The newly connected Joystick object.


-------------------------------------------------------------------------------
                                                *love2d-docs-love.joystickaxis*
|love.joystickaxis|

Called when a joystick axis moves.

Synopses: ~

        |love.joystickaxis|(`joystick`, `axis`, `value`)

Variants: ~

        |love.joystickaxis|(`joystick`, `axis`, `value`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {axis} (`number`)
                The axis number.
            • {value} (`number`)
                The new axis value.


-------------------------------------------------------------------------------
                                                 *love2d-docs-love.joystickhat*
|love.joystickhat|

Called when a joystick hat direction changes.

Synopses: ~

        |love.joystickhat|(`joystick`, `hat`, `direction`)

Variants: ~

        |love.joystickhat|(`joystick`, `hat`, `direction`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {hat} (`number`)
                The hat number.
            • {direction} (`JoystickHat`) |love2d-docs-JoystickHat|
                The new hat direction.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.joystickpressed*
|love.joystickpressed|

Called when a joystick button is pressed.

Synopses: ~

        |love.joystickpressed|(`joystick`, `button`)

Variants: ~

        |love.joystickpressed|(`joystick`, `button`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {button} (`number`)
                The button number.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.joystickreleased*
|love.joystickreleased|

Called when a joystick button is released.

Synopses: ~

        |love.joystickreleased|(`joystick`, `button`)

Variants: ~

        |love.joystickreleased|(`joystick`, `button`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The joystick object.
            • {button} (`number`)
                The button number.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.joystickremoved*
|love.joystickremoved|

Called when a Joystick is disconnected.

Synopses: ~

        |love.joystickremoved|(`joystick`)

Variants: ~

        |love.joystickremoved|(`joystick`)

        See function description

        Parameters: ~
            • {joystick} (`Joystick`) |love2d-docs-Joystick|
                The now-disconnected Joystick object.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.keypressed*
|love.keypressed|

Callback function triggered when a key is pressed.

Synopses: ~

    1.  |love.keypressed|(`key`, `scancode`, `isrepeat`)
    2.  |love.keypressed|(`key`, `isrepeat`)

Variants: ~

    1.  |love.keypressed|(`key`, `scancode`, `isrepeat`)

    1.          Scancodes are keyboard layout-independent, so the scancode 'w' will be
        generated if the key in the same place as the 'w' key on an American
        keyboard is pressed, no matter what the key is labelled or what the
        user's operating system settings are.

        Key repeat needs to be enabled with love.keyboard.setKeyRepeat for
        repeat keypress events to be received. This does not affect
        love.textinput.

        Parameters: ~
            • {key} (`KeyConstant`) |love2d-docs-KeyConstant|
                Character of the pressed key.
            • {scancode} (`Scancode`) |love2d-docs-Scancode|
                The scancode representing the pressed key.
            • {isrepeat} (`boolean`)
                Whether this keypress event is a repeat. The delay between key
                repeats depends on the user's system settings.


    2.  |love.keypressed|(`key`, `isrepeat`)

    2.          Key repeat needs to be enabled with love.keyboard.setKeyRepeat for
        repeat keypress events to be received.

        Parameters: ~
            • {key} (`KeyConstant`) |love2d-docs-KeyConstant|
                Character of the key pressed.
            • {isrepeat} (`boolean`)
                Whether this keypress event is a repeat. The delay between key
                repeats depends on the user's system settings.


-------------------------------------------------------------------------------
                                                 *love2d-docs-love.keyreleased*
|love.keyreleased|

Callback function triggered when a keyboard key is released.

Synopses: ~

        |love.keyreleased|(`key`, `scancode`)

Variants: ~

        |love.keyreleased|(`key`, `scancode`)

        Scancodes are keyboard layout-independent, so the scancode 'w' will be
        used if the key in the same place as the 'w' key on an American
        keyboard is pressed, no matter what the key is labelled or what the
        user's operating system settings are.

        Parameters: ~
            • {key} (`KeyConstant`) |love2d-docs-KeyConstant|
                Character of the released key.
            • {scancode} (`Scancode`) |love2d-docs-Scancode|
                The scancode representing the released key.


-------------------------------------------------------------------------------
                                                        *love2d-docs-love.load*
|love.load|

This function is called exactly once at the beginning of the game.

Synopses: ~

        |love.load|(`arg`, `unfilteredArg`)

Variants: ~

        |love.load|(`arg`, `unfilteredArg`)

        In LÖVE 11.0, the passed arguments excludes the game name and the
        fused command-line flag (if exist) when runs from non-fused LÖVE
        executable. Previous version pass the argument as-is without any
        filtering.

        Parameters: ~
            • {arg} (`table`)
                Command-line arguments given to the game.
            • {unfilteredArg} (`table`)
                Unfiltered command-line arguments given to the executable (see
                #Notes).


-------------------------------------------------------------------------------
                                                   *love2d-docs-love.lowmemory*
|love.lowmemory|

Callback function triggered when the system is running out of memory on mobile
devices.

Mobile operating systems may forcefully kill the game if it uses too much
memory, so any non-critical resource should be removed if possible (by setting
all variables referencing the resources to '''nil'''), when this event is
triggered. Sounds and images in particular tend to use the most memory.

Synopses: ~

        |love.lowmemory|()

Variants: ~

        |love.lowmemory|()

        See function description


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.mousefocus*
|love.mousefocus|

Callback function triggered when window receives or loses mouse focus.

Synopses: ~

        |love.mousefocus|(`focus`)

Variants: ~

        |love.mousefocus|(`focus`)

        See function description

        Parameters: ~
            • {focus} (`boolean`)
                Whether the window has mouse focus or not.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.mousemoved*
|love.mousemoved|

Callback function triggered when the mouse is moved.

Synopses: ~

        |love.mousemoved|(`x`, `y`, `dx`, `dy`, `istouch`)

Variants: ~

        |love.mousemoved|(`x`, `y`, `dx`, `dy`, `istouch`)

        If Relative Mode is enabled for the mouse, the '''dx''' and '''dy'''
        arguments of this callback will update but '''x''' and '''y''' are not
        guaranteed to.

        Parameters: ~
            • {x} (`number`)
                The mouse position on the x-axis.
            • {y} (`number`)
                The mouse position on the y-axis.
            • {dx} (`number`)
                The amount moved along the x-axis since the last time
                love.mousemoved was called.
            • {dy} (`number`)
                The amount moved along the y-axis since the last time
                love.mousemoved was called.
            • {istouch} (`boolean`)
                True if the mouse button press originated from a touchscreen
                touch-press.


-------------------------------------------------------------------------------
                                                *love2d-docs-love.mousepressed*
|love.mousepressed|

Callback function triggered when a mouse button is pressed.

Synopses: ~

        |love.mousepressed|(`x`, `y`, `button`, `istouch`, `presses`)

Variants: ~

        |love.mousepressed|(`x`, `y`, `button`, `istouch`, `presses`)

        Use love.wheelmoved to detect mouse wheel motion. It will not register
        as a button press in version 0.10.0 and newer.

        Parameters: ~
            • {x} (`number`)
                Mouse x position, in pixels.
            • {y} (`number`)
                Mouse y position, in pixels.
            • {button} (`number`)
                The button index that was pressed. 1 is the primary mouse
                button, 2 is the secondary mouse button and 3 is the middle
                button. Further buttons are mouse dependent.
            • {istouch} (`boolean`)
                True if the mouse button press originated from a touchscreen
                touch-press.
            • {presses} (`number`)
                The number of presses in a short time frame and small area,
                used to simulate double, triple clicks


-------------------------------------------------------------------------------
                                               *love2d-docs-love.mousereleased*
|love.mousereleased|

Callback function triggered when a mouse button is released.

Synopses: ~

        |love.mousereleased|(`x`, `y`, `button`, `istouch`, `presses`)

Variants: ~

        |love.mousereleased|(`x`, `y`, `button`, `istouch`, `presses`)

        See function description

        Parameters: ~
            • {x} (`number`)
                Mouse x position, in pixels.
            • {y} (`number`)
                Mouse y position, in pixels.
            • {button} (`number`)
                The button index that was released. 1 is the primary mouse
                button, 2 is the secondary mouse button and 3 is the middle
                button. Further buttons are mouse dependent.
            • {istouch} (`boolean`)
                True if the mouse button release originated from a touchscreen
                touch-release.
            • {presses} (`number`)
                The number of presses in a short time frame and small area,
                used to simulate double, triple clicks


-------------------------------------------------------------------------------
                                                        *love2d-docs-love.quit*
|love.quit|

Callback function triggered when the game is closed.

Synopses: ~

        `r` = |love.quit|()

Variants: ~

        `r` = |love.quit|()

        See function description

        Return: ~
            (`boolean`)
                Abort quitting. If true, do not close the game.
-------------------------------------------------------------------------------
                                                      *love2d-docs-love.resize*
|love.resize|

Called when the window is resized, for example if the user resizes the window,
or if love.window.setMode is called with an unsupported width or height in
fullscreen and the window chooses the closest appropriate size.

Synopses: ~

        |love.resize|(`w`, `h`)

Variants: ~

        |love.resize|(`w`, `h`)

        Calls to love.window.setMode will '''only''' trigger this event if the
        width or height of the window after the call doesn't match the
        requested width and height. This can happen if a fullscreen mode is
        requested which doesn't match any supported mode, or if the fullscreen
        type is 'desktop' and the requested width or height don't match the
        desktop resolution.

        Since 11.0, this function returns width and height in DPI-scaled units
        rather than pixels.

        Parameters: ~
            • {w} (`number`)
                The new width.
            • {h} (`number`)
                The new height.


-------------------------------------------------------------------------------
                                                         *love2d-docs-love.run*
|love.run|

The main function, containing the main loop. A sensible default is used when
left out.

Synopses: ~

        `mainLoop` = |love.run|()

Variants: ~

        `mainLoop` = |love.run|()

        See function description

        Return: ~
            (`function`)
                Function which handlers one frame, including events and
                rendering when called.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.textedited*
|love.textedited|

Called when the candidate text for an IME (Input Method Editor) has changed.

The candidate text is not the final text that the user will eventually choose.
Use love.textinput for that.

Synopses: ~

        |love.textedited|(`text`, `start`, `length`)

Variants: ~

        |love.textedited|(`text`, `start`, `length`)



        Parameters: ~
            • {text} (`string`)
                The UTF-8 encoded unicode candidate text.
            • {start} (`number`)
                The start cursor of the selected candidate text.
            • {length} (`number`)
                The length of the selected candidate text. May be 0.


-------------------------------------------------------------------------------
                                                   *love2d-docs-love.textinput*
|love.textinput|

Called when text has been entered by the user. For example if shift-2 is
pressed on an American keyboard layout, the text '@' will be generated.

Synopses: ~

        |love.textinput|(`text`)

Variants: ~

        |love.textinput|(`text`)

        Although Lua strings can store UTF-8 encoded unicode text just fine,
        many functions in Lua's string library will not treat the text as you
        might expect. For example, #text (and string.len(text)) will give the
        number of ''bytes'' in the string, rather than the number of unicode
        characters. The Lua wiki and a presentation by one of Lua's creators
        give more in-depth explanations, with some tips.

        The utf8 library can be used to operate on UTF-8 encoded unicode text
        (such as the text argument given in this function.)

        On Android and iOS, textinput is disabled by default; call
        love.keyboard.setTextInput to enable it.

        Parameters: ~
            • {text} (`string`)
                The UTF-8 encoded unicode text.


-------------------------------------------------------------------------------
                                                 *love2d-docs-love.threaderror*
|love.threaderror|

Callback function triggered when a Thread encounters an error.

Synopses: ~

        |love.threaderror|(`thread`, `errorstr`)

Variants: ~

        |love.threaderror|(`thread`, `errorstr`)

        See function description

        Parameters: ~
            • {thread} (`Thread`) |love2d-docs-Thread|
                The thread which produced the error.
            • {errorstr} (`string`)
                The error message.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.touchmoved*
|love.touchmoved|

Callback function triggered when a touch press moves inside the touch screen.

Synopses: ~

        |love.touchmoved|(`id`, `x`, `y`, `dx`, `dy`, `pressure`)

Variants: ~

        |love.touchmoved|(`id`, `x`, `y`, `dx`, `dy`, `pressure`)

        The identifier is only guaranteed to be unique for the specific touch
        press until love.touchreleased is called with that identifier, at which
        point it may be reused for new touch presses.

        The unofficial Android and iOS ports of LÖVE 0.9.2 reported touch
        positions as normalized values in the range of 1, whereas this API
        reports positions in pixels.

        Parameters: ~
            • {id} (`lightuserdata`)
                The identifier for the touch press.
            • {x} (`number`)
                The x-axis position of the touch inside the window, in pixels.
            • {y} (`number`)
                The y-axis position of the touch inside the window, in pixels.
            • {dx} (`number`)
                The x-axis movement of the touch inside the window, in pixels.
            • {dy} (`number`)
                The y-axis movement of the touch inside the window, in pixels.
            • {pressure} (`number`)
                The amount of pressure being applied. Most touch screens aren't
                pressure sensitive, in which case the pressure will be 1.


-------------------------------------------------------------------------------
                                                *love2d-docs-love.touchpressed*
|love.touchpressed|

Callback function triggered when the touch screen is touched.

Synopses: ~

        |love.touchpressed|(`id`, `x`, `y`, `dx`, `dy`, `pressure`)

Variants: ~

        |love.touchpressed|(`id`, `x`, `y`, `dx`, `dy`, `pressure`)

        The identifier is only guaranteed to be unique for the specific touch
        press until love.touchreleased is called with that identifier, at which
        point it may be reused for new touch presses.

        The unofficial Android and iOS ports of LÖVE 0.9.2 reported touch
        positions as normalized values in the range of 1, whereas this API
        reports positions in pixels.

        Parameters: ~
            • {id} (`lightuserdata`)
                The identifier for the touch press.
            • {x} (`number`)
                The x-axis position of the touch press inside the window, in
                pixels.
            • {y} (`number`)
                The y-axis position of the touch press inside the window, in
                pixels.
            • {dx} (`number`)
                The x-axis movement of the touch press inside the window, in
                pixels. This should always be zero.
            • {dy} (`number`)
                The y-axis movement of the touch press inside the window, in
                pixels. This should always be zero.
            • {pressure} (`number`)
                The amount of pressure being applied. Most touch screens aren't
                pressure sensitive, in which case the pressure will be 1.


-------------------------------------------------------------------------------
                                               *love2d-docs-love.touchreleased*
|love.touchreleased|

Callback function triggered when the touch screen stops being touched.

Synopses: ~

        |love.touchreleased|(`id`, `x`, `y`, `dx`, `dy`, `pressure`)

Variants: ~

        |love.touchreleased|(`id`, `x`, `y`, `dx`, `dy`, `pressure`)

        The identifier is only guaranteed to be unique for the specific touch
        press until love.touchreleased is called with that identifier, at which
        point it may be reused for new touch presses.

        The unofficial Android and iOS ports of LÖVE 0.9.2 reported touch
        positions as normalized values in the range of 1, whereas this API
        reports positions in pixels.

        Parameters: ~
            • {id} (`lightuserdata`)
                The identifier for the touch press.
            • {x} (`number`)
                The x-axis position of the touch inside the window, in pixels.
            • {y} (`number`)
                The y-axis position of the touch inside the window, in pixels.
            • {dx} (`number`)
                The x-axis movement of the touch inside the window, in pixels.
            • {dy} (`number`)
                The y-axis movement of the touch inside the window, in pixels.
            • {pressure} (`number`)
                The amount of pressure being applied. Most touch screens aren't
                pressure sensitive, in which case the pressure will be 1.


-------------------------------------------------------------------------------
                                                      *love2d-docs-love.update*
|love.update|

Callback function used to update the state of the game every frame.

Synopses: ~

        |love.update|(`dt`)

Variants: ~

        |love.update|(`dt`)

        See function description

        Parameters: ~
            • {dt} (`number`)
                Time since the last update in seconds.


-------------------------------------------------------------------------------
                                                     *love2d-docs-love.visible*
|love.visible|

Callback function triggered when window is minimized/hidden or unminimized by
the user.

Synopses: ~

        |love.visible|(`visible`)

Variants: ~

        |love.visible|(`visible`)

        See function description

        Parameters: ~
            • {visible} (`boolean`)
                True if the window is visible, false if it isn't.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.wheelmoved*
|love.wheelmoved|

Callback function triggered when the mouse wheel is moved.

Synopses: ~

        |love.wheelmoved|(`x`, `y`)

Variants: ~

        |love.wheelmoved|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                Amount of horizontal mouse wheel movement. Positive values
                indicate movement to the right.
            • {y} (`number`)
                Amount of vertical mouse wheel movement. Positive values
                indicate upward movement.


-------------------------------------------------------------------------------
                                                       *love2d-docs-love-enums*
The enums of |love|:

    None

-------------------------------------------------------------------------------
                                                   *love2d-docs-love-functions*
The functions of |love|:

    |getVersion|                                  |love2d-docs-love.getVersion|
    |hasDeprecationOutput|              |love2d-docs-love.hasDeprecationOutput|
    |isVersionCompatible|                |love2d-docs-love.isVersionCompatible|
    |setDeprecationOutput|              |love2d-docs-love.setDeprecationOutput|

-------------------------------------------------------------------------------
                                                  *love2d-docs-love.getVersion*
|love.getVersion|

Gets the current running version of LÖVE.

Synopses: ~

        `major`, `minor`, `revision`, `codename` = |love.getVersion|()

Variants: ~

        `major`, `minor`, `revision`, `codename` = |love.getVersion|()

        For LÖVE versions below 0.9.1, the following variables can be used
        instead (and still work in 0.9.2 and newer):

        love._version_major

        love._version_minor

        love._version_revision

        Returns: ~
            1. (`number`)
                The major version of LÖVE, i.e. 0 for version 0.9.1.

            2. (`number`)
                The minor version of LÖVE, i.e. 9 for version 0.9.1.

            3. (`number`)
                The revision version of LÖVE, i.e. 1 for version 0.9.1.

            4. (`string`)
                The codename of the current version, i.e. 'Baby Inspector' for
                version 0.9.1.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.hasDeprecationOutput*
|love.hasDeprecationOutput|

Gets whether LÖVE displays warnings when using deprecated functionality. It is
disabled by default in fused mode, and enabled by default otherwise.

When deprecation output is enabled, the first use of a formally deprecated
LÖVE API will show a message at the bottom of the screen for a short time, and
print the message to the console.

Synopses: ~

        `enabled` = |love.hasDeprecationOutput|()

Variants: ~

        `enabled` = |love.hasDeprecationOutput|()

        See function description

        Return: ~
            (`boolean`)
                Whether deprecation output is enabled.
-------------------------------------------------------------------------------
                                         *love2d-docs-love.isVersionCompatible*
|love.isVersionCompatible|

Gets whether the given version is compatible with the current running version
of LÖVE.

Synopses: ~

    1.  `compatible` = |love.isVersionCompatible|(`version`)
    2.  `compatible` = |love.isVersionCompatible|(`major`, `minor`,
        `revision`)

Variants: ~

    1.  `compatible` = |love.isVersionCompatible|(`version`)

    1.          See function description

        Parameters: ~
            • {version} (`string`)
                The version to check (for example '11.3' or '0.10.2').

        Return: ~
            (`boolean`)
                Whether the given version is compatible with the current
                running version of LÖVE.
    2.  `compatible` = |love.isVersionCompatible|(`major`, `minor`,
        `revision`)

    2.          See function description

        Parameters: ~
            • {major} (`number`)
                The major version to check (for example 11 for 11.3 or 0 for
                0.10.2).
            • {minor} (`number`)
                The minor version to check (for example 3 for 11.3 or 10 for
                0.10.2).
            • {revision} (`number`)
                The revision of version to check (for example 0 for 11.3 or 2
                for 0.10.2).

        Return: ~
            (`boolean`)
                Whether the given version is compatible with the current
                running version of LÖVE.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.setDeprecationOutput*
|love.setDeprecationOutput|

Sets whether LÖVE displays warnings when using deprecated functionality. It is
disabled by default in fused mode, and enabled by default otherwise.

When deprecation output is enabled, the first use of a formally deprecated
LÖVE API will show a message at the bottom of the screen for a short time, and
print the message to the console.

Synopses: ~

        |love.setDeprecationOutput|(`enable`)

Variants: ~

        |love.setDeprecationOutput|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                Whether to enable or disable deprecation output.


-------------------------------------------------------------------------------
                                                       *love2d-docs-love-types*
The types of |love|:

    |Data|                                                   |love2d-docs-Data|
    |Object|                                               |love2d-docs-Object|

-------------------------------------------------------------------------------
                                                             *love2d-docs-Data*
|Data|

The superclass of all data.

                                                *love2d-docs-Data-constructors*
    constructors:

        None

                                                  *love2d-docs-Data-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-Data-subtypes*
    subtypes:

        None

                                                   *love2d-docs-Data-functions*
    functions:

        |clone|                                        |love2d-docs-Data:clone|
        |getFFIPointer|                        |love2d-docs-Data:getFFIPointer|
        |getPointer|                              |love2d-docs-Data:getPointer|
        |getSize|                                    |love2d-docs-Data:getSize|
        |getString|                                |love2d-docs-Data:getString|

-------------------------------------------------------------------------------
                                                       *love2d-docs-Data:clone*
|Data:clone|

Creates a new copy of the Data object.

Synopses: ~

        `clone` = |Data:clone|()

Variants: ~

        `clone` = |Data:clone|()

        See function description

        Return: ~
            (`Data`) |love2d-docs-Data|
                The new copy.
-------------------------------------------------------------------------------
                                               *love2d-docs-Data:getFFIPointer*
|Data:getFFIPointer|

Gets an FFI pointer to the Data.

This function should be preferred instead of Data:getPointer because the latter
uses light userdata which can't store more all possible memory addresses on
some new ARM64 architectures, when LuaJIT is used.

Synopses: ~

        `pointer` = |Data:getFFIPointer|()

Variants: ~

        `pointer` = |Data:getFFIPointer|()

        See function description

        Return: ~
            (`cdata`)
                A raw void* pointer to the Data, or nil if FFI is unavailable.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Data:getPointer*
|Data:getPointer|

Gets a pointer to the Data. Can be used with libraries such as LuaJIT's FFI.

Synopses: ~

        `pointer` = |Data:getPointer|()

Variants: ~

        `pointer` = |Data:getPointer|()

        See function description

        Return: ~
            (`lightuserdata`)
                A raw pointer to the Data.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Data:getSize*
|Data:getSize|

Gets the Data's size in bytes.

Synopses: ~

        `size` = |Data:getSize|()

Variants: ~

        `size` = |Data:getSize|()

        See function description

        Return: ~
            (`number`)
                The size of the Data in bytes.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Data:getString*
|Data:getString|

Gets the full Data as a string.

Synopses: ~

        `data` = |Data:getString|()

Variants: ~

        `data` = |Data:getString|()

        See function description

        Return: ~
            (`string`)
                The raw data.

-------------------------------------------------------------------------------
                                                           *love2d-docs-Object*
|Object|

The superclass of all LÖVE types.

                                              *love2d-docs-Object-constructors*
    constructors:

        None

                                                *love2d-docs-Object-supertypes*
    supertypes:

        None

                                                  *love2d-docs-Object-subtypes*
    subtypes:

        None

                                                 *love2d-docs-Object-functions*
    functions:

        |release|                                  |love2d-docs-Object:release|
        |type|                                        |love2d-docs-Object:type|
        |typeOf|                                    |love2d-docs-Object:typeOf|

-------------------------------------------------------------------------------
                                                   *love2d-docs-Object:release*
|Object:release|

Destroys the object's Lua reference. The object will be completely deleted if
it's not referenced by any other LÖVE object or thread.

This method can be used to immediately clean up resources without waiting for
Lua's garbage collector.

Synopses: ~

        `success` = |Object:release|()

Variants: ~

        `success` = |Object:release|()

        See function description

        Return: ~
            (`boolean`)
                True if the object was released by this call, false if it had
                been previously released.
-------------------------------------------------------------------------------
                                                      *love2d-docs-Object:type*
|Object:type|

Gets the type of the object as a string.

Synopses: ~

        `type` = |Object:type|()

Variants: ~

        `type` = |Object:type|()

        See function description

        Return: ~
            (`string`)
                The type as a string.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Object:typeOf*
|Object:typeOf|

Checks whether an object is of a certain type. If the object has the type with
the specified name in its hierarchy, this function will return true.

Synopses: ~

        `b` = |Object:typeOf|(`name`)

Variants: ~

        `b` = |Object:typeOf|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the type to check for.

        Return: ~
            (`boolean`)
                True if the object is of the specified type, false otherwise.

===============================================================================
                                                       *love2d-docs-love.audio*
|love.audio|

Provides an interface to create noise with the user's speakers.

    |callbacks|                              |love2d-docs-love.audio-callbacks|
    |enums|                                      |love2d-docs-love.audio-enums|
    |functions|                              |love2d-docs-love.audio-functions|
    |types|                                      |love2d-docs-love.audio-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio-callbacks*
The callbacks of |love.audio|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.audio-enums*
The enums of |love.audio|:

    |DistanceModel|                                 |love2d-docs-DistanceModel|
    |EffectType|                                       |love2d-docs-EffectType|
    |EffectWaveform|                               |love2d-docs-EffectWaveform|
    |FilterType|                                       |love2d-docs-FilterType|
    |SourceType|                                       |love2d-docs-SourceType|
    |TimeUnit|                                           |love2d-docs-TimeUnit|

-------------------------------------------------------------------------------
                                                    *love2d-docs-DistanceModel*
|DistanceModel|

The different distance models.

Extended information can be found in the chapter "3.4. Attenuation By Distance"
of the OpenAL 1.1 specification.

    constants: ~

        `none`: string

            Sources do not get attenuated.

        `inverse`: string

            Inverse distance attenuation.

        `inverseclamped`: string

            Inverse distance attenuation. Gain is clamped. In version 0.9.2 and
            older this is named '''inverse clamped'''.

        `linear`: string

            Linear attenuation.

        `linearclamped`: string

            Linear attenuation. Gain is clamped. In version 0.9.2 and older
            this is named '''linear clamped'''.

        `exponent`: string

            Exponential attenuation.

        `exponentclamped`: string

            Exponential attenuation. Gain is clamped. In version 0.9.2 and
            older this is named '''exponent clamped'''.

-------------------------------------------------------------------------------
                                                       *love2d-docs-EffectType*
|EffectType|

The different types of effects supported by love.audio.setEffect.

    constants: ~

        `chorus`: string

            Plays multiple copies of the sound with slight pitch and time
            variation. Used to make sounds sound "fuller" or "thicker".

        `compressor`: string

            Decreases the dynamic range of the sound, making the loud and quiet
            parts closer in volume, producing a more uniform amplitude
            throughout time.

        `distortion`: string

            Alters the sound by amplifying it until it clips, shearing off
            parts of the signal, leading to a compressed and distorted sound.

        `echo`: string

            Decaying feedback based effect, on the order of seconds. Also known
            as delay; causes the sound to repeat at regular intervals at a
            decreasing volume.

        `equalizer`: string

            Adjust the frequency components of the sound using a 4-band
            (low-shelf, two band-pass and a high-shelf) equalizer.

        `flanger`: string

            Plays two copies of the sound; while varying the phase, or
            equivalently delaying one of them, by amounts on the order of
            milliseconds, resulting in phasing sounds.

        `reverb`: string

            Decaying feedback based effect, on the order of milliseconds. Used
            to simulate the reflection off of the surroundings.

        `ringmodulator`: string

            An implementation of amplitude modulation; multiplies the source
            signal with a simple waveform, to produce either volume changes, or
            inharmonic overtones.

-------------------------------------------------------------------------------
                                                   *love2d-docs-EffectWaveform*
|EffectWaveform|

The different types of waveforms that can be used with the '''ringmodulator'''
EffectType.

    constants: ~

        `sawtooth`: string

            A sawtooth wave, also known as a ramp wave. Named for its linear
            rise, and (near-)instantaneous fall along time.

        `sine`: string

            A sine wave. Follows a trigonometric sine function.

        `square`: string

            A square wave. Switches between high and low states
            (near-)instantaneously.

        `triangle`: string

            A triangle wave. Follows a linear rise and fall that repeats
            periodically.

-------------------------------------------------------------------------------
                                                       *love2d-docs-FilterType*
|FilterType|

Types of filters for Sources.

    constants: ~

        `lowpass`: string

            Low-pass filter. High frequency sounds are attenuated.

        `highpass`: string

            High-pass filter. Low frequency sounds are attenuated.

        `bandpass`: string

            Band-pass filter. Both high and low frequency sounds are attenuated
            based on the given parameters.

-------------------------------------------------------------------------------
                                                       *love2d-docs-SourceType*
|SourceType|

Types of audio sources.

A good rule of thumb is to use stream for music files and static for all short
sound effects. Basically, you want to avoid loading large files into memory at
once.

    constants: ~

        `static`: string

            The whole audio is decoded.

        `stream`: string

            The audio is decoded in chunks when needed.

        `queue`: string

            The audio must be manually queued by the user.

-------------------------------------------------------------------------------
                                                         *love2d-docs-TimeUnit*
|TimeUnit|

Units that represent time.

    constants: ~

        `seconds`: string

            Regular seconds.

        `samples`: string

            Audio samples.

-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio-functions*
The functions of |love.audio|:

    |getActiveEffects|                |love2d-docs-love.audio.getActiveEffects|
    |getActiveSourceCount|        |love2d-docs-love.audio.getActiveSourceCount|
    |getDistanceModel|                |love2d-docs-love.audio.getDistanceModel|
    |getDopplerScale|                  |love2d-docs-love.audio.getDopplerScale|
    |getEffect|                              |love2d-docs-love.audio.getEffect|
    |getMaxSceneEffects|            |love2d-docs-love.audio.getMaxSceneEffects|
    |getMaxSourceEffects|          |love2d-docs-love.audio.getMaxSourceEffects|
    |getOrientation|                    |love2d-docs-love.audio.getOrientation|
    |getPosition|                          |love2d-docs-love.audio.getPosition|
    |getRecordingDevices|          |love2d-docs-love.audio.getRecordingDevices|
    |getVelocity|                          |love2d-docs-love.audio.getVelocity|
    |getVolume|                              |love2d-docs-love.audio.getVolume|
    |isEffectsSupported|            |love2d-docs-love.audio.isEffectsSupported|
    |newQueueableSource|            |love2d-docs-love.audio.newQueueableSource|
    |newSource|                              |love2d-docs-love.audio.newSource|
    |pause|                                      |love2d-docs-love.audio.pause|
    |play|                                        |love2d-docs-love.audio.play|
    |setDistanceModel|                |love2d-docs-love.audio.setDistanceModel|
    |setDopplerScale|                  |love2d-docs-love.audio.setDopplerScale|
    |setEffect|                              |love2d-docs-love.audio.setEffect|
    |setMixWithSystem|                |love2d-docs-love.audio.setMixWithSystem|
    |setOrientation|                    |love2d-docs-love.audio.setOrientation|
    |setPosition|                          |love2d-docs-love.audio.setPosition|
    |setVelocity|                          |love2d-docs-love.audio.setVelocity|
    |setVolume|                              |love2d-docs-love.audio.setVolume|
    |stop|                                        |love2d-docs-love.audio.stop|

-------------------------------------------------------------------------------
                                      *love2d-docs-love.audio.getActiveEffects*
|love.audio.getActiveEffects|

Gets a list of the names of the currently enabled effects.

Synopses: ~

        `effects` = |love.audio.getActiveEffects|()

Variants: ~

        `effects` = |love.audio.getActiveEffects|()

        See function description

        Return: ~
            (`table`)
                The list of the names of the currently enabled effects.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.audio.getActiveSourceCount*
|love.audio.getActiveSourceCount|

Gets the current number of simultaneously playing sources.

Synopses: ~

        `count` = |love.audio.getActiveSourceCount|()

Variants: ~

        `count` = |love.audio.getActiveSourceCount|()

        See function description

        Return: ~
            (`number`)
                The current number of simultaneously playing sources.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.audio.getDistanceModel*
|love.audio.getDistanceModel|

Returns the distance attenuation model.

Synopses: ~

        `model` = |love.audio.getDistanceModel|()

Variants: ~

        `model` = |love.audio.getDistanceModel|()

        See function description

        Return: ~
            (`DistanceModel`) |love2d-docs-DistanceModel|
                The current distance model. The default is 'inverseclamped'.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.audio.getDopplerScale*
|love.audio.getDopplerScale|

Gets the current global scale factor for velocity-based doppler effects.

Synopses: ~

        `scale` = |love.audio.getDopplerScale|()

Variants: ~

        `scale` = |love.audio.getDopplerScale|()

        See function description

        Return: ~
            (`number`)
                The current doppler scale factor.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio.getEffect*
|love.audio.getEffect|

Gets the settings associated with an effect.

Synopses: ~

        `settings` = |love.audio.getEffect|(`name`)

Variants: ~

        `settings` = |love.audio.getEffect|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the effect.

        Return: ~
            (`table`)
                The settings associated with the effect.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.audio.getMaxSceneEffects*
|love.audio.getMaxSceneEffects|

Gets the maximum number of active effects supported by the system.

Synopses: ~

        `maximum` = |love.audio.getMaxSceneEffects|()

Variants: ~

        `maximum` = |love.audio.getMaxSceneEffects|()

        See function description

        Return: ~
            (`number`)
                The maximum number of active effects.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.audio.getMaxSourceEffects*
|love.audio.getMaxSourceEffects|

Gets the maximum number of active Effects in a single Source object, that the
system can support.

Synopses: ~

        `maximum` = |love.audio.getMaxSourceEffects|()

Variants: ~

        `maximum` = |love.audio.getMaxSourceEffects|()

        This function return 0 for system that doesn't support audio effects.

        Return: ~
            (`number`)
                The maximum number of active Effects per Source.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.audio.getOrientation*
|love.audio.getOrientation|

Returns the orientation of the listener.

Synopses: ~

        `fx`, `fy`, `fz`, `ux`, `uy`, `uz` =
        |love.audio.getOrientation|()

Variants: ~

        `fx`, `fy`, `fz`, `ux`, `uy`, `uz` =
        |love.audio.getOrientation|()

        See function description

        Returns: ~
            1. (`number`)
                Forward x of the listener orientation.

            2. (`number`)
                Forward y of the listener orientation.

            3. (`number`)
                Forward z of the listener orientation.

            4. (`number`)
                Up x of the listener orientation.

            5. (`number`)
                Up y of the listener orientation.

            6. (`number`)
                Up z of the listener orientation.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.audio.getPosition*
|love.audio.getPosition|

Returns the position of the listener. Please note that positional audio only
works for mono (i.e. non-stereo) sources.

Synopses: ~

        `x`, `y`, `z` = |love.audio.getPosition|()

Variants: ~

        `x`, `y`, `z` = |love.audio.getPosition|()

        See function description

        Returns: ~
            1. (`number`)
                The X position of the listener.

            2. (`number`)
                The Y position of the listener.

            3. (`number`)
                The Z position of the listener.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.audio.getRecordingDevices*
|love.audio.getRecordingDevices|

Gets a list of RecordingDevices on the system.

The first device in the list is the user's default recording device. The list
may be empty if there are no microphones connected to the system.

Audio recording is currently not supported on iOS.

Synopses: ~

        `devices` = |love.audio.getRecordingDevices|()

Variants: ~

        `devices` = |love.audio.getRecordingDevices|()

        Audio recording for Android is supported since 11.3. However, it's not
        supported when APK from Play Store is used.

        Return: ~
            (`table`)
                The list of connected recording devices.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.audio.getVelocity*
|love.audio.getVelocity|

Returns the velocity of the listener.

Synopses: ~

        `x`, `y`, `z` = |love.audio.getVelocity|()

Variants: ~

        `x`, `y`, `z` = |love.audio.getVelocity|()

        See function description

        Returns: ~
            1. (`number`)
                The X velocity of the listener.

            2. (`number`)
                The Y velocity of the listener.

            3. (`number`)
                The Z velocity of the listener.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio.getVolume*
|love.audio.getVolume|

Returns the master volume.

Synopses: ~

        `volume` = |love.audio.getVolume|()

Variants: ~

        `volume` = |love.audio.getVolume|()

        See function description

        Return: ~
            (`number`)
                The current master volume
-------------------------------------------------------------------------------
                                    *love2d-docs-love.audio.isEffectsSupported*
|love.audio.isEffectsSupported|

Gets whether audio effects are supported in the system.

Synopses: ~

        `supported` = |love.audio.isEffectsSupported|()

Variants: ~

        `supported` = |love.audio.isEffectsSupported|()

        Older Linux distributions that ship with older OpenAL library may not
        support audio effects. Furthermore, iOS doesn't

        support audio effects at all.

        Return: ~
            (`boolean`)
                True if effects are supported, false otherwise.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.audio.newQueueableSource*
|love.audio.newQueueableSource|

Creates a new Source usable for real-time generated sound playback with
Source:queue.

Synopses: ~

        `source` = |love.audio.newQueueableSource|(`samplerate`,
        `bitdepth`, `channels`, `buffercount`)

Variants: ~

        `source` = |love.audio.newQueueableSource|(`samplerate`,
        `bitdepth`, `channels`, `buffercount`)

        The sample rate, bit depth, and channel count of any SoundData used
        with Source:queue must match the parameters given to this constructor.

        Parameters: ~
            • {samplerate} (`number`)
                Number of samples per second when playing.
            • {bitdepth} (`number`)
                Bits per sample (8 or 16).
            • {channels} (`number`)
                1 for mono or 2 for stereo.
            • {buffercount} (`number`) (default: `0`)
                The number of buffers that can be queued up at any given time
                with Source:queue. Cannot be greater than 64. A sensible
                default (~8) is chosen if no value is specified.

        Return: ~
            (`Source`) |love2d-docs-Source|
                The new Source usable with Source:queue.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio.newSource*
|love.audio.newSource|

Creates a new Source from a filepath, File, Decoder or SoundData.

Sources created from SoundData are always static.

Synopses: ~

    1.  `source` = |love.audio.newSource|(`filename`, `type`)
    2.  `source` = |love.audio.newSource|(`file`, `type`)
    3.  `source` = |love.audio.newSource|(`decoder`, `type`)
    4.  `source` = |love.audio.newSource|(`data`, `type`)
    5.  `source` = |love.audio.newSource|(`data`)

Variants: ~

    1.  `source` = |love.audio.newSource|(`filename`, `type`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filepath to the audio file.
            • {type} (`SourceType`) |love2d-docs-SourceType|
                Streaming or static source.

        Return: ~
            (`Source`) |love2d-docs-Source|
                A new Source that can play the specified audio.
    2.  `source` = |love.audio.newSource|(`file`, `type`)

    2.          See function description

        Parameters: ~
            • {file} (`File`) |love2d-docs-File|
                A File pointing to an audio file.
            • {type} (`SourceType`) |love2d-docs-SourceType|
                Streaming or static source.

        Return: ~
            (`Source`) |love2d-docs-Source|
                A new Source that can play the specified audio.
    3.  `source` = |love.audio.newSource|(`decoder`, `type`)

    3.          See function description

        Parameters: ~
            • {decoder} (`Decoder`) |love2d-docs-Decoder|
                The Decoder to create a Source from.
            • {type} (`SourceType`) |love2d-docs-SourceType|
                Streaming or static source.

        Return: ~
            (`Source`) |love2d-docs-Source|
                A new Source that can play the specified audio.
    4.  `source` = |love.audio.newSource|(`data`, `type`)

    4.          See function description

        Parameters: ~
            • {data} (`FileData`) |love2d-docs-FileData|
                The FileData to create a Source from.
            • {type} (`SourceType`) |love2d-docs-SourceType|
                Streaming or static source.

        Return: ~
            (`Source`) |love2d-docs-Source|
                A new Source that can play the specified audio.
    5.  `source` = |love.audio.newSource|(`data`)

    5.          See function description

        Parameters: ~
            • {data} (`SoundData`) |love2d-docs-SoundData|
                The SoundData to create a Source from.

        Return: ~
            (`Source`) |love2d-docs-Source|
                A new Source that can play the specified audio. The SourceType
                of the returned audio is 'static'.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.audio.pause*
|love.audio.pause|

Pauses specific or all currently played Sources.

Synopses: ~

    1.  `Sources` = |love.audio.pause|()
    2.  |love.audio.pause|(`source`, `...`)
    3.  |love.audio.pause|(`sources`)

Variants: ~

    1.  `Sources` = |love.audio.pause|()

    1.          Pauses all currently active Sources and returns them.

        Return: ~
            (`table`)
                A table containing a list of Sources that were paused by this
                call.
    2.  |love.audio.pause|(`source`, `...`)

    2.          Pauses the given Sources.

        Parameters: ~
            • {source} (`Source`) |love2d-docs-Source|
                The first Source to pause.
            • {...} (`Source`) |love2d-docs-Source|
                Additional Sources to pause.


    3.  |love.audio.pause|(`sources`)

    3.          Pauses the given Sources.

        Parameters: ~
            • {sources} (`table`)
                A table containing a list of Sources to pause.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.audio.play*
|love.audio.play|

Plays the specified Source.

Synopses: ~

    1.  |love.audio.play|(`source`)
    2.  |love.audio.play|(`sources`)
    3.  |love.audio.play|(`source1`, `source2`, `...`)

Variants: ~

    1.  |love.audio.play|(`source`)

    1.          See function description

        Parameters: ~
            • {source} (`Source`) |love2d-docs-Source|
                The Source to play.


    2.  |love.audio.play|(`sources`)

    2.          Starts playing multiple Sources simultaneously.

        Parameters: ~
            • {sources} (`table`)
                Table containing a list of Sources to play.


    3.  |love.audio.play|(`source1`, `source2`, `...`)

    3.          Starts playing multiple Sources simultaneously.

        Parameters: ~
            • {source1} (`Source`) |love2d-docs-Source|
                The first Source to play.
            • {source2} (`Source`) |love2d-docs-Source|
                The second Source to play.
            • {...} (`Source`) |love2d-docs-Source|
                Additional Sources to play.


-------------------------------------------------------------------------------
                                      *love2d-docs-love.audio.setDistanceModel*
|love.audio.setDistanceModel|

Sets the distance attenuation model.

Synopses: ~

        |love.audio.setDistanceModel|(`model`)

Variants: ~

        |love.audio.setDistanceModel|(`model`)

        See function description

        Parameters: ~
            • {model} (`DistanceModel`) |love2d-docs-DistanceModel|
                The new distance model.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.audio.setDopplerScale*
|love.audio.setDopplerScale|

Sets a global scale factor for velocity-based doppler effects. The default
scale value is 1.

Synopses: ~

        |love.audio.setDopplerScale|(`scale`)

Variants: ~

        |love.audio.setDopplerScale|(`scale`)

        See function description

        Parameters: ~
            • {scale} (`number`)
                The new doppler scale factor. The scale must be greater than 0.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio.setEffect*
|love.audio.setEffect|

Defines an effect that can be applied to a Source.

Not all system supports audio effects. Use love.audio.isEffectsSupported to
check.

Synopses: ~

    1.  `success` = |love.audio.setEffect|(`name`, `settings`)
    2.  `success` = |love.audio.setEffect|(`name`, `enabled`)

Variants: ~

    1.  `success` = |love.audio.setEffect|(`name`, `settings`)

    1.          See function description

        Parameters: ~
            • {name} (`string`)
                The name of the effect.
            • {settings} (`table`)
                The settings to use for this effect, with the following fields:
                • {type} (`EffectType`) |love2d-docs-EffectType|
                    The type of effect to use.
                • {volume} (`number`)
                    The volume of the effect.
                • {...} (`number`)
                    Effect-specific settings. See EffectType for available
                    effects and their corresponding settings.

        Return: ~
            (`boolean`)
                Whether the effect was successfully created.
    2.  `success` = |love.audio.setEffect|(`name`, `enabled`)

    2.          See function description

        Parameters: ~
            • {name} (`string`)
                The name of the effect.
            • {enabled} (`boolean`) (default: `true`)
                If false and the given effect name was previously set, disables
                the effect.

        Return: ~
            (`boolean`)
                Whether the effect was successfully disabled.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.audio.setMixWithSystem*
|love.audio.setMixWithSystem|

Sets whether the system should mix the audio with the system's audio.

Synopses: ~

        `success` = |love.audio.setMixWithSystem|(`mix`)

Variants: ~

        `success` = |love.audio.setMixWithSystem|(`mix`)

        See function description

        Parameters: ~
            • {mix} (`boolean`)
                True to enable mixing, false to disable it.

        Return: ~
            (`boolean`)
                True if the change succeeded, false otherwise.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.audio.setOrientation*
|love.audio.setOrientation|

Sets the orientation of the listener.

Synopses: ~

        |love.audio.setOrientation|(`fx, fy, fz`, `ux, uy, uz`)

Variants: ~

        |love.audio.setOrientation|(`fx, fy, fz`, `ux, uy, uz`)

        See function description

        Parameters: ~
            • {fx, fy, fz} (`number`)
                Forward vector of the listener orientation.
            • {ux, uy, uz} (`number`)
                Up vector of the listener orientation.


-------------------------------------------------------------------------------
                                           *love2d-docs-love.audio.setPosition*
|love.audio.setPosition|

Sets the position of the listener, which determines how sounds play.

Synopses: ~

        |love.audio.setPosition|(`x`, `y`, `z`)

Variants: ~

        |love.audio.setPosition|(`x`, `y`, `z`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position of the listener.
            • {y} (`number`)
                The y position of the listener.
            • {z} (`number`)
                The z position of the listener.


-------------------------------------------------------------------------------
                                           *love2d-docs-love.audio.setVelocity*
|love.audio.setVelocity|

Sets the velocity of the listener.

Synopses: ~

        |love.audio.setVelocity|(`x`, `y`, `z`)

Variants: ~

        |love.audio.setVelocity|(`x`, `y`, `z`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The X velocity of the listener.
            • {y} (`number`)
                The Y velocity of the listener.
            • {z} (`number`)
                The Z velocity of the listener.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.audio.setVolume*
|love.audio.setVolume|

Sets the master volume.

Synopses: ~

        |love.audio.setVolume|(`volume`)

Variants: ~

        |love.audio.setVolume|(`volume`)

        See function description

        Parameters: ~
            • {volume} (`number`)
                1.0 is max and 0.0 is off.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.audio.stop*
|love.audio.stop|

Stops currently played sources.

Synopses: ~

    1.  |love.audio.stop|()
    2.  |love.audio.stop|(`source`)
    3.  |love.audio.stop|(`source1`, `source2`, `...`)
    4.  |love.audio.stop|(`sources`)

Variants: ~

    1.  |love.audio.stop|()

    1.          This function will stop all currently active sources.


    2.  |love.audio.stop|(`source`)

    2.          This function will only stop the specified source.

        Parameters: ~
            • {source} (`Source`) |love2d-docs-Source|
                The source on which to stop the playback.


    3.  |love.audio.stop|(`source1`, `source2`, `...`)

    3.          Simultaneously stops all given Sources.

        Parameters: ~
            • {source1} (`Source`) |love2d-docs-Source|
                The first Source to stop.
            • {source2} (`Source`) |love2d-docs-Source|
                The second Source to stop.
            • {...} (`Source`) |love2d-docs-Source|
                Additional Sources to stop.


    4.  |love.audio.stop|(`sources`)

    4.          Simultaneously stops all given Sources.

        Parameters: ~
            • {sources} (`table`)
                A table containing a list of Sources to stop.


-------------------------------------------------------------------------------
                                                 *love2d-docs-love.audio-types*
The types of |love.audio|:

    |RecordingDevice|                             |love2d-docs-RecordingDevice|
    |Source|                                               |love2d-docs-Source|

-------------------------------------------------------------------------------
                                                  *love2d-docs-RecordingDevice*
|RecordingDevice|

Represents an audio input device capable of recording sounds.

                                     *love2d-docs-RecordingDevice-constructors*
    constructors:

        None

                                       *love2d-docs-RecordingDevice-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                         *love2d-docs-RecordingDevice-subtypes*
    subtypes:

        None

                                        *love2d-docs-RecordingDevice-functions*
    functions:

        |getBitDepth|                 |love2d-docs-RecordingDevice:getBitDepth|
        |getChannelCount|         |love2d-docs-RecordingDevice:getChannelCount|
        |getData|                         |love2d-docs-RecordingDevice:getData|
        |getName|                         |love2d-docs-RecordingDevice:getName|
        |getSampleCount|           |love2d-docs-RecordingDevice:getSampleCount|
        |getSampleRate|             |love2d-docs-RecordingDevice:getSampleRate|
        |isRecording|                 |love2d-docs-RecordingDevice:isRecording|
        |start|                             |love2d-docs-RecordingDevice:start|
        |stop|                               |love2d-docs-RecordingDevice:stop|

-------------------------------------------------------------------------------
                                      *love2d-docs-RecordingDevice:getBitDepth*
|RecordingDevice:getBitDepth|

Gets the number of bits per sample in the data currently being recorded.

Synopses: ~

        `bits` = |RecordingDevice:getBitDepth|()

Variants: ~

        `bits` = |RecordingDevice:getBitDepth|()

        See function description

        Return: ~
            (`number`)
                The number of bits per sample in the data that's currently
                being recorded.
-------------------------------------------------------------------------------
                                  *love2d-docs-RecordingDevice:getChannelCount*
|RecordingDevice:getChannelCount|

Gets the number of channels currently being recorded (mono or stereo).

Synopses: ~

        `channels` = |RecordingDevice:getChannelCount|()

Variants: ~

        `channels` = |RecordingDevice:getChannelCount|()

        See function description

        Return: ~
            (`number`)
                The number of channels being recorded (1 for mono, 2 for
                stereo).
-------------------------------------------------------------------------------
                                          *love2d-docs-RecordingDevice:getData*
|RecordingDevice:getData|

Gets all recorded audio SoundData stored in the device's internal ring buffer.

The internal ring buffer is cleared when this function is called, so calling it
again will only get audio recorded after the previous call. If the device's
internal ring buffer completely fills up before getData is called, the oldest
data that doesn't fit into the buffer will be lost.

Synopses: ~

        `data` = |RecordingDevice:getData|()

Variants: ~

        `data` = |RecordingDevice:getData|()

        See function description

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                The recorded audio data, or nil if the device isn't recording.
-------------------------------------------------------------------------------
                                          *love2d-docs-RecordingDevice:getName*
|RecordingDevice:getName|

Gets the name of the recording device.

Synopses: ~

        `name` = |RecordingDevice:getName|()

Variants: ~

        `name` = |RecordingDevice:getName|()

        See function description

        Return: ~
            (`string`)
                The name of the device.
-------------------------------------------------------------------------------
                                   *love2d-docs-RecordingDevice:getSampleCount*
|RecordingDevice:getSampleCount|

Gets the number of currently recorded samples.

Synopses: ~

        `samples` = |RecordingDevice:getSampleCount|()

Variants: ~

        `samples` = |RecordingDevice:getSampleCount|()

        See function description

        Return: ~
            (`number`)
                The number of samples that have been recorded so far.
-------------------------------------------------------------------------------
                                    *love2d-docs-RecordingDevice:getSampleRate*
|RecordingDevice:getSampleRate|

Gets the number of samples per second currently being recorded.

Synopses: ~

        `rate` = |RecordingDevice:getSampleRate|()

Variants: ~

        `rate` = |RecordingDevice:getSampleRate|()

        See function description

        Return: ~
            (`number`)
                The number of samples being recorded per second (sample rate).
-------------------------------------------------------------------------------
                                      *love2d-docs-RecordingDevice:isRecording*
|RecordingDevice:isRecording|

Gets whether the device is currently recording.

Synopses: ~

        `recording` = |RecordingDevice:isRecording|()

Variants: ~

        `recording` = |RecordingDevice:isRecording|()

        See function description

        Return: ~
            (`boolean`)
                True if the recording, false otherwise.
-------------------------------------------------------------------------------
                                            *love2d-docs-RecordingDevice:start*
|RecordingDevice:start|

Begins recording audio using this device.

Synopses: ~

        `success` = |RecordingDevice:start|(`samplecount`,
        `samplerate`, `bitdepth`, `channels`)

Variants: ~

        `success` = |RecordingDevice:start|(`samplecount`,
        `samplerate`, `bitdepth`, `channels`)

        A ring buffer is used internally to store recorded data before
        RecordingDevice:getData or RecordingDevice:stop are called – the
        former clears the buffer. If the buffer completely fills up before
        getData or stop are called, the oldest data that doesn't fit into the
        buffer will be lost.

        Parameters: ~
            • {samplecount} (`number`)
                The maximum number of samples to store in an internal ring
                buffer when recording. RecordingDevice:getData clears the
                internal buffer when called.
            • {samplerate} (`number`) (default: `8000`)
                The number of samples per second to store when recording.
            • {bitdepth} (`number`) (default: `16`)
                The number of bits per sample.
            • {channels} (`number`) (default: `1`)
                Whether to record in mono or stereo. Most microphones don't
                support more than 1 channel.

        Return: ~
            (`boolean`)
                True if the device successfully began recording using the
                specified parameters, false if not.
-------------------------------------------------------------------------------
                                             *love2d-docs-RecordingDevice:stop*
|RecordingDevice:stop|

Stops recording audio from this device. Any sound data currently in the
device's buffer will be returned.

Synopses: ~

        `data` = |RecordingDevice:stop|()

Variants: ~

        `data` = |RecordingDevice:stop|()

        See function description

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                The sound data currently in the device's buffer, or nil if the
                device wasn't recording.

-------------------------------------------------------------------------------
                                                           *love2d-docs-Source*
|Source|

A Source represents audio you can play back.

You can do interesting things with Sources, like set the volume, pitch, and its
position relative to the listener. Please note that positional audio only works
for mono (i.e. non-stereo) sources.

The Source controls (play/pause/stop) act according to the following state
table.

                                              *love2d-docs-Source-constructors*
    constructors:

        |newQueueableSource|                   |love2d-docs-newQueueableSource|
        |newSource|                                     |love2d-docs-newSource|

                                                *love2d-docs-Source-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                  *love2d-docs-Source-subtypes*
    subtypes:

        None

                                                 *love2d-docs-Source-functions*
    functions:

        |clone|                                      |love2d-docs-Source:clone|
        |getActiveEffects|                |love2d-docs-Source:getActiveEffects|
        |getAirAbsorption|                |love2d-docs-Source:getAirAbsorption|
        |getAttenuationDistances|  |love2d-docs-Source:getAttenuationDistances|
        |getChannelCount|                  |love2d-docs-Source:getChannelCount|
        |getCone|                                  |love2d-docs-Source:getCone|
        |getDirection|                        |love2d-docs-Source:getDirection|
        |getDuration|                          |love2d-docs-Source:getDuration|
        |getEffect|                              |love2d-docs-Source:getEffect|
        |getFilter|                              |love2d-docs-Source:getFilter|
        |getFreeBufferCount|            |love2d-docs-Source:getFreeBufferCount|
        |getPitch|                                |love2d-docs-Source:getPitch|
        |getPosition|                          |love2d-docs-Source:getPosition|
        |getRolloff|                            |love2d-docs-Source:getRolloff|
        |getType|                                  |love2d-docs-Source:getType|
        |getVelocity|                          |love2d-docs-Source:getVelocity|
        |getVolume|                              |love2d-docs-Source:getVolume|
        |getVolumeLimits|                  |love2d-docs-Source:getVolumeLimits|
        |isLooping|                              |love2d-docs-Source:isLooping|
        |isPlaying|                              |love2d-docs-Source:isPlaying|
        |isRelative|                            |love2d-docs-Source:isRelative|
        |pause|                                      |love2d-docs-Source:pause|
        |play|                                        |love2d-docs-Source:play|
        |queue|                                      |love2d-docs-Source:queue|
        |seek|                                        |love2d-docs-Source:seek|
        |setAirAbsorption|                |love2d-docs-Source:setAirAbsorption|
        |setAttenuationDistances|  |love2d-docs-Source:setAttenuationDistances|
        |setCone|                                  |love2d-docs-Source:setCone|
        |setDirection|                        |love2d-docs-Source:setDirection|
        |setEffect|                              |love2d-docs-Source:setEffect|
        |setFilter|                              |love2d-docs-Source:setFilter|
        |setLooping|                            |love2d-docs-Source:setLooping|
        |setPitch|                                |love2d-docs-Source:setPitch|
        |setPosition|                          |love2d-docs-Source:setPosition|
        |setRelative|                          |love2d-docs-Source:setRelative|
        |setRolloff|                            |love2d-docs-Source:setRolloff|
        |setVelocity|                          |love2d-docs-Source:setVelocity|
        |setVolume|                              |love2d-docs-Source:setVolume|
        |setVolumeLimits|                  |love2d-docs-Source:setVolumeLimits|
        |stop|                                        |love2d-docs-Source:stop|
        |tell|                                        |love2d-docs-Source:tell|

-------------------------------------------------------------------------------
                                                     *love2d-docs-Source:clone*
|Source:clone|

Creates an identical copy of the Source in the stopped state.

Static Sources will use significantly less memory and take much less time to be
created if Source:clone is used to create them instead of love.audio.newSource,
so this method should be preferred when making multiple Sources which play the
same sound.

Synopses: ~

        `source` = |Source:clone|()

Variants: ~

        `source` = |Source:clone|()

        Cloned Sources inherit all the set-able state of the original Source,
        but they are initialized stopped.

        Return: ~
            (`Source`) |love2d-docs-Source|
                The new identical copy of this Source.
-------------------------------------------------------------------------------
                                          *love2d-docs-Source:getActiveEffects*
|Source:getActiveEffects|

Gets a list of the Source's active effect names.

Synopses: ~

        `effects` = |Source:getActiveEffects|()

Variants: ~

        `effects` = |Source:getActiveEffects|()

        See function description

        Return: ~
            (`table`)
                A list of the source's active effect names.
-------------------------------------------------------------------------------
                                          *love2d-docs-Source:getAirAbsorption*
|Source:getAirAbsorption|

Gets the amount of air absorption applied to the Source.

By default the value is set to 0 which means that air absorption effects are
disabled. A value of 1 will apply high frequency attenuation to the Source at a
rate of 0.05 dB per meter.

Synopses: ~

        `amount` = |Source:getAirAbsorption|()

Variants: ~

        `amount` = |Source:getAirAbsorption|()

        Audio air absorption functionality is not supported on iOS.

        Return: ~
            (`number`)
                The amount of air absorption applied to the Source.
-------------------------------------------------------------------------------
                                   *love2d-docs-Source:getAttenuationDistances*
|Source:getAttenuationDistances|

Gets the reference and maximum attenuation distances of the Source. The values,
combined with the current DistanceModel, affect how the Source's volume
attenuates based on distance from the listener.

Synopses: ~

        `ref`, `max` = |Source:getAttenuationDistances|()

Variants: ~

        `ref`, `max` = |Source:getAttenuationDistances|()

        See function description

        Returns: ~
            1. (`number`)
                The current reference attenuation distance. If the current
                DistanceModel is clamped, this is the minimum distance before
                the Source is no longer attenuated.

            2. (`number`)
                The current maximum attenuation distance.
-------------------------------------------------------------------------------
                                           *love2d-docs-Source:getChannelCount*
|Source:getChannelCount|

Gets the number of channels in the Source. Only 1-channel (mono) Sources can
use directional and positional effects.

Synopses: ~

        `channels` = |Source:getChannelCount|()

Variants: ~

        `channels` = |Source:getChannelCount|()

        See function description

        Return: ~
            (`number`)
                1 for mono, 2 for stereo.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Source:getCone*
|Source:getCone|

Gets the Source's directional volume cones. Together with Source:setDirection,
the cone angles allow for the Source's volume to vary depending on its
direction.

Synopses: ~

        `innerAngle`, `outerAngle`, `outerVolume` = |Source:getCone|()

Variants: ~

        `innerAngle`, `outerAngle`, `outerVolume` = |Source:getCone|()

        See function description

        Returns: ~
            1. (`number`)
                The inner angle from the Source's direction, in radians. The
                Source will play at normal volume if the listener is inside the
                cone defined by this angle.

            2. (`number`)
                The outer angle from the Source's direction, in radians. The
                Source will play at a volume between the normal and outer
                volumes, if the listener is in between the cones defined by the
                inner and outer angles.

            3. (`number`)
                The Source's volume when the listener is outside both the inner
                and outer cone angles.
-------------------------------------------------------------------------------
                                              *love2d-docs-Source:getDirection*
|Source:getDirection|

Gets the direction of the Source.

Synopses: ~

        `x`, `y`, `z` = |Source:getDirection|()

Variants: ~

        `x`, `y`, `z` = |Source:getDirection|()

        See function description

        Returns: ~
            1. (`number`)
                The X part of the direction vector.

            2. (`number`)
                The Y part of the direction vector.

            3. (`number`)
                The Z part of the direction vector.
-------------------------------------------------------------------------------
                                               *love2d-docs-Source:getDuration*
|Source:getDuration|

Gets the duration of the Source. For streaming Sources it may not always be
sample-accurate, and may return -1 if the duration cannot be determined at all.

Synopses: ~

        `duration` = |Source:getDuration|(`unit`)

Variants: ~

        `duration` = |Source:getDuration|(`unit`)

        See function description

        Parameters: ~
            • {unit} (`TimeUnit`) |love2d-docs-TimeUnit| (default: `'seconds'`)
                The time unit for the return value.

        Return: ~
            (`number`)
                The duration of the Source, or -1 if it cannot be determined.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:getEffect*
|Source:getEffect|

Gets the filter settings associated to a specific effect.

This function returns nil if the effect was applied with no filter settings
associated to it.

Synopses: ~

        `filtersettings` = |Source:getEffect|(`name`, `filtersettings`)

Variants: ~

        `filtersettings` = |Source:getEffect|(`name`, `filtersettings`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the effect.
            • {filtersettings} (`table`) (default: `{}`)
                An optional empty table that will be filled with the filter
                settings.

        Return: ~
            (`table`)
                The settings for the filter associated to this effect, or nil
                if the effect is not present in this Source or has no filter
                associated. The table has the following fields:
-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:getFilter*
|Source:getFilter|

Gets the filter settings currently applied to the Source.

Synopses: ~

        `settings` = |Source:getFilter|()

Variants: ~

        `settings` = |Source:getFilter|()

        See function description

        Return: ~
            (`table`)
                The filter settings to use for this Source, or nil if the
                Source has no active filter. The table has the following
                fields:
-------------------------------------------------------------------------------
                                        *love2d-docs-Source:getFreeBufferCount*
|Source:getFreeBufferCount|

Gets the number of free buffer slots in a queueable Source. If the queueable
Source is playing, this value will increase up to the amount the Source was
created with. If the queueable Source is stopped, it will process all of its
internal buffers first, in which case this function will always return the
amount it was created with.

Synopses: ~

        `buffers` = |Source:getFreeBufferCount|()

Variants: ~

        `buffers` = |Source:getFreeBufferCount|()

        See function description

        Return: ~
            (`number`)
                How many more SoundData objects can be queued up.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Source:getPitch*
|Source:getPitch|

Gets the current pitch of the Source.

Synopses: ~

        `pitch` = |Source:getPitch|()

Variants: ~

        `pitch` = |Source:getPitch|()

        See function description

        Return: ~
            (`number`)
                The pitch, where 1.0 is normal.
-------------------------------------------------------------------------------
                                               *love2d-docs-Source:getPosition*
|Source:getPosition|

Gets the position of the Source.

Synopses: ~

        `x`, `y`, `z` = |Source:getPosition|()

Variants: ~

        `x`, `y`, `z` = |Source:getPosition|()

        See function description

        Returns: ~
            1. (`number`)
                The X position of the Source.

            2. (`number`)
                The Y position of the Source.

            3. (`number`)
                The Z position of the Source.
-------------------------------------------------------------------------------
                                                *love2d-docs-Source:getRolloff*
|Source:getRolloff|

Returns the rolloff factor of the source.

Synopses: ~

        `rolloff` = |Source:getRolloff|()

Variants: ~

        `rolloff` = |Source:getRolloff|()

        See function description

        Return: ~
            (`number`)
                The rolloff factor.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Source:getType*
|Source:getType|

Gets the type of the Source.

Synopses: ~

        `sourcetype` = |Source:getType|()

Variants: ~

        `sourcetype` = |Source:getType|()

        See function description

        Return: ~
            (`SourceType`) |love2d-docs-SourceType|
                The type of the source.
-------------------------------------------------------------------------------
                                               *love2d-docs-Source:getVelocity*
|Source:getVelocity|

Gets the velocity of the Source.

Synopses: ~

        `x`, `y`, `z` = |Source:getVelocity|()

Variants: ~

        `x`, `y`, `z` = |Source:getVelocity|()

        See function description

        Returns: ~
            1. (`number`)
                The X part of the velocity vector.

            2. (`number`)
                The Y part of the velocity vector.

            3. (`number`)
                The Z part of the velocity vector.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:getVolume*
|Source:getVolume|

Gets the current volume of the Source.

Synopses: ~

        `volume` = |Source:getVolume|()

Variants: ~

        `volume` = |Source:getVolume|()

        See function description

        Return: ~
            (`number`)
                The volume of the Source, where 1.0 is normal volume.
-------------------------------------------------------------------------------
                                           *love2d-docs-Source:getVolumeLimits*
|Source:getVolumeLimits|

Returns the volume limits of the source.

Synopses: ~

        `min`, `max` = |Source:getVolumeLimits|()

Variants: ~

        `min`, `max` = |Source:getVolumeLimits|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum volume.

            2. (`number`)
                The maximum volume.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:isLooping*
|Source:isLooping|

Returns whether the Source will loop.

Synopses: ~

        `loop` = |Source:isLooping|()

Variants: ~

        `loop` = |Source:isLooping|()

        See function description

        Return: ~
            (`boolean`)
                True if the Source will loop, false otherwise.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:isPlaying*
|Source:isPlaying|

Returns whether the Source is playing.

Synopses: ~

        `playing` = |Source:isPlaying|()

Variants: ~

        `playing` = |Source:isPlaying|()

        See function description

        Return: ~
            (`boolean`)
                True if the Source is playing, false otherwise.
-------------------------------------------------------------------------------
                                                *love2d-docs-Source:isRelative*
|Source:isRelative|

Gets whether the Source's position, velocity, direction, and cone angles are
relative to the listener.

Synopses: ~

        `relative` = |Source:isRelative|()

Variants: ~

        `relative` = |Source:isRelative|()

        See function description

        Return: ~
            (`boolean`)
                True if the position, velocity, direction and cone angles are
                relative to the listener, false if they're absolute.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Source:pause*
|Source:pause|

Pauses the Source.

Synopses: ~

        |Source:pause|()

Variants: ~

        |Source:pause|()

        See function description


-------------------------------------------------------------------------------
                                                      *love2d-docs-Source:play*
|Source:play|

Starts playing the Source.

Synopses: ~

        `success` = |Source:play|()

Variants: ~

        `success` = |Source:play|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Source was able to successfully start playing.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Source:queue*
|Source:queue|

Queues SoundData for playback in a queueable Source.

This method requires the Source to be created via
love.audio.newQueueableSource.

Synopses: ~

        `success` = |Source:queue|(`sounddata`)

Variants: ~

        `success` = |Source:queue|(`sounddata`)

        See function description

        Parameters: ~
            • {sounddata} (`SoundData`) |love2d-docs-SoundData|
                The data to queue. The SoundData's sample rate, bit depth, and
                channel count must match the Source's.

        Return: ~
            (`boolean`)
                True if the data was successfully queued for playback, false if
                there were no available buffers to use for queueing.
-------------------------------------------------------------------------------
                                                      *love2d-docs-Source:seek*
|Source:seek|

Sets the currently playing position of the Source.

Synopses: ~

        |Source:seek|(`offset`, `unit`)

Variants: ~

        |Source:seek|(`offset`, `unit`)

        See function description

        Parameters: ~
            • {offset} (`number`)
                The position to seek to.
            • {unit} (`TimeUnit`) |love2d-docs-TimeUnit| (default: `'seconds'`)
                The unit of the position value.


-------------------------------------------------------------------------------
                                          *love2d-docs-Source:setAirAbsorption*
|Source:setAirAbsorption|

Sets the amount of air absorption applied to the Source.

By default the value is set to 0 which means that air absorption effects are
disabled. A value of 1 will apply high frequency attenuation to the Source at a
rate of 0.05 dB per meter.

Air absorption can simulate sound transmission through foggy air, dry air,
smoky atmosphere, etc. It can be used to simulate different atmospheric
conditions within different locations in an area.

Synopses: ~

        |Source:setAirAbsorption|(`amount`)

Variants: ~

        |Source:setAirAbsorption|(`amount`)

        Audio air absorption functionality is not supported on iOS.

        Parameters: ~
            • {amount} (`number`)
                The amount of air absorption applied to the Source. Must be
                between 0 and 10.


-------------------------------------------------------------------------------
                                   *love2d-docs-Source:setAttenuationDistances*
|Source:setAttenuationDistances|

Sets the reference and maximum attenuation distances of the Source. The
parameters, combined with the current DistanceModel, affect how the Source's
volume attenuates based on distance.

Distance attenuation is only applicable to Sources based on mono (rather than
stereo) audio.

Synopses: ~

        |Source:setAttenuationDistances|(`ref`, `max`)

Variants: ~

        |Source:setAttenuationDistances|(`ref`, `max`)

        See function description

        Parameters: ~
            • {ref} (`number`)
                The new reference attenuation distance. If the current
                DistanceModel is clamped, this is the minimum attenuation
                distance.
            • {max} (`number`)
                The new maximum attenuation distance.


-------------------------------------------------------------------------------
                                                   *love2d-docs-Source:setCone*
|Source:setCone|

Sets the Source's directional volume cones. Together with Source:setDirection,
the cone angles allow for the Source's volume to vary depending on its
direction.

Synopses: ~

        |Source:setCone|(`innerAngle`, `outerAngle`, `outerVolume`)

Variants: ~

        |Source:setCone|(`innerAngle`, `outerAngle`, `outerVolume`)

        See function description

        Parameters: ~
            • {innerAngle} (`number`)
                The inner angle from the Source's direction, in radians. The
                Source will play at normal volume if the listener is inside the
                cone defined by this angle.
            • {outerAngle} (`number`)
                The outer angle from the Source's direction, in radians. The
                Source will play at a volume between the normal and outer
                volumes, if the listener is in between the cones defined by the
                inner and outer angles.
            • {outerVolume} (`number`) (default: `0`)
                The Source's volume when the listener is outside both the inner
                and outer cone angles.


-------------------------------------------------------------------------------
                                              *love2d-docs-Source:setDirection*
|Source:setDirection|

Sets the direction vector of the Source. A zero vector makes the source
non-directional.

Synopses: ~

        |Source:setDirection|(`x`, `y`, `z`)

Variants: ~

        |Source:setDirection|(`x`, `y`, `z`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The X part of the direction vector.
            • {y} (`number`)
                The Y part of the direction vector.
            • {z} (`number`)
                The Z part of the direction vector.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:setEffect*
|Source:setEffect|

Applies an audio effect to the Source.

The effect must have been previously defined using love.audio.setEffect.

Synopses: ~

    1.  `success` = |Source:setEffect|(`name`, `enable`)
    2.  `success` = |Source:setEffect|(`name`, `filtersettings`)

Variants: ~

    1.  `success` = |Source:setEffect|(`name`, `enable`)

    1.          Applies the given previously defined effect to this Source.

        Parameters: ~
            • {name} (`string`)
                The name of the effect previously set up with
                love.audio.setEffect.
            • {enable} (`boolean`) (default: `true`)
                If false and the given effect name was previously enabled on
                this Source, disables the effect.

        Return: ~
            (`boolean`)
                Whether the effect was successfully applied to this Source.
    2.  `success` = |Source:setEffect|(`name`, `filtersettings`)

    2.          Applies the given previously defined effect to this Source, and applies
        a filter to the Source which affects the sound fed into the effect.

        Audio effect functionality is not supported on iOS.

        Parameters: ~
            • {name} (`string`)
                The name of the effect previously set up with
                love.audio.setEffect.
            • {filtersettings} (`table`)
                The filter settings to apply prior to the effect, with the
                following fields:
                • {type} (`FilterType`) |love2d-docs-FilterType|
                    The type of filter to use.
                • {volume} (`number`)
                    The overall volume of the audio. Must be between 0 and 1.
                • {highgain} (`number`)
                    Volume of high-frequency audio. Only applies to low-pass
                    and band-pass filters. Must be between 0 and 1.
                • {lowgain} (`number`)
                    Volume of low-frequency audio. Only applies to high-pass
                    and band-pass filters. Must be between 0 and 1.

        Return: ~
            (`boolean`)
                Whether the effect and filter were successfully applied to this
                Source.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:setFilter*
|Source:setFilter|

Sets a low-pass, high-pass, or band-pass filter to apply when playing the
Source.

Synopses: ~

    1.  `success` = |Source:setFilter|(`settings`)
    2.  |Source:setFilter|()

Variants: ~

    1.  `success` = |Source:setFilter|(`settings`)

    1.          See function description

        Parameters: ~
            • {settings} (`table`)
                The filter settings to use for this Source, with the following
                fields:
                • {type} (`FilterType`) |love2d-docs-FilterType|
                    The type of filter to use.
                • {volume} (`number`)
                    The overall volume of the audio. Must be between 0 and 1.
                • {highgain} (`number`)
                    Volume of high-frequency audio. Only applies to low-pass
                    and band-pass filters. Must be between 0 and 1.
                • {lowgain} (`number`)
                    Volume of low-frequency audio. Only applies to high-pass
                    and band-pass filters. Must be between 0 and 1.

        Return: ~
            (`boolean`)
                Whether the filter was successfully applied to the Source.
    2.  |Source:setFilter|()

    2.          Disables filtering on this Source.




-------------------------------------------------------------------------------
                                                *love2d-docs-Source:setLooping*
|Source:setLooping|

Sets whether the Source should loop.

Synopses: ~

        |Source:setLooping|(`loop`)

Variants: ~

        |Source:setLooping|(`loop`)

        See function description

        Parameters: ~
            • {loop} (`boolean`)
                True if the source should loop, false otherwise.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Source:setPitch*
|Source:setPitch|

Sets the pitch of the Source.

Synopses: ~

        |Source:setPitch|(`pitch`)

Variants: ~

        |Source:setPitch|(`pitch`)

        See function description

        Parameters: ~
            • {pitch} (`number`)
                Calculated with regard to 1 being the base pitch. Each
                reduction by 50 percent equals a pitch shift of -12 semitones
                (one octave reduction). Each doubling equals a pitch shift of
                12 semitones (one octave increase). Zero is not a legal value.


-------------------------------------------------------------------------------
                                               *love2d-docs-Source:setPosition*
|Source:setPosition|

Sets the position of the Source. Please note that this only works for mono
(i.e. non-stereo) sound files!

Synopses: ~

        |Source:setPosition|(`x`, `y`, `z`)

Variants: ~

        |Source:setPosition|(`x`, `y`, `z`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The X position of the Source.
            • {y} (`number`)
                The Y position of the Source.
            • {z} (`number`)
                The Z position of the Source.


-------------------------------------------------------------------------------
                                               *love2d-docs-Source:setRelative*
|Source:setRelative|

Sets whether the Source's position, velocity, direction, and cone angles are
relative to the listener, or absolute.

By default, all sources are absolute and therefore relative to the origin of
love's coordinate system 0, 0. Only absolute sources are affected by the
position of the listener. Please note that positional audio only works for mono
(i.e. non-stereo) sources.

Synopses: ~

        |Source:setRelative|(`enable`)

Variants: ~

        |Source:setRelative|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`) (default: `false`)
                True to make the position, velocity, direction and cone angles
                relative to the listener, false to make them absolute.


-------------------------------------------------------------------------------
                                                *love2d-docs-Source:setRolloff*
|Source:setRolloff|

Sets the rolloff factor which affects the strength of the used distance
attenuation.

Extended information and detailed formulas can be found in the chapter '3.4.
Attenuation By Distance' of OpenAL 1.1 specification.

Synopses: ~

        |Source:setRolloff|(`rolloff`)

Variants: ~

        |Source:setRolloff|(`rolloff`)

        See function description

        Parameters: ~
            • {rolloff} (`number`)
                The new rolloff factor.


-------------------------------------------------------------------------------
                                               *love2d-docs-Source:setVelocity*
|Source:setVelocity|

Sets the velocity of the Source.

This does '''not''' change the position of the Source, but lets the application
know how it has to calculate the doppler effect.

Synopses: ~

        |Source:setVelocity|(`x`, `y`, `z`)

Variants: ~

        |Source:setVelocity|(`x`, `y`, `z`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The X part of the velocity vector.
            • {y} (`number`)
                The Y part of the velocity vector.
            • {z} (`number`)
                The Z part of the velocity vector.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Source:setVolume*
|Source:setVolume|

Sets the current volume of the Source.

Synopses: ~

        |Source:setVolume|(`volume`)

Variants: ~

        |Source:setVolume|(`volume`)

        See function description

        Parameters: ~
            • {volume} (`number`)
                The volume for a Source, where 1.0 is normal volume. Volume
                cannot be raised above 1.0.


-------------------------------------------------------------------------------
                                           *love2d-docs-Source:setVolumeLimits*
|Source:setVolumeLimits|

Sets the volume limits of the source. The limits have to be numbers from 0 to
1.

Synopses: ~

        |Source:setVolumeLimits|(`min`, `max`)

Variants: ~

        |Source:setVolumeLimits|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum volume.
            • {max} (`number`)
                The maximum volume.


-------------------------------------------------------------------------------
                                                      *love2d-docs-Source:stop*
|Source:stop|

Stops a Source.

Synopses: ~

        |Source:stop|()

Variants: ~

        |Source:stop|()

        See function description


-------------------------------------------------------------------------------
                                                      *love2d-docs-Source:tell*
|Source:tell|

Gets the currently playing position of the Source.

Synopses: ~

        `position` = |Source:tell|(`unit`)

Variants: ~

        `position` = |Source:tell|(`unit`)

        See function description

        Parameters: ~
            • {unit} (`TimeUnit`) |love2d-docs-TimeUnit| (default: `'seconds'`)
                The type of unit for the return value.

        Return: ~
            (`number`)
                The currently playing position of the Source.

===============================================================================
                                                        *love2d-docs-love.data*
|love.data|

Provides functionality for creating and transforming data.

    |callbacks|                               |love2d-docs-love.data-callbacks|
    |enums|                                       |love2d-docs-love.data-enums|
    |functions|                               |love2d-docs-love.data-functions|
    |types|                                       |love2d-docs-love.data-types|

-------------------------------------------------------------------------------
                                              *love2d-docs-love.data-callbacks*
The callbacks of |love.data|:

    None

-------------------------------------------------------------------------------
                                                  *love2d-docs-love.data-enums*
The enums of |love.data|:

    |CompressedDataFormat|                   |love2d-docs-CompressedDataFormat|
    |ContainerType|                                 |love2d-docs-ContainerType|
    |EncodeFormat|                                   |love2d-docs-EncodeFormat|
    |HashFunction|                                   |love2d-docs-HashFunction|

-------------------------------------------------------------------------------
                                             *love2d-docs-CompressedDataFormat*
|CompressedDataFormat|

Compressed data formats.

    constants: ~

        `lz4`: string

            The LZ4 compression format. Compresses and decompresses very
            quickly, but the compression ratio is not the best. LZ4-HC is used
            when compression level 9 is specified. Some benchmarks are
            available here.

        `zlib`: string

            The zlib format is DEFLATE-compressed data with a small bit of
            header data. Compresses relatively slowly and decompresses
            moderately quickly, and has a decent compression ratio.

        `gzip`: string

            The gzip format is DEFLATE-compressed data with a slightly larger
            header than zlib. Since it uses DEFLATE it has the same compression
            characteristics as the zlib format.

        `deflate`: string

            Raw DEFLATE-compressed data (no header).

-------------------------------------------------------------------------------
                                                    *love2d-docs-ContainerType*
|ContainerType|

Return type of various data-returning functions.

    constants: ~

        `data`: string

            Return type is ByteData.

        `string`: string

            Return type is string.

-------------------------------------------------------------------------------
                                                     *love2d-docs-EncodeFormat*
|EncodeFormat|

Encoding format used to encode or decode data.

    constants: ~

        `base64`: string

            Encode/decode data as base64 binary-to-text encoding.

        `hex`: string

            Encode/decode data as hexadecimal string.

-------------------------------------------------------------------------------
                                                     *love2d-docs-HashFunction*
|HashFunction|

Hash algorithm of love.data.hash.

    constants: ~

        `md5`: string

            MD5 hash algorithm (16 bytes).

        `sha1`: string

            SHA1 hash algorithm (20 bytes).

        `sha224`: string

            SHA2 hash algorithm with message digest size of 224 bits (28
            bytes).

        `sha256`: string

            SHA2 hash algorithm with message digest size of 256 bits (32
            bytes).

        `sha384`: string

            SHA2 hash algorithm with message digest size of 384 bits (48
            bytes).

        `sha512`: string

            SHA2 hash algorithm with message digest size of 512 bits (64
            bytes).

-------------------------------------------------------------------------------
                                              *love2d-docs-love.data-functions*
The functions of |love.data|:

    |compress|                                 |love2d-docs-love.data.compress|
    |decode|                                     |love2d-docs-love.data.decode|
    |decompress|                             |love2d-docs-love.data.decompress|
    |encode|                                     |love2d-docs-love.data.encode|
    |getPackedSize|                       |love2d-docs-love.data.getPackedSize|
    |hash|                                         |love2d-docs-love.data.hash|
    |newByteData|                           |love2d-docs-love.data.newByteData|
    |newDataView|                           |love2d-docs-love.data.newDataView|
    |pack|                                         |love2d-docs-love.data.pack|
    |unpack|                                     |love2d-docs-love.data.unpack|

-------------------------------------------------------------------------------
                                               *love2d-docs-love.data.compress*
|love.data.compress|

Compresses a string or data using a specific compression algorithm.

Synopses: ~

    1.  `compressedData` = |love.data.compress|(`container`, `format`,
        `rawstring`, `level`)
    2.  `compressedData` = |love.data.compress|(`container`, `format`,
        `data`, `level`)

Variants: ~

    1.  `compressedData` = |love.data.compress|(`container`, `format`,
        `rawstring`, `level`)

    1.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the compressed data as.
            • {format} (`CompressedDataFormat`) |love2d-docs-CompressedDataFormat|
                The format to use when compressing the string.
            • {rawstring} (`string`)
                The raw (un-compressed) string to compress.
            • {level} (`number`) (default: `-1`)
                The level of compression to use, between 0 and 9. -1 indicates
                the default level. The meaning of this argument depends on the
                compression format being used.

        Return: ~
            (`CompressedDataorstring`)
                CompressedData/string which contains the compressed version of
                rawstring.
    2.  `compressedData` = |love.data.compress|(`container`, `format`,
        `data`, `level`)

    2.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the compressed data as.
            • {format} (`CompressedDataFormat`) |love2d-docs-CompressedDataFormat|
                The format to use when compressing the data.
            • {data} (`Data`) |love2d-docs-Data|
                A Data object containing the raw (un-compressed) data to
                compress.
            • {level} (`number`) (default: `-1`)
                The level of compression to use, between 0 and 9. -1 indicates
                the default level. The meaning of this argument depends on the
                compression format being used.

        Return: ~
            (`CompressedDataorstring`)
                CompressedData/string which contains the compressed version of
                data.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.data.decode*
|love.data.decode|

Decode Data or a string from any of the EncodeFormats to Data or string.

Synopses: ~

    1.  `decoded` = |love.data.decode|(`container`, `format`,
        `sourceString`)
    2.  `decoded` = |love.data.decode|(`container`, `format`,
        `sourceData`)

Variants: ~

    1.  `decoded` = |love.data.decode|(`container`, `format`,
        `sourceString`)

    1.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the decoded data as.
            • {format} (`EncodeFormat`) |love2d-docs-EncodeFormat|
                The format of the input data.
            • {sourceString} (`string`)
                The raw (encoded) data to decode.

        Return: ~
            (`ByteDataorstring`)
                ByteData/string which contains the decoded version of source.
    2.  `decoded` = |love.data.decode|(`container`, `format`,
        `sourceData`)

    2.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the decoded data as.
            • {format} (`EncodeFormat`) |love2d-docs-EncodeFormat|
                The format of the input data.
            • {sourceData} (`Data`) |love2d-docs-Data|
                The raw (encoded) data to decode.

        Return: ~
            (`ByteDataorstring`)
                ByteData/string which contains the decoded version of source.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.data.decompress*
|love.data.decompress|

Decompresses a CompressedData or previously compressed string or Data object.

Synopses: ~

    1.  `decompressedData` = |love.data.decompress|(`container`,
        `compressedData`)
    2.  `decompressedData` = |love.data.decompress|(`container`,
        `format`, `compressedString`)
    3.  `decompressedData` = |love.data.decompress|(`container`,
        `format`, `data`)

Variants: ~

    1.  `decompressedData` = |love.data.decompress|(`container`,
        `compressedData`)

    1.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the decompressed data as.
            • {compressedData} (`CompressedData`) |love2d-docs-CompressedData|
                The compressed data to decompress.

        Return: ~
            (`Dataorstring`)
                Data/string containing the raw decompressed data.
    2.  `decompressedData` = |love.data.decompress|(`container`,
        `format`, `compressedString`)

    2.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the decompressed data as.
            • {format} (`CompressedDataFormat`) |love2d-docs-CompressedDataFormat|
                The format that was used to compress the given string.
            • {compressedString} (`string`)
                A string containing data previously compressed with
                love.data.compress.

        Return: ~
            (`Dataorstring`)
                Data/string containing the raw decompressed data.
    3.  `decompressedData` = |love.data.decompress|(`container`,
        `format`, `data`)

    3.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the decompressed data as.
            • {format} (`CompressedDataFormat`) |love2d-docs-CompressedDataFormat|
                The format that was used to compress the given data.
            • {data} (`Data`) |love2d-docs-Data|
                A Data object containing data previously compressed with
                love.data.compress.

        Return: ~
            (`Dataorstring`)
                Data/string containing the raw decompressed data.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.data.encode*
|love.data.encode|

Encode Data or a string to a Data or string in one of the EncodeFormats.

Synopses: ~

    1.  `encoded` = |love.data.encode|(`container`, `format`,
        `sourceString`, `linelength`)
    2.  `encoded` = |love.data.encode|(`container`, `format`,
        `sourceData`, `linelength`)

Variants: ~

    1.  `encoded` = |love.data.encode|(`container`, `format`,
        `sourceString`, `linelength`)

    1.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the encoded data as.
            • {format} (`EncodeFormat`) |love2d-docs-EncodeFormat|
                The format of the output data.
            • {sourceString} (`string`)
                The raw data to encode.
            • {linelength} (`number`) (default: `0`)
                The maximum line length of the output. Only supported for
                base64, ignored if 0.

        Return: ~
            (`ByteDataorstring`)
                ByteData/string which contains the encoded version of source.
    2.  `encoded` = |love.data.encode|(`container`, `format`,
        `sourceData`, `linelength`)

    2.          See function description

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the encoded data as.
            • {format} (`EncodeFormat`) |love2d-docs-EncodeFormat|
                The format of the output data.
            • {sourceData} (`Data`) |love2d-docs-Data|
                The raw data to encode.
            • {linelength} (`number`) (default: `0`)
                The maximum line length of the output. Only supported for
                base64, ignored if 0.

        Return: ~
            (`ByteDataorstring`)
                ByteData/string which contains the encoded version of source.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.data.getPackedSize*
|love.data.getPackedSize|

Gets the size in bytes that a given format used with love.data.pack will use.

This function behaves the same as Lua 5.3's string.packsize.

Synopses: ~

        `size` = |love.data.getPackedSize|(`format`)

Variants: ~

        `size` = |love.data.getPackedSize|(`format`)

        The format string cannot have the variable-length options 's' or 'z'.

        Parameters: ~
            • {format} (`string`)
                A string determining how the values are packed. Follows the
                rules of Lua 5.3's string.pack format strings.

        Return: ~
            (`number`)
                The size in bytes that the packed data will use.
-------------------------------------------------------------------------------
                                                   *love2d-docs-love.data.hash*
|love.data.hash|

Compute the message digest of a string using a specified hash algorithm.

Synopses: ~

    1.  `rawdigest` = |love.data.hash|(`hashFunction`, `string`)
    2.  `rawdigest` = |love.data.hash|(`hashFunction`, `data`)

Variants: ~

    1.  `rawdigest` = |love.data.hash|(`hashFunction`, `string`)

    1.          See function description

        Parameters: ~
            • {hashFunction} (`HashFunction`) |love2d-docs-HashFunction|
                Hash algorithm to use.
            • {string} (`string`)
                String to hash.

        Return: ~
            (`string`)
                Raw message digest string.
    2.  `rawdigest` = |love.data.hash|(`hashFunction`, `data`)

    2.          To return the hex string representation of the hash, use
        love.data.encode

        hexDigestString = love.data.encode('string', 'hex',
        love.data.hash(algo, data))

        Parameters: ~
            • {hashFunction} (`HashFunction`) |love2d-docs-HashFunction|
                Hash algorithm to use.
            • {data} (`Data`) |love2d-docs-Data|
                Data to hash.

        Return: ~
            (`string`)
                Raw message digest string.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.data.newByteData*
|love.data.newByteData|

Creates a new Data object containing arbitrary bytes.

Data:getPointer along with LuaJIT's FFI can be used to manipulate the contents
of the ByteData object after it has been created.

Synopses: ~

    1.  `bytedata` = |love.data.newByteData|(`datastring`)
    2.  `bytedata` = |love.data.newByteData|(`Data`, `offset`, `size`)
    3.  `bytedata` = |love.data.newByteData|(`size`)

Variants: ~

    1.  `bytedata` = |love.data.newByteData|(`datastring`)

    1.          Creates a new ByteData by copying the contents of the specified string.

        Parameters: ~
            • {datastring} (`string`)
                The byte string to copy.

        Return: ~
            (`ByteData`) |love2d-docs-ByteData|
                The new Data object.
    2.  `bytedata` = |love.data.newByteData|(`Data`, `offset`, `size`)

    2.          Creates a new ByteData by copying from an existing Data object.

        Parameters: ~
            • {Data} (`Data`) |love2d-docs-Data|
                The existing Data object to copy.
            • {offset} (`number`) (default: `0`)
                The offset of the subsection to copy, in bytes.
            • {size} (`number`) (default: `data:getSize()`)
                The size in bytes of the new Data object.

        Return: ~
            (`ByteData`) |love2d-docs-ByteData|
                The new Data object.
    3.  `bytedata` = |love.data.newByteData|(`size`)

    3.          Creates a new empty ByteData with the specific size.

        Parameters: ~
            • {size} (`number`)
                The size in bytes of the new Data object.

        Return: ~
            (`ByteData`) |love2d-docs-ByteData|
                The new Data object.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.data.newDataView*
|love.data.newDataView|

Creates a new Data referencing a subsection of an existing Data object.

Synopses: ~

        `view` = |love.data.newDataView|(`data`, `offset`, `size`)

Variants: ~

        `view` = |love.data.newDataView|(`data`, `offset`, `size`)

        Data:getString and Data:getPointer will return the original Data
        object's contents, with the view's offset and size applied.

        Parameters: ~
            • {data} (`Data`) |love2d-docs-Data|
                The Data object to reference.
            • {offset} (`number`)
                The offset of the subsection to reference, in bytes.
            • {size} (`number`)
                The size in bytes of the subsection to reference.

        Return: ~
            (`Data`) |love2d-docs-Data|
                The new Data view.
-------------------------------------------------------------------------------
                                                   *love2d-docs-love.data.pack*
|love.data.pack|

Packs (serializes) simple Lua values.

This function behaves the same as Lua 5.3's string.pack.

Synopses: ~

        `data` = |love.data.pack|(`container`, `format`, `v1`, `...`)

Variants: ~

        `data` = |love.data.pack|(`container`, `format`, `v1`, `...`)

        Packing integers with values greater than 2^52 is not supported, as Lua
        5.1 cannot represent those values in its number type.

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the encoded data as.
            • {format} (`string`)
                A string determining how the values are packed. Follows the
                rules of Lua 5.3's string.pack format strings.
            • {v1} (`numberorbooleanorstring`)
                The first value (number, boolean, or string) to serialize.
            • {...} (`numberorbooleanorstring`)
                Additional values to serialize.

        Return: ~
            (`Dataorstring`)
                Data/string which contains the serialized data.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.data.unpack*
|love.data.unpack|

Unpacks (deserializes) a byte-string or Data into simple Lua values.

This function behaves the same as Lua 5.3's string.unpack.

Synopses: ~

    1.  `v1`, `...`, `index` = |love.data.unpack|(`format`,
        `datastring`, `pos`)
    2.  `v1`, `...`, `index` = |love.data.unpack|(`format`, `data`,
        `pos`)

Variants: ~

    1.  `v1`, `...`, `index` = |love.data.unpack|(`format`,
        `datastring`, `pos`)

    1.          See function description

        Parameters: ~
            • {format} (`string`)
                A string determining how the values were packed. Follows the
                rules of Lua 5.3's string.pack format strings.
            • {datastring} (`string`)
                A string containing the packed (serialized) data.
            • {pos} (`number`) (default: `1`)
                Where to start reading in the string. Negative values can be
                used to read relative from the end of the string.

        Returns: ~
            1. (`numberorbooleanorstring`)
                The first value (number, boolean, or string) that was unpacked.

            2. (`numberorbooleanorstring`)
                Additional unpacked values.

            3. (`number`)
                The index of the first unread byte in the data string.
    2.  `v1`, `...`, `index` = |love.data.unpack|(`format`, `data`,
        `pos`)

    2.          Unpacking integers with values greater than 2^52 is not supported, as
        Lua 5.1 cannot represent those values in its number type.

        Parameters: ~
            • {format} (`string`)
                A string determining how the values were packed. Follows the
                rules of Lua 5.3's string.pack format strings.
            • {data} (`Data`) |love2d-docs-Data|
                A Data object containing the packed (serialized) data.
            • {pos} (`number`) (default: `1`)
                1-based index indicating where to start reading in the Data.
                Negative values can be used to read relative from the end of
                the Data object.

        Returns: ~
            1. (`numberorbooleanorstring`)
                The first value (number, boolean, or string) that was unpacked.

            2. (`numberorbooleanorstring`)
                Additional unpacked values.

            3. (`number`)
                The 1-based index of the first unread byte in the Data.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.data-types*
The types of |love.data|:

    |ByteData|                                           |love2d-docs-ByteData|
    |CompressedData|                               |love2d-docs-CompressedData|

-------------------------------------------------------------------------------
                                                         *love2d-docs-ByteData*
|ByteData|

Data object containing arbitrary bytes in an contiguous memory.

There are currently no LÖVE functions provided for manipulating the contents
of a ByteData, but Data:getPointer can be used with LuaJIT's FFI to access and
write to the contents directly.

                                            *love2d-docs-ByteData-constructors*
    constructors:

        |newByteData|                                 |love2d-docs-newByteData|

                                              *love2d-docs-ByteData-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|
        |Data|                                               |love2d-docs-Data|

                                                *love2d-docs-ByteData-subtypes*
    subtypes:

        None

                                               *love2d-docs-ByteData-functions*
    functions:

        None

-------------------------------------------------------------------------------
                                                   *love2d-docs-CompressedData*
|CompressedData|

Represents byte data compressed using a specific algorithm.

love.data.decompress can be used to de-compress the data (or
love.math.decompress in 0.10.2 or earlier).

                                      *love2d-docs-CompressedData-constructors*
    constructors:

        |compress|                                       |love2d-docs-compress|

                                        *love2d-docs-CompressedData-supertypes*
    supertypes:

        |Data|                                               |love2d-docs-Data|
        |Object|                                           |love2d-docs-Object|

                                          *love2d-docs-CompressedData-subtypes*
    subtypes:

        None

                                         *love2d-docs-CompressedData-functions*
    functions:

        |getFormat|                      |love2d-docs-CompressedData:getFormat|

-------------------------------------------------------------------------------
                                         *love2d-docs-CompressedData:getFormat*
|CompressedData:getFormat|

Gets the compression format of the CompressedData.

Synopses: ~

        `format` = |CompressedData:getFormat|()

Variants: ~

        `format` = |CompressedData:getFormat|()

        See function description

        Return: ~
            (`CompressedDataFormat`) |love2d-docs-CompressedDataFormat|
                The format of the CompressedData.

===============================================================================
                                                       *love2d-docs-love.event*
|love.event|

Manages events, like keypresses.

    |callbacks|                              |love2d-docs-love.event-callbacks|
    |enums|                                      |love2d-docs-love.event-enums|
    |functions|                              |love2d-docs-love.event-functions|
    |types|                                      |love2d-docs-love.event-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.event-callbacks*
The callbacks of |love.event|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.event-enums*
The enums of |love.event|:

    |Event|                                                 |love2d-docs-Event|

-------------------------------------------------------------------------------
                                                            *love2d-docs-Event*
|Event|

Arguments to love.event.push() and the like.

Since 0.8.0, event names are no longer abbreviated.

    constants: ~

        `focus`: string

            Window focus gained or lost

        `joystickpressed`: string

            Joystick pressed

        `joystickreleased`: string

            Joystick released

        `keypressed`: string

            Key pressed

        `keyreleased`: string

            Key released

        `mousepressed`: string

            Mouse pressed

        `mousereleased`: string

            Mouse released

        `quit`: string

            Quit

        `resize`: string

            Window size changed by the user

        `visible`: string

            Window is minimized or un-minimized by the user

        `mousefocus`: string

            Window mouse focus gained or lost

        `threaderror`: string

            A Lua error has occurred in a thread

        `joystickadded`: string

            Joystick connected

        `joystickremoved`: string

            Joystick disconnected

        `joystickaxis`: string

            Joystick axis motion

        `joystickhat`: string

            Joystick hat pressed

        `gamepadpressed`: string

            Joystick's virtual gamepad button pressed

        `gamepadreleased`: string

            Joystick's virtual gamepad button released

        `gamepadaxis`: string

            Joystick's virtual gamepad axis moved

        `textinput`: string

            User entered text

        `mousemoved`: string

            Mouse position changed

        `lowmemory`: string

            Running out of memory on mobile devices system

        `textedited`: string

            Candidate text for an IME changed

        `wheelmoved`: string

            Mouse wheel moved

        `touchpressed`: string

            Touch screen touched

        `touchreleased`: string

            Touch screen stop touching

        `touchmoved`: string

            Touch press moved inside touch screen

        `directorydropped`: string

            Directory is dragged and dropped onto the window

        `filedropped`: string

            File is dragged and dropped onto the window.

        `jp`: string

            Joystick pressed

        `jr`: string

            Joystick released

        `kp`: string

            Key pressed

        `kr`: string

            Key released

        `mp`: string

            Mouse pressed

        `mr`: string

            Mouse released

        `q`: string

            Quit

        `f`: string

            Window focus gained or lost

-------------------------------------------------------------------------------
                                             *love2d-docs-love.event-functions*
The functions of |love.event|:

    |clear|                                      |love2d-docs-love.event.clear|
    |poll|                                        |love2d-docs-love.event.poll|
    |pump|                                        |love2d-docs-love.event.pump|
    |push|                                        |love2d-docs-love.event.push|
    |quit|                                        |love2d-docs-love.event.quit|
    |wait|                                        |love2d-docs-love.event.wait|

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.event.clear*
|love.event.clear|

Clears the event queue.

Synopses: ~

        |love.event.clear|()

Variants: ~

        |love.event.clear|()

        See function description


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.event.poll*
|love.event.poll|

Returns an iterator for messages in the event queue.

Synopses: ~

        `i` = |love.event.poll|()

Variants: ~

        `i` = |love.event.poll|()

        See function description

        Return: ~
            (`function`)
                Iterator function usable in a for loop.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.event.pump*
|love.event.pump|

Pump events into the event queue.

This is a low-level function, and is usually not called by the user, but by
love.run.

Note that this does need to be called for any OS to think you're still running,

and if you want to handle OS-generated events at all (think callbacks).

Synopses: ~

        |love.event.pump|()

Variants: ~

        |love.event.pump|()

        See function description


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.event.push*
|love.event.push|

Adds an event to the event queue.

From 0.10.0 onwards, you may pass an arbitrary amount of arguments with this
function, though the default callbacks don't ever use more than six.

Synopses: ~

        |love.event.push|(`n`, `a`, `b`, `c`, `d`, `e`, `f`, `...`)

Variants: ~

        |love.event.push|(`n`, `a`, `b`, `c`, `d`, `e`, `f`, `...`)

        See function description

        Parameters: ~
            • {n} (`Event`) |love2d-docs-Event|
                The name of the event.
            • {a} (`Variant`) (default: `nil`)
                First event argument.
            • {b} (`Variant`) (default: `nil`)
                Second event argument.
            • {c} (`Variant`) (default: `nil`)
                Third event argument.
            • {d} (`Variant`) (default: `nil`)
                Fourth event argument.
            • {e} (`Variant`) (default: `nil`)
                Fifth event argument.
            • {f} (`Variant`) (default: `nil`)
                Sixth event argument.
            • {...} (`Variant`) (default: `nil`)
                Further event arguments may follow.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.event.quit*
|love.event.quit|

Adds the quit event to the queue.

The quit event is a signal for the event handler to close LÖVE. It's possible
to abort the exit process with the love.quit callback.

Synopses: ~

    1.  |love.event.quit|(`exitstatus`)
    2.  |love.event.quit|(`'restart'`)

Variants: ~

    1.  |love.event.quit|(`exitstatus`)

    1.          See function description

        Parameters: ~
            • {exitstatus} (`number`) (default: `0`)
                The program exit status to use when closing the application.


    2.  |love.event.quit|(`'restart'`)

    2.          Restarts the game without relaunching the executable. This cleanly
        shuts down the main Lua state instance and creates a brand new one.

        Parameters: ~
            • {'restart'} (`string`)
                Tells the default love.run to exit and restart the game without
                relaunching the executable.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.event.wait*
|love.event.wait|

Like love.event.poll(), but blocks until there is an event in the queue.

Synopses: ~

        `n`, `a`, `b`, `c`, `d`, `e`, `f`, `...` = |love.event.wait|()

Variants: ~

        `n`, `a`, `b`, `c`, `d`, `e`, `f`, `...` = |love.event.wait|()

        See function description

        Returns: ~
            1. (`Event`) |love2d-docs-Event|
                The name of event.

            2. (`Variant`)
                First event argument.

            3. (`Variant`)
                Second event argument.

            4. (`Variant`)
                Third event argument.

            5. (`Variant`)
                Fourth event argument.

            6. (`Variant`)
                Fifth event argument.

            7. (`Variant`)
                Sixth event argument.

            8. (`Variant`)
                Further event arguments may follow.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.event-types*
The types of |love.event|:

    None

===============================================================================
                                                  *love2d-docs-love.filesystem*
|love.filesystem|

Provides an interface to the user's filesystem.

    |callbacks|                         |love2d-docs-love.filesystem-callbacks|
    |enums|                                 |love2d-docs-love.filesystem-enums|
    |functions|                         |love2d-docs-love.filesystem-functions|
    |types|                                 |love2d-docs-love.filesystem-types|

-------------------------------------------------------------------------------
                                        *love2d-docs-love.filesystem-callbacks*
The callbacks of |love.filesystem|:

    None

-------------------------------------------------------------------------------
                                            *love2d-docs-love.filesystem-enums*
The enums of |love.filesystem|:

    |BufferMode|                                       |love2d-docs-BufferMode|
    |FileDecoder|                                     |love2d-docs-FileDecoder|
    |FileMode|                                           |love2d-docs-FileMode|
    |FileType|                                           |love2d-docs-FileType|

-------------------------------------------------------------------------------
                                                       *love2d-docs-BufferMode*
|BufferMode|

Buffer modes for File objects.

    constants: ~

        `none`: string

            No buffering. The result of write and append operations appears
            immediately.

        `line`: string

            Line buffering. Write and append operations are buffered until a
            newline is output or the buffer size limit is reached.

        `full`: string

            Full buffering. Write and append operations are always buffered
            until the buffer size limit is reached.

-------------------------------------------------------------------------------
                                                      *love2d-docs-FileDecoder*
|FileDecoder|

How to decode a given FileData.

    constants: ~

        `file`: string

            The data is unencoded.

        `base64`: string

            The data is base64-encoded.

-------------------------------------------------------------------------------
                                                         *love2d-docs-FileMode*
|FileMode|

The different modes you can open a File in.

    constants: ~

        `r`: string

            Open a file for read.

        `w`: string

            Open a file for write.

        `a`: string

            Open a file for append.

        `c`: string

            Do not open a file (represents a closed file.)

-------------------------------------------------------------------------------
                                                         *love2d-docs-FileType*
|FileType|

The type of a file.

    constants: ~

        `file`: string

            Regular file.

        `directory`: string

            Directory.

        `symlink`: string

            Symbolic link.

        `other`: string

            Something completely different like a device.

-------------------------------------------------------------------------------
                                        *love2d-docs-love.filesystem-functions*
The functions of |love.filesystem|:

    |append|                               |love2d-docs-love.filesystem.append|
    |areSymlinksEnabled|       |love2d-docs-love.filesystem.areSymlinksEnabled|
    |createDirectory|             |love2d-docs-love.filesystem.createDirectory|
    |getAppdataDirectory|     |love2d-docs-love.filesystem.getAppdataDirectory|
    |getCRequirePath|             |love2d-docs-love.filesystem.getCRequirePath|
    |getDirectoryItems|         |love2d-docs-love.filesystem.getDirectoryItems|
    |getIdentity|                     |love2d-docs-love.filesystem.getIdentity|
    |getInfo|                             |love2d-docs-love.filesystem.getInfo|
    |getRealDirectory|           |love2d-docs-love.filesystem.getRealDirectory|
    |getRequirePath|               |love2d-docs-love.filesystem.getRequirePath|
    |getSaveDirectory|           |love2d-docs-love.filesystem.getSaveDirectory|
    |getSource|                         |love2d-docs-love.filesystem.getSource|
    |getSourceBaseDirectory|  |love2d-docs-love.filesystem.getSourceBaseDirectory|
    |getUserDirectory|           |love2d-docs-love.filesystem.getUserDirectory|
    |getWorkingDirectory|     |love2d-docs-love.filesystem.getWorkingDirectory|
    |init|                                   |love2d-docs-love.filesystem.init|
    |isFused|                             |love2d-docs-love.filesystem.isFused|
    |lines|                                 |love2d-docs-love.filesystem.lines|
    |load|                                   |love2d-docs-love.filesystem.load|
    |mount|                                 |love2d-docs-love.filesystem.mount|
    |newFile|                             |love2d-docs-love.filesystem.newFile|
    |newFileData|                     |love2d-docs-love.filesystem.newFileData|
    |read|                                   |love2d-docs-love.filesystem.read|
    |remove|                               |love2d-docs-love.filesystem.remove|
    |setCRequirePath|             |love2d-docs-love.filesystem.setCRequirePath|
    |setIdentity|                     |love2d-docs-love.filesystem.setIdentity|
    |setRequirePath|               |love2d-docs-love.filesystem.setRequirePath|
    |setSource|                         |love2d-docs-love.filesystem.setSource|
    |setSymlinksEnabled|       |love2d-docs-love.filesystem.setSymlinksEnabled|
    |unmount|                             |love2d-docs-love.filesystem.unmount|
    |write|                                 |love2d-docs-love.filesystem.write|

-------------------------------------------------------------------------------
                                           *love2d-docs-love.filesystem.append*
|love.filesystem.append|

Append data to an existing file.

Synopses: ~

    1.  `success`, `errormsg` = |love.filesystem.append|(`name`,
        `data`, `size`)
    2.  `success`, `errormsg` = |love.filesystem.append|(`name`,
        `data`, `size`)

Variants: ~

    1.  `success`, `errormsg` = |love.filesystem.append|(`name`,
        `data`, `size`)

    1.          See function description

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file.
            • {data} (`string`)
                The string data to append to the file.
            • {size} (`number`) (default: `all`)
                How many bytes to write.

        Returns: ~
            1. (`boolean`)
                True if the operation was successful, or nil if there was an
                error.

            2. (`string`)
                The error message on failure.
    2.  `success`, `errormsg` = |love.filesystem.append|(`name`,
        `data`, `size`)

    2.          See function description

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file.
            • {data} (`Data`) |love2d-docs-Data|
                The Data object to append to the file.
            • {size} (`number`) (default: `all`)
                How many bytes to write.

        Returns: ~
            1. (`boolean`)
                True if the operation was successful, or nil if there was an
                error.

            2. (`string`)
                The error message on failure.
-------------------------------------------------------------------------------
                               *love2d-docs-love.filesystem.areSymlinksEnabled*
|love.filesystem.areSymlinksEnabled|

Gets whether love.filesystem follows symbolic links.

Synopses: ~

        `enable` = |love.filesystem.areSymlinksEnabled|()

Variants: ~

        `enable` = |love.filesystem.areSymlinksEnabled|()

        See function description

        Return: ~
            (`boolean`)
                Whether love.filesystem follows symbolic links.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.filesystem.createDirectory*
|love.filesystem.createDirectory|

Recursively creates a directory.

When called with 'a/b' it creates both 'a' and 'a/b', if they don't exist
already.

Synopses: ~

        `success` = |love.filesystem.createDirectory|(`name`)

Variants: ~

        `success` = |love.filesystem.createDirectory|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The directory to create.

        Return: ~
            (`boolean`)
                True if the directory was created, false if not.
-------------------------------------------------------------------------------
                              *love2d-docs-love.filesystem.getAppdataDirectory*
|love.filesystem.getAppdataDirectory|

Returns the application data directory (could be the same as getUserDirectory)

Synopses: ~

        `path` = |love.filesystem.getAppdataDirectory|()

Variants: ~

        `path` = |love.filesystem.getAppdataDirectory|()

        See function description

        Return: ~
            (`string`)
                The path of the application data directory
-------------------------------------------------------------------------------
                                  *love2d-docs-love.filesystem.getCRequirePath*
|love.filesystem.getCRequirePath|

Gets the filesystem paths that will be searched for c libraries when require is
called.

The paths string returned by this function is a sequence of path templates
separated by semicolons. The argument passed to ''require'' will be inserted in
place of any question mark ('?') character in each template (after the dot
characters in the argument passed to ''require'' are replaced by directory
separators.) Additionally, any occurrence of a double question mark ('??') will
be replaced by the name passed to require and the default library extension for
the platform.

The paths are relative to the game's source and save directories, as well as
any paths mounted with love.filesystem.mount.

Synopses: ~

        `paths` = |love.filesystem.getCRequirePath|()

Variants: ~

        `paths` = |love.filesystem.getCRequirePath|()

        The default paths string is '??', which makes require('cool') try to
        load cool.dll, or cool.so depending on the platform.

        Return: ~
            (`string`)
                The paths that the ''require'' function will check for c
                libraries in love's filesystem.
-------------------------------------------------------------------------------
                                *love2d-docs-love.filesystem.getDirectoryItems*
|love.filesystem.getDirectoryItems|

Returns a table with the names of files and subdirectories in the specified
path. The table is not sorted in any way; the order is undefined.

If the path passed to the function exists in the game and the save directory,
it will list the files and directories from both places.

Synopses: ~

    1.  `files` = |love.filesystem.getDirectoryItems|(`dir`)
    2.  `files` = |love.filesystem.getDirectoryItems|(`dir`,
        `callback`)

Variants: ~

    1.  `files` = |love.filesystem.getDirectoryItems|(`dir`)

    1.          See function description

        Parameters: ~
            • {dir} (`string`)
                The directory.

        Return: ~
            (`table`)
                A sequence with the names of all files and subdirectories as
                strings.
    2.  `files` = |love.filesystem.getDirectoryItems|(`dir`,
        `callback`)

    2.          See function description

        Parameters: ~
            • {dir} (`string`)
                The directory.
            • {callback} (`function`)
                A function which is called for each file and folder in the
                directory. The filename is passed to the function as an
                argument.

        Return: ~
            (`table`)
                A sequence with the names of all files and subdirectories as
                strings.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.filesystem.getIdentity*
|love.filesystem.getIdentity|

Gets the write directory name for your game.

Note that this only returns the name of the folder to store your files in, not
the full path.

Synopses: ~

        `name` = |love.filesystem.getIdentity|()

Variants: ~

        `name` = |love.filesystem.getIdentity|()

        See function description

        Return: ~
            (`string`)
                The identity that is used as write directory.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.filesystem.getInfo*
|love.filesystem.getInfo|

Gets information about the specified file or directory.

Synopses: ~

    1.  `info` = |love.filesystem.getInfo|(`path`, `filtertype`)
    2.  `info` = |love.filesystem.getInfo|(`path`, `info`)
    3.  `info` = |love.filesystem.getInfo|(`path`, `filtertype`,
        `info`)

Variants: ~

    1.  `info` = |love.filesystem.getInfo|(`path`, `filtertype`)

    1.          See function description

        Parameters: ~
            • {path} (`string`)
                The file or directory path to check.
            • {filtertype} (`FileType`) |love2d-docs-FileType| (default: `nil`)
                If supplied, this parameter causes getInfo to only return the
                info table if the item at the given path matches the specified
                file type.

        Return: ~
            (`table`)
                A table containing information about the specified path, or nil
                if nothing exists at the path. The table contains the following
                fields:
    2.  `info` = |love.filesystem.getInfo|(`path`, `info`)

    2.          This variant accepts an existing table to fill in, instead of creating
        a new one.

        Parameters: ~
            • {path} (`string`)
                The file or directory path to check.
            • {info} (`table`)
                A table which will be filled in with info about the specified
                path.

        Return: ~
            (`table`)
                The table given as an argument, or nil if nothing exists at the
                path. The table will be filled in with the following fields:
    3.  `info` = |love.filesystem.getInfo|(`path`, `filtertype`,
        `info`)

    3.          This variant only returns info if the item at the given path is the
        same file type as specified in the filtertype argument, and accepts an
        existing table to fill in, instead of creating a new one.

        Parameters: ~
            • {path} (`string`)
                The file or directory path to check.
            • {filtertype} (`FileType`) |love2d-docs-FileType|
                Causes getInfo to only return the info table if the item at the
                given path matches the specified file type.
            • {info} (`table`)
                A table which will be filled in with info about the specified
                path.

        Return: ~
            (`table`)
                The table given as an argument, or nil if nothing exists at the
                path. The table will be filled in with the following fields:
-------------------------------------------------------------------------------
                                 *love2d-docs-love.filesystem.getRealDirectory*
|love.filesystem.getRealDirectory|

Gets the platform-specific absolute path of the directory containing a
filepath.

This can be used to determine whether a file is inside the save directory or
the game's source .love.

Synopses: ~

        `realdir` = |love.filesystem.getRealDirectory|(`filepath`)

Variants: ~

        `realdir` = |love.filesystem.getRealDirectory|(`filepath`)

        This function returns the directory containing the given ''file path'',
        rather than file. For example, if the file screenshot1.png exists in a
        directory called screenshots in the game's save directory,
        love.filesystem.getRealDirectory('screenshots/screenshot1.png') will
        return the same value as love.filesystem.getSaveDirectory.

        Parameters: ~
            • {filepath} (`string`)
                The filepath to get the directory of.

        Return: ~
            (`string`)
                The platform-specific full path of the directory containing the
                filepath.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.filesystem.getRequirePath*
|love.filesystem.getRequirePath|

Gets the filesystem paths that will be searched when require is called.

The paths string returned by this function is a sequence of path templates
separated by semicolons. The argument passed to ''require'' will be inserted in
place of any question mark ('?') character in each template (after the dot
characters in the argument passed to ''require'' are replaced by directory
separators.)

The paths are relative to the game's source and save directories, as well as
any paths mounted with love.filesystem.mount.

Synopses: ~

        `paths` = |love.filesystem.getRequirePath|()

Variants: ~

        `paths` = |love.filesystem.getRequirePath|()

        The default paths string is '?.lua;?/init.lua', which makes
        require('cool') try to load cool.lua and then try cool/init.lua if
        cool.lua doesn't exist.

        Return: ~
            (`string`)
                The paths that the ''require'' function will check in love's
                filesystem.
-------------------------------------------------------------------------------
                                 *love2d-docs-love.filesystem.getSaveDirectory*
|love.filesystem.getSaveDirectory|

Gets the full path to the designated save directory.

This can be useful if you want to use the standard io library (or something
else) to

read or write in the save directory.

Synopses: ~

        `dir` = |love.filesystem.getSaveDirectory|()

Variants: ~

        `dir` = |love.filesystem.getSaveDirectory|()

        See function description

        Return: ~
            (`string`)
                The absolute path to the save directory.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.filesystem.getSource*
|love.filesystem.getSource|

Returns the full path to the the .love file or directory. If the game is fused
to the LÖVE executable, then the executable is returned.

Synopses: ~

        `path` = |love.filesystem.getSource|()

Variants: ~

        `path` = |love.filesystem.getSource|()

        See function description

        Return: ~
            (`string`)
                The full platform-dependent path of the .love file or
                directory.
-------------------------------------------------------------------------------
                           *love2d-docs-love.filesystem.getSourceBaseDirectory*
|love.filesystem.getSourceBaseDirectory|

Returns the full path to the directory containing the .love file. If the game
is fused to the LÖVE executable, then the directory containing the executable
is returned.

If love.filesystem.isFused is true, the path returned by this function can be
passed to love.filesystem.mount, which will make the directory containing the
main game (e.g. C:\Program Files\coolgame\) readable by love.filesystem.

Synopses: ~

        `path` = |love.filesystem.getSourceBaseDirectory|()

Variants: ~

        `path` = |love.filesystem.getSourceBaseDirectory|()

        See function description

        Return: ~
            (`string`)
                The full platform-dependent path of the directory containing
                the .love file.
-------------------------------------------------------------------------------
                                 *love2d-docs-love.filesystem.getUserDirectory*
|love.filesystem.getUserDirectory|

Returns the path of the user's directory

Synopses: ~

        `path` = |love.filesystem.getUserDirectory|()

Variants: ~

        `path` = |love.filesystem.getUserDirectory|()

        See function description

        Return: ~
            (`string`)
                The path of the user's directory
-------------------------------------------------------------------------------
                              *love2d-docs-love.filesystem.getWorkingDirectory*
|love.filesystem.getWorkingDirectory|

Gets the current working directory.

Synopses: ~

        `cwd` = |love.filesystem.getWorkingDirectory|()

Variants: ~

        `cwd` = |love.filesystem.getWorkingDirectory|()

        See function description

        Return: ~
            (`string`)
                The current working directory.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.filesystem.init*
|love.filesystem.init|

Initializes love.filesystem, will be called internally, so should not be used
explicitly.

Synopses: ~

        |love.filesystem.init|(`appname`)

Variants: ~

        |love.filesystem.init|(`appname`)

        See function description

        Parameters: ~
            • {appname} (`string`)
                The name of the application binary, typically love.


-------------------------------------------------------------------------------
                                          *love2d-docs-love.filesystem.isFused*
|love.filesystem.isFused|

Gets whether the game is in fused mode or not.

If a game is in fused mode, its save directory will be directly in the Appdata
directory instead of Appdata/LOVE/. The game will also be able to load C Lua
dynamic libraries which are located in the save directory.

A game is in fused mode if the source .love has been fused to the executable
(see Game Distribution), or if '--fused' has been given as a command-line
argument when starting the game.

Synopses: ~

        `fused` = |love.filesystem.isFused|()

Variants: ~

        `fused` = |love.filesystem.isFused|()

        See function description

        Return: ~
            (`boolean`)
                True if the game is in fused mode, false otherwise.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.filesystem.lines*
|love.filesystem.lines|

Iterate over the lines in a file.

Synopses: ~

        `iterator` = |love.filesystem.lines|(`name`)

Variants: ~

        `iterator` = |love.filesystem.lines|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file

        Return: ~
            (`function`)
                A function that iterates over all the lines in the file
-------------------------------------------------------------------------------
                                             *love2d-docs-love.filesystem.load*
|love.filesystem.load|

Loads a Lua file (but does not run it).

Synopses: ~

        `chunk`, `errormsg` = |love.filesystem.load|(`name`)

Variants: ~

        `chunk`, `errormsg` = |love.filesystem.load|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file.

        Returns: ~
            1. (`function`)
                The loaded chunk.

            2. (`string`)
                The error message if file could not be opened.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.filesystem.mount*
|love.filesystem.mount|

Mounts a zip file or folder in the game's save directory for reading.

It is also possible to mount love.filesystem.getSourceBaseDirectory if the game
is in fused mode.

Synopses: ~

    1.  `success` = |love.filesystem.mount|(`archive`, `mountpoint`,
        `appendToPath`)
    2.  `success` = |love.filesystem.mount|(`filedata`, `mountpoint`,
        `appendToPath`)
    3.  `success` = |love.filesystem.mount|(`data`, `archivename`,
        `mountpoint`, `appendToPath`)

Variants: ~

    1.  `success` = |love.filesystem.mount|(`archive`, `mountpoint`,
        `appendToPath`)

    1.          See function description

        Parameters: ~
            • {archive} (`string`)
                The folder or zip file in the game's save directory to mount.
            • {mountpoint} (`string`)
                The new path the archive will be mounted to.
            • {appendToPath} (`boolean`) (default: `false`)
                Whether the archive will be searched when reading a filepath
                before or after already-mounted archives. This includes the
                game's source and save directories.

        Return: ~
            (`boolean`)
                True if the archive was successfully mounted, false otherwise.
    2.  `success` = |love.filesystem.mount|(`filedata`, `mountpoint`,
        `appendToPath`)

    2.          Mounts the contents of the given FileData in memory. The FileData's
        data must contain a zipped directory structure.

        Parameters: ~
            • {filedata} (`FileData`) |love2d-docs-FileData|
                The FileData object in memory to mount.
            • {mountpoint} (`string`)
                The new path the archive will be mounted to.
            • {appendToPath} (`boolean`) (default: `false`)
                Whether the archive will be searched when reading a filepath
                before or after already-mounted archives. This includes the
                game's source and save directories.

        Return: ~
            (`boolean`)
                True if the archive was successfully mounted, false otherwise.
    3.  `success` = |love.filesystem.mount|(`data`, `archivename`,
        `mountpoint`, `appendToPath`)

    3.          Mounts the contents of the given Data object in memory. The data must
        contain a zipped directory structure.

        Parameters: ~
            • {data} (`Data`) |love2d-docs-Data|
                The Data object in memory to mount.
            • {archivename} (`string`)
                The name to associate the mounted data with, for use with
                love.filesystem.unmount. Must be unique compared to other
                mounted data.
            • {mountpoint} (`string`)
                The new path the archive will be mounted to.
            • {appendToPath} (`boolean`) (default: `false`)
                Whether the archive will be searched when reading a filepath
                before or after already-mounted archives. This includes the
                game's source and save directories.

        Return: ~
            (`boolean`)
                True if the archive was successfully mounted, false otherwise.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.filesystem.newFile*
|love.filesystem.newFile|

Creates a new File object.

It needs to be opened before it can be accessed.

Synopses: ~

    1.  `file` = |love.filesystem.newFile|(`filename`)
    2.  `file`, `errorstr` = |love.filesystem.newFile|(`filename`,
        `mode`)

Variants: ~

    1.  `file` = |love.filesystem.newFile|(`filename`)

    1.          Please note that this function will not return any error message (e.g.
        if you use an invalid filename) because it just creates the File
        Object. You can still check if the file is valid by using File:open
        which returns a boolean and an error message if something goes wrong
        while opening the file.

        Parameters: ~
            • {filename} (`string`)
                The filename of the file.

        Return: ~
            (`File`) |love2d-docs-File|
                The new File object.
    2.  `file`, `errorstr` = |love.filesystem.newFile|(`filename`,
        `mode`)

    2.          Creates a File object and opens it for reading, writing, or appending.

        Parameters: ~
            • {filename} (`string`)
                The filename of the file.
            • {mode} (`FileMode`) |love2d-docs-FileMode|
                The mode to open the file in.

        Returns: ~
            1. (`File`) |love2d-docs-File|
                The new File object, or nil if an error occurred.

            2. (`string`)
                The error string if an error occurred.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.filesystem.newFileData*
|love.filesystem.newFileData|

Creates a new FileData object from a file on disk, or from a string in memory.

Synopses: ~

    1.  `data` = |love.filesystem.newFileData|(`contents`, `name`)
    2.  `data` = |love.filesystem.newFileData|(`originaldata`, `name`)
    3.  `data`, `err` = |love.filesystem.newFileData|(`filepath`)

Variants: ~

    1.  `data` = |love.filesystem.newFileData|(`contents`, `name`)

    1.          Creates a new FileData object from a string in memory.

        Parameters: ~
            • {contents} (`string`)
                The contents of the file in memory represented as a string.
            • {name} (`string`)
                The name of the file. The extension may be parsed and used by
                LÖVE when passing the FileData object into
                love.audio.newSource.

        Return: ~
            (`FileData`) |love2d-docs-FileData|
                The new FileData.
    2.  `data` = |love.filesystem.newFileData|(`originaldata`, `name`)

    2.          Creates a new FileData object from a Data object in memory.

        Parameters: ~
            • {originaldata} (`Data`) |love2d-docs-Data|
                The Data object to copy into the new FileData object.
            • {name} (`string`)
                The name of the file. The extension may be parsed and used by
                LÖVE when passing the FileData object into
                love.audio.newSource.

        Return: ~
            (`FileData`) |love2d-docs-FileData|
                The new FileData.
    3.  `data`, `err` = |love.filesystem.newFileData|(`filepath`)

    3.          Creates a new FileData from a file on the storage device.

        Parameters: ~
            • {filepath} (`string`)
                Path to the file.

        Returns: ~
            1. (`FileData`) |love2d-docs-FileData|
                The new FileData, or nil if an error occurred.

            2. (`string`)
                The error string, if an error occurred.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.filesystem.read*
|love.filesystem.read|

Read the contents of a file.

Synopses: ~

    1.  `contents`, `size`, `contents`, `error` =
        |love.filesystem.read|(`name`, `size`)
    2.  `contents`, `size`, `contents`, `error` =
        |love.filesystem.read|(`container`, `name`, `size`)

Variants: ~

    1.  `contents`, `size`, `contents`, `error` =
        |love.filesystem.read|(`name`, `size`)

    1.          See function description

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file.
            • {size} (`number`) (default: `all`)
                How many bytes to read.

        Returns: ~
            1. (`string`)
                The file contents.

            2. (`number`)
                How many bytes have been read.

            3. (`nil`)
                returns nil as content.

            4. (`string`)
                returns an error message.
    2.  `contents`, `size`, `contents`, `error` =
        |love.filesystem.read|(`container`, `name`, `size`)

    2.          Reads the contents of a file into either a string or a FileData object.

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the file's contents as.
            • {name} (`string`)
                The name (and path) of the file
            • {size} (`number`) (default: `all`)
                How many bytes to read

        Returns: ~
            1. (`FileDataorstring`)
                FileData or string containing the file contents.

            2. (`number`)
                How many bytes have been read.

            3. (`nil`)
                returns nil as content.

            4. (`string`)
                returns an error message.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.filesystem.remove*
|love.filesystem.remove|

Removes a file or empty directory.

Synopses: ~

        `success` = |love.filesystem.remove|(`name`)

Variants: ~

        `success` = |love.filesystem.remove|(`name`)

        The directory must be empty before removal or else it will fail. Simply
        remove all files and folders in the directory beforehand.

        If the file exists in the .love but not in the save directory, it
        returns false as well.

        An opened File prevents removal of the underlying file. Simply close
        the File to remove it.

        Parameters: ~
            • {name} (`string`)
                The file or directory to remove.

        Return: ~
            (`boolean`)
                True if the file/directory was removed, false otherwise.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.filesystem.setCRequirePath*
|love.filesystem.setCRequirePath|

Sets the filesystem paths that will be searched for c libraries when require is
called.

The paths string returned by this function is a sequence of path templates
separated by semicolons. The argument passed to ''require'' will be inserted in
place of any question mark ('?') character in each template (after the dot
characters in the argument passed to ''require'' are replaced by directory
separators.) Additionally, any occurrence of a double question mark ('??') will
be replaced by the name passed to require and the default library extension for
the platform.

The paths are relative to the game's source and save directories, as well as
any paths mounted with love.filesystem.mount.

Synopses: ~

        |love.filesystem.setCRequirePath|(`paths`)

Variants: ~

        |love.filesystem.setCRequirePath|(`paths`)

        The default paths string is '??', which makes require('cool') try to
        load cool.dll, or cool.so depending on the platform.

        Parameters: ~
            • {paths} (`string`)
                The paths that the ''require'' function will check in love's
                filesystem.


-------------------------------------------------------------------------------
                                      *love2d-docs-love.filesystem.setIdentity*
|love.filesystem.setIdentity|

Sets the write directory for your game.

Note that you can only set the name of the folder to store your files in, not
the location.

Synopses: ~

    1.  |love.filesystem.setIdentity|(`name`)
    2.  |love.filesystem.setIdentity|(`name`)

Variants: ~

    1.  |love.filesystem.setIdentity|(`name`)

    1.          See function description

        Parameters: ~
            • {name} (`string`)
                The new identity that will be used as write directory.


    2.  |love.filesystem.setIdentity|(`name`)

    2.          See function description

        Parameters: ~
            • {name} (`string`)
                The new identity that will be used as write directory.


-------------------------------------------------------------------------------
                                   *love2d-docs-love.filesystem.setRequirePath*
|love.filesystem.setRequirePath|

Sets the filesystem paths that will be searched when require is called.

The paths string given to this function is a sequence of path templates
separated by semicolons. The argument passed to ''require'' will be inserted in
place of any question mark ('?') character in each template (after the dot
characters in the argument passed to ''require'' are replaced by directory
separators.)

The paths are relative to the game's source and save directories, as well as
any paths mounted with love.filesystem.mount.

Synopses: ~

        |love.filesystem.setRequirePath|(`paths`)

Variants: ~

        |love.filesystem.setRequirePath|(`paths`)

        The default paths string is '?.lua;?/init.lua', which makes
        require('cool') try to load cool.lua and then try cool/init.lua if
        cool.lua doesn't exist.

        Parameters: ~
            • {paths} (`string`)
                The paths that the ''require'' function will check in love's
                filesystem.


-------------------------------------------------------------------------------
                                        *love2d-docs-love.filesystem.setSource*
|love.filesystem.setSource|

Sets the source of the game, where the code is present. This function can only
be called once, and is normally automatically done by LÖVE.

Synopses: ~

        |love.filesystem.setSource|(`path`)

Variants: ~

        |love.filesystem.setSource|(`path`)

        See function description

        Parameters: ~
            • {path} (`string`)
                Absolute path to the game's source folder.


-------------------------------------------------------------------------------
                               *love2d-docs-love.filesystem.setSymlinksEnabled*
|love.filesystem.setSymlinksEnabled|

Sets whether love.filesystem follows symbolic links. It is enabled by default
in version 0.10.0 and newer, and disabled by default in 0.9.2.

Synopses: ~

        |love.filesystem.setSymlinksEnabled|(`enable`)

Variants: ~

        |love.filesystem.setSymlinksEnabled|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                Whether love.filesystem should follow symbolic links.


-------------------------------------------------------------------------------
                                          *love2d-docs-love.filesystem.unmount*
|love.filesystem.unmount|

Unmounts a zip file or folder previously mounted for reading with
love.filesystem.mount.

Synopses: ~

        `success` = |love.filesystem.unmount|(`archive`)

Variants: ~

        `success` = |love.filesystem.unmount|(`archive`)

        See function description

        Parameters: ~
            • {archive} (`string`)
                The folder or zip file in the game's save directory which is
                currently mounted.

        Return: ~
            (`boolean`)
                True if the archive was successfully unmounted, false
                otherwise.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.filesystem.write*
|love.filesystem.write|

Write data to a file in the save directory. If the file existed already, it
will be completely replaced by the new contents.

Synopses: ~

    1.  `success`, `message` = |love.filesystem.write|(`name`, `data`,
        `size`)
    2.  `success`, `message` = |love.filesystem.write|(`name`, `data`,
        `size`)

Variants: ~

    1.  `success`, `message` = |love.filesystem.write|(`name`, `data`,
        `size`)

    1.          See function description

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file.
            • {data} (`string`)
                The string data to write to the file.
            • {size} (`number`) (default: `all`)
                How many bytes to write.

        Returns: ~
            1. (`boolean`)
                If the operation was successful.

            2. (`string`)
                Error message if operation was unsuccessful.
    2.  `success`, `message` = |love.filesystem.write|(`name`, `data`,
        `size`)

    2.          If you are getting the error message 'Could not set write directory',
        try setting the save directory. This is done either with
        love.filesystem.setIdentity or by setting the identity field in
        love.conf.

        '''Writing to multiple lines''': In Windows, some text editors (e.g.
        Notepad) only treat CRLF ('\r\n') as a new line.

        Parameters: ~
            • {name} (`string`)
                The name (and path) of the file.
            • {data} (`Data`) |love2d-docs-Data|
                The Data object to write to the file.
            • {size} (`number`) (default: `all`)
                How many bytes to write.

        Returns: ~
            1. (`boolean`)
                If the operation was successful.

            2. (`string`)
                Error message if operation was unsuccessful.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.filesystem-types*
The types of |love.filesystem|:

    |DroppedFile|                                     |love2d-docs-DroppedFile|
    |File|                                                   |love2d-docs-File|
    |FileData|                                           |love2d-docs-FileData|

-------------------------------------------------------------------------------
                                                      *love2d-docs-DroppedFile*
|DroppedFile|

Represents a file dropped onto the window.

Note that the DroppedFile type can only be obtained from love.filedropped
callback, and can't be constructed manually by the user.

                                         *love2d-docs-DroppedFile-constructors*
    constructors:

        None

                                           *love2d-docs-DroppedFile-supertypes*
    supertypes:

        |File|                                               |love2d-docs-File|
        |Object|                                           |love2d-docs-Object|

                                             *love2d-docs-DroppedFile-subtypes*
    subtypes:

        None

                                            *love2d-docs-DroppedFile-functions*
    functions:

        None

-------------------------------------------------------------------------------
                                                             *love2d-docs-File*
|File|

Represents a file on the filesystem. A function that takes a file path can also
take a File.

                                                *love2d-docs-File-constructors*
    constructors:

        |newFile|                                         |love2d-docs-newFile|

                                                  *love2d-docs-File-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-File-subtypes*
    subtypes:

        None

                                                   *love2d-docs-File-functions*
    functions:

        |close|                                        |love2d-docs-File:close|
        |flush|                                        |love2d-docs-File:flush|
        |getBuffer|                                |love2d-docs-File:getBuffer|
        |getFilename|                            |love2d-docs-File:getFilename|
        |getMode|                                    |love2d-docs-File:getMode|
        |getSize|                                    |love2d-docs-File:getSize|
        |isEOF|                                        |love2d-docs-File:isEOF|
        |isOpen|                                      |love2d-docs-File:isOpen|
        |lines|                                        |love2d-docs-File:lines|
        |open|                                          |love2d-docs-File:open|
        |read|                                          |love2d-docs-File:read|
        |seek|                                          |love2d-docs-File:seek|
        |setBuffer|                                |love2d-docs-File:setBuffer|
        |tell|                                          |love2d-docs-File:tell|
        |write|                                        |love2d-docs-File:write|

-------------------------------------------------------------------------------
                                                       *love2d-docs-File:close*
|File:close|

Closes a File.

Synopses: ~

        `success` = |File:close|()

Variants: ~

        `success` = |File:close|()

        See function description

        Return: ~
            (`boolean`)
                Whether closing was successful.
-------------------------------------------------------------------------------
                                                       *love2d-docs-File:flush*
|File:flush|

Flushes any buffered written data in the file to the disk.

Synopses: ~

        `success`, `err` = |File:flush|()

Variants: ~

        `success`, `err` = |File:flush|()

        See function description

        Returns: ~
            1. (`boolean`)
                Whether the file successfully flushed any buffered data to the
                disk.

            2. (`string`)
                The error string, if an error occurred and the file could not
                be flushed.
-------------------------------------------------------------------------------
                                                   *love2d-docs-File:getBuffer*
|File:getBuffer|

Gets the buffer mode of a file.

Synopses: ~

        `mode`, `size` = |File:getBuffer|()

Variants: ~

        `mode`, `size` = |File:getBuffer|()

        See function description

        Returns: ~
            1. (`BufferMode`) |love2d-docs-BufferMode|
                The current buffer mode of the file.

            2. (`number`)
                The maximum size in bytes of the file's buffer.
-------------------------------------------------------------------------------
                                                 *love2d-docs-File:getFilename*
|File:getFilename|

Gets the filename that the File object was created with. If the file object
originated from the love.filedropped callback, the filename will be the full
platform-dependent file path.

Synopses: ~

        `filename` = |File:getFilename|()

Variants: ~

        `filename` = |File:getFilename|()

        See function description

        Return: ~
            (`string`)
                The filename of the File.
-------------------------------------------------------------------------------
                                                     *love2d-docs-File:getMode*
|File:getMode|

Gets the FileMode the file has been opened with.

Synopses: ~

        `mode` = |File:getMode|()

Variants: ~

        `mode` = |File:getMode|()

        See function description

        Return: ~
            (`FileMode`) |love2d-docs-FileMode|
                The mode this file has been opened with.
-------------------------------------------------------------------------------
                                                     *love2d-docs-File:getSize*
|File:getSize|

Returns the file size.

Synopses: ~

        `size` = |File:getSize|()

Variants: ~

        `size` = |File:getSize|()

        See function description

        Return: ~
            (`number`)
                The file size in bytes.
-------------------------------------------------------------------------------
                                                       *love2d-docs-File:isEOF*
|File:isEOF|

Gets whether end-of-file has been reached.

Synopses: ~

        `eof` = |File:isEOF|()

Variants: ~

        `eof` = |File:isEOF|()

        See function description

        Return: ~
            (`boolean`)
                Whether EOF has been reached.
-------------------------------------------------------------------------------
                                                      *love2d-docs-File:isOpen*
|File:isOpen|

Gets whether the file is open.

Synopses: ~

        `open` = |File:isOpen|()

Variants: ~

        `open` = |File:isOpen|()

        See function description

        Return: ~
            (`boolean`)
                True if the file is currently open, false otherwise.
-------------------------------------------------------------------------------
                                                       *love2d-docs-File:lines*
|File:lines|

Iterate over all the lines in a file.

Synopses: ~

        `iterator` = |File:lines|()

Variants: ~

        `iterator` = |File:lines|()

        See function description

        Return: ~
            (`function`)
                The iterator (can be used in for loops).
-------------------------------------------------------------------------------
                                                        *love2d-docs-File:open*
|File:open|

Open the file for write, read or append.

Synopses: ~

        `ok`, `err` = |File:open|(`mode`)

Variants: ~

        `ok`, `err` = |File:open|(`mode`)

        If you are getting the error message 'Could not set write directory',
        try setting the save directory. This is done either with
        love.filesystem.setIdentity or by setting the identity field in
        love.conf (only available with love 0.7 or higher).

        Parameters: ~
            • {mode} (`FileMode`) |love2d-docs-FileMode|
                The mode to open the file in.

        Returns: ~
            1. (`boolean`)
                True on success, false otherwise.

            2. (`string`)
                The error string if an error occurred.
-------------------------------------------------------------------------------
                                                        *love2d-docs-File:read*
|File:read|

Read a number of bytes from a file.

Synopses: ~

    1.  `contents`, `size` = |File:read|(`bytes`)
    2.  `contents`, `size` = |File:read|(`container`, `bytes`)

Variants: ~

    1.  `contents`, `size` = |File:read|(`bytes`)

    1.          See function description

        Parameters: ~
            • {bytes} (`number`) (default: `all`)
                The number of bytes to read.

        Returns: ~
            1. (`string`)
                The contents of the read bytes.

            2. (`number`)
                How many bytes have been read.
    2.  `contents`, `size` = |File:read|(`container`, `bytes`)

    2.          Reads the contents of a file into either a string or a FileData object.

        Parameters: ~
            • {container} (`ContainerType`) |love2d-docs-ContainerType|
                What type to return the file's contents as.
            • {bytes} (`number`) (default: `all`)
                The number of bytes to read.

        Returns: ~
            1. (`FileDataorstring`)
                FileData or string containing the read bytes.

            2. (`number`)
                How many bytes have been read.
-------------------------------------------------------------------------------
                                                        *love2d-docs-File:seek*
|File:seek|

Seek to a position in a file

Synopses: ~

        `success` = |File:seek|(`pos`)

Variants: ~

        `success` = |File:seek|(`pos`)

        See function description

        Parameters: ~
            • {pos} (`number`)
                The position to seek to

        Return: ~
            (`boolean`)
                Whether the operation was successful
-------------------------------------------------------------------------------
                                                   *love2d-docs-File:setBuffer*
|File:setBuffer|

Sets the buffer mode for a file opened for writing or appending. Files with
buffering enabled will not write data to the disk until the buffer size limit
is reached, depending on the buffer mode.

File:flush will force any buffered data to be written to the disk.

Synopses: ~

        `success`, `errorstr` = |File:setBuffer|(`mode`, `size`)

Variants: ~

        `success`, `errorstr` = |File:setBuffer|(`mode`, `size`)

        See function description

        Parameters: ~
            • {mode} (`BufferMode`) |love2d-docs-BufferMode|
                The buffer mode to use.
            • {size} (`number`) (default: `0`)
                The maximum size in bytes of the file's buffer.

        Returns: ~
            1. (`boolean`)
                Whether the buffer mode was successfully set.

            2. (`string`)
                The error string, if the buffer mode could not be set and an
                error occurred.
-------------------------------------------------------------------------------
                                                        *love2d-docs-File:tell*
|File:tell|

Returns the position in the file.

Synopses: ~

        `pos` = |File:tell|()

Variants: ~

        `pos` = |File:tell|()

        See function description

        Return: ~
            (`number`)
                The current position.
-------------------------------------------------------------------------------
                                                       *love2d-docs-File:write*
|File:write|

Write data to a file.

Synopses: ~

    1.  `success`, `err` = |File:write|(`data`, `size`)
    2.  `success`, `errorstr` = |File:write|(`data`, `size`)

Variants: ~

    1.  `success`, `err` = |File:write|(`data`, `size`)

    1.          See function description

        Parameters: ~
            • {data} (`string`)
                The string data to write.
            • {size} (`number`) (default: `all`)
                How many bytes to write.

        Returns: ~
            1. (`boolean`)
                Whether the operation was successful.

            2. (`string`)
                The error string if an error occurred.
    2.  `success`, `errorstr` = |File:write|(`data`, `size`)

    2.          '''Writing to multiple lines''': In Windows, some text editors (e.g.
        Notepad before Windows 10 1809) only treat CRLF ('\r\n') as a new line.

        --example

        f = love.filesystem.newFile('note.txt')

        f:open('w')

        for i = 1, 10 do

            f:write('This is line '..i..'!\r\n')

        end

        f:close()

        Parameters: ~
            • {data} (`Data`) |love2d-docs-Data|
                The Data object to write.
            • {size} (`number`) (default: `all`)
                How many bytes to write.

        Returns: ~
            1. (`boolean`)
                Whether the operation was successful.

            2. (`string`)
                The error string if an error occurred.

-------------------------------------------------------------------------------
                                                         *love2d-docs-FileData*
|FileData|

Data representing the contents of a file.

                                            *love2d-docs-FileData-constructors*
    constructors:

        |newFileData|                                 |love2d-docs-newFileData|

                                              *love2d-docs-FileData-supertypes*
    supertypes:

        |Data|                                               |love2d-docs-Data|
        |Object|                                           |love2d-docs-Object|

                                                *love2d-docs-FileData-subtypes*
    subtypes:

        None

                                               *love2d-docs-FileData-functions*
    functions:

        |getExtension|                      |love2d-docs-FileData:getExtension|
        |getFilename|                        |love2d-docs-FileData:getFilename|

-------------------------------------------------------------------------------
                                            *love2d-docs-FileData:getExtension*
|FileData:getExtension|

Gets the extension of the FileData.

Synopses: ~

        `ext` = |FileData:getExtension|()

Variants: ~

        `ext` = |FileData:getExtension|()

        See function description

        Return: ~
            (`string`)
                The extension of the file the FileData represents.
-------------------------------------------------------------------------------
                                             *love2d-docs-FileData:getFilename*
|FileData:getFilename|

Gets the filename of the FileData.

Synopses: ~

        `name` = |FileData:getFilename|()

Variants: ~

        `name` = |FileData:getFilename|()

        See function description

        Return: ~
            (`string`)
                The name of the file the FileData represents.

===============================================================================
                                                        *love2d-docs-love.font*
|love.font|

Allows you to work with fonts.

    |callbacks|                               |love2d-docs-love.font-callbacks|
    |enums|                                       |love2d-docs-love.font-enums|
    |functions|                               |love2d-docs-love.font-functions|
    |types|                                       |love2d-docs-love.font-types|

-------------------------------------------------------------------------------
                                              *love2d-docs-love.font-callbacks*
The callbacks of |love.font|:

    None

-------------------------------------------------------------------------------
                                                  *love2d-docs-love.font-enums*
The enums of |love.font|:

    |HintingMode|                                     |love2d-docs-HintingMode|

-------------------------------------------------------------------------------
                                                      *love2d-docs-HintingMode*
|HintingMode|

True Type hinting mode.

    constants: ~

        `normal`: string

            Default hinting. Should be preferred for typical antialiased fonts.

        `light`: string

            Results in fuzzier text but can sometimes preserve the original
            glyph shapes of the text better than normal hinting.

        `mono`: string

            Results in aliased / unsmoothed text with either full opacity or
            completely transparent pixels. Should be used when antialiasing is
            not desired for the font.

        `none`: string

            Disables hinting for the font. Results in fuzzier text.

-------------------------------------------------------------------------------
                                              *love2d-docs-love.font-functions*
The functions of |love.font|:

    |newBMFontRasterizer|           |love2d-docs-love.font.newBMFontRasterizer|
    |newGlyphData|                         |love2d-docs-love.font.newGlyphData|
    |newImageRasterizer|             |love2d-docs-love.font.newImageRasterizer|
    |newRasterizer|                       |love2d-docs-love.font.newRasterizer|
    |newTrueTypeRasterizer|       |love2d-docs-love.font.newTrueTypeRasterizer|

-------------------------------------------------------------------------------
                                    *love2d-docs-love.font.newBMFontRasterizer*
|love.font.newBMFontRasterizer|

Creates a new BMFont Rasterizer.

Synopses: ~

    1.  `rasterizer` = |love.font.newBMFontRasterizer|(`imageData`,
        `glyphs`, `dpiscale`)
    2.  `rasterizer` = |love.font.newBMFontRasterizer|(`fileName`,
        `glyphs`, `dpiscale`)

Variants: ~

    1.  `rasterizer` = |love.font.newBMFontRasterizer|(`imageData`,
        `glyphs`, `dpiscale`)

    1.          See function description

        Parameters: ~
            • {imageData} (`ImageData`) |love2d-docs-ImageData|
                The image data containing the drawable pictures of font glyphs.
            • {glyphs} (`string`)
                The sequence of glyphs in the ImageData.
            • {dpiscale} (`number`) (default: `1`)
                DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    2.  `rasterizer` = |love.font.newBMFontRasterizer|(`fileName`,
        `glyphs`, `dpiscale`)

    2.          See function description

        Parameters: ~
            • {fileName} (`string`)
                The path to file containing the drawable pictures of font
                glyphs.
            • {glyphs} (`string`)
                The sequence of glyphs in the ImageData.
            • {dpiscale} (`number`) (default: `1`)
                DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.font.newGlyphData*
|love.font.newGlyphData|

Creates a new GlyphData.

Synopses: ~

        |love.font.newGlyphData|(`rasterizer`, `glyph`)

Variants: ~

        |love.font.newGlyphData|(`rasterizer`, `glyph`)

        See function description

        Parameters: ~
            • {rasterizer} (`Rasterizer`) |love2d-docs-Rasterizer|
                The Rasterizer containing the font.
            • {glyph} (`number`)
                The character code of the glyph.


-------------------------------------------------------------------------------
                                     *love2d-docs-love.font.newImageRasterizer*
|love.font.newImageRasterizer|

Creates a new Image Rasterizer.

Synopses: ~

        `rasterizer` = |love.font.newImageRasterizer|(`imageData`,
        `glyphs`, `extraSpacing`, `dpiscale`)

Variants: ~

        `rasterizer` = |love.font.newImageRasterizer|(`imageData`,
        `glyphs`, `extraSpacing`, `dpiscale`)

        Create an ImageRasterizer from the image data.

        Parameters: ~
            • {imageData} (`ImageData`) |love2d-docs-ImageData|
                Font image data.
            • {glyphs} (`string`)
                String containing font glyphs.
            • {extraSpacing} (`number`) (default: `0`)
                Font extra spacing.
            • {dpiscale} (`number`) (default: `1`)
                Font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.font.newRasterizer*
|love.font.newRasterizer|

Creates a new Rasterizer.

Synopses: ~

    1.  `rasterizer` = |love.font.newRasterizer|(`filename`)
    2.  `rasterizer` = |love.font.newRasterizer|(`data`)
    3.  `rasterizer` = |love.font.newRasterizer|(`size`, `hinting`,
        `dpiscale`)
    4.  `rasterizer` = |love.font.newRasterizer|(`fileName`, `size`,
        `hinting`, `dpiscale`)
    5.  `rasterizer` = |love.font.newRasterizer|(`fileData`, `size`,
        `hinting`, `dpiscale`)
    6.  `rasterizer` = |love.font.newRasterizer|(`imageData`, `glyphs`,
        `dpiscale`)
    7.  `rasterizer` = |love.font.newRasterizer|(`fileName`, `glyphs`,
        `dpiscale`)

Variants: ~

    1.  `rasterizer` = |love.font.newRasterizer|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The font file.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    2.  `rasterizer` = |love.font.newRasterizer|(`data`)

    2.          See function description

        Parameters: ~
            • {data} (`FileData`) |love2d-docs-FileData|
                The FileData of the font file.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    3.  `rasterizer` = |love.font.newRasterizer|(`size`, `hinting`,
        `dpiscale`)

    3.          Create a TrueTypeRasterizer with the default font.

        Parameters: ~
            • {size} (`number`) (default: `12`)
                The font size.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.window.getDPIScale()`)
                The font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    4.  `rasterizer` = |love.font.newRasterizer|(`fileName`, `size`,
        `hinting`, `dpiscale`)

    4.          Create a TrueTypeRasterizer with custom font.

        Parameters: ~
            • {fileName} (`string`)
                Path to font file.
            • {size} (`number`) (default: `12`)
                The font size.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.window.getDPIScale()`)
                The font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    5.  `rasterizer` = |love.font.newRasterizer|(`fileData`, `size`,
        `hinting`, `dpiscale`)

    5.          Create a TrueTypeRasterizer with custom font.

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                File data containing font.
            • {size} (`number`) (default: `12`)
                The font size.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.window.getDPIScale()`)
                The font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    6.  `rasterizer` = |love.font.newRasterizer|(`imageData`, `glyphs`,
        `dpiscale`)

    6.          Creates a new BMFont Rasterizer.

        Parameters: ~
            • {imageData} (`ImageData`) |love2d-docs-ImageData|
                The image data containing the drawable pictures of font glyphs.
            • {glyphs} (`string`)
                The sequence of glyphs in the ImageData.
            • {dpiscale} (`number`) (default: `1`)
                DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    7.  `rasterizer` = |love.font.newRasterizer|(`fileName`, `glyphs`,
        `dpiscale`)

    7.          Creates a new BMFont Rasterizer.

        Parameters: ~
            • {fileName} (`string`)
                The path to file containing the drawable pictures of font
                glyphs.
            • {glyphs} (`string`)
                The sequence of glyphs in the ImageData.
            • {dpiscale} (`number`) (default: `1`)
                DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.font.newTrueTypeRasterizer*
|love.font.newTrueTypeRasterizer|

Creates a new TrueType Rasterizer.

Synopses: ~

    1.  `rasterizer` = |love.font.newTrueTypeRasterizer|(`size`,
        `hinting`, `dpiscale`)
    2.  `rasterizer` = |love.font.newTrueTypeRasterizer|(`fileName`,
        `size`, `hinting`, `dpiscale`)
    3.  `rasterizer` = |love.font.newTrueTypeRasterizer|(`fileData`,
        `size`, `hinting`, `dpiscale`)

Variants: ~

    1.  `rasterizer` = |love.font.newTrueTypeRasterizer|(`size`,
        `hinting`, `dpiscale`)

    1.          Create a TrueTypeRasterizer with the default font.

        Parameters: ~
            • {size} (`number`) (default: `12`)
                The font size.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.window.getDPIScale()`)
                The font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    2.  `rasterizer` = |love.font.newTrueTypeRasterizer|(`fileName`,
        `size`, `hinting`, `dpiscale`)

    2.          Create a TrueTypeRasterizer with custom font.

        Parameters: ~
            • {fileName} (`string`)
                Path to font file.
            • {size} (`number`) (default: `12`)
                The font size.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.window.getDPIScale()`)
                The font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
    3.  `rasterizer` = |love.font.newTrueTypeRasterizer|(`fileData`,
        `size`, `hinting`, `dpiscale`)

    3.          Create a TrueTypeRasterizer with custom font.

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                File data containing font.
            • {size} (`number`) (default: `12`)
                The font size.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.window.getDPIScale()`)
                The font DPI scale.

        Return: ~
            (`Rasterizer`) |love2d-docs-Rasterizer|
                The rasterizer.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.font-types*
The types of |love.font|:

    |GlyphData|                                         |love2d-docs-GlyphData|
    |Rasterizer|                                       |love2d-docs-Rasterizer|

-------------------------------------------------------------------------------
                                                        *love2d-docs-GlyphData*
|GlyphData|

A GlyphData represents a drawable symbol of a font Rasterizer.

                                           *love2d-docs-GlyphData-constructors*
    constructors:

        None

                                             *love2d-docs-GlyphData-supertypes*
    supertypes:

        |Data|                                               |love2d-docs-Data|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-GlyphData-subtypes*
    subtypes:

        None

                                              *love2d-docs-GlyphData-functions*
    functions:

        |getAdvance|                         |love2d-docs-GlyphData:getAdvance|
        |getBearing|                         |love2d-docs-GlyphData:getBearing|
        |getBoundingBox|                 |love2d-docs-GlyphData:getBoundingBox|
        |getDimensions|                   |love2d-docs-GlyphData:getDimensions|
        |getFormat|                           |love2d-docs-GlyphData:getFormat|
        |getGlyph|                             |love2d-docs-GlyphData:getGlyph|
        |getGlyphString|                 |love2d-docs-GlyphData:getGlyphString|
        |getHeight|                           |love2d-docs-GlyphData:getHeight|
        |getWidth|                             |love2d-docs-GlyphData:getWidth|

-------------------------------------------------------------------------------
                                             *love2d-docs-GlyphData:getAdvance*
|GlyphData:getAdvance|

Gets glyph advance.

Synopses: ~

        `advance` = |GlyphData:getAdvance|()

Variants: ~

        `advance` = |GlyphData:getAdvance|()

        See function description

        Return: ~
            (`number`)
                Glyph advance.
-------------------------------------------------------------------------------
                                             *love2d-docs-GlyphData:getBearing*
|GlyphData:getBearing|

Gets glyph bearing.

Synopses: ~

        `bx`, `by` = |GlyphData:getBearing|()

Variants: ~

        `bx`, `by` = |GlyphData:getBearing|()

        See function description

        Returns: ~
            1. (`number`)
                Glyph bearing X.

            2. (`number`)
                Glyph bearing Y.
-------------------------------------------------------------------------------
                                         *love2d-docs-GlyphData:getBoundingBox*
|GlyphData:getBoundingBox|

Gets glyph bounding box.

Synopses: ~

        `x`, `y`, `width`, `height` = |GlyphData:getBoundingBox|()

Variants: ~

        `x`, `y`, `width`, `height` = |GlyphData:getBoundingBox|()

        See function description

        Returns: ~
            1. (`number`)
                Glyph position x.

            2. (`number`)
                Glyph position y.

            3. (`number`)
                Glyph width.

            4. (`number`)
                Glyph height.
-------------------------------------------------------------------------------
                                          *love2d-docs-GlyphData:getDimensions*
|GlyphData:getDimensions|

Gets glyph dimensions.

Synopses: ~

        `width`, `height` = |GlyphData:getDimensions|()

Variants: ~

        `width`, `height` = |GlyphData:getDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                Glyph width.

            2. (`number`)
                Glyph height.
-------------------------------------------------------------------------------
                                              *love2d-docs-GlyphData:getFormat*
|GlyphData:getFormat|

Gets glyph pixel format.

Synopses: ~

        `format` = |GlyphData:getFormat|()

Variants: ~

        `format` = |GlyphData:getFormat|()

        See function description

        Return: ~
            (`PixelFormat`) |love2d-docs-PixelFormat|
                Glyph pixel format.
-------------------------------------------------------------------------------
                                               *love2d-docs-GlyphData:getGlyph*
|GlyphData:getGlyph|

Gets glyph number.

Synopses: ~

        `glyph` = |GlyphData:getGlyph|()

Variants: ~

        `glyph` = |GlyphData:getGlyph|()

        See function description

        Return: ~
            (`number`)
                Glyph number.
-------------------------------------------------------------------------------
                                         *love2d-docs-GlyphData:getGlyphString*
|GlyphData:getGlyphString|

Gets glyph string.

Synopses: ~

        `glyph` = |GlyphData:getGlyphString|()

Variants: ~

        `glyph` = |GlyphData:getGlyphString|()

        See function description

        Return: ~
            (`string`)
                Glyph string.
-------------------------------------------------------------------------------
                                              *love2d-docs-GlyphData:getHeight*
|GlyphData:getHeight|

Gets glyph height.

Synopses: ~

        `height` = |GlyphData:getHeight|()

Variants: ~

        `height` = |GlyphData:getHeight|()

        See function description

        Return: ~
            (`number`)
                Glyph height.
-------------------------------------------------------------------------------
                                               *love2d-docs-GlyphData:getWidth*
|GlyphData:getWidth|

Gets glyph width.

Synopses: ~

        `width` = |GlyphData:getWidth|()

Variants: ~

        `width` = |GlyphData:getWidth|()

        See function description

        Return: ~
            (`number`)
                Glyph width.

-------------------------------------------------------------------------------
                                                       *love2d-docs-Rasterizer*
|Rasterizer|

A Rasterizer handles font rendering, containing the font data (image or
TrueType font) and drawable glyphs.

                                          *love2d-docs-Rasterizer-constructors*
    constructors:

        |newTrueTypeRasterizer|             |love2d-docs-newTrueTypeRasterizer|
        |newRasterizer|                             |love2d-docs-newRasterizer|
        |newImageRasterizer|                   |love2d-docs-newImageRasterizer|
        |newBMFontRasterizer|                 |love2d-docs-newBMFontRasterizer|

                                            *love2d-docs-Rasterizer-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                              *love2d-docs-Rasterizer-subtypes*
    subtypes:

        None

                                             *love2d-docs-Rasterizer-functions*
    functions:

        |getAdvance|                        |love2d-docs-Rasterizer:getAdvance|
        |getAscent|                          |love2d-docs-Rasterizer:getAscent|
        |getDescent|                        |love2d-docs-Rasterizer:getDescent|
        |getGlyphCount|                  |love2d-docs-Rasterizer:getGlyphCount|
        |getGlyphData|                    |love2d-docs-Rasterizer:getGlyphData|
        |getHeight|                          |love2d-docs-Rasterizer:getHeight|
        |getLineHeight|                  |love2d-docs-Rasterizer:getLineHeight|
        |hasGlyphs|                          |love2d-docs-Rasterizer:hasGlyphs|

-------------------------------------------------------------------------------
                                            *love2d-docs-Rasterizer:getAdvance*
|Rasterizer:getAdvance|

Gets font advance.

Synopses: ~

        `advance` = |Rasterizer:getAdvance|()

Variants: ~

        `advance` = |Rasterizer:getAdvance|()

        See function description

        Return: ~
            (`number`)
                Font advance.
-------------------------------------------------------------------------------
                                             *love2d-docs-Rasterizer:getAscent*
|Rasterizer:getAscent|

Gets ascent height.

Synopses: ~

        `height` = |Rasterizer:getAscent|()

Variants: ~

        `height` = |Rasterizer:getAscent|()

        See function description

        Return: ~
            (`number`)
                Ascent height.
-------------------------------------------------------------------------------
                                            *love2d-docs-Rasterizer:getDescent*
|Rasterizer:getDescent|

Gets descent height.

Synopses: ~

        `height` = |Rasterizer:getDescent|()

Variants: ~

        `height` = |Rasterizer:getDescent|()

        See function description

        Return: ~
            (`number`)
                Descent height.
-------------------------------------------------------------------------------
                                         *love2d-docs-Rasterizer:getGlyphCount*
|Rasterizer:getGlyphCount|

Gets number of glyphs in font.

Synopses: ~

        `count` = |Rasterizer:getGlyphCount|()

Variants: ~

        `count` = |Rasterizer:getGlyphCount|()

        See function description

        Return: ~
            (`number`)
                Glyphs count.
-------------------------------------------------------------------------------
                                          *love2d-docs-Rasterizer:getGlyphData*
|Rasterizer:getGlyphData|

Gets glyph data of a specified glyph.

Synopses: ~

    1.  `glyphData` = |Rasterizer:getGlyphData|(`glyph`)
    2.  `glyphData` = |Rasterizer:getGlyphData|(`glyphNumber`)

Variants: ~

    1.  `glyphData` = |Rasterizer:getGlyphData|(`glyph`)

    1.          See function description

        Parameters: ~
            • {glyph} (`string`)
                Glyph

        Return: ~
            (`GlyphData`) |love2d-docs-GlyphData|
                Glyph data
    2.  `glyphData` = |Rasterizer:getGlyphData|(`glyphNumber`)

    2.          See function description

        Parameters: ~
            • {glyphNumber} (`number`)
                Glyph number

        Return: ~
            (`GlyphData`) |love2d-docs-GlyphData|
                Glyph data
-------------------------------------------------------------------------------
                                             *love2d-docs-Rasterizer:getHeight*
|Rasterizer:getHeight|

Gets font height.

Synopses: ~

        `height` = |Rasterizer:getHeight|()

Variants: ~

        `height` = |Rasterizer:getHeight|()

        See function description

        Return: ~
            (`number`)
                Font height
-------------------------------------------------------------------------------
                                         *love2d-docs-Rasterizer:getLineHeight*
|Rasterizer:getLineHeight|

Gets line height of a font.

Synopses: ~

        `height` = |Rasterizer:getLineHeight|()

Variants: ~

        `height` = |Rasterizer:getLineHeight|()

        See function description

        Return: ~
            (`number`)
                Line height of a font.
-------------------------------------------------------------------------------
                                             *love2d-docs-Rasterizer:hasGlyphs*
|Rasterizer:hasGlyphs|

Checks if font contains specified glyphs.

Synopses: ~

        `hasGlyphs` = |Rasterizer:hasGlyphs|(`glyph1`, `...`)

Variants: ~

        `hasGlyphs` = |Rasterizer:hasGlyphs|(`glyph1`, `...`)

        See function description

        Parameters: ~
            • {glyph1} (`stringornumber`)
                Glyph
            • {...} (`stringornumber`)
                Additional glyphs

        Return: ~
            (`boolean`)
                Whatever font contains specified glyphs.

===============================================================================
                                                    *love2d-docs-love.graphics*
|love.graphics|

The primary responsibility for the love.graphics module is the drawing of
lines, shapes, text, Images and other Drawable objects onto the screen. Its
secondary responsibilities include loading external files (including Images and
Fonts) into memory, creating specialized objects (such as ParticleSystems or
Canvases) and managing screen geometry.

LÖVE's coordinate system is rooted in the upper-left corner of the screen,
which is at location (0, 0). The x axis is horizontal: larger values are
further to the right. The y axis is vertical: larger values are further towards
the bottom.

In many cases, you draw images or shapes in terms of their upper-left corner.

Many of the functions are used to manipulate the graphics coordinate system,
which is essentially the way coordinates are mapped to the display. You can
change the position, scale, and even rotation in this way.

    |callbacks|                           |love2d-docs-love.graphics-callbacks|
    |enums|                                   |love2d-docs-love.graphics-enums|
    |functions|                           |love2d-docs-love.graphics-functions|
    |types|                                   |love2d-docs-love.graphics-types|

-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics-callbacks*
The callbacks of |love.graphics|:

    None

-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics-enums*
The enums of |love.graphics|:

    |AlignMode|                                         |love2d-docs-AlignMode|
    |ArcType|                                             |love2d-docs-ArcType|
    |AreaSpreadDistribution|               |love2d-docs-AreaSpreadDistribution|
    |BlendAlphaMode|                               |love2d-docs-BlendAlphaMode|
    |BlendMode|                                         |love2d-docs-BlendMode|
    |CompareMode|                                     |love2d-docs-CompareMode|
    |CullMode|                                           |love2d-docs-CullMode|
    |DrawMode|                                           |love2d-docs-DrawMode|
    |FilterMode|                                       |love2d-docs-FilterMode|
    |GraphicsFeature|                             |love2d-docs-GraphicsFeature|
    |GraphicsLimit|                                 |love2d-docs-GraphicsLimit|
    |IndexDataType|                                 |love2d-docs-IndexDataType|
    |LineJoin|                                           |love2d-docs-LineJoin|
    |LineStyle|                                         |love2d-docs-LineStyle|
    |MeshDrawMode|                                   |love2d-docs-MeshDrawMode|
    |MipmapMode|                                       |love2d-docs-MipmapMode|
    |ParticleInsertMode|                       |love2d-docs-ParticleInsertMode|
    |SpriteBatchUsage|                           |love2d-docs-SpriteBatchUsage|
    |StackType|                                         |love2d-docs-StackType|
    |StencilAction|                                 |love2d-docs-StencilAction|
    |TextureType|                                     |love2d-docs-TextureType|
    |VertexAttributeStep|                     |love2d-docs-VertexAttributeStep|
    |VertexWinding|                                 |love2d-docs-VertexWinding|
    |WrapMode|                                           |love2d-docs-WrapMode|

-------------------------------------------------------------------------------
                                                        *love2d-docs-AlignMode*
|AlignMode|

Text alignment.

    constants: ~

        `center`: string

            Align text center.

        `left`: string

            Align text left.

        `right`: string

            Align text right.

        `justify`: string

            Align text both left and right.

-------------------------------------------------------------------------------
                                                          *love2d-docs-ArcType*
|ArcType|

Different types of arcs that can be drawn.

    constants: ~

        `pie`: string

            The arc is drawn like a slice of pie, with the arc circle connected
            to the center at its end-points.

        `open`: string

            The arc circle's two end-points are unconnected when the arc is
            drawn as a line. Behaves like the "closed" arc type when the arc is
            drawn in filled mode.

        `closed`: string

            The arc circle's two end-points are connected to each other.

-------------------------------------------------------------------------------
                                           *love2d-docs-AreaSpreadDistribution*
|AreaSpreadDistribution|

Types of particle area spread distribution.

    constants: ~

        `uniform`: string

            Uniform distribution.

        `normal`: string

            Normal (gaussian) distribution.

        `ellipse`: string

            Uniform distribution in an ellipse.

        `borderellipse`: string

            Distribution in an ellipse with particles spawning at the edges of
            the ellipse.

        `borderrectangle`: string

            Distribution in a rectangle with particles spawning at the edges of
            the rectangle.

        `none`: string

            No distribution - area spread is disabled.

-------------------------------------------------------------------------------
                                                   *love2d-docs-BlendAlphaMode*
|BlendAlphaMode|

Different ways alpha affects color blending. See BlendMode and the BlendMode
Formulas for additional notes.

    constants: ~

        `alphamultiply`: string

            The RGB values of what's drawn are multiplied by the alpha values
            of those colors during blending. This is the default alpha mode.

        `premultiplied`: string

            The RGB values of what's drawn are '''not''' multiplied by the
            alpha values of those colors during blending. For most blend modes
            to work correctly with this alpha mode, the colors of a drawn
            object need to have had their RGB values multiplied by their alpha
            values at some point previously ("premultiplied alpha").

-------------------------------------------------------------------------------
                                                        *love2d-docs-BlendMode*
|BlendMode|

Different ways to do color blending. See BlendAlphaMode and the BlendMode
Formulas for additional notes.

    constants: ~

        `alpha`: string

            Alpha blending (normal). The alpha of what's drawn determines its
            opacity.

        `replace`: string

            The colors of what's drawn completely replace what was on the
            screen, with no additional blending. The BlendAlphaMode specified
            in love.graphics.setBlendMode still affects what happens.

        `screen`: string

            'Screen' blending.

        `add`: string

            The pixel colors of what's drawn are added to the pixel colors
            already on the screen. The alpha of the screen is not modified.

        `subtract`: string

            The pixel colors of what's drawn are subtracted from the pixel
            colors already on the screen. The alpha of the screen is not
            modified.

        `multiply`: string

            The pixel colors of what's drawn are multiplied with the pixel
            colors already on the screen (darkening them). The alpha of drawn
            objects is multiplied with the alpha of the screen rather than
            determining how much the colors on the screen are affected, even
            when the "alphamultiply" BlendAlphaMode is used.

        `lighten`: string

            The pixel colors of what's drawn are compared to the existing pixel
            colors, and the larger of the two values for each color component
            is used. Only works when the "premultiplied" BlendAlphaMode is used
            in love.graphics.setBlendMode.

        `darken`: string

            The pixel colors of what's drawn are compared to the existing pixel
            colors, and the smaller of the two values for each color component
            is used. Only works when the "premultiplied" BlendAlphaMode is used
            in love.graphics.setBlendMode.

        `additive`: string

            Additive blend mode.

        `subtractive`: string

            Subtractive blend mode.

        `multiplicative`: string

            Multiply blend mode.

        `premultiplied`: string

            Premultiplied alpha blend mode.

-------------------------------------------------------------------------------
                                                      *love2d-docs-CompareMode*
|CompareMode|

Different types of per-pixel stencil test and depth test comparisons. The
pixels of an object will be drawn if the comparison succeeds, for each pixel
that the object touches.

    constants: ~

        `equal`: string

            * stencil tests: the stencil value of the pixel must be equal to
            the supplied value.
            * depth tests: the depth value of the drawn object at that pixel
            must be equal to the existing depth value of that pixel.

        `notequal`: string

            * stencil tests: the stencil value of the pixel must not be equal
            to the supplied value.
            * depth tests: the depth value of the drawn object at that pixel
            must not be equal to the existing depth value of that pixel.

        `less`: string

            * stencil tests: the stencil value of the pixel must be less than
            the supplied value.
            * depth tests: the depth value of the drawn object at that pixel
            must be less than the existing depth value of that pixel.

        `lequal`: string

            * stencil tests: the stencil value of the pixel must be less than
            or equal to the supplied value.
            * depth tests: the depth value of the drawn object at that pixel
            must be less than or equal to the existing depth value of that
            pixel.

        `gequal`: string

            * stencil tests: the stencil value of the pixel must be greater
            than or equal to the supplied value.
            * depth tests: the depth value of the drawn object at that pixel
            must be greater than or equal to the existing depth value of that
            pixel.

        `greater`: string

            * stencil tests: the stencil value of the pixel must be greater
            than the supplied value.
            * depth tests: the depth value of the drawn object at that pixel
            must be greater than the existing depth value of that pixel.

        `never`: string

            Objects will never be drawn.

        `always`: string

            Objects will always be drawn. Effectively disables the depth or
            stencil test.

-------------------------------------------------------------------------------
                                                         *love2d-docs-CullMode*
|CullMode|

How Mesh geometry is culled when rendering.

    constants: ~

        `back`: string

            Back-facing triangles in Meshes are culled (not rendered). The
            vertex order of a triangle determines whether it is back- or
            front-facing.

        `front`: string

            Front-facing triangles in Meshes are culled.

        `none`: string

            Both back- and front-facing triangles in Meshes are rendered.

-------------------------------------------------------------------------------
                                                         *love2d-docs-DrawMode*
|DrawMode|

Controls whether shapes are drawn as an outline, or filled.

    constants: ~

        `fill`: string

            Draw filled shape.

        `line`: string

            Draw outlined shape.

-------------------------------------------------------------------------------
                                                       *love2d-docs-FilterMode*
|FilterMode|

How the image is filtered when scaling.

    constants: ~

        `linear`: string

            Scale image with linear interpolation.

        `nearest`: string

            Scale image with nearest neighbor interpolation.

-------------------------------------------------------------------------------
                                                  *love2d-docs-GraphicsFeature*
|GraphicsFeature|

Graphics features that can be checked for with love.graphics.getSupported.

    constants: ~

        `clampzero`: string

            Whether the "clampzero" WrapMode is supported.

        `lighten`: string

            Whether the "lighten" and "darken" BlendModes are supported.

        `multicanvasformats`: string

            Whether multiple formats can be used in the same
            love.graphics.setCanvas call.

        `glsl3`: string

            Whether GLSL 3 Shaders can be used.

        `instancing`: string

            Whether mesh instancing is supported.

        `fullnpot`: string

            Whether textures with non-power-of-two dimensions can use
            mipmapping and the 'repeat' WrapMode.

        `pixelshaderhighp`: string

            Whether pixel shaders can use "highp" 32 bit floating point numbers
            (as opposed to just 16 bit or lower precision).

        `shaderderivatives`: string

            Whether shaders can use the dFdx, dFdy, and fwidth functions for
            computing derivatives.

-------------------------------------------------------------------------------
                                                    *love2d-docs-GraphicsLimit*
|GraphicsLimit|

Types of system-dependent graphics limits checked for using
love.graphics.getSystemLimits.

    constants: ~

        `pointsize`: string

            The maximum size of points.

        `texturesize`: string

            The maximum width or height of Images and Canvases.

        `multicanvas`: string

            The maximum number of simultaneously active canvases (via
            love.graphics.setCanvas.)

        `canvasmsaa`: string

            The maximum number of antialiasing samples for a Canvas.

        `texturelayers`: string

            The maximum number of layers in an Array texture.

        `volumetexturesize`: string

            The maximum width, height, or depth of a Volume texture.

        `cubetexturesize`: string

            The maximum width or height of a Cubemap texture.

        `anisotropy`: string

            The maximum amount of anisotropic filtering.
            Texture:setMipmapFilter internally clamps the given anisotropy
            value to the system's limit.

-------------------------------------------------------------------------------
                                                    *love2d-docs-IndexDataType*
|IndexDataType|

Vertex map datatype for Data variant of Mesh:setVertexMap.

    constants: ~

        `uint16`: string

            The vertex map is array of unsigned word (16-bit).

        `uint32`: string

            The vertex map is array of unsigned dword (32-bit).

-------------------------------------------------------------------------------
                                                         *love2d-docs-LineJoin*
|LineJoin|

Line join style.

    constants: ~

        `miter`: string

            The ends of the line segments beveled in an angle so that they join
            seamlessly.

        `none`: string

            No cap applied to the ends of the line segments.

        `bevel`: string

            Flattens the point where line segments join together.

-------------------------------------------------------------------------------
                                                        *love2d-docs-LineStyle*
|LineStyle|

The styles in which lines are drawn.

    constants: ~

        `rough`: string

            Draw rough lines.

        `smooth`: string

            Draw smooth lines.

-------------------------------------------------------------------------------
                                                     *love2d-docs-MeshDrawMode*
|MeshDrawMode|

How a Mesh's vertices are used when drawing.

    constants: ~

        `fan`: string

            The vertices create a "fan" shape with the first vertex acting as
            the hub point. Can be easily used to draw simple convex polygons.

        `strip`: string

            The vertices create a series of connected triangles using vertices
            1, 2, 3, then 3, 2, 4 (note the order), then 3, 4, 5, and so on.

        `triangles`: string

            The vertices create unconnected triangles.

        `points`: string

            The vertices are drawn as unconnected points (see
            love.graphics.setPointSize.)

-------------------------------------------------------------------------------
                                                       *love2d-docs-MipmapMode*
|MipmapMode|

Controls whether a Canvas has mipmaps, and its behaviour when it does.

    constants: ~

        `none`: string

            The Canvas has no mipmaps.

        `auto`: string

            The Canvas has mipmaps. love.graphics.setCanvas can be used to
            render to a specific mipmap level, or Canvas:generateMipmaps can
            (re-)compute all mipmap levels based on the base level.

        `manual`: string

            The Canvas has mipmaps, and all mipmap levels will automatically be
            recomputed when switching away from the Canvas with
            love.graphics.setCanvas.

-------------------------------------------------------------------------------
                                               *love2d-docs-ParticleInsertMode*
|ParticleInsertMode|

How newly created particles are added to the ParticleSystem.

    constants: ~

        `top`: string

            Particles are inserted at the top of the ParticleSystem's list of
            particles.

        `bottom`: string

            Particles are inserted at the bottom of the ParticleSystem's list
            of particles.

        `random`: string

            Particles are inserted at random positions in the ParticleSystem's
            list of particles.

-------------------------------------------------------------------------------
                                                 *love2d-docs-SpriteBatchUsage*
|SpriteBatchUsage|

Usage hints for SpriteBatches and Meshes to optimize data storage and access.

    constants: ~

        `dynamic`: string

            The object's data will change occasionally during its lifetime.

        `static`: string

            The object will not be modified after initial sprites or vertices
            are added.

        `stream`: string

            The object data will always change between draws.

-------------------------------------------------------------------------------
                                                        *love2d-docs-StackType*
|StackType|

Graphics state stack types used with love.graphics.push.

    constants: ~

        `transform`: string

            The transformation stack (love.graphics.translate,
            love.graphics.rotate, etc.)

        `all`: string

            All love.graphics state, including transform state.

-------------------------------------------------------------------------------
                                                    *love2d-docs-StencilAction*
|StencilAction|

How a stencil function modifies the stencil values of pixels it touches.

    constants: ~

        `replace`: string

            The stencil value of a pixel will be replaced by the value
            specified in love.graphics.stencil, if any object touches the
            pixel.

        `increment`: string

            The stencil value of a pixel will be incremented by 1 for each
            object that touches the pixel. If the stencil value reaches 255 it
            will stay at 255.

        `decrement`: string

            The stencil value of a pixel will be decremented by 1 for each
            object that touches the pixel. If the stencil value reaches 0 it
            will stay at 0.

        `incrementwrap`: string

            The stencil value of a pixel will be incremented by 1 for each
            object that touches the pixel. If a stencil value of 255 is
            incremented it will be set to 0.

        `decrementwrap`: string

            The stencil value of a pixel will be decremented by 1 for each
            object that touches the pixel. If the stencil value of 0 is
            decremented it will be set to 255.

        `invert`: string

            The stencil value of a pixel will be bitwise-inverted for each
            object that touches the pixel. If a stencil value of 0 is inverted
            it will become 255.

-------------------------------------------------------------------------------
                                                      *love2d-docs-TextureType*
|TextureType|

Types of textures (2D, cubemap, etc.)

    constants: ~

        `2d`: string

            Regular 2D texture with width and height.

        `array`: string

            Several same-size 2D textures organized into a single object.
            Similar to a texture atlas / sprite sheet, but avoids sprite
            bleeding and other issues.

        `cube`: string

            Cubemap texture with 6 faces. Requires a custom shader (and
            Shader:send) to use. Sampling from a cube texture in a shader takes
            a 3D direction vector instead of a texture coordinate.

        `volume`: string

            3D texture with width, height, and depth. Requires a custom shader
            to use. Volume textures can have texture filtering applied along
            the 3rd axis.

-------------------------------------------------------------------------------
                                              *love2d-docs-VertexAttributeStep*
|VertexAttributeStep|

The frequency at which a vertex shader fetches the vertex attribute's data from
the Mesh when it's drawn.

Per-instance attributes can be used to render a Mesh many times with different
positions, colors, or other attributes via a single love.graphics.drawInstanced
call, without using the love_InstanceID vertex shader variable.

    constants: ~

        `pervertex`: string

            The vertex attribute will have a unique value for each vertex in
            the Mesh.

        `perinstance`: string

            The vertex attribute will have a unique value for each instance of
            the Mesh.

-------------------------------------------------------------------------------
                                                    *love2d-docs-VertexWinding*
|VertexWinding|

How Mesh geometry vertices are ordered.

    constants: ~

        `cw`: string

            Clockwise.

        `ccw`: string

            Counter-clockwise.

-------------------------------------------------------------------------------
                                                         *love2d-docs-WrapMode*
|WrapMode|

How the image wraps inside a Quad with a larger quad size than image size. This
also affects how Meshes with texture coordinates which are outside the range of
1 are drawn, and the color returned by the Texel Shader function when using it
to sample from texture coordinates outside of the range of 1.

    constants: ~

        `clamp`: string

            Clamp the texture. Appears only once. The area outside the
            texture's normal range is colored based on the edge pixels of the
            texture.

        `repeat`: string

            Repeat the texture. Fills the whole available extent.

        `mirroredrepeat`: string

            Repeat the texture, flipping it each time it repeats. May produce
            better visual results than the repeat mode when the texture doesn't
            seamlessly tile.

        `clampzero`: string

            Clamp the texture. Fills the area outside the texture's normal
            range with transparent black (or opaque black for textures with no
            alpha channel.)

-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics-functions*
The functions of |love.graphics|:

    |applyTransform|                 |love2d-docs-love.graphics.applyTransform|
    |arc|                                       |love2d-docs-love.graphics.arc|
    |captureScreenshot|           |love2d-docs-love.graphics.captureScreenshot|
    |circle|                                 |love2d-docs-love.graphics.circle|
    |clear|                                   |love2d-docs-love.graphics.clear|
    |discard|                               |love2d-docs-love.graphics.discard|
    |draw|                                     |love2d-docs-love.graphics.draw|
    |drawInstanced|                   |love2d-docs-love.graphics.drawInstanced|
    |drawLayer|                           |love2d-docs-love.graphics.drawLayer|
    |ellipse|                               |love2d-docs-love.graphics.ellipse|
    |flushBatch|                         |love2d-docs-love.graphics.flushBatch|
    |getBackgroundColor|         |love2d-docs-love.graphics.getBackgroundColor|
    |getBlendMode|                     |love2d-docs-love.graphics.getBlendMode|
    |getCanvas|                           |love2d-docs-love.graphics.getCanvas|
    |getCanvasFormats|             |love2d-docs-love.graphics.getCanvasFormats|
    |getColor|                             |love2d-docs-love.graphics.getColor|
    |getColorMask|                     |love2d-docs-love.graphics.getColorMask|
    |getDPIScale|                       |love2d-docs-love.graphics.getDPIScale|
    |getDefaultFilter|             |love2d-docs-love.graphics.getDefaultFilter|
    |getDepthMode|                     |love2d-docs-love.graphics.getDepthMode|
    |getDimensions|                   |love2d-docs-love.graphics.getDimensions|
    |getFont|                               |love2d-docs-love.graphics.getFont|
    |getFrontFaceWinding|       |love2d-docs-love.graphics.getFrontFaceWinding|
    |getHeight|                           |love2d-docs-love.graphics.getHeight|
    |getImageFormats|               |love2d-docs-love.graphics.getImageFormats|
    |getLineJoin|                       |love2d-docs-love.graphics.getLineJoin|
    |getLineStyle|                     |love2d-docs-love.graphics.getLineStyle|
    |getLineWidth|                     |love2d-docs-love.graphics.getLineWidth|
    |getMeshCullMode|               |love2d-docs-love.graphics.getMeshCullMode|
    |getPixelDimensions|         |love2d-docs-love.graphics.getPixelDimensions|
    |getPixelHeight|                 |love2d-docs-love.graphics.getPixelHeight|
    |getPixelWidth|                   |love2d-docs-love.graphics.getPixelWidth|
    |getPointSize|                     |love2d-docs-love.graphics.getPointSize|
    |getRendererInfo|               |love2d-docs-love.graphics.getRendererInfo|
    |getScissor|                         |love2d-docs-love.graphics.getScissor|
    |getShader|                           |love2d-docs-love.graphics.getShader|
    |getStackDepth|                   |love2d-docs-love.graphics.getStackDepth|
    |getStats|                             |love2d-docs-love.graphics.getStats|
    |getStencilTest|                 |love2d-docs-love.graphics.getStencilTest|
    |getSupported|                     |love2d-docs-love.graphics.getSupported|
    |getSystemLimits|               |love2d-docs-love.graphics.getSystemLimits|
    |getTextureTypes|               |love2d-docs-love.graphics.getTextureTypes|
    |getWidth|                             |love2d-docs-love.graphics.getWidth|
    |intersectScissor|             |love2d-docs-love.graphics.intersectScissor|
    |inverseTransformPoint|   |love2d-docs-love.graphics.inverseTransformPoint|
    |isActive|                             |love2d-docs-love.graphics.isActive|
    |isGammaCorrect|                 |love2d-docs-love.graphics.isGammaCorrect|
    |isWireframe|                       |love2d-docs-love.graphics.isWireframe|
    |line|                                     |love2d-docs-love.graphics.line|
    |newArrayImage|                   |love2d-docs-love.graphics.newArrayImage|
    |newCanvas|                           |love2d-docs-love.graphics.newCanvas|
    |newCubeImage|                     |love2d-docs-love.graphics.newCubeImage|
    |newFont|                               |love2d-docs-love.graphics.newFont|
    |newImage|                             |love2d-docs-love.graphics.newImage|
    |newImageFont|                     |love2d-docs-love.graphics.newImageFont|
    |newMesh|                               |love2d-docs-love.graphics.newMesh|
    |newParticleSystem|           |love2d-docs-love.graphics.newParticleSystem|
    |newQuad|                               |love2d-docs-love.graphics.newQuad|
    |newShader|                           |love2d-docs-love.graphics.newShader|
    |newSpriteBatch|                 |love2d-docs-love.graphics.newSpriteBatch|
    |newText|                               |love2d-docs-love.graphics.newText|
    |newVideo|                             |love2d-docs-love.graphics.newVideo|
    |newVolumeImage|                 |love2d-docs-love.graphics.newVolumeImage|
    |origin|                                 |love2d-docs-love.graphics.origin|
    |points|                                 |love2d-docs-love.graphics.points|
    |polygon|                               |love2d-docs-love.graphics.polygon|
    |pop|                                       |love2d-docs-love.graphics.pop|
    |present|                               |love2d-docs-love.graphics.present|
    |print|                                   |love2d-docs-love.graphics.print|
    |printf|                                 |love2d-docs-love.graphics.printf|
    |push|                                     |love2d-docs-love.graphics.push|
    |rectangle|                           |love2d-docs-love.graphics.rectangle|
    |replaceTransform|             |love2d-docs-love.graphics.replaceTransform|
    |reset|                                   |love2d-docs-love.graphics.reset|
    |rotate|                                 |love2d-docs-love.graphics.rotate|
    |scale|                                   |love2d-docs-love.graphics.scale|
    |setBackgroundColor|         |love2d-docs-love.graphics.setBackgroundColor|
    |setBlendMode|                     |love2d-docs-love.graphics.setBlendMode|
    |setCanvas|                           |love2d-docs-love.graphics.setCanvas|
    |setColor|                             |love2d-docs-love.graphics.setColor|
    |setColorMask|                     |love2d-docs-love.graphics.setColorMask|
    |setDefaultFilter|             |love2d-docs-love.graphics.setDefaultFilter|
    |setDepthMode|                     |love2d-docs-love.graphics.setDepthMode|
    |setFont|                               |love2d-docs-love.graphics.setFont|
    |setFrontFaceWinding|       |love2d-docs-love.graphics.setFrontFaceWinding|
    |setLineJoin|                       |love2d-docs-love.graphics.setLineJoin|
    |setLineStyle|                     |love2d-docs-love.graphics.setLineStyle|
    |setLineWidth|                     |love2d-docs-love.graphics.setLineWidth|
    |setMeshCullMode|               |love2d-docs-love.graphics.setMeshCullMode|
    |setNewFont|                         |love2d-docs-love.graphics.setNewFont|
    |setPointSize|                     |love2d-docs-love.graphics.setPointSize|
    |setScissor|                         |love2d-docs-love.graphics.setScissor|
    |setShader|                           |love2d-docs-love.graphics.setShader|
    |setStencilTest|                 |love2d-docs-love.graphics.setStencilTest|
    |setWireframe|                     |love2d-docs-love.graphics.setWireframe|
    |shear|                                   |love2d-docs-love.graphics.shear|
    |stencil|                               |love2d-docs-love.graphics.stencil|
    |transformPoint|                 |love2d-docs-love.graphics.transformPoint|
    |translate|                           |love2d-docs-love.graphics.translate|
    |validateShader|                 |love2d-docs-love.graphics.validateShader|

-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.applyTransform*
|love.graphics.applyTransform|

Applies the given Transform object to the current coordinate transformation.

This effectively multiplies the existing coordinate transformation's matrix
with the Transform object's internal matrix to produce the new coordinate
transformation.

Synopses: ~

        |love.graphics.applyTransform|(`transform`)

Variants: ~

        |love.graphics.applyTransform|(`transform`)

        See function description

        Parameters: ~
            • {transform} (`Transform`) |love2d-docs-Transform|
                The Transform object to apply to the current graphics
                coordinate transform.


-------------------------------------------------------------------------------
                                                *love2d-docs-love.graphics.arc*
|love.graphics.arc|

Draws a filled or unfilled arc at position (x, y). The arc is drawn from angle1
to angle2 in radians. The segments parameter determines how many segments are
used to draw the arc. The more segments, the smoother the edge.

Synopses: ~

    1.  |love.graphics.arc|(`drawmode`, `x`, `y`, `radius`, `angle1`,
        `angle2`, `segments`)
    2.  |love.graphics.arc|(`drawmode`, `arctype`, `x`, `y`, `radius`,
        `angle1`, `angle2`, `segments`)

Variants: ~

    1.  |love.graphics.arc|(`drawmode`, `x`, `y`, `radius`, `angle1`,
        `angle2`, `segments`)

    1.          Draws an arc using the 'pie' ArcType.

        Parameters: ~
            • {drawmode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the arc.
            • {x} (`number`)
                The position of the center along x-axis.
            • {y} (`number`)
                The position of the center along y-axis.
            • {radius} (`number`)
                Radius of the arc.
            • {angle1} (`number`)
                The angle at which the arc begins.
            • {angle2} (`number`)
                The angle at which the arc terminates.
            • {segments} (`number`) (default: `10`)
                The number of segments used for drawing the arc.


    2.  |love.graphics.arc|(`drawmode`, `arctype`, `x`, `y`, `radius`,
        `angle1`, `angle2`, `segments`)

    2.  

        Parameters: ~
            • {drawmode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the arc.
            • {arctype} (`ArcType`) |love2d-docs-ArcType|
                The type of arc to draw.
            • {x} (`number`)
                The position of the center along x-axis.
            • {y} (`number`)
                The position of the center along y-axis.
            • {radius} (`number`)
                Radius of the arc.
            • {angle1} (`number`)
                The angle at which the arc begins.
            • {angle2} (`number`)
                The angle at which the arc terminates.
            • {segments} (`number`) (default: `10`)
                The number of segments used for drawing the arc.


-------------------------------------------------------------------------------
                                  *love2d-docs-love.graphics.captureScreenshot*
|love.graphics.captureScreenshot|

Creates a screenshot once the current frame is done (after love.draw has
finished).

Since this function enqueues a screenshot capture rather than executing it
immediately, it can be called from an input callback or love.update and it will
still capture all of what's drawn to the screen in that frame.

Synopses: ~

    1.  |love.graphics.captureScreenshot|(`filename`)
    2.  |love.graphics.captureScreenshot|(`callback`)
    3.  |love.graphics.captureScreenshot|(`channel`)

Variants: ~

    1.  |love.graphics.captureScreenshot|(`filename`)

    1.          Capture a screenshot and save it to a file at the end of the current
        frame.

        Parameters: ~
            • {filename} (`string`)
                The filename to save the screenshot to. The encoded image type
                is determined based on the extension of the filename, and must
                be one of the ImageFormats.


    2.  |love.graphics.captureScreenshot|(`callback`)

    2.          Capture a screenshot and call a callback with the generated ImageData
        at the end of the current frame.

        Parameters: ~
            • {callback} (`function`)
                Function which gets called once the screenshot has been
                captured. An ImageData is passed into the function as its only
                argument.


    3.  |love.graphics.captureScreenshot|(`channel`)

    3.          Capture a screenshot and push the generated ImageData to a Channel at
        the end of the current frame.

        Parameters: ~
            • {channel} (`Channel`) |love2d-docs-Channel|
                The Channel to push the generated ImageData to.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.graphics.circle*
|love.graphics.circle|

Draws a circle.

Synopses: ~

    1.  |love.graphics.circle|(`mode`, `x`, `y`, `radius`)
    2.  |love.graphics.circle|(`mode`, `x`, `y`, `radius`, `segments`)

Variants: ~

    1.  |love.graphics.circle|(`mode`, `x`, `y`, `radius`)

    1.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the circle.
            • {x} (`number`)
                The position of the center along x-axis.
            • {y} (`number`)
                The position of the center along y-axis.
            • {radius} (`number`)
                The radius of the circle.


    2.  |love.graphics.circle|(`mode`, `x`, `y`, `radius`, `segments`)

    2.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the circle.
            • {x} (`number`)
                The position of the center along x-axis.
            • {y} (`number`)
                The position of the center along y-axis.
            • {radius} (`number`)
                The radius of the circle.
            • {segments} (`number`)
                The number of segments used for drawing the circle. Note: The
                default variable for the segments parameter varies between
                different versions of LÖVE.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics.clear*
|love.graphics.clear|

Clears the screen or active Canvas to the specified color.

This function is called automatically before love.draw in the default love.run
function. See the example in love.run for a typical use of this function.

Note that the scissor area bounds the cleared region.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

In versions prior to background color instead.

Synopses: ~

    1.  |love.graphics.clear|()
    2.  |love.graphics.clear|(`r`, `g`, `b`, `a`, `clearstencil`,
        `cleardepth`)
    3.  |love.graphics.clear|(`color`, `...`, `clearstencil`,
        `cleardepth`)
    4.  |love.graphics.clear|(`clearcolor`, `clearstencil`,
        `cleardepth`)

Variants: ~

    1.  |love.graphics.clear|()

    1.          Clears the screen to the background color in 0.9.2 and earlier, or to
        transparent black (0, 0, 0, 0) in LÖVE 0.10.0 and newer.


    2.  |love.graphics.clear|(`r`, `g`, `b`, `a`, `clearstencil`,
        `cleardepth`)

    2.          Clears the screen or active Canvas to the specified color.

        Parameters: ~
            • {r} (`number`)
                The red channel of the color to clear the screen to.
            • {g} (`number`)
                The green channel of the color to clear the screen to.
            • {b} (`number`)
                The blue channel of the color to clear the screen to.
            • {a} (`number`) (default: `1`)
                The alpha channel of the color to clear the screen to.
            • {clearstencil} (`boolean`) (default: `true`)
                Whether to clear the active stencil buffer, if present. It can
                also be an integer between 0 and 255 to clear the stencil
                buffer to a specific value.
            • {cleardepth} (`boolean`) (default: `true`)
                Whether to clear the active depth buffer, if present. It can
                also be a number between 0 and 1 to clear the depth buffer to a
                specific value.


    3.  |love.graphics.clear|(`color`, `...`, `clearstencil`,
        `cleardepth`)

    3.          Clears multiple active Canvases to different colors, if multiple
        Canvases are active at once via love.graphics.setCanvas.

        A color must be specified for each active Canvas, when this function
        variant is used.

        Parameters: ~
            • {color} (`table`)
                A table in the form of {r, g, b, a} containing the color to
                clear the first active Canvas to.
            • {...} (`table`)
                Additional tables for each active Canvas.
            • {clearstencil} (`boolean`) (default: `true`)
                Whether to clear the active stencil buffer, if present. It can
                also be an integer between 0 and 255 to clear the stencil
                buffer to a specific value.
            • {cleardepth} (`boolean`) (default: `true`)
                Whether to clear the active depth buffer, if present. It can
                also be a number between 0 and 1 to clear the depth buffer to a
                specific value.


    4.  |love.graphics.clear|(`clearcolor`, `clearstencil`,
        `cleardepth`)

    4.          Clears the stencil or depth buffers without having to clear the color
        canvas as well.

        Parameters: ~
            • {clearcolor} (`boolean`)
                Whether to clear the active color canvas to transparent black
                (0, 0, 0, 0). Typically this should be set to false with this
                variant of the function.
            • {clearstencil} (`boolean`)
                Whether to clear the active stencil buffer, if present. It can
                also be an integer between 0 and 255 to clear the stencil
                buffer to a specific value.
            • {cleardepth} (`boolean`)
                Whether to clear the active depth buffer, if present. It can
                also be a number between 0 and 1 to clear the depth buffer to a
                specific value.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.discard*
|love.graphics.discard|

Discards (trashes) the contents of the screen or active Canvas. This is a
performance optimization function with niche use cases.

If the active Canvas has just been changed and the 'replace' BlendMode is about
to be used to draw something which covers the entire screen, calling
love.graphics.discard rather than calling love.graphics.clear or doing nothing
may improve performance on mobile devices.

On some desktop systems this function may do nothing.

Synopses: ~

    1.  |love.graphics.discard|(`discardcolor`, `discardstencil`)
    2.  |love.graphics.discard|(`discardcolors`, `discardstencil`)

Variants: ~

    1.  |love.graphics.discard|(`discardcolor`, `discardstencil`)

    1.          See function description

        Parameters: ~
            • {discardcolor} (`boolean`) (default: `true`)
                Whether to discard the texture(s) of the active Canvas(es) (the
                contents of the screen if no Canvas is active.)
            • {discardstencil} (`boolean`) (default: `true`)
                Whether to discard the contents of the stencil buffer of the
                screen / active Canvas.


    2.  |love.graphics.discard|(`discardcolors`, `discardstencil`)

    2.          See function description

        Parameters: ~
            • {discardcolors} (`table`)
                An array containing boolean values indicating whether to
                discard the texture of each active Canvas, when multiple
                simultaneous Canvases are active.
            • {discardstencil} (`boolean`) (default: `true`)
                Whether to discard the contents of the stencil buffer of the
                screen / active Canvas.


-------------------------------------------------------------------------------
                                               *love2d-docs-love.graphics.draw*
|love.graphics.draw|

Draws a Drawable object (an Image, Canvas, SpriteBatch, ParticleSystem, Mesh,
Text object, or Video) on the screen with optional rotation, scaling and
shearing.

Objects are drawn relative to their local coordinate system. The origin is by
default located at the top left corner of Image and Canvas. All scaling,
shearing, and rotation arguments transform the object relative to that point.
Also, the position of the origin can be specified on the screen coordinate
system.

It's possible to rotate an object about its center by offsetting the origin to
the center. Angles must be given in radians for rotation. One can also use a
negative scaling factor to flip about its centerline.

Note that the offsets are applied before rotation, scaling, or shearing;
scaling and shearing are applied before rotation.

The right and bottom edges of the object are shifted at an angle defined by the
shearing factors.

When using the default shader anything drawn with this function will be tinted
according to the currently selected color.  Set it to pure white to preserve
the object's original colors.

Synopses: ~

    1.  |love.graphics.draw|(`drawable`, `x`, `y`, `r`, `sx`, `sy`,
        `ox`, `oy`, `kx`, `ky`)
    2.  |love.graphics.draw|(`texture`, `quad`, `x`, `y`, `r`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)
    3.  |love.graphics.draw|(`drawable`, `transform`)
    4.  |love.graphics.draw|(`texture`, `quad`, `transform`)

Variants: ~

    1.  |love.graphics.draw|(`drawable`, `x`, `y`, `r`, `sx`, `sy`,
        `ox`, `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {drawable} (`Drawable`) |love2d-docs-Drawable|
                A drawable object.
            • {x} (`number`) (default: `0`)
                The position to draw the object (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    2.  |love.graphics.draw|(`texture`, `quad`, `x`, `y`, `r`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                A Texture (Image or Canvas) to texture the Quad with.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The Quad to draw on screen.
            • {x} (`number`)
                The position to draw the object (x-axis).
            • {y} (`number`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    3.  |love.graphics.draw|(`drawable`, `transform`)

    3.          See function description

        Parameters: ~
            • {drawable} (`Drawable`) |love2d-docs-Drawable|
                A drawable object.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.


    4.  |love.graphics.draw|(`texture`, `quad`, `transform`)

    4.          See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                A Texture (Image or Canvas) to texture the Quad with.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The Quad to draw on screen.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.


-------------------------------------------------------------------------------
                                      *love2d-docs-love.graphics.drawInstanced*
|love.graphics.drawInstanced|

Draws many instances of a Mesh with a single draw call, using hardware geometry
instancing.

Each instance can have unique properties (positions, colors, etc.) but will not
by default unless a custom per-instance vertex attributes or the
love_InstanceID GLSL 3 vertex shader variable is used, otherwise they will all
render at the same position on top of each other.

Instancing is not supported by some older GPUs that are only capable of using
OpenGL ES 2 or OpenGL 2. Use love.graphics.getSupported to check.

Synopses: ~

    1.  |love.graphics.drawInstanced|(`mesh`, `instancecount`, `x`,
        `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  |love.graphics.drawInstanced|(`mesh`, `instancecount`,
        `transform`)

Variants: ~

    1.  |love.graphics.drawInstanced|(`mesh`, `instancecount`, `x`,
        `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {mesh} (`Mesh`) |love2d-docs-Mesh|
                The mesh to render.
            • {instancecount} (`number`)
                The number of instances to render.
            • {x} (`number`) (default: `0`)
                The position to draw the instances (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the instances (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    2.  |love.graphics.drawInstanced|(`mesh`, `instancecount`,
        `transform`)

    2.          See function description

        Parameters: ~
            • {mesh} (`Mesh`) |love2d-docs-Mesh|
                The mesh to render.
            • {instancecount} (`number`)
                The number of instances to render.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.


-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.drawLayer*
|love.graphics.drawLayer|

Draws a layer of an Array Texture.

Synopses: ~

    1.  |love.graphics.drawLayer|(`texture`, `layerindex`, `x`, `y`,
        `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  |love.graphics.drawLayer|(`texture`, `layerindex`, `quad`, `x`,
        `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    3.  |love.graphics.drawLayer|(`texture`, `layerindex`, `transform`)
    4.  |love.graphics.drawLayer|(`texture`, `layerindex`, `quad`,
        `transform`)

Variants: ~

    1.  |love.graphics.drawLayer|(`texture`, `layerindex`, `x`, `y`,
        `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          Draws a layer of an Array Texture.

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The Array Texture to draw.
            • {layerindex} (`number`)
                The index of the layer to use when drawing.
            • {x} (`number`) (default: `0`)
                The position to draw the texture (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the texture (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    2.  |love.graphics.drawLayer|(`texture`, `layerindex`, `quad`, `x`,
        `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          Draws a layer of an Array Texture using the specified Quad.

        The specified layer index overrides any layer index set on the Quad via
        Quad:setLayer.

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The Array Texture to draw.
            • {layerindex} (`number`)
                The index of the layer to use when drawing.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The subsection of the texture's layer to use when drawing.
            • {x} (`number`) (default: `0`)
                The position to draw the texture (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the texture (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    3.  |love.graphics.drawLayer|(`texture`, `layerindex`, `transform`)

    3.          Draws a layer of an Array Texture using the specified Transform.

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The Array Texture to draw.
            • {layerindex} (`number`)
                The index of the layer to use when drawing.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.


    4.  |love.graphics.drawLayer|(`texture`, `layerindex`, `quad`,
        `transform`)

    4.          Draws a layer of an Array Texture using the specified Quad and
        Transform.

        In order to use an Array Texture or other non-2D texture types as the
        main texture in a custom void effect() variant must be used in the
        pixel shader, and MainTex must be declared as an ArrayImage or
        sampler2DArray like so: uniform ArrayImage MainTex;.

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The Array Texture to draw.
            • {layerindex} (`number`)
                The index of the layer to use when drawing.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The subsection of the texture's layer to use when drawing.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.ellipse*
|love.graphics.ellipse|

Draws an ellipse.

Synopses: ~

    1.  |love.graphics.ellipse|(`mode`, `x`, `y`, `radiusx`, `radiusy`)
    2.  |love.graphics.ellipse|(`mode`, `x`, `y`, `radiusx`, `radiusy`,
        `segments`)

Variants: ~

    1.  |love.graphics.ellipse|(`mode`, `x`, `y`, `radiusx`, `radiusy`)

    1.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the ellipse.
            • {x} (`number`)
                The position of the center along x-axis.
            • {y} (`number`)
                The position of the center along y-axis.
            • {radiusx} (`number`)
                The radius of the ellipse along the x-axis (half the ellipse's
                width).
            • {radiusy} (`number`)
                The radius of the ellipse along the y-axis (half the ellipse's
                height).


    2.  |love.graphics.ellipse|(`mode`, `x`, `y`, `radiusx`, `radiusy`,
        `segments`)

    2.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the ellipse.
            • {x} (`number`)
                The position of the center along x-axis.
            • {y} (`number`)
                The position of the center along y-axis.
            • {radiusx} (`number`)
                The radius of the ellipse along the x-axis (half the ellipse's
                width).
            • {radiusy} (`number`)
                The radius of the ellipse along the y-axis (half the ellipse's
                height).
            • {segments} (`number`)
                The number of segments used for drawing the ellipse.


-------------------------------------------------------------------------------
                                         *love2d-docs-love.graphics.flushBatch*
|love.graphics.flushBatch|

Immediately renders any pending automatically batched draws.

LÖVE will call this function internally as needed when most state is changed,
so it is not necessary to manually call it.

The current batch will be automatically flushed by love.graphics state changes
(except for the transform stack and the current color), as well as Shader:send
and methods on Textures which change their state. Using a different Image in
consecutive love.graphics.draw calls will also flush the current batch.

SpriteBatches, ParticleSystems, Meshes, and Text objects do their own batching
and do not affect automatic batching of other draws, aside from flushing the
current batch when they're drawn.

Synopses: ~

        |love.graphics.flushBatch|()

Variants: ~

        |love.graphics.flushBatch|()

        See function description


-------------------------------------------------------------------------------
                                 *love2d-docs-love.graphics.getBackgroundColor*
|love.graphics.getBackgroundColor|

Gets the current background color.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        `r`, `g`, `b`, `a` = |love.graphics.getBackgroundColor|()

Variants: ~

        `r`, `g`, `b`, `a` = |love.graphics.getBackgroundColor|()

        See function description

        Returns: ~
            1. (`number`)
                The red component (0-1).

            2. (`number`)
                The green component (0-1).

            3. (`number`)
                The blue component (0-1).

            4. (`number`)
                The alpha component (0-1).
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getBlendMode*
|love.graphics.getBlendMode|

Gets the blending mode.

Synopses: ~

        `mode`, `alphamode` = |love.graphics.getBlendMode|()

Variants: ~

        `mode`, `alphamode` = |love.graphics.getBlendMode|()

        See function description

        Returns: ~
            1. (`BlendMode`) |love2d-docs-BlendMode|
                The current blend mode.

            2. (`BlendAlphaMode`) |love2d-docs-BlendAlphaMode|
                The current blend alpha mode – it determines how the alpha of
                drawn objects affects blending.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.getCanvas*
|love.graphics.getCanvas|

Gets the current target Canvas.

Synopses: ~

        `canvas` = |love.graphics.getCanvas|()

Variants: ~

        `canvas` = |love.graphics.getCanvas|()

        See function description

        Return: ~
            (`Canvas`) |love2d-docs-Canvas|
                The Canvas set by setCanvas. Returns nil if drawing to the real
                screen.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.graphics.getCanvasFormats*
|love.graphics.getCanvasFormats|

Gets the available Canvas formats, and whether each is supported.

Synopses: ~

    1.  `formats` = |love.graphics.getCanvasFormats|()
    2.  `formats` = |love.graphics.getCanvasFormats|(`readable`)

Variants: ~

    1.  `formats` = |love.graphics.getCanvasFormats|()

    1.          See function description

        Return: ~
            (`table`)
                A table containing CanvasFormats as keys, and a boolean
                indicating whether the format is supported as values. Not all
                systems support all formats.
    2.  `formats` = |love.graphics.getCanvasFormats|(`readable`)

    2.          See function description

        Parameters: ~
            • {readable} (`boolean`)
                If true, the returned formats will only be indicated as
                supported if readable flag set to true for that format, and
                vice versa if the parameter is false.

        Return: ~
            (`table`)
                A table containing CanvasFormats as keys, and a boolean
                indicating whether the format is supported as values (taking
                into account the readable parameter). Not all systems support
                all formats.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.getColor*
|love.graphics.getColor|

Gets the current color.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        `r`, `g`, `b`, `a` = |love.graphics.getColor|()

Variants: ~

        `r`, `g`, `b`, `a` = |love.graphics.getColor|()

        See function description

        Returns: ~
            1. (`number`)
                The red component (0-1).

            2. (`number`)
                The green component (0-1).

            3. (`number`)
                The blue component (0-1).

            4. (`number`)
                The alpha component (0-1).
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getColorMask*
|love.graphics.getColorMask|

Gets the active color components used when drawing. Normally all 4 components
are active unless love.graphics.setColorMask has been used.

The color mask determines whether individual components of the colors of drawn
objects will affect the color of the screen. They affect love.graphics.clear
and Canvas:clear as well.

Synopses: ~

        `r`, `g`, `b`, `a` = |love.graphics.getColorMask|()

Variants: ~

        `r`, `g`, `b`, `a` = |love.graphics.getColorMask|()

        See function description

        Returns: ~
            1. (`boolean`)
                Whether the red color component is active when rendering.

            2. (`boolean`)
                Whether the green color component is active when rendering.

            3. (`boolean`)
                Whether the blue color component is active when rendering.

            4. (`boolean`)
                Whether the alpha color component is active when rendering.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.graphics.getDPIScale*
|love.graphics.getDPIScale|

Gets the DPI scale factor of the window.

The DPI scale factor represents relative pixel density. The pixel density
inside the window might be greater (or smaller) than the 'size' of the window.
For example on a retina screen in Mac OS X with the highdpi window flag
enabled, the window may take up the same physical size as an 800x600 window,
but the area inside the window uses 1600x1200 pixels.
love.graphics.getDPIScale() would return 2 in that case.

The love.window.fromPixels and love.window.toPixels functions can also be used
to convert between units.

The highdpi window flag must be enabled to use the full pixel density of a
Retina screen on Mac OS X and iOS. The flag currently does nothing on Windows
and Linux, and on Android it is effectively always enabled.

Synopses: ~

        `scale` = |love.graphics.getDPIScale|()

Variants: ~

        `scale` = |love.graphics.getDPIScale|()

        The units of love.graphics.getWidth, love.graphics.getHeight,
        love.mouse.getPosition, mouse events, love.touch.getPosition, and touch
        events are always in DPI-scaled units rather than pixels. In LÖVE 0.10
        and older they were in pixels.

        Return: ~
            (`number`)
                The pixel scale factor associated with the window.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.graphics.getDefaultFilter*
|love.graphics.getDefaultFilter|

Returns the default scaling filters used with Images, Canvases, and Fonts.

Synopses: ~

        `min`, `mag`, `anisotropy` = |love.graphics.getDefaultFilter|()

Variants: ~

        `min`, `mag`, `anisotropy` = |love.graphics.getDefaultFilter|()

        See function description

        Returns: ~
            1. (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode used when scaling the image down.

            2. (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode used when scaling the image up.

            3. (`number`)
                Maximum amount of Anisotropic Filtering used.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getDepthMode*
|love.graphics.getDepthMode|

Gets the current depth test mode and whether writing to the depth buffer is
enabled.

This is low-level functionality designed for use with custom vertex shaders and
Meshes with custom vertex attributes. No higher level APIs are provided to set
the depth of 2D graphics such as shapes, lines, and Images.

Synopses: ~

        `comparemode`, `write` = |love.graphics.getDepthMode|()

Variants: ~

        `comparemode`, `write` = |love.graphics.getDepthMode|()

        See function description

        Returns: ~
            1. (`CompareMode`) |love2d-docs-CompareMode|
                Depth comparison mode used for depth testing.

            2. (`boolean`)
                Whether to write update / write values to the depth buffer when
                rendering.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.graphics.getDimensions*
|love.graphics.getDimensions|

Gets the width and height in pixels of the window.

Synopses: ~

        `width`, `height` = |love.graphics.getDimensions|()

Variants: ~

        `width`, `height` = |love.graphics.getDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The width of the window.

            2. (`number`)
                The height of the window.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.getFont*
|love.graphics.getFont|

Gets the current Font object.

Synopses: ~

        `font` = |love.graphics.getFont|()

Variants: ~

        `font` = |love.graphics.getFont|()

        See function description

        Return: ~
            (`Font`) |love2d-docs-Font|
                The current Font. Automatically creates and sets the default
                font, if none is set yet.
-------------------------------------------------------------------------------
                                *love2d-docs-love.graphics.getFrontFaceWinding*
|love.graphics.getFrontFaceWinding|

Gets whether triangles with clockwise- or counterclockwise-ordered vertices are
considered front-facing.

This is designed for use in combination with Mesh face culling. Other
love.graphics shapes, lines, and sprites are not guaranteed to have a specific
winding order to their internal vertices.

Synopses: ~

        `winding` = |love.graphics.getFrontFaceWinding|()

Variants: ~

        `winding` = |love.graphics.getFrontFaceWinding|()

        See function description

        Return: ~
            (`VertexWinding`) |love2d-docs-VertexWinding|
                The winding mode being used. The default winding is
                counterclockwise ('ccw').
-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.getHeight*
|love.graphics.getHeight|

Gets the height in pixels of the window.

Synopses: ~

        `height` = |love.graphics.getHeight|()

Variants: ~

        `height` = |love.graphics.getHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the window.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.graphics.getImageFormats*
|love.graphics.getImageFormats|

Gets the raw and compressed pixel formats usable for Images, and whether each
is supported.

Synopses: ~

        `formats` = |love.graphics.getImageFormats|()

Variants: ~

        `formats` = |love.graphics.getImageFormats|()

        See function description

        Return: ~
            (`table`)
                A table containing PixelFormats as keys, and a boolean
                indicating whether the format is supported as values. Not all
                systems support all formats.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.graphics.getLineJoin*
|love.graphics.getLineJoin|

Gets the line join style.

Synopses: ~

        `join` = |love.graphics.getLineJoin|()

Variants: ~

        `join` = |love.graphics.getLineJoin|()

        See function description

        Return: ~
            (`LineJoin`) |love2d-docs-LineJoin|
                The LineJoin style.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getLineStyle*
|love.graphics.getLineStyle|

Gets the line style.

Synopses: ~

        `style` = |love.graphics.getLineStyle|()

Variants: ~

        `style` = |love.graphics.getLineStyle|()

        See function description

        Return: ~
            (`LineStyle`) |love2d-docs-LineStyle|
                The current line style.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getLineWidth*
|love.graphics.getLineWidth|

Gets the current line width.

Synopses: ~

        `width` = |love.graphics.getLineWidth|()

Variants: ~

        `width` = |love.graphics.getLineWidth|()

        This function does not work in 0.8.0, but has been fixed in version
        0.9.0. Use the following snippet to circumvent this in 0.8.0;

        love.graphics._getLineWidth = love.graphics.getLineWidth

        love.graphics._setLineWidth = love.graphics.setLineWidth

        function love.graphics.getLineWidth() return love.graphics.varlinewidth
        or 1 end

        function love.graphics.setLineWidth(w) love.graphics.varlinewidth = w;
        return love.graphics._setLineWidth(w) end

        Return: ~
            (`number`)
                The current line width.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.graphics.getMeshCullMode*
|love.graphics.getMeshCullMode|

Gets whether back-facing triangles in a Mesh are culled.

Mesh face culling is designed for use with low level custom
hardware-accelerated 3D rendering via custom vertex attributes on Meshes,
custom vertex shaders, and depth testing with a depth buffer.

Synopses: ~

        `mode` = |love.graphics.getMeshCullMode|()

Variants: ~

        `mode` = |love.graphics.getMeshCullMode|()

        See function description

        Return: ~
            (`CullMode`) |love2d-docs-CullMode|
                The Mesh face culling mode in use (whether to render
                everything, cull back-facing triangles, or cull front-facing
                triangles).
-------------------------------------------------------------------------------
                                 *love2d-docs-love.graphics.getPixelDimensions*
|love.graphics.getPixelDimensions|

Gets the width and height in pixels of the window.

love.graphics.getDimensions gets the dimensions of the window in units scaled
by the screen's DPI scale factor, rather than pixels. Use getDimensions for
calculations related to drawing to the screen and using the graphics coordinate
system (calculating the center of the screen, for example), and
getPixelDimensions only when dealing specifically with underlying pixels
(pixel-related calculations in a pixel Shader, for example).

Synopses: ~

        `pixelwidth`, `pixelheight` =
        |love.graphics.getPixelDimensions|()

Variants: ~

        `pixelwidth`, `pixelheight` =
        |love.graphics.getPixelDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The width of the window in pixels.

            2. (`number`)
                The height of the window in pixels.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.getPixelHeight*
|love.graphics.getPixelHeight|

Gets the height in pixels of the window.

The graphics coordinate system and DPI scale factor, rather than raw pixels.
Use getHeight for calculations related to drawing to the screen and using the
coordinate system (calculating the center of the screen, for example), and
getPixelHeight only when dealing specifically with underlying pixels
(pixel-related calculations in a pixel Shader, for example).

Synopses: ~

        `pixelheight` = |love.graphics.getPixelHeight|()

Variants: ~

        `pixelheight` = |love.graphics.getPixelHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the window in pixels.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.graphics.getPixelWidth*
|love.graphics.getPixelWidth|

Gets the width in pixels of the window.

The graphics coordinate system and DPI scale factor, rather than raw pixels.
Use getWidth for calculations related to drawing to the screen and using the
coordinate system (calculating the center of the screen, for example), and
getPixelWidth only when dealing specifically with underlying pixels
(pixel-related calculations in a pixel Shader, for example).

Synopses: ~

        `pixelwidth` = |love.graphics.getPixelWidth|()

Variants: ~

        `pixelwidth` = |love.graphics.getPixelWidth|()

        See function description

        Return: ~
            (`number`)
                The width of the window in pixels.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getPointSize*
|love.graphics.getPointSize|

Gets the point size.

Synopses: ~

        `size` = |love.graphics.getPointSize|()

Variants: ~

        `size` = |love.graphics.getPointSize|()

        See function description

        Return: ~
            (`number`)
                The current point size.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.graphics.getRendererInfo*
|love.graphics.getRendererInfo|

Gets information about the system's video card and drivers.

Synopses: ~

        `name`, `version`, `vendor`, `device` =
        |love.graphics.getRendererInfo|()

Variants: ~

        `name`, `version`, `vendor`, `device` =
        |love.graphics.getRendererInfo|()

        See function description

        Returns: ~
            1. (`string`)
                The name of the renderer, e.g. 'OpenGL' or 'OpenGL ES'.

            2. (`string`)
                The version of the renderer with some extra driver-dependent
                version info, e.g. '2.1 INTEL-8.10.44'.

            3. (`string`)
                The name of the graphics card vendor, e.g. 'Intel Inc'.

            4. (`string`)
                The name of the graphics card, e.g. 'Intel HD Graphics 3000
                OpenGL Engine'.
-------------------------------------------------------------------------------
                                         *love2d-docs-love.graphics.getScissor*
|love.graphics.getScissor|

Gets the current scissor box.

Synopses: ~

        `x`, `y`, `width`, `height` = |love.graphics.getScissor|()

Variants: ~

        `x`, `y`, `width`, `height` = |love.graphics.getScissor|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the top-left point of the box.

            2. (`number`)
                The y-component of the top-left point of the box.

            3. (`number`)
                The width of the box.

            4. (`number`)
                The height of the box.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.getShader*
|love.graphics.getShader|

Gets the current Shader. Returns nil if none is set.

Synopses: ~

        `shader` = |love.graphics.getShader|()

Variants: ~

        `shader` = |love.graphics.getShader|()

        See function description

        Return: ~
            (`Shader`) |love2d-docs-Shader|
                The currently active Shader, or nil if none is set.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.graphics.getStackDepth*
|love.graphics.getStackDepth|

Gets the current depth of the transform / state stack (the number of pushes
without corresponding pops).

Synopses: ~

        `depth` = |love.graphics.getStackDepth|()

Variants: ~

        `depth` = |love.graphics.getStackDepth|()

        See function description

        Return: ~
            (`number`)
                The current depth of the transform and state love.graphics
                stack.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.getStats*
|love.graphics.getStats|

Gets performance-related rendering statistics.

Synopses: ~

    1.  `stats` = |love.graphics.getStats|()
    2.  `stats` = |love.graphics.getStats|(`stats`)

Variants: ~

    1.  `stats` = |love.graphics.getStats|()

    1.          See function description

        Return: ~
            (`table`)
                A table with the following fields:
    2.  `stats` = |love.graphics.getStats|(`stats`)

    2.          This variant accepts an existing table to fill in, instead of creating
        a new one.

        Parameters: ~
            • {stats} (`table`)
                A table which will be filled in with the stat fields below.

        Return: ~
            (`table`)
                The table that was passed in above, now containing the
                following fields:
-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.getStencilTest*
|love.graphics.getStencilTest|

Gets the current stencil test configuration.

When stencil testing is enabled, the geometry of everything that is drawn
afterward will be clipped / stencilled out based on a comparison between the
arguments of this function and the stencil value of each pixel that the
geometry touches. The stencil values of pixels are affected via
love.graphics.stencil.

Each Canvas has its own per-pixel stencil values.

Synopses: ~

        `comparemode`, `comparevalue` =
        |love.graphics.getStencilTest|()

Variants: ~

        `comparemode`, `comparevalue` =
        |love.graphics.getStencilTest|()

        See function description

        Returns: ~
            1. (`CompareMode`) |love2d-docs-CompareMode|
                The type of comparison that is made for each pixel. Will be
                'always' if stencil testing is disabled.

            2. (`number`)
                The value used when comparing with the stencil value of each
                pixel.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.getSupported*
|love.graphics.getSupported|

Gets the optional graphics features and whether they're supported on the
system.

Some older or low-end systems don't always support all graphics features.

Synopses: ~

        `features` = |love.graphics.getSupported|()

Variants: ~

        `features` = |love.graphics.getSupported|()

        See function description

        Return: ~
            (`table`)
                A table containing GraphicsFeature keys, and boolean values
                indicating whether each feature is supported.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.graphics.getSystemLimits*
|love.graphics.getSystemLimits|

Gets the system-dependent maximum values for love.graphics features.

Synopses: ~

        `limits` = |love.graphics.getSystemLimits|()

Variants: ~

        `limits` = |love.graphics.getSystemLimits|()

        See function description

        Return: ~
            (`table`)
                A table containing GraphicsLimit keys, and number values.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.graphics.getTextureTypes*
|love.graphics.getTextureTypes|

Gets the available texture types, and whether each is supported.

Synopses: ~

        `texturetypes` = |love.graphics.getTextureTypes|()

Variants: ~

        `texturetypes` = |love.graphics.getTextureTypes|()

        See function description

        Return: ~
            (`table`)
                A table containing TextureTypes as keys, and a boolean
                indicating whether the type is supported as values. Not all
                systems support all types.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.getWidth*
|love.graphics.getWidth|

Gets the width in pixels of the window.

Synopses: ~

        `width` = |love.graphics.getWidth|()

Variants: ~

        `width` = |love.graphics.getWidth|()

        See function description

        Return: ~
            (`number`)
                The width of the window.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.graphics.intersectScissor*
|love.graphics.intersectScissor|

Sets the scissor to the rectangle created by the intersection of the specified
rectangle with the existing scissor.  If no scissor is active yet, it behaves
like love.graphics.setScissor.

The scissor limits the drawing area to a specified rectangle. This affects all
graphics calls, including love.graphics.clear.

The dimensions of the scissor is unaffected by graphical transformations
(translate, scale, ...).

Synopses: ~

        |love.graphics.intersectScissor|(`x`, `y`, `width`, `height`)

Variants: ~

        |love.graphics.intersectScissor|(`x`, `y`, `width`, `height`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-coordinate of the upper left corner of the rectangle to
                intersect with the existing scissor rectangle.
            • {y} (`number`)
                The y-coordinate of the upper left corner of the rectangle to
                intersect with the existing scissor rectangle.
            • {width} (`number`)
                The width of the rectangle to intersect with the existing
                scissor rectangle.
            • {height} (`number`)
                The height of the rectangle to intersect with the existing
                scissor rectangle.


-------------------------------------------------------------------------------
                              *love2d-docs-love.graphics.inverseTransformPoint*
|love.graphics.inverseTransformPoint|

Converts the given 2D position from screen-space into global coordinates.

This effectively applies the reverse of the current graphics transformations to
the given position. A similar Transform:inverseTransformPoint method exists for
Transform objects.

Synopses: ~

        `globalX`, `globalY` =
        |love.graphics.inverseTransformPoint|(`screenX`, `screenY`)

Variants: ~

        `globalX`, `globalY` =
        |love.graphics.inverseTransformPoint|(`screenX`, `screenY`)

        See function description

        Parameters: ~
            • {screenX} (`number`)
                The x component of the screen-space position.
            • {screenY} (`number`)
                The y component of the screen-space position.

        Returns: ~
            1. (`number`)
                The x component of the position in global coordinates.

            2. (`number`)
                The y component of the position in global coordinates.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.isActive*
|love.graphics.isActive|

Gets whether the graphics module is able to be used. If it is not active,
love.graphics function and method calls will not work correctly and may cause
the program to crash.
The graphics module is inactive if a window is not open, or if the app is in
the background on iOS. Typically the app's execution will be automatically
paused by the system, in the latter case.

Synopses: ~

        `active` = |love.graphics.isActive|()

Variants: ~

        `active` = |love.graphics.isActive|()

        See function description

        Return: ~
            (`boolean`)
                Whether the graphics module is active and able to be used.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.isGammaCorrect*
|love.graphics.isGammaCorrect|

Gets whether gamma-correct rendering is supported and enabled. It can be
enabled by setting t.gammacorrect = true in love.conf.

Not all devices support gamma-correct rendering, in which case it will be
automatically disabled and this function will return false. It is supported on
desktop systems which have graphics cards that are capable of using OpenGL 3 /
DirectX 10, and iOS devices that can use OpenGL ES 3.

Synopses: ~

        `gammacorrect` = |love.graphics.isGammaCorrect|()

Variants: ~

        `gammacorrect` = |love.graphics.isGammaCorrect|()

        When gamma-correct rendering is enabled, many functions and objects
        perform automatic color conversion between sRGB and linear RGB in order
        for blending and shader math to be mathematically correct (which they
        aren't if it's not enabled.)

        * The colors passed into converted from sRGB to linear RGB.

        * The colors set in text with per-character colors, points with
        per-point colors, standard custom Meshes which use the 'VertexColor'
        attribute name will automatically be converted from sRGB to linear RGB
        when those objects are drawn.

        * creating the Image.

        * Everything drawn to the screen will be blended in linear RGB and then
        the result will be converted to sRGB for display.

        * Canvases which use the 'normal' or 'srgb' CanvasFormat will have
        their content blended in linear RGB and the result will be stored in
        the canvas in sRGB, when drawing to them. When the Canvas itself is
        drawn, its pixel colors will be converted from sRGB to linear RGB in
        the same manner as Images. Keeping the canvas pixel data stored as sRGB
        allows for better precision (less banding) for darker colors compared
        to 'rgba8'.

        Because most conversions are automatically handled, your own code
        doesn't need to worry about sRGB and linear RGB color conversions when
        gamma-correct rendering is enabled, except in a couple cases:

        * If a Mesh with custom vertex attributes is used and one of the
        attributes is meant to be used as a color in a Shader, and the
        attribute isn't named 'VertexColor'.

        * If  a Shader is used which has uniform / extern variables or other
        variables that are meant to be used as colors, and Shader:sendColor
        isn't used.

        In both cases, love.math.gammaToLinear can be used to convert color
        values to linear RGB in Lua code, or the gammaCorrectColor (or
        unGammaCorrectColor if necessary) shader functions can be used inside
        shader code. Those shader functions ''only'' do conversions if
        gamma-correct rendering is actually enabled. The LOVE_GAMMA_CORRECT
        shader preprocessor define will be set if so.

        Read more about gamma-correct rendering here, here, and here.

        Return: ~
            (`boolean`)
                True if gamma-correct rendering is supported and was enabled in
                love.conf, false otherwise.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.graphics.isWireframe*
|love.graphics.isWireframe|

Gets whether wireframe mode is used when drawing.

Synopses: ~

        `wireframe` = |love.graphics.isWireframe|()

Variants: ~

        `wireframe` = |love.graphics.isWireframe|()

        See function description

        Return: ~
            (`boolean`)
                True if wireframe lines are used when drawing, false if it's
                not.
-------------------------------------------------------------------------------
                                               *love2d-docs-love.graphics.line*
|love.graphics.line|

Draws lines between points.

Synopses: ~

    1.  |love.graphics.line|(`x1`, `y1`, `x2`, `y2`, `...`)
    2.  |love.graphics.line|(`points`)

Variants: ~

    1.  |love.graphics.line|(`x1`, `y1`, `x2`, `y2`, `...`)

    1.          See function description

        Parameters: ~
            • {x1} (`number`)
                The position of first point on the x-axis.
            • {y1} (`number`)
                The position of first point on the y-axis.
            • {x2} (`number`)
                The position of second point on the x-axis.
            • {y2} (`number`)
                The position of second point on the y-axis.
            • {...} (`number`)
                You can continue passing point positions to draw a polyline.


    2.  |love.graphics.line|(`points`)

    2.          See function description

        Parameters: ~
            • {points} (`table`)
                A table of point positions, as described above.


-------------------------------------------------------------------------------
                                      *love2d-docs-love.graphics.newArrayImage*
|love.graphics.newArrayImage|

Creates a new array Image.

An array image / array texture is a single object which contains multiple
'layers' or 'slices' of 2D sub-images. It can be thought of similarly to a
texture atlas or sprite sheet, but it doesn't suffer from the same tile / quad
bleeding artifacts that texture atlases do – although every sub-image must
have the same dimensions.

A specific layer of an array image can be drawn with love.graphics.drawLayer /
SpriteBatch:addLayer, or with the Quad variant of love.graphics.draw and
Quad:setLayer, or via a custom Shader.

To use an array image in a Shader, it must be declared as a ArrayImage or
sampler2DArray type (instead of Image or sampler2D). The Texel(ArrayImage
image, vec3 texturecoord) shader function must be used to get pixel colors from
a slice of the array image. The vec3 argument contains the texture coordinate
in the first two components, and the 0-based slice index in the third
component.

Synopses: ~

        `image` = |love.graphics.newArrayImage|(`slices`, `settings`)

Variants: ~

        `image` = |love.graphics.newArrayImage|(`slices`, `settings`)

        Creates an array Image given a different image file for each slice of
        the resulting array image object.

        Illustration of how an array image works:
        [http://codeflow.org/entries/2010/dec/09/minecraft-like-rendering-experiments-in-opengl-4/illustrations/textures.jpg]

        A DPI scale of 2 (double the normal pixel density) will result in the
        image taking up the same space on-screen as an image with half its
        pixel dimensions that has a DPI scale of 1. This allows for easily
        swapping out image assets that take the same space on-screen but have
        different pixel densities, which makes supporting high-dpi / retina
        resolution require less code logic.

        In order to use an Array Texture or other non-2D texture types as the
        main texture in a custom void effect() variant must be used in the
        pixel shader, and MainTex must be declared as an ArrayImage or
        sampler2DArray like so: uniform ArrayImage MainTex;.

        Parameters: ~
            • {slices} (`table`)
                A table containing filepaths to images (or File, FileData,
                ImageData, or CompressedImageData objects), in an array. Each
                sub-image must have the same dimensions. A table of tables can
                also be given, where each sub-table contains all mipmap levels
                for the slice index of that sub-table.
            • {settings} (`table`) (default: `nil`)
                Optional table of settings to configure the array image,
                containing the following fields:
                • {mipmaps} (`boolean`) (default: `false`)
                    True to make the image use mipmaps, false to disable them.
                    Mipmaps will be automatically generated if the image isn't
                    a compressed texture format.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.
                • {dpiscale} (`number`) (default: `1`)
                    The DPI scale to use when drawing the array image and
                    calling getWidth/getHeight.

        Return: ~
            (`Image`) |love2d-docs-Image|
                An Array Image object.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.newCanvas*
|love.graphics.newCanvas|

Creates a new Canvas object for offscreen rendering.

Synopses: ~

    1.  `canvas` = |love.graphics.newCanvas|()
    2.  `canvas` = |love.graphics.newCanvas|(`width`, `height`)
    3.  `canvas` = |love.graphics.newCanvas|(`width`, `height`,
        `settings`)
    4.  `canvas` = |love.graphics.newCanvas|(`width`, `height`,
        `layers`, `settings`)

Variants: ~

    1.  `canvas` = |love.graphics.newCanvas|()

    1.          See function description

        Return: ~
            (`Canvas`) |love2d-docs-Canvas|
                A new Canvas with dimensions equal to the window's size in
                pixels.
    2.  `canvas` = |love.graphics.newCanvas|(`width`, `height`)

    2.          See function description

        Parameters: ~
            • {width} (`number`)
                The desired width of the Canvas.
            • {height} (`number`)
                The desired height of the Canvas.

        Return: ~
            (`Canvas`) |love2d-docs-Canvas|
                A new Canvas with specified width and height.
    3.  `canvas` = |love.graphics.newCanvas|(`width`, `height`,
        `settings`)

    3.          Creates a 2D or cubemap Canvas using the given settings.

        Some Canvas formats have higher system requirements than the default
        format. Use love.graphics.getCanvasFormats to check for support.

        Parameters: ~
            • {width} (`number`)
                The desired width of the Canvas.
            • {height} (`number`)
                The desired height of the Canvas.
            • {settings} (`table`) (default: `nil`)
                A table containing the given fields:
                • {type} (`TextureType`) |love2d-docs-TextureType| (default: `'2d'`)
                    The type of Canvas to create.
                • {format} (`PixelFormat`) |love2d-docs-PixelFormat| (default: `'normal'`)
                    The format of the Canvas.
                • {readable} (`boolean`)
                    Whether the Canvas is readable (drawable and accessible in
                    a Shader). True by default for regular formats, false by
                    default for depth/stencil formats.
                • {msaa} (`number`) (default: `0`)
                    The desired number of multisample antialiasing (MSAA)
                    samples used when drawing to the Canvas.
                • {dpiscale} (`number`) (default: `love.graphics.getDPIScale()`)
                    The DPI scale factor of the Canvas, used when drawing to
                    the Canvas as well as when drawing the Canvas to the
                    screen.
                • {mipmaps} (`MipmapMode`) |love2d-docs-MipmapMode| (default: `'none'`)
                    Whether the Canvas has mipmaps, and whether to
                    automatically regenerate them if so.

        Return: ~
            (`Canvas`) |love2d-docs-Canvas|
                A new Canvas with specified width and height.
    4.  `canvas` = |love.graphics.newCanvas|(`width`, `height`,
        `layers`, `settings`)

    4.          Creates a volume or array texture-type Canvas.

        Parameters: ~
            • {width} (`number`)
                The desired width of the Canvas.
            • {height} (`number`)
                The desired height of the Canvas.
            • {layers} (`number`)
                The number of array layers (if the Canvas is an Array Texture),
                or the volume depth (if the Canvas is a Volume Texture).
            • {settings} (`table`) (default: `nil`)
                A table containing the given fields:
                • {type} (`TextureType`) |love2d-docs-TextureType| (default: `'array'`)
                    The type of Canvas to create.
                • {format} (`PixelFormat`) |love2d-docs-PixelFormat| (default: `'normal'`)
                    The format of the Canvas.
                • {readable} (`boolean`) (default: `nil`)
                    Whether the Canvas is readable (drawable and accessible in
                    a Shader). True by default for regular formats, false by
                    default for depth/stencil formats.
                • {msaa} (`number`) (default: `0`)
                    The desired number of multisample antialiasing (MSAA)
                    samples used when drawing to the Canvas.
                • {dpiscale} (`number`) (default: `love.graphics.getDPIScale()`)
                    The DPI scale factor of the Canvas, used when drawing to
                    the Canvas as well as when drawing the Canvas to the
                    screen.
                • {mipmaps} (`MipmapMode`) |love2d-docs-MipmapMode| (default: `'none'`)
                    Whether the Canvas has mipmaps, and whether to
                    automatically regenerate them if so.

        Return: ~
            (`Canvas`) |love2d-docs-Canvas|
                A new Canvas with specified width and height.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.newCubeImage*
|love.graphics.newCubeImage|

Creates a new cubemap Image.

Cubemap images have 6 faces (sides) which represent a cube. They can't be
rendered directly, they can only be used in Shader code (and sent to the shader
via Shader:send).

To use a cubemap image in a Shader, it must be declared as a CubeImage or
samplerCube type (instead of Image or sampler2D). The Texel(CubeImage image,
vec3 direction) shader function must be used to get pixel colors from the
cubemap. The vec3 argument is a normalized direction from the center of the
cube, rather than explicit texture coordinates.

Each face in a cubemap image must have square dimensions.

For variants of this function which accept a single image containing multiple
cubemap faces, they must be laid out in one of the following forms in the
image:

   +y

+z +x -z

   -y

   -x

or:

   +y

-x +z +x -z

   -y

or:

+x

-x

+y

-y

+z

-z

or:

+x -x +y -y +z -z

Synopses: ~

    1.  `image` = |love.graphics.newCubeImage|(`filename`, `settings`)
    2.  `image` = |love.graphics.newCubeImage|(`faces`, `settings`)

Variants: ~

    1.  `image` = |love.graphics.newCubeImage|(`filename`, `settings`)

    1.          Creates a cubemap Image given a single image file containing multiple
        cube faces.

        Parameters: ~
            • {filename} (`string`)
                The filepath to a cubemap image file (or a File, FileData, or
                ImageData).
            • {settings} (`table`) (default: `nil`)
                Optional table of settings to configure the cubemap image,
                containing the following fields:
                • {mipmaps} (`boolean`) (default: `false`)
                    True to make the image use mipmaps, false to disable them.
                    Mipmaps will be automatically generated if the image isn't
                    a compressed texture format.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.

        Return: ~
            (`Image`) |love2d-docs-Image|
                An cubemap Image object.
    2.  `image` = |love.graphics.newCubeImage|(`faces`, `settings`)

    2.          Creates a cubemap Image given a different image file for each cube
        face.

        Parameters: ~
            • {faces} (`table`)
                A table containing 6 filepaths to images (or File, FileData,
                ImageData, or CompressedImageData objects), in an array. Each
                face image must have the same dimensions. A table of tables can
                also be given, where each sub-table contains all mipmap levels
                for the cube face index of that sub-table.
            • {settings} (`table`) (default: `nil`)
                Optional table of settings to configure the cubemap image,
                containing the following fields:
                • {mipmaps} (`boolean`) (default: `false`)
                    True to make the image use mipmaps, false to disable them.
                    Mipmaps will be automatically generated if the image isn't
                    a compressed texture format.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.

        Return: ~
            (`Image`) |love2d-docs-Image|
                An cubemap Image object.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.newFont*
|love.graphics.newFont|

Creates a new Font from a TrueType Font or BMFont file. Created fonts are not
cached, in that calling this function with the same arguments will always
create a new Font object.

All variants which accept a filename can also accept a Data object instead.

Synopses: ~

    1.  `font` = |love.graphics.newFont|(`filename`)
    2.  `font` = |love.graphics.newFont|(`filename`, `size`, `hinting`,
        `dpiscale`)
    3.  `font` = |love.graphics.newFont|(`filename`, `imagefilename`)
    4.  `font` = |love.graphics.newFont|(`size`, `hinting`, `dpiscale`)

Variants: ~

    1.  `font` = |love.graphics.newFont|(`filename`)

    1.          Create a new BMFont or TrueType font.

        If the file is a TrueType font, it will be size 12. Use the variant
        below to create a TrueType font with a custom size.

        Parameters: ~
            • {filename} (`string`)
                The filepath to the BMFont or TrueType font file.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
    2.  `font` = |love.graphics.newFont|(`filename`, `size`, `hinting`,
        `dpiscale`)

    2.          Create a new TrueType font.

        Parameters: ~
            • {filename} (`string`)
                The filepath to the TrueType font file.
            • {size} (`number`)
                The size of the font in pixels.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.graphics.getDPIScale()`)
                The DPI scale factor of the font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
    3.  `font` = |love.graphics.newFont|(`filename`, `imagefilename`)

    3.          Create a new BMFont.

        Parameters: ~
            • {filename} (`string`)
                The filepath to the BMFont file.
            • {imagefilename} (`string`)
                The filepath to the BMFont's image file. If this argument is
                omitted, the path specified inside the BMFont file will be
                used.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
    4.  `font` = |love.graphics.newFont|(`size`, `hinting`, `dpiscale`)

    4.          Create a new instance of the default font (Vera Sans) with a custom
        size.

        Parameters: ~
            • {size} (`number`) (default: `12`)
                The size of the font in pixels.
            • {hinting} (`HintingMode`) |love2d-docs-HintingMode| (default: `'normal'`)
                True Type hinting mode.
            • {dpiscale} (`number`) (default: `love.graphics.getDPIScale()`)
                The DPI scale factor of the font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.newImage*
|love.graphics.newImage|

Creates a new Image from a filepath, FileData, an ImageData, or a
CompressedImageData, and optionally generates or specifies mipmaps for the
image.

Synopses: ~

    1.  `image` = |love.graphics.newImage|(`filename`, `settings`)
    2.  `image` = |love.graphics.newImage|(`fileData`, `settings`)
    3.  `image` = |love.graphics.newImage|(`imageData`, `settings`)
    4.  `image` = |love.graphics.newImage|(`compressedImageData`,
        `settings`)

Variants: ~

    1.  `image` = |love.graphics.newImage|(`filename`, `settings`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filepath to the image file.
            • {settings} (`table`) (default: `nil`)
                A table containing the following fields:
                • {dpiscale} (`number`) (default: `1`)
                    The DPI scale to use when drawing the image and calling
                    getWidth/getHeight.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.
                • {mipmaps} (`boolean`) (default: `false`)
                    If true, mipmaps for the image will be automatically
                    generated (or taken from the images's file if possible, if
                    the image originated from a CompressedImageData).

        Return: ~
            (`Image`) |love2d-docs-Image|
                A new Image object which can be drawn on screen.
    2.  `image` = |love.graphics.newImage|(`fileData`, `settings`)

    2.          See function description

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                The FileData containing image file.
            • {settings} (`table`) (default: `nil`)
                A table containing the following fields:
                • {dpiscale} (`number`) (default: `1`)
                    The DPI scale to use when drawing the image and calling
                    getWidth/getHeight.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.
                • {mipmaps} (`boolean`) (default: `false`)
                    If true, mipmaps for the image will be automatically
                    generated (or taken from the images's file if possible, if
                    the image originated from a CompressedImageData).

        Return: ~
            (`Image`) |love2d-docs-Image|
                A new Image object which can be drawn on screen.
    3.  `image` = |love.graphics.newImage|(`imageData`, `settings`)

    3.          See function description

        Parameters: ~
            • {imageData} (`ImageData`) |love2d-docs-ImageData|
                The ImageData containing image.
            • {settings} (`table`) (default: `nil`)
                A table containing the following fields:
                • {dpiscale} (`number`) (default: `1`)
                    The DPI scale to use when drawing the image and calling
                    getWidth/getHeight.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.
                • {mipmaps} (`boolean`) (default: `false`)
                    If true, mipmaps for the image will be automatically
                    generated (or taken from the images's file if possible, if
                    the image originated from a CompressedImageData).

        Return: ~
            (`Image`) |love2d-docs-Image|
                A new Image object which can be drawn on screen.
    4.  `image` = |love.graphics.newImage|(`compressedImageData`,
        `settings`)

    4.          See function description

        Parameters: ~
            • {compressedImageData} (`CompressedImageData`) |love2d-docs-CompressedImageData|
                A CompressedImageData object. The Image will use this
                CompressedImageData to reload itself when love.window.setMode
                is called.
            • {settings} (`table`) (default: `nil`)
                A table containing the following fields:
                • {dpiscale} (`number`) (default: `1`)
                    The DPI scale to use when drawing the image and calling
                    getWidth/getHeight.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.
                • {mipmaps} (`boolean`) (default: `false`)
                    If true, mipmaps for the image will be automatically
                    generated (or taken from the images's file if possible, if
                    the image originated from a CompressedImageData).

        Return: ~
            (`Image`) |love2d-docs-Image|
                A new Image object which can be drawn on screen.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.newImageFont*
|love.graphics.newImageFont|

Creates a new specifically formatted image.

In versions prior to 0.9.0, LÖVE expects ISO 8859-1 encoding for the glyphs
string.

Synopses: ~

    1.  `font` = |love.graphics.newImageFont|(`filename`, `glyphs`)
    2.  `font` = |love.graphics.newImageFont|(`imageData`, `glyphs`)
    3.  `font` = |love.graphics.newImageFont|(`filename`, `glyphs`,
        `extraspacing`)

Variants: ~

    1.  `font` = |love.graphics.newImageFont|(`filename`, `glyphs`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filepath to the image file.
            • {glyphs} (`string`)
                A string of the characters in the image in order from left to
                right.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
    2.  `font` = |love.graphics.newImageFont|(`imageData`, `glyphs`)

    2.          See function description

        Parameters: ~
            • {imageData} (`ImageData`) |love2d-docs-ImageData|
                The ImageData object to create the font from.
            • {glyphs} (`string`)
                A string of the characters in the image in order from left to
                right.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
    3.  `font` = |love.graphics.newImageFont|(`filename`, `glyphs`,
        `extraspacing`)

    3.          Instead of using this function, consider using a BMFont generator such
        as bmfont, littera, or bmGlyph with love.graphics.newFont. Because
        slime said it was better.

        Parameters: ~
            • {filename} (`string`)
                The filepath to the image file.
            • {glyphs} (`string`)
                A string of the characters in the image in order from left to
                right.
            • {extraspacing} (`number`)
                Additional spacing (positive or negative) to apply to each
                glyph in the Font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                A Font object which can be used to draw text on screen.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.newMesh*
|love.graphics.newMesh|

Creates a new Mesh.

Use Mesh:setTexture if the Mesh should be textured with an Image or Canvas when
it's drawn.

In versions prior to 11.0, color and byte component values were within the
range of 0 to 255 instead of 0 to 1.

Synopses: ~

    1.  `mesh` = |love.graphics.newMesh|(`vertices`, `mode`, `usage`)
    2.  `mesh` = |love.graphics.newMesh|(`vertexcount`, `mode`,
        `usage`)
    3.  `mesh` = |love.graphics.newMesh|(`vertexformat`, `vertices`,
        `mode`, `usage`)
    4.  `mesh` = |love.graphics.newMesh|(`vertexformat`, `vertexcount`,
        `mode`, `usage`)
    5.  `mesh` = |love.graphics.newMesh|(`vertexcount`, `texture`,
        `mode`)

Variants: ~

    1.  `mesh` = |love.graphics.newMesh|(`vertices`, `mode`, `usage`)

    1.          Creates a standard Mesh with the specified vertices.

        Parameters: ~
            • {vertices} (`table`)
                The table filled with vertex information tables for each vertex
                as follows:
                • {1} (`number`)
                    The position of the vertex on the x-axis.
                • {2} (`number`)
                    The position of the vertex on the y-axis.
                • {3} (`number`) (default: `0`)
                    The u texture coordinate of the vertex. Texture coordinates
                    are normally in the range of 1, but can be greater or less
                    (see WrapMode.)
                • {4} (`number`) (default: `0`)
                    The v texture coordinate of the vertex. Texture coordinates
                    are normally in the range of 1, but can be greater or less
                    (see WrapMode.)
                • {5} (`number`) (default: `1`)
                    The red component of the vertex color.
                • {6} (`number`) (default: `1`)
                    The green component of the vertex color.
                • {7} (`number`) (default: `1`)
                    The blue component of the vertex color.
                • {8} (`number`) (default: `1`)
                    The alpha component of the vertex color.
            • {mode} (`MeshDrawMode`) |love2d-docs-MeshDrawMode| (default: `'fan'`)
                How the vertices are used when drawing. The default mode 'fan'
                is sufficient for simple convex polygons.
            • {usage} (`SpriteBatchUsage`) |love2d-docs-SpriteBatchUsage| (default: `'dynamic'`)
                The expected usage of the Mesh. The specified usage mode
                affects the Mesh's memory usage and performance.

        Return: ~
            (`Mesh`) |love2d-docs-Mesh|
                The new mesh.
    2.  `mesh` = |love.graphics.newMesh|(`vertexcount`, `mode`,
        `usage`)

    2.          Creates a standard Mesh with the specified number of vertices.

        Mesh:setVertices or Mesh:setVertex and Mesh:setDrawRange can be used to
        specify vertex information once the Mesh is created.

        Parameters: ~
            • {vertexcount} (`number`)
                The total number of vertices the Mesh will use. Each vertex is
                initialized to {0,0, 0,0, 1,1,1,1}.
            • {mode} (`MeshDrawMode`) |love2d-docs-MeshDrawMode| (default: `'fan'`)
                How the vertices are used when drawing. The default mode 'fan'
                is sufficient for simple convex polygons.
            • {usage} (`SpriteBatchUsage`) |love2d-docs-SpriteBatchUsage| (default: `'dynamic'`)
                The expected usage of the Mesh. The specified usage mode
                affects the Mesh's memory usage and performance.

        Return: ~
            (`Mesh`) |love2d-docs-Mesh|
                The new mesh.
    3.  `mesh` = |love.graphics.newMesh|(`vertexformat`, `vertices`,
        `mode`, `usage`)

    3.          Creates a Mesh with custom vertex attributes and the specified vertex
        data.

        The values in each vertex table are in the same order as the vertex
        attributes in the specified vertex format. If no value is supplied for
        a specific vertex attribute component, it will be set to a default
        value of 0 if its data type is 'float', or 1 if its data type is
        'byte'.

        If the data type of an attribute is 'float', components can be in the
        range 1 to 4, if the data type is 'byte' it must be 4.

        If a custom vertex attribute uses the name 'VertexPosition',
        'VertexTexCoord', or 'VertexColor', then the vertex data for that
        vertex attribute will be used for the standard vertex positions,
        texture coordinates, or vertex colors respectively, when drawing the
        Mesh. Otherwise a Vertex Shader is required in order to make use of the
        vertex attribute when the Mesh is drawn.

        A Mesh '''must''' have a 'VertexPosition' attribute in order to be
        drawn, but it can be attached from a different Mesh via
        Mesh:attachAttribute.

        To use a custom named vertex attribute in a Vertex Shader, it must be
        declared as an attribute variable of the same name. Variables can be
        sent from Vertex Shader code to Pixel Shader code by making a varying
        variable. For example:

        ''Vertex Shader code''

        attribute vec2 CoolVertexAttribute;

        varying vec2 CoolVariable;

        vec4 position(mat4 transform_projection, vec4 vertex_position)

        {

            CoolVariable = CoolVertexAttribute;

            return transform_projection * vertex_position;

        }

        ''Pixel Shader code''

        varying vec2 CoolVariable;

        vec4 effect(vec4 color, Image tex, vec2 texcoord, vec2 pixcoord)

        {

            vec4 texcolor = Texel(tex, texcoord + CoolVariable);

            return texcolor * color;

        }

        Parameters: ~
            • {vertexformat} (`table`)
                A table in the form of {attribute, ...}. Each attribute is a
                table which specifies a custom vertex attribute used for each
                vertex.
                • {attribute} (`table`)
                    A table containing the attribute's name, it's data type,
                    and the number of components in the attribute, in the form
                    of {name, datatype, components}.
                • {...} (`table`)
                    Additional vertex attribute format tables.
            • {vertices} (`table`)
                The table filled with vertex information tables for each
                vertex, in the form of {vertex, ...} where each vertex is a
                table in the form of {attributecomponent, ...}.
                • {attributecomponent} (`number`)
                    The first component of the first vertex attribute in the
                    vertex.
                • {...} (`number`)
                    Additional components of all vertex attributes in the
                    vertex.
            • {mode} (`MeshDrawMode`) |love2d-docs-MeshDrawMode| (default: `'fan'`)
                How the vertices are used when drawing. The default mode 'fan'
                is sufficient for simple convex polygons.
            • {usage} (`SpriteBatchUsage`) |love2d-docs-SpriteBatchUsage| (default: `'dynamic'`)
                The expected usage of the Mesh. The specified usage mode
                affects the Mesh's memory usage and performance.

        Return: ~
            (`Mesh`) |love2d-docs-Mesh|
                The new mesh.
    4.  `mesh` = |love.graphics.newMesh|(`vertexformat`, `vertexcount`,
        `mode`, `usage`)

    4.          Creates a Mesh with custom vertex attributes and the specified number
        of vertices.

        Each vertex attribute component is initialized to 0 if its data type is
        'float', or 1 if its data type is 'byte'. Vertex Shader is required in
        order to make use of the vertex attribute when the Mesh is drawn.

        A Mesh '''must''' have a 'VertexPosition' attribute in order to be
        drawn, but it can be attached from a different Mesh via
        Mesh:attachAttribute.

        Parameters: ~
            • {vertexformat} (`table`)
                A table in the form of {attribute, ...}. Each attribute is a
                table which specifies a custom vertex attribute used for each
                vertex.
                • {attribute} (`table`)
                    A table containing the attribute's name, it's data type,
                    and the number of components in the attribute, in the form
                    of {name, datatype, components}.
                • {...} (`table`)
                    Additional vertex attribute format tables.
            • {vertexcount} (`number`)
                The total number of vertices the Mesh will use.
            • {mode} (`MeshDrawMode`) |love2d-docs-MeshDrawMode| (default: `'fan'`)
                How the vertices are used when drawing. The default mode 'fan'
                is sufficient for simple convex polygons.
            • {usage} (`SpriteBatchUsage`) |love2d-docs-SpriteBatchUsage| (default: `'dynamic'`)
                The expected usage of the Mesh. The specified usage mode
                affects the Mesh's memory usage and performance.

        Return: ~
            (`Mesh`) |love2d-docs-Mesh|
                The new mesh.
    5.  `mesh` = |love.graphics.newMesh|(`vertexcount`, `texture`,
        `mode`)

    5.          Mesh:setVertices or Mesh:setVertex and Mesh:setDrawRange can be used to
        specify vertex information once the Mesh is created.

        Parameters: ~
            • {vertexcount} (`number`)
                The total number of vertices the Mesh will use. Each vertex is
                initialized to {0,0, 0,0, 255,255,255,255}.
            • {texture} (`Texture`) |love2d-docs-Texture| (default: `nil`)
                The Image or Canvas to use when drawing the Mesh. May be nil to
                use no texture.
            • {mode} (`MeshDrawMode`) |love2d-docs-MeshDrawMode| (default: `'fan'`)
                How the vertices are used when drawing. The default mode 'fan'
                is sufficient for simple convex polygons.

        Return: ~
            (`Mesh`) |love2d-docs-Mesh|
                The new mesh.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.graphics.newParticleSystem*
|love.graphics.newParticleSystem|

Creates a new ParticleSystem.

Synopses: ~

    1.  `system` = |love.graphics.newParticleSystem|(`image`, `buffer`)
    2.  `system` = |love.graphics.newParticleSystem|(`texture`,
        `buffer`)

Variants: ~

    1.  `system` = |love.graphics.newParticleSystem|(`image`, `buffer`)

    1.          See function description

        Parameters: ~
            • {image} (`Image`) |love2d-docs-Image|
                The image to use.
            • {buffer} (`number`) (default: `1000`)
                The max number of particles at the same time.

        Return: ~
            (`ParticleSystem`) |love2d-docs-ParticleSystem|
                A new ParticleSystem.
    2.  `system` = |love.graphics.newParticleSystem|(`texture`,
        `buffer`)

    2.          See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The texture (Image or Canvas) to use.
            • {buffer} (`number`) (default: `1000`)
                The max number of particles at the same time.

        Return: ~
            (`ParticleSystem`) |love2d-docs-ParticleSystem|
                A new ParticleSystem.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.newQuad*
|love.graphics.newQuad|

Creates a new Quad.

The purpose of a Quad is to use a fraction of an image to draw objects, as
opposed to drawing entire image. It is most useful for sprite sheets and
atlases: in a sprite atlas, multiple sprites reside in same image, quad is used
to draw a specific sprite from that image; in animated sprites with all frames
residing in the same image, quad is used to draw specific frame from the
animation.

Synopses: ~

    1.  `quad` = |love.graphics.newQuad|(`x`, `y`, `width`, `height`,
        `sw`, `sh`)
    2.  `quad` = |love.graphics.newQuad|(`x`, `y`, `width`, `height`,
        `texture`)

Variants: ~

    1.  `quad` = |love.graphics.newQuad|(`x`, `y`, `width`, `height`,
        `sw`, `sh`)

    1.          See function description

        Parameters: ~
            • {x} (`number`)
                The top-left position in the Image along the x-axis.
            • {y} (`number`)
                The top-left position in the Image along the y-axis.
            • {width} (`number`)
                The width of the Quad in the Image. (Must be greater than 0.)
            • {height} (`number`)
                The height of the Quad in the Image. (Must be greater than 0.)
            • {sw} (`number`)
                The reference width, the width of the Image. (Must be greater
                than 0.)
            • {sh} (`number`)
                The reference height, the height of the Image. (Must be greater
                than 0.)

        Return: ~
            (`Quad`) |love2d-docs-Quad|
                The new Quad.
    2.  `quad` = |love.graphics.newQuad|(`x`, `y`, `width`, `height`,
        `texture`)

    2.          See function description

        Parameters: ~
            • {x} (`number`)
                The top-left position in the Image along the x-axis.
            • {y} (`number`)
                The top-left position in the Image along the y-axis.
            • {width} (`number`)
                The width of the Quad in the Image. (Must be greater than 0.)
            • {height} (`number`)
                The height of the Quad in the Image. (Must be greater than 0.)
            • {texture} (`Texture`) |love2d-docs-Texture|
                The texture whose width and height will be used as the
                reference width and height.

        Return: ~
            (`Quad`) |love2d-docs-Quad|
                The new Quad.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.newShader*
|love.graphics.newShader|

Creates a new Shader object for hardware-accelerated vertex and pixel effects.
A Shader contains either vertex shader code, pixel shader code, or both.

Shaders are small programs which are run on the graphics card when drawing.
Vertex shaders are run once for each vertex (for example, an image has 4
vertices - one at each corner. A Mesh might have many more.) Pixel shaders are
run once for each pixel on the screen which the drawn object touches. Pixel
shader code is executed after all the object's vertices have been processed by
the vertex shader.

Synopses: ~

    1.  `shader` = |love.graphics.newShader|(`code`)
    2.  `shader` = |love.graphics.newShader|(`pixelcode`, `vertexcode`)

Variants: ~

    1.  `shader` = |love.graphics.newShader|(`code`)

    1.          See function description

        Parameters: ~
            • {code} (`string`)
                The pixel shader or vertex shader code, or a filename pointing
                to a file with the code.

        Return: ~
            (`Shader`) |love2d-docs-Shader|
                A Shader object for use in drawing operations.
    2.  `shader` = |love.graphics.newShader|(`pixelcode`, `vertexcode`)

    2.          See function description

        Parameters: ~
            • {pixelcode} (`string`)
                The pixel shader code, or a filename pointing to a file with
                the code.
            • {vertexcode} (`string`)
                The vertex shader code, or a filename pointing to a file with
                the code.

        Return: ~
            (`Shader`) |love2d-docs-Shader|
                A Shader object for use in drawing operations.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.newSpriteBatch*
|love.graphics.newSpriteBatch|

Creates a new SpriteBatch object.

Synopses: ~

    1.  `spriteBatch` = |love.graphics.newSpriteBatch|(`image`,
        `maxsprites`)
    2.  `spriteBatch` = |love.graphics.newSpriteBatch|(`image`,
        `maxsprites`, `usage`)
    3.  `spriteBatch` = |love.graphics.newSpriteBatch|(`texture`,
        `maxsprites`, `usage`)

Variants: ~

    1.  `spriteBatch` = |love.graphics.newSpriteBatch|(`image`,
        `maxsprites`)

    1.          See function description

        Parameters: ~
            • {image} (`Image`) |love2d-docs-Image|
                The Image to use for the sprites.
            • {maxsprites} (`number`) (default: `1000`)
                The maximum number of sprites that the SpriteBatch can contain
                at any given time. Since version 11.0, additional sprites added
                past this number will automatically grow the spritebatch.

        Return: ~
            (`SpriteBatch`) |love2d-docs-SpriteBatch|
                The new SpriteBatch.
    2.  `spriteBatch` = |love.graphics.newSpriteBatch|(`image`,
        `maxsprites`, `usage`)

    2.          See function description

        Parameters: ~
            • {image} (`Image`) |love2d-docs-Image|
                The Image to use for the sprites.
            • {maxsprites} (`number`) (default: `1000`)
                The maximum number of sprites that the SpriteBatch can contain
                at any given time. Since version 11.0, additional sprites added
                past this number will automatically grow the spritebatch.
            • {usage} (`SpriteBatchUsage`) |love2d-docs-SpriteBatchUsage| (default: `'dynamic'`)
                The expected usage of the SpriteBatch. The specified usage mode
                affects the SpriteBatch's memory usage and performance.

        Return: ~
            (`SpriteBatch`) |love2d-docs-SpriteBatch|
                The new SpriteBatch.
    3.  `spriteBatch` = |love.graphics.newSpriteBatch|(`texture`,
        `maxsprites`, `usage`)

    3.          See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The Image or Canvas to use for the sprites.
            • {maxsprites} (`number`) (default: `1000`)
                The maximum number of sprites that the SpriteBatch can contain
                at any given time. Since version 11.0, additional sprites added
                past this number will automatically grow the spritebatch.
            • {usage} (`SpriteBatchUsage`) |love2d-docs-SpriteBatchUsage| (default: `'dynamic'`)
                The expected usage of the SpriteBatch. The specified usage mode
                affects the SpriteBatch's memory usage and performance.

        Return: ~
            (`SpriteBatch`) |love2d-docs-SpriteBatch|
                The new SpriteBatch.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.newText*
|love.graphics.newText|

Creates a new drawable Text object.

Synopses: ~

    1.  `text` = |love.graphics.newText|(`font`, `textstring`)
    2.  `text` = |love.graphics.newText|(`font`, `coloredtext`)

Variants: ~

    1.  `text` = |love.graphics.newText|(`font`, `textstring`)

    1.          See function description

        Parameters: ~
            • {font} (`Font`) |love2d-docs-Font|
                The font to use for the text.
            • {textstring} (`string`) (default: `nil`)
                The initial string of text that the new Text object will
                contain. May be nil.

        Return: ~
            (`Text`) |love2d-docs-Text|
                The new drawable Text object.
    2.  `text` = |love.graphics.newText|(`font`, `coloredtext`)

    2.          See function description

        Parameters: ~
            • {font} (`Font`) |love2d-docs-Font|
                The font to use for the text.
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`Variant`)
                    Additional colors and strings.

        Return: ~
            (`Text`) |love2d-docs-Text|
                The new drawable Text object.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.newVideo*
|love.graphics.newVideo|

Creates a new drawable Video. Currently only Ogg Theora video files are
supported.

Synopses: ~

    1.  `video` = |love.graphics.newVideo|(`filename`)
    2.  `video` = |love.graphics.newVideo|(`videostream`)
    3.  `video` = |love.graphics.newVideo|(`filename`, `settings`)
    4.  `video` = |love.graphics.newVideo|(`filename`, `loadaudio`)
    5.  `video` = |love.graphics.newVideo|(`videostream`, `loadaudio`)

Variants: ~

    1.  `video` = |love.graphics.newVideo|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The file path to the Ogg Theora video file.

        Return: ~
            (`Video`) |love2d-docs-Video|
                A new Video.
    2.  `video` = |love.graphics.newVideo|(`videostream`)

    2.          See function description

        Parameters: ~
            • {videostream} (`VideoStream`) |love2d-docs-VideoStream|
                A video stream object.

        Return: ~
            (`Video`) |love2d-docs-Video|
                A new Video.
    3.  `video` = |love.graphics.newVideo|(`filename`, `settings`)

    3.          See function description

        Parameters: ~
            • {filename} (`string`)
                The file path to the Ogg Theora video file (or VideoStream).
            • {settings} (`table`) (default: `nil`)
                A table containing the following fields:
                • {audio} (`boolean`) (default: `false`)
                    Whether to try to load the video's audio into an audio
                    Source. If not explicitly set to true or false, it will try
                    without causing an error if the video has no audio.
                • {dpiscale} (`number`) (default: `love.graphics.getDPIScale()`)
                    The DPI scale factor of the video.

        Return: ~
            (`Video`) |love2d-docs-Video|
                A new Video.
    4.  `video` = |love.graphics.newVideo|(`filename`, `loadaudio`)

    4.          See function description

        Parameters: ~
            • {filename} (`string`)
                The file path to the Ogg Theora video file.
            • {loadaudio} (`boolean`) (default: `nil`)
                Whether to try to load the video's audio into an audio Source.
                If not explicitly set to true or false, it will try without
                causing an error if the video has no audio.

        Return: ~
            (`Video`) |love2d-docs-Video|
                A new Video.
    5.  `video` = |love.graphics.newVideo|(`videostream`, `loadaudio`)

    5.          See function description

        Parameters: ~
            • {videostream} (`VideoStream`) |love2d-docs-VideoStream|
                A video stream object.
            • {loadaudio} (`boolean`) (default: `nil`)
                Whether to try to load the video's audio into an audio Source.
                If not explicitly set to true or false, it will try without
                causing an error if the video has no audio.

        Return: ~
            (`Video`) |love2d-docs-Video|
                A new Video.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.newVolumeImage*
|love.graphics.newVolumeImage|

Creates a new volume (3D) Image.

Volume images are 3D textures with width, height, and depth. They can't be
rendered directly, they can only be used in Shader code (and sent to the shader
via Shader:send).

To use a volume image in a Shader, it must be declared as a VolumeImage or
sampler3D type (instead of Image or sampler2D). The Texel(VolumeImage image,
vec3 texcoords) shader function must be used to get pixel colors from the
volume image. The vec3 argument is a normalized texture coordinate with the z
component representing the depth to sample at (ranging from 1).

Volume images are typically used as lookup tables in shaders for color grading,
for example, because sampling using a texture coordinate that is partway in
between two pixels can interpolate across all 3 dimensions in the volume image,
resulting in a smooth gradient even when a small-sized volume image is used as
the lookup table.

Array images are a much better choice than volume images for storing multiple
different sprites in a single array image for directly drawing them.

Synopses: ~

        `image` = |love.graphics.newVolumeImage|(`layers`, `settings`)

Variants: ~

        `image` = |love.graphics.newVolumeImage|(`layers`, `settings`)

        Creates a volume Image given multiple image files with matching
        dimensions.

        Volume images are not supported on some older mobile devices. Use
        love.graphics.getTextureTypes to check at runtime.

        Parameters: ~
            • {layers} (`table`)
                A table containing filepaths to images (or File, FileData,
                ImageData, or CompressedImageData objects), in an array. A
                table of tables can also be given, where each sub-table
                represents a single mipmap level and contains all layers for
                that mipmap.
            • {settings} (`table`) (default: `nil`)
                Optional table of settings to configure the volume image,
                containing the following fields:
                • {mipmaps} (`boolean`) (default: `false`)
                    True to make the image use mipmaps, false to disable them.
                    Mipmaps will be automatically generated if the image isn't
                    a compressed texture format.
                • {linear} (`boolean`) (default: `false`)
                    True to treat the image's pixels as linear instead of sRGB,
                    when gamma correct rendering is enabled. Most images are
                    authored as sRGB.

        Return: ~
            (`Image`) |love2d-docs-Image|
                A volume Image object.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.graphics.origin*
|love.graphics.origin|

Resets the current coordinate transformation.

This function is always used to reverse any previous calls to
love.graphics.rotate, love.graphics.scale, love.graphics.shear or
love.graphics.translate. It returns the current transformation state to its
defaults.

Synopses: ~

        |love.graphics.origin|()

Variants: ~

        |love.graphics.origin|()

        See function description


-------------------------------------------------------------------------------
                                             *love2d-docs-love.graphics.points*
|love.graphics.points|

Draws one or more points.

Synopses: ~

    1.  |love.graphics.points|(`x`, `y`, `...`)
    2.  |love.graphics.points|(`points`)
    3.  |love.graphics.points|(`points`)

Variants: ~

    1.  |love.graphics.points|(`x`, `y`, `...`)

    1.          See function description

        Parameters: ~
            • {x} (`number`)
                The position of the first point on the x-axis.
            • {y} (`number`)
                The position of the first point on the y-axis.
            • {...} (`number`)
                The x and y coordinates of additional points.


    2.  |love.graphics.points|(`points`)

    2.          See function description

        Parameters: ~
            • {points} (`table`)
                A table containing multiple point positions, in the form of {x,
                y, ...}.


    3.  |love.graphics.points|(`points`)

    3.          Draws one or more individually colored points.

        In versions prior to 11.0, color component values were within the range
        of 0 to 255 instead of 0 to 1.

        The pixel grid is actually offset to the center of each pixel. So to
        get clean pixels drawn use 0.5 + integer increments.

        Points are not affected by size is always in pixels.

        Parameters: ~
            • {points} (`table`)
                A table containing multiple individually colored points, in the
                form of {point, ...}.
                • {point} (`table`)
                    A table containing the position and color of the first
                    point, in the form of {x, y, r, g, b, a}. The color
                    components are optional.
                • {...} (`table`)
                    Additional tables containing the position and color of more
                    points, in the form of {x, y, r, g, b, a}. The color
                    components are optional.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.polygon*
|love.graphics.polygon|

Draw a polygon.

Following the mode argument, this function can accept multiple numeric
arguments or a single table of numeric arguments. In either case the arguments
are interpreted as alternating x and y coordinates of the polygon's vertices.

Synopses: ~

    1.  |love.graphics.polygon|(`mode`, `...`)
    2.  |love.graphics.polygon|(`mode`, `vertices`)

Variants: ~

    1.  |love.graphics.polygon|(`mode`, `...`)

    1.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the polygon.
            • {...} (`number`)
                The vertices of the polygon.


    2.  |love.graphics.polygon|(`mode`, `vertices`)

    2.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the polygon.
            • {vertices} (`table`)
                The vertices of the polygon as a table.


-------------------------------------------------------------------------------
                                                *love2d-docs-love.graphics.pop*
|love.graphics.pop|

Pops the current coordinate transformation from the transformation stack.

This function is always used to reverse a previous push operation. It returns
the current transformation state to what it was before the last preceding push.

Synopses: ~

        |love.graphics.pop|()

Variants: ~

        |love.graphics.pop|()

        See function description


-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.present*
|love.graphics.present|

Displays the results of drawing operations on the screen.

This function is used when writing your own love.run function. It presents all
the results of your drawing operations on the screen. See the example in
love.run for a typical use of this function.

Synopses: ~

        |love.graphics.present|()

Variants: ~

        |love.graphics.present|()

        * If love.window.setMode has vsync equal to true, this function can't
        run more frequently than the refresh rate (e.g. 60 Hz), and will halt
        the program until ready if necessary.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics.print*
|love.graphics.print|

Draws text on screen. If no Font is set, one will be created and set (once) if
needed.

As of LOVE 0.7.1, when using translation and scaling functions while drawing
text, this function assumes the scale occurs first.  If you don't script with
this in mind, the text won't be in the right position, or possibly even on
screen.

love.graphics.print and love.graphics.printf both support UTF-8 encoding.
You'll also need a proper Font for special characters.

In versions prior to 11.0, color and byte component values were within the
range of 0 to 255 instead of 0 to 1.

Synopses: ~

    1.  |love.graphics.print|(`text`, `x`, `y`, `r`, `sx`, `sy`, `ox`,
        `oy`, `kx`, `ky`)
    2.  |love.graphics.print|(`coloredtext`, `x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)
    3.  |love.graphics.print|(`text`, `transform`)
    4.  |love.graphics.print|(`coloredtext`, `transform`)
    5.  |love.graphics.print|(`text`, `font`, `transform`)
    6.  |love.graphics.print|(`coloredtext`, `font`, `transform`)

Variants: ~

    1.  |love.graphics.print|(`text`, `x`, `y`, `r`, `sx`, `sy`, `ox`,
        `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {text} (`string`)
                The text to draw.
            • {x} (`number`) (default: `0`)
                The position to draw the object (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    2.  |love.graphics.print|(`coloredtext`, `x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {x} (`number`) (default: `0`)
                The position of the text on the x-axis.
            • {y} (`number`) (default: `0`)
                The position of the text on the y-axis.
            • {angle} (`number`) (default: `0`)
                The orientation of the text in radians.
            • {sx} (`number`) (default: `1`)
                Scale factor on the x-axis.
            • {sy} (`number`) (default: `sx`)
                Scale factor on the y-axis.
            • {ox} (`number`) (default: `0`)
                Origin offset on the x-axis.
            • {oy} (`number`) (default: `0`)
                Origin offset on the y-axis.
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor on the x-axis.
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor on the y-axis.


    3.  |love.graphics.print|(`text`, `transform`)

    3.          See function description

        Parameters: ~
            • {text} (`string`)
                The text to draw.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.


    4.  |love.graphics.print|(`coloredtext`, `transform`)

    4.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.


    5.  |love.graphics.print|(`text`, `font`, `transform`)

    5.          See function description

        Parameters: ~
            • {text} (`string`)
                The text to draw.
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.


    6.  |love.graphics.print|(`coloredtext`, `font`, `transform`)

    6.  

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.graphics.printf*
|love.graphics.printf|

Draws formatted text, with word wrap and alignment.

See additional notes in love.graphics.print.

The word wrap limit is applied before any scaling, rotation, and other
coordinate transformations. Therefore the amount of text per line stays
constant given the same wrap limit, even if the scale arguments change.

In version 0.9.2 and earlier, wrapping was implemented by breaking up words by
spaces and putting them back together to make sure things fit nicely within the
limit provided. However, due to the way this is done, extra spaces between
words would end up missing when printed on the screen, and some lines could
overflow past the provided wrap limit. In version 0.10.0 and newer this is no
longer the case.

In versions prior to 11.0, color and byte component values were within the
range of 0 to 255 instead of 0 to 1.

Synopses: ~

    1.  |love.graphics.printf|(`text`, `x`, `y`, `limit`, `align`, `r`,
        `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  |love.graphics.printf|(`text`, `font`, `x`, `y`, `limit`,
        `align`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    3.  |love.graphics.printf|(`text`, `transform`, `limit`, `align`)
    4.  |love.graphics.printf|(`text`, `font`, `transform`, `limit`,
        `align`)
    5.  |love.graphics.printf|(`coloredtext`, `x`, `y`, `limit`,
        `align`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    6.  |love.graphics.printf|(`coloredtext`, `font`, `x`, `y`,
        `limit`, `align`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    7.  |love.graphics.printf|(`coloredtext`, `transform`, `limit`,
        `align`)
    8.  |love.graphics.printf|(`coloredtext`, `font`, `transform`,
        `limit`, `align`)

Variants: ~

    1.  |love.graphics.printf|(`text`, `x`, `y`, `limit`, `align`, `r`,
        `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {text} (`string`)
                A text string.
            • {x} (`number`)
                The position on the x-axis.
            • {y} (`number`)
                The position on the y-axis.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    2.  |love.graphics.printf|(`text`, `font`, `x`, `y`, `limit`,
        `align`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          See function description

        Parameters: ~
            • {text} (`string`)
                A text string.
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.
            • {x} (`number`)
                The position on the x-axis.
            • {y} (`number`)
                The position on the y-axis.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    3.  |love.graphics.printf|(`text`, `transform`, `limit`, `align`)

    3.          See function description

        Parameters: ~
            • {text} (`string`)
                A text string.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.


    4.  |love.graphics.printf|(`text`, `font`, `transform`, `limit`,
        `align`)

    4.          See function description

        Parameters: ~
            • {text} (`string`)
                A text string.
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.


    5.  |love.graphics.printf|(`coloredtext`, `x`, `y`, `limit`,
        `align`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    5.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {x} (`number`)
                The position of the text (x-axis).
            • {y} (`number`)
                The position of the text (y-axis).
            • {limit} (`number`)
                The maximum width in pixels of the text before it gets
                automatically wrapped to a new line.
            • {align} (`AlignMode`) |love2d-docs-AlignMode|
                The alignment of the text.
            • {angle} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor (y-axis).


    6.  |love.graphics.printf|(`coloredtext`, `font`, `x`, `y`,
        `limit`, `align`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    6.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.
            • {x} (`number`)
                The position on the x-axis.
            • {y} (`number`)
                The position on the y-axis.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.
            • {angle} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    7.  |love.graphics.printf|(`coloredtext`, `transform`, `limit`,
        `align`)

    7.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.


    8.  |love.graphics.printf|(`coloredtext`, `font`, `transform`,
        `limit`, `align`)

    8.  

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.
            • {transform} (`Transform`) |love2d-docs-Transform|
                Transformation object.
            • {limit} (`number`)
                Wrap the line after this many horizontal pixels.
            • {align} (`AlignMode`) |love2d-docs-AlignMode| (default: `'left'`)
                The alignment.


-------------------------------------------------------------------------------
                                               *love2d-docs-love.graphics.push*
|love.graphics.push|

Copies and pushes the current coordinate transformation to the transformation
stack.

This function is always used to prepare for a corresponding pop operation
later. It stores the current coordinate transformation state into the
transformation stack and keeps it active. Later changes to the transformation
can be undone by using the pop operation, which returns the coordinate
transform to the state it was in before calling push.

Synopses: ~

    1.  |love.graphics.push|()
    2.  |love.graphics.push|(`stack`)

Variants: ~

    1.  |love.graphics.push|()

    1.          Pushes the current transformation to the transformation stack.


    2.  |love.graphics.push|(`stack`)

    2.          Pushes a specific type of state to the stack.

        Parameters: ~
            • {stack} (`StackType`) |love2d-docs-StackType|
                The type of stack to push (e.g. just transformation state, or
                all love.graphics state).


-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.rectangle*
|love.graphics.rectangle|

Draws a rectangle.

Synopses: ~

    1.  |love.graphics.rectangle|(`mode`, `x`, `y`, `width`, `height`)
    2.  |love.graphics.rectangle|(`mode`, `x`, `y`, `width`, `height`,
        `rx`, `ry`, `segments`)

Variants: ~

    1.  |love.graphics.rectangle|(`mode`, `x`, `y`, `width`, `height`)

    1.          See function description

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the rectangle.
            • {x} (`number`)
                The position of top-left corner along the x-axis.
            • {y} (`number`)
                The position of top-left corner along the y-axis.
            • {width} (`number`)
                Width of the rectangle.
            • {height} (`number`)
                Height of the rectangle.


    2.  |love.graphics.rectangle|(`mode`, `x`, `y`, `width`, `height`,
        `rx`, `ry`, `segments`)

    2.          Draws a rectangle with rounded corners.

        Parameters: ~
            • {mode} (`DrawMode`) |love2d-docs-DrawMode|
                How to draw the rectangle.
            • {x} (`number`)
                The position of top-left corner along the x-axis.
            • {y} (`number`)
                The position of top-left corner along the y-axis.
            • {width} (`number`)
                Width of the rectangle.
            • {height} (`number`)
                Height of the rectangle.
            • {rx} (`number`)
                The x-axis radius of each round corner. Cannot be greater than
                half the rectangle's width.
            • {ry} (`number`) (default: `rx`)
                The y-axis radius of each round corner. Cannot be greater than
                half the rectangle's height.
            • {segments} (`number`) (default: `nil`)
                The number of segments used for drawing the round corners. A
                default amount will be chosen if no number is given.


-------------------------------------------------------------------------------
                                   *love2d-docs-love.graphics.replaceTransform*
|love.graphics.replaceTransform|

Replaces the current coordinate transformation with the given Transform object.

Synopses: ~

        |love.graphics.replaceTransform|(`transform`)

Variants: ~

        |love.graphics.replaceTransform|(`transform`)

        See function description

        Parameters: ~
            • {transform} (`Transform`) |love2d-docs-Transform|
                The Transform object to replace the current graphics coordinate
                transform with.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics.reset*
|love.graphics.reset|

Resets the current graphics settings.

Calling reset makes the current drawing color white, the current background
color black, disables any active color component masks, disables wireframe mode
and resets the current graphics transformation to the origin. It also sets both
the point and line drawing modes to smooth and their sizes to 1.0.

Synopses: ~

        |love.graphics.reset|()

Variants: ~

        |love.graphics.reset|()

        See function description


-------------------------------------------------------------------------------
                                             *love2d-docs-love.graphics.rotate*
|love.graphics.rotate|

Rotates the coordinate system in two dimensions.

Calling this function affects all future drawing operations by rotating the
coordinate system around the origin by the given amount of radians. This change
lasts until love.draw() exits.

Synopses: ~

        |love.graphics.rotate|(`angle`)

Variants: ~

        |love.graphics.rotate|(`angle`)

        See function description

        Parameters: ~
            • {angle} (`number`)
                The amount to rotate the coordinate system in radians.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics.scale*
|love.graphics.scale|

Scales the coordinate system in two dimensions.

By default the coordinate system in LÖVE corresponds to the display pixels in
horizontal and vertical directions one-to-one, and the x-axis increases towards
the right while the y-axis increases downwards. Scaling the coordinate system
changes this relation.

After scaling by sx and sy, all coordinates are treated as if they were
multiplied by sx and sy. Every result of a drawing operation is also
correspondingly scaled, so scaling by (2, 2) for example would mean making
everything twice as large in both x- and y-directions. Scaling by a negative
value flips the coordinate system in the corresponding direction, which also
means everything will be drawn flipped or upside down, or both. Scaling by zero
is not a useful operation.

Scale and translate are not commutative operations, therefore, calling them in
different orders will change the outcome.

Scaling lasts until love.draw() exits.

Synopses: ~

        |love.graphics.scale|(`sx`, `sy`)

Variants: ~

        |love.graphics.scale|(`sx`, `sy`)

        See function description

        Parameters: ~
            • {sx} (`number`)
                The scaling in the direction of the x-axis.
            • {sy} (`number`) (default: `sx`)
                The scaling in the direction of the y-axis. If omitted, it
                defaults to same as parameter sx.


-------------------------------------------------------------------------------
                                 *love2d-docs-love.graphics.setBackgroundColor*
|love.graphics.setBackgroundColor|

Sets the background color.

Synopses: ~

    1.  |love.graphics.setBackgroundColor|(`red`, `green`, `blue`,
        `alpha`)
    2.  |love.graphics.setBackgroundColor|(`rgba`)

Variants: ~

    1.  |love.graphics.setBackgroundColor|(`red`, `green`, `blue`,
        `alpha`)

    1.          See function description

        Parameters: ~
            • {red} (`number`)
                The red component (0-1).
            • {green} (`number`)
                The green component (0-1).
            • {blue} (`number`)
                The blue component (0-1).
            • {alpha} (`number`) (default: `1`)
                The alpha component (0-1).


    2.  |love.graphics.setBackgroundColor|(`rgba`)

    2.          See function description

        Parameters: ~
            • {rgba} (`table`)
                A numerical indexed table with the red, green, blue and alpha
                values as numbers. The alpha is optional and defaults to 1 if
                it is left out.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setBlendMode*
|love.graphics.setBlendMode|

Sets the blending mode.

Synopses: ~

    1.  |love.graphics.setBlendMode|(`mode`)
    2.  |love.graphics.setBlendMode|(`mode`, `alphamode`)

Variants: ~

    1.  |love.graphics.setBlendMode|(`mode`)

    1.          See function description

        Parameters: ~
            • {mode} (`BlendMode`) |love2d-docs-BlendMode|
                The blend mode to use.


    2.  |love.graphics.setBlendMode|(`mode`, `alphamode`)

    2.          The default 'alphamultiply' alpha mode should normally be preferred
        except when drawing content with pre-multiplied alpha. If content is
        drawn to a Canvas using the 'alphamultiply' mode, the Canvas texture
        will have pre-multiplied alpha afterwards, so the 'premultiplied' alpha
        mode should generally be used when drawing a Canvas to the screen.

        Parameters: ~
            • {mode} (`BlendMode`) |love2d-docs-BlendMode|
                The blend mode to use.
            • {alphamode} (`BlendAlphaMode`) |love2d-docs-BlendAlphaMode| (default: `'alphamultiply'`)
                What to do with the alpha of drawn objects when blending.


-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.setCanvas*
|love.graphics.setCanvas|

Captures drawing operations to a Canvas.

Synopses: ~

    1.  |love.graphics.setCanvas|(`canvas`, `mipmap`)
    2.  |love.graphics.setCanvas|()
    3.  |love.graphics.setCanvas|(`canvas1`, `canvas2`, `...`)
    4.  |love.graphics.setCanvas|(`canvas`, `slice`, `mipmap`)
    5.  |love.graphics.setCanvas|(`setup`)

Variants: ~

    1.  |love.graphics.setCanvas|(`canvas`, `mipmap`)

    1.          Sets the render target to a specified stencil or depth testing with an
        active Canvas, the stencil buffer or depth buffer must be explicitly
        enabled in setCanvas via the variants below.

        Note that no canvas should be active when ''love.graphics.present'' is
        called. ''love.graphics.present'' is called at the end of love.draw in
        the default love.run, hence if you activate a canvas using this
        function, you normally need to deactivate it at some point before
        ''love.draw'' finishes.

        Parameters: ~
            • {canvas} (`Canvas`) |love2d-docs-Canvas|
                The new target.
            • {mipmap} (`number`) (default: `1`)
                The mipmap level to render to, for Canvases with mipmaps.


    2.  |love.graphics.setCanvas|()

    2.          Resets the render target to the screen, i.e. re-enables drawing to the
        screen.


    3.  |love.graphics.setCanvas|(`canvas1`, `canvas2`, `...`)

    3.          Sets the render target to multiple simultaneous 2D Canvases. All
        drawing operations until the next ''love.graphics.setCanvas'' call will
        be redirected to the specified canvases and not shown on the screen.

        Normally all drawing operations will draw only to the first canvas
        passed to the function, but that can be changed if a pixel shader is
        used with the void effect function instead of the regular vec4 effect.

        All canvas arguments must have the same widths and heights and the same
        texture type. Not all computers which support Canvases will support
        multiple render targets. If love.graphics.isSupported('multicanvas')
        returns true, at least 4 simultaneously active canvases are supported.

        Parameters: ~
            • {canvas1} (`Canvas`) |love2d-docs-Canvas|
                The first render target.
            • {canvas2} (`Canvas`) |love2d-docs-Canvas|
                The second render target.
            • {...} (`Canvas`) |love2d-docs-Canvas|
                More canvases.


    4.  |love.graphics.setCanvas|(`canvas`, `slice`, `mipmap`)

    4.          Sets the render target to the specified layer/slice and mipmap level of
        the given non-2D Canvas. All drawing operations until the next
        ''love.graphics.setCanvas'' call will be redirected to the Canvas and
        not shown on the screen.

        Parameters: ~
            • {canvas} (`Canvas`) |love2d-docs-Canvas|
                The new render target.
            • {slice} (`number`)
                For cubemaps this is the cube face index to render to (between
                1 and 6). For Array textures this is the array layer. For
                volume textures this is the depth slice. 2D canvases should use
                a value of 1.
            • {mipmap} (`number`) (default: `1`)
                The mipmap level to render to, for Canvases with mipmaps.


    5.  |love.graphics.setCanvas|(`setup`)

    5.          Sets the active render target(s) and active stencil and depth buffers
        based on the specified setup information. All drawing operations until
        the next ''love.graphics.setCanvas'' call will be redirected to the
        specified Canvases and not shown on the screen.

        The RenderTargetSetup parameters can either be a Canvas|[1]|The Canvas
        to use for this active render target.}}

        {{param|number|mipmap (1)|The mipmap level to render to, for Canvases
        with [[Texture:getMipmapCount|mipmaps.}}

        {{param|number|layer (1)|Only used for Volume and Array-type Canvases.
        For Array textures this is the array layer to render to. For volume
        textures this is the depth slice.}}

        {{param|number|face (1)|Only used for Cubemap-type Canvases. The cube
        face index to render to (between 1 and 6)}}

        Parameters: ~
            • {setup} (`table`)
                A table specifying the active Canvas(es), their mipmap levels
                and active layers if applicable, and whether to use a stencil
                and/or depth buffer.
                • {1} (`RenderTargetSetup`)
                    The Canvas to render to.
                • {2} (`RenderTargetSetup`) (default: `nil`)
                    An additional Canvas to render to, if multiple simultaneous
                    render targets are wanted.
                • {...} (`RenderTargetSetup`)
                    Additional Canvases to render to, if multiple simultaneous
                    render targets are wanted.
                • {stencil} (`boolean`) (default: `false`)
                    Whether an internally managed stencil buffer should be
                    used, if the depthstencil field isn't set.
                • {depth} (`boolean`) (default: `false`)
                    Whether an internally managed depth buffer should be used,
                    if the depthstencil field isn't set.
                • {depthstencil} (`RenderTargetSetup`) (default: `nil`)
                    An optional custom depth/stencil formatted Canvas to use
                    for the depth and/or stencil buffer.


-------------------------------------------------------------------------------
                                           *love2d-docs-love.graphics.setColor*
|love.graphics.setColor|

Sets the color used for drawing.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

    1.  |love.graphics.setColor|(`red`, `green`, `blue`, `alpha`)
    2.  |love.graphics.setColor|(`rgba`)

Variants: ~

    1.  |love.graphics.setColor|(`red`, `green`, `blue`, `alpha`)

    1.          See function description

        Parameters: ~
            • {red} (`number`)
                The amount of red.
            • {green} (`number`)
                The amount of green.
            • {blue} (`number`)
                The amount of blue.
            • {alpha} (`number`) (default: `1`)
                The amount of alpha.  The alpha value will be applied to all
                subsequent draw operations, even the drawing of an image.


    2.  |love.graphics.setColor|(`rgba`)

    2.          See function description

        Parameters: ~
            • {rgba} (`table`)
                A numerical indexed table with the red, green, blue and alpha
                values as numbers. The alpha is optional and defaults to 1 if
                it is left out.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setColorMask*
|love.graphics.setColorMask|

Sets the color mask. Enables or disables specific color components when
rendering and clearing the screen. For example, if '''red''' is set to
'''false''', no further changes will be made to the red component of any
pixels.

Synopses: ~

    1.  |love.graphics.setColorMask|(`red`, `green`, `blue`, `alpha`)
    2.  |love.graphics.setColorMask|()

Variants: ~

    1.  |love.graphics.setColorMask|(`red`, `green`, `blue`, `alpha`)

    1.          Enables color masking for the specified color components.

        Parameters: ~
            • {red} (`boolean`)
                Render red component.
            • {green} (`boolean`)
                Render green component.
            • {blue} (`boolean`)
                Render blue component.
            • {alpha} (`boolean`)
                Render alpha component.


    2.  |love.graphics.setColorMask|()

    2.          Disables color masking.


-------------------------------------------------------------------------------
                                   *love2d-docs-love.graphics.setDefaultFilter*
|love.graphics.setDefaultFilter|

Sets the default scaling filters used with Images, Canvases, and Fonts.

Synopses: ~

        |love.graphics.setDefaultFilter|(`min`, `mag`, `anisotropy`)

Variants: ~

        |love.graphics.setDefaultFilter|(`min`, `mag`, `anisotropy`)

        This function does not apply retroactively to loaded images.

        Parameters: ~
            • {min} (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode used when scaling the image down.
            • {mag} (`FilterMode`) |love2d-docs-FilterMode| (default: `min`)
                Filter mode used when scaling the image up.
            • {anisotropy} (`number`) (default: `1`)
                Maximum amount of Anisotropic Filtering used.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setDepthMode*
|love.graphics.setDepthMode|

Configures depth testing and writing to the depth buffer.

This is low-level functionality designed for use with custom vertex shaders and
Meshes with custom vertex attributes. No higher level APIs are provided to set
the depth of 2D graphics such as shapes, lines, and Images.

Synopses: ~

    1.  |love.graphics.setDepthMode|(`comparemode`, `write`)
    2.  |love.graphics.setDepthMode|()

Variants: ~

    1.  |love.graphics.setDepthMode|(`comparemode`, `write`)

    1.          See function description

        Parameters: ~
            • {comparemode} (`CompareMode`) |love2d-docs-CompareMode|
                Depth comparison mode used for depth testing.
            • {write} (`boolean`)
                Whether to write update / write values to the depth buffer when
                rendering.


    2.  |love.graphics.setDepthMode|()

    2.          Disables depth testing and depth writes.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.setFont*
|love.graphics.setFont|

Set an already-loaded Font as the current font or create and load a new one
from the file and size.

It's recommended that Font objects are created with love.graphics.newFont in
the loading stage and then passed to this function in the drawing stage.

Synopses: ~

        |love.graphics.setFont|(`font`)

Variants: ~

        |love.graphics.setFont|(`font`)

        See function description

        Parameters: ~
            • {font} (`Font`) |love2d-docs-Font|
                The Font object to use.


-------------------------------------------------------------------------------
                                *love2d-docs-love.graphics.setFrontFaceWinding*
|love.graphics.setFrontFaceWinding|

Sets whether triangles with clockwise- or counterclockwise-ordered vertices are
considered front-facing.

This is designed for use in combination with Mesh face culling. Other
love.graphics shapes, lines, and sprites are not guaranteed to have a specific
winding order to their internal vertices.

Synopses: ~

        |love.graphics.setFrontFaceWinding|(`winding`)

Variants: ~

        |love.graphics.setFrontFaceWinding|(`winding`)

        See function description

        Parameters: ~
            • {winding} (`VertexWinding`) |love2d-docs-VertexWinding|
                The winding mode to use. The default winding is
                counterclockwise ('ccw').


-------------------------------------------------------------------------------
                                        *love2d-docs-love.graphics.setLineJoin*
|love.graphics.setLineJoin|

Sets the line join style. See LineJoin for the possible options.

Synopses: ~

        |love.graphics.setLineJoin|(`join`)

Variants: ~

        |love.graphics.setLineJoin|(`join`)

        See function description

        Parameters: ~
            • {join} (`LineJoin`) |love2d-docs-LineJoin|
                The LineJoin to use.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setLineStyle*
|love.graphics.setLineStyle|

Sets the line style.

Synopses: ~

        |love.graphics.setLineStyle|(`style`)

Variants: ~

        |love.graphics.setLineStyle|(`style`)

        See function description

        Parameters: ~
            • {style} (`LineStyle`) |love2d-docs-LineStyle|
                The LineStyle to use. Line styles include smooth and rough.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setLineWidth*
|love.graphics.setLineWidth|

Sets the line width.

Synopses: ~

        |love.graphics.setLineWidth|(`width`)

Variants: ~

        |love.graphics.setLineWidth|(`width`)

        See function description

        Parameters: ~
            • {width} (`number`)
                The width of the line.


-------------------------------------------------------------------------------
                                    *love2d-docs-love.graphics.setMeshCullMode*
|love.graphics.setMeshCullMode|

Sets whether back-facing triangles in a Mesh are culled.

This is designed for use with low level custom hardware-accelerated 3D
rendering via custom vertex attributes on Meshes, custom vertex shaders, and
depth testing with a depth buffer.

By default, both front- and back-facing triangles in Meshes are rendered.

Synopses: ~

        |love.graphics.setMeshCullMode|(`mode`)

Variants: ~

        |love.graphics.setMeshCullMode|(`mode`)

        See function description

        Parameters: ~
            • {mode} (`CullMode`) |love2d-docs-CullMode|
                The Mesh face culling mode to use (whether to render
                everything, cull back-facing triangles, or cull front-facing
                triangles).


-------------------------------------------------------------------------------
                                         *love2d-docs-love.graphics.setNewFont*
|love.graphics.setNewFont|

Creates and sets a new Font.

Synopses: ~

    1.  `font` = |love.graphics.setNewFont|(`size`)
    2.  `font` = |love.graphics.setNewFont|(`filename`, `size`)
    3.  `font` = |love.graphics.setNewFont|(`file`, `size`)
    4.  `font` = |love.graphics.setNewFont|(`data`, `size`)
    5.  `font` = |love.graphics.setNewFont|(`rasterizer`)

Variants: ~

    1.  `font` = |love.graphics.setNewFont|(`size`)

    1.          See function description

        Parameters: ~
            • {size} (`number`) (default: `12`)
                The size of the font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                The new font.
    2.  `font` = |love.graphics.setNewFont|(`filename`, `size`)

    2.          See function description

        Parameters: ~
            • {filename} (`string`)
                The path and name of the file with the font.
            • {size} (`number`) (default: `12`)
                The size of the font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                The new font.
    3.  `font` = |love.graphics.setNewFont|(`file`, `size`)

    3.          See function description

        Parameters: ~
            • {file} (`File`) |love2d-docs-File|
                A File with the font.
            • {size} (`number`) (default: `12`)
                The size of the font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                The new font.
    4.  `font` = |love.graphics.setNewFont|(`data`, `size`)

    4.          See function description

        Parameters: ~
            • {data} (`Data`) |love2d-docs-Data|
                A Data with the font.
            • {size} (`number`) (default: `12`)
                The size of the font.

        Return: ~
            (`Font`) |love2d-docs-Font|
                The new font.
    5.  `font` = |love.graphics.setNewFont|(`rasterizer`)

    5.          See function description

        Parameters: ~
            • {rasterizer} (`Rasterizer`) |love2d-docs-Rasterizer|
                A rasterizer.

        Return: ~
            (`Font`) |love2d-docs-Font|
                The new font.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setPointSize*
|love.graphics.setPointSize|

Sets the point size.

Synopses: ~

        |love.graphics.setPointSize|(`size`)

Variants: ~

        |love.graphics.setPointSize|(`size`)

        See function description

        Parameters: ~
            • {size} (`number`)
                The new point size.


-------------------------------------------------------------------------------
                                         *love2d-docs-love.graphics.setScissor*
|love.graphics.setScissor|

Sets or disables scissor.

The scissor limits the drawing area to a specified rectangle. This affects all
graphics calls, including love.graphics.clear.

The dimensions of the scissor is unaffected by graphical transformations
(translate, scale, ...).

Synopses: ~

    1.  |love.graphics.setScissor|(`x`, `y`, `width`, `height`)
    2.  |love.graphics.setScissor|()

Variants: ~

    1.  |love.graphics.setScissor|(`x`, `y`, `width`, `height`)

    1.          Limits the drawing area to a specified rectangle.

        Parameters: ~
            • {x} (`number`)
                x coordinate of upper left corner.
            • {y} (`number`)
                y coordinate of upper left corner.
            • {width} (`number`)
                width of clipping rectangle.
            • {height} (`number`)
                height of clipping rectangle.


    2.  |love.graphics.setScissor|()

    2.          Disables scissor.


-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.setShader*
|love.graphics.setShader|

Sets or resets a Shader as the current pixel effect or vertex shaders. All
drawing operations until the next ''love.graphics.setShader'' will be drawn
using the Shader object specified.

Synopses: ~

    1.  |love.graphics.setShader|(`shader`)
    2.  |love.graphics.setShader|()

Variants: ~

    1.  |love.graphics.setShader|(`shader`)

    1.          Sets the current shader to a specified Shader. All drawing operations
        until the next ''love.graphics.setShader'' will be drawn using the
        Shader object specified.

        Parameters: ~
            • {shader} (`Shader`) |love2d-docs-Shader|
                The new shader.


    2.  |love.graphics.setShader|()

    2.          Disables shaders, allowing unfiltered drawing operations.


-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.setStencilTest*
|love.graphics.setStencilTest|

Configures or disables stencil testing.

When stencil testing is enabled, the geometry of everything that is drawn
afterward will be clipped / stencilled out based on a comparison between the
arguments of this function and the stencil value of each pixel that the
geometry touches. The stencil values of pixels are affected via
love.graphics.stencil.

Synopses: ~

    1.  |love.graphics.setStencilTest|(`comparemode`, `comparevalue`)
    2.  |love.graphics.setStencilTest|()

Variants: ~

    1.  |love.graphics.setStencilTest|(`comparemode`, `comparevalue`)

    1.          See function description

        Parameters: ~
            • {comparemode} (`CompareMode`) |love2d-docs-CompareMode|
                The type of comparison to make for each pixel.
            • {comparevalue} (`number`)
                The value to use when comparing with the stencil value of each
                pixel. Must be between 0 and 255.


    2.  |love.graphics.setStencilTest|()

    2.          Disables stencil testing.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.graphics.setWireframe*
|love.graphics.setWireframe|

Sets whether wireframe lines will be used when drawing.

Synopses: ~

        |love.graphics.setWireframe|(`enable`)

Variants: ~

        |love.graphics.setWireframe|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable wireframe mode when drawing, false to disable
                it.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics.shear*
|love.graphics.shear|

Shears the coordinate system.

Synopses: ~

        |love.graphics.shear|(`kx`, `ky`)

Variants: ~

        |love.graphics.shear|(`kx`, `ky`)

        See function description

        Parameters: ~
            • {kx} (`number`)
                The shear factor on the x-axis.
            • {ky} (`number`)
                The shear factor on the y-axis.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.graphics.stencil*
|love.graphics.stencil|

Draws geometry as a stencil.

The geometry drawn by the supplied function sets invisible stencil values of
pixels, instead of setting pixel colors. The stencil buffer (which contains
those stencil values) can act like a mask / stencil -
love.graphics.setStencilTest can be used afterward to determine how further
rendering is affected by the stencil values in each pixel.

Stencil values are integers within the range of 255.

Synopses: ~

        |love.graphics.stencil|(`stencilfunction`, `action`, `value`,
        `keepvalues`)

Variants: ~

        |love.graphics.stencil|(`stencilfunction`, `action`, `value`,
        `keepvalues`)

        It is possible to draw to the screen and to the stencil values of
        pixels at the same time, by using love.graphics.setColorMask inside the
        stencil function to enable drawing to all color components.

        Parameters: ~
            • {stencilfunction} (`function`)
                Function which draws geometry. The stencil values of pixels,
                rather than the color of each pixel, will be affected by the
                geometry.
            • {action} (`StencilAction`) |love2d-docs-StencilAction| (default: `'replace'`)
                How to modify any stencil values of pixels that are touched by
                what's drawn in the stencil function.
            • {value} (`number`) (default: `1`)
                The new stencil value to use for pixels if the 'replace'
                stencil action is used. Has no effect with other stencil
                actions. Must be between 0 and 255.
            • {keepvalues} (`boolean`) (default: `false`)
                True to preserve old stencil values of pixels, false to re-set
                every pixel's stencil value to 0 before executing the stencil
                function. love.graphics.clear will also re-set all stencil
                values.


-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.transformPoint*
|love.graphics.transformPoint|

Converts the given 2D position from global coordinates into screen-space.

This effectively applies the current graphics transformations to the given
position. A similar Transform:transformPoint method exists for Transform
objects.

Synopses: ~

        `screenX`, `screenY` =
        |love.graphics.transformPoint|(`globalX`, `globalY`)

Variants: ~

        `screenX`, `screenY` =
        |love.graphics.transformPoint|(`globalX`, `globalY`)

        See function description

        Parameters: ~
            • {globalX} (`number`)
                The x component of the position in global coordinates.
            • {globalY} (`number`)
                The y component of the position in global coordinates.

        Returns: ~
            1. (`number`)
                The x component of the position with graphics transformations
                applied.

            2. (`number`)
                The y component of the position with graphics transformations
                applied.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.graphics.translate*
|love.graphics.translate|

Translates the coordinate system in two dimensions.

When this function is called with two numbers, dx, and dy, all the following
drawing operations take effect as if their x and y coordinates were x+dx and
y+dy.

Scale and translate are not commutative operations, therefore, calling them in
different orders will change the outcome.

This change lasts until love.draw() exits or else a love.graphics.pop reverts
to a previous love.graphics.push.

Translating using whole numbers will prevent tearing/blurring of images and
fonts draw after translating.

Synopses: ~

        |love.graphics.translate|(`dx`, `dy`)

Variants: ~

        |love.graphics.translate|(`dx`, `dy`)

        See function description

        Parameters: ~
            • {dx} (`number`)
                The translation relative to the x-axis.
            • {dy} (`number`)
                The translation relative to the y-axis.


-------------------------------------------------------------------------------
                                     *love2d-docs-love.graphics.validateShader*
|love.graphics.validateShader|

Validates shader code. Check if specified shader code does not contain any
errors.

Synopses: ~

    1.  `status`, `message` = |love.graphics.validateShader|(`gles`,
        `code`)
    2.  `status`, `message` = |love.graphics.validateShader|(`gles`,
        `pixelcode`, `vertexcode`)

Variants: ~

    1.  `status`, `message` = |love.graphics.validateShader|(`gles`,
        `code`)

    1.          See function description

        Parameters: ~
            • {gles} (`boolean`)
                Validate code as GLSL ES shader.
            • {code} (`string`)
                The pixel shader or vertex shader code, or a filename pointing
                to a file with the code.

        Returns: ~
            1. (`boolean`)
                true if specified shader code doesn't contain any errors. false
                otherwise.

            2. (`string`)
                Reason why shader code validation failed (or nil if validation
                succeded).
    2.  `status`, `message` = |love.graphics.validateShader|(`gles`,
        `pixelcode`, `vertexcode`)

    2.          See function description

        Parameters: ~
            • {gles} (`boolean`)
                Validate code as GLSL ES shader.
            • {pixelcode} (`string`)
                The pixel shader code, or a filename pointing to a file with
                the code.
            • {vertexcode} (`string`)
                The vertex shader code, or a filename pointing to a file with
                the code.

        Returns: ~
            1. (`boolean`)
                true if specified shader code doesn't contain any errors. false
                otherwise.

            2. (`string`)
                Reason why shader code validation failed (or nil if validation
                succeded).
-------------------------------------------------------------------------------
                                              *love2d-docs-love.graphics-types*
The types of |love.graphics|:

    |Canvas|                                               |love2d-docs-Canvas|
    |Drawable|                                           |love2d-docs-Drawable|
    |Font|                                                   |love2d-docs-Font|
    |Image|                                                 |love2d-docs-Image|
    |Mesh|                                                   |love2d-docs-Mesh|
    |ParticleSystem|                               |love2d-docs-ParticleSystem|
    |Quad|                                                   |love2d-docs-Quad|
    |Shader|                                               |love2d-docs-Shader|
    |SpriteBatch|                                     |love2d-docs-SpriteBatch|
    |Text|                                                   |love2d-docs-Text|
    |Texture|                                             |love2d-docs-Texture|
    |Video|                                                 |love2d-docs-Video|

-------------------------------------------------------------------------------
                                                           *love2d-docs-Canvas*
|Canvas|

A Canvas is used for off-screen rendering. Think of it as an invisible screen
that you can draw to, but that will not be visible until you draw it to the
actual visible screen. It is also known as "render to texture".

By drawing things that do not change position often (such as background items)
to the Canvas, and then drawing the entire Canvas instead of each item,  you
can reduce the number of draw operations performed each frame.

In versions prior to love.graphics.isSupported("canvas") could be used to check
for support at runtime.

                                              *love2d-docs-Canvas-constructors*
    constructors:

        |newCanvas|                                     |love2d-docs-newCanvas|
        |getCanvas|                                     |love2d-docs-getCanvas|

                                                *love2d-docs-Canvas-supertypes*
    supertypes:

        |Texture|                                         |love2d-docs-Texture|
        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                                  *love2d-docs-Canvas-subtypes*
    subtypes:

        None

                                                 *love2d-docs-Canvas-functions*
    functions:

        |generateMipmaps|                  |love2d-docs-Canvas:generateMipmaps|
        |getMSAA|                                  |love2d-docs-Canvas:getMSAA|
        |getMipmapMode|                      |love2d-docs-Canvas:getMipmapMode|
        |newImageData|                        |love2d-docs-Canvas:newImageData|
        |renderTo|                                |love2d-docs-Canvas:renderTo|

-------------------------------------------------------------------------------
                                           *love2d-docs-Canvas:generateMipmaps*
|Canvas:generateMipmaps|

Generates mipmaps for the Canvas, based on the contents of the
highest-resolution mipmap level.

The Canvas must be created with mipmaps set to a MipmapMode other than 'none'
for this function to work. It should only be called while the Canvas is not the
active render target.

If the mipmap mode is set to 'auto', this function is automatically called
inside love.graphics.setCanvas when switching from this Canvas to another
Canvas or to the main screen.

Synopses: ~

        |Canvas:generateMipmaps|()

Variants: ~

        |Canvas:generateMipmaps|()

        See function description


-------------------------------------------------------------------------------
                                                   *love2d-docs-Canvas:getMSAA*
|Canvas:getMSAA|

Gets the number of multisample antialiasing (MSAA) samples used when drawing to
the Canvas.

This may be different than the number used as an argument to
love.graphics.newCanvas if the system running LÖVE doesn't support that
number.

Synopses: ~

        `samples` = |Canvas:getMSAA|()

Variants: ~

        `samples` = |Canvas:getMSAA|()

        See function description

        Return: ~
            (`number`)
                The number of multisample antialiasing samples used by the
                canvas when drawing to it.
-------------------------------------------------------------------------------
                                             *love2d-docs-Canvas:getMipmapMode*
|Canvas:getMipmapMode|

Gets the MipmapMode this Canvas was created with.

Synopses: ~

        `mode` = |Canvas:getMipmapMode|()

Variants: ~

        `mode` = |Canvas:getMipmapMode|()

        See function description

        Return: ~
            (`MipmapMode`) |love2d-docs-MipmapMode|
                The mipmap mode this Canvas was created with.
-------------------------------------------------------------------------------
                                              *love2d-docs-Canvas:newImageData*
|Canvas:newImageData|

Generates ImageData from the contents of the Canvas.

Synopses: ~

    1.  `data` = |Canvas:newImageData|()
    2.  `data` = |Canvas:newImageData|(`slice`, `mipmap`, `x`, `y`,
        `width`, `height`)

Variants: ~

    1.  `data` = |Canvas:newImageData|()

    1.          See function description

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new ImageData made from the Canvas' contents.
    2.  `data` = |Canvas:newImageData|(`slice`, `mipmap`, `x`, `y`,
        `width`, `height`)

    2.          See function description

        Parameters: ~
            • {slice} (`number`)
                The cubemap face index, array index, or depth layer for
                cubemap, array, or volume type Canvases, respectively. This
                argument is ignored for regular 2D canvases.
            • {mipmap} (`number`) (default: `1`)
                The mipmap index to use, for Canvases with mipmaps.
            • {x} (`number`)
                The x-axis of the top-left corner (in pixels) of the area
                within the Canvas to capture.
            • {y} (`number`)
                The y-axis of the top-left corner (in pixels) of the area
                within the Canvas to capture.
            • {width} (`number`)
                The width in pixels of the area within the Canvas to capture.
            • {height} (`number`)
                The height in pixels of the area within the Canvas to capture.

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new ImageData made from the Canvas' contents.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Canvas:renderTo*
|Canvas:renderTo|

Render to the Canvas using a function.

This is a shortcut to love.graphics.setCanvas:

canvas:renderTo( func )

is the same as

love.graphics.setCanvas( canvas )

func()

love.graphics.setCanvas()

Synopses: ~

        |Canvas:renderTo|(`func`, `...`)

Variants: ~

        |Canvas:renderTo|(`func`, `...`)

        See function description

        Parameters: ~
            • {func} (`function`)
                A function performing drawing operations.
            • {...} (`any`)
                Additional arguments to call the function with.



-------------------------------------------------------------------------------
                                                         *love2d-docs-Drawable*
|Drawable|

Superclass for all things that can be drawn on screen. This is an abstract type
that can't be created directly.

                                            *love2d-docs-Drawable-constructors*
    constructors:

        None

                                              *love2d-docs-Drawable-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                *love2d-docs-Drawable-subtypes*
    subtypes:

        None

                                               *love2d-docs-Drawable-functions*
    functions:

        None

-------------------------------------------------------------------------------
                                                             *love2d-docs-Font*
|Font|

Defines the shape of characters that can be drawn onto the screen.

                                                *love2d-docs-Font-constructors*
    constructors:

        |getFont|                                         |love2d-docs-getFont|
        |newFont|                                         |love2d-docs-newFont|
        |setNewFont|                                   |love2d-docs-setNewFont|
        |newImageFont|                               |love2d-docs-newImageFont|

                                                  *love2d-docs-Font-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-Font-subtypes*
    subtypes:

        None

                                                   *love2d-docs-Font-functions*
    functions:

        |getAscent|                                |love2d-docs-Font:getAscent|
        |getBaseline|                            |love2d-docs-Font:getBaseline|
        |getDPIScale|                            |love2d-docs-Font:getDPIScale|
        |getDescent|                              |love2d-docs-Font:getDescent|
        |getFilter|                                |love2d-docs-Font:getFilter|
        |getHeight|                                |love2d-docs-Font:getHeight|
        |getKerning|                              |love2d-docs-Font:getKerning|
        |getLineHeight|                        |love2d-docs-Font:getLineHeight|
        |getWidth|                                  |love2d-docs-Font:getWidth|
        |getWrap|                                    |love2d-docs-Font:getWrap|
        |hasGlyphs|                                |love2d-docs-Font:hasGlyphs|
        |setFallbacks|                          |love2d-docs-Font:setFallbacks|
        |setFilter|                                |love2d-docs-Font:setFilter|
        |setLineHeight|                        |love2d-docs-Font:setLineHeight|

-------------------------------------------------------------------------------
                                                   *love2d-docs-Font:getAscent*
|Font:getAscent|

Gets the ascent of the Font.

The ascent spans the distance between the baseline and the top of the glyph
that reaches farthest from the baseline.

Synopses: ~

        `ascent` = |Font:getAscent|()

Variants: ~

        `ascent` = |Font:getAscent|()

        See function description

        Return: ~
            (`number`)
                The ascent of the Font in pixels.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Font:getBaseline*
|Font:getBaseline|

Gets the baseline of the Font.

Most scripts share the notion of a baseline: an imaginary horizontal line on
which characters rest. In some scripts, parts of glyphs lie below the baseline.

Synopses: ~

        `baseline` = |Font:getBaseline|()

Variants: ~

        `baseline` = |Font:getBaseline|()

        See function description

        Return: ~
            (`number`)
                The baseline of the Font in pixels.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Font:getDPIScale*
|Font:getDPIScale|

Gets the DPI scale factor of the Font.

The DPI scale factor represents relative pixel density. A DPI scale factor of 2
means the font's glyphs have twice the pixel density in each dimension (4 times
as many pixels in the same area) compared to a font with a DPI scale factor of
1.

The font size of TrueType fonts is scaled internally by the font's specified
DPI scale factor. By default, LÖVE uses the screen's DPI scale factor when
creating TrueType fonts.

Synopses: ~

        `dpiscale` = |Font:getDPIScale|()

Variants: ~

        `dpiscale` = |Font:getDPIScale|()

        See function description

        Return: ~
            (`number`)
                The DPI scale factor of the Font.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Font:getDescent*
|Font:getDescent|

Gets the descent of the Font.

The descent spans the distance between the baseline and the lowest descending
glyph in a typeface.

Synopses: ~

        `descent` = |Font:getDescent|()

Variants: ~

        `descent` = |Font:getDescent|()

        See function description

        Return: ~
            (`number`)
                The descent of the Font in pixels.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Font:getFilter*
|Font:getFilter|

Gets the filter mode for a font.

Synopses: ~

        `min`, `mag`, `anisotropy` = |Font:getFilter|()

Variants: ~

        `min`, `mag`, `anisotropy` = |Font:getFilter|()

        See function description

        Returns: ~
            1. (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode used when minifying the font.

            2. (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode used when magnifying the font.

            3. (`number`)
                Maximum amount of anisotropic filtering used.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Font:getHeight*
|Font:getHeight|

Gets the height of the Font.

The height of the font is the size including any spacing; the height which it
will need.

Synopses: ~

        `height` = |Font:getHeight|()

Variants: ~

        `height` = |Font:getHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the Font in pixels.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Font:getKerning*
|Font:getKerning|

Gets the kerning between two characters in the Font.

Kerning is normally handled automatically in love.graphics.print, Text objects,
Font:getWidth, Font:getWrap, etc. This function is useful when stitching text
together manually.

Synopses: ~

    1.  `kerning` = |Font:getKerning|(`leftchar`, `rightchar`)
    2.  `kerning` = |Font:getKerning|(`leftglyph`, `rightglyph`)

Variants: ~

    1.  `kerning` = |Font:getKerning|(`leftchar`, `rightchar`)

    1.          See function description

        Parameters: ~
            • {leftchar} (`string`)
                The left character.
            • {rightchar} (`string`)
                The right character.

        Return: ~
            (`number`)
                The kerning amount to add to the spacing between the two
                characters. May be negative.
    2.  `kerning` = |Font:getKerning|(`leftglyph`, `rightglyph`)

    2.          See function description

        Parameters: ~
            • {leftglyph} (`number`)
                The unicode number for the left glyph.
            • {rightglyph} (`number`)
                The unicode number for the right glyph.

        Return: ~
            (`number`)
                The kerning amount to add to the spacing between the two
                characters. May be negative.
-------------------------------------------------------------------------------
                                               *love2d-docs-Font:getLineHeight*
|Font:getLineHeight|

Gets the line height.

This will be the value previously set by Font:setLineHeight, or 1.0 by default.

Synopses: ~

        `height` = |Font:getLineHeight|()

Variants: ~

        `height` = |Font:getLineHeight|()

        See function description

        Return: ~
            (`number`)
                The current line height.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Font:getWidth*
|Font:getWidth|

Determines the maximum width (accounting for newlines) taken by the given
string.

Synopses: ~

        `width` = |Font:getWidth|(`text`)

Variants: ~

        `width` = |Font:getWidth|(`text`)

        See function description

        Parameters: ~
            • {text} (`string`)
                A string.

        Return: ~
            (`number`)
                The width of the text.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Font:getWrap*
|Font:getWrap|

Gets formatting information for text, given a wrap limit.

This function accounts for newlines correctly (i.e. '\n').

Synopses: ~

    1.  `width`, `wrappedtext` = |Font:getWrap|(`text`, `wraplimit`)
    2.  `width`, `wrappedtext` = |Font:getWrap|(`coloredtext`,
        `wraplimit`)

Variants: ~

    1.  `width`, `wrappedtext` = |Font:getWrap|(`text`, `wraplimit`)

    1.          See function description

        Parameters: ~
            • {text} (`string`)
                The text that will be wrapped.
            • {wraplimit} (`number`)
                The maximum width in pixels of each line that ''text'' is
                allowed before wrapping.

        Returns: ~
            1. (`number`)
                The maximum width of the wrapped text.

            2. (`table`)
                A sequence containing each line of text that was wrapped.
    2.  `width`, `wrappedtext` = |Font:getWrap|(`coloredtext`,
        `wraplimit`)

    2.          See function description

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {wraplimit} (`number`)
                The maximum width in pixels of each line that ''text'' is
                allowed before wrapping.

        Returns: ~
            1. (`number`)
                The maximum width of the wrapped text.

            2. (`table`)
                A sequence containing each line of text that was wrapped.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Font:hasGlyphs*
|Font:hasGlyphs|

Gets whether the Font can render a character or string.

Synopses: ~

    1.  `hasglyph` = |Font:hasGlyphs|(`text`)
    2.  `hasglyph` = |Font:hasGlyphs|(`character1`, `character2`)
    3.  `hasglyph` = |Font:hasGlyphs|(`codepoint1`, `codepoint2`)

Variants: ~

    1.  `hasglyph` = |Font:hasGlyphs|(`text`)

    1.          See function description

        Parameters: ~
            • {text} (`string`)
                A UTF-8 encoded unicode string.

        Return: ~
            (`boolean`)
                Whether the font can render all the UTF-8 characters in the
                string.
    2.  `hasglyph` = |Font:hasGlyphs|(`character1`, `character2`)

    2.          See function description

        Parameters: ~
            • {character1} (`string`)
                A unicode character.
            • {character2} (`string`)
                Another unicode character.

        Return: ~
            (`boolean`)
                Whether the font can render all the glyphs represented by the
                characters.
    3.  `hasglyph` = |Font:hasGlyphs|(`codepoint1`, `codepoint2`)

    3.          See function description

        Parameters: ~
            • {codepoint1} (`number`)
                A unicode codepoint number.
            • {codepoint2} (`number`)
                Another unicode codepoint number.

        Return: ~
            (`boolean`)
                Whether the font can render all the glyphs represented by the
                codepoint numbers.
-------------------------------------------------------------------------------
                                                *love2d-docs-Font:setFallbacks*
|Font:setFallbacks|

Sets the fallback fonts. When the Font doesn't contain a glyph, it will
substitute the glyph from the next subsequent fallback Fonts. This is akin to
setting a 'font stack' in Cascading Style Sheets (CSS).

Synopses: ~

        |Font:setFallbacks|(`fallbackfont1`, `...`)

Variants: ~

        |Font:setFallbacks|(`fallbackfont1`, `...`)

        If this is called it should be before love.graphics.print,
        Font:getWrap, and other Font methods which use glyph positioning
        information are called.

        Every fallback Font must be created from the same file type as the
        primary Font. For example, a Font created from a .ttf file can only use
        fallback Fonts that were created from .ttf files.

        Parameters: ~
            • {fallbackfont1} (`Font`) |love2d-docs-Font|
                The first fallback Font to use.
            • {...} (`Font`) |love2d-docs-Font|
                Additional fallback Fonts.


-------------------------------------------------------------------------------
                                                   *love2d-docs-Font:setFilter*
|Font:setFilter|

Sets the filter mode for a font.

Synopses: ~

        |Font:setFilter|(`min`, `mag`, `anisotropy`)

Variants: ~

        |Font:setFilter|(`min`, `mag`, `anisotropy`)

        See function description

        Parameters: ~
            • {min} (`FilterMode`) |love2d-docs-FilterMode|
                How to scale a font down.
            • {mag} (`FilterMode`) |love2d-docs-FilterMode|
                How to scale a font up.
            • {anisotropy} (`number`) (default: `1`)
                Maximum amount of anisotropic filtering used.


-------------------------------------------------------------------------------
                                               *love2d-docs-Font:setLineHeight*
|Font:setLineHeight|

Sets the line height.

When rendering the font in lines the actual height will be determined by the
line height multiplied by the height of the font. The default is 1.0.

Synopses: ~

        |Font:setLineHeight|(`height`)

Variants: ~

        |Font:setLineHeight|(`height`)

        See function description

        Parameters: ~
            • {height} (`number`)
                The new line height.



-------------------------------------------------------------------------------
                                                            *love2d-docs-Image*
|Image|

Drawable image type.

                                               *love2d-docs-Image-constructors*
    constructors:

        |newImage|                                       |love2d-docs-newImage|
        |newVolumeImage|                           |love2d-docs-newVolumeImage|
        |newCubeImage|                               |love2d-docs-newCubeImage|
        |newArrayImage|                             |love2d-docs-newArrayImage|

                                                 *love2d-docs-Image-supertypes*
    supertypes:

        |Texture|                                         |love2d-docs-Texture|
        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                                   *love2d-docs-Image-subtypes*
    subtypes:

        None

                                                  *love2d-docs-Image-functions*
    functions:

        |isCompressed|                         |love2d-docs-Image:isCompressed|
        |isFormatLinear|                     |love2d-docs-Image:isFormatLinear|
        |replacePixels|                       |love2d-docs-Image:replacePixels|

-------------------------------------------------------------------------------
                                               *love2d-docs-Image:isCompressed*
|Image:isCompressed|

Gets whether the Image was created from CompressedData.

Compressed images take up less space in VRAM, and drawing a compressed image
will generally be more efficient than drawing one created from raw pixel data.

Synopses: ~

        `compressed` = |Image:isCompressed|()

Variants: ~

        `compressed` = |Image:isCompressed|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Image is stored as a compressed texture on the GPU.
-------------------------------------------------------------------------------
                                             *love2d-docs-Image:isFormatLinear*
|Image:isFormatLinear|

Gets whether the Image was created with the linear (non-gamma corrected) flag
set to true.

This method always returns false when gamma-correct rendering is not enabled.

Synopses: ~

        `linear` = |Image:isFormatLinear|()

Variants: ~

        `linear` = |Image:isFormatLinear|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Image's internal pixel format is linear (not gamma
                corrected), when gamma-correct rendering is enabled.
-------------------------------------------------------------------------------
                                              *love2d-docs-Image:replacePixels*
|Image:replacePixels|

Replace the contents of an Image.

Synopses: ~

        |Image:replacePixels|(`data`, `slice`, `mipmap`, `x`, `y`,
        `reloadmipmaps`)

Variants: ~

        |Image:replacePixels|(`data`, `slice`, `mipmap`, `x`, `y`,
        `reloadmipmaps`)

        See function description

        Parameters: ~
            • {data} (`ImageData`) |love2d-docs-ImageData|
                The new ImageData to replace the contents with.
            • {slice} (`number`) (default: `1`)
                Which cubemap face, array index, or volume layer to replace, if
                applicable.
            • {mipmap} (`number`) (default: `1`)
                The mimap level to replace, if the Image has mipmaps.
            • {x} (`number`) (default: `0`)
                The x-offset in pixels from the top-left of the image to
                replace. The given ImageData's width plus this value must not
                be greater than the pixel width of the Image's specified mipmap
                level.
            • {y} (`number`) (default: `0`)
                The y-offset in pixels from the top-left of the image to
                replace. The given ImageData's height plus this value must not
                be greater than the pixel height of the Image's specified
                mipmap level.
            • {reloadmipmaps} (`boolean`) (default: `false`)
                Whether to generate new mipmaps after replacing the Image's
                pixels. True by default if the Image was created with
                automatically generated mipmaps, false by default otherwise.



-------------------------------------------------------------------------------
                                                             *love2d-docs-Mesh*
|Mesh|

A 2D polygon mesh used for drawing arbitrary textured shapes.

                                                *love2d-docs-Mesh-constructors*
    constructors:

        |newMesh|                                         |love2d-docs-newMesh|

                                                  *love2d-docs-Mesh-supertypes*
    supertypes:

        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-Mesh-subtypes*
    subtypes:

        None

                                                   *love2d-docs-Mesh-functions*
    functions:

        |attachAttribute|                    |love2d-docs-Mesh:attachAttribute|
        |detachAttribute|                    |love2d-docs-Mesh:detachAttribute|
        |flush|                                        |love2d-docs-Mesh:flush|
        |getDrawMode|                            |love2d-docs-Mesh:getDrawMode|
        |getDrawRange|                          |love2d-docs-Mesh:getDrawRange|
        |getTexture|                              |love2d-docs-Mesh:getTexture|
        |getVertex|                                |love2d-docs-Mesh:getVertex|
        |getVertexAttribute|              |love2d-docs-Mesh:getVertexAttribute|
        |getVertexCount|                      |love2d-docs-Mesh:getVertexCount|
        |getVertexFormat|                    |love2d-docs-Mesh:getVertexFormat|
        |getVertexMap|                          |love2d-docs-Mesh:getVertexMap|
        |isAttributeEnabled|              |love2d-docs-Mesh:isAttributeEnabled|
        |setAttributeEnabled|            |love2d-docs-Mesh:setAttributeEnabled|
        |setDrawMode|                            |love2d-docs-Mesh:setDrawMode|
        |setDrawRange|                          |love2d-docs-Mesh:setDrawRange|
        |setTexture|                              |love2d-docs-Mesh:setTexture|
        |setVertex|                                |love2d-docs-Mesh:setVertex|
        |setVertexAttribute|              |love2d-docs-Mesh:setVertexAttribute|
        |setVertexMap|                          |love2d-docs-Mesh:setVertexMap|
        |setVertices|                            |love2d-docs-Mesh:setVertices|

-------------------------------------------------------------------------------
                                             *love2d-docs-Mesh:attachAttribute*
|Mesh:attachAttribute|

Attaches a vertex attribute from a different Mesh onto this Mesh, for use when
drawing. This can be used to share vertex attribute data between several
different Meshes.

Synopses: ~

    1.  |Mesh:attachAttribute|(`name`, `mesh`)
    2.  |Mesh:attachAttribute|(`name`, `mesh`, `step`, `attachname`)

Variants: ~

    1.  |Mesh:attachAttribute|(`name`, `mesh`)

    1.          See function description

        Parameters: ~
            • {name} (`string`)
                The name of the vertex attribute to attach.
            • {mesh} (`Mesh`) |love2d-docs-Mesh|
                The Mesh to get the vertex attribute from.


    2.  |Mesh:attachAttribute|(`name`, `mesh`, `step`, `attachname`)

    2.          If a Mesh wasn't created with a custom vertex format, it will have 3
        vertex attributes named VertexPosition, VertexTexCoord, and
        VertexColor.

        Custom named attributes can be accessed in a vertex shader by declaring
        them as attribute vec4 MyCustomAttributeName; at the top-level of the
        vertex shader code. The name must match what was specified in the
        Mesh's vertex format and in the name argument of Mesh:attachAttribute.

        Parameters: ~
            • {name} (`string`)
                The name of the vertex attribute to attach.
            • {mesh} (`Mesh`) |love2d-docs-Mesh|
                The Mesh to get the vertex attribute from.
            • {step} (`VertexAttributeStep`) |love2d-docs-VertexAttributeStep| (default: `'pervertex'`)
                Whether the attribute will be per-vertex or per-instance when
                the mesh is drawn.
            • {attachname} (`string`) (default: `name`)
                The name of the attribute to use in shader code. Defaults to
                the name of the attribute in the given mesh. Can be used to use
                a different name for this attribute when rendering.


-------------------------------------------------------------------------------
                                             *love2d-docs-Mesh:detachAttribute*
|Mesh:detachAttribute|

Removes a previously attached vertex attribute from this Mesh.

Synopses: ~

        `success` = |Mesh:detachAttribute|(`name`)

Variants: ~

        `success` = |Mesh:detachAttribute|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the attached vertex attribute to detach.

        Return: ~
            (`boolean`)
                Whether the attribute was successfully detached.
-------------------------------------------------------------------------------
                                                       *love2d-docs-Mesh:flush*
|Mesh:flush|

Immediately sends all modified vertex data in the Mesh to the graphics card.

Normally it isn't necessary to call this method as love.graphics.draw(mesh,
...) will do it automatically if needed, but explicitly using **Mesh:flush**
gives more control over when the work happens.

If this method is used, it generally shouldn't be called more than once (at
most) between love.graphics.draw(mesh, ...) calls.

Synopses: ~

        |Mesh:flush|()

Variants: ~

        |Mesh:flush|()

        See function description


-------------------------------------------------------------------------------
                                                 *love2d-docs-Mesh:getDrawMode*
|Mesh:getDrawMode|

Gets the mode used when drawing the Mesh.

Synopses: ~

        `mode` = |Mesh:getDrawMode|()

Variants: ~

        `mode` = |Mesh:getDrawMode|()

        See function description

        Return: ~
            (`MeshDrawMode`) |love2d-docs-MeshDrawMode|
                The mode used when drawing the Mesh.
-------------------------------------------------------------------------------
                                                *love2d-docs-Mesh:getDrawRange*
|Mesh:getDrawRange|

Gets the range of vertices used when drawing the Mesh.

Synopses: ~

        `min`, `max` = |Mesh:getDrawRange|()

Variants: ~

        `min`, `max` = |Mesh:getDrawRange|()

        If the Mesh's draw range has not been set previously with
        Mesh:setDrawRange, this function will return nil.

        Returns: ~
            1. (`number`)
                The index of the first vertex used when drawing, or the index
                of the first value in the vertex map used if one is set for
                this Mesh.

            2. (`number`)
                The index of the last vertex used when drawing, or the index of
                the last value in the vertex map used if one is set for this
                Mesh.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Mesh:getTexture*
|Mesh:getTexture|

Gets the texture (Image or Canvas) used when drawing the Mesh.

Synopses: ~

        `texture` = |Mesh:getTexture|()

Variants: ~

        `texture` = |Mesh:getTexture|()

        See function description

        Return: ~
            (`Texture`) |love2d-docs-Texture|
                The Image or Canvas to texture the Mesh with when drawing, or
                nil if none is set.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Mesh:getVertex*
|Mesh:getVertex|

Gets the properties of a vertex in the Mesh.

In versions prior to 11.0, color and byte component values were within the
range of 0 to 255 instead of 0 to 1.

Synopses: ~

    1.  `attributecomponent`, `...` = |Mesh:getVertex|(`index`)
    2.  `x`, `y`, `u`, `v`, `r`, `g`, `b`, `a` =
        |Mesh:getVertex|(`index`)

Variants: ~

    1.  `attributecomponent`, `...` = |Mesh:getVertex|(`index`)

    1.          The values are returned in the same order as the vertex attributes in
        the Mesh's vertex format. A standard Mesh that wasn't created with a
        custom vertex format will return two position numbers, two texture
        coordinate numbers, and four color components: x, y, u, v, r, g, b, a.

        Parameters: ~
            • {index} (`number`)
                The one-based index of the vertex you want to retrieve the
                information for.

        Returns: ~
            1. (`number`)
                The first component of the first vertex attribute in the
                specified vertex.

            2. (`number`)
                Additional components of all vertex attributes in the specified
                vertex.
    2.  `x`, `y`, `u`, `v`, `r`, `g`, `b`, `a` =
        |Mesh:getVertex|(`index`)

    2.          Gets the vertex components of a Mesh that wasn't created with a custom
        vertex format.

        Parameters: ~
            • {index} (`number`)
                The index of the vertex you want to retrieve the information
                for.

        Returns: ~
            1. (`number`)
                The position of the vertex on the x-axis.

            2. (`number`)
                The position of the vertex on the y-axis.

            3. (`number`)
                The horizontal component of the texture coordinate.

            4. (`number`)
                The vertical component of the texture coordinate.

            5. (`number`)
                The red component of the vertex's color.

            6. (`number`)
                The green component of the vertex's color.

            7. (`number`)
                The blue component of the vertex's color.

            8. (`number`)
                The alpha component of the vertex's color.
-------------------------------------------------------------------------------
                                          *love2d-docs-Mesh:getVertexAttribute*
|Mesh:getVertexAttribute|

Gets the properties of a specific attribute within a vertex in the Mesh.

Meshes without a custom vertex format specified in love.graphics.newMesh have
position as their first attribute, texture coordinates as their second
attribute, and color as their third attribute.

Synopses: ~

        `value1`, `value2`, `...` =
        |Mesh:getVertexAttribute|(`vertexindex`, `attributeindex`)

Variants: ~

        `value1`, `value2`, `...` =
        |Mesh:getVertexAttribute|(`vertexindex`, `attributeindex`)

        See function description

        Parameters: ~
            • {vertexindex} (`number`)
                The index of the the vertex you want to retrieve the attribute
                for (one-based).
            • {attributeindex} (`number`)
                The index of the attribute within the vertex to be retrieved
                (one-based).

        Returns: ~
            1. (`number`)
                The value of the first component of the attribute.

            2. (`number`)
                The value of the second component of the attribute.

            3. (`number`)
                Any additional vertex attribute components.
-------------------------------------------------------------------------------
                                              *love2d-docs-Mesh:getVertexCount*
|Mesh:getVertexCount|

Gets the total number of vertices in the Mesh.

Synopses: ~

        `count` = |Mesh:getVertexCount|()

Variants: ~

        `count` = |Mesh:getVertexCount|()

        See function description

        Return: ~
            (`number`)
                The total number of vertices in the mesh.
-------------------------------------------------------------------------------
                                             *love2d-docs-Mesh:getVertexFormat*
|Mesh:getVertexFormat|

Gets the vertex format that the Mesh was created with.

Synopses: ~

        `format` = |Mesh:getVertexFormat|()

Variants: ~

        `format` = |Mesh:getVertexFormat|()

        If a Mesh wasn't created with a custom vertex format, it will have the
        following vertex format:

        defaultformat = {

            {'VertexPosition', 'float', 2}, -- The x,y position of each vertex.

            {'VertexTexCoord', 'float', 2}, -- The u,v texture coordinates of
        each vertex.

            {'VertexColor', 'byte', 4} -- The r,g,b,a color of each vertex.

        }

        Return: ~
            (`table`)
                The vertex format of the Mesh, which is a table containing
                tables for each vertex attribute the Mesh was created with, in
                the form of {attribute, ...}.
-------------------------------------------------------------------------------
                                                *love2d-docs-Mesh:getVertexMap*
|Mesh:getVertexMap|

Gets the vertex map for the Mesh. The vertex map describes the order in which
the vertices are used when the Mesh is drawn. The vertices, vertex map, and
mesh draw mode work together to determine what exactly is displayed on the
screen.

If no vertex map has been set previously via Mesh:setVertexMap, then this
function will return nil in LÖVE 0.10.0+, or an empty table in 0.9.2 and
older.

Synopses: ~

        `map` = |Mesh:getVertexMap|()

Variants: ~

        `map` = |Mesh:getVertexMap|()

        See function description

        Return: ~
            (`table`)
                A table containing the list of vertex indices used when
                drawing.
-------------------------------------------------------------------------------
                                          *love2d-docs-Mesh:isAttributeEnabled*
|Mesh:isAttributeEnabled|

Gets whether a specific vertex attribute in the Mesh is enabled. Vertex data
from disabled attributes is not used when drawing the Mesh.

Synopses: ~

        `enabled` = |Mesh:isAttributeEnabled|(`name`)

Variants: ~

        `enabled` = |Mesh:isAttributeEnabled|(`name`)

        If a Mesh wasn't created with a custom vertex format, it will have 3
        vertex attributes named VertexPosition, VertexTexCoord, and
        VertexColor. Otherwise the attribute name must either match one of the
        vertex attributes specified in the vertex format when creating the
        Mesh,

        or must match a vertex attribute from another Mesh attached to this
        Mesh via Mesh:attachAttribute.

        Parameters: ~
            • {name} (`string`)
                The name of the vertex attribute to be checked.

        Return: ~
            (`boolean`)
                Whether the vertex attribute is used when drawing this Mesh.
-------------------------------------------------------------------------------
                                         *love2d-docs-Mesh:setAttributeEnabled*
|Mesh:setAttributeEnabled|

Enables or disables a specific vertex attribute in the Mesh. Vertex data from
disabled attributes is not used when drawing the Mesh.

Synopses: ~

        |Mesh:setAttributeEnabled|(`name`, `enable`)

Variants: ~

        |Mesh:setAttributeEnabled|(`name`, `enable`)

        If a Mesh wasn't created with a custom vertex format, it will have 3
        vertex attributes named VertexPosition, VertexTexCoord, and
        VertexColor. Otherwise the attribute name must either match one of the
        vertex attributes specified in the vertex format when creating the
        Mesh,

        or must match a vertex attribute from another Mesh attached to this
        Mesh via Mesh:attachAttribute.

        Parameters: ~
            • {name} (`string`)
                The name of the vertex attribute to enable or disable.
            • {enable} (`boolean`)
                Whether the vertex attribute is used when drawing this Mesh.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Mesh:setDrawMode*
|Mesh:setDrawMode|

Sets the mode used when drawing the Mesh.

Synopses: ~

        |Mesh:setDrawMode|(`mode`)

Variants: ~

        |Mesh:setDrawMode|(`mode`)

        See function description

        Parameters: ~
            • {mode} (`MeshDrawMode`) |love2d-docs-MeshDrawMode|
                The mode to use when drawing the Mesh.


-------------------------------------------------------------------------------
                                                *love2d-docs-Mesh:setDrawRange*
|Mesh:setDrawRange|

Restricts the drawn vertices of the Mesh to a subset of the total.

Synopses: ~

    1.  |Mesh:setDrawRange|(`start`, `count`)
    2.  |Mesh:setDrawRange|()

Variants: ~

    1.  |Mesh:setDrawRange|(`start`, `count`)

    1.          See function description

        Parameters: ~
            • {start} (`number`)
                The index of the first vertex to use when drawing, or the index
                of the first value in the vertex map to use if one is set for
                this Mesh.
            • {count} (`number`)
                The number of vertices to use when drawing, or number of values
                in the vertex map to use if one is set for this Mesh.


    2.  |Mesh:setDrawRange|()

    2.          Allows all vertices in the Mesh to be drawn.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Mesh:setTexture*
|Mesh:setTexture|

Sets the texture (Image or Canvas) used when drawing the Mesh.

Synopses: ~

    1.  |Mesh:setTexture|(`texture`)
    2.  |Mesh:setTexture|()

Variants: ~

    1.  |Mesh:setTexture|(`texture`)

    1.          See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The Image or Canvas to texture the Mesh with when drawing.


    2.  |Mesh:setTexture|()

    2.          Disables any texture from being used when drawing the Mesh. Untextured
        meshes have a white color by default.


-------------------------------------------------------------------------------
                                                   *love2d-docs-Mesh:setVertex*
|Mesh:setVertex|

Sets the properties of a vertex in the Mesh.

In versions prior to 11.0, color and byte component values were within the
range of 0 to 255 instead of 0 to 1.

Synopses: ~

    1.  |Mesh:setVertex|(`index`, `attributecomponent`, `...`)
    2.  |Mesh:setVertex|(`index`, `vertex`)
    3.  |Mesh:setVertex|(`index`, `x`, `y`, `u`, `v`, `r`, `g`, `b`,
        `a`)
    4.  |Mesh:setVertex|(`index`, `vertex`)

Variants: ~

    1.  |Mesh:setVertex|(`index`, `attributecomponent`, `...`)

    1.          The arguments are in the same order as the vertex attributes in the
        Mesh's vertex format. A standard Mesh that wasn't created with a custom
        vertex format will use two position numbers, two texture coordinate
        numbers, and four color components per vertex: x, y, u, v, r, g, b, a.

        If no value is supplied for a specific vertex attribute component, it
        will be set to a default value of 0 if its data type is 'float', or 1
        if its data type is 'byte'.

        Parameters: ~
            • {index} (`number`)
                The index of the the vertex you want to modify (one-based).
            • {attributecomponent} (`number`)
                The first component of the first vertex attribute in the
                specified vertex.
            • {...} (`number`)
                Additional components of all vertex attributes in the specified
                vertex.


    2.  |Mesh:setVertex|(`index`, `vertex`)

    2.          The table indices are in the same order as the vertex attributes in the
        Mesh's vertex format. A standard Mesh that wasn't created with a custom
        vertex format will use two position numbers, two texture coordinate
        numbers, and four color components per vertex: x, y, u, v, r, g, b, a.

        If no value is supplied for a specific vertex attribute component, it
        will be set to a default value of 0 if its data type is 'float', or 1
        if its data type is 'byte'.

        Parameters: ~
            • {index} (`number`)
                The index of the the vertex you want to modify (one-based).
            • {vertex} (`table`)
                A table with vertex information, in the form of
                {attributecomponent, ...}.
                • {attributecomponent} (`number`)
                    The first component of the first vertex attribute in the
                    specified vertex.
                • {...} (`number`)
                    Additional components of all vertex attributes in the
                    specified vertex.


    3.  |Mesh:setVertex|(`index`, `x`, `y`, `u`, `v`, `r`, `g`, `b`,
        `a`)

    3.          Sets the vertex components of a Mesh that wasn't created with a custom
        vertex format.

        Parameters: ~
            • {index} (`number`)
                The index of the the vertex you want to modify (one-based).
            • {x} (`number`)
                The position of the vertex on the x-axis.
            • {y} (`number`)
                The position of the vertex on the y-axis.
            • {u} (`number`)
                The horizontal component of the texture coordinate.
            • {v} (`number`)
                The vertical component of the texture coordinate.
            • {r} (`number`) (default: `1`)
                The red component of the vertex's color.
            • {g} (`number`) (default: `1`)
                The green component of the vertex's color.
            • {b} (`number`) (default: `1`)
                The blue component of the vertex's color.
            • {a} (`number`) (default: `1`)
                The alpha component of the vertex's color.


    4.  |Mesh:setVertex|(`index`, `vertex`)

    4.          Sets the vertex components of a Mesh that wasn't created with a custom
        vertex format.

        Parameters: ~
            • {index} (`number`)
                The index of the the vertex you want to modify (one-based).
            • {vertex} (`table`)
                A table with vertex information.
                • {1} (`number`)
                    The position of the vertex on the x-axis.
                • {2} (`number`)
                    The position of the vertex on the y-axis.
                • {3} (`number`)
                    The u texture coordinate. Texture coordinates are normally
                    in the range of 1, but can be greater or less (see
                    WrapMode.)
                • {4} (`number`)
                    The v texture coordinate. Texture coordinates are normally
                    in the range of 1, but can be greater or less (see
                    WrapMode.)
                • {5} (`number`) (default: `1`)
                    The red color component.
                • {6} (`number`) (default: `1`)
                    The green color component.
                • {7} (`number`) (default: `1`)
                    The blue color component.
                • {8} (`number`) (default: `1`)
                    The alpha color component.


-------------------------------------------------------------------------------
                                          *love2d-docs-Mesh:setVertexAttribute*
|Mesh:setVertexAttribute|

Sets the properties of a specific attribute within a vertex in the Mesh.

Meshes without a custom vertex format specified in love.graphics.newMesh have
position as their first attribute, texture coordinates as their second
attribute, and color as their third attribute.

Synopses: ~

        |Mesh:setVertexAttribute|(`vertexindex`, `attributeindex`,
        `value1`, `value2`, `...`)

Variants: ~

        |Mesh:setVertexAttribute|(`vertexindex`, `attributeindex`,
        `value1`, `value2`, `...`)

        Attribute components which exist within the attribute but are not
        specified as arguments default to 0 for attributes with the float data
        type, and 255 for the byte data type.

        Parameters: ~
            • {vertexindex} (`number`)
                The index of the the vertex to be modified (one-based).
            • {attributeindex} (`number`)
                The index of the attribute within the vertex to be modified
                (one-based).
            • {value1} (`number`)
                The new value for the first component of the attribute.
            • {value2} (`number`)
                The new value for the second component of the attribute.
            • {...} (`number`)
                Any additional vertex attribute components.


-------------------------------------------------------------------------------
                                                *love2d-docs-Mesh:setVertexMap*
|Mesh:setVertexMap|

Sets the vertex map for the Mesh. The vertex map describes the order in which
the vertices are used when the Mesh is drawn. The vertices, vertex map, and
mesh draw mode work together to determine what exactly is displayed on the
screen.

The vertex map allows you to re-order or reuse vertices when drawing without
changing the actual vertex parameters or duplicating vertices. It is especially
useful when combined with different Mesh Draw Modes.

Synopses: ~

    1.  |Mesh:setVertexMap|(`map`)
    2.  |Mesh:setVertexMap|(`vi1`, `vi2`, `vi3`)
    3.  |Mesh:setVertexMap|(`data`, `datatype`)

Variants: ~

    1.  |Mesh:setVertexMap|(`map`)

    1.          See function description

        Parameters: ~
            • {map} (`table`)
                A table containing a list of vertex indices to use when
                drawing. Values must be in the range of Mesh:getVertexCount().


    2.  |Mesh:setVertexMap|(`vi1`, `vi2`, `vi3`)

    2.          See function description

        Parameters: ~
            • {vi1} (`number`)
                The index of the first vertex to use when drawing. Must be in
                the range of Mesh:getVertexCount().
            • {vi2} (`number`)
                The index of the second vertex to use when drawing.
            • {vi3} (`number`)
                The index of the third vertex to use when drawing.


    3.  |Mesh:setVertexMap|(`data`, `datatype`)

    3.          See function description

        Parameters: ~
            • {data} (`Data`) |love2d-docs-Data|
                Array of vertex indices to use when drawing. Values must be in
                the range of Mesh:getVertexCount()-1
            • {datatype} (`IndexDataType`) |love2d-docs-IndexDataType|
                Datatype of the vertex indices array above.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Mesh:setVertices*
|Mesh:setVertices|

Replaces a range of vertices in the Mesh with new ones. The total number of
vertices in a Mesh cannot be changed after it has been created. This is often
more efficient than calling Mesh:setVertex in a loop.

Synopses: ~

    1.  |Mesh:setVertices|(`vertices`, `startvertex`, `count`)
    2.  |Mesh:setVertices|(`data`, `startvertex`)
    3.  |Mesh:setVertices|(`vertices`)

Variants: ~

    1.  |Mesh:setVertices|(`vertices`, `startvertex`, `count`)

    1.          The values in each vertex table are in the same order as the vertex
        attributes in the Mesh's vertex format. A standard Mesh that wasn't
        created with a custom vertex format will use two position numbers, two
        texture coordinate numbers, and four color components per vertex: x, y,
        u, v, r, g, b, a.

        If no value is supplied for a specific vertex attribute component, it
        will be set to a default value of 0 if its data type is 'float', or 255
        if its data type is 'byte'.

        Parameters: ~
            • {vertices} (`table`)
                The table filled with vertex information tables for each
                vertex, in the form of {vertex, ...} where each vertex is a
                table in the form of {attributecomponent, ...}.
                • {attributecomponent} (`number`)
                    The first component of the first vertex attribute in the
                    vertex.
                • {...} (`number`)
                    Additional components of all vertex attributes in the
                    vertex.
            • {startvertex} (`number`) (default: `1`)
                The index of the first vertex to replace.
            • {count} (`number`) (default: `all`)
                Amount of vertices to replace.


    2.  |Mesh:setVertices|(`data`, `startvertex`)

    2.          Sets the vertex components of the Mesh by copying directly from the
        memory of a Data object.

        If LuaJIT's FFI is used to populate the Data object via Data:getPointer
        and ffi.cast, this variant can be drastically more efficient than other
        methods of setting Mesh vertex data.

        Parameters: ~
            • {data} (`Data`) |love2d-docs-Data|
                A Data object to copy from. The contents of the Data must match
                the layout of this Mesh's vertex format.
            • {startvertex} (`number`) (default: `1`)
                The index of the first vertex to replace.


    3.  |Mesh:setVertices|(`vertices`)

    3.          Sets the vertex components of a Mesh that wasn't created with a custom
        vertex format.

        Parameters: ~
            • {vertices} (`table`)
                The table filled with vertex information tables for each vertex
                as follows:
                • {1} (`number`)
                    The position of the vertex on the x-axis.
                • {2} (`number`)
                    The position of the vertex on the y-axis.
                • {3} (`number`)
                    The horizontal component of the texture coordinate. Texture
                    coordinates are normally in the range of 1, but can be
                    greater or less (see WrapMode).
                • {4} (`number`)
                    The vertical component of the texture coordinate. Texture
                    coordinates are normally in the range of 1, but can be
                    greater or less (see WrapMode).
                • {5} (`number`) (default: `1`)
                    The red color component.
                • {6} (`number`) (default: `1`)
                    The green color component.
                • {7} (`number`) (default: `1`)
                    The blue color component.
                • {8} (`number`) (default: `1`)
                    The alpha color component.



-------------------------------------------------------------------------------
                                                   *love2d-docs-ParticleSystem*
|ParticleSystem|

A ParticleSystem can be used to create particle effects like fire or smoke.

The particle system has to be created using update it in the update callback to
see any changes in the particles emitted.

The particle system won't create any particles unless you call
setParticleLifetime and setEmissionRate.

                                      *love2d-docs-ParticleSystem-constructors*
    constructors:

        |newParticleSystem|                     |love2d-docs-newParticleSystem|

                                        *love2d-docs-ParticleSystem-supertypes*
    supertypes:

        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                          *love2d-docs-ParticleSystem-subtypes*
    subtypes:

        None

                                         *love2d-docs-ParticleSystem-functions*
    functions:

        |clone|                              |love2d-docs-ParticleSystem:clone|
        |emit|                                |love2d-docs-ParticleSystem:emit|
        |getBufferSize|              |love2d-docs-ParticleSystem:getBufferSize|
        |getColors|                      |love2d-docs-ParticleSystem:getColors|
        |getCount|                        |love2d-docs-ParticleSystem:getCount|
        |getDirection|                |love2d-docs-ParticleSystem:getDirection|
        |getEmissionArea|          |love2d-docs-ParticleSystem:getEmissionArea|
        |getEmissionRate|          |love2d-docs-ParticleSystem:getEmissionRate|
        |getEmitterLifetime|    |love2d-docs-ParticleSystem:getEmitterLifetime|
        |getInsertMode|              |love2d-docs-ParticleSystem:getInsertMode|
        |getLinearAcceleration|  |love2d-docs-ParticleSystem:getLinearAcceleration|
        |getLinearDamping|        |love2d-docs-ParticleSystem:getLinearDamping|
        |getOffset|                      |love2d-docs-ParticleSystem:getOffset|
        |getParticleLifetime|  |love2d-docs-ParticleSystem:getParticleLifetime|
        |getPosition|                  |love2d-docs-ParticleSystem:getPosition|
        |getQuads|                        |love2d-docs-ParticleSystem:getQuads|
        |getRadialAcceleration|  |love2d-docs-ParticleSystem:getRadialAcceleration|
        |getRotation|                  |love2d-docs-ParticleSystem:getRotation|
        |getSizeVariation|        |love2d-docs-ParticleSystem:getSizeVariation|
        |getSizes|                        |love2d-docs-ParticleSystem:getSizes|
        |getSpeed|                        |love2d-docs-ParticleSystem:getSpeed|
        |getSpin|                          |love2d-docs-ParticleSystem:getSpin|
        |getSpinVariation|        |love2d-docs-ParticleSystem:getSpinVariation|
        |getSpread|                      |love2d-docs-ParticleSystem:getSpread|
        |getTangentialAcceleration|  |love2d-docs-ParticleSystem:getTangentialAcceleration|
        |getTexture|                    |love2d-docs-ParticleSystem:getTexture|
        |hasRelativeRotation|  |love2d-docs-ParticleSystem:hasRelativeRotation|
        |isActive|                        |love2d-docs-ParticleSystem:isActive|
        |isPaused|                        |love2d-docs-ParticleSystem:isPaused|
        |isStopped|                      |love2d-docs-ParticleSystem:isStopped|
        |moveTo|                            |love2d-docs-ParticleSystem:moveTo|
        |pause|                              |love2d-docs-ParticleSystem:pause|
        |reset|                              |love2d-docs-ParticleSystem:reset|
        |setBufferSize|              |love2d-docs-ParticleSystem:setBufferSize|
        |setColors|                      |love2d-docs-ParticleSystem:setColors|
        |setDirection|                |love2d-docs-ParticleSystem:setDirection|
        |setEmissionArea|          |love2d-docs-ParticleSystem:setEmissionArea|
        |setEmissionRate|          |love2d-docs-ParticleSystem:setEmissionRate|
        |setEmitterLifetime|    |love2d-docs-ParticleSystem:setEmitterLifetime|
        |setInsertMode|              |love2d-docs-ParticleSystem:setInsertMode|
        |setLinearAcceleration|  |love2d-docs-ParticleSystem:setLinearAcceleration|
        |setLinearDamping|        |love2d-docs-ParticleSystem:setLinearDamping|
        |setOffset|                      |love2d-docs-ParticleSystem:setOffset|
        |setParticleLifetime|  |love2d-docs-ParticleSystem:setParticleLifetime|
        |setPosition|                  |love2d-docs-ParticleSystem:setPosition|
        |setQuads|                        |love2d-docs-ParticleSystem:setQuads|
        |setRadialAcceleration|  |love2d-docs-ParticleSystem:setRadialAcceleration|
        |setRelativeRotation|  |love2d-docs-ParticleSystem:setRelativeRotation|
        |setRotation|                  |love2d-docs-ParticleSystem:setRotation|
        |setSizeVariation|        |love2d-docs-ParticleSystem:setSizeVariation|
        |setSizes|                        |love2d-docs-ParticleSystem:setSizes|
        |setSpeed|                        |love2d-docs-ParticleSystem:setSpeed|
        |setSpin|                          |love2d-docs-ParticleSystem:setSpin|
        |setSpinVariation|        |love2d-docs-ParticleSystem:setSpinVariation|
        |setSpread|                      |love2d-docs-ParticleSystem:setSpread|
        |setTangentialAcceleration|  |love2d-docs-ParticleSystem:setTangentialAcceleration|
        |setTexture|                    |love2d-docs-ParticleSystem:setTexture|
        |start|                              |love2d-docs-ParticleSystem:start|
        |stop|                                |love2d-docs-ParticleSystem:stop|
        |update|                            |love2d-docs-ParticleSystem:update|

-------------------------------------------------------------------------------
                                             *love2d-docs-ParticleSystem:clone*
|ParticleSystem:clone|

Creates an identical copy of the ParticleSystem in the stopped state.

Synopses: ~

        `particlesystem` = |ParticleSystem:clone|()

Variants: ~

        `particlesystem` = |ParticleSystem:clone|()

        Cloned ParticleSystem inherit all the set-able state of the original
        ParticleSystem, but they are initialized stopped.

        Return: ~
            (`ParticleSystem`) |love2d-docs-ParticleSystem|
                The new identical copy of this ParticleSystem.
-------------------------------------------------------------------------------
                                              *love2d-docs-ParticleSystem:emit*
|ParticleSystem:emit|

Emits a burst of particles from the particle emitter.

Synopses: ~

        |ParticleSystem:emit|(`numparticles`)

Variants: ~

        |ParticleSystem:emit|(`numparticles`)

        See function description

        Parameters: ~
            • {numparticles} (`number`)
                The amount of particles to emit. The number of emitted
                particles will be truncated if the particle system's max buffer
                size is reached.


-------------------------------------------------------------------------------
                                     *love2d-docs-ParticleSystem:getBufferSize*
|ParticleSystem:getBufferSize|

Gets the maximum number of particles the ParticleSystem can have at once.

Synopses: ~

        `size` = |ParticleSystem:getBufferSize|()

Variants: ~

        `size` = |ParticleSystem:getBufferSize|()

        See function description

        Return: ~
            (`number`)
                The maximum number of particles.
-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:getColors*
|ParticleSystem:getColors|

Gets the series of colors applied to the particle sprite.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        `r1`, `g1`, `b1`, `a1`, `r2`, `g2`, `b2`, `a2`, `r8`, `g8`,
        `b8`, `a8` = |ParticleSystem:getColors|()

Variants: ~

        `r1`, `g1`, `b1`, `a1`, `r2`, `g2`, `b2`, `a2`, `r8`, `g8`,
        `b8`, `a8` = |ParticleSystem:getColors|()

        See function description

        Returns: ~
            1. (`number`)
                First color, red component (0-1).

            2. (`number`)
                First color, green component (0-1).

            3. (`number`)
                First color, blue component (0-1).

            4. (`number`)
                First color, alpha component (0-1).

            5. (`number`)
                Second color, red component (0-1).

            6. (`number`)
                Second color, green component (0-1).

            7. (`number`)
                Second color, blue component (0-1).

            8. (`number`)
                Second color, alpha component (0-1).

            9. (`number`)
                Eighth color, red component (0-1).

            10. (`number`)
                Eighth color, green component (0-1).

            11. (`number`)
                Eighth color, blue component (0-1).

            12. (`number`)
                Eighth color, alpha component (0-1).
-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:getCount*
|ParticleSystem:getCount|

Gets the number of particles that are currently in the system.

Synopses: ~

        `count` = |ParticleSystem:getCount|()

Variants: ~

        `count` = |ParticleSystem:getCount|()

        See function description

        Return: ~
            (`number`)
                The current number of live particles.
-------------------------------------------------------------------------------
                                      *love2d-docs-ParticleSystem:getDirection*
|ParticleSystem:getDirection|

Gets the direction of the particle emitter (in radians).

Synopses: ~

        `direction` = |ParticleSystem:getDirection|()

Variants: ~

        `direction` = |ParticleSystem:getDirection|()

        See function description

        Return: ~
            (`number`)
                The direction of the emitter (radians).
-------------------------------------------------------------------------------
                                   *love2d-docs-ParticleSystem:getEmissionArea*
|ParticleSystem:getEmissionArea|

Gets the area-based spawn parameters for the particles.

Synopses: ~

        `distribution`, `dx`, `dy`, `angle`,
        `directionRelativeToCenter` = |ParticleSystem:getEmissionArea|()

Variants: ~

        `distribution`, `dx`, `dy`, `angle`,
        `directionRelativeToCenter` = |ParticleSystem:getEmissionArea|()

        See function description

        Returns: ~
            1. (`AreaSpreadDistribution`) |love2d-docs-AreaSpreadDistribution|
                The type of distribution for new particles.

            2. (`number`)
                The maximum spawn distance from the emitter along the x-axis
                for uniform distribution, or the standard deviation along the
                x-axis for normal distribution.

            3. (`number`)
                The maximum spawn distance from the emitter along the y-axis
                for uniform distribution, or the standard deviation along the
                y-axis for normal distribution.

            4. (`number`)
                The angle in radians of the emission area.

            5. (`boolean`)
                True if newly spawned particles will be oriented relative to
                the center of the emission area, false otherwise.
-------------------------------------------------------------------------------
                                   *love2d-docs-ParticleSystem:getEmissionRate*
|ParticleSystem:getEmissionRate|

Gets the amount of particles emitted per second.

Synopses: ~

        `rate` = |ParticleSystem:getEmissionRate|()

Variants: ~

        `rate` = |ParticleSystem:getEmissionRate|()

        See function description

        Return: ~
            (`number`)
                The amount of particles per second.
-------------------------------------------------------------------------------
                                *love2d-docs-ParticleSystem:getEmitterLifetime*
|ParticleSystem:getEmitterLifetime|

Gets how long the particle system will emit particles (if -1 then it emits
particles forever).

Synopses: ~

        `life` = |ParticleSystem:getEmitterLifetime|()

Variants: ~

        `life` = |ParticleSystem:getEmitterLifetime|()

        See function description

        Return: ~
            (`number`)
                The lifetime of the emitter (in seconds).
-------------------------------------------------------------------------------
                                     *love2d-docs-ParticleSystem:getInsertMode*
|ParticleSystem:getInsertMode|

Gets the mode used when the ParticleSystem adds new particles.

Synopses: ~

        `mode` = |ParticleSystem:getInsertMode|()

Variants: ~

        `mode` = |ParticleSystem:getInsertMode|()

        See function description

        Return: ~
            (`ParticleInsertMode`) |love2d-docs-ParticleInsertMode|
                The mode used when the ParticleSystem adds new particles.
-------------------------------------------------------------------------------
                             *love2d-docs-ParticleSystem:getLinearAcceleration*
|ParticleSystem:getLinearAcceleration|

Gets the linear acceleration (acceleration along the x and y axes) for
particles.

Every particle created will accelerate along the x and y axes between xmin,ymin
and xmax,ymax.

Synopses: ~

        `xmin`, `ymin`, `xmax`, `ymax` =
        |ParticleSystem:getLinearAcceleration|()

Variants: ~

        `xmin`, `ymin`, `xmax`, `ymax` =
        |ParticleSystem:getLinearAcceleration|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum acceleration along the x axis.

            2. (`number`)
                The minimum acceleration along the y axis.

            3. (`number`)
                The maximum acceleration along the x axis.

            4. (`number`)
                The maximum acceleration along the y axis.
-------------------------------------------------------------------------------
                                  *love2d-docs-ParticleSystem:getLinearDamping*
|ParticleSystem:getLinearDamping|

Gets the amount of linear damping (constant deceleration) for particles.

Synopses: ~

        `min`, `max` = |ParticleSystem:getLinearDamping|()

Variants: ~

        `min`, `max` = |ParticleSystem:getLinearDamping|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum amount of linear damping applied to particles.

            2. (`number`)
                The maximum amount of linear damping applied to particles.
-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:getOffset*
|ParticleSystem:getOffset|

Gets the particle image's draw offset.

Synopses: ~

        `ox`, `oy` = |ParticleSystem:getOffset|()

Variants: ~

        `ox`, `oy` = |ParticleSystem:getOffset|()

        See function description

        Returns: ~
            1. (`number`)
                The x coordinate of the particle image's draw offset.

            2. (`number`)
                The y coordinate of the particle image's draw offset.
-------------------------------------------------------------------------------
                               *love2d-docs-ParticleSystem:getParticleLifetime*
|ParticleSystem:getParticleLifetime|

Gets the lifetime of the particles.

Synopses: ~

        `min`, `max` = |ParticleSystem:getParticleLifetime|()

Variants: ~

        `min`, `max` = |ParticleSystem:getParticleLifetime|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum life of the particles (in seconds).

            2. (`number`)
                The maximum life of the particles (in seconds).
-------------------------------------------------------------------------------
                                       *love2d-docs-ParticleSystem:getPosition*
|ParticleSystem:getPosition|

Gets the position of the emitter.

Synopses: ~

        `x`, `y` = |ParticleSystem:getPosition|()

Variants: ~

        `x`, `y` = |ParticleSystem:getPosition|()

        See function description

        Returns: ~
            1. (`number`)
                Position along x-axis.

            2. (`number`)
                Position along y-axis.
-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:getQuads*
|ParticleSystem:getQuads|

Gets the series of Quads used for the particle sprites.

Synopses: ~

        `quads` = |ParticleSystem:getQuads|()

Variants: ~

        `quads` = |ParticleSystem:getQuads|()

        See function description

        Return: ~
            (`table`)
                A table containing the Quads used.
-------------------------------------------------------------------------------
                             *love2d-docs-ParticleSystem:getRadialAcceleration*
|ParticleSystem:getRadialAcceleration|

Gets the radial acceleration (away from the emitter).

Synopses: ~

        `min`, `max` = |ParticleSystem:getRadialAcceleration|()

Variants: ~

        `min`, `max` = |ParticleSystem:getRadialAcceleration|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum acceleration.

            2. (`number`)
                The maximum acceleration.
-------------------------------------------------------------------------------
                                       *love2d-docs-ParticleSystem:getRotation*
|ParticleSystem:getRotation|

Gets the rotation of the image upon particle creation (in radians).

Synopses: ~

        `min`, `max` = |ParticleSystem:getRotation|()

Variants: ~

        `min`, `max` = |ParticleSystem:getRotation|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum initial angle (radians).

            2. (`number`)
                The maximum initial angle (radians).
-------------------------------------------------------------------------------
                                  *love2d-docs-ParticleSystem:getSizeVariation*
|ParticleSystem:getSizeVariation|

Gets the amount of size variation (0 meaning no variation and 1 meaning full
variation between start and end).

Synopses: ~

        `variation` = |ParticleSystem:getSizeVariation|()

Variants: ~

        `variation` = |ParticleSystem:getSizeVariation|()

        See function description

        Return: ~
            (`number`)
                The amount of variation (0 meaning no variation and 1 meaning
                full variation between start and end).
-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:getSizes*
|ParticleSystem:getSizes|

Gets the series of sizes by which the sprite is scaled. 1.0 is normal size. The
particle system will interpolate between each size evenly over the particle's
lifetime.

Synopses: ~

        `size1`, `size2`, `size8` = |ParticleSystem:getSizes|()

Variants: ~

        `size1`, `size2`, `size8` = |ParticleSystem:getSizes|()

        See function description

        Returns: ~
            1. (`number`)
                The first size.

            2. (`number`)
                The second size.

            3. (`number`)
                The eighth size.
-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:getSpeed*
|ParticleSystem:getSpeed|

Gets the speed of the particles.

Synopses: ~

        `min`, `max` = |ParticleSystem:getSpeed|()

Variants: ~

        `min`, `max` = |ParticleSystem:getSpeed|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum linear speed of the particles.

            2. (`number`)
                The maximum linear speed of the particles.
-------------------------------------------------------------------------------
                                           *love2d-docs-ParticleSystem:getSpin*
|ParticleSystem:getSpin|

Gets the spin of the sprite.

Synopses: ~

        `min`, `max`, `variation` = |ParticleSystem:getSpin|()

Variants: ~

        `min`, `max`, `variation` = |ParticleSystem:getSpin|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum spin (radians per second).

            2. (`number`)
                The maximum spin (radians per second).

            3. (`number`)
                The degree of variation (0 meaning no variation and 1 meaning
                full variation between start and end).
-------------------------------------------------------------------------------
                                  *love2d-docs-ParticleSystem:getSpinVariation*
|ParticleSystem:getSpinVariation|

Gets the amount of spin variation (0 meaning no variation and 1 meaning full
variation between start and end).

Synopses: ~

        `variation` = |ParticleSystem:getSpinVariation|()

Variants: ~

        `variation` = |ParticleSystem:getSpinVariation|()

        See function description

        Return: ~
            (`number`)
                The amount of variation (0 meaning no variation and 1 meaning
                full variation between start and end).
-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:getSpread*
|ParticleSystem:getSpread|

Gets the amount of directional spread of the particle emitter (in radians).

Synopses: ~

        `spread` = |ParticleSystem:getSpread|()

Variants: ~

        `spread` = |ParticleSystem:getSpread|()

        See function description

        Return: ~
            (`number`)
                The spread of the emitter (radians).
-------------------------------------------------------------------------------
                         *love2d-docs-ParticleSystem:getTangentialAcceleration*
|ParticleSystem:getTangentialAcceleration|

Gets the tangential acceleration (acceleration perpendicular to the particle's
direction).

Synopses: ~

        `min`, `max` = |ParticleSystem:getTangentialAcceleration|()

Variants: ~

        `min`, `max` = |ParticleSystem:getTangentialAcceleration|()

        See function description

        Returns: ~
            1. (`number`)
                The minimum acceleration.

            2. (`number`)
                The maximum acceleration.
-------------------------------------------------------------------------------
                                        *love2d-docs-ParticleSystem:getTexture*
|ParticleSystem:getTexture|

Gets the texture (Image or Canvas) used for the particles.

Synopses: ~

        `texture` = |ParticleSystem:getTexture|()

Variants: ~

        `texture` = |ParticleSystem:getTexture|()

        See function description

        Return: ~
            (`Texture`) |love2d-docs-Texture|
                The Image or Canvas used for the particles.
-------------------------------------------------------------------------------
                               *love2d-docs-ParticleSystem:hasRelativeRotation*
|ParticleSystem:hasRelativeRotation|

Gets whether particle angles and rotations are relative to their velocities. If
enabled, particles are aligned to the angle of their velocities and rotate
relative to that angle.

Synopses: ~

        `enable` = |ParticleSystem:hasRelativeRotation|()

Variants: ~

        `enable` = |ParticleSystem:hasRelativeRotation|()

        See function description

        Return: ~
            (`boolean`)
                True if relative particle rotation is enabled, false if it's
                disabled.
-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:isActive*
|ParticleSystem:isActive|

Checks whether the particle system is actively emitting particles.

Synopses: ~

        `active` = |ParticleSystem:isActive|()

Variants: ~

        `active` = |ParticleSystem:isActive|()

        See function description

        Return: ~
            (`boolean`)
                True if system is active, false otherwise.
-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:isPaused*
|ParticleSystem:isPaused|

Checks whether the particle system is paused.

Synopses: ~

        `paused` = |ParticleSystem:isPaused|()

Variants: ~

        `paused` = |ParticleSystem:isPaused|()

        See function description

        Return: ~
            (`boolean`)
                True if system is paused, false otherwise.
-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:isStopped*
|ParticleSystem:isStopped|

Checks whether the particle system is stopped.

Synopses: ~

        `stopped` = |ParticleSystem:isStopped|()

Variants: ~

        `stopped` = |ParticleSystem:isStopped|()

        See function description

        Return: ~
            (`boolean`)
                True if system is stopped, false otherwise.
-------------------------------------------------------------------------------
                                            *love2d-docs-ParticleSystem:moveTo*
|ParticleSystem:moveTo|

Moves the position of the emitter. This results in smoother particle spawning
behaviour than if ParticleSystem:setPosition is used every frame.

Synopses: ~

        |ParticleSystem:moveTo|(`x`, `y`)

Variants: ~

        |ParticleSystem:moveTo|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                Position along x-axis.
            • {y} (`number`)
                Position along y-axis.


-------------------------------------------------------------------------------
                                             *love2d-docs-ParticleSystem:pause*
|ParticleSystem:pause|

Pauses the particle emitter.

Synopses: ~

        |ParticleSystem:pause|()

Variants: ~

        |ParticleSystem:pause|()

        See function description


-------------------------------------------------------------------------------
                                             *love2d-docs-ParticleSystem:reset*
|ParticleSystem:reset|

Resets the particle emitter, removing any existing particles and resetting the
lifetime counter.

Synopses: ~

        |ParticleSystem:reset|()

Variants: ~

        |ParticleSystem:reset|()

        See function description


-------------------------------------------------------------------------------
                                     *love2d-docs-ParticleSystem:setBufferSize*
|ParticleSystem:setBufferSize|

Sets the size of the buffer (the max allowed amount of particles in the
system).

Synopses: ~

        |ParticleSystem:setBufferSize|(`size`)

Variants: ~

        |ParticleSystem:setBufferSize|(`size`)

        See function description

        Parameters: ~
            • {size} (`number`)
                The buffer size.


-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:setColors*
|ParticleSystem:setColors|

Sets a series of colors to apply to the particle sprite. The particle system
will interpolate between each color evenly over the particle's lifetime.

Arguments can be passed in groups of four, representing the components of the
desired RGBA value, or as tables of RGBA component values, with a default alpha
value of 1 if only three values are given. At least one color must be
specified. A maximum of eight may be used.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

    1.  |ParticleSystem:setColors|(`r1`, `g1`, `b1`, `a1`, `...`)
    2.  |ParticleSystem:setColors|(`rgba1`, `...`)

Variants: ~

    1.  |ParticleSystem:setColors|(`r1`, `g1`, `b1`, `a1`, `...`)

    1.          See function description

        Parameters: ~
            • {r1} (`number`)
                First color, red component (0-1).
            • {g1} (`number`)
                First color, green component (0-1).
            • {b1} (`number`)
                First color, blue component (0-1).
            • {a1} (`number`) (default: `1`)
                First color, alpha component (0-1).
            • {...} (`number`)
                Additional colors.


    2.  |ParticleSystem:setColors|(`rgba1`, `...`)

    2.          See function description

        Parameters: ~
            • {rgba1} (`table`)
                First color, a numerical indexed table with the red, green,
                blue and alpha values as numbers (0-1). The alpha is optional
                and defaults to 1 if it is left out.
            • {...} (`table`)
                Additional color, a numerical indexed table with the red,
                green, blue and alpha values as numbers (0-1). The alpha is
                optional and defaults to 1 if it is left out.


-------------------------------------------------------------------------------
                                      *love2d-docs-ParticleSystem:setDirection*
|ParticleSystem:setDirection|

Sets the direction the particles will be emitted in.

Synopses: ~

        |ParticleSystem:setDirection|(`direction`)

Variants: ~

        |ParticleSystem:setDirection|(`direction`)

        See function description

        Parameters: ~
            • {direction} (`number`)
                The direction of the particles (in radians).


-------------------------------------------------------------------------------
                                   *love2d-docs-ParticleSystem:setEmissionArea*
|ParticleSystem:setEmissionArea|

Sets area-based spawn parameters for the particles. Newly created particles
will spawn in an area around the emitter based on the parameters to this
function.

Synopses: ~

        |ParticleSystem:setEmissionArea|(`distribution`, `dx`, `dy`,
        `angle`, `directionRelativeToCenter`)

Variants: ~

        |ParticleSystem:setEmissionArea|(`distribution`, `dx`, `dy`,
        `angle`, `directionRelativeToCenter`)

        See function description

        Parameters: ~
            • {distribution} (`AreaSpreadDistribution`) |love2d-docs-AreaSpreadDistribution|
                The type of distribution for new particles.
            • {dx} (`number`)
                The maximum spawn distance from the emitter along the x-axis
                for uniform distribution, or the standard deviation along the
                x-axis for normal distribution.
            • {dy} (`number`)
                The maximum spawn distance from the emitter along the y-axis
                for uniform distribution, or the standard deviation along the
                y-axis for normal distribution.
            • {angle} (`number`) (default: `0`)
                The angle in radians of the emission area.
            • {directionRelativeToCenter} (`boolean`) (default: `false`)
                True if newly spawned particles will be oriented relative to
                the center of the emission area, false otherwise.


-------------------------------------------------------------------------------
                                   *love2d-docs-ParticleSystem:setEmissionRate*
|ParticleSystem:setEmissionRate|

Sets the amount of particles emitted per second.

Synopses: ~

        |ParticleSystem:setEmissionRate|(`rate`)

Variants: ~

        |ParticleSystem:setEmissionRate|(`rate`)

        See function description

        Parameters: ~
            • {rate} (`number`)
                The amount of particles per second.


-------------------------------------------------------------------------------
                                *love2d-docs-ParticleSystem:setEmitterLifetime*
|ParticleSystem:setEmitterLifetime|

Sets how long the particle system should emit particles (if -1 then it emits
particles forever).

Synopses: ~

        |ParticleSystem:setEmitterLifetime|(`life`)

Variants: ~

        |ParticleSystem:setEmitterLifetime|(`life`)

        See function description

        Parameters: ~
            • {life} (`number`)
                The lifetime of the emitter (in seconds).


-------------------------------------------------------------------------------
                                     *love2d-docs-ParticleSystem:setInsertMode*
|ParticleSystem:setInsertMode|

Sets the mode to use when the ParticleSystem adds new particles.

Synopses: ~

        |ParticleSystem:setInsertMode|(`mode`)

Variants: ~

        |ParticleSystem:setInsertMode|(`mode`)

        See function description

        Parameters: ~
            • {mode} (`ParticleInsertMode`) |love2d-docs-ParticleInsertMode|
                The mode to use when the ParticleSystem adds new particles.


-------------------------------------------------------------------------------
                             *love2d-docs-ParticleSystem:setLinearAcceleration*
|ParticleSystem:setLinearAcceleration|

Sets the linear acceleration (acceleration along the x and y axes) for
particles.

Every particle created will accelerate along the x and y axes between xmin,ymin
and xmax,ymax.

Synopses: ~

        |ParticleSystem:setLinearAcceleration|(`xmin`, `ymin`, `xmax`,
        `ymax`)

Variants: ~

        |ParticleSystem:setLinearAcceleration|(`xmin`, `ymin`, `xmax`,
        `ymax`)

        See function description

        Parameters: ~
            • {xmin} (`number`)
                The minimum acceleration along the x axis.
            • {ymin} (`number`)
                The minimum acceleration along the y axis.
            • {xmax} (`number`) (default: `xmin`)
                The maximum acceleration along the x axis.
            • {ymax} (`number`) (default: `ymin`)
                The maximum acceleration along the y axis.


-------------------------------------------------------------------------------
                                  *love2d-docs-ParticleSystem:setLinearDamping*
|ParticleSystem:setLinearDamping|

Sets the amount of linear damping (constant deceleration) for particles.

Synopses: ~

        |ParticleSystem:setLinearDamping|(`min`, `max`)

Variants: ~

        |ParticleSystem:setLinearDamping|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum amount of linear damping applied to particles.
            • {max} (`number`) (default: `min`)
                The maximum amount of linear damping applied to particles.


-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:setOffset*
|ParticleSystem:setOffset|

Set the offset position which the particle sprite is rotated around.

If this function is not used, the particles rotate around their center.

Synopses: ~

        |ParticleSystem:setOffset|(`x`, `y`)

Variants: ~

        |ParticleSystem:setOffset|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x coordinate of the rotation offset.
            • {y} (`number`)
                The y coordinate of the rotation offset.


-------------------------------------------------------------------------------
                               *love2d-docs-ParticleSystem:setParticleLifetime*
|ParticleSystem:setParticleLifetime|

Sets the lifetime of the particles.

Synopses: ~

        |ParticleSystem:setParticleLifetime|(`min`, `max`)

Variants: ~

        |ParticleSystem:setParticleLifetime|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum life of the particles (in seconds).
            • {max} (`number`) (default: `min`)
                The maximum life of the particles (in seconds).


-------------------------------------------------------------------------------
                                       *love2d-docs-ParticleSystem:setPosition*
|ParticleSystem:setPosition|

Sets the position of the emitter.

Synopses: ~

        |ParticleSystem:setPosition|(`x`, `y`)

Variants: ~

        |ParticleSystem:setPosition|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                Position along x-axis.
            • {y} (`number`)
                Position along y-axis.


-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:setQuads*
|ParticleSystem:setQuads|

Sets a series of Quads to use for the particle sprites. Particles will choose a
Quad from the list based on the particle's current lifetime, allowing for the
use of animated sprite sheets with ParticleSystems.

Synopses: ~

    1.  |ParticleSystem:setQuads|(`quad1`, `...`)
    2.  |ParticleSystem:setQuads|(`quads`)

Variants: ~

    1.  |ParticleSystem:setQuads|(`quad1`, `...`)

    1.          See function description

        Parameters: ~
            • {quad1} (`Quad`) |love2d-docs-Quad|
                The first Quad to use.
            • {...} (`Quad`) |love2d-docs-Quad|
                Additional Quads to use.


    2.  |ParticleSystem:setQuads|(`quads`)

    2.          See function description

        Parameters: ~
            • {quads} (`table`)
                A table containing the Quads to use.


-------------------------------------------------------------------------------
                             *love2d-docs-ParticleSystem:setRadialAcceleration*
|ParticleSystem:setRadialAcceleration|

Set the radial acceleration (away from the emitter).

Synopses: ~

        |ParticleSystem:setRadialAcceleration|(`min`, `max`)

Variants: ~

        |ParticleSystem:setRadialAcceleration|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum acceleration.
            • {max} (`number`) (default: `min`)
                The maximum acceleration.


-------------------------------------------------------------------------------
                               *love2d-docs-ParticleSystem:setRelativeRotation*
|ParticleSystem:setRelativeRotation|

Sets whether particle angles and rotations are relative to their velocities. If
enabled, particles are aligned to the angle of their velocities and rotate
relative to that angle.

Synopses: ~

        |ParticleSystem:setRelativeRotation|(`enable`)

Variants: ~

        |ParticleSystem:setRelativeRotation|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable relative particle rotation, false to disable it.


-------------------------------------------------------------------------------
                                       *love2d-docs-ParticleSystem:setRotation*
|ParticleSystem:setRotation|

Sets the rotation of the image upon particle creation (in radians).

Synopses: ~

        |ParticleSystem:setRotation|(`min`, `max`)

Variants: ~

        |ParticleSystem:setRotation|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum initial angle (radians).
            • {max} (`number`) (default: `min`)
                The maximum initial angle (radians).


-------------------------------------------------------------------------------
                                  *love2d-docs-ParticleSystem:setSizeVariation*
|ParticleSystem:setSizeVariation|

Sets the amount of size variation (0 meaning no variation and 1 meaning full
variation between start and end).

Synopses: ~

        |ParticleSystem:setSizeVariation|(`variation`)

Variants: ~

        |ParticleSystem:setSizeVariation|(`variation`)

        See function description

        Parameters: ~
            • {variation} (`number`)
                The amount of variation (0 meaning no variation and 1 meaning
                full variation between start and end).


-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:setSizes*
|ParticleSystem:setSizes|

Sets a series of sizes by which to scale a particle sprite. 1.0 is normal size.
The particle system will interpolate between each size evenly over the
particle's lifetime.

At least one size must be specified. A maximum of eight may be used.

Synopses: ~

        |ParticleSystem:setSizes|(`size1`, `size2`, `size8`)

Variants: ~

        |ParticleSystem:setSizes|(`size1`, `size2`, `size8`)

        See function description

        Parameters: ~
            • {size1} (`number`)
                The first size.
            • {size2} (`number`) (default: `nil`)
                The second size.
            • {size8} (`number`) (default: `nil`)
                The eighth size.


-------------------------------------------------------------------------------
                                          *love2d-docs-ParticleSystem:setSpeed*
|ParticleSystem:setSpeed|

Sets the speed of the particles.

Synopses: ~

        |ParticleSystem:setSpeed|(`min`, `max`)

Variants: ~

        |ParticleSystem:setSpeed|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum linear speed of the particles.
            • {max} (`number`) (default: `min`)
                The maximum linear speed of the particles.


-------------------------------------------------------------------------------
                                           *love2d-docs-ParticleSystem:setSpin*
|ParticleSystem:setSpin|

Sets the spin of the sprite.

Synopses: ~

        |ParticleSystem:setSpin|(`min`, `max`)

Variants: ~

        |ParticleSystem:setSpin|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum spin (radians per second).
            • {max} (`number`) (default: `min`)
                The maximum spin (radians per second).


-------------------------------------------------------------------------------
                                  *love2d-docs-ParticleSystem:setSpinVariation*
|ParticleSystem:setSpinVariation|

Sets the amount of spin variation (0 meaning no variation and 1 meaning full
variation between start and end).

Synopses: ~

        |ParticleSystem:setSpinVariation|(`variation`)

Variants: ~

        |ParticleSystem:setSpinVariation|(`variation`)

        See function description

        Parameters: ~
            • {variation} (`number`)
                The amount of variation (0 meaning no variation and 1 meaning
                full variation between start and end).


-------------------------------------------------------------------------------
                                         *love2d-docs-ParticleSystem:setSpread*
|ParticleSystem:setSpread|

Sets the amount of spread for the system.

Synopses: ~

        |ParticleSystem:setSpread|(`spread`)

Variants: ~

        |ParticleSystem:setSpread|(`spread`)

        See function description

        Parameters: ~
            • {spread} (`number`)
                The amount of spread (radians).


-------------------------------------------------------------------------------
                         *love2d-docs-ParticleSystem:setTangentialAcceleration*
|ParticleSystem:setTangentialAcceleration|

Sets the tangential acceleration (acceleration perpendicular to the particle's
direction).

Synopses: ~

        |ParticleSystem:setTangentialAcceleration|(`min`, `max`)

Variants: ~

        |ParticleSystem:setTangentialAcceleration|(`min`, `max`)

        See function description

        Parameters: ~
            • {min} (`number`)
                The minimum acceleration.
            • {max} (`number`) (default: `min`)
                The maximum acceleration.


-------------------------------------------------------------------------------
                                        *love2d-docs-ParticleSystem:setTexture*
|ParticleSystem:setTexture|

Sets the texture (Image or Canvas) to be used for the particles.

Synopses: ~

        |ParticleSystem:setTexture|(`texture`)

Variants: ~

        |ParticleSystem:setTexture|(`texture`)

        See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                An Image or Canvas to use for the particles.


-------------------------------------------------------------------------------
                                             *love2d-docs-ParticleSystem:start*
|ParticleSystem:start|

Starts the particle emitter.

Synopses: ~

        |ParticleSystem:start|()

Variants: ~

        |ParticleSystem:start|()

        See function description


-------------------------------------------------------------------------------
                                              *love2d-docs-ParticleSystem:stop*
|ParticleSystem:stop|

Stops the particle emitter, resetting the lifetime counter.

Synopses: ~

        |ParticleSystem:stop|()

Variants: ~

        |ParticleSystem:stop|()

        See function description


-------------------------------------------------------------------------------
                                            *love2d-docs-ParticleSystem:update*
|ParticleSystem:update|

Updates the particle system; moving, creating and killing particles.

Synopses: ~

        |ParticleSystem:update|(`dt`)

Variants: ~

        |ParticleSystem:update|(`dt`)

        See function description

        Parameters: ~
            • {dt} (`number`)
                The time (seconds) since last frame.



-------------------------------------------------------------------------------
                                                             *love2d-docs-Quad*
|Quad|

A quadrilateral (a polygon with four sides and four corners) with texture
coordinate information.

Quads can be used to select part of a texture to draw. In this way, one large
texture atlas can be loaded, and then split up into sub-images.

                                                *love2d-docs-Quad-constructors*
    constructors:

        |newQuad|                                         |love2d-docs-newQuad|

                                                  *love2d-docs-Quad-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-Quad-subtypes*
    subtypes:

        None

                                                   *love2d-docs-Quad-functions*
    functions:

        |getTextureDimensions|          |love2d-docs-Quad:getTextureDimensions|
        |getViewport|                            |love2d-docs-Quad:getViewport|
        |setViewport|                            |love2d-docs-Quad:setViewport|

-------------------------------------------------------------------------------
                                        *love2d-docs-Quad:getTextureDimensions*
|Quad:getTextureDimensions|

Gets reference texture dimensions initially specified in love.graphics.newQuad.

Synopses: ~

        `sw`, `sh` = |Quad:getTextureDimensions|()

Variants: ~

        `sw`, `sh` = |Quad:getTextureDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The Texture width used by the Quad.

            2. (`number`)
                The Texture height used by the Quad.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Quad:getViewport*
|Quad:getViewport|

Gets the current viewport of this Quad.

Synopses: ~

        `x`, `y`, `w`, `h` = |Quad:getViewport|()

Variants: ~

        `x`, `y`, `w`, `h` = |Quad:getViewport|()

        See function description

        Returns: ~
            1. (`number`)
                The top-left corner along the x-axis.

            2. (`number`)
                The top-left corner along the y-axis.

            3. (`number`)
                The width of the viewport.

            4. (`number`)
                The height of the viewport.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Quad:setViewport*
|Quad:setViewport|

Sets the texture coordinates according to a viewport.

Synopses: ~

        |Quad:setViewport|(`x`, `y`, `w`, `h`, `sw`, `sh`)

Variants: ~

        |Quad:setViewport|(`x`, `y`, `w`, `h`, `sw`, `sh`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The top-left corner along the x-axis.
            • {y} (`number`)
                The top-left corner along the y-axis.
            • {w} (`number`)
                The width of the viewport.
            • {h} (`number`)
                The height of the viewport.
            • {sw} (`number`) (default: `nil`)
                Optional new reference width, the width of the Texture. Must be
                greater than 0 if set.
            • {sh} (`number`) (default: `nil`)
                Optional new reference height, the height of the Texture. Must
                be greater than 0 if set.



-------------------------------------------------------------------------------
                                                           *love2d-docs-Shader*
|Shader|

A Shader is used for advanced hardware-accelerated pixel or vertex
manipulation. These effects are written in a language based on GLSL (OpenGL
Shading Language) with a few things simplified for easier coding.

Potential uses for shaders include HDR/bloom, motion blur,
grayscale/invert/sepia/any kind of color effect, reflection/refraction,
distortions, bump mapping, and much more! Here is a collection of basic shaders
and good starting point to learn: https://github.com/vrld/moonshine

                                              *love2d-docs-Shader-constructors*
    constructors:

        |getShader|                                     |love2d-docs-getShader|
        |newShader|                                     |love2d-docs-newShader|

                                                *love2d-docs-Shader-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                  *love2d-docs-Shader-subtypes*
    subtypes:

        None

                                                 *love2d-docs-Shader-functions*
    functions:

        |getWarnings|                          |love2d-docs-Shader:getWarnings|
        |hasUniform|                            |love2d-docs-Shader:hasUniform|
        |send|                                        |love2d-docs-Shader:send|
        |sendColor|                              |love2d-docs-Shader:sendColor|

-------------------------------------------------------------------------------
                                               *love2d-docs-Shader:getWarnings*
|Shader:getWarnings|

Returns any warning and error messages from compiling the shader code. This can
be used for debugging your shaders if there's anything the graphics hardware
doesn't like.

Synopses: ~

        `warnings` = |Shader:getWarnings|()

Variants: ~

        `warnings` = |Shader:getWarnings|()

        See function description

        Return: ~
            (`string`)
                Warning and error messages (if any).
-------------------------------------------------------------------------------
                                                *love2d-docs-Shader:hasUniform*
|Shader:hasUniform|

Gets whether a uniform / extern variable exists in the Shader.

If a graphics driver's shader compiler determines that a uniform / extern
variable doesn't affect the final output of the shader, it may optimize the
variable out. This function will return false in that case.

Synopses: ~

        `hasuniform` = |Shader:hasUniform|(`name`)

Variants: ~

        `hasuniform` = |Shader:hasUniform|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the uniform variable.

        Return: ~
            (`boolean`)
                Whether the uniform exists in the shader and affects its final
                output.
-------------------------------------------------------------------------------
                                                      *love2d-docs-Shader:send*
|Shader:send|

Sends one or more values to a special (''uniform'') variable inside the shader.
Uniform variables have to be marked using the ''uniform'' or ''extern''
keyword, e.g.

uniform float time;  // 'float' is the typical number type used in GLSL
shaders.

uniform float varsvec2 light_pos;

uniform vec4 colors[4;

The corresponding send calls would be

shader:send('time', t)

shader:send('vars',a,b)

shader:send('light_pos', {light_x, light_y})

shader:send('colors', {r1, g1, b1, a1},  {r2, g2, b2, a2},  {r3, g3, b3, a3},
{r4, g4, b4, a4})

Uniform / extern variables are read-only in the shader code and remain constant
until modified by a Shader:send call. Uniform variables can be accessed in both
the Vertex and Pixel components of a shader, as long as the variable is
declared in each.

Synopses: ~

    1.  |Shader:send|(`name`, `number`, `...`)
    2.  |Shader:send|(`name`, `vector`, `...`)
    3.  |Shader:send|(`name`, `matrix`, `...`)
    4.  |Shader:send|(`name`, `texture`)
    5.  |Shader:send|(`name`, `boolean`, `...`)
    6.  |Shader:send|(`name`, `matrixlayout`, `matrix`, `...`)
    7.  |Shader:send|(`name`, `data`, `offset`, `size`)
    8.  |Shader:send|(`name`, `data`, `matrixlayout`, `offset`, `size`)
    9.  |Shader:send|(`name`, `matrixlayout`, `data`, `offset`, `size`)

Variants: ~

    1.  |Shader:send|(`name`, `number`, `...`)

    1.          Because all numbers in Lua are floating point, in versions prior to
        0.10.2 you must use the function Shader:sendInt to send values to
        uniform int variables in the shader's code.

        Parameters: ~
            • {name} (`string`)
                Name of the number to send to the shader.
            • {number} (`number`)
                Number to send to store in the uniform variable.
            • {...} (`number`)
                Additional numbers to send if the uniform variable is an array.


    2.  |Shader:send|(`name`, `vector`, `...`)

    2.          See function description

        Parameters: ~
            • {name} (`string`)
                Name of the vector to send to the shader.
            • {vector} (`table`)
                Numbers to send to the uniform variable as a vector. The number
                of elements in the table determines the type of the vector
                (e.g. two numbers -&gt; vec2). At least two and at most four
                numbers can be used.
            • {...} (`table`)
                Additional vectors to send if the uniform variable is an array.
                All vectors need to be of the same size (e.g. only vec3's).


    3.  |Shader:send|(`name`, `matrix`, `...`)

    3.          See function description

        Parameters: ~
            • {name} (`string`)
                Name of the matrix to send to the shader.
            • {matrix} (`table`)
                2x2, 3x3, or 4x4 matrix to send to the uniform variable. Using
                table form: {{a,b,c,d}, {e,f,g,h}, ... } or (since version
                0.10.2) {a,b,c,d, e,f,g,h, ...}. The order in 0.10.2 is
                column-major; starting in 11.0 it's row-major instead.
            • {...} (`table`)
                Additional matrices of the same type as ''matrix'' to store in
                a uniform array.


    4.  |Shader:send|(`name`, `texture`)

    4.          See function description

        Parameters: ~
            • {name} (`string`)
                Name of the Texture to send to the shader.
            • {texture} (`Texture`) |love2d-docs-Texture|
                Texture (Image or Canvas) to send to the uniform variable.


    5.  |Shader:send|(`name`, `boolean`, `...`)

    5.          See function description

        Parameters: ~
            • {name} (`string`)
                Name of the boolean to send to the shader.
            • {boolean} (`boolean`)
                Boolean to send to store in the uniform variable.
            • {...} (`boolean`)
                Additional booleans to send if the uniform variable is an
                array.


    6.  |Shader:send|(`name`, `matrixlayout`, `matrix`, `...`)

    6.          See function description

        Parameters: ~
            • {name} (`string`)
                Name of the matrix to send to the shader.
            • {matrixlayout} (`MatrixLayout`) |love2d-docs-MatrixLayout|
                The layout (row- or column-major) of the matrix.
            • {matrix} (`table`)
                2x2, 3x3, or 4x4 matrix to send to the uniform variable. Using
                table form: {{a,b,c,d}, {e,f,g,h}, ... } or {a,b,c,d, e,f,g,h,
                ...}.
            • {...} (`table`)
                Additional matrices of the same type as ''matrix'' to store in
                a uniform array.


    7.  |Shader:send|(`name`, `data`, `offset`, `size`)

    7.          Sends uniform values to the Shader sourced from the contents of a Data
        object. This directly copies the bytes of the data.

        Parameters: ~
            • {name} (`string`)
                Name of the uniform to send to the shader.
            • {data} (`Data`) |love2d-docs-Data|
                Data object containing the values to send.
            • {offset} (`number`) (default: `0`)
                Offset in bytes from the start of the Data object.
            • {size} (`number`) (default: `all`)
                Size in bytes of the data to send. If nil, as many bytes as the
                specified uniform uses will be copied.


    8.  |Shader:send|(`name`, `data`, `matrixlayout`, `offset`, `size`)

    8.          Sends uniform matrices to the Shader sourced from the contents of a
        Data object. This directly copies the bytes of the data.

        Parameters: ~
            • {name} (`string`)
                Name of the uniform matrix to send to the shader.
            • {data} (`Data`) |love2d-docs-Data|
                Data object containing the values to send.
            • {matrixlayout} (`MatrixLayout`) |love2d-docs-MatrixLayout|
                The layout (row- or column-major) of the matrix in memory.
            • {offset} (`number`) (default: `0`)
                Offset in bytes from the start of the Data object.
            • {size} (`number`) (default: `all`)
                Size in bytes of the data to send. If nil, as many bytes as the
                specified uniform uses will be copied.


    9.  |Shader:send|(`name`, `matrixlayout`, `data`, `offset`, `size`)

    9.          Sends uniform matrices to the Shader sourced from the contents of a
        Data object. This directly copies the bytes of the data.

        Parameters: ~
            • {name} (`string`)
                Name of the uniform matrix to send to the shader.
            • {matrixlayout} (`MatrixLayout`) |love2d-docs-MatrixLayout|
                The layout (row- or column-major) of the matrix in memory.
            • {data} (`Data`) |love2d-docs-Data|
                Data object containing the values to send.
            • {offset} (`number`) (default: `0`)
                Offset in bytes from the start of the Data object.
            • {size} (`number`) (default: `all`)
                Size in bytes of the data to send. If nil, as many bytes as the
                specified uniform uses will be copied.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Shader:sendColor*
|Shader:sendColor|

Sends one or more colors to a special (''extern'' / ''uniform'') vec3 or vec4
variable inside the shader. The color components must be in the range of 1. The
colors are gamma-corrected if global gamma-correction is enabled.

Extern variables must be marked using the ''extern'' keyword, e.g.

extern vec4 Color;

The corresponding sendColor call would be

shader:sendColor('Color', {r, g, b, a})

Extern variables can be accessed in both the Vertex and Pixel stages of a
shader, as long as the variable is declared in each.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        |Shader:sendColor|(`name`, `color`, `...`)

Variants: ~

        |Shader:sendColor|(`name`, `color`, `...`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the color extern variable to send to in the shader.
            • {color} (`table`)
                A table with red, green, blue, and optional alpha color
                components in the range of 1 to send to the extern as a vector.
            • {...} (`table`)
                Additional colors to send in case the extern is an array. All
                colors need to be of the same size (e.g. only vec3's).



-------------------------------------------------------------------------------
                                                      *love2d-docs-SpriteBatch*
|SpriteBatch|

Using a single image, draw any number of identical copies of the image using a
single call to love.graphics.draw(). This can be used, for example, to draw
repeating copies of a single background image with high performance.

A SpriteBatch can be even more useful when the underlying image is a texture
atlas (a single image file containing many independent images); by adding Quads
to the batch, different sub-images from within the atlas can be drawn.

                                         *love2d-docs-SpriteBatch-constructors*
    constructors:

        |newSpriteBatch|                           |love2d-docs-newSpriteBatch|

                                           *love2d-docs-SpriteBatch-supertypes*
    supertypes:

        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                             *love2d-docs-SpriteBatch-subtypes*
    subtypes:

        None

                                            *love2d-docs-SpriteBatch-functions*
    functions:

        |add|                                     |love2d-docs-SpriteBatch:add|
        |addLayer|                           |love2d-docs-SpriteBatch:addLayer|
        |attachAttribute|             |love2d-docs-SpriteBatch:attachAttribute|
        |clear|                                 |love2d-docs-SpriteBatch:clear|
        |flush|                                 |love2d-docs-SpriteBatch:flush|
        |getBufferSize|                 |love2d-docs-SpriteBatch:getBufferSize|
        |getColor|                           |love2d-docs-SpriteBatch:getColor|
        |getCount|                           |love2d-docs-SpriteBatch:getCount|
        |getTexture|                       |love2d-docs-SpriteBatch:getTexture|
        |set|                                     |love2d-docs-SpriteBatch:set|
        |setColor|                           |love2d-docs-SpriteBatch:setColor|
        |setDrawRange|                   |love2d-docs-SpriteBatch:setDrawRange|
        |setLayer|                           |love2d-docs-SpriteBatch:setLayer|
        |setTexture|                       |love2d-docs-SpriteBatch:setTexture|

-------------------------------------------------------------------------------
                                                  *love2d-docs-SpriteBatch:add*
|SpriteBatch:add|

Adds a sprite to the batch. Sprites are drawn in the order they are added.

Synopses: ~

    1.  `id` = |SpriteBatch:add|(`x`, `y`, `r`, `sx`, `sy`, `ox`, `oy`,
        `kx`, `ky`)
    2.  `id` = |SpriteBatch:add|(`quad`, `x`, `y`, `r`, `sx`, `sy`,
        `ox`, `oy`, `kx`, `ky`)

Variants: ~

    1.  `id` = |SpriteBatch:add|(`x`, `y`, `r`, `sx`, `sy`, `ox`, `oy`,
        `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {x} (`number`)
                The position to draw the object (x-axis).
            • {y} (`number`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shear factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shear factor (y-axis).

        Return: ~
            (`number`)
                An identifier for the added sprite.
    2.  `id` = |SpriteBatch:add|(`quad`, `x`, `y`, `r`, `sx`, `sy`,
        `ox`, `oy`, `kx`, `ky`)

    2.          Adds a Quad to the batch.

        Parameters: ~
            • {quad} (`Quad`) |love2d-docs-Quad|
                The Quad to add.
            • {x} (`number`)
                The position to draw the object (x-axis).
            • {y} (`number`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shear factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shear factor (y-axis).

        Return: ~
            (`number`)
                An identifier for the added sprite.
-------------------------------------------------------------------------------
                                             *love2d-docs-SpriteBatch:addLayer*
|SpriteBatch:addLayer|

Adds a sprite to a batch created with an Array Texture.

Synopses: ~

    1.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`, `x`, `y`,
        `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`, `quad`,
        `x`, `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    3.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`,
        `transform`)
    4.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`, `quad`,
        `transform`)

Variants: ~

    1.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`, `x`, `y`,
        `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          Adds a layer of the SpriteBatch's Array Texture.

        Parameters: ~
            • {layerindex} (`number`)
                The index of the layer to use for this sprite.
            • {x} (`number`) (default: `0`)
                The position to draw the sprite (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the sprite (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).

        Return: ~
            (`number`)
                The index of the added sprite, for use with SpriteBatch:set or
                SpriteBatch:setLayer.
    2.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`, `quad`,
        `x`, `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          Adds a layer of the SpriteBatch's Array Texture using the specified
        Quad.

        The specified layer index overrides any layer index set on the Quad via
        Quad:setLayer.

        Parameters: ~
            • {layerindex} (`number`)
                The index of the layer to use for this sprite.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The subsection of the texture's layer to use when drawing the
                sprite.
            • {x} (`number`) (default: `0`)
                The position to draw the sprite (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the sprite (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).

        Return: ~
            (`number`)
                The index of the added sprite, for use with SpriteBatch:set or
                SpriteBatch:setLayer.
    3.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`,
        `transform`)

    3.          Adds a layer of the SpriteBatch's Array Texture using the specified
        Transform.

        Parameters: ~
            • {layerindex} (`number`)
                The index of the layer to use for this sprite.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.

        Return: ~
            (`number`)
                The index of the added sprite, for use with SpriteBatch:set or
                SpriteBatch:setLayer.
    4.  `spriteindex` = |SpriteBatch:addLayer|(`layerindex`, `quad`,
        `transform`)

    4.          Adds a layer of the SpriteBatch's Array Texture using the specified
        Quad and Transform.

        In order to use an Array Texture or other non-2D texture types as the
        main texture in a custom void effect() variant must be used in the
        pixel shader, and MainTex must be declared as an ArrayImage or
        sampler2DArray like so: uniform ArrayImage MainTex;.

        Parameters: ~
            • {layerindex} (`number`)
                The index of the layer to use for this sprite.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The subsection of the texture's layer to use when drawing the
                sprite.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.

        Return: ~
            (`number`)
                The index of the added sprite, for use with SpriteBatch:set or
                SpriteBatch:setLayer.
-------------------------------------------------------------------------------
                                      *love2d-docs-SpriteBatch:attachAttribute*
|SpriteBatch:attachAttribute|

Attaches a per-vertex attribute from a Mesh onto this SpriteBatch, for use when
drawing. This can be combined with a Shader to augment a SpriteBatch with
per-vertex or additional per-sprite information instead of just having
per-sprite colors.

Each sprite in a SpriteBatch has 4 vertices in the following order: top-left,
bottom-left, top-right, bottom-right. The index returned by SpriteBatch:add
(and used by SpriteBatch:set) can used to determine the first vertex of a
specific sprite with the formula 1 + 4 * ( id - 1 ).

Synopses: ~

        |SpriteBatch:attachAttribute|(`name`, `mesh`)

Variants: ~

        |SpriteBatch:attachAttribute|(`name`, `mesh`)

        If a created with a custom vertex format, it will have 3 vertex
        attributes named VertexPosition, VertexTexCoord, and VertexColor. If
        vertex attributes with those names are attached to the SpriteBatch, it
        will override the SpriteBatch's sprite positions, texture coordinates,
        and sprite colors, respectively.

        Custom named attributes can be accessed in a vertex shader by declaring
        them as attribute vec4 MyCustomAttributeName; at the top-level of the
        vertex shader code. The name must match what was specified in the
        Mesh's vertex format and in the name argument of
        SpriteBatch:attachAttribute.

        A Mesh must have at least 4 * SpriteBatch:getBufferSize vertices in
        order to be attachable to a SpriteBatch.

        Parameters: ~
            • {name} (`string`)
                The name of the vertex attribute to attach.
            • {mesh} (`Mesh`) |love2d-docs-Mesh|
                The Mesh to get the vertex attribute from.


-------------------------------------------------------------------------------
                                                *love2d-docs-SpriteBatch:clear*
|SpriteBatch:clear|

Removes all sprites from the buffer.

Synopses: ~

        |SpriteBatch:clear|()

Variants: ~

        |SpriteBatch:clear|()

        See function description


-------------------------------------------------------------------------------
                                                *love2d-docs-SpriteBatch:flush*
|SpriteBatch:flush|

Immediately sends all new and modified sprite data in the batch to the graphics
card.

Normally it isn't necessary to call this method as
love.graphics.draw(spritebatch, ...) will do it automatically if needed, but
explicitly using SpriteBatch:flush gives more control over when the work
happens.

If this method is used, it generally shouldn't be called more than once (at
most) between love.graphics.draw(spritebatch, ...) calls.

Synopses: ~

        |SpriteBatch:flush|()

Variants: ~

        |SpriteBatch:flush|()

        See function description


-------------------------------------------------------------------------------
                                        *love2d-docs-SpriteBatch:getBufferSize*
|SpriteBatch:getBufferSize|

Gets the maximum number of sprites the SpriteBatch can hold.

Synopses: ~

        `size` = |SpriteBatch:getBufferSize|()

Variants: ~

        `size` = |SpriteBatch:getBufferSize|()

        See function description

        Return: ~
            (`number`)
                The maximum number of sprites the batch can hold.
-------------------------------------------------------------------------------
                                             *love2d-docs-SpriteBatch:getColor*
|SpriteBatch:getColor|

Gets the color that will be used for the next add and set operations.

If no color has been set with SpriteBatch:setColor or the current SpriteBatch
color has been cleared, this method will return nil.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        `r`, `g`, `b`, `a` = |SpriteBatch:getColor|()

Variants: ~

        `r`, `g`, `b`, `a` = |SpriteBatch:getColor|()

        See function description

        Returns: ~
            1. (`number`)
                The red component (0-1).

            2. (`number`)
                The green component (0-1).

            3. (`number`)
                The blue component (0-1).

            4. (`number`)
                The alpha component (0-1).
-------------------------------------------------------------------------------
                                             *love2d-docs-SpriteBatch:getCount*
|SpriteBatch:getCount|

Gets the number of sprites currently in the SpriteBatch.

Synopses: ~

        `count` = |SpriteBatch:getCount|()

Variants: ~

        `count` = |SpriteBatch:getCount|()

        See function description

        Return: ~
            (`number`)
                The number of sprites currently in the batch.
-------------------------------------------------------------------------------
                                           *love2d-docs-SpriteBatch:getTexture*
|SpriteBatch:getTexture|

Gets the texture (Image or Canvas) used by the SpriteBatch.

Synopses: ~

        `texture` = |SpriteBatch:getTexture|()

Variants: ~

        `texture` = |SpriteBatch:getTexture|()

        See function description

        Return: ~
            (`Texture`) |love2d-docs-Texture|
                The Image or Canvas used by the SpriteBatch.
-------------------------------------------------------------------------------
                                                  *love2d-docs-SpriteBatch:set*
|SpriteBatch:set|

Changes a sprite in the batch. This requires the sprite index returned by
SpriteBatch:add or SpriteBatch:addLayer.

Synopses: ~

    1.  |SpriteBatch:set|(`spriteindex`, `x`, `y`, `r`, `sx`, `sy`,
        `ox`, `oy`, `kx`, `ky`)
    2.  |SpriteBatch:set|(`spriteindex`, `quad`, `x`, `y`, `r`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

Variants: ~

    1.  |SpriteBatch:set|(`spriteindex`, `x`, `y`, `r`, `sx`, `sy`,
        `ox`, `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {spriteindex} (`number`)
                The index of the sprite that will be changed.
            • {x} (`number`)
                The position to draw the object (x-axis).
            • {y} (`number`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shear factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shear factor (y-axis).


    2.  |SpriteBatch:set|(`spriteindex`, `quad`, `x`, `y`, `r`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          Changes a sprite with a Quad in the batch. This requires the index
        returned by SpriteBatch:add or SpriteBatch:addLayer.

        SpriteBatches do not support removing individual sprites. One can do a
        pseudo removal (instead of clearing and re-adding everything) by:

        SpriteBatch:set(id, 0, 0, 0, 0, 0)

        This makes all the sprite's vertices equal (because the x and y scales
        are 0), which prevents the GPU from fully processing the sprite when
        drawing the SpriteBatch.

        Parameters: ~
            • {spriteindex} (`number`)
                The index of the sprite that will be changed.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The Quad used on the image of the batch.
            • {x} (`number`)
                The position to draw the object (x-axis).
            • {y} (`number`)
                The position to draw the object (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shear factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shear factor (y-axis).


-------------------------------------------------------------------------------
                                             *love2d-docs-SpriteBatch:setColor*
|SpriteBatch:setColor|

Sets the color that will be used for the next add and set operations. Calling
the function without arguments will disable all per-sprite colors for the
SpriteBatch.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

In version 0.9.2 and older, the global color set with love.graphics.setColor
will not work on the SpriteBatch if any of the sprites has its own color.

Synopses: ~

    1.  |SpriteBatch:setColor|(`r`, `g`, `b`, `a`)
    2.  |SpriteBatch:setColor|()

Variants: ~

    1.  |SpriteBatch:setColor|(`r`, `g`, `b`, `a`)

    1.          See function description

        Parameters: ~
            • {r} (`number`)
                The amount of red.
            • {g} (`number`)
                The amount of green.
            • {b} (`number`)
                The amount of blue.
            • {a} (`number`) (default: `1`)
                The amount of alpha.


    2.  |SpriteBatch:setColor|()

    2.          Disables all per-sprite colors for this SpriteBatch.


-------------------------------------------------------------------------------
                                         *love2d-docs-SpriteBatch:setDrawRange*
|SpriteBatch:setDrawRange|

Restricts the drawn sprites in the SpriteBatch to a subset of the total.

Synopses: ~

    1.  |SpriteBatch:setDrawRange|(`start`, `count`)
    2.  |SpriteBatch:setDrawRange|()

Variants: ~

    1.  |SpriteBatch:setDrawRange|(`start`, `count`)

    1.          See function description

        Parameters: ~
            • {start} (`number`)
                The index of the first sprite to draw. Index 1 corresponds to
                the first sprite added with SpriteBatch:add.
            • {count} (`number`)
                The number of sprites to draw.


    2.  |SpriteBatch:setDrawRange|()

    2.          Allows all sprites in the SpriteBatch to be drawn.


-------------------------------------------------------------------------------
                                             *love2d-docs-SpriteBatch:setLayer*
|SpriteBatch:setLayer|

Changes a sprite previously added with add or addLayer, in a batch created with
an Array Texture.

Synopses: ~

    1.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`, `x`, `y`,
        `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`, `quad`,
        `x`, `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    3.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`,
        `transform`)
    4.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`, `quad`,
        `transform`)

Variants: ~

    1.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`, `x`, `y`,
        `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          Changes the sprite in the SpriteBatch.

        Parameters: ~
            • {spriteindex} (`number`)
                The index of the existing sprite to replace.
            • {layerindex} (`number`)
                The index of the layer in the Array Texture to use for this
                sprite.
            • {x} (`number`) (default: `0`)
                The position to draw the sprite (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the sprite (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    2.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`, `quad`,
        `x`, `y`, `r`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          Adds a layer of the SpriteBatch's Array Texture using the specified
        Quad.

        The specified layer index overrides any layer index set on the Quad via
        Quad:setLayer.

        Parameters: ~
            • {spriteindex} (`number`)
                The index of the existing sprite to replace.
            • {layerindex} (`number`)
                The index of the layer to use for this sprite.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The subsection of the texture's layer to use when drawing the
                sprite.
            • {x} (`number`) (default: `0`)
                The position to draw the sprite (x-axis).
            • {y} (`number`) (default: `0`)
                The position to draw the sprite (y-axis).
            • {r} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing factor (y-axis).


    3.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`,
        `transform`)

    3.          Adds a layer of the SpriteBatch's Array Texture using the specified
        Transform.

        Parameters: ~
            • {spriteindex} (`number`)
                The index of the existing sprite to replace.
            • {layerindex} (`number`)
                The index of the layer to use for the sprite.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.


    4.  |SpriteBatch:setLayer|(`spriteindex`, `layerindex`, `quad`,
        `transform`)

    4.          Adds a layer of the SpriteBatch's Array Texture using the specified
        Quad and Transform.

        The specified layer index overrides any layer index set on the Quad via
        Quad:setLayer.

        Parameters: ~
            • {spriteindex} (`number`)
                The index of the existing sprite to replace.
            • {layerindex} (`number`)
                The index of the layer to use for the sprite.
            • {quad} (`Quad`) |love2d-docs-Quad|
                The subsection of the texture's layer to use when drawing the
                sprite.
            • {transform} (`Transform`) |love2d-docs-Transform|
                A transform object.


-------------------------------------------------------------------------------
                                           *love2d-docs-SpriteBatch:setTexture*
|SpriteBatch:setTexture|

Sets the texture (Image or Canvas) used for the sprites in the batch, when
drawing.

Synopses: ~

        |SpriteBatch:setTexture|(`texture`)

Variants: ~

        |SpriteBatch:setTexture|(`texture`)

        See function description

        Parameters: ~
            • {texture} (`Texture`) |love2d-docs-Texture|
                The new Image or Canvas to use for the sprites in the batch.



-------------------------------------------------------------------------------
                                                             *love2d-docs-Text*
|Text|

Drawable text.

                                                *love2d-docs-Text-constructors*
    constructors:

        |newText|                                         |love2d-docs-newText|

                                                  *love2d-docs-Text-supertypes*
    supertypes:

        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-Text-subtypes*
    subtypes:

        None

                                                   *love2d-docs-Text-functions*
    functions:

        |add|                                            |love2d-docs-Text:add|
        |addf|                                          |love2d-docs-Text:addf|
        |clear|                                        |love2d-docs-Text:clear|
        |getDimensions|                        |love2d-docs-Text:getDimensions|
        |getFont|                                    |love2d-docs-Text:getFont|
        |getHeight|                                |love2d-docs-Text:getHeight|
        |getWidth|                                  |love2d-docs-Text:getWidth|
        |set|                                            |love2d-docs-Text:set|
        |setFont|                                    |love2d-docs-Text:setFont|
        |setf|                                          |love2d-docs-Text:setf|

-------------------------------------------------------------------------------
                                                         *love2d-docs-Text:add*
|Text:add|

Adds additional colored text to the Text object at the specified position.

Synopses: ~

    1.  `index` = |Text:add|(`textstring`, `x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  `index` = |Text:add|(`coloredtext`, `x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

Variants: ~

    1.  `index` = |Text:add|(`textstring`, `x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {textstring} (`string`)
                The text to add to the object.
            • {x} (`number`) (default: `0`)
                The position of the new text on the x-axis.
            • {y} (`number`) (default: `0`)
                The position of the new text on the y-axis.
            • {angle} (`number`) (default: `0`)
                The orientation of the new text in radians.
            • {sx} (`number`) (default: `1`)
                Scale factor on the x-axis.
            • {sy} (`number`) (default: `sx`)
                Scale factor on the y-axis.
            • {ox} (`number`) (default: `0`)
                Origin offset on the x-axis.
            • {oy} (`number`) (default: `0`)
                Origin offset on the y-axis.
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor on the x-axis.
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor on the y-axis.

        Return: ~
            (`number`)
                An index number that can be used with Text:getWidth or
                Text:getHeight.
    2.  `index` = |Text:add|(`coloredtext`, `x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text, when drawing the Text object.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {x} (`number`) (default: `0`)
                The position of the new text on the x-axis.
            • {y} (`number`) (default: `0`)
                The position of the new text on the y-axis.
            • {angle} (`number`) (default: `0`)
                The orientation of the new text in radians.
            • {sx} (`number`) (default: `1`)
                Scale factor on the x-axis.
            • {sy} (`number`) (default: `sx`)
                Scale factor on the y-axis.
            • {ox} (`number`) (default: `0`)
                Origin offset on the x-axis.
            • {oy} (`number`) (default: `0`)
                Origin offset on the y-axis.
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor on the x-axis.
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor on the y-axis.

        Return: ~
            (`number`)
                An index number that can be used with Text:getWidth or
                Text:getHeight.
-------------------------------------------------------------------------------
                                                        *love2d-docs-Text:addf*
|Text:addf|

Adds additional formatted / colored text to the Text object at the specified
position.

The word wrap limit is applied before any scaling, rotation, and other
coordinate transformations. Therefore the amount of text per line stays
constant given the same wrap limit, even if the scale arguments change.

Synopses: ~

    1.  `index` = |Text:addf|(`textstring`, `wraplimit`, `align`, `x`,
        `y`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)
    2.  `index` = |Text:addf|(`coloredtext`, `wraplimit`, `align`, `x`,
        `y`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

Variants: ~

    1.  `index` = |Text:addf|(`textstring`, `wraplimit`, `align`, `x`,
        `y`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    1.          See function description

        Parameters: ~
            • {textstring} (`string`)
                The text to add to the object.
            • {wraplimit} (`number`)
                The maximum width in pixels of the text before it gets
                automatically wrapped to a new line.
            • {align} (`AlignMode`) |love2d-docs-AlignMode|
                The alignment of the text.
            • {x} (`number`)
                The position of the new text (x-axis).
            • {y} (`number`)
                The position of the new text (y-axis).
            • {angle} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor (y-axis).

        Return: ~
            (`number`)
                An index number that can be used with Text:getWidth or
                Text:getHeight.
    2.  `index` = |Text:addf|(`coloredtext`, `wraplimit`, `align`, `x`,
        `y`, `angle`, `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text, when drawing the Text object.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to add to the object, in
                the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {wraplimit} (`number`)
                The maximum width in pixels of the text before it gets
                automatically wrapped to a new line.
            • {align} (`AlignMode`) |love2d-docs-AlignMode|
                The alignment of the text.
            • {x} (`number`)
                The position of the new text (x-axis).
            • {y} (`number`)
                The position of the new text (y-axis).
            • {angle} (`number`) (default: `0`)
                Orientation (radians).
            • {sx} (`number`) (default: `1`)
                Scale factor (x-axis).
            • {sy} (`number`) (default: `sx`)
                Scale factor (y-axis).
            • {ox} (`number`) (default: `0`)
                Origin offset (x-axis).
            • {oy} (`number`) (default: `0`)
                Origin offset (y-axis).
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor (x-axis).
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor (y-axis).

        Return: ~
            (`number`)
                An index number that can be used with Text:getWidth or
                Text:getHeight.
-------------------------------------------------------------------------------
                                                       *love2d-docs-Text:clear*
|Text:clear|

Clears the contents of the Text object.

Synopses: ~

        |Text:clear|()

Variants: ~

        |Text:clear|()

        See function description


-------------------------------------------------------------------------------
                                               *love2d-docs-Text:getDimensions*
|Text:getDimensions|

Gets the width and height of the text in pixels.

Synopses: ~

    1.  `width`, `height` = |Text:getDimensions|()
    2.  `width`, `height` = |Text:getDimensions|(`index`)

Variants: ~

    1.  `width`, `height` = |Text:getDimensions|()

    1.          See function description

        Returns: ~
            1. (`number`)
                The width of the text. If multiple sub-strings have been added
                with Text:add, the width of the last sub-string is returned.

            2. (`number`)
                The height of the text. If multiple sub-strings have been added
                with Text:add, the height of the last sub-string is returned.
    2.  `width`, `height` = |Text:getDimensions|(`index`)

    2.          Gets the width and height of a specific sub-string that was previously
        added to the Text object.

        Parameters: ~
            • {index} (`number`)
                An index number returned by Text:add or Text:addf.

        Returns: ~
            1. (`number`)
                The width of the sub-string (before scaling and other
                transformations).

            2. (`number`)
                The height of the sub-string (before scaling and other
                transformations).
-------------------------------------------------------------------------------
                                                     *love2d-docs-Text:getFont*
|Text:getFont|

Gets the Font used with the Text object.

Synopses: ~

        `font` = |Text:getFont|()

Variants: ~

        `font` = |Text:getFont|()

        See function description

        Return: ~
            (`Font`) |love2d-docs-Font|
                The font used with this Text object.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Text:getHeight*
|Text:getHeight|

Gets the height of the text in pixels.

Synopses: ~

    1.  ` height ` = |Text:getHeight|()
    2.  `height` = |Text:getHeight|(`index`)

Variants: ~

    1.  ` height ` = |Text:getHeight|()

    1.          See function description

        Return: ~
            (`number`)
                The height of the text. If multiple sub-strings have been added
                with Text:add, the height of the last sub-string is returned.
    2.  `height` = |Text:getHeight|(`index`)

    2.          Gets the height of a specific sub-string that was previously added to
        the Text object.

        Parameters: ~
            • {index} (`number`)
                An index number returned by Text:add or Text:addf.

        Return: ~
            (`number`)
                The height of the sub-string (before scaling and other
                transformations).
-------------------------------------------------------------------------------
                                                    *love2d-docs-Text:getWidth*
|Text:getWidth|

Gets the width of the text in pixels.

Synopses: ~

    1.  `width` = |Text:getWidth|()
    2.  `width` = |Text:getWidth|(`index`)

Variants: ~

    1.  `width` = |Text:getWidth|()

    1.          See function description

        Return: ~
            (`number`)
                The width of the text. If multiple sub-strings have been added
                with Text:add, the width of the last sub-string is returned.
    2.  `width` = |Text:getWidth|(`index`)

    2.          Gets the width of a specific sub-string that was previously added to
        the Text object.

        Parameters: ~
            • {index} (`number`)
                An index number returned by Text:add or Text:addf.

        Return: ~
            (`number`)
                The width of the sub-string (before scaling and other
                transformations).
-------------------------------------------------------------------------------
                                                         *love2d-docs-Text:set*
|Text:set|

Replaces the contents of the Text object with a new unformatted string.

Synopses: ~

    1.  |Text:set|(`textstring`)
    2.  |Text:set|(`coloredtext`)

Variants: ~

    1.  |Text:set|(`textstring`)

    1.          See function description

        Parameters: ~
            • {textstring} (`string`)
                The new string of text to use.


    2.  |Text:set|(`coloredtext`)

    2.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text, when drawing the Text object.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to use as the new text,
                in the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.


-------------------------------------------------------------------------------
                                                     *love2d-docs-Text:setFont*
|Text:setFont|

Replaces the Font used with the text.

Synopses: ~

        |Text:setFont|(`font`)

Variants: ~

        |Text:setFont|(`font`)

        See function description

        Parameters: ~
            • {font} (`Font`) |love2d-docs-Font|
                The new font to use with this Text object.


-------------------------------------------------------------------------------
                                                        *love2d-docs-Text:setf*
|Text:setf|

Replaces the contents of the Text object with a new formatted string.

Synopses: ~

    1.  |Text:setf|(`textstring`, `wraplimit`, `align`)
    2.  |Text:setf|(`coloredtext`, `wraplimit`, `align`)

Variants: ~

    1.  |Text:setf|(`textstring`, `wraplimit`, `align`)

    1.          See function description

        Parameters: ~
            • {textstring} (`string`)
                The new string of text to use.
            • {wraplimit} (`number`)
                The maximum width in pixels of the text before it gets
                automatically wrapped to a new line.
            • {align} (`AlignMode`) |love2d-docs-AlignMode|
                The alignment of the text.


    2.  |Text:setf|(`coloredtext`, `wraplimit`, `align`)

    2.          The color set by love.graphics.setColor will be combined (multiplied)
        with the colors of the text, when drawing the Text object.

        Parameters: ~
            • {coloredtext} (`table`)
                A table containing colors and strings to use as the new text,
                in the form of {color1, string1, color2, string2, ...}.
                • {color1} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string1} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {color2} (`table`)
                    A table containing red, green, blue, and optional alpha
                    components to use as a color for the next string in the
                    table, in the form of {red, green, blue, alpha}.
                • {string2} (`string`)
                    A string of text which has a color specified by the
                    previous color.
                • {...} (`tablesandstrings`)
                    Additional colors and strings.
            • {wraplimit} (`number`)
                The maximum width in pixels of the text before it gets
                automatically wrapped to a new line.
            • {align} (`AlignMode`) |love2d-docs-AlignMode|
                The alignment of the text.



-------------------------------------------------------------------------------
                                                          *love2d-docs-Texture*
|Texture|

Superclass for drawable objects which represent a texture. All Textures can be
drawn with Quads. This is an abstract type that can't be created directly.

                                             *love2d-docs-Texture-constructors*
    constructors:

        None

                                               *love2d-docs-Texture-supertypes*
    supertypes:

        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                                 *love2d-docs-Texture-subtypes*
    subtypes:

        None

                                                *love2d-docs-Texture-functions*
    functions:

        |getDPIScale|                         |love2d-docs-Texture:getDPIScale|
        |getDepth|                               |love2d-docs-Texture:getDepth|
        |getDepthSampleMode|           |love2d-docs-Texture:getDepthSampleMode|
        |getDimensions|                     |love2d-docs-Texture:getDimensions|
        |getFilter|                             |love2d-docs-Texture:getFilter|
        |getFormat|                             |love2d-docs-Texture:getFormat|
        |getHeight|                             |love2d-docs-Texture:getHeight|
        |getLayerCount|                     |love2d-docs-Texture:getLayerCount|
        |getMipmapCount|                   |love2d-docs-Texture:getMipmapCount|
        |getMipmapFilter|                 |love2d-docs-Texture:getMipmapFilter|
        |getPixelDimensions|           |love2d-docs-Texture:getPixelDimensions|
        |getPixelHeight|                   |love2d-docs-Texture:getPixelHeight|
        |getPixelWidth|                     |love2d-docs-Texture:getPixelWidth|
        |getTextureType|                   |love2d-docs-Texture:getTextureType|
        |getWidth|                               |love2d-docs-Texture:getWidth|
        |getWrap|                                 |love2d-docs-Texture:getWrap|
        |isReadable|                           |love2d-docs-Texture:isReadable|
        |setDepthSampleMode|           |love2d-docs-Texture:setDepthSampleMode|
        |setFilter|                             |love2d-docs-Texture:setFilter|
        |setMipmapFilter|                 |love2d-docs-Texture:setMipmapFilter|
        |setWrap|                                 |love2d-docs-Texture:setWrap|

-------------------------------------------------------------------------------
                                              *love2d-docs-Texture:getDPIScale*
|Texture:getDPIScale|

Gets the DPI scale factor of the Texture.

The DPI scale factor represents relative pixel density. A DPI scale factor of 2
means the texture has twice the pixel density in each dimension (4 times as
many pixels in the same area) compared to a texture with a DPI scale factor of
1.

For example, a texture with pixel dimensions of 100x100 with a DPI scale factor
of 2 will be drawn as if it was 50x50. This is useful with high-dpi /  retina
displays to easily allow swapping out higher or lower pixel density Images and
Canvases without needing any extra manual scaling logic.

Synopses: ~

        `dpiscale` = |Texture:getDPIScale|()

Variants: ~

        `dpiscale` = |Texture:getDPIScale|()

        See function description

        Return: ~
            (`number`)
                The DPI scale factor of the Texture.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Texture:getDepth*
|Texture:getDepth|

Gets the depth of a Volume Texture. Returns 1 for 2D, Cubemap, and Array
textures.

Synopses: ~

        `depth` = |Texture:getDepth|()

Variants: ~

        `depth` = |Texture:getDepth|()

        See function description

        Return: ~
            (`number`)
                The depth of the volume Texture.
-------------------------------------------------------------------------------
                                       *love2d-docs-Texture:getDepthSampleMode*
|Texture:getDepthSampleMode|

Gets the comparison mode used when sampling from a depth texture in a shader.

Depth texture comparison modes are advanced low-level functionality typically
used with shadow mapping in 3D.

Synopses: ~

        `compare` = |Texture:getDepthSampleMode|()

Variants: ~

        `compare` = |Texture:getDepthSampleMode|()

        See function description

        Return: ~
            (`CompareMode`) |love2d-docs-CompareMode|
                The comparison mode used when sampling from this texture in a
                shader, or nil if setDepthSampleMode has not been called on
                this Texture.
-------------------------------------------------------------------------------
                                            *love2d-docs-Texture:getDimensions*
|Texture:getDimensions|

Gets the width and height of the Texture.

Synopses: ~

        `width`, `height` = |Texture:getDimensions|()

Variants: ~

        `width`, `height` = |Texture:getDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The width of the Texture.

            2. (`number`)
                The height of the Texture.
-------------------------------------------------------------------------------
                                                *love2d-docs-Texture:getFilter*
|Texture:getFilter|

Gets the filter mode of the Texture.

Synopses: ~

        `min`, `mag`, `anisotropy` = |Texture:getFilter|()

Variants: ~

        `min`, `mag`, `anisotropy` = |Texture:getFilter|()

        See function description

        Returns: ~
            1. (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode to use when minifying the texture (rendering it at
                a smaller size on-screen than its size in pixels).

            2. (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode to use when magnifying the texture (rendering it at
                a smaller size on-screen than its size in pixels).

            3. (`number`)
                Maximum amount of anisotropic filtering used.
-------------------------------------------------------------------------------
                                                *love2d-docs-Texture:getFormat*
|Texture:getFormat|

Gets the pixel format of the Texture.

Synopses: ~

        `format` = |Texture:getFormat|()

Variants: ~

        `format` = |Texture:getFormat|()

        See function description

        Return: ~
            (`PixelFormat`) |love2d-docs-PixelFormat|
                The pixel format the Texture was created with.
-------------------------------------------------------------------------------
                                                *love2d-docs-Texture:getHeight*
|Texture:getHeight|

Gets the height of the Texture.

Synopses: ~

        `height` = |Texture:getHeight|()

Variants: ~

        `height` = |Texture:getHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the Texture.
-------------------------------------------------------------------------------
                                            *love2d-docs-Texture:getLayerCount*
|Texture:getLayerCount|

Gets the number of layers / slices in an Array Texture. Returns 1 for 2D,
Cubemap, and Volume textures.

Synopses: ~

        `layers` = |Texture:getLayerCount|()

Variants: ~

        `layers` = |Texture:getLayerCount|()

        See function description

        Return: ~
            (`number`)
                The number of layers in the Array Texture.
-------------------------------------------------------------------------------
                                           *love2d-docs-Texture:getMipmapCount*
|Texture:getMipmapCount|

Gets the number of mipmaps contained in the Texture. If the texture was not
created with mipmaps, it will return 1.

Synopses: ~

        `mipmaps` = |Texture:getMipmapCount|()

Variants: ~

        `mipmaps` = |Texture:getMipmapCount|()

        See function description

        Return: ~
            (`number`)
                The number of mipmaps in the Texture.
-------------------------------------------------------------------------------
                                          *love2d-docs-Texture:getMipmapFilter*
|Texture:getMipmapFilter|

Gets the mipmap filter mode for a Texture. Prior to 11.0 this method only
worked on Images.

Synopses: ~

        `mode`, `sharpness` = |Texture:getMipmapFilter|()

Variants: ~

        `mode`, `sharpness` = |Texture:getMipmapFilter|()

        See function description

        Returns: ~
            1. (`FilterMode`) |love2d-docs-FilterMode|
                The filter mode used in between mipmap levels. nil if mipmap
                filtering is not enabled.

            2. (`number`)
                Value used to determine whether the image should use more or
                less detailed mipmap levels than normal when drawing.
-------------------------------------------------------------------------------
                                       *love2d-docs-Texture:getPixelDimensions*
|Texture:getPixelDimensions|

Gets the width and height in pixels of the Texture.

Texture:getDimensions gets the dimensions of the texture in units scaled by the
texture's DPI scale factor, rather than pixels. Use getDimensions for
calculations related to drawing the texture (calculating an origin offset, for
example), and getPixelDimensions only when dealing specifically with pixels,
for example when using Canvas:newImageData.

Synopses: ~

        `pixelwidth`, `pixelheight` = |Texture:getPixelDimensions|()

Variants: ~

        `pixelwidth`, `pixelheight` = |Texture:getPixelDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The width of the Texture, in pixels.

            2. (`number`)
                The height of the Texture, in pixels.
-------------------------------------------------------------------------------
                                           *love2d-docs-Texture:getPixelHeight*
|Texture:getPixelHeight|

Gets the height in pixels of the Texture.

DPI scale factor, rather than pixels. Use getHeight for calculations related to
drawing the texture (calculating an origin offset, for example), and
getPixelHeight only when dealing specifically with pixels, for example when
using Canvas:newImageData.

Synopses: ~

        `pixelheight` = |Texture:getPixelHeight|()

Variants: ~

        `pixelheight` = |Texture:getPixelHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the Texture, in pixels.
-------------------------------------------------------------------------------
                                            *love2d-docs-Texture:getPixelWidth*
|Texture:getPixelWidth|

Gets the width in pixels of the Texture.

DPI scale factor, rather than pixels. Use getWidth for calculations related to
drawing the texture (calculating an origin offset, for example), and
getPixelWidth only when dealing specifically with pixels, for example when
using Canvas:newImageData.

Synopses: ~

        `pixelwidth` = |Texture:getPixelWidth|()

Variants: ~

        `pixelwidth` = |Texture:getPixelWidth|()

        See function description

        Return: ~
            (`number`)
                The width of the Texture, in pixels.
-------------------------------------------------------------------------------
                                           *love2d-docs-Texture:getTextureType*
|Texture:getTextureType|

Gets the type of the Texture.

Synopses: ~

        `texturetype` = |Texture:getTextureType|()

Variants: ~

        `texturetype` = |Texture:getTextureType|()

        See function description

        Return: ~
            (`TextureType`) |love2d-docs-TextureType|
                The type of the Texture.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Texture:getWidth*
|Texture:getWidth|

Gets the width of the Texture.

Synopses: ~

        `width` = |Texture:getWidth|()

Variants: ~

        `width` = |Texture:getWidth|()

        See function description

        Return: ~
            (`number`)
                The width of the Texture.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Texture:getWrap*
|Texture:getWrap|

Gets the wrapping properties of a Texture.

This function returns the currently set horizontal and vertical wrapping modes
for the texture.

Synopses: ~

        `horiz`, `vert`, `depth` = |Texture:getWrap|()

Variants: ~

        `horiz`, `vert`, `depth` = |Texture:getWrap|()

        See function description

        Returns: ~
            1. (`WrapMode`) |love2d-docs-WrapMode|
                Horizontal wrapping mode of the texture.

            2. (`WrapMode`) |love2d-docs-WrapMode|
                Vertical wrapping mode of the texture.

            3. (`WrapMode`) |love2d-docs-WrapMode|
                Wrapping mode for the z-axis of a Volume texture.
-------------------------------------------------------------------------------
                                               *love2d-docs-Texture:isReadable*
|Texture:isReadable|

Gets whether the Texture can be drawn and sent to a Shader.

Canvases created with stencil and/or depth PixelFormats are not readable by
default, unless readable=true is specified in the settings table passed into
love.graphics.newCanvas.

Non-readable Canvases can still be rendered to.

Synopses: ~

        `readable` = |Texture:isReadable|()

Variants: ~

        `readable` = |Texture:isReadable|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Texture is readable.
-------------------------------------------------------------------------------
                                       *love2d-docs-Texture:setDepthSampleMode*
|Texture:setDepthSampleMode|

Sets the comparison mode used when sampling from a depth texture in a shader.
Depth texture comparison modes are advanced low-level functionality typically
used with shadow mapping in 3D.

When using a depth texture with a comparison mode set in a shader, it must be
declared as a sampler2DShadow and used in a GLSL 3 Shader. The result of
accessing the texture in the shader will return a float between 0 and 1,
proportional to the number of samples (up to 4 samples will be used if bilinear
filtering is enabled) that passed the test set by the comparison operation.

Depth texture comparison can only be used with readable depth-formatted
Canvases.

Synopses: ~

        |Texture:setDepthSampleMode|(`compare`)

Variants: ~

        |Texture:setDepthSampleMode|(`compare`)

        See function description

        Parameters: ~
            • {compare} (`CompareMode`) |love2d-docs-CompareMode|
                The comparison mode used when sampling from this texture in a
                shader.


-------------------------------------------------------------------------------
                                                *love2d-docs-Texture:setFilter*
|Texture:setFilter|

Sets the filter mode of the Texture.

Synopses: ~

        |Texture:setFilter|(`min`, `mag`, `anisotropy`)

Variants: ~

        |Texture:setFilter|(`min`, `mag`, `anisotropy`)

        See function description

        Parameters: ~
            • {min} (`FilterMode`) |love2d-docs-FilterMode|
                Filter mode to use when minifying the texture (rendering it at
                a smaller size on-screen than its size in pixels).
            • {mag} (`FilterMode`) |love2d-docs-FilterMode| (default: `min`)
                Filter mode to use when magnifying the texture (rendering it at
                a larger size on-screen than its size in pixels).
            • {anisotropy} (`number`) (default: `1`)
                Maximum amount of anisotropic filtering to use.


-------------------------------------------------------------------------------
                                          *love2d-docs-Texture:setMipmapFilter*
|Texture:setMipmapFilter|

Sets the mipmap filter mode for a Texture. Prior to 11.0 this method only
worked on Images.

Mipmapping is useful when drawing a texture at a reduced scale. It can improve
performance and reduce aliasing issues.

In created with the mipmaps flag enabled for the mipmap filter to have any
effect. In versions prior to 0.10.0 it's best to call this method directly
after creating the image with love.graphics.newImage, to avoid bugs in certain
graphics drivers.

Due to hardware restrictions and driver bugs, in versions prior to 0.10.0
images that weren't loaded from a CompressedData must have power-of-two
dimensions (64x64, 512x256, etc.) to use mipmaps.

Synopses: ~

    1.  |Texture:setMipmapFilter|(`filtermode`, `sharpness`)
    2.  |Texture:setMipmapFilter|()

Variants: ~

    1.  |Texture:setMipmapFilter|(`filtermode`, `sharpness`)

    1.          On mobile devices (Android and iOS), the sharpness parameter is not
        supported and will do nothing. You can use a custom compressed and its
        CompressedData has mipmap data included, it will use that.

        Parameters: ~
            • {filtermode} (`FilterMode`) |love2d-docs-FilterMode|
                The filter mode to use in between mipmap levels. 'nearest' will
                often give better performance.
            • {sharpness} (`number`) (default: `0`)
                A positive sharpness value makes the texture use a more
                detailed mipmap level when drawing, at the expense of
                performance. A negative value does the reverse.


    2.  |Texture:setMipmapFilter|()

    2.          Disables mipmap filtering.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Texture:setWrap*
|Texture:setWrap|

Sets the wrapping properties of a Texture.

This function sets the way a Texture is repeated when it is drawn with a Quad
that is larger than the texture's extent, or when a custom Shader is used which
uses texture coordinates outside of [0, 1]. A texture may be clamped or set to
repeat in both horizontal and vertical directions.

Clamped textures appear only once (with the edges of the texture stretching to
fill the extent of the Quad), whereas repeated ones repeat as many times as
there is room in the Quad.

Synopses: ~

        |Texture:setWrap|(`horiz`, `vert`, `depth`)

Variants: ~

        |Texture:setWrap|(`horiz`, `vert`, `depth`)

        See function description

        Parameters: ~
            • {horiz} (`WrapMode`) |love2d-docs-WrapMode|
                Horizontal wrapping mode of the texture.
            • {vert} (`WrapMode`) |love2d-docs-WrapMode| (default: `horiz`)
                Vertical wrapping mode of the texture.
            • {depth} (`WrapMode`) |love2d-docs-WrapMode| (default: `horiz`)
                Wrapping mode for the z-axis of a Volume texture.



-------------------------------------------------------------------------------
                                                            *love2d-docs-Video*
|Video|

A drawable video.

                                               *love2d-docs-Video-constructors*
    constructors:

        |newVideo|                                       |love2d-docs-newVideo|

                                                 *love2d-docs-Video-supertypes*
    supertypes:

        |Drawable|                                       |love2d-docs-Drawable|
        |Object|                                           |love2d-docs-Object|

                                                   *love2d-docs-Video-subtypes*
    subtypes:

        None

                                                  *love2d-docs-Video-functions*
    functions:

        |getDimensions|                       |love2d-docs-Video:getDimensions|
        |getFilter|                               |love2d-docs-Video:getFilter|
        |getHeight|                               |love2d-docs-Video:getHeight|
        |getSource|                               |love2d-docs-Video:getSource|
        |getStream|                               |love2d-docs-Video:getStream|
        |getWidth|                                 |love2d-docs-Video:getWidth|
        |isPlaying|                               |love2d-docs-Video:isPlaying|
        |pause|                                       |love2d-docs-Video:pause|
        |play|                                         |love2d-docs-Video:play|
        |rewind|                                     |love2d-docs-Video:rewind|
        |seek|                                         |love2d-docs-Video:seek|
        |setFilter|                               |love2d-docs-Video:setFilter|
        |setSource|                               |love2d-docs-Video:setSource|
        |tell|                                         |love2d-docs-Video:tell|

-------------------------------------------------------------------------------
                                              *love2d-docs-Video:getDimensions*
|Video:getDimensions|

Gets the width and height of the Video in pixels.

Synopses: ~

        `width`, `height` = |Video:getDimensions|()

Variants: ~

        `width`, `height` = |Video:getDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The width of the Video.

            2. (`number`)
                The height of the Video.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:getFilter*
|Video:getFilter|

Gets the scaling filters used when drawing the Video.

Synopses: ~

        `min`, `mag`, `anisotropy` = |Video:getFilter|()

Variants: ~

        `min`, `mag`, `anisotropy` = |Video:getFilter|()

        See function description

        Returns: ~
            1. (`FilterMode`) |love2d-docs-FilterMode|
                The filter mode used when scaling the Video down.

            2. (`FilterMode`) |love2d-docs-FilterMode|
                The filter mode used when scaling the Video up.

            3. (`number`)
                Maximum amount of anisotropic filtering used.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:getHeight*
|Video:getHeight|

Gets the height of the Video in pixels.

Synopses: ~

        `height` = |Video:getHeight|()

Variants: ~

        `height` = |Video:getHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the Video.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:getSource*
|Video:getSource|

Gets the audio Source used for playing back the video's audio. May return nil
if the video has no audio, or if Video:setSource is called with a nil argument.

Synopses: ~

        `source` = |Video:getSource|()

Variants: ~

        `source` = |Video:getSource|()

        See function description

        Return: ~
            (`Source`) |love2d-docs-Source|
                The audio Source used for audio playback, or nil if the video
                has no audio.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:getStream*
|Video:getStream|

Gets the VideoStream object used for decoding and controlling the video.

Synopses: ~

        `stream` = |Video:getStream|()

Variants: ~

        `stream` = |Video:getStream|()

        See function description

        Return: ~
            (`VideoStream`) |love2d-docs-VideoStream|
                The VideoStream used for decoding and controlling the video.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Video:getWidth*
|Video:getWidth|

Gets the width of the Video in pixels.

Synopses: ~

        `width` = |Video:getWidth|()

Variants: ~

        `width` = |Video:getWidth|()

        See function description

        Return: ~
            (`number`)
                The width of the Video.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:isPlaying*
|Video:isPlaying|

Gets whether the Video is currently playing.

Synopses: ~

        `playing` = |Video:isPlaying|()

Variants: ~

        `playing` = |Video:isPlaying|()

        See function description

        Return: ~
            (`boolean`)
                Whether the video is playing.
-------------------------------------------------------------------------------
                                                      *love2d-docs-Video:pause*
|Video:pause|

Pauses the Video.

Synopses: ~

        |Video:pause|()

Variants: ~

        |Video:pause|()

        See function description


-------------------------------------------------------------------------------
                                                       *love2d-docs-Video:play*
|Video:play|

Starts playing the Video. In order for the video to appear onscreen it must be
drawn with love.graphics.draw.

Synopses: ~

        |Video:play|()

Variants: ~

        |Video:play|()

        See function description


-------------------------------------------------------------------------------
                                                     *love2d-docs-Video:rewind*
|Video:rewind|

Rewinds the Video to the beginning.

Synopses: ~

        |Video:rewind|()

Variants: ~

        |Video:rewind|()

        See function description


-------------------------------------------------------------------------------
                                                       *love2d-docs-Video:seek*
|Video:seek|

Sets the current playback position of the Video.

Synopses: ~

        |Video:seek|(`offset`)

Variants: ~

        |Video:seek|(`offset`)

        See function description

        Parameters: ~
            • {offset} (`number`)
                The time in seconds since the beginning of the Video.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:setFilter*
|Video:setFilter|

Sets the scaling filters used when drawing the Video.

Synopses: ~

        |Video:setFilter|(`min`, `mag`, `anisotropy`)

Variants: ~

        |Video:setFilter|(`min`, `mag`, `anisotropy`)

        See function description

        Parameters: ~
            • {min} (`FilterMode`) |love2d-docs-FilterMode|
                The filter mode used when scaling the Video down.
            • {mag} (`FilterMode`) |love2d-docs-FilterMode|
                The filter mode used when scaling the Video up.
            • {anisotropy} (`number`) (default: `1`)
                Maximum amount of anisotropic filtering used.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Video:setSource*
|Video:setSource|

Sets the audio Source used for playing back the video's audio. The audio Source
also controls playback speed and synchronization.

Synopses: ~

        |Video:setSource|(`source`)

Variants: ~

        |Video:setSource|(`source`)

        See function description

        Parameters: ~
            • {source} (`Source`) |love2d-docs-Source| (default: `nil`)
                The audio Source used for audio playback, or nil to disable
                audio synchronization.


-------------------------------------------------------------------------------
                                                       *love2d-docs-Video:tell*
|Video:tell|

Gets the current playback position of the Video.

Synopses: ~

        `seconds` = |Video:tell|()

Variants: ~

        `seconds` = |Video:tell|()

        See function description

        Return: ~
            (`number`)
                The time in seconds since the beginning of the Video.

===============================================================================
                                                       *love2d-docs-love.image*
|love.image|

Provides an interface to decode encoded image data.

    |callbacks|                              |love2d-docs-love.image-callbacks|
    |enums|                                      |love2d-docs-love.image-enums|
    |functions|                              |love2d-docs-love.image-functions|
    |types|                                      |love2d-docs-love.image-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.image-callbacks*
The callbacks of |love.image|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.image-enums*
The enums of |love.image|:

    |CompressedImageFormat|                 |love2d-docs-CompressedImageFormat|
    |ImageFormat|                                     |love2d-docs-ImageFormat|
    |PixelFormat|                                     |love2d-docs-PixelFormat|

-------------------------------------------------------------------------------
                                            *love2d-docs-CompressedImageFormat*
|CompressedImageFormat|

Compressed image data formats. Here and here are a couple overviews of many of
the formats.

Unlike traditional PNG or jpeg, these formats stay compressed in RAM and in the
graphics card's VRAM. This is good for saving memory space as well as improving
performance, since the graphics card will be able to keep more of the image's
pixels in its fast-access cache when drawing it.

    constants: ~

        `DXT1`: string

            The DXT1 format. RGB data at 4 bits per pixel (compared to 32 bits
            for ImageData and regular Images.) Suitable for fully opaque images
            on desktop systems.

        `DXT3`: string

            The DXT3 format. RGBA data at 8 bits per pixel. Smooth variations
            in opacity do not mix well with this format.

        `DXT5`: string

            The DXT5 format. RGBA data at 8 bits per pixel. Recommended for
            images with varying opacity on desktop systems.

        `BC4`: string

            The BC4 format (also known as 3Dc+ or ATI1.) Stores just the red
            channel, at 4 bits per pixel.

        `BC4s`: string

            The signed variant of the BC4 format. Same as above but pixel
            values in the texture are in the range of 1 instead of 1 in
            shaders.

        `BC5`: string

            The BC5 format (also known as 3Dc or ATI2.) Stores red and green
            channels at 8 bits per pixel.

        `BC5s`: string

            The signed variant of the BC5 format.

        `BC6h`: string

            The BC6H format. Stores half-precision floating-point RGB data in
            the range of 65504 at 8 bits per pixel. Suitable for HDR images on
            desktop systems.

        `BC6hs`: string

            The signed variant of the BC6H format. Stores RGB data in the range
            of +65504.

        `BC7`: string

            The BC7 format (also known as BPTC.) Stores RGB or RGBA data at 8
            bits per pixel.

        `ETC1`: string

            The ETC1 format. RGB data at 4 bits per pixel. Suitable for fully
            opaque images on older Android devices.

        `ETC2rgb`: string

            The RGB variant of the ETC2 format. RGB data at 4 bits per pixel.
            Suitable for fully opaque images on newer mobile devices.

        `ETC2rgba`: string

            The RGBA variant of the ETC2 format. RGBA data at 8 bits per pixel.
            Recommended for images with varying opacity on newer mobile
            devices.

        `ETC2rgba1`: string

            The RGBA variant of the ETC2 format where pixels are either fully
            transparent or fully opaque. RGBA data at 4 bits per pixel.

        `EACr`: string

            The single-channel variant of the EAC format. Stores just the red
            channel, at 4 bits per pixel.

        `EACrs`: string

            The signed single-channel variant of the EAC format. Same as above
            but pixel values in the texture are in the range of 1 instead of 1
            in shaders.

        `EACrg`: string

            The two-channel variant of the EAC format. Stores red and green
            channels at 8 bits per pixel.

        `EACrgs`: string

            The signed two-channel variant of the EAC format.

        `PVR1rgb2`: string

            The 2 bit per pixel RGB variant of the PVRTC1 format. Stores RGB
            data at 2 bits per pixel. Textures compressed with PVRTC1 formats
            must be square and power-of-two sized.

        `PVR1rgb4`: string

            The 4 bit per pixel RGB variant of the PVRTC1 format. Stores RGB
            data at 4 bits per pixel.

        `PVR1rgba2`: string

            The 2 bit per pixel RGBA variant of the PVRTC1 format.

        `PVR1rgba4`: string

            The 4 bit per pixel RGBA variant of the PVRTC1 format.

        `ASTC4x4`: string

            The 4x4 pixels per block variant of the ASTC format. RGBA data at 8
            bits per pixel.

        `ASTC5x4`: string

            The 5x4 pixels per block variant of the ASTC format. RGBA data at
            6.4 bits per pixel.

        `ASTC5x5`: string

            The 5x5 pixels per block variant of the ASTC format. RGBA data at
            5.12 bits per pixel.

        `ASTC6x5`: string

            The 6x5 pixels per block variant of the ASTC format. RGBA data at
            4.27 bits per pixel.

        `ASTC6x6`: string

            The 6x6 pixels per block variant of the ASTC format. RGBA data at
            3.56 bits per pixel.

        `ASTC8x5`: string

            The 8x5 pixels per block variant of the ASTC format. RGBA data at
            3.2 bits per pixel.

        `ASTC8x6`: string

            The 8x6 pixels per block variant of the ASTC format. RGBA data at
            2.67 bits per pixel.

        `ASTC8x8`: string

            The 8x8 pixels per block variant of the ASTC format. RGBA data at 2
            bits per pixel.

        `ASTC10x5`: string

            The 10x5 pixels per block variant of the ASTC format. RGBA data at
            2.56 bits per pixel.

        `ASTC10x6`: string

            The 10x6 pixels per block variant of the ASTC format. RGBA data at
            2.13 bits per pixel.

        `ASTC10x8`: string

            The 10x8 pixels per block variant of the ASTC format. RGBA data at
            1.6 bits per pixel.

        `ASTC10x10`: string

            The 10x10 pixels per block variant of the ASTC format. RGBA data at
            1.28 bits per pixel.

        `ASTC12x10`: string

            The 12x10 pixels per block variant of the ASTC format. RGBA data at
            1.07 bits per pixel.

        `ASTC12x12`: string

            The 12x12 pixels per block variant of the ASTC format. RGBA data at
            0.89 bits per pixel.

-------------------------------------------------------------------------------
                                                      *love2d-docs-ImageFormat*
|ImageFormat|

Encoded image formats.

    constants: ~

        `tga`: string

            Targa image format.

        `png`: string

            PNG image format.

        `jpg`: string

            JPG image format.

        `bmp`: string

            BMP image format.

-------------------------------------------------------------------------------
                                                      *love2d-docs-PixelFormat*
|PixelFormat|

Pixel formats for Textures, ImageData, and CompressedImageData.

    constants: ~

        `unknown`: string

            Indicates unknown pixel format, used internally.

        `normal`: string

            Alias for rgba8, or srgba8 if gamma-correct rendering is enabled.

        `hdr`: string

            A format suitable for high dynamic range content - an alias for the
            rgba16f format, normally.

        `r8`: string

            Single-channel (red component) format (8 bpp).

        `rg8`: string

            Two channels (red and green components) with 8 bits per channel (16
            bpp).

        `rgba8`: string

            8 bits per channel (32 bpp) RGBA. Color channel values range from
            0-255 (0-1 in shaders).

        `srgba8`: string

            gamma-correct version of rgba8.

        `r16`: string

            Single-channel (red component) format (16 bpp).

        `rg16`: string

            Two channels (red and green components) with 16 bits per channel
            (32 bpp).

        `rgba16`: string

            16 bits per channel (64 bpp) RGBA. Color channel values range from
            0-65535 (0-1 in shaders).

        `r16f`: string

            Floating point single-channel format (16 bpp). Color values can
            range from [-65504, +65504].

        `rg16f`: string

            Floating point two-channel format with 16 bits per channel (32
            bpp). Color values can range from [-65504, +65504].

        `rgba16f`: string

            Floating point RGBA with 16 bits per channel (64 bpp). Color values
            can range from [-65504, +65504].

        `r32f`: string

            Floating point single-channel format (32 bpp).

        `rg32f`: string

            Floating point two-channel format with 32 bits per channel (64
            bpp).

        `rgba32f`: string

            Floating point RGBA with 32 bits per channel (128 bpp).

        `la8`: string

            Same as rg8, but accessed as (L, L, L, A)

        `rgba4`: string

            4 bits per channel (16 bpp) RGBA.

        `rgb5a1`: string

            RGB with 5 bits each, and a 1-bit alpha channel (16 bpp).

        `rgb565`: string

            RGB with 5, 6, and 5 bits each, respectively (16 bpp). There is no
            alpha channel in this format.

        `rgb10a2`: string

            RGB with 10 bits per channel, and a 2-bit alpha channel (32 bpp).

        `rg11b10f`: string

            Floating point RGB with 11 bits in the red and green channels, and
            10 bits in the blue channel (32 bpp). There is no alpha channel.
            Color values can range from [0, +65024].

        `stencil8`: string

            No depth buffer and 8-bit stencil buffer.

        `depth16`: string

            16-bit depth buffer and no stencil buffer.

        `depth24`: string

            24-bit depth buffer and no stencil buffer.

        `depth32f`: string

            32-bit float depth buffer and no stencil buffer.

        `depth24stencil8`: string

            24-bit depth buffer and 8-bit stencil buffer.

        `depth32fstencil8`: string

            32-bit float depth buffer and 8-bit stencil buffer.

        `DXT1`: string

            The DXT1 format. RGB data at 4 bits per pixel (compared to 32 bits
            for ImageData and regular Images.) Suitable for fully opaque images
            on desktop systems.

        `DXT3`: string

            The DXT3 format. RGBA data at 8 bits per pixel. Smooth variations
            in opacity do not mix well with this format.

        `DXT5`: string

            The DXT5 format. RGBA data at 8 bits per pixel. Recommended for
            images with varying opacity on desktop systems.

        `BC4`: string

            The BC4 format (also known as 3Dc+ or ATI1.) Stores just the red
            channel, at 4 bits per pixel.

        `BC4s`: string

            The signed variant of the BC4 format. Same as above but pixel
            values in the texture are in the range of 1 instead of 1 in
            shaders.

        `BC5`: string

            The BC5 format (also known as 3Dc or ATI2.) Stores red and green
            channels at 8 bits per pixel.

        `BC5s`: string

            The signed variant of the BC5 format.

        `BC6h`: string

            The BC6H format. Stores half-precision floating-point RGB data in
            the range of 65504 at 8 bits per pixel. Suitable for HDR images on
            desktop systems.

        `BC6hs`: string

            The signed variant of the BC6H format. Stores RGB data in the range
            of +65504.

        `BC7`: string

            The BC7 format (also known as BPTC.) Stores RGB or RGBA data at 8
            bits per pixel.

        `ETC1`: string

            The ETC1 format. RGB data at 4 bits per pixel. Suitable for fully
            opaque images on older Android devices.

        `ETC2rgb`: string

            The RGB variant of the ETC2 format. RGB data at 4 bits per pixel.
            Suitable for fully opaque images on newer mobile devices.

        `ETC2rgba`: string

            The RGBA variant of the ETC2 format. RGBA data at 8 bits per pixel.
            Recommended for images with varying opacity on newer mobile
            devices.

        `ETC2rgba1`: string

            The RGBA variant of the ETC2 format where pixels are either fully
            transparent or fully opaque. RGBA data at 4 bits per pixel.

        `EACr`: string

            The single-channel variant of the EAC format. Stores just the red
            channel, at 4 bits per pixel.

        `EACrs`: string

            The signed single-channel variant of the EAC format. Same as above
            but pixel values in the texture are in the range of 1 instead of 1
            in shaders.

        `EACrg`: string

            The two-channel variant of the EAC format. Stores red and green
            channels at 8 bits per pixel.

        `EACrgs`: string

            The signed two-channel variant of the EAC format.

        `PVR1rgb2`: string

            The 2 bit per pixel RGB variant of the PVRTC1 format. Stores RGB
            data at 2 bits per pixel. Textures compressed with PVRTC1 formats
            must be square and power-of-two sized.

        `PVR1rgb4`: string

            The 4 bit per pixel RGB variant of the PVRTC1 format. Stores RGB
            data at 4 bits per pixel.

        `PVR1rgba2`: string

            The 2 bit per pixel RGBA variant of the PVRTC1 format.

        `PVR1rgba4`: string

            The 4 bit per pixel RGBA variant of the PVRTC1 format.

        `ASTC4x4`: string

            The 4x4 pixels per block variant of the ASTC format. RGBA data at 8
            bits per pixel.

        `ASTC5x4`: string

            The 5x4 pixels per block variant of the ASTC format. RGBA data at
            6.4 bits per pixel.

        `ASTC5x5`: string

            The 5x5 pixels per block variant of the ASTC format. RGBA data at
            5.12 bits per pixel.

        `ASTC6x5`: string

            The 6x5 pixels per block variant of the ASTC format. RGBA data at
            4.27 bits per pixel.

        `ASTC6x6`: string

            The 6x6 pixels per block variant of the ASTC format. RGBA data at
            3.56 bits per pixel.

        `ASTC8x5`: string

            The 8x5 pixels per block variant of the ASTC format. RGBA data at
            3.2 bits per pixel.

        `ASTC8x6`: string

            The 8x6 pixels per block variant of the ASTC format. RGBA data at
            2.67 bits per pixel.

        `ASTC8x8`: string

            The 8x8 pixels per block variant of the ASTC format. RGBA data at 2
            bits per pixel.

        `ASTC10x5`: string

            The 10x5 pixels per block variant of the ASTC format. RGBA data at
            2.56 bits per pixel.

        `ASTC10x6`: string

            The 10x6 pixels per block variant of the ASTC format. RGBA data at
            2.13 bits per pixel.

        `ASTC10x8`: string

            The 10x8 pixels per block variant of the ASTC format. RGBA data at
            1.6 bits per pixel.

        `ASTC10x10`: string

            The 10x10 pixels per block variant of the ASTC format. RGBA data at
            1.28 bits per pixel.

        `ASTC12x10`: string

            The 12x10 pixels per block variant of the ASTC format. RGBA data at
            1.07 bits per pixel.

        `ASTC12x12`: string

            The 12x12 pixels per block variant of the ASTC format. RGBA data at
            0.89 bits per pixel.

-------------------------------------------------------------------------------
                                             *love2d-docs-love.image-functions*
The functions of |love.image|:

    |isCompressed|                        |love2d-docs-love.image.isCompressed|
    |newCompressedData|              |love2d-docs-love.image.newCompressedData|
    |newImageData|                        |love2d-docs-love.image.newImageData|

-------------------------------------------------------------------------------
                                          *love2d-docs-love.image.isCompressed*
|love.image.isCompressed|

Determines whether a file can be loaded as CompressedImageData.

Synopses: ~

    1.  `compressed` = |love.image.isCompressed|(`filename`)
    2.  `compressed` = |love.image.isCompressed|(`fileData`)

Variants: ~

    1.  `compressed` = |love.image.isCompressed|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filename of the potentially compressed image file.

        Return: ~
            (`boolean`)
                Whether the file can be loaded as CompressedImageData or not.
    2.  `compressed` = |love.image.isCompressed|(`fileData`)

    2.          See function description

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                A FileData potentially containing a compressed image.

        Return: ~
            (`boolean`)
                Whether the FileData can be loaded as CompressedImageData or
                not.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.image.newCompressedData*
|love.image.newCompressedData|

Create a new CompressedImageData object from a compressed image file. LÖVE
supports several compressed texture formats, enumerated in the
CompressedImageFormat page.

Synopses: ~

    1.  `compressedImageData` =
        |love.image.newCompressedData|(`filename`)
    2.  `compressedImageData` =
        |love.image.newCompressedData|(`fileData`)

Variants: ~

    1.  `compressedImageData` =
        |love.image.newCompressedData|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filename of the compressed image file.

        Return: ~
            (`CompressedImageData`) |love2d-docs-CompressedImageData|
                The new CompressedImageData object.
    2.  `compressedImageData` =
        |love.image.newCompressedData|(`fileData`)

    2.          See function description

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                A FileData containing a compressed image.

        Return: ~
            (`CompressedImageData`) |love2d-docs-CompressedImageData|
                The new CompressedImageData object.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.image.newImageData*
|love.image.newImageData|

Creates a new ImageData object.

Synopses: ~

    1.  `imageData` = |love.image.newImageData|(`width`, `height`)
    2.  `imageData` = |love.image.newImageData|(`width`, `height`,
        `format`, `data`)
    3.  `imageData` = |love.image.newImageData|(`width`, `height`,
        `data`)
    4.  `imageData` = |love.image.newImageData|(`filename`)
    5.  `imageData` = |love.image.newImageData|(`filedata`)

Variants: ~

    1.  `imageData` = |love.image.newImageData|(`width`, `height`)

    1.          See function description

        Parameters: ~
            • {width} (`number`)
                The width of the ImageData.
            • {height} (`number`)
                The height of the ImageData.

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new blank ImageData object. Each pixel's color values,
                (including the alpha values!) will be set to zero.
    2.  `imageData` = |love.image.newImageData|(`width`, `height`,
        `format`, `data`)

    2.          See function description

        Parameters: ~
            • {width} (`number`)
                The width of the ImageData.
            • {height} (`number`)
                The height of the ImageData.
            • {format} (`PixelFormat`) |love2d-docs-PixelFormat| (default: `'rgba8'`)
                The pixel format of the ImageData.
            • {data} (`string`) (default: `nil`)
                Optional raw byte data to load into the ImageData, in the
                format specified by ''format''.

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new ImageData object.
    3.  `imageData` = |love.image.newImageData|(`width`, `height`,
        `data`)

    3.          See function description

        Parameters: ~
            • {width} (`number`)
                The width of the ImageData.
            • {height} (`number`)
                The height of the ImageData.
            • {data} (`string`)
                The data to load into the ImageData (RGBA bytes, left to right
                and top to bottom).

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new ImageData object.
    4.  `imageData` = |love.image.newImageData|(`filename`)

    4.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filename of the image file.

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new ImageData object.
    5.  `imageData` = |love.image.newImageData|(`filedata`)

    5.          See function description

        Parameters: ~
            • {filedata} (`FileData`) |love2d-docs-FileData|
                The encoded file data to decode into image data.

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The new ImageData object.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.image-types*
The types of |love.image|:

    |CompressedImageData|                     |love2d-docs-CompressedImageData|
    |ImageData|                                         |love2d-docs-ImageData|

-------------------------------------------------------------------------------
                                              *love2d-docs-CompressedImageData*
|CompressedImageData|

Represents compressed image data designed to stay compressed in RAM.

CompressedImageData encompasses standard compressed texture formats such as
DXT1, DXT5, and BC5 / 3Dc.

You can't draw CompressedImageData directly to the screen. See Image for that.

                                 *love2d-docs-CompressedImageData-constructors*
    constructors:

        |newCompressedData|                     |love2d-docs-newCompressedData|

                                   *love2d-docs-CompressedImageData-supertypes*
    supertypes:

        |Data|                                               |love2d-docs-Data|
        |Object|                                           |love2d-docs-Object|

                                     *love2d-docs-CompressedImageData-subtypes*
    subtypes:

        None

                                    *love2d-docs-CompressedImageData-functions*
    functions:

        |getDimensions|         |love2d-docs-CompressedImageData:getDimensions|
        |getFormat|                 |love2d-docs-CompressedImageData:getFormat|
        |getHeight|                 |love2d-docs-CompressedImageData:getHeight|
        |getMipmapCount|       |love2d-docs-CompressedImageData:getMipmapCount|
        |getWidth|                   |love2d-docs-CompressedImageData:getWidth|

-------------------------------------------------------------------------------
                                *love2d-docs-CompressedImageData:getDimensions*
|CompressedImageData:getDimensions|

Gets the width and height of the CompressedImageData.

Synopses: ~

    1.  `width`, `height` = |CompressedImageData:getDimensions|()
    2.  `width`, `height` =
        |CompressedImageData:getDimensions|(`level`)

Variants: ~

    1.  `width`, `height` = |CompressedImageData:getDimensions|()

    1.          See function description

        Returns: ~
            1. (`number`)
                The width of the CompressedImageData.

            2. (`number`)
                The height of the CompressedImageData.
    2.  `width`, `height` =
        |CompressedImageData:getDimensions|(`level`)

    2.          See function description

        Parameters: ~
            • {level} (`number`)
                A mipmap level. Must be in the range of
                CompressedImageData:getMipmapCount().

        Returns: ~
            1. (`number`)
                The width of a specific mipmap level of the
                CompressedImageData.

            2. (`number`)
                The height of a specific mipmap level of the
                CompressedImageData.
-------------------------------------------------------------------------------
                                    *love2d-docs-CompressedImageData:getFormat*
|CompressedImageData:getFormat|

Gets the format of the CompressedImageData.

Synopses: ~

        `format` = |CompressedImageData:getFormat|()

Variants: ~

        `format` = |CompressedImageData:getFormat|()

        See function description

        Return: ~
            (`CompressedImageFormat`) |love2d-docs-CompressedImageFormat|
                The format of the CompressedImageData.
-------------------------------------------------------------------------------
                                    *love2d-docs-CompressedImageData:getHeight*
|CompressedImageData:getHeight|

Gets the height of the CompressedImageData.

Synopses: ~

    1.  `height` = |CompressedImageData:getHeight|()
    2.  `height` = |CompressedImageData:getHeight|(`level`)

Variants: ~

    1.  `height` = |CompressedImageData:getHeight|()

    1.          See function description

        Return: ~
            (`number`)
                The height of the CompressedImageData.
    2.  `height` = |CompressedImageData:getHeight|(`level`)

    2.          See function description

        Parameters: ~
            • {level} (`number`)
                A mipmap level. Must be in the range of
                CompressedImageData:getMipmapCount().

        Return: ~
            (`number`)
                The height of a specific mipmap level of the
                CompressedImageData.
-------------------------------------------------------------------------------
                               *love2d-docs-CompressedImageData:getMipmapCount*
|CompressedImageData:getMipmapCount|

Gets the number of mipmap levels in the CompressedImageData. The base mipmap
level (original image) is included in the count.

Synopses: ~

        `mipmaps` = |CompressedImageData:getMipmapCount|()

Variants: ~

        `mipmaps` = |CompressedImageData:getMipmapCount|()

        Mipmap filtering cannot be activated for an Image:setMipmapFilter will
        error. Most tools which can create compressed textures are able to
        automatically generate mipmaps for them in the same file.

        Return: ~
            (`number`)
                The number of mipmap levels stored in the CompressedImageData.
-------------------------------------------------------------------------------
                                     *love2d-docs-CompressedImageData:getWidth*
|CompressedImageData:getWidth|

Gets the width of the CompressedImageData.

Synopses: ~

    1.  `width` = |CompressedImageData:getWidth|()
    2.  `width` = |CompressedImageData:getWidth|(`level`)

Variants: ~

    1.  `width` = |CompressedImageData:getWidth|()

    1.          See function description

        Return: ~
            (`number`)
                The width of the CompressedImageData.
    2.  `width` = |CompressedImageData:getWidth|(`level`)

    2.          See function description

        Parameters: ~
            • {level} (`number`)
                A mipmap level. Must be in the range of
                CompressedImageData:getMipmapCount().

        Return: ~
            (`number`)
                The width of a specific mipmap level of the
                CompressedImageData.

-------------------------------------------------------------------------------
                                                        *love2d-docs-ImageData*
|ImageData|

Raw (decoded) image data.

You can't draw ImageData directly to screen. See Image for that.

                                           *love2d-docs-ImageData-constructors*
    constructors:

        |newImageData|                               |love2d-docs-newImageData|

                                             *love2d-docs-ImageData-supertypes*
    supertypes:

        |Data|                                               |love2d-docs-Data|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-ImageData-subtypes*
    subtypes:

        None

                                              *love2d-docs-ImageData-functions*
    functions:

        |encode|                                 |love2d-docs-ImageData:encode|
        |getDimensions|                   |love2d-docs-ImageData:getDimensions|
        |getHeight|                           |love2d-docs-ImageData:getHeight|
        |getPixel|                             |love2d-docs-ImageData:getPixel|
        |getWidth|                             |love2d-docs-ImageData:getWidth|
        |mapPixel|                             |love2d-docs-ImageData:mapPixel|
        |paste|                                   |love2d-docs-ImageData:paste|
        |setPixel|                             |love2d-docs-ImageData:setPixel|
        |getFormat|                           |love2d-docs-ImageData:getFormat|

-------------------------------------------------------------------------------
                                                 *love2d-docs-ImageData:encode*
|ImageData:encode|

Encodes the ImageData and optionally writes it to the save directory.

Synopses: ~

    1.  `filedata` = |ImageData:encode|(`format`, `filename`)
    2.  |ImageData:encode|(`outFile`)
    3.  |ImageData:encode|(`outFile`, `format`)

Variants: ~

    1.  `filedata` = |ImageData:encode|(`format`, `filename`)

    1.          See function description

        Parameters: ~
            • {format} (`ImageFormat`) |love2d-docs-ImageFormat|
                The format to encode the image as.
            • {filename} (`string`) (default: `nil`)
                The filename to write the file to. If nil, no file will be
                written but the FileData will still be returned.

        Return: ~
            (`FileData`) |love2d-docs-FileData|
                The encoded image as a new FileData object.
    2.  |ImageData:encode|(`outFile`)

    2.          See function description

        Parameters: ~
            • {outFile} (`string`)
                Name of a file to write encoded data to. The format will be
                automatically deduced from the file extension.


    3.  |ImageData:encode|(`outFile`, `format`)

    3.          See function description

        Parameters: ~
            • {outFile} (`string`)
                Name of a file to write encoded data to.
            • {format} (`ImageFormat`) |love2d-docs-ImageFormat|
                The format to encode the image in.


-------------------------------------------------------------------------------
                                          *love2d-docs-ImageData:getDimensions*
|ImageData:getDimensions|

Gets the width and height of the ImageData in pixels.

Synopses: ~

        `width`, `height` = |ImageData:getDimensions|()

Variants: ~

        `width`, `height` = |ImageData:getDimensions|()

        See function description

        Returns: ~
            1. (`number`)
                The width of the ImageData in pixels.

            2. (`number`)
                The height of the ImageData in pixels.
-------------------------------------------------------------------------------
                                              *love2d-docs-ImageData:getHeight*
|ImageData:getHeight|

Gets the height of the ImageData in pixels.

Synopses: ~

        `height` = |ImageData:getHeight|()

Variants: ~

        `height` = |ImageData:getHeight|()

        See function description

        Return: ~
            (`number`)
                The height of the ImageData in pixels.
-------------------------------------------------------------------------------
                                               *love2d-docs-ImageData:getPixel*
|ImageData:getPixel|

Gets the color of a pixel at a specific position in the image.

Valid x and y values start at 0 and go up to image width and height minus 1.
Non-integer values are floored.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        `r`, `g`, `b`, `a` = |ImageData:getPixel|(`x`, `y`)

Variants: ~

        `r`, `g`, `b`, `a` = |ImageData:getPixel|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The position of the pixel on the x-axis.
            • {y} (`number`)
                The position of the pixel on the y-axis.

        Returns: ~
            1. (`number`)
                The red component (0-1).

            2. (`number`)
                The green component (0-1).

            3. (`number`)
                The blue component (0-1).

            4. (`number`)
                The alpha component (0-1).
-------------------------------------------------------------------------------
                                               *love2d-docs-ImageData:getWidth*
|ImageData:getWidth|

Gets the width of the ImageData in pixels.

Synopses: ~

        `width` = |ImageData:getWidth|()

Variants: ~

        `width` = |ImageData:getWidth|()

        See function description

        Return: ~
            (`number`)
                The width of the ImageData in pixels.
-------------------------------------------------------------------------------
                                               *love2d-docs-ImageData:mapPixel*
|ImageData:mapPixel|

Transform an image by applying a function to every pixel.

This function is a higher-order function. It takes another function as a
parameter, and calls it once for each pixel in the ImageData.

The passed function is called with six parameters for each pixel in turn. The
parameters are numbers that represent the x and y coordinates of the pixel and
its red, green, blue and alpha values. The function should return the new red,
green, blue, and alpha values for that pixel.

function pixelFunction(x, y, r, g, b, a)

    -- template for defining your own pixel mapping function

    -- perform computations giving the new values for r, g, b and a

    -- ...

    return r, g, b, a

end

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

        |ImageData:mapPixel|(`pixelFunction`, `x`, `y`, `width`,
        `height`)

Variants: ~

        |ImageData:mapPixel|(`pixelFunction`, `x`, `y`, `width`,
        `height`)

        See function description

        Parameters: ~
            • {pixelFunction} (`function`)
                Function to apply to every pixel.
            • {x} (`number`) (default: `0`)
                The x-axis of the top-left corner of the area within the
                ImageData to apply the function to.
            • {y} (`number`) (default: `0`)
                The y-axis of the top-left corner of the area within the
                ImageData to apply the function to.
            • {width} (`number`) (default: `ImageData:getWidth()`)
                The width of the area within the ImageData to apply the
                function to.
            • {height} (`number`) (default: `ImageData:getHeight()`)
                The height of the area within the ImageData to apply the
                function to.


-------------------------------------------------------------------------------
                                                  *love2d-docs-ImageData:paste*
|ImageData:paste|

Paste into ImageData from another source ImageData.

Synopses: ~

        |ImageData:paste|(`source`, `dx`, `dy`, `sx`, `sy`, `sw`, `sh`)

Variants: ~

        |ImageData:paste|(`source`, `dx`, `dy`, `sx`, `sy`, `sw`, `sh`)

        Note that this function just replaces the contents in the destination
        rectangle; it does not do any alpha blending.

        Parameters: ~
            • {source} (`ImageData`) |love2d-docs-ImageData|
                Source ImageData from which to copy.
            • {dx} (`number`)
                Destination top-left position on x-axis.
            • {dy} (`number`)
                Destination top-left position on y-axis.
            • {sx} (`number`)
                Source top-left position on x-axis.
            • {sy} (`number`)
                Source top-left position on y-axis.
            • {sw} (`number`)
                Source width.
            • {sh} (`number`)
                Source height.


-------------------------------------------------------------------------------
                                               *love2d-docs-ImageData:setPixel*
|ImageData:setPixel|

Sets the color of a pixel at a specific position in the image.

Valid x and y values start at 0 and go up to image width and height minus 1.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

    1.  |ImageData:setPixel|(`x`, `y`, `r`, `g`, `b`, `a`)
    2.  |ImageData:setPixel|(`x`, `y`, `color`)

Variants: ~

    1.  |ImageData:setPixel|(`x`, `y`, `r`, `g`, `b`, `a`)

    1.          See function description

        Parameters: ~
            • {x} (`number`)
                The position of the pixel on the x-axis.
            • {y} (`number`)
                The position of the pixel on the y-axis.
            • {r} (`number`)
                The red component (0-1).
            • {g} (`number`)
                The green component (0-1).
            • {b} (`number`)
                The blue component (0-1).
            • {a} (`number`)
                The alpha component (0-1).


    2.  |ImageData:setPixel|(`x`, `y`, `color`)

    2.          See function description

        Parameters: ~
            • {x} (`number`)
                The position of the pixel on the x-axis.
            • {y} (`number`)
                The position of the pixel on the y-axis.
            • {color} (`table`)
                A numerical indexed table with the red, green, blue and alpha
                values as numbers.


-------------------------------------------------------------------------------
                                              *love2d-docs-ImageData:getFormat*
|ImageData:getFormat|

Gets the pixel format of the ImageData.

Synopses: ~

        `format` = |ImageData:getFormat|()

Variants: ~

        `format` = |ImageData:getFormat|()

        See function description

        Return: ~
            (`PixelFormat`) |love2d-docs-PixelFormat|
                The pixel format the ImageData was created with.

===============================================================================
                                                    *love2d-docs-love.joystick*
|love.joystick|

Provides an interface to the user's joystick.

    |callbacks|                           |love2d-docs-love.joystick-callbacks|
    |enums|                                   |love2d-docs-love.joystick-enums|
    |functions|                           |love2d-docs-love.joystick-functions|
    |types|                                   |love2d-docs-love.joystick-types|

-------------------------------------------------------------------------------
                                          *love2d-docs-love.joystick-callbacks*
The callbacks of |love.joystick|:

    None

-------------------------------------------------------------------------------
                                              *love2d-docs-love.joystick-enums*
The enums of |love.joystick|:

    |GamepadAxis|                                     |love2d-docs-GamepadAxis|
    |GamepadButton|                                 |love2d-docs-GamepadButton|
    |JoystickHat|                                     |love2d-docs-JoystickHat|
    |JoystickInputType|                         |love2d-docs-JoystickInputType|

-------------------------------------------------------------------------------
                                                      *love2d-docs-GamepadAxis*
|GamepadAxis|

Virtual gamepad axes.

    constants: ~

        `leftx`: string

            The x-axis of the left thumbstick.

        `lefty`: string

            The y-axis of the left thumbstick.

        `rightx`: string

            The x-axis of the right thumbstick.

        `righty`: string

            The y-axis of the right thumbstick.

        `triggerleft`: string

            Left analog trigger.

        `triggerright`: string

            Right analog trigger.

-------------------------------------------------------------------------------
                                                    *love2d-docs-GamepadButton*
|GamepadButton|

Virtual gamepad buttons.

    constants: ~

        `a`: string

            Bottom face button (A).

        `b`: string

            Right face button (B).

        `x`: string

            Left face button (X).

        `y`: string

            Top face button (Y).

        `back`: string

            Back button.

        `guide`: string

            Guide button.

        `start`: string

            Start button.

        `leftstick`: string

            Left stick click button.

        `rightstick`: string

            Right stick click button.

        `leftshoulder`: string

            Left bumper.

        `rightshoulder`: string

            Right bumper.

        `dpup`: string

            D-pad up.

        `dpdown`: string

            D-pad down.

        `dpleft`: string

            D-pad left.

        `dpright`: string

            D-pad right.

-------------------------------------------------------------------------------
                                                      *love2d-docs-JoystickHat*
|JoystickHat|

Joystick hat positions.

    constants: ~

        `c`: string

            Centered

        `d`: string

            Down

        `l`: string

            Left

        `ld`: string

            Left+Down

        `lu`: string

            Left+Up

        `r`: string

            Right

        `rd`: string

            Right+Down

        `ru`: string

            Right+Up

        `u`: string

            Up

-------------------------------------------------------------------------------
                                                *love2d-docs-JoystickInputType*
|JoystickInputType|

Types of Joystick inputs.

    constants: ~

        `axis`: string

            Analog axis.

        `button`: string

            Button.

        `hat`: string

            8-direction hat value.

-------------------------------------------------------------------------------
                                          *love2d-docs-love.joystick-functions*
The functions of |love.joystick|:

    |getGamepadMappingString|  |love2d-docs-love.joystick.getGamepadMappingString|
    |getJoystickCount|             |love2d-docs-love.joystick.getJoystickCount|
    |getJoysticks|                     |love2d-docs-love.joystick.getJoysticks|
    |loadGamepadMappings|       |love2d-docs-love.joystick.loadGamepadMappings|
    |saveGamepadMappings|       |love2d-docs-love.joystick.saveGamepadMappings|
    |setGamepadMapping|           |love2d-docs-love.joystick.setGamepadMapping|

-------------------------------------------------------------------------------
                            *love2d-docs-love.joystick.getGamepadMappingString*
|love.joystick.getGamepadMappingString|

Gets the full gamepad mapping string of the Joysticks which have the given
GUID, or nil if the GUID isn't recognized as a gamepad.

The mapping string contains binding information used to map the Joystick's
buttons an axes to the standard gamepad layout, and can be used later with
love.joystick.loadGamepadMappings.

Synopses: ~

        `mappingstring` =
        |love.joystick.getGamepadMappingString|(`guid`)

Variants: ~

        `mappingstring` =
        |love.joystick.getGamepadMappingString|(`guid`)

        See function description

        Parameters: ~
            • {guid} (`string`)
                The GUID value to get the mapping string for.

        Return: ~
            (`string`)
                A string containing the Joystick's gamepad mappings, or nil if
                the GUID is not recognized as a gamepad.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.joystick.getJoystickCount*
|love.joystick.getJoystickCount|

Gets the number of connected joysticks.

Synopses: ~

        `joystickcount` = |love.joystick.getJoystickCount|()

Variants: ~

        `joystickcount` = |love.joystick.getJoystickCount|()

        See function description

        Return: ~
            (`number`)
                The number of connected joysticks.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.joystick.getJoysticks*
|love.joystick.getJoysticks|

Gets a list of connected Joysticks.

Synopses: ~

        `joysticks` = |love.joystick.getJoysticks|()

Variants: ~

        `joysticks` = |love.joystick.getJoysticks|()

        See function description

        Return: ~
            (`table`)
                The list of currently connected Joysticks.
-------------------------------------------------------------------------------
                                *love2d-docs-love.joystick.loadGamepadMappings*
|love.joystick.loadGamepadMappings|

Loads a gamepad mappings string or file created with
love.joystick.saveGamepadMappings.

It also recognizes any SDL gamecontroller mapping string, such as those created
with Steam's Big Picture controller configure interface, or this nice database.
If a new mapping is loaded for an already known controller GUID, the later
version will overwrite the one currently loaded.

Synopses: ~

    1.  |love.joystick.loadGamepadMappings|(`filename`)
    2.  |love.joystick.loadGamepadMappings|(`mappings`)

Variants: ~

    1.  |love.joystick.loadGamepadMappings|(`filename`)

    1.          Loads a gamepad mappings string from a file.

        Parameters: ~
            • {filename} (`string`)
                The filename to load the mappings string from.


    2.  |love.joystick.loadGamepadMappings|(`mappings`)

    2.          Loads a gamepad mappings string directly.

        Parameters: ~
            • {mappings} (`string`)
                The mappings string to load.


-------------------------------------------------------------------------------
                                *love2d-docs-love.joystick.saveGamepadMappings*
|love.joystick.saveGamepadMappings|

Saves the virtual gamepad mappings of all recognized as gamepads and have
either been recently used or their gamepad bindings have been modified.

The mappings are stored as a string for use with
love.joystick.loadGamepadMappings.

Synopses: ~

    1.  `mappings` = |love.joystick.saveGamepadMappings|(`filename`)
    2.  `mappings` = |love.joystick.saveGamepadMappings|()

Variants: ~

    1.  `mappings` = |love.joystick.saveGamepadMappings|(`filename`)

    1.          Saves the gamepad mappings of all relevant joysticks to a file.

        Parameters: ~
            • {filename} (`string`)
                The filename to save the mappings string to.

        Return: ~
            (`string`)
                The mappings string that was written to the file.
    2.  `mappings` = |love.joystick.saveGamepadMappings|()

    2.          Returns the mappings string without writing to a file.

        Return: ~
            (`string`)
                The mappings string.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.joystick.setGamepadMapping*
|love.joystick.setGamepadMapping|

Binds a virtual gamepad input to a button, axis or hat for all Joysticks of a
certain type. For example, if this function is used with a GUID returned by a
Dualshock 3 controller in OS X, the binding will affect Joystick:getGamepadAxis
and Joystick:isGamepadDown for ''all'' Dualshock 3 controllers used with the
game when run in OS X.

LÖVE includes built-in gamepad bindings for many common controllers. This
function lets you change the bindings or add new ones for types of Joysticks
which aren't recognized as gamepads by default.

The virtual gamepad buttons and axes are designed around the Xbox 360
controller layout.

Synopses: ~

    1.  `success` = |love.joystick.setGamepadMapping|(`guid`, `button`,
        `inputtype`, `inputindex`, `hatdir`)
    2.  `success` = |love.joystick.setGamepadMapping|(`guid`, `axis`,
        `inputtype`, `inputindex`, `hatdir`)

Variants: ~

    1.  `success` = |love.joystick.setGamepadMapping|(`guid`, `button`,
        `inputtype`, `inputindex`, `hatdir`)

    1.          See function description

        Parameters: ~
            • {guid} (`string`)
                The OS-dependent GUID for the type of Joystick the binding will
                affect.
            • {button} (`GamepadButton`) |love2d-docs-GamepadButton|
                The virtual gamepad button to bind.
            • {inputtype} (`JoystickInputType`) |love2d-docs-JoystickInputType|
                The type of input to bind the virtual gamepad button to.
            • {inputindex} (`number`)
                The index of the axis, button, or hat to bind the virtual
                gamepad button to.
            • {hatdir} (`JoystickHat`) |love2d-docs-JoystickHat| (default: `nil`)
                The direction of the hat, if the virtual gamepad button will be
                bound to a hat. nil otherwise.

        Return: ~
            (`boolean`)
                Whether the virtual gamepad button was successfully bound.
    2.  `success` = |love.joystick.setGamepadMapping|(`guid`, `axis`,
        `inputtype`, `inputindex`, `hatdir`)

    2.          The physical locations for the bound gamepad axes and buttons should
        correspond as closely as possible to the layout of a standard Xbox 360
        controller.

        Parameters: ~
            • {guid} (`string`)
                The OS-dependent GUID for the type of Joystick the binding will
                affect.
            • {axis} (`GamepadAxis`) |love2d-docs-GamepadAxis|
                The virtual gamepad axis to bind.
            • {inputtype} (`JoystickInputType`) |love2d-docs-JoystickInputType|
                The type of input to bind the virtual gamepad axis to.
            • {inputindex} (`number`)
                The index of the axis, button, or hat to bind the virtual
                gamepad axis to.
            • {hatdir} (`JoystickHat`) |love2d-docs-JoystickHat| (default: `nil`)
                The direction of the hat, if the virtual gamepad axis will be
                bound to a hat. nil otherwise.

        Return: ~
            (`boolean`)
                Whether the virtual gamepad axis was successfully bound.
-------------------------------------------------------------------------------
                                              *love2d-docs-love.joystick-types*
The types of |love.joystick|:

    |Joystick|                                           |love2d-docs-Joystick|

-------------------------------------------------------------------------------
                                                         *love2d-docs-Joystick*
|Joystick|

Represents a physical joystick.

                                            *love2d-docs-Joystick-constructors*
    constructors:

        None

                                              *love2d-docs-Joystick-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                *love2d-docs-Joystick-subtypes*
    subtypes:

        None

                                               *love2d-docs-Joystick-functions*
    functions:

        |getAxes|                                |love2d-docs-Joystick:getAxes|
        |getAxis|                                |love2d-docs-Joystick:getAxis|
        |getAxisCount|                      |love2d-docs-Joystick:getAxisCount|
        |getButtonCount|                  |love2d-docs-Joystick:getButtonCount|
        |getDeviceInfo|                    |love2d-docs-Joystick:getDeviceInfo|
        |getGUID|                                |love2d-docs-Joystick:getGUID|
        |getGamepadAxis|                  |love2d-docs-Joystick:getGamepadAxis|
        |getGamepadMapping|            |love2d-docs-Joystick:getGamepadMapping|
        |getGamepadMappingString|  |love2d-docs-Joystick:getGamepadMappingString|
        |getHat|                                  |love2d-docs-Joystick:getHat|
        |getHatCount|                        |love2d-docs-Joystick:getHatCount|
        |getID|                                    |love2d-docs-Joystick:getID|
        |getName|                                |love2d-docs-Joystick:getName|
        |getVibration|                      |love2d-docs-Joystick:getVibration|
        |isConnected|                        |love2d-docs-Joystick:isConnected|
        |isDown|                                  |love2d-docs-Joystick:isDown|
        |isGamepad|                            |love2d-docs-Joystick:isGamepad|
        |isGamepadDown|                    |love2d-docs-Joystick:isGamepadDown|
        |isVibrationSupported|      |love2d-docs-Joystick:isVibrationSupported|
        |setVibration|                      |love2d-docs-Joystick:setVibration|

-------------------------------------------------------------------------------
                                                 *love2d-docs-Joystick:getAxes*
|Joystick:getAxes|

Gets the direction of each axis.

Synopses: ~

        `axisDir1`, `axisDir2`, `axisDirN` = |Joystick:getAxes|()

Variants: ~

        `axisDir1`, `axisDir2`, `axisDirN` = |Joystick:getAxes|()

        See function description

        Returns: ~
            1. (`number`)
                Direction of axis1.

            2. (`number`)
                Direction of axis2.

            3. (`number`)
                Direction of axisN.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Joystick:getAxis*
|Joystick:getAxis|

Gets the direction of an axis.

Synopses: ~

        `direction` = |Joystick:getAxis|(`axis`)

Variants: ~

        `direction` = |Joystick:getAxis|(`axis`)

        See function description

        Parameters: ~
            • {axis} (`number`)
                The index of the axis to be checked.

        Return: ~
            (`number`)
                Current value of the axis.
-------------------------------------------------------------------------------
                                            *love2d-docs-Joystick:getAxisCount*
|Joystick:getAxisCount|

Gets the number of axes on the joystick.

Synopses: ~

        `axes` = |Joystick:getAxisCount|()

Variants: ~

        `axes` = |Joystick:getAxisCount|()

        See function description

        Return: ~
            (`number`)
                The number of axes available.
-------------------------------------------------------------------------------
                                          *love2d-docs-Joystick:getButtonCount*
|Joystick:getButtonCount|

Gets the number of buttons on the joystick.

Synopses: ~

        `buttons` = |Joystick:getButtonCount|()

Variants: ~

        `buttons` = |Joystick:getButtonCount|()

        See function description

        Return: ~
            (`number`)
                The number of buttons available.
-------------------------------------------------------------------------------
                                           *love2d-docs-Joystick:getDeviceInfo*
|Joystick:getDeviceInfo|

Gets the USB vendor ID, product ID, and product version numbers of joystick
which consistent across operating systems.

Can be used to show different icons, etc. for different gamepads.

Synopses: ~

        `vendorID`, `productID`, `productVersion` =
        |Joystick:getDeviceInfo|()

Variants: ~

        `vendorID`, `productID`, `productVersion` =
        |Joystick:getDeviceInfo|()

        Some Linux distribution may not ship with SDL 2.0.6 or later, in which
        case this function will returns 0 for all the three values.

        Returns: ~
            1. (`number`)
                The USB vendor ID of the joystick.

            2. (`number`)
                The USB product ID of the joystick.

            3. (`number`)
                The product version of the joystick.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Joystick:getGUID*
|Joystick:getGUID|

Gets a stable GUID unique to the type of the physical joystick which does not
change over time. For example, all Sony Dualshock 3 controllers in OS X have
the same GUID. The value is platform-dependent.

Synopses: ~

        `guid` = |Joystick:getGUID|()

Variants: ~

        `guid` = |Joystick:getGUID|()

        See function description

        Return: ~
            (`string`)
                The Joystick type's OS-dependent unique identifier.
-------------------------------------------------------------------------------
                                          *love2d-docs-Joystick:getGamepadAxis*
|Joystick:getGamepadAxis|

Gets the direction of a virtual gamepad axis. If the Joystick isn't recognized
as a gamepad or isn't connected, this function will always return 0.

Synopses: ~

        `direction` = |Joystick:getGamepadAxis|(`axis`)

Variants: ~

        `direction` = |Joystick:getGamepadAxis|(`axis`)

        See function description

        Parameters: ~
            • {axis} (`GamepadAxis`) |love2d-docs-GamepadAxis|
                The virtual axis to be checked.

        Return: ~
            (`number`)
                Current value of the axis.
-------------------------------------------------------------------------------
                                       *love2d-docs-Joystick:getGamepadMapping*
|Joystick:getGamepadMapping|

Gets the button, axis or hat that a virtual gamepad input is bound to.

Synopses: ~

    1.  `inputtype`, `inputindex`, `hatdirection` =
        |Joystick:getGamepadMapping|(`axis`)
    2.  `inputtype`, `inputindex`, `hatdirection` =
        |Joystick:getGamepadMapping|(`button`)

Variants: ~

    1.  `inputtype`, `inputindex`, `hatdirection` =
        |Joystick:getGamepadMapping|(`axis`)

    1.          Returns nil if the Joystick isn't recognized as a gamepad or the
        virtual gamepad axis is not bound to a Joystick input.

        Parameters: ~
            • {axis} (`GamepadAxis`) |love2d-docs-GamepadAxis|
                The virtual gamepad axis to get the binding for.

        Returns: ~
            1. (`JoystickInputType`) |love2d-docs-JoystickInputType|
                The type of input the virtual gamepad axis is bound to.

            2. (`number`)
                The index of the Joystick's button, axis or hat that the
                virtual gamepad axis is bound to.

            3. (`JoystickHat`) |love2d-docs-JoystickHat|
                The direction of the hat, if the virtual gamepad axis is bound
                to a hat. nil otherwise.
    2.  `inputtype`, `inputindex`, `hatdirection` =
        |Joystick:getGamepadMapping|(`button`)

    2.          The physical locations for the virtual gamepad axes and buttons
        correspond as closely as possible to the layout of a standard Xbox 360
        controller.

        Parameters: ~
            • {button} (`GamepadButton`) |love2d-docs-GamepadButton|
                The virtual gamepad button to get the binding for.

        Returns: ~
            1. (`JoystickInputType`) |love2d-docs-JoystickInputType|
                The type of input the virtual gamepad button is bound to.

            2. (`number`)
                The index of the Joystick's button, axis or hat that the
                virtual gamepad button is bound to.

            3. (`JoystickHat`) |love2d-docs-JoystickHat|
                The direction of the hat, if the virtual gamepad button is
                bound to a hat. nil otherwise.
-------------------------------------------------------------------------------
                                 *love2d-docs-Joystick:getGamepadMappingString*
|Joystick:getGamepadMappingString|

Gets the full gamepad mapping string of this Joystick, or nil if it's not
recognized as a gamepad.

The mapping string contains binding information used to map the Joystick's
buttons an axes to the standard gamepad layout, and can be used later with
love.joystick.loadGamepadMappings.

Synopses: ~

        `mappingstring` = |Joystick:getGamepadMappingString|()

Variants: ~

        `mappingstring` = |Joystick:getGamepadMappingString|()

        See function description

        Return: ~
            (`string`)
                A string containing the Joystick's gamepad mappings, or nil if
                the Joystick is not recognized as a gamepad.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Joystick:getHat*
|Joystick:getHat|

Gets the direction of the Joystick's hat.

Synopses: ~

        `direction` = |Joystick:getHat|(`hat`)

Variants: ~

        `direction` = |Joystick:getHat|(`hat`)

        See function description

        Parameters: ~
            • {hat} (`number`)
                The index of the hat to be checked.

        Return: ~
            (`JoystickHat`) |love2d-docs-JoystickHat|
                The direction the hat is pushed.
-------------------------------------------------------------------------------
                                             *love2d-docs-Joystick:getHatCount*
|Joystick:getHatCount|

Gets the number of hats on the joystick.

Synopses: ~

        `hats` = |Joystick:getHatCount|()

Variants: ~

        `hats` = |Joystick:getHatCount|()

        See function description

        Return: ~
            (`number`)
                How many hats the joystick has.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Joystick:getID*
|Joystick:getID|

Gets the joystick's unique identifier. The identifier will remain the same for
the life of the game, even when the Joystick is disconnected and reconnected,
but it '''will''' change when the game is re-launched.

Synopses: ~

        `id`, `instanceid` = |Joystick:getID|()

Variants: ~

        `id`, `instanceid` = |Joystick:getID|()

        See function description

        Returns: ~
            1. (`number`)
                The Joystick's unique identifier. Remains the same as long as
                the game is running.

            2. (`number`)
                Unique instance identifier. Changes every time the Joystick is
                reconnected. nil if the Joystick is not connected.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Joystick:getName*
|Joystick:getName|

Gets the name of the joystick.

Synopses: ~

        `name` = |Joystick:getName|()

Variants: ~

        `name` = |Joystick:getName|()

        See function description

        Return: ~
            (`string`)
                The name of the joystick.
-------------------------------------------------------------------------------
                                            *love2d-docs-Joystick:getVibration*
|Joystick:getVibration|

Gets the current vibration motor strengths on a Joystick with rumble support.

Synopses: ~

        `left`, `right` = |Joystick:getVibration|()

Variants: ~

        `left`, `right` = |Joystick:getVibration|()

        See function description

        Returns: ~
            1. (`number`)
                Current strength of the left vibration motor on the Joystick.

            2. (`number`)
                Current strength of the right vibration motor on the Joystick.
-------------------------------------------------------------------------------
                                             *love2d-docs-Joystick:isConnected*
|Joystick:isConnected|

Gets whether the Joystick is connected.

Synopses: ~

        `connected` = |Joystick:isConnected|()

Variants: ~

        `connected` = |Joystick:isConnected|()

        See function description

        Return: ~
            (`boolean`)
                True if the Joystick is currently connected, false otherwise.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Joystick:isDown*
|Joystick:isDown|

Checks if a button on the Joystick is pressed.

LÖVE 0.9.0 had a bug which required the button indices passed to
Joystick:isDown to be 0-based instead of 1-based, for example button 1 would be
0 for this function. It was fixed in 0.9.1.

Synopses: ~

        `anyDown` = |Joystick:isDown|(`buttonN`)

Variants: ~

        `anyDown` = |Joystick:isDown|(`buttonN`)

        See function description

        Parameters: ~
            • {buttonN} (`number`)
                The index of a button to check.

        Return: ~
            (`boolean`)
                True if any supplied button is down, false if not.
-------------------------------------------------------------------------------
                                               *love2d-docs-Joystick:isGamepad*
|Joystick:isGamepad|

Gets whether the Joystick is recognized as a gamepad. If this is the case, the
Joystick's buttons and axes can be used in a standardized manner across
different operating systems and joystick models via Joystick:getGamepadAxis,
Joystick:isGamepadDown, love.gamepadpressed, and related functions.

LÖVE automatically recognizes most popular controllers with a similar layout
to the Xbox 360 controller as gamepads, but you can add more with
love.joystick.setGamepadMapping.

Synopses: ~

        `isgamepad` = |Joystick:isGamepad|()

Variants: ~

        `isgamepad` = |Joystick:isGamepad|()

        If the Joystick is recognized as a gamepad, the physical locations for
        the virtual gamepad axes and buttons correspond as closely as possible
        to the layout of a standard Xbox 360 controller.

        Return: ~
            (`boolean`)
                True if the Joystick is recognized as a gamepad, false
                otherwise.
-------------------------------------------------------------------------------
                                           *love2d-docs-Joystick:isGamepadDown*
|Joystick:isGamepadDown|

Checks if a virtual gamepad button on the Joystick is pressed. If the Joystick
is not recognized as a Gamepad or isn't connected, then this function will
always return false.

Synopses: ~

        `anyDown` = |Joystick:isGamepadDown|(`buttonN`)

Variants: ~

        `anyDown` = |Joystick:isGamepadDown|(`buttonN`)

        See function description

        Parameters: ~
            • {buttonN} (`GamepadButton`) |love2d-docs-GamepadButton|
                The gamepad button to check.

        Return: ~
            (`boolean`)
                True if any supplied button is down, false if not.
-------------------------------------------------------------------------------
                                    *love2d-docs-Joystick:isVibrationSupported*
|Joystick:isVibrationSupported|

Gets whether the Joystick supports vibration.

Synopses: ~

        `supported` = |Joystick:isVibrationSupported|()

Variants: ~

        `supported` = |Joystick:isVibrationSupported|()

        The very first call to this function may take more time than expected
        because SDL's Haptic / Force Feedback subsystem needs to be
        initialized.

        Return: ~
            (`boolean`)
                True if rumble / force feedback vibration is supported on this
                Joystick, false if not.
-------------------------------------------------------------------------------
                                            *love2d-docs-Joystick:setVibration*
|Joystick:setVibration|

Sets the vibration motor speeds on a Joystick with rumble support. Most common
gamepads have this functionality, although not all drivers give proper support.
Use Joystick:isVibrationSupported to check.

Synopses: ~

    1.  `success` = |Joystick:setVibration|(`left`, `right`)
    2.  `success` = |Joystick:setVibration|()
    3.  `success` = |Joystick:setVibration|(`left`, `right`,
        `duration`)

Variants: ~

    1.  `success` = |Joystick:setVibration|(`left`, `right`)

    1.          See function description

        Parameters: ~
            • {left} (`number`)
                Strength of the left vibration motor on the Joystick. Must be
                in the range of 1.
            • {right} (`number`)
                Strength of the right vibration motor on the Joystick. Must be
                in the range of 1.

        Return: ~
            (`boolean`)
                True if the vibration was successfully applied, false if not.
    2.  `success` = |Joystick:setVibration|()

    2.          Disables vibration.

        Return: ~
            (`boolean`)
                True if the vibration was successfully disabled, false if not.
    3.  `success` = |Joystick:setVibration|(`left`, `right`,
        `duration`)

    3.          If the Joystick only has a single vibration motor, it will still work
        but it will use the largest value of the left and right parameters.

        The Xbox 360 controller on Mac OS X only has support for vibration if a
        modified version of the Tattiebogle driver is used.

        The very first call to this function may take more time than expected
        because SDL's Haptic / Force Feedback subsystem needs to be
        initialized.

        Parameters: ~
            • {left} (`number`)
                Strength of the left vibration motor on the Joystick. Must be
                in the range of 1.
            • {right} (`number`)
                Strength of the right vibration motor on the Joystick. Must be
                in the range of 1.
            • {duration} (`number`) (default: `-1`)
                The duration of the vibration in seconds. A negative value
                means infinite duration.

        Return: ~
            (`boolean`)
                True if the vibration was successfully applied, false if not.

===============================================================================
                                                    *love2d-docs-love.keyboard*
|love.keyboard|

Provides an interface to the user's keyboard.

    |callbacks|                           |love2d-docs-love.keyboard-callbacks|
    |enums|                                   |love2d-docs-love.keyboard-enums|
    |functions|                           |love2d-docs-love.keyboard-functions|
    |types|                                   |love2d-docs-love.keyboard-types|

-------------------------------------------------------------------------------
                                          *love2d-docs-love.keyboard-callbacks*
The callbacks of |love.keyboard|:

    None

-------------------------------------------------------------------------------
                                              *love2d-docs-love.keyboard-enums*
The enums of |love.keyboard|:

    |KeyConstant|                                     |love2d-docs-KeyConstant|
    |Scancode|                                           |love2d-docs-Scancode|

-------------------------------------------------------------------------------
                                                      *love2d-docs-KeyConstant*
|KeyConstant|

All the keys you can press. Note that some keys may not be available on your
keyboard or system.

    constants: ~

        `a`: string

            The A key

        `b`: string

            The B key

        `c`: string

            The C key

        `d`: string

            The D key

        `e`: string

            The E key

        `f`: string

            The F key

        `g`: string

            The G key

        `h`: string

            The H key

        `i`: string

            The I key

        `j`: string

            The J key

        `k`: string

            The K key

        `l`: string

            The L key

        `m`: string

            The M key

        `n`: string

            The N key

        `o`: string

            The O key

        `p`: string

            The P key

        `q`: string

            The Q key

        `r`: string

            The R key

        `s`: string

            The S key

        `t`: string

            The T key

        `u`: string

            The U key

        `v`: string

            The V key

        `w`: string

            The W key

        `x`: string

            The X key

        `y`: string

            The Y key

        `z`: string

            The Z key

        `0`: string

            The zero key

        `1`: string

            The one key

        `2`: string

            The two key

        `3`: string

            The three key

        `4`: string

            The four key

        `5`: string

            The five key

        `6`: string

            The six key

        `7`: string

            The seven key

        `8`: string

            The eight key

        `9`: string

            The nine key

        `space`: string

            Space key

        `!`: string

            Exclamation mark key

        `"`: string

            Double quote key

        `#`: string

            Hash key

        `$`: string

            Dollar key

        `&`: string

            Ampersand key

        `'`: string

            Single quote key

        `(`: string

            Left parenthesis key

        `)`: string

            Right parenthesis key

        `*`: string

            Asterisk key

        `+`: string

            Plus key

        `,`: string

            Comma key

        `-`: string

            Hyphen-minus key

        `.`: string

            Full stop key

        `/`: string

            Slash key

        `:`: string

            Colon key

        `;`: string

            Semicolon key

        `<`: string

            Less-than key

        `=`: string

            Equal key

        `>`: string

            Greater-than key

        `?`: string

            Question mark key

        `@`: string

            At sign key

        `[`: string

            Left square bracket key

        `\`: string

            Backslash key

        `]`: string

            Right square bracket key

        `^`: string

            Caret key

        `_`: string

            Underscore key

        ```: string

            Grave accent key

        `kp0`: string

            The numpad zero key

        `kp1`: string

            The numpad one key

        `kp2`: string

            The numpad two key

        `kp3`: string

            The numpad three key

        `kp4`: string

            The numpad four key

        `kp5`: string

            The numpad five key

        `kp6`: string

            The numpad six key

        `kp7`: string

            The numpad seven key

        `kp8`: string

            The numpad eight key

        `kp9`: string

            The numpad nine key

        `kp.`: string

            The numpad decimal point key

        `kp/`: string

            The numpad division key

        `kp*`: string

            The numpad multiplication key

        `kp-`: string

            The numpad substraction key

        `kp+`: string

            The numpad addition key

        `kpenter`: string

            The numpad enter key

        `kp=`: string

            The numpad equals key

        `up`: string

            Up cursor key

        `down`: string

            Down cursor key

        `right`: string

            Right cursor key

        `left`: string

            Left cursor key

        `home`: string

            Home key

        `end`: string

            End key

        `pageup`: string

            Page up key

        `pagedown`: string

            Page down key

        `insert`: string

            Insert key

        `backspace`: string

            Backspace key

        `tab`: string

            Tab key

        `clear`: string

            Clear key

        `return`: string

            Return key

        `delete`: string

            Delete key

        `f1`: string

            The 1st function key

        `f2`: string

            The 2nd function key

        `f3`: string

            The 3rd function key

        `f4`: string

            The 4th function key

        `f5`: string

            The 5th function key

        `f6`: string

            The 6th function key

        `f7`: string

            The 7th function key

        `f8`: string

            The 8th function key

        `f9`: string

            The 9th function key

        `f10`: string

            The 10th function key

        `f11`: string

            The 11th function key

        `f12`: string

            The 12th function key

        `f13`: string

            The 13th function key

        `f14`: string

            The 14th function key

        `f15`: string

            The 15th function key

        `numlock`: string

            Num-lock key

        `capslock`: string

            Caps-lock key

        `scrollock`: string

            Scroll-lock key

        `rshift`: string

            Right shift key

        `lshift`: string

            Left shift key

        `rctrl`: string

            Right control key

        `lctrl`: string

            Left control key

        `ralt`: string

            Right alt key

        `lalt`: string

            Left alt key

        `rmeta`: string

            Right meta key

        `lmeta`: string

            Left meta key

        `lsuper`: string

            Left super key

        `rsuper`: string

            Right super key

        `mode`: string

            Mode key

        `compose`: string

            Compose key

        `pause`: string

            Pause key

        `escape`: string

            Escape key

        `help`: string

            Help key

        `print`: string

            Print key

        `sysreq`: string

            System request key

        `break`: string

            Break key

        `menu`: string

            Menu key

        `power`: string

            Power key

        `euro`: string

            Euro (&euro;) key

        `undo`: string

            Undo key

        `www`: string

            WWW key

        `mail`: string

            Mail key

        `calculator`: string

            Calculator key

        `appsearch`: string

            Application search key

        `apphome`: string

            Application home key

        `appback`: string

            Application back key

        `appforward`: string

            Application forward key

        `apprefresh`: string

            Application refresh key

        `appbookmarks`: string

            Application bookmarks key

-------------------------------------------------------------------------------
                                                         *love2d-docs-Scancode*
|Scancode|

Keyboard scancodes.

Scancodes are keyboard layout-independent, so the scancode "w" will be
generated if the key in the same place as the "w" key on an American QWERTY
keyboard is pressed, no matter what the key is labelled or what the user's
operating system settings are.

Using scancodes, rather than keycodes, is useful because keyboards with layouts
differing from the US/UK layout(s) might have keys that generate 'unknown'
keycodes, but the scancodes will still be detected. This however would
necessitate having a list for each keyboard layout one would choose to support.

One could use textinput or textedited instead, but those only give back the end
result of keys used, i.e. you can't get modifiers on their own from it, only
the final symbols that were generated.

    constants: ~

        `a`: string

            The 'A' key on an American layout.

        `b`: string

            The 'B' key on an American layout.

        `c`: string

            The 'C' key on an American layout.

        `d`: string

            The 'D' key on an American layout.

        `e`: string

            The 'E' key on an American layout.

        `f`: string

            The 'F' key on an American layout.

        `g`: string

            The 'G' key on an American layout.

        `h`: string

            The 'H' key on an American layout.

        `i`: string

            The 'I' key on an American layout.

        `j`: string

            The 'J' key on an American layout.

        `k`: string

            The 'K' key on an American layout.

        `l`: string

            The 'L' key on an American layout.

        `m`: string

            The 'M' key on an American layout.

        `n`: string

            The 'N' key on an American layout.

        `o`: string

            The 'O' key on an American layout.

        `p`: string

            The 'P' key on an American layout.

        `q`: string

            The 'Q' key on an American layout.

        `r`: string

            The 'R' key on an American layout.

        `s`: string

            The 'S' key on an American layout.

        `t`: string

            The 'T' key on an American layout.

        `u`: string

            The 'U' key on an American layout.

        `v`: string

            The 'V' key on an American layout.

        `w`: string

            The 'W' key on an American layout.

        `x`: string

            The 'X' key on an American layout.

        `y`: string

            The 'Y' key on an American layout.

        `z`: string

            The 'Z' key on an American layout.

        `1`: string

            The '1' key on an American layout.

        `2`: string

            The '2' key on an American layout.

        `3`: string

            The '3' key on an American layout.

        `4`: string

            The '4' key on an American layout.

        `5`: string

            The '5' key on an American layout.

        `6`: string

            The '6' key on an American layout.

        `7`: string

            The '7' key on an American layout.

        `8`: string

            The '8' key on an American layout.

        `9`: string

            The '9' key on an American layout.

        `0`: string

            The '0' key on an American layout.

        `return`: string

            The 'return' / 'enter' key on an American layout.

        `escape`: string

            The 'escape' key on an American layout.

        `backspace`: string

            The 'backspace' key on an American layout.

        `tab`: string

            The 'tab' key on an American layout.

        `space`: string

            The spacebar on an American layout.

        `-`: string

            The minus key on an American layout.

        `=`: string

            The equals key on an American layout.

        `[`: string

            The left-bracket key on an American layout.

        `]`: string

            The right-bracket key on an American layout.

        `\`: string

            The backslash key on an American layout.

        `nonus#`: string

            The non-U.S. hash scancode.

        `;`: string

            The semicolon key on an American layout.

        `'`: string

            The apostrophe key on an American layout.

        ```: string

            The back-tick / grave key on an American layout.

        `,`: string

            The comma key on an American layout.

        `.`: string

            The period key on an American layout.

        `/`: string

            The forward-slash key on an American layout.

        `capslock`: string

            The capslock key on an American layout.

        `f1`: string

            The F1 key on an American layout.

        `f2`: string

            The F2 key on an American layout.

        `f3`: string

            The F3 key on an American layout.

        `f4`: string

            The F4 key on an American layout.

        `f5`: string

            The F5 key on an American layout.

        `f6`: string

            The F6 key on an American layout.

        `f7`: string

            The F7 key on an American layout.

        `f8`: string

            The F8 key on an American layout.

        `f9`: string

            The F9 key on an American layout.

        `f10`: string

            The F10 key on an American layout.

        `f11`: string

            The F11 key on an American layout.

        `f12`: string

            The F12 key on an American layout.

        `f13`: string

            The F13 key on an American layout.

        `f14`: string

            The F14 key on an American layout.

        `f15`: string

            The F15 key on an American layout.

        `f16`: string

            The F16 key on an American layout.

        `f17`: string

            The F17 key on an American layout.

        `f18`: string

            The F18 key on an American layout.

        `f19`: string

            The F19 key on an American layout.

        `f20`: string

            The F20 key on an American layout.

        `f21`: string

            The F21 key on an American layout.

        `f22`: string

            The F22 key on an American layout.

        `f23`: string

            The F23 key on an American layout.

        `f24`: string

            The F24 key on an American layout.

        `lctrl`: string

            The left control key on an American layout.

        `lshift`: string

            The left shift key on an American layout.

        `lalt`: string

            The left alt / option key on an American layout.

        `lgui`: string

            The left GUI (command / windows / super) key on an American layout.

        `rctrl`: string

            The right control key on an American layout.

        `rshift`: string

            The right shift key on an American layout.

        `ralt`: string

            The right alt / option key on an American layout.

        `rgui`: string

            The right GUI (command / windows / super) key on an American
            layout.

        `printscreen`: string

            The printscreen key on an American layout.

        `scrolllock`: string

            The scroll-lock key on an American layout.

        `pause`: string

            The pause key on an American layout.

        `insert`: string

            The insert key on an American layout.

        `home`: string

            The home key on an American layout.

        `numlock`: string

            The numlock / clear key on an American layout.

        `pageup`: string

            The page-up key on an American layout.

        `delete`: string

            The forward-delete key on an American layout.

        `end`: string

            The end key on an American layout.

        `pagedown`: string

            The page-down key on an American layout.

        `right`: string

            The right-arrow key on an American layout.

        `left`: string

            The left-arrow key on an American layout.

        `down`: string

            The down-arrow key on an American layout.

        `up`: string

            The up-arrow key on an American layout.

        `nonusbackslash`: string

            The non-U.S. backslash scancode.

        `application`: string

            The application key on an American layout. Windows contextual menu,
            compose key.

        `execute`: string

            The 'execute' key on an American layout.

        `help`: string

            The 'help' key on an American layout.

        `menu`: string

            The 'menu' key on an American layout.

        `select`: string

            The 'select' key on an American layout.

        `stop`: string

            The 'stop' key on an American layout.

        `again`: string

            The 'again' key on an American layout.

        `undo`: string

            The 'undo' key on an American layout.

        `cut`: string

            The 'cut' key on an American layout.

        `copy`: string

            The 'copy' key on an American layout.

        `paste`: string

            The 'paste' key on an American layout.

        `find`: string

            The 'find' key on an American layout.

        `kp/`: string

            The keypad forward-slash key on an American layout.

        `kp*`: string

            The keypad '*' key on an American layout.

        `kp-`: string

            The keypad minus key on an American layout.

        `kp+`: string

            The keypad plus key on an American layout.

        `kp=`: string

            The keypad equals key on an American layout.

        `kpenter`: string

            The keypad enter key on an American layout.

        `kp1`: string

            The keypad '1' key on an American layout.

        `kp2`: string

            The keypad '2' key on an American layout.

        `kp3`: string

            The keypad '3' key on an American layout.

        `kp4`: string

            The keypad '4' key on an American layout.

        `kp5`: string

            The keypad '5' key on an American layout.

        `kp6`: string

            The keypad '6' key on an American layout.

        `kp7`: string

            The keypad '7' key on an American layout.

        `kp8`: string

            The keypad '8' key on an American layout.

        `kp9`: string

            The keypad '9' key on an American layout.

        `kp0`: string

            The keypad '0' key on an American layout.

        `kp.`: string

            The keypad period key on an American layout.

        `international1`: string

            The 1st international key on an American layout. Used on Asian
            keyboards.

        `international2`: string

            The 2nd international key on an American layout.

        `international3`: string

            The 3rd international  key on an American layout. Yen.

        `international4`: string

            The 4th international key on an American layout.

        `international5`: string

            The 5th international key on an American layout.

        `international6`: string

            The 6th international key on an American layout.

        `international7`: string

            The 7th international key on an American layout.

        `international8`: string

            The 8th international key on an American layout.

        `international9`: string

            The 9th international key on an American layout.

        `lang1`: string

            Hangul/English toggle scancode.

        `lang2`: string

            Hanja conversion scancode.

        `lang3`: string

            Katakana scancode.

        `lang4`: string

            Hiragana scancode.

        `lang5`: string

            Zenkaku/Hankaku scancode.

        `mute`: string

            The mute key on an American layout.

        `volumeup`: string

            The volume up key on an American layout.

        `volumedown`: string

            The volume down key on an American layout.

        `audionext`: string

            The audio next track key on an American layout.

        `audioprev`: string

            The audio previous track key on an American layout.

        `audiostop`: string

            The audio stop key on an American layout.

        `audioplay`: string

            The audio play key on an American layout.

        `audiomute`: string

            The audio mute key on an American layout.

        `mediaselect`: string

            The media select key on an American layout.

        `www`: string

            The 'WWW' key on an American layout.

        `mail`: string

            The Mail key on an American layout.

        `calculator`: string

            The calculator key on an American layout.

        `computer`: string

            The 'computer' key on an American layout.

        `acsearch`: string

            The AC Search key on an American layout.

        `achome`: string

            The AC Home key on an American layout.

        `acback`: string

            The AC Back key on an American layout.

        `acforward`: string

            The AC Forward key on an American layout.

        `acstop`: string

            Th AC Stop key on an American layout.

        `acrefresh`: string

            The AC Refresh key on an American layout.

        `acbookmarks`: string

            The AC Bookmarks key on an American layout.

        `power`: string

            The system power scancode.

        `brightnessdown`: string

            The brightness-down scancode.

        `brightnessup`: string

            The brightness-up scancode.

        `displayswitch`: string

            The display switch scancode.

        `kbdillumtoggle`: string

            The keyboard illumination toggle scancode.

        `kbdillumdown`: string

            The keyboard illumination down scancode.

        `kbdillumup`: string

            The keyboard illumination up scancode.

        `eject`: string

            The eject scancode.

        `sleep`: string

            The system sleep scancode.

        `alterase`: string

            The alt-erase key on an American layout.

        `sysreq`: string

            The sysreq key on an American layout.

        `cancel`: string

            The 'cancel' key on an American layout.

        `clear`: string

            The 'clear' key on an American layout.

        `prior`: string

            The 'prior' key on an American layout.

        `return2`: string

            The 'return2' key on an American layout.

        `separator`: string

            The 'separator' key on an American layout.

        `out`: string

            The 'out' key on an American layout.

        `oper`: string

            The 'oper' key on an American layout.

        `clearagain`: string

            The 'clearagain' key on an American layout.

        `crsel`: string

            The 'crsel' key on an American layout.

        `exsel`: string

            The 'exsel' key on an American layout.

        `kp00`: string

            The keypad 00 key on an American layout.

        `kp000`: string

            The keypad 000 key on an American layout.

        `thsousandsseparator`: string

            The thousands-separator key on an American layout.

        `decimalseparator`: string

            The decimal separator key on an American layout.

        `currencyunit`: string

            The currency unit key on an American layout.

        `currencysubunit`: string

            The currency sub-unit key on an American layout.

        `app1`: string

            The 'app1' scancode.

        `app2`: string

            The 'app2' scancode.

        `unknown`: string

            An unknown key.

-------------------------------------------------------------------------------
                                          *love2d-docs-love.keyboard-functions*
The functions of |love.keyboard|:

    |getKeyFromScancode|         |love2d-docs-love.keyboard.getKeyFromScancode|
    |getScancodeFromKey|         |love2d-docs-love.keyboard.getScancodeFromKey|
    |hasKeyRepeat|                     |love2d-docs-love.keyboard.hasKeyRepeat|
    |hasScreenKeyboard|           |love2d-docs-love.keyboard.hasScreenKeyboard|
    |hasTextInput|                     |love2d-docs-love.keyboard.hasTextInput|
    |isDown|                                 |love2d-docs-love.keyboard.isDown|
    |isScancodeDown|                 |love2d-docs-love.keyboard.isScancodeDown|
    |setKeyRepeat|                     |love2d-docs-love.keyboard.setKeyRepeat|
    |setTextInput|                     |love2d-docs-love.keyboard.setTextInput|

-------------------------------------------------------------------------------
                                 *love2d-docs-love.keyboard.getKeyFromScancode*
|love.keyboard.getKeyFromScancode|

Gets the key corresponding to the given hardware scancode.

Unlike key constants, Scancodes are keyboard layout-independent. For example
the scancode 'w' will be generated if the key in the same place as the 'w' key
on an American keyboard is pressed, no matter what the key is labelled or what
the user's operating system settings are.

Scancodes are useful for creating default controls that have the same physical
locations on on all systems.

Synopses: ~

        `key` = |love.keyboard.getKeyFromScancode|(`scancode`)

Variants: ~

        `key` = |love.keyboard.getKeyFromScancode|(`scancode`)

        See function description

        Parameters: ~
            • {scancode} (`Scancode`) |love2d-docs-Scancode|
                The scancode to get the key from.

        Return: ~
            (`KeyConstant`) |love2d-docs-KeyConstant|
                The key corresponding to the given scancode, or 'unknown' if
                the scancode doesn't map to a KeyConstant on the current
                system.
-------------------------------------------------------------------------------
                                 *love2d-docs-love.keyboard.getScancodeFromKey*
|love.keyboard.getScancodeFromKey|

Gets the hardware scancode corresponding to the given key.

Unlike key constants, Scancodes are keyboard layout-independent. For example
the scancode 'w' will be generated if the key in the same place as the 'w' key
on an American keyboard is pressed, no matter what the key is labelled or what
the user's operating system settings are.

Scancodes are useful for creating default controls that have the same physical
locations on on all systems.

Synopses: ~

        `scancode` = |love.keyboard.getScancodeFromKey|(`key`)

Variants: ~

        `scancode` = |love.keyboard.getScancodeFromKey|(`key`)

        See function description

        Parameters: ~
            • {key} (`KeyConstant`) |love2d-docs-KeyConstant|
                The key to get the scancode from.

        Return: ~
            (`Scancode`) |love2d-docs-Scancode|
                The scancode corresponding to the given key, or 'unknown' if
                the given key has no known physical representation on the
                current system.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.keyboard.hasKeyRepeat*
|love.keyboard.hasKeyRepeat|

Gets whether key repeat is enabled.

Synopses: ~

        `enabled` = |love.keyboard.hasKeyRepeat|()

Variants: ~

        `enabled` = |love.keyboard.hasKeyRepeat|()

        See function description

        Return: ~
            (`boolean`)
                Whether key repeat is enabled.
-------------------------------------------------------------------------------
                                  *love2d-docs-love.keyboard.hasScreenKeyboard*
|love.keyboard.hasScreenKeyboard|

Gets whether screen keyboard is supported.

Synopses: ~

        `supported` = |love.keyboard.hasScreenKeyboard|()

Variants: ~

        `supported` = |love.keyboard.hasScreenKeyboard|()

        See function description

        Return: ~
            (`boolean`)
                Whether screen keyboard is supported.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.keyboard.hasTextInput*
|love.keyboard.hasTextInput|

Gets whether text input events are enabled.

Synopses: ~

        `enabled` = |love.keyboard.hasTextInput|()

Variants: ~

        `enabled` = |love.keyboard.hasTextInput|()

        See function description

        Return: ~
            (`boolean`)
                Whether text input events are enabled.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.keyboard.isDown*
|love.keyboard.isDown|

Checks whether a certain key is down. Not to be confused with love.keypressed
or love.keyreleased.

Synopses: ~

    1.  `down` = |love.keyboard.isDown|(`key`)
    2.  `anyDown` = |love.keyboard.isDown|(`key`, `...`)

Variants: ~

    1.  `down` = |love.keyboard.isDown|(`key`)

    1.          See function description

        Parameters: ~
            • {key} (`KeyConstant`) |love2d-docs-KeyConstant|
                The key to check.

        Return: ~
            (`boolean`)
                True if the key is down, false if not.
    2.  `anyDown` = |love.keyboard.isDown|(`key`, `...`)

    2.          See function description

        Parameters: ~
            • {key} (`KeyConstant`) |love2d-docs-KeyConstant|
                A key to check.
            • {...} (`KeyConstant`) |love2d-docs-KeyConstant|
                Additional keys to check.

        Return: ~
            (`boolean`)
                True if any supplied key is down, false if not.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.keyboard.isScancodeDown*
|love.keyboard.isScancodeDown|

Checks whether the specified Scancodes are pressed. Not to be confused with
love.keypressed or love.keyreleased.

Unlike regular KeyConstants, Scancodes are keyboard layout-independent. The
scancode 'w' is used if the key in the same place as the 'w' key on an American
keyboard is pressed, no matter what the key is labelled or what the user's
operating system settings are.

Synopses: ~

        `down` = |love.keyboard.isScancodeDown|(`scancode`, `...`)

Variants: ~

        `down` = |love.keyboard.isScancodeDown|(`scancode`, `...`)

        See function description

        Parameters: ~
            • {scancode} (`Scancode`) |love2d-docs-Scancode|
                A Scancode to check.
            • {...} (`Scancode`) |love2d-docs-Scancode|
                Additional Scancodes to check.

        Return: ~
            (`boolean`)
                True if any supplied Scancode is down, false if not.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.keyboard.setKeyRepeat*
|love.keyboard.setKeyRepeat|

Enables or disables key repeat for love.keypressed. It is disabled by default.

Synopses: ~

        |love.keyboard.setKeyRepeat|(`enable`)

Variants: ~

        |love.keyboard.setKeyRepeat|(`enable`)

        The interval between repeats depends on the user's system settings.
        This function doesn't affect whether love.textinput is called multiple
        times while a key is held down.

        Parameters: ~
            • {enable} (`boolean`)
                Whether repeat keypress events should be enabled when a key is
                held down.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.keyboard.setTextInput*
|love.keyboard.setTextInput|

Enables or disables text input events. It is enabled by default on Windows,
Mac, and Linux, and disabled by default on iOS and Android.

On touch devices, this shows the system's native on-screen keyboard when it's
enabled.

Synopses: ~

    1.  |love.keyboard.setTextInput|(`enable`)
    2.  |love.keyboard.setTextInput|(`enable`, `x`, `y`, `w`, `h`)

Variants: ~

    1.  |love.keyboard.setTextInput|(`enable`)

    1.          See function description

        Parameters: ~
            • {enable} (`boolean`)
                Whether text input events should be enabled.


    2.  |love.keyboard.setTextInput|(`enable`, `x`, `y`, `w`, `h`)

    2.          On iOS and Android this variant tells the OS that the specified
        rectangle is where text will show up in the game, which prevents the
        system on-screen keyboard from covering the text.

        Parameters: ~
            • {enable} (`boolean`)
                Whether text input events should be enabled.
            • {x} (`number`)
                Text rectangle x position.
            • {y} (`number`)
                Text rectangle y position.
            • {w} (`number`)
                Text rectangle width.
            • {h} (`number`)
                Text rectangle height.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.keyboard-types*
The types of |love.keyboard|:

    None

===============================================================================
                                                        *love2d-docs-love.math*
|love.math|

Provides system-independent mathematical functions.

    |callbacks|                               |love2d-docs-love.math-callbacks|
    |enums|                                       |love2d-docs-love.math-enums|
    |functions|                               |love2d-docs-love.math-functions|
    |types|                                       |love2d-docs-love.math-types|

-------------------------------------------------------------------------------
                                              *love2d-docs-love.math-callbacks*
The callbacks of |love.math|:

    None

-------------------------------------------------------------------------------
                                                  *love2d-docs-love.math-enums*
The enums of |love.math|:

    |MatrixLayout|                                   |love2d-docs-MatrixLayout|

-------------------------------------------------------------------------------
                                                     *love2d-docs-MatrixLayout*
|MatrixLayout|

The layout of matrix elements (row-major or column-major).

    constants: ~

        `row`: string

            The matrix is row-major:

        `column`: string

            The matrix is column-major:

-------------------------------------------------------------------------------
                                              *love2d-docs-love.math-functions*
The functions of |love.math|:

    |colorFromBytes|                     |love2d-docs-love.math.colorFromBytes|
    |colorToBytes|                         |love2d-docs-love.math.colorToBytes|
    |gammaToLinear|                       |love2d-docs-love.math.gammaToLinear|
    |getRandomSeed|                       |love2d-docs-love.math.getRandomSeed|
    |getRandomState|                     |love2d-docs-love.math.getRandomState|
    |isConvex|                                 |love2d-docs-love.math.isConvex|
    |linearToGamma|                       |love2d-docs-love.math.linearToGamma|
    |newBezierCurve|                     |love2d-docs-love.math.newBezierCurve|
    |newRandomGenerator|             |love2d-docs-love.math.newRandomGenerator|
    |newTransform|                         |love2d-docs-love.math.newTransform|
    |noise|                                       |love2d-docs-love.math.noise|
    |random|                                     |love2d-docs-love.math.random|
    |randomNormal|                         |love2d-docs-love.math.randomNormal|
    |setRandomSeed|                       |love2d-docs-love.math.setRandomSeed|
    |setRandomState|                     |love2d-docs-love.math.setRandomState|
    |triangulate|                           |love2d-docs-love.math.triangulate|

-------------------------------------------------------------------------------
                                         *love2d-docs-love.math.colorFromBytes*
|love.math.colorFromBytes|

Converts a color from 0..255 to 0..1 range.

Synopses: ~

        `r`, `g`, `b`, `a` = |love.math.colorFromBytes|(`rb`, `gb`,
        `bb`, `ab`)

Variants: ~

        `r`, `g`, `b`, `a` = |love.math.colorFromBytes|(`rb`, `gb`,
        `bb`, `ab`)

        Here's implementation for 11.2 and earlier.

        function love.math.colorFromBytes(r, g, b, a)

        	if type(r) == 'table' then

        		r, g, b, a = rr[2, rr[4

        	end

        	r = clamp01(floor(r + 0.5) / 255)

        	g = clamp01(floor(g + 0.5) / 255)

        	b = clamp01(floor(b + 0.5) / 255)

        	a = a ~= nil and clamp01(floor(a + 0.5) / 255) or nil

        	return r, g, b, a

        end

        Where clamp01 is defined as follows

        local function clamp01(x)

        	return math.min(math.max(x, 0), 1)

        end

        Parameters: ~
            • {rb} (`number`)
                Red color component in 0..255 range.
            • {gb} (`number`)
                Green color component in 0..255 range.
            • {bb} (`number`)
                Blue color component in 0..255 range.
            • {ab} (`number`) (default: `nil`)
                Alpha color component in 0..255 range.

        Returns: ~
            1. (`number`)
                Red color component in 0..1 range.

            2. (`number`)
                Green color component in 0..1 range.

            3. (`number`)
                Blue color component in 0..1 range.

            4. (`number`)
                Alpha color component in 0..1 range or nil if alpha is not
                specified.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.math.colorToBytes*
|love.math.colorToBytes|

Converts a color from 0..1 to 0..255 range.

Synopses: ~

        `rb`, `gb`, `bb`, `ab` = |love.math.colorToBytes|(`r`, `g`,
        `b`, `a`)

Variants: ~

        `rb`, `gb`, `bb`, `ab` = |love.math.colorToBytes|(`r`, `g`,
        `b`, `a`)

        Here's implementation for 11.2 and earlier.

        function love.math.colorToBytes(r, g, b, a)

        	if type(r) == 'table' then

        		r, g, b, a = rr[2, rr[4

        	end

        	r = floor(clamp01(r) * 255 + 0.5)

        	g = floor(clamp01(g) * 255 + 0.5)

        	b = floor(clamp01(b) * 255 + 0.5)

        	a = a ~= nil and floor(clamp01(a) * 255 + 0.5) or nil

        	return r, g, b, a

        end

        Where clamp01 is defined as follows

        local function clamp01(x)

        	return math.min(math.max(x, 0), 1)

        end

        Parameters: ~
            • {r} (`number`)
                Red color component.
            • {g} (`number`)
                Green color component.
            • {b} (`number`)
                Blue color component.
            • {a} (`number`) (default: `nil`)
                Alpha color component.

        Returns: ~
            1. (`number`)
                Red color component in 0..255 range.

            2. (`number`)
                Green color component in 0..255 range.

            3. (`number`)
                Blue color component in 0..255 range.

            4. (`number`)
                Alpha color component in 0..255 range or nil if alpha is not
                specified.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.math.gammaToLinear*
|love.math.gammaToLinear|

Converts a color from gamma-space (sRGB) to linear-space (RGB). This is useful
when doing gamma-correct rendering and you need to do math in linear RGB in the
few cases where LÖVE doesn't handle conversions automatically.

Read more about gamma-correct rendering here, here, and here.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

    1.  `lr`, `lg`, `lb` = |love.math.gammaToLinear|(`r`, `g`, `b`)
    2.  `lr`, `lg`, `lb` = |love.math.gammaToLinear|(`color`)
    3.  `lc` = |love.math.gammaToLinear|(`c`)

Variants: ~

    1.  `lr`, `lg`, `lb` = |love.math.gammaToLinear|(`r`, `g`, `b`)

    1.          An alpha value can be passed into the function as a fourth argument,
        but it will be returned unchanged because alpha is always linear.

        Parameters: ~
            • {r} (`number`)
                The red channel of the sRGB color to convert.
            • {g} (`number`)
                The green channel of the sRGB color to convert.
            • {b} (`number`)
                The blue channel of the sRGB color to convert.

        Returns: ~
            1. (`number`)
                The red channel of the converted color in linear RGB space.

            2. (`number`)
                The green channel of the converted color in linear RGB space.

            3. (`number`)
                The blue channel of the converted color in linear RGB space.
    2.  `lr`, `lg`, `lb` = |love.math.gammaToLinear|(`color`)

    2.          See function description

        Parameters: ~
            • {color} (`table`)
                An array with the red, green, and blue channels of the sRGB
                color to convert.

        Returns: ~
            1. (`number`)
                The red channel of the converted color in linear RGB space.

            2. (`number`)
                The green channel of the converted color in linear RGB space.

            3. (`number`)
                The blue channel of the converted color in linear RGB space.
    3.  `lc` = |love.math.gammaToLinear|(`c`)

    3.          See function description

        Parameters: ~
            • {c} (`number`)
                The value of a color channel in sRGB space to convert.

        Return: ~
            (`number`)
                The value of the color channel in linear RGB space.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.math.getRandomSeed*
|love.math.getRandomSeed|

Gets the seed of the random number generator.

The seed is split into two numbers due to Lua's use of doubles for all number
values - doubles can't accurately represent integer  values above 2^53, but the
seed can be an integer value up to 2^64.

Synopses: ~

        `low`, `high` = |love.math.getRandomSeed|()

Variants: ~

        `low`, `high` = |love.math.getRandomSeed|()

        See function description

        Returns: ~
            1. (`number`)
                Integer number representing the lower 32 bits of the random
                number generator's 64 bit seed value.

            2. (`number`)
                Integer number representing the higher 32 bits of the random
                number generator's 64 bit seed value.
-------------------------------------------------------------------------------
                                         *love2d-docs-love.math.getRandomState*
|love.math.getRandomState|

Gets the current state of the random number generator. This returns an opaque
implementation-dependent string which is only useful for later use with
love.math.setRandomState or RandomGenerator:setState.

This is different from love.math.getRandomSeed in that getRandomState gets the
random number generator's current state, whereas getRandomSeed gets the
previously set seed number.

Synopses: ~

        `state` = |love.math.getRandomState|()

Variants: ~

        `state` = |love.math.getRandomState|()

        The value of the state string does not depend on the current operating
        system.

        Return: ~
            (`string`)
                The current state of the random number generator, represented
                as a string.
-------------------------------------------------------------------------------
                                               *love2d-docs-love.math.isConvex*
|love.math.isConvex|

Checks whether a polygon is convex.

PolygonShapes in love.physics, some forms of Meshes, and polygons drawn with
love.graphics.polygon must be simple convex polygons.

Synopses: ~

    1.  `convex` = |love.math.isConvex|(`vertices`)
    2.  `convex` = |love.math.isConvex|(`x1`, `y1`, `x2`, `y2`, `...`)

Variants: ~

    1.  `convex` = |love.math.isConvex|(`vertices`)

    1.          See function description

        Parameters: ~
            • {vertices} (`table`)
                The vertices of the polygon as a table in the form of {x1, y1,
                x2, y2, x3, y3, ...}.

        Return: ~
            (`boolean`)
                Whether the given polygon is convex.
    2.  `convex` = |love.math.isConvex|(`x1`, `y1`, `x2`, `y2`, `...`)

    2.          See function description

        Parameters: ~
            • {x1} (`number`)
                The position of the first vertex of the polygon on the x-axis.
            • {y1} (`number`)
                The position of the first vertex of the polygon on the y-axis.
            • {x2} (`number`)
                The position of the second vertex of the polygon on the x-axis.
            • {y2} (`number`)
                The position of the second vertex of the polygon on the y-axis.
            • {...} (`number`)
                Additional position of the vertex of the polygon on the x-axis
                and y-axis.

        Return: ~
            (`boolean`)
                Whether the given polygon is convex.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.math.linearToGamma*
|love.math.linearToGamma|

Converts a color from linear-space (RGB) to gamma-space (sRGB). This is useful
when storing linear RGB color values in an image, because the linear RGB color
space has less precision than sRGB for dark colors, which can result in
noticeable color banding when drawing.

In general, colors chosen based on what they look like on-screen are already in
gamma-space and should not be double-converted. Colors calculated using math
are often in the linear RGB space.

Read more about gamma-correct rendering here, here, and here.

In versions prior to 11.0, color component values were within the range of 0 to
255 instead of 0 to 1.

Synopses: ~

    1.  `cr`, `cg`, `cb` = |love.math.linearToGamma|(`lr`, `lg`, `lb`)
    2.  `cr`, `cg`, `cb` = |love.math.linearToGamma|(`color`)
    3.  `c` = |love.math.linearToGamma|(`lc`)

Variants: ~

    1.  `cr`, `cg`, `cb` = |love.math.linearToGamma|(`lr`, `lg`, `lb`)

    1.          An alpha value can be passed into the function as a fourth argument,
        but it will be returned unchanged because alpha is always linear.

        Parameters: ~
            • {lr} (`number`)
                The red channel of the linear RGB color to convert.
            • {lg} (`number`)
                The green channel of the linear RGB color to convert.
            • {lb} (`number`)
                The blue channel of the linear RGB color to convert.

        Returns: ~
            1. (`number`)
                The red channel of the converted color in gamma sRGB space.

            2. (`number`)
                The green channel of the converted color in gamma sRGB space.

            3. (`number`)
                The blue channel of the converted color in gamma sRGB space.
    2.  `cr`, `cg`, `cb` = |love.math.linearToGamma|(`color`)

    2.          See function description

        Parameters: ~
            • {color} (`table`)
                An array with the red, green, and blue channels of the linear
                RGB color to convert.

        Returns: ~
            1. (`number`)
                The red channel of the converted color in gamma sRGB space.

            2. (`number`)
                The green channel of the converted color in gamma sRGB space.

            3. (`number`)
                The blue channel of the converted color in gamma sRGB space.
    3.  `c` = |love.math.linearToGamma|(`lc`)

    3.          See function description

        Parameters: ~
            • {lc} (`number`)
                The value of a color channel in linear RGB space to convert.

        Return: ~
            (`number`)
                The value of the color channel in gamma sRGB space.
-------------------------------------------------------------------------------
                                         *love2d-docs-love.math.newBezierCurve*
|love.math.newBezierCurve|

Creates a new BezierCurve object.

The number of vertices in the control polygon determines the degree of the
curve, e.g. three vertices define a quadratic (degree 2) Bézier curve, four
vertices define a cubic (degree 3) Bézier curve, etc.

Synopses: ~

    1.  `curve` = |love.math.newBezierCurve|(`vertices`)
    2.  `curve` = |love.math.newBezierCurve|(`x1`, `y1`, `x2`, `y2`,
        `...`)

Variants: ~

    1.  `curve` = |love.math.newBezierCurve|(`vertices`)

    1.          See function description

        Parameters: ~
            • {vertices} (`table`)
                The vertices of the control polygon as a table in the form of
                {x1, y1, x2, y2, x3, y3, ...}.

        Return: ~
            (`BezierCurve`) |love2d-docs-BezierCurve|
                A Bézier curve object.
    2.  `curve` = |love.math.newBezierCurve|(`x1`, `y1`, `x2`, `y2`,
        `...`)

    2.          See function description

        Parameters: ~
            • {x1} (`number`)
                The position of the first vertex of the control polygon on the
                x-axis.
            • {y1} (`number`)
                The position of the first vertex of the control polygon on the
                y-axis.
            • {x2} (`number`)
                The position of the second vertex of the control polygon on the
                x-axis.
            • {y2} (`number`)
                The position of the second vertex of the control polygon on the
                y-axis.
            • {...} (`number`)
                Additional position of the vertex of the control polygon on the
                x-axis and y-axis.

        Return: ~
            (`BezierCurve`) |love2d-docs-BezierCurve|
                A Bézier curve object.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.math.newRandomGenerator*
|love.math.newRandomGenerator|

Creates a new RandomGenerator object which is completely independent of other
RandomGenerator objects and random functions.

Synopses: ~

    1.  `rng` = |love.math.newRandomGenerator|()
    2.  `rng` = |love.math.newRandomGenerator|(`seed`)
    3.  `rng` = |love.math.newRandomGenerator|(`low`, `high`)

Variants: ~

    1.  `rng` = |love.math.newRandomGenerator|()

    1.          See function description

        Return: ~
            (`RandomGenerator`) |love2d-docs-RandomGenerator|
                The new Random Number Generator object.
    2.  `rng` = |love.math.newRandomGenerator|(`seed`)

    2.          See RandomGenerator:setSeed.

        Parameters: ~
            • {seed} (`number`)
                The initial seed number to use for this object.

        Return: ~
            (`RandomGenerator`) |love2d-docs-RandomGenerator|
                The new Random Number Generator object.
    3.  `rng` = |love.math.newRandomGenerator|(`low`, `high`)

    3.          See RandomGenerator:setSeed.

        Parameters: ~
            • {low} (`number`)
                The lower 32 bits of the seed number to use for this object.
            • {high} (`number`)
                The higher 32 bits of the seed number to use for this object.

        Return: ~
            (`RandomGenerator`) |love2d-docs-RandomGenerator|
                The new Random Number Generator object.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.math.newTransform*
|love.math.newTransform|

Creates a new Transform object.

Synopses: ~

    1.  `transform` = |love.math.newTransform|()
    2.  `transform` = |love.math.newTransform|(`x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

Variants: ~

    1.  `transform` = |love.math.newTransform|()

    1.          Creates a Transform with no transformations applied. Call methods on
        the returned object to apply transformations.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The new Transform object.
    2.  `transform` = |love.math.newTransform|(`x`, `y`, `angle`, `sx`,
        `sy`, `ox`, `oy`, `kx`, `ky`)

    2.          Creates a Transform with the specified transformation applied on
        creation.

        Parameters: ~
            • {x} (`number`)
                The position of the new Transform on the x-axis.
            • {y} (`number`)
                The position of the new Transform on the y-axis.
            • {angle} (`number`) (default: `0`)
                The orientation of the new Transform in radians.
            • {sx} (`number`) (default: `1`)
                Scale factor on the x-axis.
            • {sy} (`number`) (default: `sx`)
                Scale factor on the y-axis.
            • {ox} (`number`) (default: `0`)
                Origin offset on the x-axis.
            • {oy} (`number`) (default: `0`)
                Origin offset on the y-axis.
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor on the x-axis.
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor on the y-axis.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The new Transform object.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.math.noise*
|love.math.noise|

Generates a Simplex or Perlin noise value in 1-4 dimensions. The return value
will always be the same, given the same arguments.

Simplex noise is closely related to Perlin noise. It is widely used for
procedural content generation.

There are many webpages which discuss Perlin and Simplex noise in detail.

Synopses: ~

    1.  `value` = |love.math.noise|(`x`)
    2.  `value` = |love.math.noise|(`x`, `y`)
    3.  `value` = |love.math.noise|(`x`, `y`, `z`)
    4.  `value` = |love.math.noise|(`x`, `y`, `z`, `w`)

Variants: ~

    1.  `value` = |love.math.noise|(`x`)

    1.          Generates Simplex noise from 1 dimension.

        Parameters: ~
            • {x} (`number`)
                The number used to generate the noise value.

        Return: ~
            (`number`)
                The noise value in the range of 1.
    2.  `value` = |love.math.noise|(`x`, `y`)

    2.          Generates Simplex noise from 2 dimensions.

        Parameters: ~
            • {x} (`number`)
                The first value of the 2-dimensional vector used to generate
                the noise value.
            • {y} (`number`)
                The second value of the 2-dimensional vector used to generate
                the noise value.

        Return: ~
            (`number`)
                The noise value in the range of 1.
    3.  `value` = |love.math.noise|(`x`, `y`, `z`)

    3.          Generates Perlin noise (Simplex noise in version 0.9.2 and older) from
        3 dimensions.

        Parameters: ~
            • {x} (`number`)
                The first value of the 3-dimensional vector used to generate
                the noise value.
            • {y} (`number`)
                The second value of the 3-dimensional vector used to generate
                the noise value.
            • {z} (`number`)
                The third value of the 3-dimensional vector used to generate
                the noise value.

        Return: ~
            (`number`)
                The noise value in the range of 1.
    4.  `value` = |love.math.noise|(`x`, `y`, `z`, `w`)

    4.          Generates Perlin noise (Simplex noise in version 0.9.2 and older) from
        4 dimensions.

        Parameters: ~
            • {x} (`number`)
                The first value of the 4-dimensional vector used to generate
                the noise value.
            • {y} (`number`)
                The second value of the 4-dimensional vector used to generate
                the noise value.
            • {z} (`number`)
                The third value of the 4-dimensional vector used to generate
                the noise value.
            • {w} (`number`)
                The fourth value of the 4-dimensional vector used to generate
                the noise value.

        Return: ~
            (`number`)
                The noise value in the range of 1.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.math.random*
|love.math.random|

Generates a pseudo-random number in a platform independent manner. The default
love.run seeds this function at startup, so you generally don't need to seed it
yourself.

Synopses: ~

    1.  `number` = |love.math.random|()
    2.  `number` = |love.math.random|(`max`)
    3.  `number` = |love.math.random|(`min`, `max`)

Variants: ~

    1.  `number` = |love.math.random|()

    1.          Get uniformly distributed pseudo-random real number within 1.

        Return: ~
            (`number`)
                The pseudo-random number.
    2.  `number` = |love.math.random|(`max`)

    2.          Get a uniformly distributed pseudo-random integer within max.

        Parameters: ~
            • {max} (`number`)
                The maximum possible value it should return.

        Return: ~
            (`number`)
                The pseudo-random integer number.
    3.  `number` = |love.math.random|(`min`, `max`)

    3.          Get uniformly distributed pseudo-random integer within max.

        Parameters: ~
            • {min} (`number`)
                The minimum possible value it should return.
            • {max} (`number`)
                The maximum possible value it should return.

        Return: ~
            (`number`)
                The pseudo-random integer number.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.math.randomNormal*
|love.math.randomNormal|

Get a normally distributed pseudo random number.

Synopses: ~

        `number` = |love.math.randomNormal|(`stddev`, `mean`)

Variants: ~

        `number` = |love.math.randomNormal|(`stddev`, `mean`)

        See function description

        Parameters: ~
            • {stddev} (`number`) (default: `1`)
                Standard deviation of the distribution.
            • {mean} (`number`) (default: `0`)
                The mean of the distribution.

        Return: ~
            (`number`)
                Normally distributed random number with variance (stddev)² and
                the specified mean.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.math.setRandomSeed*
|love.math.setRandomSeed|

Sets the seed of the random number generator using the specified integer
number. This is called internally at startup, so you generally don't need to
call it yourself.

Synopses: ~

    1.  |love.math.setRandomSeed|(`seed`)
    2.  |love.math.setRandomSeed|(`low`, `high`)

Variants: ~

    1.  |love.math.setRandomSeed|(`seed`)

    1.          Due to Lua's use of double-precision floating point numbers, integer
        values above 2^53 cannot be accurately represented. Use the other
        variant of the function if you want to use a larger number.

        Parameters: ~
            • {seed} (`number`)
                The integer number with which you want to seed the
                randomization. Must be within the range of 2^53 - 1.


    2.  |love.math.setRandomSeed|(`low`, `high`)

    2.          Combines two 32-bit integer numbers into a 64-bit integer value and
        sets the seed of the random number generator using the value.

        Parameters: ~
            • {low} (`number`)
                The lower 32 bits of the seed value. Must be within the range
                of 2^32 - 1.
            • {high} (`number`)
                The higher 32 bits of the seed value. Must be within the range
                of 2^32 - 1.


-------------------------------------------------------------------------------
                                         *love2d-docs-love.math.setRandomState*
|love.math.setRandomState|

Sets the current state of the random number generator. The value used as an
argument for this function is an opaque implementation-dependent string and
should only originate from a previous call to love.math.getRandomState.

This is different from love.math.setRandomSeed in that setRandomState directly
sets the random number generator's current implementation-dependent state,
whereas setRandomSeed gives it a new seed value.

Synopses: ~

        |love.math.setRandomState|(`state`)

Variants: ~

        |love.math.setRandomState|(`state`)

        The effect of the state string does not depend on the current operating
        system.

        Parameters: ~
            • {state} (`string`)
                The new state of the random number generator, represented as a
                string. This should originate from a previous call to
                love.math.getRandomState.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.math.triangulate*
|love.math.triangulate|

Decomposes a simple convex or concave polygon into triangles.

Synopses: ~

    1.  `triangles` = |love.math.triangulate|(`polygon`)
    2.  `triangles` = |love.math.triangulate|(`x1`, `y1`, `x2`, `y2`,
        `x3`, `y3`)

Variants: ~

    1.  `triangles` = |love.math.triangulate|(`polygon`)

    1.          See function description

        Parameters: ~
            • {polygon} (`table`)
                Polygon to triangulate. Must not intersect itself.

        Return: ~
            (`table`)
                List of triangles the polygon is composed of, in the form of
                {{x1, y1, x2, y2, x3, y3},  {x1, y1, x2, y2, x3, y3}, ...}.
    2.  `triangles` = |love.math.triangulate|(`x1`, `y1`, `x2`, `y2`,
        `x3`, `y3`)

    2.          See function description

        Parameters: ~
            • {x1} (`number`)
                The position of the first vertex of the polygon on the x-axis.
            • {y1} (`number`)
                The position of the first vertex of the polygon on the y-axis.
            • {x2} (`number`)
                The position of the second vertex of the polygon on the x-axis.
            • {y2} (`number`)
                The position of the second vertex of the polygon on the y-axis.
            • {x3} (`number`)
                The position of the third vertex of the polygon on the x-axis.
            • {y3} (`number`)
                The position of the third vertex of the polygon on the y-axis.

        Return: ~
            (`table`)
                List of triangles the polygon is composed of, in the form of
                {{x1, y1, x2, y2, x3, y3},  {x1, y1, x2, y2, x3, y3}, ...}.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.math-types*
The types of |love.math|:

    |BezierCurve|                                     |love2d-docs-BezierCurve|
    |RandomGenerator|                             |love2d-docs-RandomGenerator|
    |Transform|                                         |love2d-docs-Transform|

-------------------------------------------------------------------------------
                                                      *love2d-docs-BezierCurve*
|BezierCurve|

A Bézier curve object that can evaluate and render Bézier curves of arbitrary
degree.

For more information on Bézier curves check this great article on Wikipedia.

                                         *love2d-docs-BezierCurve-constructors*
    constructors:

        |newBezierCurve|                           |love2d-docs-newBezierCurve|

                                           *love2d-docs-BezierCurve-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                             *love2d-docs-BezierCurve-subtypes*
    subtypes:

        None

                                            *love2d-docs-BezierCurve-functions*
    functions:

        |evaluate|                           |love2d-docs-BezierCurve:evaluate|
        |getControlPoint|             |love2d-docs-BezierCurve:getControlPoint|
        |getControlPointCount|   |love2d-docs-BezierCurve:getControlPointCount|
        |getDegree|                         |love2d-docs-BezierCurve:getDegree|
        |getDerivative|                 |love2d-docs-BezierCurve:getDerivative|
        |getSegment|                       |love2d-docs-BezierCurve:getSegment|
        |insertControlPoint|       |love2d-docs-BezierCurve:insertControlPoint|
        |removeControlPoint|       |love2d-docs-BezierCurve:removeControlPoint|
        |render|                               |love2d-docs-BezierCurve:render|
        |renderSegment|                 |love2d-docs-BezierCurve:renderSegment|
        |rotate|                               |love2d-docs-BezierCurve:rotate|
        |scale|                                 |love2d-docs-BezierCurve:scale|
        |setControlPoint|             |love2d-docs-BezierCurve:setControlPoint|
        |translate|                         |love2d-docs-BezierCurve:translate|

-------------------------------------------------------------------------------
                                             *love2d-docs-BezierCurve:evaluate*
|BezierCurve:evaluate|

Evaluate Bézier curve at parameter t. The parameter must be between 0 and 1
(inclusive).

This function can be used to move objects along paths or tween parameters.
However it should not be used to render the curve, see BezierCurve:render for
that purpose.

Synopses: ~

        `x`, `y` = |BezierCurve:evaluate|(`t`)

Variants: ~

        `x`, `y` = |BezierCurve:evaluate|(`t`)

        See function description

        Parameters: ~
            • {t} (`number`)
                Where to evaluate the curve.

        Returns: ~
            1. (`number`)
                x coordinate of the curve at parameter t.

            2. (`number`)
                y coordinate of the curve at parameter t.
-------------------------------------------------------------------------------
                                      *love2d-docs-BezierCurve:getControlPoint*
|BezierCurve:getControlPoint|

Get coordinates of the i-th control point. Indices start with 1.

Synopses: ~

        `x`, `y` = |BezierCurve:getControlPoint|(`i`)

Variants: ~

        `x`, `y` = |BezierCurve:getControlPoint|(`i`)

        See function description

        Parameters: ~
            • {i} (`number`)
                Index of the control point.

        Returns: ~
            1. (`number`)
                Position of the control point along the x axis.

            2. (`number`)
                Position of the control point along the y axis.
-------------------------------------------------------------------------------
                                 *love2d-docs-BezierCurve:getControlPointCount*
|BezierCurve:getControlPointCount|

Get the number of control points in the Bézier curve.

Synopses: ~

        `count` = |BezierCurve:getControlPointCount|()

Variants: ~

        `count` = |BezierCurve:getControlPointCount|()

        See function description

        Return: ~
            (`number`)
                The number of control points.
-------------------------------------------------------------------------------
                                            *love2d-docs-BezierCurve:getDegree*
|BezierCurve:getDegree|

Get degree of the Bézier curve. The degree is equal to
number-of-control-points - 1.

Synopses: ~

        `degree` = |BezierCurve:getDegree|()

Variants: ~

        `degree` = |BezierCurve:getDegree|()

        See function description

        Return: ~
            (`number`)
                Degree of the Bézier curve.
-------------------------------------------------------------------------------
                                        *love2d-docs-BezierCurve:getDerivative*
|BezierCurve:getDerivative|

Get the derivative of the Bézier curve.

This function can be used to rotate sprites moving along a curve in the
direction of the movement and compute the direction perpendicular to the curve
at some parameter t.

Synopses: ~

        `derivative` = |BezierCurve:getDerivative|()

Variants: ~

        `derivative` = |BezierCurve:getDerivative|()

        See function description

        Return: ~
            (`BezierCurve`) |love2d-docs-BezierCurve|
                The derivative curve.
-------------------------------------------------------------------------------
                                           *love2d-docs-BezierCurve:getSegment*
|BezierCurve:getSegment|

Gets a BezierCurve that corresponds to the specified segment of this
BezierCurve.

Synopses: ~

        `curve` = |BezierCurve:getSegment|(`startpoint`, `endpoint`)

Variants: ~

        `curve` = |BezierCurve:getSegment|(`startpoint`, `endpoint`)

        See function description

        Parameters: ~
            • {startpoint} (`number`)
                The starting point along the curve. Must be between 0 and 1.
            • {endpoint} (`number`)
                The end of the segment. Must be between 0 and 1.

        Return: ~
            (`BezierCurve`) |love2d-docs-BezierCurve|
                A BezierCurve that corresponds to the specified segment.
-------------------------------------------------------------------------------
                                   *love2d-docs-BezierCurve:insertControlPoint*
|BezierCurve:insertControlPoint|

Insert control point as the new i-th control point. Existing control points
from i onwards are pushed back by 1. Indices start with 1. Negative indices
wrap around: -1 is the last control point, -2 the one before the last, etc.

Synopses: ~

        |BezierCurve:insertControlPoint|(`x`, `y`, `i`)

Variants: ~

        |BezierCurve:insertControlPoint|(`x`, `y`, `i`)

        See function description

        Parameters: ~
            • {x} (`number`)
                Position of the control point along the x axis.
            • {y} (`number`)
                Position of the control point along the y axis.
            • {i} (`number`) (default: `-1`)
                Index of the control point.


-------------------------------------------------------------------------------
                                   *love2d-docs-BezierCurve:removeControlPoint*
|BezierCurve:removeControlPoint|

Removes the specified control point.

Synopses: ~

        |BezierCurve:removeControlPoint|(`index`)

Variants: ~

        |BezierCurve:removeControlPoint|(`index`)

        See function description

        Parameters: ~
            • {index} (`number`)
                The index of the control point to remove.


-------------------------------------------------------------------------------
                                               *love2d-docs-BezierCurve:render*
|BezierCurve:render|

Get a list of coordinates to be used with love.graphics.line.

This function samples the Bézier curve using recursive subdivision. You can
control the recursion depth using the depth parameter.

If you are just interested to know the position on the curve given a parameter,
use BezierCurve:evaluate.

Synopses: ~

        `coordinates` = |BezierCurve:render|(`depth`)

Variants: ~

        `coordinates` = |BezierCurve:render|(`depth`)

        See function description

        Parameters: ~
            • {depth} (`number`) (default: `5`)
                Number of recursive subdivision steps.

        Return: ~
            (`table`)
                List of x,y-coordinate pairs of points on the curve.
-------------------------------------------------------------------------------
                                        *love2d-docs-BezierCurve:renderSegment*
|BezierCurve:renderSegment|

Get a list of coordinates on a specific part of the curve, to be used with
love.graphics.line.

This function samples the Bézier curve using recursive subdivision. You can
control the recursion depth using the depth parameter.

If you are just need to know the position on the curve given a parameter, use
BezierCurve:evaluate.

Synopses: ~

        `coordinates` = |BezierCurve:renderSegment|(`startpoint`,
        `endpoint`, `depth`)

Variants: ~

        `coordinates` = |BezierCurve:renderSegment|(`startpoint`,
        `endpoint`, `depth`)

        See function description

        Parameters: ~
            • {startpoint} (`number`)
                The starting point along the curve. Must be between 0 and 1.
            • {endpoint} (`number`)
                The end of the segment to render. Must be between 0 and 1.
            • {depth} (`number`) (default: `5`)
                Number of recursive subdivision steps.

        Return: ~
            (`table`)
                List of x,y-coordinate pairs of points on the specified part of
                the curve.
-------------------------------------------------------------------------------
                                               *love2d-docs-BezierCurve:rotate*
|BezierCurve:rotate|

Rotate the Bézier curve by an angle.

Synopses: ~

        |BezierCurve:rotate|(`angle`, `ox`, `oy`)

Variants: ~

        |BezierCurve:rotate|(`angle`, `ox`, `oy`)

        See function description

        Parameters: ~
            • {angle} (`number`)
                Rotation angle in radians.
            • {ox} (`number`) (default: `0`)
                X coordinate of the rotation center.
            • {oy} (`number`) (default: `0`)
                Y coordinate of the rotation center.


-------------------------------------------------------------------------------
                                                *love2d-docs-BezierCurve:scale*
|BezierCurve:scale|

Scale the Bézier curve by a factor.

Synopses: ~

        |BezierCurve:scale|(`s`, `ox`, `oy`)

Variants: ~

        |BezierCurve:scale|(`s`, `ox`, `oy`)

        See function description

        Parameters: ~
            • {s} (`number`)
                Scale factor.
            • {ox} (`number`) (default: `0`)
                X coordinate of the scaling center.
            • {oy} (`number`) (default: `0`)
                Y coordinate of the scaling center.


-------------------------------------------------------------------------------
                                      *love2d-docs-BezierCurve:setControlPoint*
|BezierCurve:setControlPoint|

Set coordinates of the i-th control point. Indices start with 1.

Synopses: ~

        |BezierCurve:setControlPoint|(`i`, `x`, `y`)

Variants: ~

        |BezierCurve:setControlPoint|(`i`, `x`, `y`)

        See function description

        Parameters: ~
            • {i} (`number`)
                Index of the control point.
            • {x} (`number`)
                Position of the control point along the x axis.
            • {y} (`number`)
                Position of the control point along the y axis.


-------------------------------------------------------------------------------
                                            *love2d-docs-BezierCurve:translate*
|BezierCurve:translate|

Move the Bézier curve by an offset.

Synopses: ~

        |BezierCurve:translate|(`dx`, `dy`)

Variants: ~

        |BezierCurve:translate|(`dx`, `dy`)

        See function description

        Parameters: ~
            • {dx} (`number`)
                Offset along the x axis.
            • {dy} (`number`)
                Offset along the y axis.



-------------------------------------------------------------------------------
                                                  *love2d-docs-RandomGenerator*
|RandomGenerator|

A random number generation object which has its own random state.

                                     *love2d-docs-RandomGenerator-constructors*
    constructors:

        |newRandomGenerator|                   |love2d-docs-newRandomGenerator|

                                       *love2d-docs-RandomGenerator-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                         *love2d-docs-RandomGenerator-subtypes*
    subtypes:

        None

                                        *love2d-docs-RandomGenerator-functions*
    functions:

        |getSeed|                         |love2d-docs-RandomGenerator:getSeed|
        |getState|                       |love2d-docs-RandomGenerator:getState|
        |random|                           |love2d-docs-RandomGenerator:random|
        |randomNormal|               |love2d-docs-RandomGenerator:randomNormal|
        |setSeed|                         |love2d-docs-RandomGenerator:setSeed|
        |setState|                       |love2d-docs-RandomGenerator:setState|

-------------------------------------------------------------------------------
                                          *love2d-docs-RandomGenerator:getSeed*
|RandomGenerator:getSeed|

Gets the seed of the random number generator object.

The seed is split into two numbers due to Lua's use of doubles for all number
values - doubles can't accurately represent integer  values above 2^53, but the
seed value is an integer number in the range of 2^64 - 1.

Synopses: ~

        `low`, `high` = |RandomGenerator:getSeed|()

Variants: ~

        `low`, `high` = |RandomGenerator:getSeed|()

        See function description

        Returns: ~
            1. (`number`)
                Integer number representing the lower 32 bits of the
                RandomGenerator's 64 bit seed value.

            2. (`number`)
                Integer number representing the higher 32 bits of the
                RandomGenerator's 64 bit seed value.
-------------------------------------------------------------------------------
                                         *love2d-docs-RandomGenerator:getState*
|RandomGenerator:getState|

Gets the current state of the random number generator. This returns an opaque
string which is only useful for later use with RandomGenerator:setState in the
same major version of LÖVE.

This is different from RandomGenerator:getSeed in that getState gets the
RandomGenerator's current state, whereas getSeed gets the previously set seed
number.

Synopses: ~

        `state` = |RandomGenerator:getState|()

Variants: ~

        `state` = |RandomGenerator:getState|()

        The value of the state string does not depend on the current operating
        system.

        Return: ~
            (`string`)
                The current state of the RandomGenerator object, represented as
                a string.
-------------------------------------------------------------------------------
                                           *love2d-docs-RandomGenerator:random*
|RandomGenerator:random|

Generates a pseudo-random number in a platform independent manner.

Synopses: ~

    1.  `number` = |RandomGenerator:random|()
    2.  `number` = |RandomGenerator:random|(`max`)
    3.  `number` = |RandomGenerator:random|(`min`, `max`)

Variants: ~

    1.  `number` = |RandomGenerator:random|()

    1.          Get uniformly distributed pseudo-random number within 1.

        Return: ~
            (`number`)
                The pseudo-random number.
    2.  `number` = |RandomGenerator:random|(`max`)

    2.          Get uniformly distributed pseudo-random integer number within max.

        Parameters: ~
            • {max} (`number`)
                The maximum possible value it should return.

        Return: ~
            (`number`)
                The pseudo-random integer number.
    3.  `number` = |RandomGenerator:random|(`min`, `max`)

    3.          Get uniformly distributed pseudo-random integer number within max.

        Parameters: ~
            • {min} (`number`)
                The minimum possible value it should return.
            • {max} (`number`)
                The maximum possible value it should return.

        Return: ~
            (`number`)
                The pseudo-random integer number.
-------------------------------------------------------------------------------
                                     *love2d-docs-RandomGenerator:randomNormal*
|RandomGenerator:randomNormal|

Get a normally distributed pseudo random number.

Synopses: ~

        `number` = |RandomGenerator:randomNormal|(`stddev`, `mean`)

Variants: ~

        `number` = |RandomGenerator:randomNormal|(`stddev`, `mean`)

        See function description

        Parameters: ~
            • {stddev} (`number`) (default: `1`)
                Standard deviation of the distribution.
            • {mean} (`number`) (default: `0`)
                The mean of the distribution.

        Return: ~
            (`number`)
                Normally distributed random number with variance (stddev)² and
                the specified mean.
-------------------------------------------------------------------------------
                                          *love2d-docs-RandomGenerator:setSeed*
|RandomGenerator:setSeed|

Sets the seed of the random number generator using the specified integer
number.

Synopses: ~

    1.  |RandomGenerator:setSeed|(`seed`)
    2.  |RandomGenerator:setSeed|(`low`, `high`)

Variants: ~

    1.  |RandomGenerator:setSeed|(`seed`)

    1.          Due to Lua's use of double-precision floating point numbers, values
        above 2^53 cannot be accurately represented. Use the other variant of
        this function if your seed will have a larger value.

        Parameters: ~
            • {seed} (`number`)
                The integer number with which you want to seed the
                randomization. Must be within the range of 2^53.


    2.  |RandomGenerator:setSeed|(`low`, `high`)

    2.          Combines two 32-bit integer numbers into a 64-bit integer value and
        sets the seed of the random number generator using the value.

        Parameters: ~
            • {low} (`number`)
                The lower 32 bits of the seed value. Must be within the range
                of 2^32 - 1.
            • {high} (`number`)
                The higher 32 bits of the seed value. Must be within the range
                of 2^32 - 1.


-------------------------------------------------------------------------------
                                         *love2d-docs-RandomGenerator:setState*
|RandomGenerator:setState|

Sets the current state of the random number generator. The value used as an
argument for this function is an opaque string and should only originate from a
previous call to RandomGenerator:getState in the same major version of LÖVE.

This is different from RandomGenerator:setSeed in that setState directly sets
the RandomGenerator's current implementation-dependent state, whereas setSeed
gives it a new seed value.

Synopses: ~

        |RandomGenerator:setState|(`state`)

Variants: ~

        |RandomGenerator:setState|(`state`)

        The effect of the state string does not depend on the current operating
        system.

        Parameters: ~
            • {state} (`string`)
                The new state of the RandomGenerator object, represented as a
                string. This should originate from a previous call to
                RandomGenerator:getState.



-------------------------------------------------------------------------------
                                                        *love2d-docs-Transform*
|Transform|

Object containing a coordinate system transformation.

The love.graphics module has several functions and function variants which
accept Transform objects.

                                           *love2d-docs-Transform-constructors*
    constructors:

        |newTransform|                               |love2d-docs-newTransform|

                                             *love2d-docs-Transform-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-Transform-subtypes*
    subtypes:

        None

                                              *love2d-docs-Transform-functions*
    functions:

        |apply|                                   |love2d-docs-Transform:apply|
        |clone|                                   |love2d-docs-Transform:clone|
        |getMatrix|                           |love2d-docs-Transform:getMatrix|
        |inverse|                               |love2d-docs-Transform:inverse|
        |inverseTransformPoint|   |love2d-docs-Transform:inverseTransformPoint|
        |isAffine2DTransform|       |love2d-docs-Transform:isAffine2DTransform|
        |reset|                                   |love2d-docs-Transform:reset|
        |rotate|                                 |love2d-docs-Transform:rotate|
        |scale|                                   |love2d-docs-Transform:scale|
        |setMatrix|                           |love2d-docs-Transform:setMatrix|
        |setTransformation|           |love2d-docs-Transform:setTransformation|
        |shear|                                   |love2d-docs-Transform:shear|
        |transformPoint|                 |love2d-docs-Transform:transformPoint|
        |translate|                           |love2d-docs-Transform:translate|

-------------------------------------------------------------------------------
                                                  *love2d-docs-Transform:apply*
|Transform:apply|

Applies the given other Transform object to this one.

This effectively multiplies this Transform's internal transformation matrix
with the other Transform's (i.e. self * other), and stores the result in this
object.

Synopses: ~

        `transform` = |Transform:apply|(`other`)

Variants: ~

        `transform` = |Transform:apply|(`other`)

        See function description

        Parameters: ~
            • {other} (`Transform`) |love2d-docs-Transform|
                The other Transform object to apply to this Transform.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Transform:clone*
|Transform:clone|

Creates a new copy of this Transform.

Synopses: ~

        `clone` = |Transform:clone|()

Variants: ~

        `clone` = |Transform:clone|()

        See function description

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The copy of this Transform.
-------------------------------------------------------------------------------
                                              *love2d-docs-Transform:getMatrix*
|Transform:getMatrix|

Gets the internal 4x4 transformation matrix stored by this Transform. The
matrix is returned in row-major order.

Synopses: ~

        `e1_1`, `e1_2`, `e1_3`, `e1_4`, `e2_1`, `e2_2`, `e2_3`, `e2_4`,
        `e3_1`, `e3_2`, `e3_3`, `e3_4`, `e4_1`, `e4_2`, `e4_3`, `e4_4` =
        |Transform:getMatrix|()

Variants: ~

        `e1_1`, `e1_2`, `e1_3`, `e1_4`, `e2_1`, `e2_2`, `e2_3`, `e2_4`,
        `e3_1`, `e3_2`, `e3_3`, `e3_4`, `e4_1`, `e4_2`, `e4_3`, `e4_4` =
        |Transform:getMatrix|()

        See function description

        Returns: ~
            1. (`number`)
                The first column of the first row of the matrix.

            2. (`number`)
                The second column of the first row of the matrix.

            3. (`number`)
                The third column of the first row of the matrix.

            4. (`number`)
                The fourth column of the first row of the matrix.

            5. (`number`)
                The first column of the second row of the matrix.

            6. (`number`)
                The second column of the second row of the matrix.

            7. (`number`)
                The third column of the second row of the matrix.

            8. (`number`)
                The fourth column of the second row of the matrix.

            9. (`number`)
                The first column of the third row of the matrix.

            10. (`number`)
                The second column of the third row of the matrix.

            11. (`number`)
                The third column of the third row of the matrix.

            12. (`number`)
                The fourth column of the third row of the matrix.

            13. (`number`)
                The first column of the fourth row of the matrix.

            14. (`number`)
                The second column of the fourth row of the matrix.

            15. (`number`)
                The third column of the fourth row of the matrix.

            16. (`number`)
                The fourth column of the fourth row of the matrix.
-------------------------------------------------------------------------------
                                                *love2d-docs-Transform:inverse*
|Transform:inverse|

Creates a new Transform containing the inverse of this Transform.

Synopses: ~

        `inverse` = |Transform:inverse|()

Variants: ~

        `inverse` = |Transform:inverse|()

        See function description

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                A new Transform object representing the inverse of this
                Transform's matrix.
-------------------------------------------------------------------------------
                                  *love2d-docs-Transform:inverseTransformPoint*
|Transform:inverseTransformPoint|

Applies the reverse of the Transform object's transformation to the given 2D
position.

This effectively converts the given position from the local coordinate space of
the Transform into global coordinates.

One use of this method can be to convert a screen-space mouse position into
global world coordinates, if the given Transform has transformations applied
that are used for a camera system in-game.

Synopses: ~

        `globalX`, `globalY` =
        |Transform:inverseTransformPoint|(`localX`, `localY`)

Variants: ~

        `globalX`, `globalY` =
        |Transform:inverseTransformPoint|(`localX`, `localY`)

        See function description

        Parameters: ~
            • {localX} (`number`)
                The x component of the position with the transform applied.
            • {localY} (`number`)
                The y component of the position with the transform applied.

        Returns: ~
            1. (`number`)
                The x component of the position in global coordinates.

            2. (`number`)
                The y component of the position in global coordinates.
-------------------------------------------------------------------------------
                                    *love2d-docs-Transform:isAffine2DTransform*
|Transform:isAffine2DTransform|

Checks whether the Transform is an affine transformation.

Synopses: ~

        `affine` = |Transform:isAffine2DTransform|()

Variants: ~

        `affine` = |Transform:isAffine2DTransform|()

        See function description

        Return: ~
            (`boolean`)
                true if the transform object is an affine transformation, false
                otherwise.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Transform:reset*
|Transform:reset|

Resets the Transform to an identity state. All previously applied
transformations are erased.

Synopses: ~

        `transform` = |Transform:reset|()

Variants: ~

        `transform` = |Transform:reset|()

        See function description

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Transform:rotate*
|Transform:rotate|

Applies a rotation to the Transform's coordinate system. This method does not
reset any previously applied transformations.

Synopses: ~

        `transform` = |Transform:rotate|(`angle`)

Variants: ~

        `transform` = |Transform:rotate|(`angle`)

        See function description

        Parameters: ~
            • {angle} (`number`)
                The relative angle in radians to rotate this Transform by.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Transform:scale*
|Transform:scale|

Scales the Transform's coordinate system. This method does not reset any
previously applied transformations.

Synopses: ~

        `transform` = |Transform:scale|(`sx`, `sy`)

Variants: ~

        `transform` = |Transform:scale|(`sx`, `sy`)

        See function description

        Parameters: ~
            • {sx} (`number`)
                The relative scale factor along the x-axis.
            • {sy} (`number`) (default: `sx`)
                The relative scale factor along the y-axis.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                              *love2d-docs-Transform:setMatrix*
|Transform:setMatrix|

Directly sets the Transform's internal 4x4 transformation matrix.

Synopses: ~

    1.  `transform` = |Transform:setMatrix|(`e1_1`, `e1_2`, `e1_3`,
        `e1_4`, `e2_1`, `e2_2`, `e2_3`, `e2_4`, `e3_1`, `e3_2`, `e3_3`, `e3_4`,
        `e4_1`, `e4_2`, `e4_3`, `e4_4`)
    2.  `transform` = |Transform:setMatrix|(`layout`, `e1_1`, `e1_2`,
        `e1_3`, `e1_4`, `e2_1`, `e2_2`, `e2_3`, `e2_4`, `e3_1`, `e3_2`, `e3_3`,
        `e3_4`, `e4_1`, `e4_2`, `e4_3`, `e4_4`)
    3.  `transform` = |Transform:setMatrix|(`layout`, `matrix`)
    4.  `transform` = |Transform:setMatrix|(`layout`, `matrix`)

Variants: ~

    1.  `transform` = |Transform:setMatrix|(`e1_1`, `e1_2`, `e1_3`,
        `e1_4`, `e2_1`, `e2_2`, `e2_3`, `e2_4`, `e3_1`, `e3_2`, `e3_3`, `e3_4`,
        `e4_1`, `e4_2`, `e4_3`, `e4_4`)

    1.          See function description

        Parameters: ~
            • {e1_1} (`number`)
                The first column of the first row of the matrix.
            • {e1_2} (`number`)
                The second column of the first row of the matrix.
            • {e1_3} (`number`)
                The third column of the first row of the matrix.
            • {e1_4} (`number`)
                The fourth column of the first row of the matrix.
            • {e2_1} (`number`)
                The first column of the second row of the matrix.
            • {e2_2} (`number`)
                The second column of the second row of the matrix.
            • {e2_3} (`number`)
                The third column of the second row of the matrix.
            • {e2_4} (`number`)
                The fourth column of the second row of the matrix.
            • {e3_1} (`number`)
                The first column of the third row of the matrix.
            • {e3_2} (`number`)
                The second column of the third row of the matrix.
            • {e3_3} (`number`)
                The third column of the third row of the matrix.
            • {e3_4} (`number`)
                The fourth column of the third row of the matrix.
            • {e4_1} (`number`)
                The first column of the fourth row of the matrix.
            • {e4_2} (`number`)
                The second column of the fourth row of the matrix.
            • {e4_3} (`number`)
                The third column of the fourth row of the matrix.
            • {e4_4} (`number`)
                The fourth column of the fourth row of the matrix.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
    2.  `transform` = |Transform:setMatrix|(`layout`, `e1_1`, `e1_2`,
        `e1_3`, `e1_4`, `e2_1`, `e2_2`, `e2_3`, `e2_4`, `e3_1`, `e3_2`, `e3_3`,
        `e3_4`, `e4_1`, `e4_2`, `e4_3`, `e4_4`)

    2.          See function description

        Parameters: ~
            • {layout} (`MatrixLayout`) |love2d-docs-MatrixLayout|
                How to interpret the matrix element arguments (row-major or
                column-major).
            • {e1_1} (`number`)
                The first column of the first row of the matrix.
            • {e1_2} (`number`)
                The second column of the first row or the first column of the
                second row of the matrix, depending on the specified layout.
            • {e1_3} (`number`)
                The third column/row of the first row/column of the matrix.
            • {e1_4} (`number`)
                The fourth column/row of the first row/column of the matrix.
            • {e2_1} (`number`)
                The first column/row of the second row/column of the matrix.
            • {e2_2} (`number`)
                The second column/row of the second row/column of the matrix.
            • {e2_3} (`number`)
                The third column/row of the second row/column of the matrix.
            • {e2_4} (`number`)
                The fourth column/row of the second row/column of the matrix.
            • {e3_1} (`number`)
                The first column/row of the third row/column of the matrix.
            • {e3_2} (`number`)
                The second column/row of the third row/column of the matrix.
            • {e3_3} (`number`)
                The third column/row of the third row/column of the matrix.
            • {e3_4} (`number`)
                The fourth column/row of the third row/column of the matrix.
            • {e4_1} (`number`)
                The first column/row of the fourth row/column of the matrix.
            • {e4_2} (`number`)
                The second column/row of the fourth row/column of the matrix.
            • {e4_3} (`number`)
                The third column/row of the fourth row/column of the matrix.
            • {e4_4} (`number`)
                The fourth column of the fourth row of the matrix.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
    3.  `transform` = |Transform:setMatrix|(`layout`, `matrix`)

    3.          See function description

        Parameters: ~
            • {layout} (`MatrixLayout`) |love2d-docs-MatrixLayout|
                How to interpret the matrix element arguments (row-major or
                column-major).
            • {matrix} (`table`)
                A flat table containing the 16 matrix elements.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
    4.  `transform` = |Transform:setMatrix|(`layout`, `matrix`)

    4.          See function description

        Parameters: ~
            • {layout} (`MatrixLayout`) |love2d-docs-MatrixLayout|
                How to interpret the matrix element arguments (row-major or
                column-major).
            • {matrix} (`table`)
                A table of 4 tables, with each sub-table containing 4 matrix
                elements.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                      *love2d-docs-Transform:setTransformation*
|Transform:setTransformation|

Resets the Transform to the specified transformation parameters.

Synopses: ~

        `transform` = |Transform:setTransformation|(`x`, `y`, `angle`,
        `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

Variants: ~

        `transform` = |Transform:setTransformation|(`x`, `y`, `angle`,
        `sx`, `sy`, `ox`, `oy`, `kx`, `ky`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The position of the Transform on the x-axis.
            • {y} (`number`)
                The position of the Transform on the y-axis.
            • {angle} (`number`) (default: `0`)
                The orientation of the Transform in radians.
            • {sx} (`number`) (default: `1`)
                Scale factor on the x-axis.
            • {sy} (`number`) (default: `sx`)
                Scale factor on the y-axis.
            • {ox} (`number`) (default: `0`)
                Origin offset on the x-axis.
            • {oy} (`number`) (default: `0`)
                Origin offset on the y-axis.
            • {kx} (`number`) (default: `0`)
                Shearing / skew factor on the x-axis.
            • {ky} (`number`) (default: `0`)
                Shearing / skew factor on the y-axis.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Transform:shear*
|Transform:shear|

Applies a shear factor (skew) to the Transform's coordinate system. This method
does not reset any previously applied transformations.

Synopses: ~

        `transform` = |Transform:shear|(`kx`, `ky`)

Variants: ~

        `transform` = |Transform:shear|(`kx`, `ky`)

        See function description

        Parameters: ~
            • {kx} (`number`)
                The shear factor along the x-axis.
            • {ky} (`number`)
                The shear factor along the y-axis.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.
-------------------------------------------------------------------------------
                                         *love2d-docs-Transform:transformPoint*
|Transform:transformPoint|

Applies the Transform object's transformation to the given 2D position.

This effectively converts the given position from global coordinates into the
local coordinate space of the Transform.

Synopses: ~

        `localX`, `localY` = |Transform:transformPoint|(`globalX`,
        `globalY`)

Variants: ~

        `localX`, `localY` = |Transform:transformPoint|(`globalX`,
        `globalY`)

        See function description

        Parameters: ~
            • {globalX} (`number`)
                The x component of the position in global coordinates.
            • {globalY} (`number`)
                The y component of the position in global coordinates.

        Returns: ~
            1. (`number`)
                The x component of the position with the transform applied.

            2. (`number`)
                The y component of the position with the transform applied.
-------------------------------------------------------------------------------
                                              *love2d-docs-Transform:translate*
|Transform:translate|

Applies a translation to the Transform's coordinate system. This method does
not reset any previously applied transformations.

Synopses: ~

        `transform` = |Transform:translate|(`dx`, `dy`)

Variants: ~

        `transform` = |Transform:translate|(`dx`, `dy`)

        See function description

        Parameters: ~
            • {dx} (`number`)
                The relative translation along the x-axis.
            • {dy} (`number`)
                The relative translation along the y-axis.

        Return: ~
            (`Transform`) |love2d-docs-Transform|
                The Transform object the method was called on. Allows easily
                chaining Transform methods.

===============================================================================
                                                       *love2d-docs-love.mouse*
|love.mouse|

Provides an interface to the user's mouse.

    |callbacks|                              |love2d-docs-love.mouse-callbacks|
    |enums|                                      |love2d-docs-love.mouse-enums|
    |functions|                              |love2d-docs-love.mouse-functions|
    |types|                                      |love2d-docs-love.mouse-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse-callbacks*
The callbacks of |love.mouse|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.mouse-enums*
The enums of |love.mouse|:

    |CursorType|                                       |love2d-docs-CursorType|

-------------------------------------------------------------------------------
                                                       *love2d-docs-CursorType*
|CursorType|

Types of hardware cursors.

    constants: ~

        `image`: string

            The cursor is using a custom image.

        `arrow`: string

            An arrow pointer.

        `ibeam`: string

            An I-beam, normally used when mousing over editable or selectable
            text.

        `wait`: string

            Wait graphic.

        `waitarrow`: string

            Small wait cursor with an arrow pointer.

        `crosshair`: string

            Crosshair symbol.

        `sizenwse`: string

            Double arrow pointing to the top-left and bottom-right.

        `sizenesw`: string

            Double arrow pointing to the top-right and bottom-left.

        `sizewe`: string

            Double arrow pointing left and right.

        `sizens`: string

            Double arrow pointing up and down.

        `sizeall`: string

            Four-pointed arrow pointing up, down, left, and right.

        `no`: string

            Slashed circle or crossbones.

        `hand`: string

            Hand symbol.

-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse-functions*
The functions of |love.mouse|:

    |getCursor|                              |love2d-docs-love.mouse.getCursor|
    |getPosition|                          |love2d-docs-love.mouse.getPosition|
    |getRelativeMode|                  |love2d-docs-love.mouse.getRelativeMode|
    |getSystemCursor|                  |love2d-docs-love.mouse.getSystemCursor|
    |getX|                                        |love2d-docs-love.mouse.getX|
    |getY|                                        |love2d-docs-love.mouse.getY|
    |isCursorSupported|              |love2d-docs-love.mouse.isCursorSupported|
    |isDown|                                    |love2d-docs-love.mouse.isDown|
    |isGrabbed|                              |love2d-docs-love.mouse.isGrabbed|
    |isVisible|                              |love2d-docs-love.mouse.isVisible|
    |newCursor|                              |love2d-docs-love.mouse.newCursor|
    |setCursor|                              |love2d-docs-love.mouse.setCursor|
    |setGrabbed|                            |love2d-docs-love.mouse.setGrabbed|
    |setPosition|                          |love2d-docs-love.mouse.setPosition|
    |setRelativeMode|                  |love2d-docs-love.mouse.setRelativeMode|
    |setVisible|                            |love2d-docs-love.mouse.setVisible|
    |setX|                                        |love2d-docs-love.mouse.setX|
    |setY|                                        |love2d-docs-love.mouse.setY|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse.getCursor*
|love.mouse.getCursor|

Gets the current Cursor.

Synopses: ~

        `cursor` = |love.mouse.getCursor|()

Variants: ~

        `cursor` = |love.mouse.getCursor|()

        See function description

        Return: ~
            (`Cursor`) |love2d-docs-Cursor|
                The current cursor, or nil if no cursor is set.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.mouse.getPosition*
|love.mouse.getPosition|

Returns the current position of the mouse.

Synopses: ~

        `x`, `y` = |love.mouse.getPosition|()

Variants: ~

        `x`, `y` = |love.mouse.getPosition|()

        See function description

        Returns: ~
            1. (`number`)
                The position of the mouse along the x-axis.

            2. (`number`)
                The position of the mouse along the y-axis.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.mouse.getRelativeMode*
|love.mouse.getRelativeMode|

Gets whether relative mode is enabled for the mouse.

If relative mode is enabled, the cursor is hidden and doesn't move when the
mouse does, but relative mouse motion events are still generated via
love.mousemoved. This lets the mouse move in any direction indefinitely without
the cursor getting stuck at the edges of the screen.

The reported position of the mouse is not updated while relative mode is
enabled, even when relative mouse motion events are generated.

Synopses: ~

        `enabled` = |love.mouse.getRelativeMode|()

Variants: ~

        `enabled` = |love.mouse.getRelativeMode|()

        See function description

        Return: ~
            (`boolean`)
                True if relative mode is enabled, false if it's disabled.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.mouse.getSystemCursor*
|love.mouse.getSystemCursor|

Gets a Cursor object representing a system-native hardware cursor.

Hardware cursors are framerate-independent and work the same way as normal
operating system cursors. Unlike drawing an image at the mouse's current
coordinates, hardware cursors never have visible lag between when the mouse is
moved and when the cursor position updates, even at low framerates.

Synopses: ~

        `cursor` = |love.mouse.getSystemCursor|(`ctype`)

Variants: ~

        `cursor` = |love.mouse.getSystemCursor|(`ctype`)

        The 'image' CursorType is not a valid argument. Use
        love.mouse.newCursor to create a hardware cursor using a custom image.

        Parameters: ~
            • {ctype} (`CursorType`) |love2d-docs-CursorType|
                The type of system cursor to get.

        Return: ~
            (`Cursor`) |love2d-docs-Cursor|
                The Cursor object representing the system cursor type.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.mouse.getX*
|love.mouse.getX|

Returns the current x-position of the mouse.

Synopses: ~

        `x` = |love.mouse.getX|()

Variants: ~

        `x` = |love.mouse.getX|()

        See function description

        Return: ~
            (`number`)
                The position of the mouse along the x-axis.
-------------------------------------------------------------------------------
                                                  *love2d-docs-love.mouse.getY*
|love.mouse.getY|

Returns the current y-position of the mouse.

Synopses: ~

        `y` = |love.mouse.getY|()

Variants: ~

        `y` = |love.mouse.getY|()

        See function description

        Return: ~
            (`number`)
                The position of the mouse along the y-axis.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.mouse.isCursorSupported*
|love.mouse.isCursorSupported|

Gets whether cursor functionality is supported.

If it isn't supported, calling love.mouse.newCursor and
love.mouse.getSystemCursor will cause an error. Mobile devices do not support
cursors.

Synopses: ~

        `supported` = |love.mouse.isCursorSupported|()

Variants: ~

        `supported` = |love.mouse.isCursorSupported|()

        See function description

        Return: ~
            (`boolean`)
                Whether the system has cursor functionality.
-------------------------------------------------------------------------------
                                                *love2d-docs-love.mouse.isDown*
|love.mouse.isDown|

Checks whether a certain mouse button is down.

This function does not detect mouse wheel scrolling; you must use the
love.wheelmoved (or love.mousepressed in version 0.9.2 and older) callback for
that.

Synopses: ~

        `down` = |love.mouse.isDown|(`button`, `...`)

Variants: ~

        `down` = |love.mouse.isDown|(`button`, `...`)

        See function description

        Parameters: ~
            • {button} (`number`)
                The index of a button to check. 1 is the primary mouse button,
                2 is the secondary mouse button and 3 is the middle button.
                Further buttons are mouse dependant.
            • {...} (`number`)
                Additional button numbers to check.

        Return: ~
            (`boolean`)
                True if any specified button is down.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse.isGrabbed*
|love.mouse.isGrabbed|

Checks if the mouse is grabbed.

Synopses: ~

        `grabbed` = |love.mouse.isGrabbed|()

Variants: ~

        `grabbed` = |love.mouse.isGrabbed|()

        See function description

        Return: ~
            (`boolean`)
                True if the cursor is grabbed, false if it is not.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse.isVisible*
|love.mouse.isVisible|

Checks if the cursor is visible.

Synopses: ~

        `visible` = |love.mouse.isVisible|()

Variants: ~

        `visible` = |love.mouse.isVisible|()

        See function description

        Return: ~
            (`boolean`)
                True if the cursor to visible, false if the cursor is hidden.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse.newCursor*
|love.mouse.newCursor|

Creates a new hardware Cursor object from an image file or ImageData.

Hardware cursors are framerate-independent and work the same way as normal
operating system cursors. Unlike drawing an image at the mouse's current
coordinates, hardware cursors never have visible lag between when the mouse is
moved and when the cursor position updates, even at low framerates.

The hot spot is the point the operating system uses to determine what was
clicked and at what position the mouse cursor is. For example, the normal arrow
pointer normally has its hot spot at the top left of the image, but a crosshair
cursor might have it in the middle.

Synopses: ~

    1.  `cursor` = |love.mouse.newCursor|(`imageData`, `hotx`, `hoty`)
    2.  `cursor` = |love.mouse.newCursor|(`filename`, `hotx`, `hoty`)
    3.  `cursor` = |love.mouse.newCursor|(`fileData`, `hotx`, `hoty`)

Variants: ~

    1.  `cursor` = |love.mouse.newCursor|(`imageData`, `hotx`, `hoty`)

    1.          See function description

        Parameters: ~
            • {imageData} (`ImageData`) |love2d-docs-ImageData|
                The ImageData to use for the new Cursor.
            • {hotx} (`number`) (default: `0`)
                The x-coordinate in the ImageData of the cursor's hot spot.
            • {hoty} (`number`) (default: `0`)
                The y-coordinate in the ImageData of the cursor's hot spot.

        Return: ~
            (`Cursor`) |love2d-docs-Cursor|
                The new Cursor object.
    2.  `cursor` = |love.mouse.newCursor|(`filename`, `hotx`, `hoty`)

    2.          See function description

        Parameters: ~
            • {filename} (`string`)
                Path to the image to use for the new Cursor.
            • {hotx} (`number`) (default: `0`)
                The x-coordinate in the image of the cursor's hot spot.
            • {hoty} (`number`) (default: `0`)
                The y-coordinate in the image of the cursor's hot spot.

        Return: ~
            (`Cursor`) |love2d-docs-Cursor|
                The new Cursor object.
    3.  `cursor` = |love.mouse.newCursor|(`fileData`, `hotx`, `hoty`)

    3.          See function description

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                Data representing the image to use for the new Cursor.
            • {hotx} (`number`) (default: `0`)
                The x-coordinate in the image of the cursor's hot spot.
            • {hoty} (`number`) (default: `0`)
                The y-coordinate in the image of the cursor's hot spot.

        Return: ~
            (`Cursor`) |love2d-docs-Cursor|
                The new Cursor object.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.mouse.setCursor*
|love.mouse.setCursor|

Sets the current mouse cursor.

Synopses: ~

    1.  |love.mouse.setCursor|(`cursor`)
    2.  |love.mouse.setCursor|()

Variants: ~

    1.  |love.mouse.setCursor|(`cursor`)

    1.          See function description

        Parameters: ~
            • {cursor} (`Cursor`) |love2d-docs-Cursor|
                The Cursor object to use as the current mouse cursor.


    2.  |love.mouse.setCursor|()

    2.          Resets the current mouse cursor to the default.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.mouse.setGrabbed*
|love.mouse.setGrabbed|

Grabs the mouse and confines it to the window.

Synopses: ~

        |love.mouse.setGrabbed|(`grab`)

Variants: ~

        |love.mouse.setGrabbed|(`grab`)

        See function description

        Parameters: ~
            • {grab} (`boolean`)
                True to confine the mouse, false to let it leave the window.


-------------------------------------------------------------------------------
                                           *love2d-docs-love.mouse.setPosition*
|love.mouse.setPosition|

Sets the current position of the mouse. Non-integer values are floored.

Synopses: ~

        |love.mouse.setPosition|(`x`, `y`)

Variants: ~

        |love.mouse.setPosition|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The new position of the mouse along the x-axis.
            • {y} (`number`)
                The new position of the mouse along the y-axis.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.mouse.setRelativeMode*
|love.mouse.setRelativeMode|

Sets whether relative mode is enabled for the mouse.

When relative mode is enabled, the cursor is hidden and doesn't move when the
mouse does, but relative mouse motion events are still generated via
love.mousemoved. This lets the mouse move in any direction indefinitely without
the cursor getting stuck at the edges of the screen.

The reported position of the mouse may not be updated while relative mode is
enabled, even when relative mouse motion events are generated.

Synopses: ~

        |love.mouse.setRelativeMode|(`enable`)

Variants: ~

        |love.mouse.setRelativeMode|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable relative mode, false to disable it.


-------------------------------------------------------------------------------
                                            *love2d-docs-love.mouse.setVisible*
|love.mouse.setVisible|

Sets the current visibility of the cursor.

Synopses: ~

        |love.mouse.setVisible|(`visible`)

Variants: ~

        |love.mouse.setVisible|(`visible`)

        See function description

        Parameters: ~
            • {visible} (`boolean`)
                True to set the cursor to visible, false to hide the cursor.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.mouse.setX*
|love.mouse.setX|

Sets the current X position of the mouse.

Non-integer values are floored.

Synopses: ~

        |love.mouse.setX|(`x`)

Variants: ~

        |love.mouse.setX|(`x`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The new position of the mouse along the x-axis.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.mouse.setY*
|love.mouse.setY|

Sets the current Y position of the mouse.

Non-integer values are floored.

Synopses: ~

        |love.mouse.setY|(`y`)

Variants: ~

        |love.mouse.setY|(`y`)

        See function description

        Parameters: ~
            • {y} (`number`)
                The new position of the mouse along the y-axis.


-------------------------------------------------------------------------------
                                                 *love2d-docs-love.mouse-types*
The types of |love.mouse|:

    |Cursor|                                               |love2d-docs-Cursor|

-------------------------------------------------------------------------------
                                                           *love2d-docs-Cursor*
|Cursor|

Represents a hardware cursor.

                                              *love2d-docs-Cursor-constructors*
    constructors:

        |getCursor|                                     |love2d-docs-getCursor|
        |newCursor|                                     |love2d-docs-newCursor|
        |getSystemCursor|                         |love2d-docs-getSystemCursor|

                                                *love2d-docs-Cursor-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                  *love2d-docs-Cursor-subtypes*
    subtypes:

        None

                                                 *love2d-docs-Cursor-functions*
    functions:

        |getType|                                  |love2d-docs-Cursor:getType|

-------------------------------------------------------------------------------
                                                   *love2d-docs-Cursor:getType*
|Cursor:getType|

Gets the type of the Cursor.

Synopses: ~

        `ctype` = |Cursor:getType|()

Variants: ~

        `ctype` = |Cursor:getType|()

        See function description

        Return: ~
            (`CursorType`) |love2d-docs-CursorType|
                The type of the Cursor.

===============================================================================
                                                     *love2d-docs-love.physics*
|love.physics|

Can simulate 2D rigid body physics in a realistic manner. This module is based
on Box2D, and this API corresponds to the Box2D API as closely as possible.

    |callbacks|                            |love2d-docs-love.physics-callbacks|
    |enums|                                    |love2d-docs-love.physics-enums|
    |functions|                            |love2d-docs-love.physics-functions|
    |types|                                    |love2d-docs-love.physics-types|

-------------------------------------------------------------------------------
                                           *love2d-docs-love.physics-callbacks*
The callbacks of |love.physics|:

    None

-------------------------------------------------------------------------------
                                               *love2d-docs-love.physics-enums*
The enums of |love.physics|:

    |BodyType|                                           |love2d-docs-BodyType|
    |JointType|                                         |love2d-docs-JointType|
    |ShapeType|                                         |love2d-docs-ShapeType|

-------------------------------------------------------------------------------
                                                         *love2d-docs-BodyType*
|BodyType|

The types of a Body.

    constants: ~

        `static`: string

            Static bodies do not move.

        `dynamic`: string

            Dynamic bodies collide with all bodies.

        `kinematic`: string

            Kinematic bodies only collide with dynamic bodies.

-------------------------------------------------------------------------------
                                                        *love2d-docs-JointType*
|JointType|

Different types of joints.

    constants: ~

        `distance`: string

            A DistanceJoint.

        `friction`: string

            A FrictionJoint.

        `gear`: string

            A GearJoint.

        `mouse`: string

            A MouseJoint.

        `prismatic`: string

            A PrismaticJoint.

        `pulley`: string

            A PulleyJoint.

        `revolute`: string

            A RevoluteJoint.

        `rope`: string

            A RopeJoint.

        `weld`: string

            A WeldJoint.

-------------------------------------------------------------------------------
                                                        *love2d-docs-ShapeType*
|ShapeType|

The different types of Shapes, as returned by Shape:getType.

    constants: ~

        `circle`: string

            The Shape is a CircleShape.

        `polygon`: string

            The Shape is a PolygonShape.

        `edge`: string

            The Shape is a EdgeShape.

        `chain`: string

            The Shape is a ChainShape.

-------------------------------------------------------------------------------
                                           *love2d-docs-love.physics-functions*
The functions of |love.physics|:

    |getDistance|                        |love2d-docs-love.physics.getDistance|
    |getMeter|                              |love2d-docs-love.physics.getMeter|
    |newBody|                                |love2d-docs-love.physics.newBody|
    |newChainShape|                    |love2d-docs-love.physics.newChainShape|
    |newCircleShape|                  |love2d-docs-love.physics.newCircleShape|
    |newDistanceJoint|              |love2d-docs-love.physics.newDistanceJoint|
    |newEdgeShape|                      |love2d-docs-love.physics.newEdgeShape|
    |newFixture|                          |love2d-docs-love.physics.newFixture|
    |newFrictionJoint|              |love2d-docs-love.physics.newFrictionJoint|
    |newGearJoint|                      |love2d-docs-love.physics.newGearJoint|
    |newMotorJoint|                    |love2d-docs-love.physics.newMotorJoint|
    |newMouseJoint|                    |love2d-docs-love.physics.newMouseJoint|
    |newPolygonShape|                |love2d-docs-love.physics.newPolygonShape|
    |newPrismaticJoint|            |love2d-docs-love.physics.newPrismaticJoint|
    |newPulleyJoint|                  |love2d-docs-love.physics.newPulleyJoint|
    |newRectangleShape|            |love2d-docs-love.physics.newRectangleShape|
    |newRevoluteJoint|              |love2d-docs-love.physics.newRevoluteJoint|
    |newRopeJoint|                      |love2d-docs-love.physics.newRopeJoint|
    |newWeldJoint|                      |love2d-docs-love.physics.newWeldJoint|
    |newWheelJoint|                    |love2d-docs-love.physics.newWheelJoint|
    |newWorld|                              |love2d-docs-love.physics.newWorld|
    |setMeter|                              |love2d-docs-love.physics.setMeter|

-------------------------------------------------------------------------------
                                         *love2d-docs-love.physics.getDistance*
|love.physics.getDistance|

Returns the two closest points between two fixtures and their distance.

Synopses: ~

        `distance`, `x1`, `y1`, `x2`, `y2` =
        |love.physics.getDistance|(`fixture1`, `fixture2`)

Variants: ~

        `distance`, `x1`, `y1`, `x2`, `y2` =
        |love.physics.getDistance|(`fixture1`, `fixture2`)

        See function description

        Parameters: ~
            • {fixture1} (`Fixture`) |love2d-docs-Fixture|
                The first fixture.
            • {fixture2} (`Fixture`) |love2d-docs-Fixture|
                The second fixture.

        Returns: ~
            1. (`number`)
                The distance of the two points.

            2. (`number`)
                The x-coordinate of the first point.

            3. (`number`)
                The y-coordinate of the first point.

            4. (`number`)
                The x-coordinate of the second point.

            5. (`number`)
                The y-coordinate of the second point.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.physics.getMeter*
|love.physics.getMeter|

Returns the meter scale factor.

All coordinates in the physics module are divided by this number, creating a
convenient way to draw the objects directly to the screen without the need for
graphics transformations.

It is recommended to create shapes no larger than 10 times the scale. This is
important because Box2D is tuned to work well with shape sizes from 0.1 to 10
meters.

Synopses: ~

        `scale` = |love.physics.getMeter|()

Variants: ~

        `scale` = |love.physics.getMeter|()

        See function description

        Return: ~
            (`number`)
                The scale factor as an integer.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.physics.newBody*
|love.physics.newBody|

Creates a new body.

There are three types of bodies.

* Static bodies do not move, have a infinite mass, and can be used for level
boundaries.

* Dynamic bodies are the main actors in the simulation, they collide with
everything.

* Kinematic bodies do not react to forces and only collide with dynamic bodies.

The mass of the body gets calculated when a Fixture is attached or removed, but
can be changed at any time with Body:setMass or Body:resetMassData.

Synopses: ~

        `body` = |love.physics.newBody|(`world`, `x`, `y`, `type`)

Variants: ~

        `body` = |love.physics.newBody|(`world`, `x`, `y`, `type`)

        See function description

        Parameters: ~
            • {world} (`World`) |love2d-docs-World|
                The world to create the body in.
            • {x} (`number`) (default: `0`)
                The x position of the body.
            • {y} (`number`) (default: `0`)
                The y position of the body.
            • {type} (`BodyType`) |love2d-docs-BodyType| (default: `'static'`)
                The type of the body.

        Return: ~
            (`Body`) |love2d-docs-Body|
                A new body.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.physics.newChainShape*
|love.physics.newChainShape|

Creates a new ChainShape.

Synopses: ~

    1.  `shape` = |love.physics.newChainShape|(`loop`, `x1`, `y1`,
        `x2`, `y2`, `...`)
    2.  `shape` = |love.physics.newChainShape|(`loop`, `points`)

Variants: ~

    1.  `shape` = |love.physics.newChainShape|(`loop`, `x1`, `y1`,
        `x2`, `y2`, `...`)

    1.          See function description

        Parameters: ~
            • {loop} (`boolean`)
                If the chain should loop back to the first point.
            • {x1} (`number`)
                The x position of the first point.
            • {y1} (`number`)
                The y position of the first point.
            • {x2} (`number`)
                The x position of the second point.
            • {y2} (`number`)
                The y position of the second point.
            • {...} (`number`)
                Additional point positions.

        Return: ~
            (`ChainShape`) |love2d-docs-ChainShape|
                The new shape.
    2.  `shape` = |love.physics.newChainShape|(`loop`, `points`)

    2.          See function description

        Parameters: ~
            • {loop} (`boolean`)
                If the chain should loop back to the first point.
            • {points} (`table`)
                A list of points to construct the ChainShape, in the form of
                {x1, y1, x2, y2, ...}.

        Return: ~
            (`ChainShape`) |love2d-docs-ChainShape|
                The new shape.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.physics.newCircleShape*
|love.physics.newCircleShape|

Creates a new CircleShape.

Synopses: ~

    1.  `shape` = |love.physics.newCircleShape|(`radius`)
    2.  `shape` = |love.physics.newCircleShape|(`x`, `y`, `radius`)

Variants: ~

    1.  `shape` = |love.physics.newCircleShape|(`radius`)

    1.          See function description

        Parameters: ~
            • {radius} (`number`)
                The radius of the circle.

        Return: ~
            (`CircleShape`) |love2d-docs-CircleShape|
                The new shape.
    2.  `shape` = |love.physics.newCircleShape|(`x`, `y`, `radius`)

    2.          See function description

        Parameters: ~
            • {x} (`number`)
                The x position of the circle.
            • {y} (`number`)
                The y position of the circle.
            • {radius} (`number`)
                The radius of the circle.

        Return: ~
            (`CircleShape`) |love2d-docs-CircleShape|
                The new shape.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.physics.newDistanceJoint*
|love.physics.newDistanceJoint|

Creates a DistanceJoint between two bodies.

This joint constrains the distance between two points on two bodies to be
constant. These two points are specified in world coordinates and the two
bodies are assumed to be in place when this joint is created. The first anchor
point is connected to the first body and the second to the second body, and the
points define the length of the distance joint.

Synopses: ~

        `joint` = |love.physics.newDistanceJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `collideConnected`)

Variants: ~

        `joint` = |love.physics.newDistanceJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `collideConnected`)

        See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x1} (`number`)
                The x position of the first anchor point (world space).
            • {y1} (`number`)
                The y position of the first anchor point (world space).
            • {x2} (`number`)
                The x position of the second anchor point (world space).
            • {y2} (`number`)
                The y position of the second anchor point (world space).
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`DistanceJoint`) |love2d-docs-DistanceJoint|
                The new distance joint.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.physics.newEdgeShape*
|love.physics.newEdgeShape|

Creates a new EdgeShape.

Synopses: ~

        `shape` = |love.physics.newEdgeShape|(`x1`, `y1`, `x2`, `y2`)

Variants: ~

        `shape` = |love.physics.newEdgeShape|(`x1`, `y1`, `x2`, `y2`)

        See function description

        Parameters: ~
            • {x1} (`number`)
                The x position of the first point.
            • {y1} (`number`)
                The y position of the first point.
            • {x2} (`number`)
                The x position of the second point.
            • {y2} (`number`)
                The y position of the second point.

        Return: ~
            (`EdgeShape`) |love2d-docs-EdgeShape|
                The new shape.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.physics.newFixture*
|love.physics.newFixture|

Creates and attaches a Fixture to a body.

Note that the Shape object is copied rather than kept as a reference when the
Fixture is created. To get the Shape object that the Fixture owns, use
Fixture:getShape.

Synopses: ~

        `fixture` = |love.physics.newFixture|(`body`, `shape`,
        `density`)

Variants: ~

        `fixture` = |love.physics.newFixture|(`body`, `shape`,
        `density`)

        See function description

        Parameters: ~
            • {body} (`Body`) |love2d-docs-Body|
                The body which gets the fixture attached.
            • {shape} (`Shape`) |love2d-docs-Shape|
                The shape to be copied to the fixture.
            • {density} (`number`) (default: `1`)
                The density of the fixture.

        Return: ~
            (`Fixture`) |love2d-docs-Fixture|
                The new fixture.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.physics.newFrictionJoint*
|love.physics.newFrictionJoint|

Create a friction joint between two bodies. A FrictionJoint applies friction to
a body.

Synopses: ~

    1.  `joint` = |love.physics.newFrictionJoint|(`body1`, `body2`,
        `x`, `y`, `collideConnected`)
    2.  `joint` = |love.physics.newFrictionJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `collideConnected`)

Variants: ~

    1.  `joint` = |love.physics.newFrictionJoint|(`body1`, `body2`,
        `x`, `y`, `collideConnected`)

    1.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x} (`number`)
                The x position of the anchor point.
            • {y} (`number`)
                The y position of the anchor point.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`FrictionJoint`) |love2d-docs-FrictionJoint|
                The new FrictionJoint.
    2.  `joint` = |love.physics.newFrictionJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `collideConnected`)

    2.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x1} (`number`)
                The x position of the first anchor point.
            • {y1} (`number`)
                The y position of the first anchor point.
            • {x2} (`number`)
                The x position of the second anchor point.
            • {y2} (`number`)
                The y position of the second anchor point.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`FrictionJoint`) |love2d-docs-FrictionJoint|
                The new FrictionJoint.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.physics.newGearJoint*
|love.physics.newGearJoint|

Create a GearJoint connecting two Joints.

The gear joint connects two joints that must be either  prismatic or  revolute
joints. Using this joint requires that the joints it uses connect their
respective bodies to the ground and have the ground as the first body. When
destroying the bodies and joints you must make sure you destroy the gear joint
before the other joints.

The gear joint has a ratio the determines how the angular or distance values of
the connected joints relate to each other. The formula coordinate1 + ratio *
coordinate2 always has a constant value that is set when the gear joint is
created.

Synopses: ~

        `joint` = |love.physics.newGearJoint|(`joint1`, `joint2`,
        `ratio`, `collideConnected`)

Variants: ~

        `joint` = |love.physics.newGearJoint|(`joint1`, `joint2`,
        `ratio`, `collideConnected`)

        See function description

        Parameters: ~
            • {joint1} (`Joint`) |love2d-docs-Joint|
                The first joint to connect with a gear joint.
            • {joint2} (`Joint`) |love2d-docs-Joint|
                The second joint to connect with a gear joint.
            • {ratio} (`number`) (default: `1`)
                The gear ratio.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`GearJoint`) |love2d-docs-GearJoint|
                The new gear joint.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.physics.newMotorJoint*
|love.physics.newMotorJoint|

Creates a joint between two bodies which controls the relative motion between
them.

Position and rotation offsets can be specified once the MotorJoint has been
created, as well as the maximum motor force and torque that will be be applied
to reach the target offsets.

Synopses: ~

    1.  `joint` = |love.physics.newMotorJoint|(`body1`, `body2`,
        `correctionFactor`)
    2.  `joint` = |love.physics.newMotorJoint|(`body1`, `body2`,
        `correctionFactor`, `collideConnected`)

Variants: ~

    1.  `joint` = |love.physics.newMotorJoint|(`body1`, `body2`,
        `correctionFactor`)

    1.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {correctionFactor} (`number`) (default: `0.3`)
                The joint's initial position correction factor, in the range of
                1.

        Return: ~
            (`MotorJoint`) |love2d-docs-MotorJoint|
                The new MotorJoint.
    2.  `joint` = |love.physics.newMotorJoint|(`body1`, `body2`,
        `correctionFactor`, `collideConnected`)

    2.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {correctionFactor} (`number`) (default: `0.3`)
                The joint's initial position correction factor, in the range of
                1.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`MotorJoint`) |love2d-docs-MotorJoint|
                The new MotorJoint.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.physics.newMouseJoint*
|love.physics.newMouseJoint|

Create a joint between a body and the mouse.

This joint actually connects the body to a fixed point in the world. To make it
follow the mouse, the fixed point must be updated every timestep (example
below).

The advantage of using a MouseJoint instead of just changing a body position
directly is that collisions and reactions to other joints are handled by the
physics engine.

Synopses: ~

        `joint` = |love.physics.newMouseJoint|(`body`, `x`, `y`)

Variants: ~

        `joint` = |love.physics.newMouseJoint|(`body`, `x`, `y`)

        See function description

        Parameters: ~
            • {body} (`Body`) |love2d-docs-Body|
                The body to attach to the mouse.
            • {x} (`number`)
                The x position of the connecting point.
            • {y} (`number`)
                The y position of the connecting point.

        Return: ~
            (`MouseJoint`) |love2d-docs-MouseJoint|
                The new mouse joint.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.physics.newPolygonShape*
|love.physics.newPolygonShape|

Creates a new PolygonShape.

This shape can have 8 vertices at most, and must form a convex shape.

Synopses: ~

    1.  `shape` = |love.physics.newPolygonShape|(`x1`, `y1`, `x2`,
        `y2`, `x3`, `y3`, `...`)
    2.  `shape` = |love.physics.newPolygonShape|(`vertices`)

Variants: ~

    1.  `shape` = |love.physics.newPolygonShape|(`x1`, `y1`, `x2`,
        `y2`, `x3`, `y3`, `...`)

    1.          See function description

        Parameters: ~
            • {x1} (`number`)
                The x position of the first point.
            • {y1} (`number`)
                The y position of the first point.
            • {x2} (`number`)
                The x position of the second point.
            • {y2} (`number`)
                The y position of the second point.
            • {x3} (`number`)
                The x position of the third point.
            • {y3} (`number`)
                The y position of the third point.
            • {...} (`number`)
                You can continue passing more point positions to create the
                PolygonShape.

        Return: ~
            (`PolygonShape`) |love2d-docs-PolygonShape|
                A new PolygonShape.
    2.  `shape` = |love.physics.newPolygonShape|(`vertices`)

    2.          See function description

        Parameters: ~
            • {vertices} (`table`)
                A list of vertices to construct the polygon, in the form of
                {x1, y1, x2, y2, x3, y3, ...}.

        Return: ~
            (`PolygonShape`) |love2d-docs-PolygonShape|
                A new PolygonShape.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.physics.newPrismaticJoint*
|love.physics.newPrismaticJoint|

Creates a PrismaticJoint between two bodies.

A prismatic joint constrains two bodies to move relatively to each other on a
specified axis. It does not allow for relative rotation. Its definition and
operation are similar to a  revolute joint, but with translation and force
substituted for angle and torque.

Synopses: ~

    1.  `joint` = |love.physics.newPrismaticJoint|(`body1`, `body2`,
        `x`, `y`, `ax`, `ay`, `collideConnected`)
    2.  `joint` = |love.physics.newPrismaticJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `ax`, `ay`, `collideConnected`)
    3.  `joint` = |love.physics.newPrismaticJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `ax`, `ay`, `collideConnected`,
        `referenceAngle`)

Variants: ~

    1.  `joint` = |love.physics.newPrismaticJoint|(`body1`, `body2`,
        `x`, `y`, `ax`, `ay`, `collideConnected`)

    1.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to connect with a prismatic joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to connect with a prismatic joint.
            • {x} (`number`)
                The x coordinate of the anchor point.
            • {y} (`number`)
                The y coordinate of the anchor point.
            • {ax} (`number`)
                The x coordinate of the axis vector.
            • {ay} (`number`)
                The y coordinate of the axis vector.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`PrismaticJoint`) |love2d-docs-PrismaticJoint|
                The new prismatic joint.
    2.  `joint` = |love.physics.newPrismaticJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `ax`, `ay`, `collideConnected`)

    2.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to connect with a prismatic joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to connect with a prismatic joint.
            • {x1} (`number`)
                The x coordinate of the first anchor point.
            • {y1} (`number`)
                The y coordinate of the first anchor point.
            • {x2} (`number`)
                The x coordinate of the second anchor point.
            • {y2} (`number`)
                The y coordinate of the second anchor point.
            • {ax} (`number`)
                The x coordinate of the axis unit vector.
            • {ay} (`number`)
                The y coordinate of the axis unit vector.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`PrismaticJoint`) |love2d-docs-PrismaticJoint|
                The new prismatic joint.
    3.  `joint` = |love.physics.newPrismaticJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `ax`, `ay`, `collideConnected`,
        `referenceAngle`)

    3.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to connect with a prismatic joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to connect with a prismatic joint.
            • {x1} (`number`)
                The x coordinate of the first anchor point.
            • {y1} (`number`)
                The y coordinate of the first anchor point.
            • {x2} (`number`)
                The x coordinate of the second anchor point.
            • {y2} (`number`)
                The y coordinate of the second anchor point.
            • {ax} (`number`)
                The x coordinate of the axis unit vector.
            • {ay} (`number`)
                The y coordinate of the axis unit vector.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.
            • {referenceAngle} (`number`) (default: `0`)
                The reference angle between body1 and body2, in radians.

        Return: ~
            (`PrismaticJoint`) |love2d-docs-PrismaticJoint|
                The new prismatic joint.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.physics.newPulleyJoint*
|love.physics.newPulleyJoint|

Creates a PulleyJoint to join two bodies to each other and the ground.

The pulley joint simulates a pulley with an optional block and tackle. If the
ratio parameter has a value different from one, then the simulated rope extends
faster on one side than the other. In a pulley joint the total length of the
simulated rope is the constant length1 + ratio * length2, which is set when the
pulley joint is created.

Pulley joints can behave unpredictably if one side is fully extended. It is
recommended that the method  setMaxLengths  be used to constrain the maximum
lengths each side can attain.

Synopses: ~

        `joint` = |love.physics.newPulleyJoint|(`body1`, `body2`,
        `gx1`, `gy1`, `gx2`, `gy2`, `x1`, `y1`, `x2`, `y2`, `ratio`,
        `collideConnected`)

Variants: ~

        `joint` = |love.physics.newPulleyJoint|(`body1`, `body2`,
        `gx1`, `gy1`, `gx2`, `gy2`, `x1`, `y1`, `x2`, `y2`, `ratio`,
        `collideConnected`)

        See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to connect with a pulley joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to connect with a pulley joint.
            • {gx1} (`number`)
                The x coordinate of the first body's ground anchor.
            • {gy1} (`number`)
                The y coordinate of the first body's ground anchor.
            • {gx2} (`number`)
                The x coordinate of the second body's ground anchor.
            • {gy2} (`number`)
                The y coordinate of the second body's ground anchor.
            • {x1} (`number`)
                The x coordinate of the pulley joint anchor in the first body.
            • {y1} (`number`)
                The y coordinate of the pulley joint anchor in the first body.
            • {x2} (`number`)
                The x coordinate of the pulley joint anchor in the second body.
            • {y2} (`number`)
                The y coordinate of the pulley joint anchor in the second body.
            • {ratio} (`number`) (default: `1`)
                The joint ratio.
            • {collideConnected} (`boolean`) (default: `true`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`PulleyJoint`) |love2d-docs-PulleyJoint|
                The new pulley joint.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.physics.newRectangleShape*
|love.physics.newRectangleShape|

Shorthand for creating rectangular PolygonShapes.

By default, the local origin is located at the '''center''' of the rectangle as
opposed to the top left for graphics.

Synopses: ~

    1.  `shape` = |love.physics.newRectangleShape|(`width`, `height`)
    2.  `shape` = |love.physics.newRectangleShape|(`x`, `y`, `width`,
        `height`, `angle`)

Variants: ~

    1.  `shape` = |love.physics.newRectangleShape|(`width`, `height`)

    1.          See function description

        Parameters: ~
            • {width} (`number`)
                The width of the rectangle.
            • {height} (`number`)
                The height of the rectangle.

        Return: ~
            (`PolygonShape`) |love2d-docs-PolygonShape|
                A new PolygonShape.
    2.  `shape` = |love.physics.newRectangleShape|(`x`, `y`, `width`,
        `height`, `angle`)

    2.          See function description

        Parameters: ~
            • {x} (`number`)
                The offset along the x-axis.
            • {y} (`number`)
                The offset along the y-axis.
            • {width} (`number`)
                The width of the rectangle.
            • {height} (`number`)
                The height of the rectangle.
            • {angle} (`number`) (default: `0`)
                The initial angle of the rectangle.

        Return: ~
            (`PolygonShape`) |love2d-docs-PolygonShape|
                A new PolygonShape.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.physics.newRevoluteJoint*
|love.physics.newRevoluteJoint|

Creates a pivot joint between two bodies.

This joint connects two bodies to a point around which they can pivot.

Synopses: ~

    1.  `joint` = |love.physics.newRevoluteJoint|(`body1`, `body2`,
        `x`, `y`, `collideConnected`)
    2.  `joint` = |love.physics.newRevoluteJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `collideConnected`, `referenceAngle`)

Variants: ~

    1.  `joint` = |love.physics.newRevoluteJoint|(`body1`, `body2`,
        `x`, `y`, `collideConnected`)

    1.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body.
            • {x} (`number`)
                The x position of the connecting point.
            • {y} (`number`)
                The y position of the connecting point.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`RevoluteJoint`) |love2d-docs-RevoluteJoint|
                The new revolute joint.
    2.  `joint` = |love.physics.newRevoluteJoint|(`body1`, `body2`,
        `x1`, `y1`, `x2`, `y2`, `collideConnected`, `referenceAngle`)

    2.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body.
            • {x1} (`number`)
                The x position of the first connecting point.
            • {y1} (`number`)
                The y position of the first connecting point.
            • {x2} (`number`)
                The x position of the second connecting point.
            • {y2} (`number`)
                The y position of the second connecting point.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.
            • {referenceAngle} (`number`) (default: `0`)
                The reference angle between body1 and body2, in radians.

        Return: ~
            (`RevoluteJoint`) |love2d-docs-RevoluteJoint|
                The new revolute joint.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.physics.newRopeJoint*
|love.physics.newRopeJoint|

Creates a joint between two bodies. Its only function is enforcing a max
distance between these bodies.

Synopses: ~

        `joint` = |love.physics.newRopeJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `maxLength`, `collideConnected`)

Variants: ~

        `joint` = |love.physics.newRopeJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `maxLength`, `collideConnected`)

        See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x1} (`number`)
                The x position of the first anchor point.
            • {y1} (`number`)
                The y position of the first anchor point.
            • {x2} (`number`)
                The x position of the second anchor point.
            • {y2} (`number`)
                The y position of the second anchor point.
            • {maxLength} (`number`)
                The maximum distance for the bodies.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`RopeJoint`) |love2d-docs-RopeJoint|
                The new RopeJoint.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.physics.newWeldJoint*
|love.physics.newWeldJoint|

Creates a constraint joint between two bodies. A WeldJoint essentially glues
two bodies together. The constraint is a bit soft, however, due to Box2D's
iterative solver.

Synopses: ~

    1.  `joint` = |love.physics.newWeldJoint|(`body1`, `body2`, `x`,
        `y`, `collideConnected`)
    2.  `joint` = |love.physics.newWeldJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `collideConnected`)
    3.  `joint` = |love.physics.newWeldJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `collideConnected`, `referenceAngle`)

Variants: ~

    1.  `joint` = |love.physics.newWeldJoint|(`body1`, `body2`, `x`,
        `y`, `collideConnected`)

    1.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x} (`number`)
                The x position of the anchor point (world space).
            • {y} (`number`)
                The y position of the anchor point (world space).
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`WeldJoint`) |love2d-docs-WeldJoint|
                The new WeldJoint.
    2.  `joint` = |love.physics.newWeldJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `collideConnected`)

    2.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x1} (`number`)
                The x position of the first anchor point (world space).
            • {y1} (`number`)
                The y position of the first anchor point (world space).
            • {x2} (`number`)
                The x position of the second anchor point (world space).
            • {y2} (`number`)
                The y position of the second anchor point (world space).
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`WeldJoint`) |love2d-docs-WeldJoint|
                The new WeldJoint.
    3.  `joint` = |love.physics.newWeldJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `collideConnected`, `referenceAngle`)

    3.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body to attach to the joint.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body to attach to the joint.
            • {x1} (`number`)
                The x position of the first anchor point (world space).
            • {y1} (`number`)
                The y position of the first anchor point  (world space).
            • {x2} (`number`)
                The x position of the second anchor point (world space).
            • {y2} (`number`)
                The y position of the second anchor point (world space).
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.
            • {referenceAngle} (`number`) (default: `0`)
                The reference angle between body1 and body2, in radians.

        Return: ~
            (`WeldJoint`) |love2d-docs-WeldJoint|
                The new WeldJoint.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.physics.newWheelJoint*
|love.physics.newWheelJoint|

Creates a wheel joint.

Synopses: ~

    1.  `joint` = |love.physics.newWheelJoint|(`body1`, `body2`, `x`,
        `y`, `ax`, `ay`, `collideConnected`)
    2.  `joint` = |love.physics.newWheelJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `ax`, `ay`, `collideConnected`)

Variants: ~

    1.  `joint` = |love.physics.newWheelJoint|(`body1`, `body2`, `x`,
        `y`, `ax`, `ay`, `collideConnected`)

    1.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body.
            • {x} (`number`)
                The x position of the anchor point.
            • {y} (`number`)
                The y position of the anchor point.
            • {ax} (`number`)
                The x position of the axis unit vector.
            • {ay} (`number`)
                The y position of the axis unit vector.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`WheelJoint`) |love2d-docs-WheelJoint|
                The new WheelJoint.
    2.  `joint` = |love.physics.newWheelJoint|(`body1`, `body2`, `x1`,
        `y1`, `x2`, `y2`, `ax`, `ay`, `collideConnected`)

    2.          See function description

        Parameters: ~
            • {body1} (`Body`) |love2d-docs-Body|
                The first body.
            • {body2} (`Body`) |love2d-docs-Body|
                The second body.
            • {x1} (`number`)
                The x position of the first anchor point.
            • {y1} (`number`)
                The y position of the first anchor point.
            • {x2} (`number`)
                The x position of the second anchor point.
            • {y2} (`number`)
                The y position of the second anchor point.
            • {ax} (`number`)
                The x position of the axis unit vector.
            • {ay} (`number`)
                The y position of the axis unit vector.
            • {collideConnected} (`boolean`) (default: `false`)
                Specifies whether the two bodies should collide with each
                other.

        Return: ~
            (`WheelJoint`) |love2d-docs-WheelJoint|
                The new WheelJoint.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.physics.newWorld*
|love.physics.newWorld|

Creates a new World.

Synopses: ~

        `world` = |love.physics.newWorld|(`xg`, `yg`, `sleep`)

Variants: ~

        `world` = |love.physics.newWorld|(`xg`, `yg`, `sleep`)

        See function description

        Parameters: ~
            • {xg} (`number`) (default: `0`)
                The x component of gravity.
            • {yg} (`number`) (default: `0`)
                The y component of gravity.
            • {sleep} (`boolean`) (default: `true`)
                Whether the bodies in this world are allowed to sleep.

        Return: ~
            (`World`) |love2d-docs-World|
                A brave new World.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.physics.setMeter*
|love.physics.setMeter|

Sets the pixels to meter scale factor.

All coordinates in the physics module are divided by this number and converted
to meters, and it creates a convenient way to draw the objects directly to the
screen without the need for graphics transformations.

It is recommended to create shapes no larger than 10 times the scale. This is
important because Box2D is tuned to work well with shape sizes from 0.1 to 10
meters. The default meter scale is 30.

Synopses: ~

        |love.physics.setMeter|(`scale`)

Variants: ~

        |love.physics.setMeter|(`scale`)

        See function description

        Parameters: ~
            • {scale} (`number`)
                The scale factor as an integer.


-------------------------------------------------------------------------------
                                               *love2d-docs-love.physics-types*
The types of |love.physics|:

    |Body|                                                   |love2d-docs-Body|
    |ChainShape|                                       |love2d-docs-ChainShape|
    |CircleShape|                                     |love2d-docs-CircleShape|
    |Contact|                                             |love2d-docs-Contact|
    |DistanceJoint|                                 |love2d-docs-DistanceJoint|
    |EdgeShape|                                         |love2d-docs-EdgeShape|
    |Fixture|                                             |love2d-docs-Fixture|
    |FrictionJoint|                                 |love2d-docs-FrictionJoint|
    |GearJoint|                                         |love2d-docs-GearJoint|
    |Joint|                                                 |love2d-docs-Joint|
    |MotorJoint|                                       |love2d-docs-MotorJoint|
    |MouseJoint|                                       |love2d-docs-MouseJoint|
    |PolygonShape|                                   |love2d-docs-PolygonShape|
    |PrismaticJoint|                               |love2d-docs-PrismaticJoint|
    |PulleyJoint|                                     |love2d-docs-PulleyJoint|
    |RevoluteJoint|                                 |love2d-docs-RevoluteJoint|
    |RopeJoint|                                         |love2d-docs-RopeJoint|
    |Shape|                                                 |love2d-docs-Shape|
    |WeldJoint|                                         |love2d-docs-WeldJoint|
    |WheelJoint|                                       |love2d-docs-WheelJoint|
    |World|                                                 |love2d-docs-World|

-------------------------------------------------------------------------------
                                                             *love2d-docs-Body*
|Body|

Bodies are objects with velocity and position.

                                                *love2d-docs-Body-constructors*
    constructors:

        |newBody|                                         |love2d-docs-newBody|

                                                  *love2d-docs-Body-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                    *love2d-docs-Body-subtypes*
    subtypes:

        None

                                                   *love2d-docs-Body-functions*
    functions:

        |applyAngularImpulse|            |love2d-docs-Body:applyAngularImpulse|
        |applyForce|                              |love2d-docs-Body:applyForce|
        |applyLinearImpulse|              |love2d-docs-Body:applyLinearImpulse|
        |applyTorque|                            |love2d-docs-Body:applyTorque|
        |destroy|                                    |love2d-docs-Body:destroy|
        |getAngle|                                  |love2d-docs-Body:getAngle|
        |getAngularDamping|                |love2d-docs-Body:getAngularDamping|
        |getAngularVelocity|              |love2d-docs-Body:getAngularVelocity|
        |getContacts|                            |love2d-docs-Body:getContacts|
        |getFixtures|                            |love2d-docs-Body:getFixtures|
        |getGravityScale|                    |love2d-docs-Body:getGravityScale|
        |getInertia|                              |love2d-docs-Body:getInertia|
        |getJoints|                                |love2d-docs-Body:getJoints|
        |getLinearDamping|                  |love2d-docs-Body:getLinearDamping|
        |getLinearVelocity|                |love2d-docs-Body:getLinearVelocity|
        |getLinearVelocityFromLocalPoi-|  |love2d-docs-Body:getLinearVelocityFromLocalPoint|
        |getLinearVelocityFromWorldPoi-|  |love2d-docs-Body:getLinearVelocityFromWorldPoint|
        |getLocalCenter|                      |love2d-docs-Body:getLocalCenter|
        |getLocalPoint|                        |love2d-docs-Body:getLocalPoint|
        |getLocalPoints|                      |love2d-docs-Body:getLocalPoints|
        |getLocalVector|                      |love2d-docs-Body:getLocalVector|
        |getMass|                                    |love2d-docs-Body:getMass|
        |getMassData|                            |love2d-docs-Body:getMassData|
        |getPosition|                            |love2d-docs-Body:getPosition|
        |getTransform|                          |love2d-docs-Body:getTransform|
        |getType|                                    |love2d-docs-Body:getType|
        |getUserData|                            |love2d-docs-Body:getUserData|
        |getWorld|                                  |love2d-docs-Body:getWorld|
        |getWorldCenter|                      |love2d-docs-Body:getWorldCenter|
        |getWorldPoint|                        |love2d-docs-Body:getWorldPoint|
        |getWorldPoints|                      |love2d-docs-Body:getWorldPoints|
        |getWorldVector|                      |love2d-docs-Body:getWorldVector|
        |getX|                                          |love2d-docs-Body:getX|
        |getY|                                          |love2d-docs-Body:getY|
        |isActive|                                  |love2d-docs-Body:isActive|
        |isAwake|                                    |love2d-docs-Body:isAwake|
        |isBullet|                                  |love2d-docs-Body:isBullet|
        |isDestroyed|                            |love2d-docs-Body:isDestroyed|
        |isFixedRotation|                    |love2d-docs-Body:isFixedRotation|
        |isSleepingAllowed|                |love2d-docs-Body:isSleepingAllowed|
        |isTouching|                              |love2d-docs-Body:isTouching|
        |resetMassData|                        |love2d-docs-Body:resetMassData|
        |setActive|                                |love2d-docs-Body:setActive|
        |setAngle|                                  |love2d-docs-Body:setAngle|
        |setAngularDamping|                |love2d-docs-Body:setAngularDamping|
        |setAngularVelocity|              |love2d-docs-Body:setAngularVelocity|
        |setAwake|                                  |love2d-docs-Body:setAwake|
        |setBullet|                                |love2d-docs-Body:setBullet|
        |setFixedRotation|                  |love2d-docs-Body:setFixedRotation|
        |setGravityScale|                    |love2d-docs-Body:setGravityScale|
        |setInertia|                              |love2d-docs-Body:setInertia|
        |setLinearDamping|                  |love2d-docs-Body:setLinearDamping|
        |setLinearVelocity|                |love2d-docs-Body:setLinearVelocity|
        |setMass|                                    |love2d-docs-Body:setMass|
        |setMassData|                            |love2d-docs-Body:setMassData|
        |setPosition|                            |love2d-docs-Body:setPosition|
        |setSleepingAllowed|              |love2d-docs-Body:setSleepingAllowed|
        |setTransform|                          |love2d-docs-Body:setTransform|
        |setType|                                    |love2d-docs-Body:setType|
        |setUserData|                            |love2d-docs-Body:setUserData|
        |setX|                                          |love2d-docs-Body:setX|
        |setY|                                          |love2d-docs-Body:setY|

-------------------------------------------------------------------------------
                                         *love2d-docs-Body:applyAngularImpulse*
|Body:applyAngularImpulse|

Applies an angular impulse to a body. This makes a single, instantaneous
addition to the body momentum.

A body with with a larger mass will react less. The reaction does '''not'''
depend on the timestep, and is equivalent to applying a force continuously for
1 second. Impulses are best used to give a single push to a body. For a
continuous push to a body it is better to use Body:applyForce.

Synopses: ~

        |Body:applyAngularImpulse|(`impulse`)

Variants: ~

        |Body:applyAngularImpulse|(`impulse`)

        See function description

        Parameters: ~
            • {impulse} (`number`)
                The impulse in kilogram-square meter per second.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Body:applyForce*
|Body:applyForce|

Apply force to a Body.

A force pushes a body in a direction. A body with with a larger mass will react
less. The reaction also depends on how long a force is applied: since the force
acts continuously over the entire timestep, a short timestep will only push the
body for a short time. Thus forces are best used for many timesteps to give a
continuous push to a body (like gravity). For a single push that is independent
of timestep, it is better to use Body:applyLinearImpulse.

If the position to apply the force is not given, it will act on the center of
mass of the body. The part of the force not directed towards the center of mass
will cause the body to spin (and depends on the rotational inertia).

Note that the force components and position must be given in world coordinates.

Synopses: ~

    1.  |Body:applyForce|(`fx`, `fy`)
    2.  |Body:applyForce|(`fx`, `fy`, `x`, `y`)

Variants: ~

    1.  |Body:applyForce|(`fx`, `fy`)

    1.          See function description

        Parameters: ~
            • {fx} (`number`)
                The x component of force to apply to the center of mass.
            • {fy} (`number`)
                The y component of force to apply to the center of mass.


    2.  |Body:applyForce|(`fx`, `fy`, `x`, `y`)

    2.          See function description

        Parameters: ~
            • {fx} (`number`)
                The x component of force to apply.
            • {fy} (`number`)
                The y component of force to apply.
            • {x} (`number`)
                The x position to apply the force.
            • {y} (`number`)
                The y position to apply the force.


-------------------------------------------------------------------------------
                                          *love2d-docs-Body:applyLinearImpulse*
|Body:applyLinearImpulse|

Applies an impulse to a body.

This makes a single, instantaneous addition to the body momentum.

An impulse pushes a body in a direction. A body with with a larger mass will
react less. The reaction does '''not''' depend on the timestep, and is
equivalent to applying a force continuously for 1 second. Impulses are best
used to give a single push to a body. For a continuous push to a body it is
better to use Body:applyForce.

If the position to apply the impulse is not given, it will act on the center of
mass of the body. The part of the impulse not directed towards the center of
mass will cause the body to spin (and depends on the rotational inertia).

Note that the impulse components and position must be given in world
coordinates.

Synopses: ~

    1.  |Body:applyLinearImpulse|(`ix`, `iy`)
    2.  |Body:applyLinearImpulse|(`ix`, `iy`, `x`, `y`)

Variants: ~

    1.  |Body:applyLinearImpulse|(`ix`, `iy`)

    1.          See function description

        Parameters: ~
            • {ix} (`number`)
                The x component of the impulse applied to the center of mass.
            • {iy} (`number`)
                The y component of the impulse applied to the center of mass.


    2.  |Body:applyLinearImpulse|(`ix`, `iy`, `x`, `y`)

    2.          See function description

        Parameters: ~
            • {ix} (`number`)
                The x component of the impulse.
            • {iy} (`number`)
                The y component of the impulse.
            • {x} (`number`)
                The x position to apply the impulse.
            • {y} (`number`)
                The y position to apply the impulse.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:applyTorque*
|Body:applyTorque|

Apply torque to a body.

Torque is like a force that will change the angular velocity (spin) of a body.
The effect will depend on the rotational inertia a body has.

Synopses: ~

        |Body:applyTorque|(`torque`)

Variants: ~

        |Body:applyTorque|(`torque`)

        See function description

        Parameters: ~
            • {torque} (`number`)
                The torque to apply.


-------------------------------------------------------------------------------
                                                     *love2d-docs-Body:destroy*
|Body:destroy|

Explicitly destroys the Body and all fixtures and joints attached to it.

An error will occur if you attempt to use the object after calling this
function. In 0.7.2, when you don't have time to wait for garbage collection,
this function may be used to free the object immediately.

Synopses: ~

        |Body:destroy|()

Variants: ~

        |Body:destroy|()

        See function description


-------------------------------------------------------------------------------
                                                    *love2d-docs-Body:getAngle*
|Body:getAngle|

Get the angle of the body.

The angle is measured in radians. If you need to transform it to degrees, use
math.deg.

A value of 0 radians will mean 'looking to the right'. Although radians
increase counter-clockwise, the y axis points down so it becomes ''clockwise''
from our point of view.

Synopses: ~

        `angle` = |Body:getAngle|()

Variants: ~

        `angle` = |Body:getAngle|()

        See function description

        Return: ~
            (`number`)
                The angle in radians.
-------------------------------------------------------------------------------
                                           *love2d-docs-Body:getAngularDamping*
|Body:getAngularDamping|

Gets the Angular damping of the Body

The angular damping is the ''rate of decrease of the angular velocity over
time'': A spinning body with no damping and no external forces will continue
spinning indefinitely. A spinning body with damping will gradually stop
spinning.

Damping is not the same as friction - they can be modelled together. However,
only damping is provided by Box2D (and LOVE).

Damping parameters should be between 0 and infinity, with 0 meaning no damping,
and infinity meaning full damping. Normally you will use a damping value
between 0 and 0.1.

Synopses: ~

        `damping` = |Body:getAngularDamping|()

Variants: ~

        `damping` = |Body:getAngularDamping|()

        See function description

        Return: ~
            (`number`)
                The value of the angular damping.
-------------------------------------------------------------------------------
                                          *love2d-docs-Body:getAngularVelocity*
|Body:getAngularVelocity|

Get the angular velocity of the Body.

The angular velocity is the ''rate of change of angle over time''.

It is changed in World:update by applying torques, off centre forces/impulses,
and angular damping. It can be set directly with Body:setAngularVelocity.

If you need the ''rate of change of position over time'', use
Body:getLinearVelocity.

Synopses: ~

        `w` = |Body:getAngularVelocity|()

Variants: ~

        `w` = |Body:getAngularVelocity|()

        See function description

        Return: ~
            (`number`)
                The angular velocity in radians/second.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:getContacts*
|Body:getContacts|

Gets a list of all Contacts attached to the Body.

Synopses: ~

        `contacts` = |Body:getContacts|()

Variants: ~

        `contacts` = |Body:getContacts|()

        See function description

        Return: ~
            (`table`)
                A list with all contacts associated with the Body.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:getFixtures*
|Body:getFixtures|

Returns a table with all fixtures.

Synopses: ~

        `fixtures` = |Body:getFixtures|()

Variants: ~

        `fixtures` = |Body:getFixtures|()

        See function description

        Return: ~
            (`table`)
                A sequence with all fixtures.
-------------------------------------------------------------------------------
                                             *love2d-docs-Body:getGravityScale*
|Body:getGravityScale|

Returns the gravity scale factor.

Synopses: ~

        `scale` = |Body:getGravityScale|()

Variants: ~

        `scale` = |Body:getGravityScale|()

        See function description

        Return: ~
            (`number`)
                The gravity scale factor.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Body:getInertia*
|Body:getInertia|

Gets the rotational inertia of the body.

The rotational inertia is how hard is it to make the body spin.

Synopses: ~

        `inertia` = |Body:getInertia|()

Variants: ~

        `inertia` = |Body:getInertia|()

        See function description

        Return: ~
            (`number`)
                The rotational inertial of the body.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Body:getJoints*
|Body:getJoints|

Returns a table containing the Joints attached to this Body.

Synopses: ~

        `joints` = |Body:getJoints|()

Variants: ~

        `joints` = |Body:getJoints|()

        See function description

        Return: ~
            (`table`)
                A sequence with the Joints attached to the Body.
-------------------------------------------------------------------------------
                                            *love2d-docs-Body:getLinearDamping*
|Body:getLinearDamping|

Gets the linear damping of the Body.

The linear damping is the ''rate of decrease of the linear velocity over
time''. A moving body with no damping and no external forces will continue
moving indefinitely, as is the case in space. A moving body with damping will
gradually stop moving.

Damping is not the same as friction - they can be modelled together.

Synopses: ~

        `damping` = |Body:getLinearDamping|()

Variants: ~

        `damping` = |Body:getLinearDamping|()

        See function description

        Return: ~
            (`number`)
                The value of the linear damping.
-------------------------------------------------------------------------------
                                           *love2d-docs-Body:getLinearVelocity*
|Body:getLinearVelocity|

Gets the linear velocity of the Body from its center of mass.

The linear velocity is the ''rate of change of position over time''.

If you need the ''rate of change of angle over time'', use
Body:getAngularVelocity.

If you need to get the linear velocity of a point different from the center of
mass:

*  Body:getLinearVelocityFromLocalPoint allows you to specify the point in
local coordinates.

*  Body:getLinearVelocityFromWorldPoint allows you to specify the point in
world coordinates.

See page 136 of 'Essential Mathematics for Games and Interactive Applications'
for definitions of local and world coordinates.

Synopses: ~

        `x`, `y` = |Body:getLinearVelocity|()

Variants: ~

        `x`, `y` = |Body:getLinearVelocity|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the velocity vector

            2. (`number`)
                The y-component of the velocity vector
-------------------------------------------------------------------------------
                             *love2d-docs-Body:getLinearVelocityFromLocalPoint*
|Body:getLinearVelocityFromLocalPoint|

Get the linear velocity of a point on the body.

The linear velocity for a point on the body is the velocity of the body center
of mass plus the velocity at that point from the body spinning.

The point on the body must given in local coordinates. Use
Body:getLinearVelocityFromWorldPoint to specify this with world coordinates.

Synopses: ~

        `vx`, `vy` = |Body:getLinearVelocityFromLocalPoint|(`x`, `y`)

Variants: ~

        `vx`, `vy` = |Body:getLinearVelocityFromLocalPoint|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position to measure velocity.
            • {y} (`number`)
                The y position to measure velocity.

        Returns: ~
            1. (`number`)
                The x component of velocity at point (x,y).

            2. (`number`)
                The y component of velocity at point (x,y).
-------------------------------------------------------------------------------
                             *love2d-docs-Body:getLinearVelocityFromWorldPoint*
|Body:getLinearVelocityFromWorldPoint|

Get the linear velocity of a point on the body.

The linear velocity for a point on the body is the velocity of the body center
of mass plus the velocity at that point from the body spinning.

The point on the body must given in world coordinates. Use
Body:getLinearVelocityFromLocalPoint to specify this with local coordinates.

Synopses: ~

        `vx`, `vy` = |Body:getLinearVelocityFromWorldPoint|(`x`, `y`)

Variants: ~

        `vx`, `vy` = |Body:getLinearVelocityFromWorldPoint|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position to measure velocity.
            • {y} (`number`)
                The y position to measure velocity.

        Returns: ~
            1. (`number`)
                The x component of velocity at point (x,y).

            2. (`number`)
                The y component of velocity at point (x,y).
-------------------------------------------------------------------------------
                                              *love2d-docs-Body:getLocalCenter*
|Body:getLocalCenter|

Get the center of mass position in local coordinates.

Use Body:getWorldCenter to get the center of mass in world coordinates.

Synopses: ~

        `x`, `y` = |Body:getLocalCenter|()

Variants: ~

        `x`, `y` = |Body:getLocalCenter|()

        See function description

        Returns: ~
            1. (`number`)
                The x coordinate of the center of mass.

            2. (`number`)
                The y coordinate of the center of mass.
-------------------------------------------------------------------------------
                                               *love2d-docs-Body:getLocalPoint*
|Body:getLocalPoint|

Transform a point from world coordinates to local coordinates.

Synopses: ~

        `localX`, `localY` = |Body:getLocalPoint|(`worldX`, `worldY`)

Variants: ~

        `localX`, `localY` = |Body:getLocalPoint|(`worldX`, `worldY`)

        See function description

        Parameters: ~
            • {worldX} (`number`)
                The x position in world coordinates.
            • {worldY} (`number`)
                The y position in world coordinates.

        Returns: ~
            1. (`number`)
                The x position in local coordinates.

            2. (`number`)
                The y position in local coordinates.
-------------------------------------------------------------------------------
                                              *love2d-docs-Body:getLocalPoints*
|Body:getLocalPoints|

Transforms multiple points from world coordinates to local coordinates.

Synopses: ~

        `x1`, `y1`, `x2`, `y2`, `...` = |Body:getLocalPoints|(`x1`,
        `y1`, `x2`, `y2`, `...`)

Variants: ~

        `x1`, `y1`, `x2`, `y2`, `...` = |Body:getLocalPoints|(`x1`,
        `y1`, `x2`, `y2`, `...`)

        See function description

        Parameters: ~
            • {x1} (`number`)
                (Argument) The x position of the first point.
            • {y1} (`number`)
                (Argument) The y position of the first point.
            • {x2} (`number`)
                (Argument) The x position of the second point.
            • {y2} (`number`)
                (Argument) The y position of the second point.
            • {...} (`number`)
                (Argument) You can continue passing x and y position of the
                points.

        Returns: ~
            1. (`number`)
                (Result) The transformed x position of the first point.

            2. (`number`)
                (Result) The transformed y position of the first point.

            3. (`number`)
                (Result) The transformed x position of the second point.

            4. (`number`)
                (Result) The transformed y position of the second point.

            5. (`number`)
                (Result) Additional transformed x and y position of the points.
-------------------------------------------------------------------------------
                                              *love2d-docs-Body:getLocalVector*
|Body:getLocalVector|

Transform a vector from world coordinates to local coordinates.

Synopses: ~

        `localX`, `localY` = |Body:getLocalVector|(`worldX`, `worldY`)

Variants: ~

        `localX`, `localY` = |Body:getLocalVector|(`worldX`, `worldY`)

        See function description

        Parameters: ~
            • {worldX} (`number`)
                The vector x component in world coordinates.
            • {worldY} (`number`)
                The vector y component in world coordinates.

        Returns: ~
            1. (`number`)
                The vector x component in local coordinates.

            2. (`number`)
                The vector y component in local coordinates.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Body:getMass*
|Body:getMass|

Get the mass of the body.

Static bodies always have a mass of 0.

Synopses: ~

        `mass` = |Body:getMass|()

Variants: ~

        `mass` = |Body:getMass|()

        See function description

        Return: ~
            (`number`)
                The mass of the body (in kilograms).
-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:getMassData*
|Body:getMassData|

Returns the mass, its center, and the rotational inertia.

Synopses: ~

        `x`, `y`, `mass`, `inertia` = |Body:getMassData|()

Variants: ~

        `x`, `y`, `mass`, `inertia` = |Body:getMassData|()

        See function description

        Returns: ~
            1. (`number`)
                The x position of the center of mass.

            2. (`number`)
                The y position of the center of mass.

            3. (`number`)
                The mass of the body.

            4. (`number`)
                The rotational inertia.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:getPosition*
|Body:getPosition|

Get the position of the body.

Note that this may not be the center of mass of the body.

Synopses: ~

        `x`, `y` = |Body:getPosition|()

Variants: ~

        `x`, `y` = |Body:getPosition|()

        See function description

        Returns: ~
            1. (`number`)
                The x position.

            2. (`number`)
                The y position.
-------------------------------------------------------------------------------
                                                *love2d-docs-Body:getTransform*
|Body:getTransform|

Get the position and angle of the body.

Note that the position may not be the center of mass of the body. An angle of 0
radians will mean 'looking to the right'. Although radians increase
counter-clockwise, the y axis points down so it becomes clockwise from our
point of view.

Synopses: ~

        `x`, `y`, `angle` = |Body:getTransform|()

Variants: ~

        `x`, `y`, `angle` = |Body:getTransform|()

        See function description

        Returns: ~
            1. (`number`)
                The x component of the position.

            2. (`number`)
                The y component of the position.

            3. (`number`)
                The angle in radians.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Body:getType*
|Body:getType|

Returns the type of the body.

Synopses: ~

        `type` = |Body:getType|()

Variants: ~

        `type` = |Body:getType|()

        See function description

        Return: ~
            (`BodyType`) |love2d-docs-BodyType|
                The body type.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:getUserData*
|Body:getUserData|

Returns the Lua value associated with this Body.

Synopses: ~

        `value` = |Body:getUserData|()

Variants: ~

        `value` = |Body:getUserData|()

        See function description

        Return: ~
            (`any`)
                The Lua value associated with the Body.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Body:getWorld*
|Body:getWorld|

Gets the World the body lives in.

Synopses: ~

        `world` = |Body:getWorld|()

Variants: ~

        `world` = |Body:getWorld|()

        See function description

        Return: ~
            (`World`) |love2d-docs-World|
                The world the body lives in.
-------------------------------------------------------------------------------
                                              *love2d-docs-Body:getWorldCenter*
|Body:getWorldCenter|

Get the center of mass position in world coordinates.

Use Body:getLocalCenter to get the center of mass in local coordinates.

Synopses: ~

        `x`, `y` = |Body:getWorldCenter|()

Variants: ~

        `x`, `y` = |Body:getWorldCenter|()

        See function description

        Returns: ~
            1. (`number`)
                The x coordinate of the center of mass.

            2. (`number`)
                The y coordinate of the center of mass.
-------------------------------------------------------------------------------
                                               *love2d-docs-Body:getWorldPoint*
|Body:getWorldPoint|

Transform a point from local coordinates to world coordinates.

Synopses: ~

        `worldX`, `worldY` = |Body:getWorldPoint|(`localX`, `localY`)

Variants: ~

        `worldX`, `worldY` = |Body:getWorldPoint|(`localX`, `localY`)

        See function description

        Parameters: ~
            • {localX} (`number`)
                The x position in local coordinates.
            • {localY} (`number`)
                The y position in local coordinates.

        Returns: ~
            1. (`number`)
                The x position in world coordinates.

            2. (`number`)
                The y position in world coordinates.
-------------------------------------------------------------------------------
                                              *love2d-docs-Body:getWorldPoints*
|Body:getWorldPoints|

Transforms multiple points from local coordinates to world coordinates.

Synopses: ~

        `x1`, `y1`, `x2`, `y2` = |Body:getWorldPoints|(`x1`, `y1`,
        `x2`, `y2`)

Variants: ~

        `x1`, `y1`, `x2`, `y2` = |Body:getWorldPoints|(`x1`, `y1`,
        `x2`, `y2`)

        See function description

        Parameters: ~
            • {x1} (`number`)
                The x position of the first point.
            • {y1} (`number`)
                The y position of the first point.
            • {x2} (`number`)
                The x position of the second point.
            • {y2} (`number`)
                The y position of the second point.

        Returns: ~
            1. (`number`)
                The transformed x position of the first point.

            2. (`number`)
                The transformed y position of the first point.

            3. (`number`)
                The transformed x position of the second point.

            4. (`number`)
                The transformed y position of the second point.
-------------------------------------------------------------------------------
                                              *love2d-docs-Body:getWorldVector*
|Body:getWorldVector|

Transform a vector from local coordinates to world coordinates.

Synopses: ~

        `worldX`, `worldY` = |Body:getWorldVector|(`localX`, `localY`)

Variants: ~

        `worldX`, `worldY` = |Body:getWorldVector|(`localX`, `localY`)

        See function description

        Parameters: ~
            • {localX} (`number`)
                The vector x component in local coordinates.
            • {localY} (`number`)
                The vector y component in local coordinates.

        Returns: ~
            1. (`number`)
                The vector x component in world coordinates.

            2. (`number`)
                The vector y component in world coordinates.
-------------------------------------------------------------------------------
                                                        *love2d-docs-Body:getX*
|Body:getX|

Get the x position of the body in world coordinates.

Synopses: ~

        `x` = |Body:getX|()

Variants: ~

        `x` = |Body:getX|()

        See function description

        Return: ~
            (`number`)
                The x position in world coordinates.
-------------------------------------------------------------------------------
                                                        *love2d-docs-Body:getY*
|Body:getY|

Get the y position of the body in world coordinates.

Synopses: ~

        `y` = |Body:getY|()

Variants: ~

        `y` = |Body:getY|()

        See function description

        Return: ~
            (`number`)
                The y position in world coordinates.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Body:isActive*
|Body:isActive|

Returns whether the body is actively used in the simulation.

Synopses: ~

        `status` = |Body:isActive|()

Variants: ~

        `status` = |Body:isActive|()

        See function description

        Return: ~
            (`boolean`)
                True if the body is active or false if not.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Body:isAwake*
|Body:isAwake|

Returns the sleep status of the body.

Synopses: ~

        `status` = |Body:isAwake|()

Variants: ~

        `status` = |Body:isAwake|()

        See function description

        Return: ~
            (`boolean`)
                True if the body is awake or false if not.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Body:isBullet*
|Body:isBullet|

Get the bullet status of a body.

There are two methods to check for body collisions:

*  at their location when the world is updated (default)

*  using continuous collision detection (CCD)

The default method is efficient, but a body moving very quickly may sometimes
jump over another body without producing a collision. A body that is set as a
bullet will use CCD. This is less efficient, but is guaranteed not to jump when
moving quickly.

Note that static bodies (with zero mass) always use CCD, so your walls will not
let a fast moving body pass through even if it is not a bullet.

Synopses: ~

        `status` = |Body:isBullet|()

Variants: ~

        `status` = |Body:isBullet|()

        See function description

        Return: ~
            (`boolean`)
                The bullet status of the body.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:isDestroyed*
|Body:isDestroyed|

Gets whether the Body is destroyed. Destroyed bodies cannot be used.

Synopses: ~

        `destroyed` = |Body:isDestroyed|()

Variants: ~

        `destroyed` = |Body:isDestroyed|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Body is destroyed.
-------------------------------------------------------------------------------
                                             *love2d-docs-Body:isFixedRotation*
|Body:isFixedRotation|

Returns whether the body rotation is locked.

Synopses: ~

        `fixed` = |Body:isFixedRotation|()

Variants: ~

        `fixed` = |Body:isFixedRotation|()

        See function description

        Return: ~
            (`boolean`)
                True if the body's rotation is locked or false if not.
-------------------------------------------------------------------------------
                                           *love2d-docs-Body:isSleepingAllowed*
|Body:isSleepingAllowed|

Returns the sleeping behaviour of the body.

Synopses: ~

        `allowed` = |Body:isSleepingAllowed|()

Variants: ~

        `allowed` = |Body:isSleepingAllowed|()

        See function description

        Return: ~
            (`boolean`)
                True if the body is allowed to sleep or false if not.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Body:isTouching*
|Body:isTouching|

Gets whether the Body is touching the given other Body.

Synopses: ~

        `touching` = |Body:isTouching|(`otherbody`)

Variants: ~

        `touching` = |Body:isTouching|(`otherbody`)

        See function description

        Parameters: ~
            • {otherbody} (`Body`) |love2d-docs-Body|
                The other body to check.

        Return: ~
            (`boolean`)
                True if this body is touching the other body, false otherwise.
-------------------------------------------------------------------------------
                                               *love2d-docs-Body:resetMassData*
|Body:resetMassData|

Resets the mass of the body by recalculating it from the mass properties of the
fixtures.

Synopses: ~

        |Body:resetMassData|()

Variants: ~

        |Body:resetMassData|()

        See function description


-------------------------------------------------------------------------------
                                                   *love2d-docs-Body:setActive*
|Body:setActive|

Sets whether the body is active in the world.

An inactive body does not take part in the simulation. It will not move or
cause any collisions.

Synopses: ~

        |Body:setActive|(`active`)

Variants: ~

        |Body:setActive|(`active`)

        See function description

        Parameters: ~
            • {active} (`boolean`)
                If the body is active or not.


-------------------------------------------------------------------------------
                                                    *love2d-docs-Body:setAngle*
|Body:setAngle|

Set the angle of the body.

The angle is measured in radians. If you need to transform it from degrees, use
math.rad.

A value of 0 radians will mean 'looking to the right'. Although radians
increase counter-clockwise, the y axis points down so it becomes ''clockwise''
from our point of view.

It is possible to cause a collision with another body by changing its angle.

Synopses: ~

        |Body:setAngle|(`angle`)

Variants: ~

        |Body:setAngle|(`angle`)

        See function description

        Parameters: ~
            • {angle} (`number`)
                The angle in radians.


-------------------------------------------------------------------------------
                                           *love2d-docs-Body:setAngularDamping*
|Body:setAngularDamping|

Sets the angular damping of a Body

See Body:getAngularDamping for a definition of angular damping.

Angular damping can take any value from 0 to infinity. It is recommended to
stay between 0 and 0.1, though. Other values will look unrealistic.

Synopses: ~

        |Body:setAngularDamping|(`damping`)

Variants: ~

        |Body:setAngularDamping|(`damping`)

        See function description

        Parameters: ~
            • {damping} (`number`)
                The new angular damping.


-------------------------------------------------------------------------------
                                          *love2d-docs-Body:setAngularVelocity*
|Body:setAngularVelocity|

Sets the angular velocity of a Body.

The angular velocity is the ''rate of change of angle over time''.

This function will not accumulate anything; any impulses previously applied
since the last call to World:update will be lost.

Synopses: ~

        |Body:setAngularVelocity|(`w`)

Variants: ~

        |Body:setAngularVelocity|(`w`)

        See function description

        Parameters: ~
            • {w} (`number`)
                The new angular velocity, in radians per second


-------------------------------------------------------------------------------
                                                    *love2d-docs-Body:setAwake*
|Body:setAwake|

Wakes the body up or puts it to sleep.

Synopses: ~

        |Body:setAwake|(`awake`)

Variants: ~

        |Body:setAwake|(`awake`)

        See function description

        Parameters: ~
            • {awake} (`boolean`)
                The body sleep status.


-------------------------------------------------------------------------------
                                                   *love2d-docs-Body:setBullet*
|Body:setBullet|

Set the bullet status of a body.

There are two methods to check for body collisions:

*  at their location when the world is updated (default)

*  using continuous collision detection (CCD)

The default method is efficient, but a body moving very quickly may sometimes
jump over another body without producing a collision. A body that is set as a
bullet will use CCD. This is less efficient, but is guaranteed not to jump when
moving quickly.

Note that static bodies (with zero mass) always use CCD, so your walls will not
let a fast moving body pass through even if it is not a bullet.

Synopses: ~

        |Body:setBullet|(`status`)

Variants: ~

        |Body:setBullet|(`status`)

        See function description

        Parameters: ~
            • {status} (`boolean`)
                The bullet status of the body.


-------------------------------------------------------------------------------
                                            *love2d-docs-Body:setFixedRotation*
|Body:setFixedRotation|

Set whether a body has fixed rotation.

Bodies with fixed rotation don't vary the speed at which they rotate. Calling
this function causes the mass to be reset.

Synopses: ~

        |Body:setFixedRotation|(`isFixed`)

Variants: ~

        |Body:setFixedRotation|(`isFixed`)

        See function description

        Parameters: ~
            • {isFixed} (`boolean`)
                Whether the body should have fixed rotation.


-------------------------------------------------------------------------------
                                             *love2d-docs-Body:setGravityScale*
|Body:setGravityScale|

Sets a new gravity scale factor for the body.

Synopses: ~

        |Body:setGravityScale|(`scale`)

Variants: ~

        |Body:setGravityScale|(`scale`)

        See function description

        Parameters: ~
            • {scale} (`number`)
                The new gravity scale factor.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Body:setInertia*
|Body:setInertia|

Set the inertia of a body.

Synopses: ~

        |Body:setInertia|(`inertia`)

Variants: ~

        |Body:setInertia|(`inertia`)

        See function description

        Parameters: ~
            • {inertia} (`number`)
                The new moment of inertia, in kilograms * pixel squared.


-------------------------------------------------------------------------------
                                            *love2d-docs-Body:setLinearDamping*
|Body:setLinearDamping|

Sets the linear damping of a Body

See Body:getLinearDamping for a definition of linear damping.

Linear damping can take any value from 0 to infinity. It is recommended to stay
between 0 and 0.1, though. Other values will make the objects look 'floaty'(if
gravity is enabled).

Synopses: ~

        |Body:setLinearDamping|(`ld`)

Variants: ~

        |Body:setLinearDamping|(`ld`)

        See function description

        Parameters: ~
            • {ld} (`number`)
                The new linear damping


-------------------------------------------------------------------------------
                                           *love2d-docs-Body:setLinearVelocity*
|Body:setLinearVelocity|

Sets a new linear velocity for the Body.

This function will not accumulate anything; any impulses previously applied
since the last call to World:update will be lost.

Synopses: ~

        |Body:setLinearVelocity|(`x`, `y`)

Variants: ~

        |Body:setLinearVelocity|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the velocity vector.
            • {y} (`number`)
                The y-component of the velocity vector.


-------------------------------------------------------------------------------
                                                     *love2d-docs-Body:setMass*
|Body:setMass|

Sets a new body mass.

Synopses: ~

        |Body:setMass|(`mass`)

Variants: ~

        |Body:setMass|(`mass`)

        See function description

        Parameters: ~
            • {mass} (`number`)
                The mass, in kilograms.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:setMassData*
|Body:setMassData|

Overrides the calculated mass data.

Synopses: ~

        |Body:setMassData|(`x`, `y`, `mass`, `inertia`)

Variants: ~

        |Body:setMassData|(`x`, `y`, `mass`, `inertia`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position of the center of mass.
            • {y} (`number`)
                The y position of the center of mass.
            • {mass} (`number`)
                The mass of the body.
            • {inertia} (`number`)
                The rotational inertia.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:setPosition*
|Body:setPosition|

Set the position of the body.

Note that this may not be the center of mass of the body.

This function cannot wake up the body.

Synopses: ~

        |Body:setPosition|(`x`, `y`)

Variants: ~

        |Body:setPosition|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position.
            • {y} (`number`)
                The y position.


-------------------------------------------------------------------------------
                                          *love2d-docs-Body:setSleepingAllowed*
|Body:setSleepingAllowed|

Sets the sleeping behaviour of the body. Should sleeping be allowed, a body at
rest will automatically sleep. A sleeping body is not simulated unless it
collided with an awake body. Be wary that one can end up with a situation like
a floating sleeping body if the floor was removed.

Synopses: ~

        |Body:setSleepingAllowed|(`allowed`)

Variants: ~

        |Body:setSleepingAllowed|(`allowed`)

        See function description

        Parameters: ~
            • {allowed} (`boolean`)
                True if the body is allowed to sleep or false if not.


-------------------------------------------------------------------------------
                                                *love2d-docs-Body:setTransform*
|Body:setTransform|

Set the position and angle of the body.

Note that the position may not be the center of mass of the body. An angle of 0
radians will mean 'looking to the right'. Although radians increase
counter-clockwise, the y axis points down so it becomes clockwise from our
point of view.

This function cannot wake up the body.

Synopses: ~

        |Body:setTransform|(`x`, `y`, `angle`)

Variants: ~

        |Body:setTransform|(`x`, `y`, `angle`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x component of the position.
            • {y} (`number`)
                The y component of the position.
            • {angle} (`number`)
                The angle in radians.


-------------------------------------------------------------------------------
                                                     *love2d-docs-Body:setType*
|Body:setType|

Sets a new body type.

Synopses: ~

        |Body:setType|(`type`)

Variants: ~

        |Body:setType|(`type`)

        See function description

        Parameters: ~
            • {type} (`BodyType`) |love2d-docs-BodyType|
                The new type.


-------------------------------------------------------------------------------
                                                 *love2d-docs-Body:setUserData*
|Body:setUserData|

Associates a Lua value with the Body.

To delete the reference, explicitly pass nil.

Synopses: ~

        |Body:setUserData|(`value`)

Variants: ~

        |Body:setUserData|(`value`)

        See function description

        Parameters: ~
            • {value} (`any`)
                The Lua value to associate with the Body.


-------------------------------------------------------------------------------
                                                        *love2d-docs-Body:setX*
|Body:setX|

Set the x position of the body.

This function cannot wake up the body.

Synopses: ~

        |Body:setX|(`x`)

Variants: ~

        |Body:setX|(`x`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position.


-------------------------------------------------------------------------------
                                                        *love2d-docs-Body:setY*
|Body:setY|

Set the y position of the body.

This function cannot wake up the body.

Synopses: ~

        |Body:setY|(`y`)

Variants: ~

        |Body:setY|(`y`)

        See function description

        Parameters: ~
            • {y} (`number`)
                The y position.



-------------------------------------------------------------------------------
                                                       *love2d-docs-ChainShape*
|ChainShape|

A ChainShape consists of multiple line segments. It can be used to create the
boundaries of your terrain. The shape does not have volume and can only collide
with PolygonShape and CircleShape.

Unlike the PolygonShape, the ChainShape does not have a vertices limit or has
to form a convex shape, but self intersections are not supported.

                                          *love2d-docs-ChainShape-constructors*
    constructors:

        |newChainShape|                             |love2d-docs-newChainShape|

                                            *love2d-docs-ChainShape-supertypes*
    supertypes:

        |Shape|                                             |love2d-docs-Shape|
        |Object|                                           |love2d-docs-Object|

                                              *love2d-docs-ChainShape-subtypes*
    subtypes:

        None

                                             *love2d-docs-ChainShape-functions*
    functions:

        |getChildEdge|                    |love2d-docs-ChainShape:getChildEdge|
        |getNextVertex|                  |love2d-docs-ChainShape:getNextVertex|
        |getPoint|                            |love2d-docs-ChainShape:getPoint|
        |getPoints|                          |love2d-docs-ChainShape:getPoints|
        |getPreviousVertex|          |love2d-docs-ChainShape:getPreviousVertex|
        |getVertexCount|                |love2d-docs-ChainShape:getVertexCount|
        |setNextVertex|                  |love2d-docs-ChainShape:setNextVertex|
        |setPreviousVertex|          |love2d-docs-ChainShape:setPreviousVertex|

-------------------------------------------------------------------------------
                                          *love2d-docs-ChainShape:getChildEdge*
|ChainShape:getChildEdge|

Returns a child of the shape as an EdgeShape.

Synopses: ~

        `shape` = |ChainShape:getChildEdge|(`index`)

Variants: ~

        `shape` = |ChainShape:getChildEdge|(`index`)

        See function description

        Parameters: ~
            • {index} (`number`)
                The index of the child.

        Return: ~
            (`EdgeShape`) |love2d-docs-EdgeShape|
                The child as an EdgeShape.
-------------------------------------------------------------------------------
                                         *love2d-docs-ChainShape:getNextVertex*
|ChainShape:getNextVertex|

Gets the vertex that establishes a connection to the next shape.

Setting next and previous ChainShape vertices can help prevent unwanted
collisions when a flat shape slides along the edge and moves over to the new
shape.

Synopses: ~

        `x`, `y` = |ChainShape:getNextVertex|()

Variants: ~

        `x`, `y` = |ChainShape:getNextVertex|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the vertex, or nil if
                ChainShape:setNextVertex hasn't been called.

            2. (`number`)
                The y-component of the vertex, or nil if
                ChainShape:setNextVertex hasn't been called.
-------------------------------------------------------------------------------
                                              *love2d-docs-ChainShape:getPoint*
|ChainShape:getPoint|

Returns a point of the shape.

Synopses: ~

        `x`, `y` = |ChainShape:getPoint|(`index`)

Variants: ~

        `x`, `y` = |ChainShape:getPoint|(`index`)

        See function description

        Parameters: ~
            • {index} (`number`)
                The index of the point to return.

        Returns: ~
            1. (`number`)
                The x-coordinate of the point.

            2. (`number`)
                The y-coordinate of the point.
-------------------------------------------------------------------------------
                                             *love2d-docs-ChainShape:getPoints*
|ChainShape:getPoints|

Returns all points of the shape.

Synopses: ~

        `x1`, `y1`, `x2`, `y2` = |ChainShape:getPoints|()

Variants: ~

        `x1`, `y1`, `x2`, `y2` = |ChainShape:getPoints|()

        See function description

        Returns: ~
            1. (`number`)
                The x-coordinate of the first point.

            2. (`number`)
                The y-coordinate of the first point.

            3. (`number`)
                The x-coordinate of the second point.

            4. (`number`)
                The y-coordinate of the second point.
-------------------------------------------------------------------------------
                                     *love2d-docs-ChainShape:getPreviousVertex*
|ChainShape:getPreviousVertex|

Gets the vertex that establishes a connection to the previous shape.

Setting next and previous ChainShape vertices can help prevent unwanted
collisions when a flat shape slides along the edge and moves over to the new
shape.

Synopses: ~

        `x`, `y` = |ChainShape:getPreviousVertex|()

Variants: ~

        `x`, `y` = |ChainShape:getPreviousVertex|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the vertex, or nil if
                ChainShape:setPreviousVertex hasn't been called.

            2. (`number`)
                The y-component of the vertex, or nil if
                ChainShape:setPreviousVertex hasn't been called.
-------------------------------------------------------------------------------
                                        *love2d-docs-ChainShape:getVertexCount*
|ChainShape:getVertexCount|

Returns the number of vertices the shape has.

Synopses: ~

        `count` = |ChainShape:getVertexCount|()

Variants: ~

        `count` = |ChainShape:getVertexCount|()

        See function description

        Return: ~
            (`number`)
                The number of vertices.
-------------------------------------------------------------------------------
                                         *love2d-docs-ChainShape:setNextVertex*
|ChainShape:setNextVertex|

Sets a vertex that establishes a connection to the next shape.

This can help prevent unwanted collisions when a flat shape slides along the
edge and moves over to the new shape.

Synopses: ~

        |ChainShape:setNextVertex|(`x`, `y`)

Variants: ~

        |ChainShape:setNextVertex|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the vertex.
            • {y} (`number`)
                The y-component of the vertex.


-------------------------------------------------------------------------------
                                     *love2d-docs-ChainShape:setPreviousVertex*
|ChainShape:setPreviousVertex|

Sets a vertex that establishes a connection to the previous shape.

This can help prevent unwanted collisions when a flat shape slides along the
edge and moves over to the new shape.

Synopses: ~

        |ChainShape:setPreviousVertex|(`x`, `y`)

Variants: ~

        |ChainShape:setPreviousVertex|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the vertex.
            • {y} (`number`)
                The y-component of the vertex.



-------------------------------------------------------------------------------
                                                      *love2d-docs-CircleShape*
|CircleShape|

Circle extends Shape and adds a radius and a local position.

                                         *love2d-docs-CircleShape-constructors*
    constructors:

        |newCircleShape|                           |love2d-docs-newCircleShape|

                                           *love2d-docs-CircleShape-supertypes*
    supertypes:

        |Shape|                                             |love2d-docs-Shape|
        |Object|                                           |love2d-docs-Object|

                                             *love2d-docs-CircleShape-subtypes*
    subtypes:

        None

                                            *love2d-docs-CircleShape-functions*
    functions:

        |getPoint|                           |love2d-docs-CircleShape:getPoint|
        |getRadius|                         |love2d-docs-CircleShape:getRadius|
        |setPoint|                           |love2d-docs-CircleShape:setPoint|
        |setRadius|                         |love2d-docs-CircleShape:setRadius|

-------------------------------------------------------------------------------
                                             *love2d-docs-CircleShape:getPoint*
|CircleShape:getPoint|

Gets the center point of the circle shape.

Synopses: ~

        `x`, `y` = |CircleShape:getPoint|()

Variants: ~

        `x`, `y` = |CircleShape:getPoint|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the center point of the circle.

            2. (`number`)
                The y-component of the center point of the circle.
-------------------------------------------------------------------------------
                                            *love2d-docs-CircleShape:getRadius*
|CircleShape:getRadius|

Gets the radius of the circle shape.

Synopses: ~

        `radius` = |CircleShape:getRadius|()

Variants: ~

        `radius` = |CircleShape:getRadius|()

        See function description

        Return: ~
            (`number`)
                The radius of the circle
-------------------------------------------------------------------------------
                                             *love2d-docs-CircleShape:setPoint*
|CircleShape:setPoint|

Sets the location of the center of the circle shape.

Synopses: ~

        |CircleShape:setPoint|(`x`, `y`)

Variants: ~

        |CircleShape:setPoint|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the new center point of the circle.
            • {y} (`number`)
                The y-component of the new center point of the circle.


-------------------------------------------------------------------------------
                                            *love2d-docs-CircleShape:setRadius*
|CircleShape:setRadius|

Sets the radius of the circle.

Synopses: ~

        |CircleShape:setRadius|(`radius`)

Variants: ~

        |CircleShape:setRadius|(`radius`)

        See function description

        Parameters: ~
            • {radius} (`number`)
                The radius of the circle



-------------------------------------------------------------------------------
                                                          *love2d-docs-Contact*
|Contact|

Contacts are objects created to manage collisions in worlds.

                                             *love2d-docs-Contact-constructors*
    constructors:

        None

                                               *love2d-docs-Contact-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                 *love2d-docs-Contact-subtypes*
    subtypes:

        None

                                                *love2d-docs-Contact-functions*
    functions:

        |getChildren|                         |love2d-docs-Contact:getChildren|
        |getFixtures|                         |love2d-docs-Contact:getFixtures|
        |getFriction|                         |love2d-docs-Contact:getFriction|
        |getNormal|                             |love2d-docs-Contact:getNormal|
        |getPositions|                       |love2d-docs-Contact:getPositions|
        |getRestitution|                   |love2d-docs-Contact:getRestitution|
        |isEnabled|                             |love2d-docs-Contact:isEnabled|
        |isTouching|                           |love2d-docs-Contact:isTouching|
        |resetFriction|                     |love2d-docs-Contact:resetFriction|
        |resetRestitution|               |love2d-docs-Contact:resetRestitution|
        |setEnabled|                           |love2d-docs-Contact:setEnabled|
        |setFriction|                         |love2d-docs-Contact:setFriction|
        |setRestitution|                   |love2d-docs-Contact:setRestitution|

-------------------------------------------------------------------------------
                                              *love2d-docs-Contact:getChildren*
|Contact:getChildren|

Gets the child indices of the shapes of the two colliding fixtures. For
ChainShapes, an index of 1 is the first edge in the chain.
Used together with Fixture:rayCast or ChainShape:getChildEdge.

Synopses: ~

        `indexA`, `indexB` = |Contact:getChildren|()

Variants: ~

        `indexA`, `indexB` = |Contact:getChildren|()

        See function description

        Returns: ~
            1. (`number`)
                The child index of the first fixture's shape.

            2. (`number`)
                The child index of the second fixture's shape.
-------------------------------------------------------------------------------
                                              *love2d-docs-Contact:getFixtures*
|Contact:getFixtures|

Gets the two Fixtures that hold the shapes that are in contact.

Synopses: ~

        `fixtureA`, `fixtureB` = |Contact:getFixtures|()

Variants: ~

        `fixtureA`, `fixtureB` = |Contact:getFixtures|()

        See function description

        Returns: ~
            1. (`Fixture`) |love2d-docs-Fixture|
                The first Fixture.

            2. (`Fixture`) |love2d-docs-Fixture|
                The second Fixture.
-------------------------------------------------------------------------------
                                              *love2d-docs-Contact:getFriction*
|Contact:getFriction|

Get the friction between two shapes that are in contact.

Synopses: ~

        `friction` = |Contact:getFriction|()

Variants: ~

        `friction` = |Contact:getFriction|()

        See function description

        Return: ~
            (`number`)
                The friction of the contact.
-------------------------------------------------------------------------------
                                                *love2d-docs-Contact:getNormal*
|Contact:getNormal|

Get the normal vector between two shapes that are in contact.

This function returns the coordinates of a unit vector that points from the
first shape to the second.

Synopses: ~

        `nx`, `ny` = |Contact:getNormal|()

Variants: ~

        `nx`, `ny` = |Contact:getNormal|()

        See function description

        Returns: ~
            1. (`number`)
                The x component of the normal vector.

            2. (`number`)
                The y component of the normal vector.
-------------------------------------------------------------------------------
                                             *love2d-docs-Contact:getPositions*
|Contact:getPositions|

Returns the contact points of the two colliding fixtures. There can be one or
two points.

Synopses: ~

        `x1`, `y1`, `x2`, `y2` = |Contact:getPositions|()

Variants: ~

        `x1`, `y1`, `x2`, `y2` = |Contact:getPositions|()

        See function description

        Returns: ~
            1. (`number`)
                The x coordinate of the first contact point.

            2. (`number`)
                The y coordinate of the first contact point.

            3. (`number`)
                The x coordinate of the second contact point.

            4. (`number`)
                The y coordinate of the second contact point.
-------------------------------------------------------------------------------
                                           *love2d-docs-Contact:getRestitution*
|Contact:getRestitution|

Get the restitution between two shapes that are in contact.

Synopses: ~

        `restitution` = |Contact:getRestitution|()

Variants: ~

        `restitution` = |Contact:getRestitution|()

        See function description

        Return: ~
            (`number`)
                The restitution between the two shapes.
-------------------------------------------------------------------------------
                                                *love2d-docs-Contact:isEnabled*
|Contact:isEnabled|

Returns whether the contact is enabled. The collision will be ignored if a
contact gets disabled in the preSolve callback.

Synopses: ~

        `enabled` = |Contact:isEnabled|()

Variants: ~

        `enabled` = |Contact:isEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false otherwise.
-------------------------------------------------------------------------------
                                               *love2d-docs-Contact:isTouching*
|Contact:isTouching|

Returns whether the two colliding fixtures are touching each other.

Synopses: ~

        `touching` = |Contact:isTouching|()

Variants: ~

        `touching` = |Contact:isTouching|()

        See function description

        Return: ~
            (`boolean`)
                True if they touch or false if not.
-------------------------------------------------------------------------------
                                            *love2d-docs-Contact:resetFriction*
|Contact:resetFriction|

Resets the contact friction to the mixture value of both fixtures.

Synopses: ~

        |Contact:resetFriction|()

Variants: ~

        |Contact:resetFriction|()

        See function description


-------------------------------------------------------------------------------
                                         *love2d-docs-Contact:resetRestitution*
|Contact:resetRestitution|

Resets the contact restitution to the mixture value of both fixtures.

Synopses: ~

        |Contact:resetRestitution|()

Variants: ~

        |Contact:resetRestitution|()

        See function description


-------------------------------------------------------------------------------
                                               *love2d-docs-Contact:setEnabled*
|Contact:setEnabled|

Enables or disables the contact.

Synopses: ~

        |Contact:setEnabled|(`enabled`)

Variants: ~

        |Contact:setEnabled|(`enabled`)

        See function description

        Parameters: ~
            • {enabled} (`boolean`)
                True to enable or false to disable.


-------------------------------------------------------------------------------
                                              *love2d-docs-Contact:setFriction*
|Contact:setFriction|

Sets the contact friction.

Synopses: ~

        |Contact:setFriction|(`friction`)

Variants: ~

        |Contact:setFriction|(`friction`)

        See function description

        Parameters: ~
            • {friction} (`number`)
                The contact friction.


-------------------------------------------------------------------------------
                                           *love2d-docs-Contact:setRestitution*
|Contact:setRestitution|

Sets the contact restitution.

Synopses: ~

        |Contact:setRestitution|(`restitution`)

Variants: ~

        |Contact:setRestitution|(`restitution`)

        See function description

        Parameters: ~
            • {restitution} (`number`)
                The contact restitution.



-------------------------------------------------------------------------------
                                                    *love2d-docs-DistanceJoint*
|DistanceJoint|

Keeps two bodies at the same distance.

                                       *love2d-docs-DistanceJoint-constructors*
    constructors:

        |newDistanceJoint|                       |love2d-docs-newDistanceJoint|

                                         *love2d-docs-DistanceJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                           *love2d-docs-DistanceJoint-subtypes*
    subtypes:

        None

                                          *love2d-docs-DistanceJoint-functions*
    functions:

        |getDampingRatio|           |love2d-docs-DistanceJoint:getDampingRatio|
        |getFrequency|                 |love2d-docs-DistanceJoint:getFrequency|
        |getLength|                       |love2d-docs-DistanceJoint:getLength|
        |setDampingRatio|           |love2d-docs-DistanceJoint:setDampingRatio|
        |setFrequency|                 |love2d-docs-DistanceJoint:setFrequency|
        |setLength|                       |love2d-docs-DistanceJoint:setLength|

-------------------------------------------------------------------------------
                                    *love2d-docs-DistanceJoint:getDampingRatio*
|DistanceJoint:getDampingRatio|

Gets the damping ratio.

Synopses: ~

        `ratio` = |DistanceJoint:getDampingRatio|()

Variants: ~

        `ratio` = |DistanceJoint:getDampingRatio|()

        See function description

        Return: ~
            (`number`)
                The damping ratio.
-------------------------------------------------------------------------------
                                       *love2d-docs-DistanceJoint:getFrequency*
|DistanceJoint:getFrequency|

Gets the response speed.

Synopses: ~

        `Hz` = |DistanceJoint:getFrequency|()

Variants: ~

        `Hz` = |DistanceJoint:getFrequency|()

        See function description

        Return: ~
            (`number`)
                The response speed.
-------------------------------------------------------------------------------
                                          *love2d-docs-DistanceJoint:getLength*
|DistanceJoint:getLength|

Gets the equilibrium distance between the two Bodies.

Synopses: ~

        `l` = |DistanceJoint:getLength|()

Variants: ~

        `l` = |DistanceJoint:getLength|()

        See function description

        Return: ~
            (`number`)
                The length between the two Bodies.
-------------------------------------------------------------------------------
                                    *love2d-docs-DistanceJoint:setDampingRatio*
|DistanceJoint:setDampingRatio|

Sets the damping ratio.

Synopses: ~

        |DistanceJoint:setDampingRatio|(`ratio`)

Variants: ~

        |DistanceJoint:setDampingRatio|(`ratio`)

        See function description

        Parameters: ~
            • {ratio} (`number`)
                The damping ratio.


-------------------------------------------------------------------------------
                                       *love2d-docs-DistanceJoint:setFrequency*
|DistanceJoint:setFrequency|

Sets the response speed.

Synopses: ~

        |DistanceJoint:setFrequency|(`Hz`)

Variants: ~

        |DistanceJoint:setFrequency|(`Hz`)

        See function description

        Parameters: ~
            • {Hz} (`number`)
                The response speed.


-------------------------------------------------------------------------------
                                          *love2d-docs-DistanceJoint:setLength*
|DistanceJoint:setLength|

Sets the equilibrium distance between the two Bodies.

Synopses: ~

        |DistanceJoint:setLength|(`l`)

Variants: ~

        |DistanceJoint:setLength|(`l`)

        See function description

        Parameters: ~
            • {l} (`number`)
                The length between the two Bodies.



-------------------------------------------------------------------------------
                                                        *love2d-docs-EdgeShape*
|EdgeShape|

A EdgeShape is a line segment. They can be used to create the boundaries of
your terrain. The shape does not have volume and can only collide with
PolygonShape and CircleShape.

                                           *love2d-docs-EdgeShape-constructors*
    constructors:

        |newEdgeShape|                               |love2d-docs-newEdgeShape|

                                             *love2d-docs-EdgeShape-supertypes*
    supertypes:

        |Shape|                                             |love2d-docs-Shape|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-EdgeShape-subtypes*
    subtypes:

        None

                                              *love2d-docs-EdgeShape-functions*
    functions:

        |getNextVertex|                   |love2d-docs-EdgeShape:getNextVertex|
        |getPoints|                           |love2d-docs-EdgeShape:getPoints|
        |getPreviousVertex|           |love2d-docs-EdgeShape:getPreviousVertex|
        |setNextVertex|                   |love2d-docs-EdgeShape:setNextVertex|
        |setPreviousVertex|           |love2d-docs-EdgeShape:setPreviousVertex|

-------------------------------------------------------------------------------
                                          *love2d-docs-EdgeShape:getNextVertex*
|EdgeShape:getNextVertex|

Gets the vertex that establishes a connection to the next shape.

Setting next and previous EdgeShape vertices can help prevent unwanted
collisions when a flat shape slides along the edge and moves over to the new
shape.

Synopses: ~

        `x`, `y` = |EdgeShape:getNextVertex|()

Variants: ~

        `x`, `y` = |EdgeShape:getNextVertex|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the vertex, or nil if
                EdgeShape:setNextVertex hasn't been called.

            2. (`number`)
                The y-component of the vertex, or nil if
                EdgeShape:setNextVertex hasn't been called.
-------------------------------------------------------------------------------
                                              *love2d-docs-EdgeShape:getPoints*
|EdgeShape:getPoints|

Returns the local coordinates of the edge points.

Synopses: ~

        `x1`, `y1`, `x2`, `y2` = |EdgeShape:getPoints|()

Variants: ~

        `x1`, `y1`, `x2`, `y2` = |EdgeShape:getPoints|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the first vertex.

            2. (`number`)
                The y-component of the first vertex.

            3. (`number`)
                The x-component of the second vertex.

            4. (`number`)
                The y-component of the second vertex.
-------------------------------------------------------------------------------
                                      *love2d-docs-EdgeShape:getPreviousVertex*
|EdgeShape:getPreviousVertex|

Gets the vertex that establishes a connection to the previous shape.

Setting next and previous EdgeShape vertices can help prevent unwanted
collisions when a flat shape slides along the edge and moves over to the new
shape.

Synopses: ~

        `x`, `y` = |EdgeShape:getPreviousVertex|()

Variants: ~

        `x`, `y` = |EdgeShape:getPreviousVertex|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the vertex, or nil if
                EdgeShape:setPreviousVertex hasn't been called.

            2. (`number`)
                The y-component of the vertex, or nil if
                EdgeShape:setPreviousVertex hasn't been called.
-------------------------------------------------------------------------------
                                          *love2d-docs-EdgeShape:setNextVertex*
|EdgeShape:setNextVertex|

Sets a vertex that establishes a connection to the next shape.

This can help prevent unwanted collisions when a flat shape slides along the
edge and moves over to the new shape.

Synopses: ~

        |EdgeShape:setNextVertex|(`x`, `y`)

Variants: ~

        |EdgeShape:setNextVertex|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the vertex.
            • {y} (`number`)
                The y-component of the vertex.


-------------------------------------------------------------------------------
                                      *love2d-docs-EdgeShape:setPreviousVertex*
|EdgeShape:setPreviousVertex|

Sets a vertex that establishes a connection to the previous shape.

This can help prevent unwanted collisions when a flat shape slides along the
edge and moves over to the new shape.

Synopses: ~

        |EdgeShape:setPreviousVertex|(`x`, `y`)

Variants: ~

        |EdgeShape:setPreviousVertex|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the vertex.
            • {y} (`number`)
                The y-component of the vertex.



-------------------------------------------------------------------------------
                                                          *love2d-docs-Fixture*
|Fixture|

Fixtures attach shapes to bodies.

                                             *love2d-docs-Fixture-constructors*
    constructors:

        |newFixture|                                   |love2d-docs-newFixture|

                                               *love2d-docs-Fixture-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                 *love2d-docs-Fixture-subtypes*
    subtypes:

        None

                                                *love2d-docs-Fixture-functions*
    functions:

        |destroy|                                 |love2d-docs-Fixture:destroy|
        |getBody|                                 |love2d-docs-Fixture:getBody|
        |getBoundingBox|                   |love2d-docs-Fixture:getBoundingBox|
        |getCategory|                         |love2d-docs-Fixture:getCategory|
        |getDensity|                           |love2d-docs-Fixture:getDensity|
        |getFilterData|                     |love2d-docs-Fixture:getFilterData|
        |getFriction|                         |love2d-docs-Fixture:getFriction|
        |getGroupIndex|                     |love2d-docs-Fixture:getGroupIndex|
        |getMask|                                 |love2d-docs-Fixture:getMask|
        |getMassData|                         |love2d-docs-Fixture:getMassData|
        |getRestitution|                   |love2d-docs-Fixture:getRestitution|
        |getShape|                               |love2d-docs-Fixture:getShape|
        |getUserData|                         |love2d-docs-Fixture:getUserData|
        |isDestroyed|                         |love2d-docs-Fixture:isDestroyed|
        |isSensor|                               |love2d-docs-Fixture:isSensor|
        |rayCast|                                 |love2d-docs-Fixture:rayCast|
        |setCategory|                         |love2d-docs-Fixture:setCategory|
        |setDensity|                           |love2d-docs-Fixture:setDensity|
        |setFilterData|                     |love2d-docs-Fixture:setFilterData|
        |setFriction|                         |love2d-docs-Fixture:setFriction|
        |setGroupIndex|                     |love2d-docs-Fixture:setGroupIndex|
        |setMask|                                 |love2d-docs-Fixture:setMask|
        |setRestitution|                   |love2d-docs-Fixture:setRestitution|
        |setSensor|                             |love2d-docs-Fixture:setSensor|
        |setUserData|                         |love2d-docs-Fixture:setUserData|
        |testPoint|                             |love2d-docs-Fixture:testPoint|

-------------------------------------------------------------------------------
                                                  *love2d-docs-Fixture:destroy*
|Fixture:destroy|

Destroys the fixture.

Synopses: ~

        |Fixture:destroy|()

Variants: ~

        |Fixture:destroy|()

        See function description


-------------------------------------------------------------------------------
                                                  *love2d-docs-Fixture:getBody*
|Fixture:getBody|

Returns the body to which the fixture is attached.

Synopses: ~

        `body` = |Fixture:getBody|()

Variants: ~

        `body` = |Fixture:getBody|()

        See function description

        Return: ~
            (`Body`) |love2d-docs-Body|
                The parent body.
-------------------------------------------------------------------------------
                                           *love2d-docs-Fixture:getBoundingBox*
|Fixture:getBoundingBox|

Returns the points of the fixture bounding box. In case the fixture has
multiple children a 1-based index can be specified. For example, a fixture will
have multiple children with a chain shape.

Synopses: ~

        `topLeftX`, `topLeftY`, `bottomRightX`, `bottomRightY` =
        |Fixture:getBoundingBox|(`index`)

Variants: ~

        `topLeftX`, `topLeftY`, `bottomRightX`, `bottomRightY` =
        |Fixture:getBoundingBox|(`index`)

        See function description

        Parameters: ~
            • {index} (`number`) (default: `1`)
                A bounding box of the fixture.

        Returns: ~
            1. (`number`)
                The x position of the top-left point.

            2. (`number`)
                The y position of the top-left point.

            3. (`number`)
                The x position of the bottom-right point.

            4. (`number`)
                The y position of the bottom-right point.
-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:getCategory*
|Fixture:getCategory|

Returns the categories the fixture belongs to.

Synopses: ~

        `...` = |Fixture:getCategory|()

Variants: ~

        `...` = |Fixture:getCategory|()

        See function description

        Return: ~
            (`number`)
                The categories.
-------------------------------------------------------------------------------
                                               *love2d-docs-Fixture:getDensity*
|Fixture:getDensity|

Returns the density of the fixture.

Synopses: ~

        `density` = |Fixture:getDensity|()

Variants: ~

        `density` = |Fixture:getDensity|()

        See function description

        Return: ~
            (`number`)
                The fixture density in kilograms per square meter.
-------------------------------------------------------------------------------
                                            *love2d-docs-Fixture:getFilterData*
|Fixture:getFilterData|

Returns the filter data of the fixture.

Categories and masks are encoded as the bits of a 16-bit integer.

Synopses: ~

        `categories`, `mask`, `group` = |Fixture:getFilterData|()

Variants: ~

        `categories`, `mask`, `group` = |Fixture:getFilterData|()

        See function description

        Returns: ~
            1. (`number`)
                The categories as an integer from 0 to 65535.

            2. (`number`)
                The mask as an integer from 0 to 65535.

            3. (`number`)
                The group as an integer from -32768 to 32767.
-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:getFriction*
|Fixture:getFriction|

Returns the friction of the fixture.

Synopses: ~

        `friction` = |Fixture:getFriction|()

Variants: ~

        `friction` = |Fixture:getFriction|()

        See function description

        Return: ~
            (`number`)
                The fixture friction.
-------------------------------------------------------------------------------
                                            *love2d-docs-Fixture:getGroupIndex*
|Fixture:getGroupIndex|

Returns the group the fixture belongs to. Fixtures with the same group will
always collide if the group is positive or never collide if it's negative. The
group zero means no group.

The groups range from -32768 to 32767.

Synopses: ~

        `group` = |Fixture:getGroupIndex|()

Variants: ~

        `group` = |Fixture:getGroupIndex|()

        See function description

        Return: ~
            (`number`)
                The group of the fixture.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Fixture:getMask*
|Fixture:getMask|

Returns which categories this fixture should '''NOT''' collide with.

Synopses: ~

        `...` = |Fixture:getMask|()

Variants: ~

        `...` = |Fixture:getMask|()

        See function description

        Return: ~
            (`number`)
                The masks.
-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:getMassData*
|Fixture:getMassData|

Returns the mass, its center and the rotational inertia.

Synopses: ~

        `x`, `y`, `mass`, `inertia` = |Fixture:getMassData|()

Variants: ~

        `x`, `y`, `mass`, `inertia` = |Fixture:getMassData|()

        See function description

        Returns: ~
            1. (`number`)
                The x position of the center of mass.

            2. (`number`)
                The y position of the center of mass.

            3. (`number`)
                The mass of the fixture.

            4. (`number`)
                The rotational inertia.
-------------------------------------------------------------------------------
                                           *love2d-docs-Fixture:getRestitution*
|Fixture:getRestitution|

Returns the restitution of the fixture.

Synopses: ~

        `restitution` = |Fixture:getRestitution|()

Variants: ~

        `restitution` = |Fixture:getRestitution|()

        See function description

        Return: ~
            (`number`)
                The fixture restitution.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Fixture:getShape*
|Fixture:getShape|

Returns the shape of the fixture. This shape is a reference to the actual data
used in the simulation. It's possible to change its values between timesteps.

Synopses: ~

        `shape` = |Fixture:getShape|()

Variants: ~

        `shape` = |Fixture:getShape|()

        See function description

        Return: ~
            (`Shape`) |love2d-docs-Shape|
                The fixture's shape.
-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:getUserData*
|Fixture:getUserData|

Returns the Lua value associated with this fixture.

Synopses: ~

        `value` = |Fixture:getUserData|()

Variants: ~

        `value` = |Fixture:getUserData|()

        See function description

        Return: ~
            (`any`)
                The Lua value associated with the fixture.
-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:isDestroyed*
|Fixture:isDestroyed|

Gets whether the Fixture is destroyed. Destroyed fixtures cannot be used.

Synopses: ~

        `destroyed` = |Fixture:isDestroyed|()

Variants: ~

        `destroyed` = |Fixture:isDestroyed|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Fixture is destroyed.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Fixture:isSensor*
|Fixture:isSensor|

Returns whether the fixture is a sensor.

Synopses: ~

        `sensor` = |Fixture:isSensor|()

Variants: ~

        `sensor` = |Fixture:isSensor|()

        See function description

        Return: ~
            (`boolean`)
                If the fixture is a sensor.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Fixture:rayCast*
|Fixture:rayCast|

Casts a ray against the shape of the fixture and returns the surface normal
vector and the line position where the ray hit. If the ray missed the shape,
nil will be returned.

The ray starts on the first point of the input line and goes towards the second
point of the line. The fifth argument is the maximum distance the ray is going
to travel as a scale factor of the input line length.

The childIndex parameter is used to specify which child of a parent shape, such
as a ChainShape, will be ray casted. For ChainShapes, the index of 1 is the
first edge on the chain. Ray casting a parent shape will only test the child
specified so if you want to test every shape of the parent, you must loop
through all of its children.

The world position of the impact can be calculated by multiplying the line
vector with the third return value and adding it to the line starting point.

hitx, hity = x1 + (x2 - x1) * fraction, y1 + (y2 - y1) * fraction

Synopses: ~

        `xn`, `yn`, `fraction` = |Fixture:rayCast|(`x1`, `y1`, `x2`,
        `y2`, `maxFraction`, `childIndex`)

Variants: ~

        `xn`, `yn`, `fraction` = |Fixture:rayCast|(`x1`, `y1`, `x2`,
        `y2`, `maxFraction`, `childIndex`)

        See function description

        Parameters: ~
            • {x1} (`number`)
                The x position of the input line starting point.
            • {y1} (`number`)
                The y position of the input line starting point.
            • {x2} (`number`)
                The x position of the input line end point.
            • {y2} (`number`)
                The y position of the input line end point.
            • {maxFraction} (`number`)
                Ray length parameter.
            • {childIndex} (`number`) (default: `1`)
                The index of the child the ray gets cast against.

        Returns: ~
            1. (`number`)
                The x component of the normal vector of the edge where the ray
                hit the shape.

            2. (`number`)
                The y component of the normal vector of the edge where the ray
                hit the shape.

            3. (`number`)
                The position on the input line where the intersection happened
                as a factor of the line length.
-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:setCategory*
|Fixture:setCategory|

Sets the categories the fixture belongs to. There can be up to 16 categories
represented as a number from 1 to 16.

All fixture's default category is 1.

Synopses: ~

        |Fixture:setCategory|(`...`)

Variants: ~

        |Fixture:setCategory|(`...`)

        See function description

        Parameters: ~
            • {...} (`number`)
                The categories.


-------------------------------------------------------------------------------
                                               *love2d-docs-Fixture:setDensity*
|Fixture:setDensity|

Sets the density of the fixture. Call Body:resetMassData if this needs to take
effect immediately.

Synopses: ~

        |Fixture:setDensity|(`density`)

Variants: ~

        |Fixture:setDensity|(`density`)

        See function description

        Parameters: ~
            • {density} (`number`)
                The fixture density in kilograms per square meter.


-------------------------------------------------------------------------------
                                            *love2d-docs-Fixture:setFilterData*
|Fixture:setFilterData|

Sets the filter data of the fixture.

Groups, categories, and mask can be used to define the collision behaviour of
the fixture.

If two fixtures are in the same group they either always collide if the group
is positive, or never collide if it's negative. If the group is zero or they do
not match, then the contact filter checks if the fixtures select a category of
the other fixture with their masks. The fixtures do not collide if that's not
the case. If they do have each other's categories selected, the return value of
the custom contact filter will be used. They always collide if none was set.

There can be up to 16 categories. Categories and masks are encoded as the bits
of a 16-bit integer.

When created, prior to calling this function, all fixtures have category set to
1, mask set to 65535 (all categories) and group set to 0.

This function allows setting all filter data for a fixture at once. To set only
the categories, the mask or the group, you can use Fixture:setCategory,
Fixture:setMask or Fixture:setGroupIndex respectively.

Synopses: ~

        |Fixture:setFilterData|(`categories`, `mask`, `group`)

Variants: ~

        |Fixture:setFilterData|(`categories`, `mask`, `group`)

        See function description

        Parameters: ~
            • {categories} (`number`)
                The categories as an integer from 0 to 65535.
            • {mask} (`number`)
                The mask as an integer from 0 to 65535.
            • {group} (`number`)
                The group as an integer from -32768 to 32767.


-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:setFriction*
|Fixture:setFriction|

Sets the friction of the fixture.

Friction determines how shapes react when they 'slide' along other shapes. Low
friction indicates a slippery surface, like ice, while high friction indicates
a rough surface, like concrete. Range: 0.0 - 1.0.

Synopses: ~

        |Fixture:setFriction|(`friction`)

Variants: ~

        |Fixture:setFriction|(`friction`)

        See function description

        Parameters: ~
            • {friction} (`number`)
                The fixture friction.


-------------------------------------------------------------------------------
                                            *love2d-docs-Fixture:setGroupIndex*
|Fixture:setGroupIndex|

Sets the group the fixture belongs to. Fixtures with the same group will always
collide if the group is positive or never collide if it's negative. The group
zero means no group.

The groups range from -32768 to 32767.

Synopses: ~

        |Fixture:setGroupIndex|(`group`)

Variants: ~

        |Fixture:setGroupIndex|(`group`)

        See function description

        Parameters: ~
            • {group} (`number`)
                The group as an integer from -32768 to 32767.


-------------------------------------------------------------------------------
                                                  *love2d-docs-Fixture:setMask*
|Fixture:setMask|

Sets the category mask of the fixture. There can be up to 16 categories
represented as a number from 1 to 16.

This fixture will '''NOT''' collide with the fixtures that are in the selected
categories if the other fixture also has a category of this fixture selected.

Synopses: ~

        |Fixture:setMask|(`...`)

Variants: ~

        |Fixture:setMask|(`...`)

        See function description

        Parameters: ~
            • {...} (`number`)
                The masks.


-------------------------------------------------------------------------------
                                           *love2d-docs-Fixture:setRestitution*
|Fixture:setRestitution|

Sets the restitution of the fixture.

Synopses: ~

        |Fixture:setRestitution|(`restitution`)

Variants: ~

        |Fixture:setRestitution|(`restitution`)

        See function description

        Parameters: ~
            • {restitution} (`number`)
                The fixture restitution.


-------------------------------------------------------------------------------
                                                *love2d-docs-Fixture:setSensor*
|Fixture:setSensor|

Sets whether the fixture should act as a sensor.

Sensors do not cause collision responses, but the begin-contact and end-contact
World callbacks will still be called for this fixture.

Synopses: ~

        |Fixture:setSensor|(`sensor`)

Variants: ~

        |Fixture:setSensor|(`sensor`)

        See function description

        Parameters: ~
            • {sensor} (`boolean`)
                The sensor status.


-------------------------------------------------------------------------------
                                              *love2d-docs-Fixture:setUserData*
|Fixture:setUserData|

Associates a Lua value with the fixture.

To delete the reference, explicitly pass nil.

Synopses: ~

        |Fixture:setUserData|(`value`)

Variants: ~

        |Fixture:setUserData|(`value`)

        See function description

        Parameters: ~
            • {value} (`any`)
                The Lua value to associate with the fixture.


-------------------------------------------------------------------------------
                                                *love2d-docs-Fixture:testPoint*
|Fixture:testPoint|

Checks if a point is inside the shape of the fixture.

Synopses: ~

        `isInside` = |Fixture:testPoint|(`x`, `y`)

Variants: ~

        `isInside` = |Fixture:testPoint|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x position of the point.
            • {y} (`number`)
                The y position of the point.

        Return: ~
            (`boolean`)
                True if the point is inside or false if it is outside.

-------------------------------------------------------------------------------
                                                    *love2d-docs-FrictionJoint*
|FrictionJoint|

A FrictionJoint applies friction to a body.

                                       *love2d-docs-FrictionJoint-constructors*
    constructors:

        |newFrictionJoint|                       |love2d-docs-newFrictionJoint|

                                         *love2d-docs-FrictionJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                           *love2d-docs-FrictionJoint-subtypes*
    subtypes:

        None

                                          *love2d-docs-FrictionJoint-functions*
    functions:

        |getMaxForce|                   |love2d-docs-FrictionJoint:getMaxForce|
        |getMaxTorque|                 |love2d-docs-FrictionJoint:getMaxTorque|
        |setMaxForce|                   |love2d-docs-FrictionJoint:setMaxForce|
        |setMaxTorque|                 |love2d-docs-FrictionJoint:setMaxTorque|

-------------------------------------------------------------------------------
                                        *love2d-docs-FrictionJoint:getMaxForce*
|FrictionJoint:getMaxForce|

Gets the maximum friction force in Newtons.

Synopses: ~

        `force` = |FrictionJoint:getMaxForce|()

Variants: ~

        `force` = |FrictionJoint:getMaxForce|()

        See function description

        Return: ~
            (`number`)
                Maximum force in Newtons.
-------------------------------------------------------------------------------
                                       *love2d-docs-FrictionJoint:getMaxTorque*
|FrictionJoint:getMaxTorque|

Gets the maximum friction torque in Newton-meters.

Synopses: ~

        `torque` = |FrictionJoint:getMaxTorque|()

Variants: ~

        `torque` = |FrictionJoint:getMaxTorque|()

        See function description

        Return: ~
            (`number`)
                Maximum torque in Newton-meters.
-------------------------------------------------------------------------------
                                        *love2d-docs-FrictionJoint:setMaxForce*
|FrictionJoint:setMaxForce|

Sets the maximum friction force in Newtons.

Synopses: ~

        |FrictionJoint:setMaxForce|(`maxForce`)

Variants: ~

        |FrictionJoint:setMaxForce|(`maxForce`)

        See function description

        Parameters: ~
            • {maxForce} (`number`)
                Max force in Newtons.


-------------------------------------------------------------------------------
                                       *love2d-docs-FrictionJoint:setMaxTorque*
|FrictionJoint:setMaxTorque|

Sets the maximum friction torque in Newton-meters.

Synopses: ~

        |FrictionJoint:setMaxTorque|(`torque`)

Variants: ~

        |FrictionJoint:setMaxTorque|(`torque`)

        See function description

        Parameters: ~
            • {torque} (`number`)
                Maximum torque in Newton-meters.



-------------------------------------------------------------------------------
                                                        *love2d-docs-GearJoint*
|GearJoint|

Keeps bodies together in such a way that they act like gears.

                                           *love2d-docs-GearJoint-constructors*
    constructors:

        |newGearJoint|                               |love2d-docs-newGearJoint|

                                             *love2d-docs-GearJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-GearJoint-subtypes*
    subtypes:

        None

                                              *love2d-docs-GearJoint-functions*
    functions:

        |getJoints|                           |love2d-docs-GearJoint:getJoints|
        |getRatio|                             |love2d-docs-GearJoint:getRatio|
        |setRatio|                             |love2d-docs-GearJoint:setRatio|

-------------------------------------------------------------------------------
                                              *love2d-docs-GearJoint:getJoints*
|GearJoint:getJoints|

Get the Joints connected by this GearJoint.

Synopses: ~

        `joint1`, `joint2` = |GearJoint:getJoints|()

Variants: ~

        `joint1`, `joint2` = |GearJoint:getJoints|()

        See function description

        Returns: ~
            1. (`Joint`) |love2d-docs-Joint|
                The first connected Joint.

            2. (`Joint`) |love2d-docs-Joint|
                The second connected Joint.
-------------------------------------------------------------------------------
                                               *love2d-docs-GearJoint:getRatio*
|GearJoint:getRatio|

Get the ratio of a gear joint.

Synopses: ~

        `ratio` = |GearJoint:getRatio|()

Variants: ~

        `ratio` = |GearJoint:getRatio|()

        See function description

        Return: ~
            (`number`)
                The ratio of the joint.
-------------------------------------------------------------------------------
                                               *love2d-docs-GearJoint:setRatio*
|GearJoint:setRatio|

Set the ratio of a gear joint.

Synopses: ~

        |GearJoint:setRatio|(`ratio`)

Variants: ~

        |GearJoint:setRatio|(`ratio`)

        See function description

        Parameters: ~
            • {ratio} (`number`)
                The new ratio of the joint.



-------------------------------------------------------------------------------
                                                            *love2d-docs-Joint*
|Joint|

Attach multiple bodies together to interact in unique ways.

                                               *love2d-docs-Joint-constructors*
    constructors:

        None

                                                 *love2d-docs-Joint-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                   *love2d-docs-Joint-subtypes*
    subtypes:

        None

                                                  *love2d-docs-Joint-functions*
    functions:

        |destroy|                                   |love2d-docs-Joint:destroy|
        |getAnchors|                             |love2d-docs-Joint:getAnchors|
        |getBodies|                               |love2d-docs-Joint:getBodies|
        |getCollideConnected|           |love2d-docs-Joint:getCollideConnected|
        |getReactionForce|                 |love2d-docs-Joint:getReactionForce|
        |getReactionTorque|               |love2d-docs-Joint:getReactionTorque|
        |getType|                                   |love2d-docs-Joint:getType|
        |getUserData|                           |love2d-docs-Joint:getUserData|
        |isDestroyed|                           |love2d-docs-Joint:isDestroyed|
        |setUserData|                           |love2d-docs-Joint:setUserData|

-------------------------------------------------------------------------------
                                                    *love2d-docs-Joint:destroy*
|Joint:destroy|

Explicitly destroys the Joint. An error will occur if you attempt to use the
object after calling this function.

In 0.7.2, when you don't have time to wait for garbage collection, this
function

may be used to free the object immediately.

Synopses: ~

        |Joint:destroy|()

Variants: ~

        |Joint:destroy|()

        See function description


-------------------------------------------------------------------------------
                                                 *love2d-docs-Joint:getAnchors*
|Joint:getAnchors|

Get the anchor points of the joint.

Synopses: ~

        `x1`, `y1`, `x2`, `y2` = |Joint:getAnchors|()

Variants: ~

        `x1`, `y1`, `x2`, `y2` = |Joint:getAnchors|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the anchor on Body 1.

            2. (`number`)
                The y-component of the anchor on Body 1.

            3. (`number`)
                The x-component of the anchor on Body 2.

            4. (`number`)
                The y-component of the anchor on Body 2.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Joint:getBodies*
|Joint:getBodies|

Gets the bodies that the Joint is attached to.

Synopses: ~

        `bodyA`, `bodyB` = |Joint:getBodies|()

Variants: ~

        `bodyA`, `bodyB` = |Joint:getBodies|()

        See function description

        Returns: ~
            1. (`Body`) |love2d-docs-Body|
                The first Body.

            2. (`Body`) |love2d-docs-Body|
                The second Body.
-------------------------------------------------------------------------------
                                        *love2d-docs-Joint:getCollideConnected*
|Joint:getCollideConnected|

Gets whether the connected Bodies collide.

Synopses: ~

        `c` = |Joint:getCollideConnected|()

Variants: ~

        `c` = |Joint:getCollideConnected|()

        See function description

        Return: ~
            (`boolean`)
                True if they collide, false otherwise.
-------------------------------------------------------------------------------
                                           *love2d-docs-Joint:getReactionForce*
|Joint:getReactionForce|

Returns the reaction force in newtons on the second body

Synopses: ~

        `x`, `y` = |Joint:getReactionForce|(`x`)

Variants: ~

        `x`, `y` = |Joint:getReactionForce|(`x`)

        See function description

        Parameters: ~
            • {x} (`number`)
                How long the force applies. Usually the inverse time step or
                1/dt.

        Returns: ~
            1. (`number`)
                The x-component of the force.

            2. (`number`)
                The y-component of the force.
-------------------------------------------------------------------------------
                                          *love2d-docs-Joint:getReactionTorque*
|Joint:getReactionTorque|

Returns the reaction torque on the second body.

Synopses: ~

        `torque` = |Joint:getReactionTorque|(`invdt`)

Variants: ~

        `torque` = |Joint:getReactionTorque|(`invdt`)

        See function description

        Parameters: ~
            • {invdt} (`number`)
                How long the force applies. Usually the inverse time step or
                1/dt.

        Return: ~
            (`number`)
                The reaction torque on the second body.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Joint:getType*
|Joint:getType|

Gets a string representing the type.

Synopses: ~

        `type` = |Joint:getType|()

Variants: ~

        `type` = |Joint:getType|()

        See function description

        Return: ~
            (`JointType`) |love2d-docs-JointType|
                A string with the name of the Joint type.
-------------------------------------------------------------------------------
                                                *love2d-docs-Joint:getUserData*
|Joint:getUserData|

Returns the Lua value associated with this Joint.

Synopses: ~

        `value` = |Joint:getUserData|()

Variants: ~

        `value` = |Joint:getUserData|()

        See function description

        Return: ~
            (`any`)
                The Lua value associated with the Joint.
-------------------------------------------------------------------------------
                                                *love2d-docs-Joint:isDestroyed*
|Joint:isDestroyed|

Gets whether the Joint is destroyed. Destroyed joints cannot be used.

Synopses: ~

        `destroyed` = |Joint:isDestroyed|()

Variants: ~

        `destroyed` = |Joint:isDestroyed|()

        See function description

        Return: ~
            (`boolean`)
                Whether the Joint is destroyed.
-------------------------------------------------------------------------------
                                                *love2d-docs-Joint:setUserData*
|Joint:setUserData|

Associates a Lua value with the Joint.

To delete the reference, explicitly pass nil.

Synopses: ~

        |Joint:setUserData|(`value`)

Variants: ~

        |Joint:setUserData|(`value`)

        See function description

        Parameters: ~
            • {value} (`any`)
                The Lua value to associate with the Joint.



-------------------------------------------------------------------------------
                                                       *love2d-docs-MotorJoint*
|MotorJoint|

Controls the relative motion between two Bodies. Position and rotation offsets
can be specified, as well as the maximum motor force and torque that will be
applied to reach the target offsets.

                                          *love2d-docs-MotorJoint-constructors*
    constructors:

        |newMotorJoint|                             |love2d-docs-newMotorJoint|

                                            *love2d-docs-MotorJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                              *love2d-docs-MotorJoint-subtypes*
    subtypes:

        None

                                             *love2d-docs-MotorJoint-functions*
    functions:

        |getAngularOffset|            |love2d-docs-MotorJoint:getAngularOffset|
        |getLinearOffset|              |love2d-docs-MotorJoint:getLinearOffset|
        |setAngularOffset|            |love2d-docs-MotorJoint:setAngularOffset|
        |setLinearOffset|              |love2d-docs-MotorJoint:setLinearOffset|

-------------------------------------------------------------------------------
                                      *love2d-docs-MotorJoint:getAngularOffset*
|MotorJoint:getAngularOffset|

Gets the target angular offset between the two Bodies the Joint is attached to.

Synopses: ~

        `angleoffset` = |MotorJoint:getAngularOffset|()

Variants: ~

        `angleoffset` = |MotorJoint:getAngularOffset|()

        See function description

        Return: ~
            (`number`)
                The target angular offset in radians: the second body's angle
                minus the first body's angle.
-------------------------------------------------------------------------------
                                       *love2d-docs-MotorJoint:getLinearOffset*
|MotorJoint:getLinearOffset|

Gets the target linear offset between the two Bodies the Joint is attached to.

Synopses: ~

        `x`, `y` = |MotorJoint:getLinearOffset|()

Variants: ~

        `x`, `y` = |MotorJoint:getLinearOffset|()

        See function description

        Returns: ~
            1. (`number`)
                The x component of the target linear offset, relative to the
                first Body.

            2. (`number`)
                The y component of the target linear offset, relative to the
                first Body.
-------------------------------------------------------------------------------
                                      *love2d-docs-MotorJoint:setAngularOffset*
|MotorJoint:setAngularOffset|

Sets the target angluar offset between the two Bodies the Joint is attached to.

Synopses: ~

        |MotorJoint:setAngularOffset|(`angleoffset`)

Variants: ~

        |MotorJoint:setAngularOffset|(`angleoffset`)

        See function description

        Parameters: ~
            • {angleoffset} (`number`)
                The target angular offset in radians: the second body's angle
                minus the first body's angle.


-------------------------------------------------------------------------------
                                       *love2d-docs-MotorJoint:setLinearOffset*
|MotorJoint:setLinearOffset|

Sets the target linear offset between the two Bodies the Joint is attached to.

Synopses: ~

        |MotorJoint:setLinearOffset|(`x`, `y`)

Variants: ~

        |MotorJoint:setLinearOffset|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x component of the target linear offset, relative to the
                first Body.
            • {y} (`number`)
                The y component of the target linear offset, relative to the
                first Body.



-------------------------------------------------------------------------------
                                                       *love2d-docs-MouseJoint*
|MouseJoint|

For controlling objects with the mouse.

                                          *love2d-docs-MouseJoint-constructors*
    constructors:

        |newMouseJoint|                             |love2d-docs-newMouseJoint|

                                            *love2d-docs-MouseJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                              *love2d-docs-MouseJoint-subtypes*
    subtypes:

        None

                                             *love2d-docs-MouseJoint-functions*
    functions:

        |getDampingRatio|              |love2d-docs-MouseJoint:getDampingRatio|
        |getFrequency|                    |love2d-docs-MouseJoint:getFrequency|
        |getMaxForce|                      |love2d-docs-MouseJoint:getMaxForce|
        |getTarget|                          |love2d-docs-MouseJoint:getTarget|
        |setDampingRatio|              |love2d-docs-MouseJoint:setDampingRatio|
        |setFrequency|                    |love2d-docs-MouseJoint:setFrequency|
        |setMaxForce|                      |love2d-docs-MouseJoint:setMaxForce|
        |setTarget|                          |love2d-docs-MouseJoint:setTarget|

-------------------------------------------------------------------------------
                                       *love2d-docs-MouseJoint:getDampingRatio*
|MouseJoint:getDampingRatio|

Returns the damping ratio.

Synopses: ~

        `ratio` = |MouseJoint:getDampingRatio|()

Variants: ~

        `ratio` = |MouseJoint:getDampingRatio|()

        See function description

        Return: ~
            (`number`)
                The new damping ratio.
-------------------------------------------------------------------------------
                                          *love2d-docs-MouseJoint:getFrequency*
|MouseJoint:getFrequency|

Returns the frequency.

Synopses: ~

        `freq` = |MouseJoint:getFrequency|()

Variants: ~

        `freq` = |MouseJoint:getFrequency|()

        See function description

        Return: ~
            (`number`)
                The frequency in hertz.
-------------------------------------------------------------------------------
                                           *love2d-docs-MouseJoint:getMaxForce*
|MouseJoint:getMaxForce|

Gets the highest allowed force.

Synopses: ~

        `f` = |MouseJoint:getMaxForce|()

Variants: ~

        `f` = |MouseJoint:getMaxForce|()

        See function description

        Return: ~
            (`number`)
                The max allowed force.
-------------------------------------------------------------------------------
                                             *love2d-docs-MouseJoint:getTarget*
|MouseJoint:getTarget|

Gets the target point.

Synopses: ~

        `x`, `y` = |MouseJoint:getTarget|()

Variants: ~

        `x`, `y` = |MouseJoint:getTarget|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the target.

            2. (`number`)
                The x-component of the target.
-------------------------------------------------------------------------------
                                       *love2d-docs-MouseJoint:setDampingRatio*
|MouseJoint:setDampingRatio|

Sets a new damping ratio.

Synopses: ~

        |MouseJoint:setDampingRatio|(`ratio`)

Variants: ~

        |MouseJoint:setDampingRatio|(`ratio`)

        See function description

        Parameters: ~
            • {ratio} (`number`)
                The new damping ratio.


-------------------------------------------------------------------------------
                                          *love2d-docs-MouseJoint:setFrequency*
|MouseJoint:setFrequency|

Sets a new frequency.

Synopses: ~

        |MouseJoint:setFrequency|(`freq`)

Variants: ~

        |MouseJoint:setFrequency|(`freq`)

        See function description

        Parameters: ~
            • {freq} (`number`)
                The new frequency in hertz.


-------------------------------------------------------------------------------
                                           *love2d-docs-MouseJoint:setMaxForce*
|MouseJoint:setMaxForce|

Sets the highest allowed force.

Synopses: ~

        |MouseJoint:setMaxForce|(`f`)

Variants: ~

        |MouseJoint:setMaxForce|(`f`)

        See function description

        Parameters: ~
            • {f} (`number`)
                The max allowed force.


-------------------------------------------------------------------------------
                                             *love2d-docs-MouseJoint:setTarget*
|MouseJoint:setTarget|

Sets the target point.

Synopses: ~

        |MouseJoint:setTarget|(`x`, `y`)

Variants: ~

        |MouseJoint:setTarget|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-component of the target.
            • {y} (`number`)
                The y-component of the target.



-------------------------------------------------------------------------------
                                                     *love2d-docs-PolygonShape*
|PolygonShape|

A PolygonShape is a convex polygon with up to 8 vertices.

                                        *love2d-docs-PolygonShape-constructors*
    constructors:

        |newPolygonShape|                         |love2d-docs-newPolygonShape|
        |newRectangleShape|                     |love2d-docs-newRectangleShape|

                                          *love2d-docs-PolygonShape-supertypes*
    supertypes:

        |Shape|                                             |love2d-docs-Shape|
        |Object|                                           |love2d-docs-Object|

                                            *love2d-docs-PolygonShape-subtypes*
    subtypes:

        None

                                           *love2d-docs-PolygonShape-functions*
    functions:

        |getPoints|                        |love2d-docs-PolygonShape:getPoints|

-------------------------------------------------------------------------------
                                           *love2d-docs-PolygonShape:getPoints*
|PolygonShape:getPoints|

Get the local coordinates of the polygon's vertices.

This function has a variable number of return values. It can be used in a
nested fashion with love.graphics.polygon.

Synopses: ~

        `x1`, `y1`, `x2`, `y2` = |PolygonShape:getPoints|()

Variants: ~

        `x1`, `y1`, `x2`, `y2` = |PolygonShape:getPoints|()

        See function description

        Returns: ~
            1. (`number`)
                The x-component of the first vertex.

            2. (`number`)
                The y-component of the first vertex.

            3. (`number`)
                The x-component of the second vertex.

            4. (`number`)
                The y-component of the second vertex.

-------------------------------------------------------------------------------
                                                   *love2d-docs-PrismaticJoint*
|PrismaticJoint|

Restricts relative motion between Bodies to one shared axis.

                                      *love2d-docs-PrismaticJoint-constructors*
    constructors:

        |newPrismaticJoint|                     |love2d-docs-newPrismaticJoint|

                                        *love2d-docs-PrismaticJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                          *love2d-docs-PrismaticJoint-subtypes*
    subtypes:

        None

                                         *love2d-docs-PrismaticJoint-functions*
    functions:

        |areLimitsEnabled|        |love2d-docs-PrismaticJoint:areLimitsEnabled|
        |getAxis|                          |love2d-docs-PrismaticJoint:getAxis|
        |getJointSpeed|              |love2d-docs-PrismaticJoint:getJointSpeed|
        |getJointTranslation|  |love2d-docs-PrismaticJoint:getJointTranslation|
        |getLimits|                      |love2d-docs-PrismaticJoint:getLimits|
        |getLowerLimit|              |love2d-docs-PrismaticJoint:getLowerLimit|
        |getMaxMotorForce|        |love2d-docs-PrismaticJoint:getMaxMotorForce|
        |getMotorForce|              |love2d-docs-PrismaticJoint:getMotorForce|
        |getMotorSpeed|              |love2d-docs-PrismaticJoint:getMotorSpeed|
        |getReferenceAngle|      |love2d-docs-PrismaticJoint:getReferenceAngle|
        |getUpperLimit|              |love2d-docs-PrismaticJoint:getUpperLimit|
        |isMotorEnabled|            |love2d-docs-PrismaticJoint:isMotorEnabled|
        |setLimits|                      |love2d-docs-PrismaticJoint:setLimits|
        |setLimitsEnabled|        |love2d-docs-PrismaticJoint:setLimitsEnabled|
        |setLowerLimit|              |love2d-docs-PrismaticJoint:setLowerLimit|
        |setMaxMotorForce|        |love2d-docs-PrismaticJoint:setMaxMotorForce|
        |setMotorEnabled|          |love2d-docs-PrismaticJoint:setMotorEnabled|
        |setMotorSpeed|              |love2d-docs-PrismaticJoint:setMotorSpeed|
        |setUpperLimit|              |love2d-docs-PrismaticJoint:setUpperLimit|

-------------------------------------------------------------------------------
                                  *love2d-docs-PrismaticJoint:areLimitsEnabled*
|PrismaticJoint:areLimitsEnabled|

Checks whether the limits are enabled.

Synopses: ~

        `enabled` = |PrismaticJoint:areLimitsEnabled|()

Variants: ~

        `enabled` = |PrismaticJoint:areLimitsEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false otherwise.
-------------------------------------------------------------------------------
                                           *love2d-docs-PrismaticJoint:getAxis*
|PrismaticJoint:getAxis|

Gets the world-space axis vector of the Prismatic Joint.

Synopses: ~

        `x`, `y` = |PrismaticJoint:getAxis|()

Variants: ~

        `x`, `y` = |PrismaticJoint:getAxis|()

        See function description

        Returns: ~
            1. (`number`)
                The x-axis coordinate of the world-space axis vector.

            2. (`number`)
                The y-axis coordinate of the world-space axis vector.
-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:getJointSpeed*
|PrismaticJoint:getJointSpeed|

Get the current joint angle speed.

Synopses: ~

        `s` = |PrismaticJoint:getJointSpeed|()

Variants: ~

        `s` = |PrismaticJoint:getJointSpeed|()

        See function description

        Return: ~
            (`number`)
                Joint angle speed in meters/second.
-------------------------------------------------------------------------------
                               *love2d-docs-PrismaticJoint:getJointTranslation*
|PrismaticJoint:getJointTranslation|

Get the current joint translation.

Synopses: ~

        `t` = |PrismaticJoint:getJointTranslation|()

Variants: ~

        `t` = |PrismaticJoint:getJointTranslation|()

        See function description

        Return: ~
            (`number`)
                Joint translation, usually in meters..
-------------------------------------------------------------------------------
                                         *love2d-docs-PrismaticJoint:getLimits*
|PrismaticJoint:getLimits|

Gets the joint limits.

Synopses: ~

        `lower`, `upper` = |PrismaticJoint:getLimits|()

Variants: ~

        `lower`, `upper` = |PrismaticJoint:getLimits|()

        See function description

        Returns: ~
            1. (`number`)
                The lower limit, usually in meters.

            2. (`number`)
                The upper limit, usually in meters.
-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:getLowerLimit*
|PrismaticJoint:getLowerLimit|

Gets the lower limit.

Synopses: ~

        `lower` = |PrismaticJoint:getLowerLimit|()

Variants: ~

        `lower` = |PrismaticJoint:getLowerLimit|()

        See function description

        Return: ~
            (`number`)
                The lower limit, usually in meters.
-------------------------------------------------------------------------------
                                  *love2d-docs-PrismaticJoint:getMaxMotorForce*
|PrismaticJoint:getMaxMotorForce|

Gets the maximum motor force.

Synopses: ~

        `f` = |PrismaticJoint:getMaxMotorForce|()

Variants: ~

        `f` = |PrismaticJoint:getMaxMotorForce|()

        See function description

        Return: ~
            (`number`)
                The maximum motor force, usually in N.
-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:getMotorForce*
|PrismaticJoint:getMotorForce|

Returns the current motor force.

Synopses: ~

        `force` = |PrismaticJoint:getMotorForce|(`invdt`)

Variants: ~

        `force` = |PrismaticJoint:getMotorForce|(`invdt`)

        See function description

        Parameters: ~
            • {invdt} (`number`)
                How long the force applies. Usually the inverse time step or
                1/dt.

        Return: ~
            (`number`)
                The force on the motor in newtons.
-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:getMotorSpeed*
|PrismaticJoint:getMotorSpeed|

Gets the motor speed.

Synopses: ~

        `s` = |PrismaticJoint:getMotorSpeed|()

Variants: ~

        `s` = |PrismaticJoint:getMotorSpeed|()

        See function description

        Return: ~
            (`number`)
                The motor speed, usually in meters per second.
-------------------------------------------------------------------------------
                                 *love2d-docs-PrismaticJoint:getReferenceAngle*
|PrismaticJoint:getReferenceAngle|

Gets the reference angle.

Synopses: ~

        `angle` = |PrismaticJoint:getReferenceAngle|()

Variants: ~

        `angle` = |PrismaticJoint:getReferenceAngle|()

        See function description

        Return: ~
            (`number`)
                The reference angle in radians.
-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:getUpperLimit*
|PrismaticJoint:getUpperLimit|

Gets the upper limit.

Synopses: ~

        `upper` = |PrismaticJoint:getUpperLimit|()

Variants: ~

        `upper` = |PrismaticJoint:getUpperLimit|()

        See function description

        Return: ~
            (`number`)
                The upper limit, usually in meters.
-------------------------------------------------------------------------------
                                    *love2d-docs-PrismaticJoint:isMotorEnabled*
|PrismaticJoint:isMotorEnabled|

Checks whether the motor is enabled.

Synopses: ~

        `enabled` = |PrismaticJoint:isMotorEnabled|()

Variants: ~

        `enabled` = |PrismaticJoint:isMotorEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false if disabled.
-------------------------------------------------------------------------------
                                         *love2d-docs-PrismaticJoint:setLimits*
|PrismaticJoint:setLimits|

Sets the limits.

Synopses: ~

        |PrismaticJoint:setLimits|(`lower`, `upper`)

Variants: ~

        |PrismaticJoint:setLimits|(`lower`, `upper`)

        See function description

        Parameters: ~
            • {lower} (`number`)
                The lower limit, usually in meters.
            • {upper} (`number`)
                The upper limit, usually in meters.


-------------------------------------------------------------------------------
                                  *love2d-docs-PrismaticJoint:setLimitsEnabled*
|PrismaticJoint:setLimitsEnabled|

Enables/disables the joint limit.

Synopses: ~

        `enable` = |PrismaticJoint:setLimitsEnabled|()

Variants: ~

        `enable` = |PrismaticJoint:setLimitsEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false if disabled.
-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:setLowerLimit*
|PrismaticJoint:setLowerLimit|

Sets the lower limit.

Synopses: ~

        |PrismaticJoint:setLowerLimit|(`lower`)

Variants: ~

        |PrismaticJoint:setLowerLimit|(`lower`)

        See function description

        Parameters: ~
            • {lower} (`number`)
                The lower limit, usually in meters.


-------------------------------------------------------------------------------
                                  *love2d-docs-PrismaticJoint:setMaxMotorForce*
|PrismaticJoint:setMaxMotorForce|

Set the maximum motor force.

Synopses: ~

        |PrismaticJoint:setMaxMotorForce|(`f`)

Variants: ~

        |PrismaticJoint:setMaxMotorForce|(`f`)

        See function description

        Parameters: ~
            • {f} (`number`)
                The maximum motor force, usually in N.


-------------------------------------------------------------------------------
                                   *love2d-docs-PrismaticJoint:setMotorEnabled*
|PrismaticJoint:setMotorEnabled|

Enables/disables the joint motor.

Synopses: ~

        |PrismaticJoint:setMotorEnabled|(`enable`)

Variants: ~

        |PrismaticJoint:setMotorEnabled|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable, false to disable.


-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:setMotorSpeed*
|PrismaticJoint:setMotorSpeed|

Sets the motor speed.

Synopses: ~

        |PrismaticJoint:setMotorSpeed|(`s`)

Variants: ~

        |PrismaticJoint:setMotorSpeed|(`s`)

        See function description

        Parameters: ~
            • {s} (`number`)
                The motor speed, usually in meters per second.


-------------------------------------------------------------------------------
                                     *love2d-docs-PrismaticJoint:setUpperLimit*
|PrismaticJoint:setUpperLimit|

Sets the upper limit.

Synopses: ~

        |PrismaticJoint:setUpperLimit|(`upper`)

Variants: ~

        |PrismaticJoint:setUpperLimit|(`upper`)

        See function description

        Parameters: ~
            • {upper} (`number`)
                The upper limit, usually in meters.



-------------------------------------------------------------------------------
                                                      *love2d-docs-PulleyJoint*
|PulleyJoint|

Allows you to simulate bodies connected through pulleys.

                                         *love2d-docs-PulleyJoint-constructors*
    constructors:

        |newPulleyJoint|                           |love2d-docs-newPulleyJoint|

                                           *love2d-docs-PulleyJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                             *love2d-docs-PulleyJoint-subtypes*
    subtypes:

        None

                                            *love2d-docs-PulleyJoint-functions*
    functions:

        |getConstant|                     |love2d-docs-PulleyJoint:getConstant|
        |getGroundAnchors|           |love2d-docs-PulleyJoint:getGroundAnchors|
        |getLengthA|                       |love2d-docs-PulleyJoint:getLengthA|
        |getLengthB|                       |love2d-docs-PulleyJoint:getLengthB|
        |getMaxLengths|                 |love2d-docs-PulleyJoint:getMaxLengths|
        |getRatio|                           |love2d-docs-PulleyJoint:getRatio|
        |setConstant|                     |love2d-docs-PulleyJoint:setConstant|
        |setMaxLengths|                 |love2d-docs-PulleyJoint:setMaxLengths|
        |setRatio|                           |love2d-docs-PulleyJoint:setRatio|

-------------------------------------------------------------------------------
                                          *love2d-docs-PulleyJoint:getConstant*
|PulleyJoint:getConstant|

Get the total length of the rope.

Synopses: ~

        `length` = |PulleyJoint:getConstant|()

Variants: ~

        `length` = |PulleyJoint:getConstant|()

        See function description

        Return: ~
            (`number`)
                The length of the rope in the joint.
-------------------------------------------------------------------------------
                                     *love2d-docs-PulleyJoint:getGroundAnchors*
|PulleyJoint:getGroundAnchors|

Get the ground anchor positions in world coordinates.

Synopses: ~

        `a1x`, `a1y`, `a2x`, `a2y` = |PulleyJoint:getGroundAnchors|()

Variants: ~

        `a1x`, `a1y`, `a2x`, `a2y` = |PulleyJoint:getGroundAnchors|()

        See function description

        Returns: ~
            1. (`number`)
                The x coordinate of the first anchor.

            2. (`number`)
                The y coordinate of the first anchor.

            3. (`number`)
                The x coordinate of the second anchor.

            4. (`number`)
                The y coordinate of the second anchor.
-------------------------------------------------------------------------------
                                           *love2d-docs-PulleyJoint:getLengthA*
|PulleyJoint:getLengthA|

Get the current length of the rope segment attached to the first body.

Synopses: ~

        `length` = |PulleyJoint:getLengthA|()

Variants: ~

        `length` = |PulleyJoint:getLengthA|()

        See function description

        Return: ~
            (`number`)
                The length of the rope segment.
-------------------------------------------------------------------------------
                                           *love2d-docs-PulleyJoint:getLengthB*
|PulleyJoint:getLengthB|

Get the current length of the rope segment attached to the second body.

Synopses: ~

        `length` = |PulleyJoint:getLengthB|()

Variants: ~

        `length` = |PulleyJoint:getLengthB|()

        See function description

        Return: ~
            (`number`)
                The length of the rope segment.
-------------------------------------------------------------------------------
                                        *love2d-docs-PulleyJoint:getMaxLengths*
|PulleyJoint:getMaxLengths|

Get the maximum lengths of the rope segments.

Synopses: ~

        `len1`, `len2` = |PulleyJoint:getMaxLengths|()

Variants: ~

        `len1`, `len2` = |PulleyJoint:getMaxLengths|()

        See function description

        Returns: ~
            1. (`number`)
                The maximum length of the first rope segment.

            2. (`number`)
                The maximum length of the second rope segment.
-------------------------------------------------------------------------------
                                             *love2d-docs-PulleyJoint:getRatio*
|PulleyJoint:getRatio|

Get the pulley ratio.

Synopses: ~

        `ratio` = |PulleyJoint:getRatio|()

Variants: ~

        `ratio` = |PulleyJoint:getRatio|()

        See function description

        Return: ~
            (`number`)
                The pulley ratio of the joint.
-------------------------------------------------------------------------------
                                          *love2d-docs-PulleyJoint:setConstant*
|PulleyJoint:setConstant|

Set the total length of the rope.

Setting a new length for the rope updates the maximum length values of the
joint.

Synopses: ~

        |PulleyJoint:setConstant|(`length`)

Variants: ~

        |PulleyJoint:setConstant|(`length`)

        See function description

        Parameters: ~
            • {length} (`number`)
                The new length of the rope in the joint.


-------------------------------------------------------------------------------
                                        *love2d-docs-PulleyJoint:setMaxLengths*
|PulleyJoint:setMaxLengths|

Set the maximum lengths of the rope segments.

The physics module also imposes maximum values for the rope segments. If the
parameters exceed these values, the maximum values are set instead of the
requested values.

Synopses: ~

        |PulleyJoint:setMaxLengths|(`max1`, `max2`)

Variants: ~

        |PulleyJoint:setMaxLengths|(`max1`, `max2`)

        See function description

        Parameters: ~
            • {max1} (`number`)
                The new maximum length of the first segment.
            • {max2} (`number`)
                The new maximum length of the second segment.


-------------------------------------------------------------------------------
                                             *love2d-docs-PulleyJoint:setRatio*
|PulleyJoint:setRatio|

Set the pulley ratio.

Synopses: ~

        |PulleyJoint:setRatio|(`ratio`)

Variants: ~

        |PulleyJoint:setRatio|(`ratio`)

        See function description

        Parameters: ~
            • {ratio} (`number`)
                The new pulley ratio of the joint.



-------------------------------------------------------------------------------
                                                    *love2d-docs-RevoluteJoint*
|RevoluteJoint|

Allow two Bodies to revolve around a shared point.

                                       *love2d-docs-RevoluteJoint-constructors*
    constructors:

        |newRevoluteJoint|                       |love2d-docs-newRevoluteJoint|

                                         *love2d-docs-RevoluteJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                           *love2d-docs-RevoluteJoint-subtypes*
    subtypes:

        None

                                          *love2d-docs-RevoluteJoint-functions*
    functions:

        |areLimitsEnabled|         |love2d-docs-RevoluteJoint:areLimitsEnabled|
        |getJointAngle|               |love2d-docs-RevoluteJoint:getJointAngle|
        |getJointSpeed|               |love2d-docs-RevoluteJoint:getJointSpeed|
        |getLimits|                       |love2d-docs-RevoluteJoint:getLimits|
        |getLowerLimit|               |love2d-docs-RevoluteJoint:getLowerLimit|
        |getMaxMotorTorque|       |love2d-docs-RevoluteJoint:getMaxMotorTorque|
        |getMotorSpeed|               |love2d-docs-RevoluteJoint:getMotorSpeed|
        |getMotorTorque|             |love2d-docs-RevoluteJoint:getMotorTorque|
        |getReferenceAngle|       |love2d-docs-RevoluteJoint:getReferenceAngle|
        |getUpperLimit|               |love2d-docs-RevoluteJoint:getUpperLimit|
        |hasLimitsEnabled|         |love2d-docs-RevoluteJoint:hasLimitsEnabled|
        |isMotorEnabled|             |love2d-docs-RevoluteJoint:isMotorEnabled|
        |setLimits|                       |love2d-docs-RevoluteJoint:setLimits|
        |setLimitsEnabled|         |love2d-docs-RevoluteJoint:setLimitsEnabled|
        |setLowerLimit|               |love2d-docs-RevoluteJoint:setLowerLimit|
        |setMaxMotorTorque|       |love2d-docs-RevoluteJoint:setMaxMotorTorque|
        |setMotorEnabled|           |love2d-docs-RevoluteJoint:setMotorEnabled|
        |setMotorSpeed|               |love2d-docs-RevoluteJoint:setMotorSpeed|
        |setUpperLimit|               |love2d-docs-RevoluteJoint:setUpperLimit|

-------------------------------------------------------------------------------
                                   *love2d-docs-RevoluteJoint:areLimitsEnabled*
|RevoluteJoint:areLimitsEnabled|

Checks whether limits are enabled.

Synopses: ~

        `enabled` = |RevoluteJoint:areLimitsEnabled|()

Variants: ~

        `enabled` = |RevoluteJoint:areLimitsEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false otherwise.
-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:getJointAngle*
|RevoluteJoint:getJointAngle|

Get the current joint angle.

Synopses: ~

        `angle` = |RevoluteJoint:getJointAngle|()

Variants: ~

        `angle` = |RevoluteJoint:getJointAngle|()

        See function description

        Return: ~
            (`number`)
                The joint angle in radians.
-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:getJointSpeed*
|RevoluteJoint:getJointSpeed|

Get the current joint angle speed.

Synopses: ~

        `s` = |RevoluteJoint:getJointSpeed|()

Variants: ~

        `s` = |RevoluteJoint:getJointSpeed|()

        See function description

        Return: ~
            (`number`)
                Joint angle speed in radians/second.
-------------------------------------------------------------------------------
                                          *love2d-docs-RevoluteJoint:getLimits*
|RevoluteJoint:getLimits|

Gets the joint limits.

Synopses: ~

        `lower`, `upper` = |RevoluteJoint:getLimits|()

Variants: ~

        `lower`, `upper` = |RevoluteJoint:getLimits|()

        See function description

        Returns: ~
            1. (`number`)
                The lower limit, in radians.

            2. (`number`)
                The upper limit, in radians.
-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:getLowerLimit*
|RevoluteJoint:getLowerLimit|

Gets the lower limit.

Synopses: ~

        `lower` = |RevoluteJoint:getLowerLimit|()

Variants: ~

        `lower` = |RevoluteJoint:getLowerLimit|()

        See function description

        Return: ~
            (`number`)
                The lower limit, in radians.
-------------------------------------------------------------------------------
                                  *love2d-docs-RevoluteJoint:getMaxMotorTorque*
|RevoluteJoint:getMaxMotorTorque|

Gets the maximum motor force.

Synopses: ~

        `f` = |RevoluteJoint:getMaxMotorTorque|()

Variants: ~

        `f` = |RevoluteJoint:getMaxMotorTorque|()

        See function description

        Return: ~
            (`number`)
                The maximum motor force, in Nm.
-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:getMotorSpeed*
|RevoluteJoint:getMotorSpeed|

Gets the motor speed.

Synopses: ~

        `s` = |RevoluteJoint:getMotorSpeed|()

Variants: ~

        `s` = |RevoluteJoint:getMotorSpeed|()

        See function description

        Return: ~
            (`number`)
                The motor speed, radians per second.
-------------------------------------------------------------------------------
                                     *love2d-docs-RevoluteJoint:getMotorTorque*
|RevoluteJoint:getMotorTorque|

Get the current motor force.

Synopses: ~

        `f` = |RevoluteJoint:getMotorTorque|()

Variants: ~

        `f` = |RevoluteJoint:getMotorTorque|()

        See function description

        Return: ~
            (`number`)
                The current motor force, in Nm.
-------------------------------------------------------------------------------
                                  *love2d-docs-RevoluteJoint:getReferenceAngle*
|RevoluteJoint:getReferenceAngle|

Gets the reference angle.

Synopses: ~

        `angle` = |RevoluteJoint:getReferenceAngle|()

Variants: ~

        `angle` = |RevoluteJoint:getReferenceAngle|()

        See function description

        Return: ~
            (`number`)
                The reference angle in radians.
-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:getUpperLimit*
|RevoluteJoint:getUpperLimit|

Gets the upper limit.

Synopses: ~

        `upper` = |RevoluteJoint:getUpperLimit|()

Variants: ~

        `upper` = |RevoluteJoint:getUpperLimit|()

        See function description

        Return: ~
            (`number`)
                The upper limit, in radians.
-------------------------------------------------------------------------------
                                   *love2d-docs-RevoluteJoint:hasLimitsEnabled*
|RevoluteJoint:hasLimitsEnabled|

Checks whether limits are enabled.

Synopses: ~

        `enabled` = |RevoluteJoint:hasLimitsEnabled|()

Variants: ~

        `enabled` = |RevoluteJoint:hasLimitsEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false otherwise.
-------------------------------------------------------------------------------
                                     *love2d-docs-RevoluteJoint:isMotorEnabled*
|RevoluteJoint:isMotorEnabled|

Checks whether the motor is enabled.

Synopses: ~

        `enabled` = |RevoluteJoint:isMotorEnabled|()

Variants: ~

        `enabled` = |RevoluteJoint:isMotorEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if enabled, false if disabled.
-------------------------------------------------------------------------------
                                          *love2d-docs-RevoluteJoint:setLimits*
|RevoluteJoint:setLimits|

Sets the limits.

Synopses: ~

        |RevoluteJoint:setLimits|(`lower`, `upper`)

Variants: ~

        |RevoluteJoint:setLimits|(`lower`, `upper`)

        See function description

        Parameters: ~
            • {lower} (`number`)
                The lower limit, in radians.
            • {upper} (`number`)
                The upper limit, in radians.


-------------------------------------------------------------------------------
                                   *love2d-docs-RevoluteJoint:setLimitsEnabled*
|RevoluteJoint:setLimitsEnabled|

Enables/disables the joint limit.

Synopses: ~

        |RevoluteJoint:setLimitsEnabled|(`enable`)

Variants: ~

        |RevoluteJoint:setLimitsEnabled|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable, false to disable.


-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:setLowerLimit*
|RevoluteJoint:setLowerLimit|

Sets the lower limit.

Synopses: ~

        |RevoluteJoint:setLowerLimit|(`lower`)

Variants: ~

        |RevoluteJoint:setLowerLimit|(`lower`)

        See function description

        Parameters: ~
            • {lower} (`number`)
                The lower limit, in radians.


-------------------------------------------------------------------------------
                                  *love2d-docs-RevoluteJoint:setMaxMotorTorque*
|RevoluteJoint:setMaxMotorTorque|

Set the maximum motor force.

Synopses: ~

        |RevoluteJoint:setMaxMotorTorque|(`f`)

Variants: ~

        |RevoluteJoint:setMaxMotorTorque|(`f`)

        See function description

        Parameters: ~
            • {f} (`number`)
                The maximum motor force, in Nm.


-------------------------------------------------------------------------------
                                    *love2d-docs-RevoluteJoint:setMotorEnabled*
|RevoluteJoint:setMotorEnabled|

Enables/disables the joint motor.

Synopses: ~

        |RevoluteJoint:setMotorEnabled|(`enable`)

Variants: ~

        |RevoluteJoint:setMotorEnabled|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable, false to disable.


-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:setMotorSpeed*
|RevoluteJoint:setMotorSpeed|

Sets the motor speed.

Synopses: ~

        |RevoluteJoint:setMotorSpeed|(`s`)

Variants: ~

        |RevoluteJoint:setMotorSpeed|(`s`)

        See function description

        Parameters: ~
            • {s} (`number`)
                The motor speed, radians per second.


-------------------------------------------------------------------------------
                                      *love2d-docs-RevoluteJoint:setUpperLimit*
|RevoluteJoint:setUpperLimit|

Sets the upper limit.

Synopses: ~

        |RevoluteJoint:setUpperLimit|(`upper`)

Variants: ~

        |RevoluteJoint:setUpperLimit|(`upper`)

        See function description

        Parameters: ~
            • {upper} (`number`)
                The upper limit, in radians.



-------------------------------------------------------------------------------
                                                        *love2d-docs-RopeJoint*
|RopeJoint|

The RopeJoint enforces a maximum distance between two points on two bodies. It
has no other effect.

                                           *love2d-docs-RopeJoint-constructors*
    constructors:

        |newRopeJoint|                               |love2d-docs-newRopeJoint|

                                             *love2d-docs-RopeJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-RopeJoint-subtypes*
    subtypes:

        None

                                              *love2d-docs-RopeJoint-functions*
    functions:

        |getMaxLength|                     |love2d-docs-RopeJoint:getMaxLength|
        |setMaxLength|                     |love2d-docs-RopeJoint:setMaxLength|

-------------------------------------------------------------------------------
                                           *love2d-docs-RopeJoint:getMaxLength*
|RopeJoint:getMaxLength|

Gets the maximum length of a RopeJoint.

Synopses: ~

        `maxLength` = |RopeJoint:getMaxLength|()

Variants: ~

        `maxLength` = |RopeJoint:getMaxLength|()

        See function description

        Return: ~
            (`number`)
                The maximum length of the RopeJoint.
-------------------------------------------------------------------------------
                                           *love2d-docs-RopeJoint:setMaxLength*
|RopeJoint:setMaxLength|

Sets the maximum length of a RopeJoint.

Synopses: ~

        |RopeJoint:setMaxLength|(`maxLength`)

Variants: ~

        |RopeJoint:setMaxLength|(`maxLength`)

        See function description

        Parameters: ~
            • {maxLength} (`number`)
                The new maximum length of the RopeJoint.



-------------------------------------------------------------------------------
                                                            *love2d-docs-Shape*
|Shape|

Shapes are solid 2d geometrical objects which handle the mass and collision of
a Body in love.physics.

Shapes are attached to a Body via a Fixture. The Shape object is copied when
this happens.

The Shape's position is relative to the position of the Body it has been
attached to.

                                               *love2d-docs-Shape-constructors*
    constructors:

        None

                                                 *love2d-docs-Shape-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                   *love2d-docs-Shape-subtypes*
    subtypes:

        None

                                                  *love2d-docs-Shape-functions*
    functions:

        |computeAABB|                           |love2d-docs-Shape:computeAABB|
        |computeMass|                           |love2d-docs-Shape:computeMass|
        |getChildCount|                       |love2d-docs-Shape:getChildCount|
        |getRadius|                               |love2d-docs-Shape:getRadius|
        |getType|                                   |love2d-docs-Shape:getType|
        |rayCast|                                   |love2d-docs-Shape:rayCast|
        |testPoint|                               |love2d-docs-Shape:testPoint|

-------------------------------------------------------------------------------
                                                *love2d-docs-Shape:computeAABB*
|Shape:computeAABB|

Returns the points of the bounding box for the transformed shape.

Synopses: ~

        `topLeftX`, `topLeftY`, `bottomRightX`, `bottomRightY` =
        |Shape:computeAABB|(`tx`, `ty`, `tr`, `childIndex`)

Variants: ~

        `topLeftX`, `topLeftY`, `bottomRightX`, `bottomRightY` =
        |Shape:computeAABB|(`tx`, `ty`, `tr`, `childIndex`)

        See function description

        Parameters: ~
            • {tx} (`number`)
                The translation of the shape on the x-axis.
            • {ty} (`number`)
                The translation of the shape on the y-axis.
            • {tr} (`number`)
                The shape rotation.
            • {childIndex} (`number`) (default: `1`)
                The index of the child to compute the bounding box of.

        Returns: ~
            1. (`number`)
                The x position of the top-left point.

            2. (`number`)
                The y position of the top-left point.

            3. (`number`)
                The x position of the bottom-right point.

            4. (`number`)
                The y position of the bottom-right point.
-------------------------------------------------------------------------------
                                                *love2d-docs-Shape:computeMass*
|Shape:computeMass|

Computes the mass properties for the shape with the specified density.

Synopses: ~

        `x`, `y`, `mass`, `inertia` = |Shape:computeMass|(`density`)

Variants: ~

        `x`, `y`, `mass`, `inertia` = |Shape:computeMass|(`density`)

        See function description

        Parameters: ~
            • {density} (`number`)
                The shape density.

        Returns: ~
            1. (`number`)
                The x postition of the center of mass.

            2. (`number`)
                The y postition of the center of mass.

            3. (`number`)
                The mass of the shape.

            4. (`number`)
                The rotational inertia.
-------------------------------------------------------------------------------
                                              *love2d-docs-Shape:getChildCount*
|Shape:getChildCount|

Returns the number of children the shape has.

Synopses: ~

        `count` = |Shape:getChildCount|()

Variants: ~

        `count` = |Shape:getChildCount|()

        See function description

        Return: ~
            (`number`)
                The number of children.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Shape:getRadius*
|Shape:getRadius|

Gets the radius of the shape.

Synopses: ~

        `radius` = |Shape:getRadius|()

Variants: ~

        `radius` = |Shape:getRadius|()

        See function description

        Return: ~
            (`number`)
                The radius of the shape.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Shape:getType*
|Shape:getType|

Gets a string representing the Shape.

This function can be useful for conditional debug drawing.

Synopses: ~

        `type` = |Shape:getType|()

Variants: ~

        `type` = |Shape:getType|()

        See function description

        Return: ~
            (`ShapeType`) |love2d-docs-ShapeType|
                The type of the Shape.
-------------------------------------------------------------------------------
                                                    *love2d-docs-Shape:rayCast*
|Shape:rayCast|

Casts a ray against the shape and returns the surface normal vector and the
line position where the ray hit. If the ray missed the shape, nil will be
returned. The Shape can be transformed to get it into the desired position.

The ray starts on the first point of the input line and goes towards the second
point of the line. The fourth argument is the maximum distance the ray is going
to travel as a scale factor of the input line length.

The childIndex parameter is used to specify which child of a parent shape, such
as a ChainShape, will be ray casted. For ChainShapes, the index of 1 is the
first edge on the chain. Ray casting a parent shape will only test the child
specified so if you want to test every shape of the parent, you must loop
through all of its children.

The world position of the impact can be calculated by multiplying the line
vector with the third return value and adding it to the line starting point.

hitx, hity = x1 + (x2 - x1) * fraction, y1 + (y2 - y1) * fraction

Synopses: ~

        `xn`, `yn`, `fraction` = |Shape:rayCast|(`x1`, `y1`, `x2`,
        `y2`, `maxFraction`, `tx`, `ty`, `tr`, `childIndex`)

Variants: ~

        `xn`, `yn`, `fraction` = |Shape:rayCast|(`x1`, `y1`, `x2`,
        `y2`, `maxFraction`, `tx`, `ty`, `tr`, `childIndex`)

        See function description

        Parameters: ~
            • {x1} (`number`)
                The x position of the input line starting point.
            • {y1} (`number`)
                The y position of the input line starting point.
            • {x2} (`number`)
                The x position of the input line end point.
            • {y2} (`number`)
                The y position of the input line end point.
            • {maxFraction} (`number`)
                Ray length parameter.
            • {tx} (`number`)
                The translation of the shape on the x-axis.
            • {ty} (`number`)
                The translation of the shape on the y-axis.
            • {tr} (`number`)
                The shape rotation.
            • {childIndex} (`number`) (default: `1`)
                The index of the child the ray gets cast against.

        Returns: ~
            1. (`number`)
                The x component of the normal vector of the edge where the ray
                hit the shape.

            2. (`number`)
                The y component of the normal vector of the edge where the ray
                hit the shape.

            3. (`number`)
                The position on the input line where the intersection happened
                as a factor of the line length.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Shape:testPoint*
|Shape:testPoint|

This is particularly useful for mouse interaction with the shapes. By looping
through all shapes and testing the mouse position with this function, we can
find which shapes the mouse touches.

Synopses: ~

        `hit` = |Shape:testPoint|(`tx`, `ty`, `tr`, `x`, `y`)

Variants: ~

        `hit` = |Shape:testPoint|(`tx`, `ty`, `tr`, `x`, `y`)

        See function description

        Parameters: ~
            • {tx} (`number`)
                Translates the shape along the x-axis.
            • {ty} (`number`)
                Translates the shape along the y-axis.
            • {tr} (`number`)
                Rotates the shape.
            • {x} (`number`)
                The x-component of the point.
            • {y} (`number`)
                The y-component of the point.

        Return: ~
            (`boolean`)
                True if inside, false if outside

-------------------------------------------------------------------------------
                                                        *love2d-docs-WeldJoint*
|WeldJoint|

A WeldJoint essentially glues two bodies together.

                                           *love2d-docs-WeldJoint-constructors*
    constructors:

        |newWeldJoint|                               |love2d-docs-newWeldJoint|

                                             *love2d-docs-WeldJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-WeldJoint-subtypes*
    subtypes:

        None

                                              *love2d-docs-WeldJoint-functions*
    functions:

        |getDampingRatio|               |love2d-docs-WeldJoint:getDampingRatio|
        |getFrequency|                     |love2d-docs-WeldJoint:getFrequency|
        |getReferenceAngle|           |love2d-docs-WeldJoint:getReferenceAngle|
        |setDampingRatio|               |love2d-docs-WeldJoint:setDampingRatio|
        |setFrequency|                     |love2d-docs-WeldJoint:setFrequency|

-------------------------------------------------------------------------------
                                        *love2d-docs-WeldJoint:getDampingRatio*
|WeldJoint:getDampingRatio|

Returns the damping ratio of the joint.

Synopses: ~

        `ratio` = |WeldJoint:getDampingRatio|()

Variants: ~

        `ratio` = |WeldJoint:getDampingRatio|()

        See function description

        Return: ~
            (`number`)
                The damping ratio.
-------------------------------------------------------------------------------
                                           *love2d-docs-WeldJoint:getFrequency*
|WeldJoint:getFrequency|

Returns the frequency.

Synopses: ~

        `freq` = |WeldJoint:getFrequency|()

Variants: ~

        `freq` = |WeldJoint:getFrequency|()

        See function description

        Return: ~
            (`number`)
                The frequency in hertz.
-------------------------------------------------------------------------------
                                      *love2d-docs-WeldJoint:getReferenceAngle*
|WeldJoint:getReferenceAngle|

Gets the reference angle.

Synopses: ~

        `angle` = |WeldJoint:getReferenceAngle|()

Variants: ~

        `angle` = |WeldJoint:getReferenceAngle|()

        See function description

        Return: ~
            (`number`)
                The reference angle in radians.
-------------------------------------------------------------------------------
                                        *love2d-docs-WeldJoint:setDampingRatio*
|WeldJoint:setDampingRatio|

Sets a new damping ratio.

Synopses: ~

        |WeldJoint:setDampingRatio|(`ratio`)

Variants: ~

        |WeldJoint:setDampingRatio|(`ratio`)

        See function description

        Parameters: ~
            • {ratio} (`number`)
                The new damping ratio.


-------------------------------------------------------------------------------
                                           *love2d-docs-WeldJoint:setFrequency*
|WeldJoint:setFrequency|

Sets a new frequency.

Synopses: ~

        |WeldJoint:setFrequency|(`freq`)

Variants: ~

        |WeldJoint:setFrequency|(`freq`)

        See function description

        Parameters: ~
            • {freq} (`number`)
                The new frequency in hertz.



-------------------------------------------------------------------------------
                                                       *love2d-docs-WheelJoint*
|WheelJoint|

Restricts a point on the second body to a line on the first body.

                                          *love2d-docs-WheelJoint-constructors*
    constructors:

        |newWheelJoint|                             |love2d-docs-newWheelJoint|

                                            *love2d-docs-WheelJoint-supertypes*
    supertypes:

        |Joint|                                             |love2d-docs-Joint|
        |Object|                                           |love2d-docs-Object|

                                              *love2d-docs-WheelJoint-subtypes*
    subtypes:

        None

                                             *love2d-docs-WheelJoint-functions*
    functions:

        |getAxis|                              |love2d-docs-WheelJoint:getAxis|
        |getJointSpeed|                  |love2d-docs-WheelJoint:getJointSpeed|
        |getJointTranslation|      |love2d-docs-WheelJoint:getJointTranslation|
        |getMaxMotorTorque|          |love2d-docs-WheelJoint:getMaxMotorTorque|
        |getMotorSpeed|                  |love2d-docs-WheelJoint:getMotorSpeed|
        |getMotorTorque|                |love2d-docs-WheelJoint:getMotorTorque|
        |getSpringDampingRatio|  |love2d-docs-WheelJoint:getSpringDampingRatio|
        |getSpringFrequency|        |love2d-docs-WheelJoint:getSpringFrequency|
        |isMotorEnabled|                |love2d-docs-WheelJoint:isMotorEnabled|
        |setMaxMotorTorque|          |love2d-docs-WheelJoint:setMaxMotorTorque|
        |setMotorEnabled|              |love2d-docs-WheelJoint:setMotorEnabled|
        |setMotorSpeed|                  |love2d-docs-WheelJoint:setMotorSpeed|
        |setSpringDampingRatio|  |love2d-docs-WheelJoint:setSpringDampingRatio|
        |setSpringFrequency|        |love2d-docs-WheelJoint:setSpringFrequency|

-------------------------------------------------------------------------------
                                               *love2d-docs-WheelJoint:getAxis*
|WheelJoint:getAxis|

Gets the world-space axis vector of the Wheel Joint.

Synopses: ~

        `x`, `y` = |WheelJoint:getAxis|()

Variants: ~

        `x`, `y` = |WheelJoint:getAxis|()

        See function description

        Returns: ~
            1. (`number`)
                The x-axis coordinate of the world-space axis vector.

            2. (`number`)
                The y-axis coordinate of the world-space axis vector.
-------------------------------------------------------------------------------
                                         *love2d-docs-WheelJoint:getJointSpeed*
|WheelJoint:getJointSpeed|

Returns the current joint translation speed.

Synopses: ~

        `speed` = |WheelJoint:getJointSpeed|()

Variants: ~

        `speed` = |WheelJoint:getJointSpeed|()

        See function description

        Return: ~
            (`number`)
                The translation speed of the joint in meters per second.
-------------------------------------------------------------------------------
                                   *love2d-docs-WheelJoint:getJointTranslation*
|WheelJoint:getJointTranslation|

Returns the current joint translation.

Synopses: ~

        `position` = |WheelJoint:getJointTranslation|()

Variants: ~

        `position` = |WheelJoint:getJointTranslation|()

        See function description

        Return: ~
            (`number`)
                The translation of the joint in meters.
-------------------------------------------------------------------------------
                                     *love2d-docs-WheelJoint:getMaxMotorTorque*
|WheelJoint:getMaxMotorTorque|

Returns the maximum motor torque.

Synopses: ~

        `maxTorque` = |WheelJoint:getMaxMotorTorque|()

Variants: ~

        `maxTorque` = |WheelJoint:getMaxMotorTorque|()

        See function description

        Return: ~
            (`number`)
                The maximum torque of the joint motor in newton meters.
-------------------------------------------------------------------------------
                                         *love2d-docs-WheelJoint:getMotorSpeed*
|WheelJoint:getMotorSpeed|

Returns the speed of the motor.

Synopses: ~

        `speed` = |WheelJoint:getMotorSpeed|()

Variants: ~

        `speed` = |WheelJoint:getMotorSpeed|()

        See function description

        Return: ~
            (`number`)
                The speed of the joint motor in radians per second.
-------------------------------------------------------------------------------
                                        *love2d-docs-WheelJoint:getMotorTorque*
|WheelJoint:getMotorTorque|

Returns the current torque on the motor.

Synopses: ~

        `torque` = |WheelJoint:getMotorTorque|(`invdt`)

Variants: ~

        `torque` = |WheelJoint:getMotorTorque|(`invdt`)

        See function description

        Parameters: ~
            • {invdt} (`number`)
                How long the force applies. Usually the inverse time step or
                1/dt.

        Return: ~
            (`number`)
                The torque on the motor in newton meters.
-------------------------------------------------------------------------------
                                 *love2d-docs-WheelJoint:getSpringDampingRatio*
|WheelJoint:getSpringDampingRatio|

Returns the damping ratio.

Synopses: ~

        `ratio` = |WheelJoint:getSpringDampingRatio|()

Variants: ~

        `ratio` = |WheelJoint:getSpringDampingRatio|()

        See function description

        Return: ~
            (`number`)
                The damping ratio.
-------------------------------------------------------------------------------
                                    *love2d-docs-WheelJoint:getSpringFrequency*
|WheelJoint:getSpringFrequency|

Returns the spring frequency.

Synopses: ~

        `freq` = |WheelJoint:getSpringFrequency|()

Variants: ~

        `freq` = |WheelJoint:getSpringFrequency|()

        See function description

        Return: ~
            (`number`)
                The frequency in hertz.
-------------------------------------------------------------------------------
                                        *love2d-docs-WheelJoint:isMotorEnabled*
|WheelJoint:isMotorEnabled|

Checks if the joint motor is running.

Synopses: ~

        `on` = |WheelJoint:isMotorEnabled|()

Variants: ~

        `on` = |WheelJoint:isMotorEnabled|()

        See function description

        Return: ~
            (`boolean`)
                The status of the joint motor.
-------------------------------------------------------------------------------
                                     *love2d-docs-WheelJoint:setMaxMotorTorque*
|WheelJoint:setMaxMotorTorque|

Sets a new maximum motor torque.

Synopses: ~

        |WheelJoint:setMaxMotorTorque|(`maxTorque`)

Variants: ~

        |WheelJoint:setMaxMotorTorque|(`maxTorque`)

        See function description

        Parameters: ~
            • {maxTorque} (`number`)
                The new maximum torque for the joint motor in newton meters.


-------------------------------------------------------------------------------
                                       *love2d-docs-WheelJoint:setMotorEnabled*
|WheelJoint:setMotorEnabled|

Starts and stops the joint motor.

Synopses: ~

        |WheelJoint:setMotorEnabled|(`enable`)

Variants: ~

        |WheelJoint:setMotorEnabled|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True turns the motor on and false turns it off.


-------------------------------------------------------------------------------
                                         *love2d-docs-WheelJoint:setMotorSpeed*
|WheelJoint:setMotorSpeed|

Sets a new speed for the motor.

Synopses: ~

        |WheelJoint:setMotorSpeed|(`speed`)

Variants: ~

        |WheelJoint:setMotorSpeed|(`speed`)

        See function description

        Parameters: ~
            • {speed} (`number`)
                The new speed for the joint motor in radians per second.


-------------------------------------------------------------------------------
                                 *love2d-docs-WheelJoint:setSpringDampingRatio*
|WheelJoint:setSpringDampingRatio|

Sets a new damping ratio.

Synopses: ~

        |WheelJoint:setSpringDampingRatio|(`ratio`)

Variants: ~

        |WheelJoint:setSpringDampingRatio|(`ratio`)

        See function description

        Parameters: ~
            • {ratio} (`number`)
                The new damping ratio.


-------------------------------------------------------------------------------
                                    *love2d-docs-WheelJoint:setSpringFrequency*
|WheelJoint:setSpringFrequency|

Sets a new spring frequency.

Synopses: ~

        |WheelJoint:setSpringFrequency|(`freq`)

Variants: ~

        |WheelJoint:setSpringFrequency|(`freq`)

        See function description

        Parameters: ~
            • {freq} (`number`)
                The new frequency in hertz.



-------------------------------------------------------------------------------
                                                            *love2d-docs-World*
|World|

A world is an object that contains all bodies and joints.

                                               *love2d-docs-World-constructors*
    constructors:

        |newWorld|                                       |love2d-docs-newWorld|

                                                 *love2d-docs-World-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                   *love2d-docs-World-subtypes*
    subtypes:

        None

                                                  *love2d-docs-World-functions*
    functions:

        |destroy|                                   |love2d-docs-World:destroy|
        |getBodies|                               |love2d-docs-World:getBodies|
        |getBodyCount|                         |love2d-docs-World:getBodyCount|
        |getCallbacks|                         |love2d-docs-World:getCallbacks|
        |getContactCount|                   |love2d-docs-World:getContactCount|
        |getContactFilter|                 |love2d-docs-World:getContactFilter|
        |getContacts|                           |love2d-docs-World:getContacts|
        |getGravity|                             |love2d-docs-World:getGravity|
        |getJointCount|                       |love2d-docs-World:getJointCount|
        |getJoints|                               |love2d-docs-World:getJoints|
        |isDestroyed|                           |love2d-docs-World:isDestroyed|
        |isLocked|                                 |love2d-docs-World:isLocked|
        |isSleepingAllowed|               |love2d-docs-World:isSleepingAllowed|
        |queryBoundingBox|                 |love2d-docs-World:queryBoundingBox|
        |rayCast|                                   |love2d-docs-World:rayCast|
        |setCallbacks|                         |love2d-docs-World:setCallbacks|
        |setContactFilter|                 |love2d-docs-World:setContactFilter|
        |setGravity|                             |love2d-docs-World:setGravity|
        |setSleepingAllowed|             |love2d-docs-World:setSleepingAllowed|
        |translateOrigin|                   |love2d-docs-World:translateOrigin|
        |update|                                     |love2d-docs-World:update|

-------------------------------------------------------------------------------
                                                    *love2d-docs-World:destroy*
|World:destroy|

Destroys the world, taking all bodies, joints, fixtures and their shapes with
it.

An error will occur if you attempt to use any of the destroyed objects after
calling this function.

Synopses: ~

        |World:destroy|()

Variants: ~

        |World:destroy|()

        See function description


-------------------------------------------------------------------------------
                                                  *love2d-docs-World:getBodies*
|World:getBodies|

Returns a table with all bodies.

Synopses: ~

        `bodies` = |World:getBodies|()

Variants: ~

        `bodies` = |World:getBodies|()

        See function description

        Return: ~
            (`table`)
                A sequence with all bodies.
-------------------------------------------------------------------------------
                                               *love2d-docs-World:getBodyCount*
|World:getBodyCount|

Returns the number of bodies in the world.

Synopses: ~

        `n` = |World:getBodyCount|()

Variants: ~

        `n` = |World:getBodyCount|()

        See function description

        Return: ~
            (`number`)
                The number of bodies in the world.
-------------------------------------------------------------------------------
                                               *love2d-docs-World:getCallbacks*
|World:getCallbacks|

Returns functions for the callbacks during the world update.

Synopses: ~

        `beginContact`, `endContact`, `preSolve`, `postSolve` =
        |World:getCallbacks|()

Variants: ~

        `beginContact`, `endContact`, `preSolve`, `postSolve` =
        |World:getCallbacks|()

        See function description

        Returns: ~
            1. (`function`)
                Gets called when two fixtures begin to overlap.

            2. (`function`)
                Gets called when two fixtures cease to overlap.

            3. (`function`)
                Gets called before a collision gets resolved.

            4. (`function`)
                Gets called after the collision has been resolved.
-------------------------------------------------------------------------------
                                            *love2d-docs-World:getContactCount*
|World:getContactCount|

Returns the number of contacts in the world.

Synopses: ~

        `n` = |World:getContactCount|()

Variants: ~

        `n` = |World:getContactCount|()

        See function description

        Return: ~
            (`number`)
                The number of contacts in the world.
-------------------------------------------------------------------------------
                                           *love2d-docs-World:getContactFilter*
|World:getContactFilter|

Returns the function for collision filtering.

Synopses: ~

        `contactFilter` = |World:getContactFilter|()

Variants: ~

        `contactFilter` = |World:getContactFilter|()

        See function description

        Return: ~
            (`function`)
                The function that handles the contact filtering.
-------------------------------------------------------------------------------
                                                *love2d-docs-World:getContacts*
|World:getContacts|

Returns a table with all Contacts.

Synopses: ~

        `contacts` = |World:getContacts|()

Variants: ~

        `contacts` = |World:getContacts|()

        See function description

        Return: ~
            (`table`)
                A sequence with all Contacts.
-------------------------------------------------------------------------------
                                                 *love2d-docs-World:getGravity*
|World:getGravity|

Get the gravity of the world.

Synopses: ~

        `x`, `y` = |World:getGravity|()

Variants: ~

        `x`, `y` = |World:getGravity|()

        See function description

        Returns: ~
            1. (`number`)
                The x component of gravity.

            2. (`number`)
                The y component of gravity.
-------------------------------------------------------------------------------
                                              *love2d-docs-World:getJointCount*
|World:getJointCount|

Returns the number of joints in the world.

Synopses: ~

        `n` = |World:getJointCount|()

Variants: ~

        `n` = |World:getJointCount|()

        See function description

        Return: ~
            (`number`)
                The number of joints in the world.
-------------------------------------------------------------------------------
                                                  *love2d-docs-World:getJoints*
|World:getJoints|

Returns a table with all joints.

Synopses: ~

        `joints` = |World:getJoints|()

Variants: ~

        `joints` = |World:getJoints|()

        See function description

        Return: ~
            (`table`)
                A sequence with all joints.
-------------------------------------------------------------------------------
                                                *love2d-docs-World:isDestroyed*
|World:isDestroyed|

Gets whether the World is destroyed. Destroyed worlds cannot be used.

Synopses: ~

        `destroyed` = |World:isDestroyed|()

Variants: ~

        `destroyed` = |World:isDestroyed|()

        See function description

        Return: ~
            (`boolean`)
                Whether the World is destroyed.
-------------------------------------------------------------------------------
                                                   *love2d-docs-World:isLocked*
|World:isLocked|

Returns if the world is updating its state.

This will return true inside the callbacks from World:setCallbacks.

Synopses: ~

        `locked` = |World:isLocked|()

Variants: ~

        `locked` = |World:isLocked|()

        See function description

        Return: ~
            (`boolean`)
                Will be true if the world is in the process of updating its
                state.
-------------------------------------------------------------------------------
                                          *love2d-docs-World:isSleepingAllowed*
|World:isSleepingAllowed|

Gets the sleep behaviour of the world.

Synopses: ~

        `allow` = |World:isSleepingAllowed|()

Variants: ~

        `allow` = |World:isSleepingAllowed|()

        See function description

        Return: ~
            (`boolean`)
                True if bodies in the world are allowed to sleep, or false if
                not.
-------------------------------------------------------------------------------
                                           *love2d-docs-World:queryBoundingBox*
|World:queryBoundingBox|

Calls a function for each fixture inside the specified area by searching for
any overlapping bounding box (Fixture:getBoundingBox).

Synopses: ~

        |World:queryBoundingBox|(`topLeftX`, `topLeftY`,
        `bottomRightX`, `bottomRightY`, `callback`)

Variants: ~

        |World:queryBoundingBox|(`topLeftX`, `topLeftY`,
        `bottomRightX`, `bottomRightY`, `callback`)

        See function description

        Parameters: ~
            • {topLeftX} (`number`)
                The x position of the top-left point.
            • {topLeftY} (`number`)
                The y position of the top-left point.
            • {bottomRightX} (`number`)
                The x position of the bottom-right point.
            • {bottomRightY} (`number`)
                The y position of the bottom-right point.
            • {callback} (`function`)
                This function gets passed one argument, the fixture, and should
                return a boolean. The search will continue if it is true or
                stop if it is false.


-------------------------------------------------------------------------------
                                                    *love2d-docs-World:rayCast*
|World:rayCast|

Casts a ray and calls a function for each fixtures it intersects.

Synopses: ~

        |World:rayCast|(`x1`, `y1`, `x2`, `y2`, `callback`)

Variants: ~

        |World:rayCast|(`x1`, `y1`, `x2`, `y2`, `callback`)

        There is a bug in LÖVE 0.8.0 where the normal vector passed to the
        callback function gets scaled by love.physics.getMeter.

        Parameters: ~
            • {x1} (`number`)
                The x position of the starting point of the ray.
            • {y1} (`number`)
                The x position of the starting point of the ray.
            • {x2} (`number`)
                The x position of the end point of the ray.
            • {y2} (`number`)
                The x value of the surface normal vector of the shape edge.
            • {callback} (`function`)
                A function called for each fixture intersected by the ray. The
                function gets six arguments and should return a number as a
                control value. The intersection points fed into the function
                will be in an arbitrary order. If you wish to find the closest
                point of intersection, you'll need to do that yourself within
                the function. The easiest way to do that is by using the
                fraction value.


-------------------------------------------------------------------------------
                                               *love2d-docs-World:setCallbacks*
|World:setCallbacks|

Sets functions for the collision callbacks during the world update.

Four Lua functions can be given as arguments. The value nil removes a function.

When called, each function will be passed three arguments. The first two
arguments are the colliding fixtures and the third argument is the Contact
between them. The postSolve callback additionally gets the normal and tangent
impulse for each contact point. See notes.

If you are interested to know when exactly each callback is called, consult a
Box2d manual

Synopses: ~

        |World:setCallbacks|(`beginContact`, `endContact`, `preSolve`,
        `postSolve`)

Variants: ~

        |World:setCallbacks|(`beginContact`, `endContact`, `preSolve`,
        `postSolve`)

        See function description

        Parameters: ~
            • {beginContact} (`function`)
                Gets called when two fixtures begin to overlap.
            • {endContact} (`function`)
                Gets called when two fixtures cease to overlap. This will also
                be called outside of a world update, when colliding objects are
                destroyed.
            • {preSolve} (`function`) (default: `nil`)
                Gets called before a collision gets resolved.
            • {postSolve} (`function`) (default: `nil`)
                Gets called after the collision has been resolved.


-------------------------------------------------------------------------------
                                           *love2d-docs-World:setContactFilter*
|World:setContactFilter|

Sets a function for collision filtering.

If the group and category filtering doesn't generate a collision decision, this
function gets called with the two fixtures as arguments. The function should
return a boolean value where true means the fixtures will collide and false
means they will pass through each other.

Synopses: ~

        |World:setContactFilter|(`filter`)

Variants: ~

        |World:setContactFilter|(`filter`)

        See function description

        Parameters: ~
            • {filter} (`function`)
                The function handling the contact filtering.


-------------------------------------------------------------------------------
                                                 *love2d-docs-World:setGravity*
|World:setGravity|

Set the gravity of the world.

Synopses: ~

        |World:setGravity|(`x`, `y`)

Variants: ~

        |World:setGravity|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x component of gravity.
            • {y} (`number`)
                The y component of gravity.


-------------------------------------------------------------------------------
                                         *love2d-docs-World:setSleepingAllowed*
|World:setSleepingAllowed|

Sets the sleep behaviour of the world.

Synopses: ~

        |World:setSleepingAllowed|(`allow`)

Variants: ~

        |World:setSleepingAllowed|(`allow`)

        See function description

        Parameters: ~
            • {allow} (`boolean`)
                True if bodies in the world are allowed to sleep, or false if
                not.


-------------------------------------------------------------------------------
                                            *love2d-docs-World:translateOrigin*
|World:translateOrigin|

Translates the World's origin. Useful in large worlds where floating point
precision issues become noticeable at far distances from the origin.

Synopses: ~

        |World:translateOrigin|(`x`, `y`)

Variants: ~

        |World:translateOrigin|(`x`, `y`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x component of the new origin with respect to the old
                origin.
            • {y} (`number`)
                The y component of the new origin with respect to the old
                origin.


-------------------------------------------------------------------------------
                                                     *love2d-docs-World:update*
|World:update|

Update the state of the world.

Synopses: ~

        |World:update|(`dt`, `velocityiterations`,
        `positioniterations`)

Variants: ~

        |World:update|(`dt`, `velocityiterations`,
        `positioniterations`)

        See function description

        Parameters: ~
            • {dt} (`number`)
                The time (in seconds) to advance the physics simulation.
            • {velocityiterations} (`number`) (default: `8`)
                The maximum number of steps used to determine the new
                velocities when resolving a collision.
            • {positioniterations} (`number`) (default: `3`)
                The maximum number of steps used to determine the new positions
                when resolving a collision.



===============================================================================
                                                       *love2d-docs-love.sound*
|love.sound|

This module is responsible for decoding sound files. It can't play the sounds,
see love.audio for that.

    |callbacks|                              |love2d-docs-love.sound-callbacks|
    |enums|                                      |love2d-docs-love.sound-enums|
    |functions|                              |love2d-docs-love.sound-functions|
    |types|                                      |love2d-docs-love.sound-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.sound-callbacks*
The callbacks of |love.sound|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.sound-enums*
The enums of |love.sound|:

    None

-------------------------------------------------------------------------------
                                             *love2d-docs-love.sound-functions*
The functions of |love.sound|:

    |newDecoder|                            |love2d-docs-love.sound.newDecoder|
    |newSoundData|                        |love2d-docs-love.sound.newSoundData|

-------------------------------------------------------------------------------
                                            *love2d-docs-love.sound.newDecoder*
|love.sound.newDecoder|

Attempts to find a decoder for the encoded sound data in the specified file.

Synopses: ~

    1.  `decoder` = |love.sound.newDecoder|(`file`, `buffer`)
    2.  `decoder` = |love.sound.newDecoder|(`filename`, `buffer`)

Variants: ~

    1.  `decoder` = |love.sound.newDecoder|(`file`, `buffer`)

    1.          See function description

        Parameters: ~
            • {file} (`File`) |love2d-docs-File|
                The file with encoded sound data.
            • {buffer} (`number`) (default: `2048`)
                The size of each decoded chunk, in bytes.

        Return: ~
            (`Decoder`) |love2d-docs-Decoder|
                A new Decoder object.
    2.  `decoder` = |love.sound.newDecoder|(`filename`, `buffer`)

    2.          See function description

        Parameters: ~
            • {filename} (`string`)
                The filename of the file with encoded sound data.
            • {buffer} (`number`) (default: `2048`)
                The size of each decoded chunk, in bytes.

        Return: ~
            (`Decoder`) |love2d-docs-Decoder|
                A new Decoder object.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.sound.newSoundData*
|love.sound.newSoundData|

Creates new SoundData from a filepath, File, or Decoder. It's also possible to
create SoundData with a custom sample rate, channel and bit depth.

The sound data will be decoded to the memory in a raw format. It is recommended
to create only short sounds like effects, as a 3 minute song uses 30 MB of
memory this way.

Synopses: ~

    1.  `soundData` = |love.sound.newSoundData|(`filename`)
    2.  `soundData` = |love.sound.newSoundData|(`file`)
    3.  `soundData` = |love.sound.newSoundData|(`decoder`)
    4.  `soundData` = |love.sound.newSoundData|(`samples`, `rate`,
        `bits`, `channels`)

Variants: ~

    1.  `soundData` = |love.sound.newSoundData|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The file name of the file to load.

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                A new SoundData object.
    2.  `soundData` = |love.sound.newSoundData|(`file`)

    2.          See function description

        Parameters: ~
            • {file} (`File`) |love2d-docs-File|
                A File pointing to an audio file.

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                A new SoundData object.
    3.  `soundData` = |love.sound.newSoundData|(`decoder`)

    3.          See function description

        Parameters: ~
            • {decoder} (`Decoder`) |love2d-docs-Decoder|
                Decode data from this Decoder until EOF.

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                A new SoundData object.
    4.  `soundData` = |love.sound.newSoundData|(`samples`, `rate`,
        `bits`, `channels`)

    4.          See function description

        Parameters: ~
            • {samples} (`number`)
                Total number of samples.
            • {rate} (`number`) (default: `44100`)
                Number of samples per second
            • {bits} (`number`) (default: `16`)
                Bits per sample (8 or 16).
            • {channels} (`number`) (default: `2`)
                Either 1 for mono or 2 for stereo.

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                A new SoundData object.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.sound-types*
The types of |love.sound|:

    |Decoder|                                             |love2d-docs-Decoder|
    |SoundData|                                         |love2d-docs-SoundData|

-------------------------------------------------------------------------------
                                                          *love2d-docs-Decoder*
|Decoder|

An object which can gradually decode a sound file.

                                             *love2d-docs-Decoder-constructors*
    constructors:

        |newDecoder|                                   |love2d-docs-newDecoder|

                                               *love2d-docs-Decoder-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                 *love2d-docs-Decoder-subtypes*
    subtypes:

        None

                                                *love2d-docs-Decoder-functions*
    functions:

        |clone|                                     |love2d-docs-Decoder:clone|
        |decode|                                   |love2d-docs-Decoder:decode|
        |getBitDepth|                         |love2d-docs-Decoder:getBitDepth|
        |getChannelCount|                 |love2d-docs-Decoder:getChannelCount|
        |getDuration|                         |love2d-docs-Decoder:getDuration|
        |getSampleRate|                     |love2d-docs-Decoder:getSampleRate|
        |seek|                                       |love2d-docs-Decoder:seek|

-------------------------------------------------------------------------------
                                                    *love2d-docs-Decoder:clone*
|Decoder:clone|

Creates a new copy of current decoder.

The new decoder will start decoding from the beginning of the audio stream.

Synopses: ~

        `decoder` = |Decoder:clone|()

Variants: ~

        `decoder` = |Decoder:clone|()

        See function description

        Return: ~
            (`Decoder`) |love2d-docs-Decoder|
                New copy of the decoder.
-------------------------------------------------------------------------------
                                                   *love2d-docs-Decoder:decode*
|Decoder:decode|

Decodes the audio and returns a SoundData object containing the decoded audio
data.

Synopses: ~

        `soundData` = |Decoder:decode|()

Variants: ~

        `soundData` = |Decoder:decode|()

        See function description

        Return: ~
            (`SoundData`) |love2d-docs-SoundData|
                Decoded audio data.
-------------------------------------------------------------------------------
                                              *love2d-docs-Decoder:getBitDepth*
|Decoder:getBitDepth|

Returns the number of bits per sample.

Synopses: ~

        `bitDepth` = |Decoder:getBitDepth|()

Variants: ~

        `bitDepth` = |Decoder:getBitDepth|()

        See function description

        Return: ~
            (`number`)
                Either 8, or 16.
-------------------------------------------------------------------------------
                                          *love2d-docs-Decoder:getChannelCount*
|Decoder:getChannelCount|

Returns the number of channels in the stream.

Synopses: ~

        `channels` = |Decoder:getChannelCount|()

Variants: ~

        `channels` = |Decoder:getChannelCount|()

        See function description

        Return: ~
            (`number`)
                1 for mono, 2 for stereo.
-------------------------------------------------------------------------------
                                              *love2d-docs-Decoder:getDuration*
|Decoder:getDuration|

Gets the duration of the sound file. It may not always be sample-accurate, and
it may return -1 if the duration cannot be determined at all.

Synopses: ~

        `duration` = |Decoder:getDuration|()

Variants: ~

        `duration` = |Decoder:getDuration|()

        See function description

        Return: ~
            (`number`)
                The duration of the sound file in seconds, or -1 if it cannot
                be determined.
-------------------------------------------------------------------------------
                                            *love2d-docs-Decoder:getSampleRate*
|Decoder:getSampleRate|

Returns the sample rate of the Decoder.

Synopses: ~

        `rate` = |Decoder:getSampleRate|()

Variants: ~

        `rate` = |Decoder:getSampleRate|()

        See function description

        Return: ~
            (`number`)
                Number of samples per second.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Decoder:seek*
|Decoder:seek|

Sets the currently playing position of the Decoder.

Synopses: ~

        |Decoder:seek|(`offset`)

Variants: ~

        |Decoder:seek|(`offset`)

        See function description

        Parameters: ~
            • {offset} (`number`)
                The position to seek to, in seconds.



-------------------------------------------------------------------------------
                                                        *love2d-docs-SoundData*
|SoundData|

Contains raw audio samples.

You can not play SoundData back directly. You must wrap a Source object around
it.

                                           *love2d-docs-SoundData-constructors*
    constructors:

        |newSoundData|                               |love2d-docs-newSoundData|

                                             *love2d-docs-SoundData-supertypes*
    supertypes:

        |Data|                                               |love2d-docs-Data|
        |Object|                                           |love2d-docs-Object|

                                               *love2d-docs-SoundData-subtypes*
    subtypes:

        None

                                              *love2d-docs-SoundData-functions*
    functions:

        |getBitDepth|                       |love2d-docs-SoundData:getBitDepth|
        |getChannelCount|               |love2d-docs-SoundData:getChannelCount|
        |getDuration|                       |love2d-docs-SoundData:getDuration|
        |getSample|                           |love2d-docs-SoundData:getSample|
        |getSampleCount|                 |love2d-docs-SoundData:getSampleCount|
        |getSampleRate|                   |love2d-docs-SoundData:getSampleRate|
        |setSample|                           |love2d-docs-SoundData:setSample|

-------------------------------------------------------------------------------
                                            *love2d-docs-SoundData:getBitDepth*
|SoundData:getBitDepth|

Returns the number of bits per sample.

Synopses: ~

        `bitdepth` = |SoundData:getBitDepth|()

Variants: ~

        `bitdepth` = |SoundData:getBitDepth|()

        See function description

        Return: ~
            (`number`)
                Either 8, or 16.
-------------------------------------------------------------------------------
                                        *love2d-docs-SoundData:getChannelCount*
|SoundData:getChannelCount|

Returns the number of channels in the SoundData.

Synopses: ~

        `channels` = |SoundData:getChannelCount|()

Variants: ~

        `channels` = |SoundData:getChannelCount|()

        See function description

        Return: ~
            (`number`)
                1 for mono, 2 for stereo.
-------------------------------------------------------------------------------
                                            *love2d-docs-SoundData:getDuration*
|SoundData:getDuration|

Gets the duration of the sound data.

Synopses: ~

        `duration` = |SoundData:getDuration|()

Variants: ~

        `duration` = |SoundData:getDuration|()

        See function description

        Return: ~
            (`number`)
                The duration of the sound data in seconds.
-------------------------------------------------------------------------------
                                              *love2d-docs-SoundData:getSample*
|SoundData:getSample|

Gets the value of the sample-point at the specified position. For stereo
SoundData objects, the data from the left and right channels are interleaved in
that order.

Synopses: ~

    1.  `sample` = |SoundData:getSample|(`i`)
    2.  `sample` = |SoundData:getSample|(`i`, `channel`)

Variants: ~

    1.  `sample` = |SoundData:getSample|(`i`)

    1.          See function description

        Parameters: ~
            • {i} (`number`)
                An integer value specifying the position of the sample
                (starting at 0).

        Return: ~
            (`number`)
                The normalized samplepoint (range -1.0 to 1.0).
    2.  `sample` = |SoundData:getSample|(`i`, `channel`)

    2.          Gets the value of a sample using an explicit sample index instead of
        interleaving them in the sample position parameter.

        Parameters: ~
            • {i} (`number`)
                An integer value specifying the position of the sample
                (starting at 0).
            • {channel} (`number`)
                The index of the channel to get within the given sample.

        Return: ~
            (`number`)
                The normalized samplepoint (range -1.0 to 1.0).
-------------------------------------------------------------------------------
                                         *love2d-docs-SoundData:getSampleCount*
|SoundData:getSampleCount|

Returns the number of samples per channel of the SoundData.

Synopses: ~

        `count` = |SoundData:getSampleCount|()

Variants: ~

        `count` = |SoundData:getSampleCount|()

        See function description

        Return: ~
            (`number`)
                Total number of samples.
-------------------------------------------------------------------------------
                                          *love2d-docs-SoundData:getSampleRate*
|SoundData:getSampleRate|

Returns the sample rate of the SoundData.

Synopses: ~

        `rate` = |SoundData:getSampleRate|()

Variants: ~

        `rate` = |SoundData:getSampleRate|()

        See function description

        Return: ~
            (`number`)
                Number of samples per second.
-------------------------------------------------------------------------------
                                              *love2d-docs-SoundData:setSample*
|SoundData:setSample|

Sets the value of the sample-point at the specified position. For stereo
SoundData objects, the data from the left and right channels are interleaved in
that order.

Synopses: ~

    1.  |SoundData:setSample|(`i`, `sample`)
    2.  |SoundData:setSample|(`i`, `channel`, `sample`)

Variants: ~

    1.  |SoundData:setSample|(`i`, `sample`)

    1.          See function description

        Parameters: ~
            • {i} (`number`)
                An integer value specifying the position of the sample
                (starting at 0).
            • {sample} (`number`)
                The normalized samplepoint (range -1.0 to 1.0).


    2.  |SoundData:setSample|(`i`, `channel`, `sample`)

    2.          Sets the value of a sample using an explicit sample index instead of
        interleaving them in the sample position parameter.

        Parameters: ~
            • {i} (`number`)
                An integer value specifying the position of the sample
                (starting at 0).
            • {channel} (`number`)
                The index of the channel to set within the given sample.
            • {sample} (`number`)
                The normalized samplepoint (range -1.0 to 1.0).



===============================================================================
                                                      *love2d-docs-love.system*
|love.system|

Provides access to information about the user's system.

    |callbacks|                             |love2d-docs-love.system-callbacks|
    |enums|                                     |love2d-docs-love.system-enums|
    |functions|                             |love2d-docs-love.system-functions|
    |types|                                     |love2d-docs-love.system-types|

-------------------------------------------------------------------------------
                                            *love2d-docs-love.system-callbacks*
The callbacks of |love.system|:

    None

-------------------------------------------------------------------------------
                                                *love2d-docs-love.system-enums*
The enums of |love.system|:

    |PowerState|                                       |love2d-docs-PowerState|

-------------------------------------------------------------------------------
                                                       *love2d-docs-PowerState*
|PowerState|

The basic state of the system's power supply.

    constants: ~

        `unknown`: string

            Cannot determine power status.

        `battery`: string

            Not plugged in, running on a battery.

        `nobattery`: string

            Plugged in, no battery available.

        `charging`: string

            Plugged in, charging battery.

        `charged`: string

            Plugged in, battery is fully charged.

-------------------------------------------------------------------------------
                                            *love2d-docs-love.system-functions*
The functions of |love.system|:

    |getClipboardText|               |love2d-docs-love.system.getClipboardText|
    |getOS|                                     |love2d-docs-love.system.getOS|
    |getPowerInfo|                       |love2d-docs-love.system.getPowerInfo|
    |getProcessorCount|             |love2d-docs-love.system.getProcessorCount|
    |hasBackgroundMusic|           |love2d-docs-love.system.hasBackgroundMusic|
    |openURL|                                 |love2d-docs-love.system.openURL|
    |setClipboardText|               |love2d-docs-love.system.setClipboardText|
    |vibrate|                                 |love2d-docs-love.system.vibrate|

-------------------------------------------------------------------------------
                                     *love2d-docs-love.system.getClipboardText*
|love.system.getClipboardText|

Gets text from the clipboard.

Synopses: ~

        `text` = |love.system.getClipboardText|()

Variants: ~

        `text` = |love.system.getClipboardText|()

        See function description

        Return: ~
            (`string`)
                The text currently held in the system's clipboard.
-------------------------------------------------------------------------------
                                                *love2d-docs-love.system.getOS*
|love.system.getOS|

Gets the current operating system. In general, LÖVE abstracts away the need to
know the current operating system, but there are a few cases where it can be
useful (especially in combination with os.execute.)

Synopses: ~

        `osString` = |love.system.getOS|()

Variants: ~

        `osString` = |love.system.getOS|()

        In LÖVE version 0.8.0, the '''love._os''' string contains the current
        operating system.

        Return: ~
            (`string`)
                The current operating system. 'OS X', 'Windows', 'Linux',
                'Android' or 'iOS'.
-------------------------------------------------------------------------------
                                         *love2d-docs-love.system.getPowerInfo*
|love.system.getPowerInfo|

Gets information about the system's power supply.

Synopses: ~

        `state`, `percent`, `seconds` = |love.system.getPowerInfo|()

Variants: ~

        `state`, `percent`, `seconds` = |love.system.getPowerInfo|()

        See function description

        Returns: ~
            1. (`PowerState`) |love2d-docs-PowerState|
                The basic state of the power supply.

            2. (`number`)
                Percentage of battery life left, between 0 and 100. nil if the
                value can't be determined or there's no battery.

            3. (`number`)
                Seconds of battery life left. nil if the value can't be
                determined or there's no battery.
-------------------------------------------------------------------------------
                                    *love2d-docs-love.system.getProcessorCount*
|love.system.getProcessorCount|

Gets the amount of logical processor in the system.

Synopses: ~

        `processorCount` = |love.system.getProcessorCount|()

Variants: ~

        `processorCount` = |love.system.getProcessorCount|()

        The number includes the threads reported if technologies such as
        Intel's Hyper-threading are enabled. For example, on a 4-core CPU with
        Hyper-threading, this function will return 8.

        Return: ~
            (`number`)
                Amount of logical processors.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.system.hasBackgroundMusic*
|love.system.hasBackgroundMusic|

Gets whether another application on the system is playing music in the
background.

Currently this is implemented on iOS and Android, and will always return false
on other operating systems. The t.audio.mixwithsystem flag in love.conf can be
used to configure whether background audio / music from other apps should play
while LÖVE is open.

Synopses: ~

        `backgroundmusic` = |love.system.hasBackgroundMusic|()

Variants: ~

        `backgroundmusic` = |love.system.hasBackgroundMusic|()

        See function description

        Return: ~
            (`boolean`)
                True if the user is playing music in the background via another
                app, false otherwise.
-------------------------------------------------------------------------------
                                              *love2d-docs-love.system.openURL*
|love.system.openURL|

Opens a URL with the user's web or file browser.

Synopses: ~

        `success` = |love.system.openURL|(`url`)

Variants: ~

        `success` = |love.system.openURL|(`url`)

        Passing file:// scheme in Android 7.0 (Nougat) and later always result
        in failure. Prior to 11.2, this will crash LÖVE instead of returning
        false.

        Parameters: ~
            • {url} (`string`)
                The URL to open. Must be formatted as a proper URL.

        Return: ~
            (`boolean`)
                Whether the URL was opened successfully.
-------------------------------------------------------------------------------
                                     *love2d-docs-love.system.setClipboardText*
|love.system.setClipboardText|

Puts text in the clipboard.

Synopses: ~

        |love.system.setClipboardText|(`text`)

Variants: ~

        |love.system.setClipboardText|(`text`)

        See function description

        Parameters: ~
            • {text} (`string`)
                The new text to hold in the system's clipboard.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.system.vibrate*
|love.system.vibrate|

Causes the device to vibrate, if possible. Currently this will only work on
Android and iOS devices that have a built-in vibration motor.

Synopses: ~

        |love.system.vibrate|(`seconds`)

Variants: ~

        |love.system.vibrate|(`seconds`)

        See function description

        Parameters: ~
            • {seconds} (`number`) (default: `0.5`)
                The duration to vibrate for. If called on an iOS device, it
                will always vibrate for 0.5 seconds due to limitations in the
                iOS system APIs.


-------------------------------------------------------------------------------
                                                *love2d-docs-love.system-types*
The types of |love.system|:

    None

===============================================================================
                                                      *love2d-docs-love.thread*
|love.thread|

Allows you to work with threads.

Threads are separate Lua environments, running in parallel to the main code. As
their code runs separately, they can be used to compute complex operations
without adversely affecting the frame rate of the main thread. However, as they
are separate environments, they cannot access the variables and functions of
the main thread, and communication between threads is limited.

All LOVE objects (userdata) are shared among threads so you'll only have to
send their references across threads. You may run into concurrency issues if
you manipulate an object on multiple threads at the same time.

When a Thread is started, it only loads the love.thread module. Every other
module has to be loaded with require.

    |callbacks|                             |love2d-docs-love.thread-callbacks|
    |enums|                                     |love2d-docs-love.thread-enums|
    |functions|                             |love2d-docs-love.thread-functions|
    |types|                                     |love2d-docs-love.thread-types|

-------------------------------------------------------------------------------
                                            *love2d-docs-love.thread-callbacks*
The callbacks of |love.thread|:

    None

-------------------------------------------------------------------------------
                                                *love2d-docs-love.thread-enums*
The enums of |love.thread|:

    None

-------------------------------------------------------------------------------
                                            *love2d-docs-love.thread-functions*
The functions of |love.thread|:

    |getChannel|                           |love2d-docs-love.thread.getChannel|
    |newChannel|                           |love2d-docs-love.thread.newChannel|
    |newThread|                             |love2d-docs-love.thread.newThread|

-------------------------------------------------------------------------------
                                           *love2d-docs-love.thread.getChannel*
|love.thread.getChannel|

Creates or retrieves a named thread channel.

Synopses: ~

        `channel` = |love.thread.getChannel|(`name`)

Variants: ~

        `channel` = |love.thread.getChannel|(`name`)

        See function description

        Parameters: ~
            • {name} (`string`)
                The name of the channel you want to create or retrieve.

        Return: ~
            (`Channel`) |love2d-docs-Channel|
                The Channel object associated with the name.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.thread.newChannel*
|love.thread.newChannel|

Create a new unnamed thread channel.

One use for them is to pass new unnamed channels to other threads via
Channel:push on a named channel.

Synopses: ~

        `channel` = |love.thread.newChannel|()

Variants: ~

        `channel` = |love.thread.newChannel|()

        See function description

        Return: ~
            (`Channel`) |love2d-docs-Channel|
                The new Channel object.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.thread.newThread*
|love.thread.newThread|

Creates a new Thread from a filename, string or FileData object containing Lua
code.

Synopses: ~

    1.  `thread` = |love.thread.newThread|(`filename`)
    2.  `thread` = |love.thread.newThread|(`fileData`)
    3.  `thread` = |love.thread.newThread|(`codestring`)

Variants: ~

    1.  `thread` = |love.thread.newThread|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The name of the Lua file to use as the source.

        Return: ~
            (`Thread`) |love2d-docs-Thread|
                A new Thread that has yet to be started.
    2.  `thread` = |love.thread.newThread|(`fileData`)

    2.          See function description

        Parameters: ~
            • {fileData} (`FileData`) |love2d-docs-FileData|
                The FileData containing the Lua code to use as the source.

        Return: ~
            (`Thread`) |love2d-docs-Thread|
                A new Thread that has yet to be started.
    3.  `thread` = |love.thread.newThread|(`codestring`)

    3.          See function description

        Parameters: ~
            • {codestring} (`string`)
                A string containing the Lua code to use as the source. It needs
                to either be at least 1024 characters long, or contain at least
                one newline.

        Return: ~
            (`Thread`) |love2d-docs-Thread|
                A new Thread that has yet to be started.
-------------------------------------------------------------------------------
                                                *love2d-docs-love.thread-types*
The types of |love.thread|:

    |Channel|                                             |love2d-docs-Channel|
    |Thread|                                               |love2d-docs-Thread|

-------------------------------------------------------------------------------
                                                          *love2d-docs-Channel*
|Channel|

An object which can be used to send and receive data between different threads.

                                             *love2d-docs-Channel-constructors*
    constructors:

        |newChannel|                                   |love2d-docs-newChannel|
        |getChannel|                                   |love2d-docs-getChannel|

                                               *love2d-docs-Channel-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                 *love2d-docs-Channel-subtypes*
    subtypes:

        None

                                                *love2d-docs-Channel-functions*
    functions:

        |clear|                                     |love2d-docs-Channel:clear|
        |demand|                                   |love2d-docs-Channel:demand|
        |getCount|                               |love2d-docs-Channel:getCount|
        |hasRead|                                 |love2d-docs-Channel:hasRead|
        |peek|                                       |love2d-docs-Channel:peek|
        |performAtomic|                     |love2d-docs-Channel:performAtomic|
        |pop|                                         |love2d-docs-Channel:pop|
        |push|                                       |love2d-docs-Channel:push|
        |supply|                                   |love2d-docs-Channel:supply|

-------------------------------------------------------------------------------
                                                    *love2d-docs-Channel:clear*
|Channel:clear|

Clears all the messages in the Channel queue.

Synopses: ~

        |Channel:clear|()

Variants: ~

        |Channel:clear|()

        See function description


-------------------------------------------------------------------------------
                                                   *love2d-docs-Channel:demand*
|Channel:demand|

Retrieves the value of a Channel message and removes it from the message queue.

It waits until a message is in the queue then returns the message value.

Synopses: ~

    1.  `value` = |Channel:demand|()
    2.  `value` = |Channel:demand|(`timeout`)

Variants: ~

    1.  `value` = |Channel:demand|()

    1.          See function description

        Return: ~
            (`Variant`)
                The contents of the message.
    2.  `value` = |Channel:demand|(`timeout`)

    2.          See function description

        Parameters: ~
            • {timeout} (`number`)
                The maximum amount of time to wait.

        Return: ~
            (`Variant`)
                The contents of the message or nil if the timeout expired.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Channel:getCount*
|Channel:getCount|

Retrieves the number of messages in the thread Channel queue.

Synopses: ~

        `count` = |Channel:getCount|()

Variants: ~

        `count` = |Channel:getCount|()

        See function description

        Return: ~
            (`number`)
                The number of messages in the queue.
-------------------------------------------------------------------------------
                                                  *love2d-docs-Channel:hasRead*
|Channel:hasRead|

Gets whether a pushed value has been popped or otherwise removed from the
Channel.

Synopses: ~

        `hasread` = |Channel:hasRead|(`id`)

Variants: ~

        `hasread` = |Channel:hasRead|(`id`)

        See function description

        Parameters: ~
            • {id} (`number`)
                An id value previously returned by Channel:push.

        Return: ~
            (`boolean`)
                Whether the value represented by the id has been removed from
                the Channel via Channel:pop, Channel:demand, or Channel:clear.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Channel:peek*
|Channel:peek|

Retrieves the value of a Channel message, but leaves it in the queue.

It returns nil if there's no message in the queue.

Synopses: ~

        `value` = |Channel:peek|()

Variants: ~

        `value` = |Channel:peek|()

        See function description

        Return: ~
            (`Variant`)
                The contents of the message.
-------------------------------------------------------------------------------
                                            *love2d-docs-Channel:performAtomic*
|Channel:performAtomic|

Executes the specified function atomically with respect to this Channel.

Calling multiple methods in a row on the same Channel is often useful. However
if multiple Threads are calling this Channel's methods at the same time, the
different calls on each Thread might end up interleaved (e.g. one or more of
the second thread's calls may happen in between the first thread's calls.)

This method avoids that issue by making sure the Thread calling the method has
exclusive access to the Channel until the specified function has returned.

Synopses: ~

        `ret1`, `...` = |Channel:performAtomic|(`func`, `...`)

Variants: ~

        `ret1`, `...` = |Channel:performAtomic|(`func`, `...`)

        See function description

        Parameters: ~
            • {func} (`function`)
                The function to call, the form of function(channel, arg1, arg2,
                ...) end. The Channel is passed as the first argument to the
                function when it is called.
            • {...} (`any`)
                Additional arguments that the given function will receive when
                it is called.

        Returns: ~
            1. (`any`)
                The first return value of the given function (if any.)

            2. (`any`)
                Any other return values.
-------------------------------------------------------------------------------
                                                      *love2d-docs-Channel:pop*
|Channel:pop|

Retrieves the value of a Channel message and removes it from the message queue.

It returns nil if there are no messages in the queue.

Synopses: ~

        `value` = |Channel:pop|()

Variants: ~

        `value` = |Channel:pop|()

        See function description

        Return: ~
            (`Variant`)
                The contents of the message.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Channel:push*
|Channel:push|

Send a message to the thread Channel.

See Variant for the list of supported types.

Synopses: ~

        `id` = |Channel:push|(`value`)

Variants: ~

        `id` = |Channel:push|(`value`)

        See function description

        Parameters: ~
            • {value} (`Variant`)
                The contents of the message.

        Return: ~
            (`number`)
                Identifier which can be supplied to Channel:hasRead
-------------------------------------------------------------------------------
                                                   *love2d-docs-Channel:supply*
|Channel:supply|

Send a message to the thread Channel and wait for a thread to accept it.

See Variant for the list of supported types.

Synopses: ~

    1.  `success` = |Channel:supply|(`value`)
    2.  `success` = |Channel:supply|(`value`, `timeout`)

Variants: ~

    1.  `success` = |Channel:supply|(`value`)

    1.          See function description

        Parameters: ~
            • {value} (`Variant`)
                The contents of the message.

        Return: ~
            (`boolean`)
                Whether the message was successfully supplied (always true).
    2.  `success` = |Channel:supply|(`value`, `timeout`)

    2.          See function description

        Parameters: ~
            • {value} (`Variant`)
                The contents of the message.
            • {timeout} (`number`)
                The maximum amount of time to wait.

        Return: ~
            (`boolean`)
                Whether the message was successfully supplied before the
                timeout expired.

-------------------------------------------------------------------------------
                                                           *love2d-docs-Thread*
|Thread|

A Thread is a chunk of code that can run in parallel with other threads. Data
can be sent between different threads with Channel objects.

                                              *love2d-docs-Thread-constructors*
    constructors:

        |newThread|                                     |love2d-docs-newThread|

                                                *love2d-docs-Thread-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                                  *love2d-docs-Thread-subtypes*
    subtypes:

        None

                                                 *love2d-docs-Thread-functions*
    functions:

        |getError|                                |love2d-docs-Thread:getError|
        |isRunning|                              |love2d-docs-Thread:isRunning|
        |start|                                      |love2d-docs-Thread:start|
        |wait|                                        |love2d-docs-Thread:wait|

-------------------------------------------------------------------------------
                                                  *love2d-docs-Thread:getError*
|Thread:getError|

Retrieves the error string from the thread if it produced an error.

Synopses: ~

        `err` = |Thread:getError|()

Variants: ~

        `err` = |Thread:getError|()

        See function description

        Return: ~
            (`string`)
                The error message, or nil if the Thread has not caused an
                error.
-------------------------------------------------------------------------------
                                                 *love2d-docs-Thread:isRunning*
|Thread:isRunning|

Returns whether the thread is currently running.

Threads which are not running can be (re)started with Thread:start.

Synopses: ~

        `value` = |Thread:isRunning|()

Variants: ~

        `value` = |Thread:isRunning|()

        See function description

        Return: ~
            (`boolean`)
                True if the thread is running, false otherwise.
-------------------------------------------------------------------------------
                                                     *love2d-docs-Thread:start*
|Thread:start|

Starts the thread.

Beginning with version 0.9.0, threads can be restarted after they have
completed their execution.

Synopses: ~

    1.  |Thread:start|()
    2.  |Thread:start|(`...`)

Variants: ~

    1.  |Thread:start|()

    1.          See function description


    2.  |Thread:start|(`...`)

    2.          Arguments passed to Thread:start are accessible in the thread's main
        file via '''...''' (the vararg expression.)

        Parameters: ~
            • {...} (`Variant`)
                A string, number, boolean, LÖVE object, or simple table.


-------------------------------------------------------------------------------
                                                      *love2d-docs-Thread:wait*
|Thread:wait|

Wait for a thread to finish.

This call will block until the thread finishes.

Synopses: ~

        |Thread:wait|()

Variants: ~

        |Thread:wait|()

        See function description



===============================================================================
                                                       *love2d-docs-love.timer*
|love.timer|

Provides an interface to the user's clock.

    |callbacks|                              |love2d-docs-love.timer-callbacks|
    |enums|                                      |love2d-docs-love.timer-enums|
    |functions|                              |love2d-docs-love.timer-functions|
    |types|                                      |love2d-docs-love.timer-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.timer-callbacks*
The callbacks of |love.timer|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.timer-enums*
The enums of |love.timer|:

    None

-------------------------------------------------------------------------------
                                             *love2d-docs-love.timer-functions*
The functions of |love.timer|:

    |getAverageDelta|                  |love2d-docs-love.timer.getAverageDelta|
    |getDelta|                                |love2d-docs-love.timer.getDelta|
    |getFPS|                                    |love2d-docs-love.timer.getFPS|
    |getTime|                                  |love2d-docs-love.timer.getTime|
    |sleep|                                      |love2d-docs-love.timer.sleep|
    |step|                                        |love2d-docs-love.timer.step|

-------------------------------------------------------------------------------
                                       *love2d-docs-love.timer.getAverageDelta*
|love.timer.getAverageDelta|

Returns the average delta time (seconds per frame) over the last second.

Synopses: ~

        `delta` = |love.timer.getAverageDelta|()

Variants: ~

        `delta` = |love.timer.getAverageDelta|()

        See function description

        Return: ~
            (`number`)
                The average delta time over the last second.
-------------------------------------------------------------------------------
                                              *love2d-docs-love.timer.getDelta*
|love.timer.getDelta|

Returns the time between the last two frames.

Synopses: ~

        `dt` = |love.timer.getDelta|()

Variants: ~

        `dt` = |love.timer.getDelta|()

        See function description

        Return: ~
            (`number`)
                The time passed (in seconds).
-------------------------------------------------------------------------------
                                                *love2d-docs-love.timer.getFPS*
|love.timer.getFPS|

Returns the current frames per second.

Synopses: ~

        `fps` = |love.timer.getFPS|()

Variants: ~

        `fps` = |love.timer.getFPS|()

        See function description

        Return: ~
            (`number`)
                The current FPS.
-------------------------------------------------------------------------------
                                               *love2d-docs-love.timer.getTime*
|love.timer.getTime|

Returns the value of a timer with an unspecified starting time.

This function should only be used to calculate differences between points in
time, as the starting time of the timer is unknown.

Synopses: ~

        `time` = |love.timer.getTime|()

Variants: ~

        `time` = |love.timer.getTime|()

        See function description

        Return: ~
            (`number`)
                The time in seconds. Given as a decimal, accurate to the
                microsecond.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.timer.sleep*
|love.timer.sleep|

Pauses the current thread for the specified amount of time.

Synopses: ~

        |love.timer.sleep|(`s`)

Variants: ~

        |love.timer.sleep|(`s`)

        See function description

        Parameters: ~
            • {s} (`number`)
                Seconds to sleep for.


-------------------------------------------------------------------------------
                                                  *love2d-docs-love.timer.step*
|love.timer.step|

Measures the time between two frames.

Calling this changes the return value of love.timer.getDelta.

Synopses: ~

        `dt` = |love.timer.step|()

Variants: ~

        `dt` = |love.timer.step|()

        See function description

        Return: ~
            (`number`)
                The time passed (in seconds).
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.timer-types*
The types of |love.timer|:

    None

===============================================================================
                                                       *love2d-docs-love.touch*
|love.touch|

Provides an interface to touch-screen presses.

    |callbacks|                              |love2d-docs-love.touch-callbacks|
    |enums|                                      |love2d-docs-love.touch-enums|
    |functions|                              |love2d-docs-love.touch-functions|
    |types|                                      |love2d-docs-love.touch-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.touch-callbacks*
The callbacks of |love.touch|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.touch-enums*
The enums of |love.touch|:

    None

-------------------------------------------------------------------------------
                                             *love2d-docs-love.touch-functions*
The functions of |love.touch|:

    |getPosition|                          |love2d-docs-love.touch.getPosition|
    |getPressure|                          |love2d-docs-love.touch.getPressure|
    |getTouches|                            |love2d-docs-love.touch.getTouches|

-------------------------------------------------------------------------------
                                           *love2d-docs-love.touch.getPosition*
|love.touch.getPosition|

Gets the current position of the specified touch-press, in pixels.

Synopses: ~

        `x`, `y` = |love.touch.getPosition|(`id`)

Variants: ~

        `x`, `y` = |love.touch.getPosition|(`id`)

        The unofficial Android and iOS ports of LÖVE 0.9.2 reported
        touch-press positions as normalized values in the range of 1, whereas
        this API reports positions in pixels.

        Parameters: ~
            • {id} (`lightuserdata`)
                The identifier of the touch-press. Use love.touch.getTouches,
                love.touchpressed, or love.touchmoved to obtain touch id
                values.

        Returns: ~
            1. (`number`)
                The position along the x-axis of the touch-press inside the
                window, in pixels.

            2. (`number`)
                The position along the y-axis of the touch-press inside the
                window, in pixels.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.touch.getPressure*
|love.touch.getPressure|

Gets the current pressure of the specified touch-press.

Synopses: ~

        `pressure` = |love.touch.getPressure|(`id`)

Variants: ~

        `pressure` = |love.touch.getPressure|(`id`)

        See function description

        Parameters: ~
            • {id} (`lightuserdata`)
                The identifier of the touch-press. Use love.touch.getTouches,
                love.touchpressed, or love.touchmoved to obtain touch id
                values.

        Return: ~
            (`number`)
                The pressure of the touch-press. Most touch screens aren't
                pressure sensitive, in which case the pressure will be 1.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.touch.getTouches*
|love.touch.getTouches|

Gets a list of all active touch-presses.

Synopses: ~

        `touches` = |love.touch.getTouches|()

Variants: ~

        `touches` = |love.touch.getTouches|()

        The id values are the same as those used as arguments to
        love.touchpressed, love.touchmoved, and love.touchreleased.

        The id value of a specific touch-press is only guaranteed to be unique
        for the duration of that touch-press. As soon as love.touchreleased is
        called using that id, it may be reused for a new touch-press via
        love.touchpressed.

        Return: ~
            (`table`)
                A list of active touch-press id values, which can be used with
                love.touch.getPosition.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.touch-types*
The types of |love.touch|:

    None

===============================================================================
                                                       *love2d-docs-love.video*
|love.video|

This module is responsible for decoding, controlling, and streaming video
files.

It can't draw the videos, see love.graphics.newVideo and Video objects for
that.

    |callbacks|                              |love2d-docs-love.video-callbacks|
    |enums|                                      |love2d-docs-love.video-enums|
    |functions|                              |love2d-docs-love.video-functions|
    |types|                                      |love2d-docs-love.video-types|

-------------------------------------------------------------------------------
                                             *love2d-docs-love.video-callbacks*
The callbacks of |love.video|:

    None

-------------------------------------------------------------------------------
                                                 *love2d-docs-love.video-enums*
The enums of |love.video|:

    None

-------------------------------------------------------------------------------
                                             *love2d-docs-love.video-functions*
The functions of |love.video|:

    |newVideoStream|                    |love2d-docs-love.video.newVideoStream|

-------------------------------------------------------------------------------
                                        *love2d-docs-love.video.newVideoStream*
|love.video.newVideoStream|

Creates a new VideoStream. Currently only Ogg Theora video files are supported.
VideoStreams can't draw videos, see love.graphics.newVideo for that.

Synopses: ~

    1.  `videostream` = |love.video.newVideoStream|(`filename`)
    2.  `videostream` = |love.video.newVideoStream|(`file`)

Variants: ~

    1.  `videostream` = |love.video.newVideoStream|(`filename`)

    1.          See function description

        Parameters: ~
            • {filename} (`string`)
                The file path to the Ogg Theora video file.

        Return: ~
            (`VideoStream`) |love2d-docs-VideoStream|
                A new VideoStream.
    2.  `videostream` = |love.video.newVideoStream|(`file`)

    2.          See function description

        Parameters: ~
            • {file} (`File`) |love2d-docs-File|
                The File object containing the Ogg Theora video.

        Return: ~
            (`VideoStream`) |love2d-docs-VideoStream|
                A new VideoStream.
-------------------------------------------------------------------------------
                                                 *love2d-docs-love.video-types*
The types of |love.video|:

    |VideoStream|                                     |love2d-docs-VideoStream|

-------------------------------------------------------------------------------
                                                      *love2d-docs-VideoStream*
|VideoStream|

An object which decodes, streams, and controls Videos.

                                         *love2d-docs-VideoStream-constructors*
    constructors:

        |newVideoStream|                           |love2d-docs-newVideoStream|

                                           *love2d-docs-VideoStream-supertypes*
    supertypes:

        |Object|                                           |love2d-docs-Object|

                                             *love2d-docs-VideoStream-subtypes*
    subtypes:

        None

                                            *love2d-docs-VideoStream-functions*
    functions:

        |getFilename|                     |love2d-docs-VideoStream:getFilename|
        |isPlaying|                         |love2d-docs-VideoStream:isPlaying|
        |pause|                                 |love2d-docs-VideoStream:pause|
        |play|                                   |love2d-docs-VideoStream:play|
        |rewind|                               |love2d-docs-VideoStream:rewind|
        |seek|                                   |love2d-docs-VideoStream:seek|
        |tell|                                   |love2d-docs-VideoStream:tell|

-------------------------------------------------------------------------------
                                          *love2d-docs-VideoStream:getFilename*
|VideoStream:getFilename|

Gets the filename of the VideoStream.

Synopses: ~

        `filename` = |VideoStream:getFilename|()

Variants: ~

        `filename` = |VideoStream:getFilename|()

        See function description

        Return: ~
            (`string`)
                The filename of the VideoStream
-------------------------------------------------------------------------------
                                            *love2d-docs-VideoStream:isPlaying*
|VideoStream:isPlaying|

Gets whether the VideoStream is playing.

Synopses: ~

        `playing` = |VideoStream:isPlaying|()

Variants: ~

        `playing` = |VideoStream:isPlaying|()

        See function description

        Return: ~
            (`boolean`)
                Whether the VideoStream is playing.
-------------------------------------------------------------------------------
                                                *love2d-docs-VideoStream:pause*
|VideoStream:pause|

Pauses the VideoStream.

Synopses: ~

        |VideoStream:pause|()

Variants: ~

        |VideoStream:pause|()

        See function description


-------------------------------------------------------------------------------
                                                 *love2d-docs-VideoStream:play*
|VideoStream:play|

Plays the VideoStream.

Synopses: ~

        |VideoStream:play|()

Variants: ~

        |VideoStream:play|()

        See function description


-------------------------------------------------------------------------------
                                               *love2d-docs-VideoStream:rewind*
|VideoStream:rewind|

Rewinds the VideoStream. Synonym to VideoStream:seek(0).

Synopses: ~

        |VideoStream:rewind|()

Variants: ~

        |VideoStream:rewind|()

        See function description


-------------------------------------------------------------------------------
                                                 *love2d-docs-VideoStream:seek*
|VideoStream:seek|

Sets the current playback position of the VideoStream.

Synopses: ~

        |VideoStream:seek|(`offset`)

Variants: ~

        |VideoStream:seek|(`offset`)

        See function description

        Parameters: ~
            • {offset} (`number`)
                The time in seconds since the beginning of the VideoStream.


-------------------------------------------------------------------------------
                                                 *love2d-docs-VideoStream:tell*
|VideoStream:tell|

Gets the current playback position of the VideoStream.

Synopses: ~

        `seconds` = |VideoStream:tell|()

Variants: ~

        `seconds` = |VideoStream:tell|()

        See function description

        Return: ~
            (`number`)
                The number of seconds sionce the beginning of the VideoStream.

===============================================================================
                                                      *love2d-docs-love.window*
|love.window|

Provides an interface for modifying and retrieving information about the
program's window.

    |callbacks|                             |love2d-docs-love.window-callbacks|
    |enums|                                     |love2d-docs-love.window-enums|
    |functions|                             |love2d-docs-love.window-functions|
    |types|                                     |love2d-docs-love.window-types|

-------------------------------------------------------------------------------
                                            *love2d-docs-love.window-callbacks*
The callbacks of |love.window|:

    None

-------------------------------------------------------------------------------
                                                *love2d-docs-love.window-enums*
The enums of |love.window|:

    |DisplayOrientation|                       |love2d-docs-DisplayOrientation|
    |FullscreenType|                               |love2d-docs-FullscreenType|
    |MessageBoxType|                               |love2d-docs-MessageBoxType|

-------------------------------------------------------------------------------
                                               *love2d-docs-DisplayOrientation*
|DisplayOrientation|

Types of device display orientation.

    constants: ~

        `unknown`: string

            Orientation cannot be determined.

        `landscape`: string

            Landscape orientation.

        `landscapeflipped`: string

            Landscape orientation (flipped).

        `portrait`: string

            Portrait orientation.

        `portraitflipped`: string

            Portrait orientation (flipped).

-------------------------------------------------------------------------------
                                                   *love2d-docs-FullscreenType*
|FullscreenType|

Types of fullscreen modes.

    constants: ~

        `desktop`: string

            Sometimes known as borderless fullscreen windowed mode. A
            borderless screen-sized window is created which sits on top of all
            desktop UI elements. The window is automatically resized to match
            the dimensions of the desktop, and its size cannot be changed.

        `exclusive`: string

            Standard exclusive-fullscreen mode. Changes the display mode
            (actual resolution) of the monitor.

        `normal`: string

            Standard exclusive-fullscreen mode. Changes the display mode
            (actual resolution) of the monitor.

-------------------------------------------------------------------------------
                                                   *love2d-docs-MessageBoxType*
|MessageBoxType|

Types of message box dialogs. Different types may have slightly different
looks.

    constants: ~

        `info`: string

            Informational dialog.

        `warning`: string

            Warning dialog.

        `error`: string

            Error dialog.

-------------------------------------------------------------------------------
                                            *love2d-docs-love.window-functions*
The functions of |love.window|:

    |close|                                     |love2d-docs-love.window.close|
    |fromPixels|                           |love2d-docs-love.window.fromPixels|
    |getDPIScale|                         |love2d-docs-love.window.getDPIScale|
    |getDesktopDimensions|       |love2d-docs-love.window.getDesktopDimensions|
    |getDisplayCount|                 |love2d-docs-love.window.getDisplayCount|
    |getDisplayName|                   |love2d-docs-love.window.getDisplayName|
    |getDisplayOrientation|     |love2d-docs-love.window.getDisplayOrientation|
    |getFullscreen|                     |love2d-docs-love.window.getFullscreen|
    |getFullscreenModes|           |love2d-docs-love.window.getFullscreenModes|
    |getIcon|                                 |love2d-docs-love.window.getIcon|
    |getMode|                                 |love2d-docs-love.window.getMode|
    |getPosition|                         |love2d-docs-love.window.getPosition|
    |getSafeArea|                         |love2d-docs-love.window.getSafeArea|
    |getTitle|                               |love2d-docs-love.window.getTitle|
    |getVSync|                               |love2d-docs-love.window.getVSync|
    |hasFocus|                               |love2d-docs-love.window.hasFocus|
    |hasMouseFocus|                     |love2d-docs-love.window.hasMouseFocus|
    |isDisplaySleepEnabled|     |love2d-docs-love.window.isDisplaySleepEnabled|
    |isMaximized|                         |love2d-docs-love.window.isMaximized|
    |isMinimized|                         |love2d-docs-love.window.isMinimized|
    |isOpen|                                   |love2d-docs-love.window.isOpen|
    |isVisible|                             |love2d-docs-love.window.isVisible|
    |maximize|                               |love2d-docs-love.window.maximize|
    |minimize|                               |love2d-docs-love.window.minimize|
    |requestAttention|               |love2d-docs-love.window.requestAttention|
    |restore|                                 |love2d-docs-love.window.restore|
    |setDisplaySleepEnabled|   |love2d-docs-love.window.setDisplaySleepEnabled|
    |setFullscreen|                     |love2d-docs-love.window.setFullscreen|
    |setIcon|                                 |love2d-docs-love.window.setIcon|
    |setMode|                                 |love2d-docs-love.window.setMode|
    |setPosition|                         |love2d-docs-love.window.setPosition|
    |setTitle|                               |love2d-docs-love.window.setTitle|
    |setVSync|                               |love2d-docs-love.window.setVSync|
    |showMessageBox|                   |love2d-docs-love.window.showMessageBox|
    |toPixels|                               |love2d-docs-love.window.toPixels|
    |updateMode|                           |love2d-docs-love.window.updateMode|

-------------------------------------------------------------------------------
                                                *love2d-docs-love.window.close*
|love.window.close|

Closes the window. It can be reopened with love.window.setMode.

Synopses: ~

        |love.window.close|()

Variants: ~

        |love.window.close|()

        See function description


-------------------------------------------------------------------------------
                                           *love2d-docs-love.window.fromPixels*
|love.window.fromPixels|

Converts a number from pixels to density-independent units.

The pixel density inside the window might be greater (or smaller) than the
'size' of the window. For example on a retina screen in Mac OS X with the
highdpi window flag enabled, the window may take up the same physical size as
an 800x600 window, but the area inside the window uses 1600x1200 pixels.
love.window.fromPixels(1600) would return 800 in that case.

This function converts coordinates from pixels to the size users are expecting
them to display at onscreen. love.window.toPixels does the opposite. The
highdpi window flag must be enabled to use the full pixel density of a Retina
screen on Mac OS X and iOS. The flag currently does nothing on Windows and
Linux, and on Android it is effectively always enabled.

Most LÖVE functions return values and expect arguments in terms of pixels
rather than density-independent units.

Synopses: ~

    1.  `value` = |love.window.fromPixels|(`pixelvalue`)
    2.  `x`, `y` = |love.window.fromPixels|(`px`, `py`)

Variants: ~

    1.  `value` = |love.window.fromPixels|(`pixelvalue`)

    1.          See function description

        Parameters: ~
            • {pixelvalue} (`number`)
                A number in pixels to convert to density-independent units.

        Return: ~
            (`number`)
                The converted number, in density-independent units.
    2.  `x`, `y` = |love.window.fromPixels|(`px`, `py`)

    2.          The units of love.graphics.getWidth, love.graphics.getHeight,
        love.mouse.getPosition, mouse events, love.touch.getPosition, and touch
        events are always in terms of pixels.

        Parameters: ~
            • {px} (`number`)
                The x-axis value of a coordinate in pixels.
            • {py} (`number`)
                The y-axis value of a coordinate in pixels.

        Returns: ~
            1. (`number`)
                The converted x-axis value of the coordinate, in
                density-independent units.

            2. (`number`)
                The converted y-axis value of the coordinate, in
                density-independent units.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.window.getDPIScale*
|love.window.getDPIScale|

Gets the DPI scale factor associated with the window.

The pixel density inside the window might be greater (or smaller) than the
'size' of the window. For example on a retina screen in Mac OS X with the
highdpi window flag enabled, the window may take up the same physical size as
an 800x600 window, but the area inside the window uses 1600x1200 pixels.
love.window.getDPIScale() would return 2.0 in that case.

The love.window.fromPixels and love.window.toPixels functions can also be used
to convert between units.

The highdpi window flag must be enabled to use the full pixel density of a
Retina screen on Mac OS X and iOS. The flag currently does nothing on Windows
and Linux, and on Android it is effectively always enabled.

Synopses: ~

        `scale` = |love.window.getDPIScale|()

Variants: ~

        `scale` = |love.window.getDPIScale|()

        The units of love.graphics.getWidth, love.graphics.getHeight,
        love.mouse.getPosition, mouse events, love.touch.getPosition, and touch
        events are always in terms of pixels.

        Return: ~
            (`number`)
                The pixel scale factor associated with the window.
-------------------------------------------------------------------------------
                                 *love2d-docs-love.window.getDesktopDimensions*
|love.window.getDesktopDimensions|

Gets the width and height of the desktop.

Synopses: ~

        `width`, `height` =
        |love.window.getDesktopDimensions|(`displayindex`)

Variants: ~

        `width`, `height` =
        |love.window.getDesktopDimensions|(`displayindex`)

        See function description

        Parameters: ~
            • {displayindex} (`number`) (default: `1`)
                The index of the display, if multiple monitors are available.

        Returns: ~
            1. (`number`)
                The width of the desktop.

            2. (`number`)
                The height of the desktop.
-------------------------------------------------------------------------------
                                      *love2d-docs-love.window.getDisplayCount*
|love.window.getDisplayCount|

Gets the number of connected monitors.

Synopses: ~

        `count` = |love.window.getDisplayCount|()

Variants: ~

        `count` = |love.window.getDisplayCount|()

        See function description

        Return: ~
            (`number`)
                The number of currently connected displays.
-------------------------------------------------------------------------------
                                       *love2d-docs-love.window.getDisplayName*
|love.window.getDisplayName|

Gets the name of a display.

Synopses: ~

        `name` = |love.window.getDisplayName|(`displayindex`)

Variants: ~

        `name` = |love.window.getDisplayName|(`displayindex`)

        See function description

        Parameters: ~
            • {displayindex} (`number`) (default: `1`)
                The index of the display to get the name of.

        Return: ~
            (`string`)
                The name of the specified display.
-------------------------------------------------------------------------------
                                *love2d-docs-love.window.getDisplayOrientation*
|love.window.getDisplayOrientation|

Gets current device display orientation.

Synopses: ~

        `orientation` =
        |love.window.getDisplayOrientation|(`displayindex`)

Variants: ~

        `orientation` =
        |love.window.getDisplayOrientation|(`displayindex`)

        See function description

        Parameters: ~
            • {displayindex} (`number`) (default: `nil`)
                Display index to get its display orientation, or nil for
                default display index.

        Return: ~
            (`DisplayOrientation`) |love2d-docs-DisplayOrientation|
                Current device display orientation.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.window.getFullscreen*
|love.window.getFullscreen|

Gets whether the window is fullscreen.

Synopses: ~

        `fullscreen`, `fstype` = |love.window.getFullscreen|()

Variants: ~

        `fullscreen`, `fstype` = |love.window.getFullscreen|()

        See function description

        Returns: ~
            1. (`boolean`)
                True if the window is fullscreen, false otherwise.

            2. (`FullscreenType`) |love2d-docs-FullscreenType|
                The type of fullscreen mode used.
-------------------------------------------------------------------------------
                                   *love2d-docs-love.window.getFullscreenModes*
|love.window.getFullscreenModes|

Gets a list of supported fullscreen modes.

Synopses: ~

        `modes` = |love.window.getFullscreenModes|(`displayindex`)

Variants: ~

        `modes` = |love.window.getFullscreenModes|(`displayindex`)

        See function description

        Parameters: ~
            • {displayindex} (`number`) (default: `1`)
                The index of the display, if multiple monitors are available.

        Return: ~
            (`table`)
                A table of width/height pairs. (Note that this may not be in
                order.)
-------------------------------------------------------------------------------
                                              *love2d-docs-love.window.getIcon*
|love.window.getIcon|

Gets the window icon.

Synopses: ~

        `imagedata` = |love.window.getIcon|()

Variants: ~

        `imagedata` = |love.window.getIcon|()

        See function description

        Return: ~
            (`ImageData`) |love2d-docs-ImageData|
                The window icon imagedata, or nil if no icon has been set with
                love.window.setIcon.
-------------------------------------------------------------------------------
                                              *love2d-docs-love.window.getMode*
|love.window.getMode|

Gets the display mode and properties of the window.

Synopses: ~

        `width`, `height`, `flags` = |love.window.getMode|()

Variants: ~

        `width`, `height`, `flags` = |love.window.getMode|()

        See function description

        Returns: ~
            1. (`number`)
                Window width.

            2. (`number`)
                Window height.

            3. (`table`)
                Table with the window properties:
-------------------------------------------------------------------------------
                                          *love2d-docs-love.window.getPosition*
|love.window.getPosition|

Gets the position of the window on the screen.

The window position is in the coordinate space of the display it is currently
in.

Synopses: ~

        `x`, `y`, `displayindex` = |love.window.getPosition|()

Variants: ~

        `x`, `y`, `displayindex` = |love.window.getPosition|()

        See function description

        Returns: ~
            1. (`number`)
                The x-coordinate of the window's position.

            2. (`number`)
                The y-coordinate of the window's position.

            3. (`number`)
                The index of the display that the window is in.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.window.getSafeArea*
|love.window.getSafeArea|

Gets area inside the window which is known to be unobstructed by a system title
bar, the iPhone X notch, etc. Useful for making sure UI elements can be seen by
the user.

Synopses: ~

        `x`, `y`, `w`, `h` = |love.window.getSafeArea|()

Variants: ~

        `x`, `y`, `w`, `h` = |love.window.getSafeArea|()

        Values returned are in DPI-scaled units (the same coordinate system as
        most other window-related APIs), not in pixels.

        Returns: ~
            1. (`number`)
                Starting position of safe area (x-axis).

            2. (`number`)
                Starting position of safe area (y-axis).

            3. (`number`)
                Width of safe area.

            4. (`number`)
                Height of safe area.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.getTitle*
|love.window.getTitle|

Gets the window title.

Synopses: ~

        `title` = |love.window.getTitle|()

Variants: ~

        `title` = |love.window.getTitle|()

        See function description

        Return: ~
            (`string`)
                The current window title.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.getVSync*
|love.window.getVSync|

Gets current vertical synchronization (vsync).

Synopses: ~

        `vsync` = |love.window.getVSync|()

Variants: ~

        `vsync` = |love.window.getVSync|()

        This can be less expensive alternative to love.window.getMode if you
        want to get current vsync status.

        Return: ~
            (`number`)
                Current vsync status. 1 if enabled, 0 if disabled, and -1 for
                adaptive vsync.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.hasFocus*
|love.window.hasFocus|

Checks if the game window has keyboard focus.

Synopses: ~

        `focus` = |love.window.hasFocus|()

Variants: ~

        `focus` = |love.window.hasFocus|()

        See function description

        Return: ~
            (`boolean`)
                True if the window has the focus or false if not.
-------------------------------------------------------------------------------
                                        *love2d-docs-love.window.hasMouseFocus*
|love.window.hasMouseFocus|

Checks if the game window has mouse focus.

Synopses: ~

        `focus` = |love.window.hasMouseFocus|()

Variants: ~

        `focus` = |love.window.hasMouseFocus|()

        See function description

        Return: ~
            (`boolean`)
                True if the window has mouse focus or false if not.
-------------------------------------------------------------------------------
                                *love2d-docs-love.window.isDisplaySleepEnabled*
|love.window.isDisplaySleepEnabled|

Gets whether the display is allowed to sleep while the program is running.

Display sleep is disabled by default. Some types of input (e.g. joystick button
presses) might not prevent the display from sleeping, if display sleep is
allowed.

Synopses: ~

        `enabled` = |love.window.isDisplaySleepEnabled|()

Variants: ~

        `enabled` = |love.window.isDisplaySleepEnabled|()

        See function description

        Return: ~
            (`boolean`)
                True if system display sleep is enabled / allowed, false
                otherwise.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.window.isMaximized*
|love.window.isMaximized|

Gets whether the Window is currently maximized.

The window can be maximized if it is not fullscreen and is resizable, and
either the user has pressed the window's Maximize button or
love.window.maximize has been called.

Synopses: ~

        `maximized` = |love.window.isMaximized|()

Variants: ~

        `maximized` = |love.window.isMaximized|()

        See function description

        Return: ~
            (`boolean`)
                True if the window is currently maximized in windowed mode,
                false otherwise.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.window.isMinimized*
|love.window.isMinimized|

Gets whether the Window is currently minimized.

Synopses: ~

        `minimized` = |love.window.isMinimized|()

Variants: ~

        `minimized` = |love.window.isMinimized|()

        See function description

        Return: ~
            (`boolean`)
                True if the window is currently minimized, false otherwise.
-------------------------------------------------------------------------------
                                               *love2d-docs-love.window.isOpen*
|love.window.isOpen|

Checks if the window is open.

Synopses: ~

        `open` = |love.window.isOpen|()

Variants: ~

        `open` = |love.window.isOpen|()

        See function description

        Return: ~
            (`boolean`)
                True if the window is open, false otherwise.
-------------------------------------------------------------------------------
                                            *love2d-docs-love.window.isVisible*
|love.window.isVisible|

Checks if the game window is visible.

The window is considered visible if it's not minimized and the program isn't
hidden.

Synopses: ~

        `visible` = |love.window.isVisible|()

Variants: ~

        `visible` = |love.window.isVisible|()

        See function description

        Return: ~
            (`boolean`)
                True if the window is visible or false if not.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.maximize*
|love.window.maximize|

Makes the window as large as possible.

This function has no effect if the window isn't resizable, since it essentially
programmatically presses the window's 'maximize' button.

Synopses: ~

        |love.window.maximize|()

Variants: ~

        |love.window.maximize|()

        See function description


-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.minimize*
|love.window.minimize|

Minimizes the window to the system's task bar / dock.

Synopses: ~

        |love.window.minimize|()

Variants: ~

        |love.window.minimize|()

        See function description


-------------------------------------------------------------------------------
                                     *love2d-docs-love.window.requestAttention*
|love.window.requestAttention|

Causes the window to request the attention of the user if it is not in the
foreground.

In Windows the taskbar icon will flash, and in OS X the dock icon will bounce.

Synopses: ~

        |love.window.requestAttention|(`continuous`)

Variants: ~

        |love.window.requestAttention|(`continuous`)

        See function description

        Parameters: ~
            • {continuous} (`boolean`) (default: `false`)
                Whether to continuously request attention until the window
                becomes active, or to do it only once.


-------------------------------------------------------------------------------
                                              *love2d-docs-love.window.restore*
|love.window.restore|

Restores the size and position of the window if it was minimized or maximized.

Synopses: ~

        |love.window.restore|()

Variants: ~

        |love.window.restore|()

        See function description


-------------------------------------------------------------------------------
                               *love2d-docs-love.window.setDisplaySleepEnabled*
|love.window.setDisplaySleepEnabled|

Sets whether the display is allowed to sleep while the program is running.

Display sleep is disabled by default. Some types of input (e.g. joystick button
presses) might not prevent the display from sleeping, if display sleep is
allowed.

Synopses: ~

        |love.window.setDisplaySleepEnabled|(`enable`)

Variants: ~

        |love.window.setDisplaySleepEnabled|(`enable`)

        See function description

        Parameters: ~
            • {enable} (`boolean`)
                True to enable system display sleep, false to disable it.


-------------------------------------------------------------------------------
                                        *love2d-docs-love.window.setFullscreen*
|love.window.setFullscreen|

Enters or exits fullscreen. The display to use when entering fullscreen is
chosen based on which display the window is currently in, if multiple monitors
are connected.

Synopses: ~

    1.  `success` = |love.window.setFullscreen|(`fullscreen`)
    2.  `success` = |love.window.setFullscreen|(`fullscreen`, `fstype`)

Variants: ~

    1.  `success` = |love.window.setFullscreen|(`fullscreen`)

    1.          See function description

        Parameters: ~
            • {fullscreen} (`boolean`)
                Whether to enter or exit fullscreen mode.

        Return: ~
            (`boolean`)
                True if an attempt to enter fullscreen was successful, false
                otherwise.
    2.  `success` = |love.window.setFullscreen|(`fullscreen`, `fstype`)

    2.          If fullscreen mode is entered and the window size doesn't match one of
        the monitor's display modes (in normal fullscreen mode) or the window
        size doesn't match the desktop size (in 'desktop' fullscreen mode), the
        window will be resized appropriately. The window will revert back to
        its original size again when fullscreen mode is exited using this
        function.

        Parameters: ~
            • {fullscreen} (`boolean`)
                Whether to enter or exit fullscreen mode.
            • {fstype} (`FullscreenType`) |love2d-docs-FullscreenType|
                The type of fullscreen mode to use.

        Return: ~
            (`boolean`)
                True if an attempt to enter fullscreen was successful, false
                otherwise.
-------------------------------------------------------------------------------
                                              *love2d-docs-love.window.setIcon*
|love.window.setIcon|

Sets the window icon until the game is quit. Not all operating systems support
very large icon images.

Synopses: ~

        `success` = |love.window.setIcon|(`imagedata`)

Variants: ~

        `success` = |love.window.setIcon|(`imagedata`)

        See function description

        Parameters: ~
            • {imagedata} (`ImageData`) |love2d-docs-ImageData|
                The window icon image.

        Return: ~
            (`boolean`)
                Whether the icon has been set successfully.
-------------------------------------------------------------------------------
                                              *love2d-docs-love.window.setMode*
|love.window.setMode|

Sets the display mode and properties of the window.

If width or height is 0, setMode will use the width and height of the desktop.

Changing the display mode may have side effects: for example, canvases will be
cleared and values sent to shaders with canvases beforehand or re-draw to them
afterward if you need to.

Synopses: ~

        `success` = |love.window.setMode|(`width`, `height`, `flags`)

Variants: ~

        `success` = |love.window.setMode|(`width`, `height`, `flags`)

        * If fullscreen is enabled and the width or height is not supported
        (see resize event will be triggered.

        * If the fullscreen type is 'desktop', then the window will be
        automatically resized to the desktop resolution.

        * If the width and height is bigger than or equal to the desktop
        dimensions (this includes setting both to 0) and fullscreen is set to
        false, it will appear 'visually' fullscreen, but it's not true
        fullscreen and conf.lua (i.e. t.window = false) and use this function
        to manually create the window, then you must not call any other
        love.graphics.* function before this one. Doing so will result in
        undefined behavior and/or crashes because OpenGL cannot function
        properly without a window.

        * Transparent backgrounds are currently not supported.

        Parameters: ~
            • {width} (`number`)
                Display width.
            • {height} (`number`)
                Display height.
            • {flags} (`table`)
                The flags table with the options:
                • {fullscreen} (`boolean`) (default: `false`)
                    Fullscreen (true), or windowed (false).
                • {fullscreentype} (`FullscreenType`) |love2d-docs-FullscreenType| (default: `'desktop'`)
                    The type of fullscreen to use. This defaults to 'normal' in
                    0.9.0 through 0.9.2 and to 'desktop' in 0.10.0 and older.
                • {vsync} (`boolean`) (default: `true`)
                    True if LÖVE should wait for vsync, false otherwise.
                • {msaa} (`number`) (default: `0`)
                    The number of antialiasing samples.
                • {stencil} (`boolean`) (default: `true`)
                    Whether a stencil buffer should be allocated. If true, the
                    stencil buffer will have 8 bits.
                • {depth} (`number`) (default: `0`)
                    The number of bits in the depth buffer.
                • {resizable} (`boolean`) (default: `false`)
                    True if the window should be resizable in windowed mode,
                    false otherwise.
                • {borderless} (`boolean`) (default: `false`)
                    True if the window should be borderless in windowed mode,
                    false otherwise.
                • {centered} (`boolean`) (default: `true`)
                    True if the window should be centered in windowed mode,
                    false otherwise.
                • {display} (`number`) (default: `1`)
                    The index of the display to show the window in, if multiple
                    monitors are available.
                • {minwidth} (`number`) (default: `1`)
                    The minimum width of the window, if it's resizable. Cannot
                    be less than 1.
                • {minheight} (`number`) (default: `1`)
                    The minimum height of the window, if it's resizable. Cannot
                    be less than 1.
                • {highdpi} (`boolean`) (default: `false`)
                    True if high-dpi mode should be used on Retina displays in
                    macOS and iOS. Does nothing on non-Retina displays.
                • {x} (`number`) (default: `nil`)
                    The x-coordinate of the window's position in the specified
                    display.
                • {y} (`number`) (default: `nil`)
                    The y-coordinate of the window's position in the specified
                    display.
                • {usedpiscale} (`boolean`) (default: `true`)
                    Disables automatic DPI scaling when false.
                • {srgb} (`boolean`) (default: `false`)
                    Removed in 0.10.0 (set t.gammacorrect in conf.lua instead).
                    True if sRGB gamma correction should be applied when
                    drawing to the screen.

        Return: ~
            (`boolean`)
                True if successful, false otherwise.
-------------------------------------------------------------------------------
                                          *love2d-docs-love.window.setPosition*
|love.window.setPosition|

Sets the position of the window on the screen.

The window position is in the coordinate space of the specified display.

Synopses: ~

        |love.window.setPosition|(`x`, `y`, `displayindex`)

Variants: ~

        |love.window.setPosition|(`x`, `y`, `displayindex`)

        See function description

        Parameters: ~
            • {x} (`number`)
                The x-coordinate of the window's position.
            • {y} (`number`)
                The y-coordinate of the window's position.
            • {displayindex} (`number`) (default: `1`)
                The index of the display that the new window position is
                relative to.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.setTitle*
|love.window.setTitle|

Sets the window title.

Synopses: ~

        |love.window.setTitle|(`title`)

Variants: ~

        |love.window.setTitle|(`title`)

        See function description

        Parameters: ~
            • {title} (`string`)
                The new window title.


-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.setVSync*
|love.window.setVSync|

Sets vertical synchronization mode.

Synopses: ~

        |love.window.setVSync|(`vsync`)

Variants: ~

        |love.window.setVSync|(`vsync`)

        * Not all graphics drivers support adaptive vsync (-1 value). In that
        case, it will be automatically set to 1.

        * It is recommended to keep vsync activated if you don't know about the
        possible implications of turning it off.

        * This function doesn't recreate the window, unlike love.window.setMode
        and love.window.updateMode.

        Parameters: ~
            • {vsync} (`number`)
                VSync number: 1 to enable, 0 to disable, and -1 for adaptive
                vsync.


-------------------------------------------------------------------------------
                                       *love2d-docs-love.window.showMessageBox*
|love.window.showMessageBox|

Displays a message box dialog above the love window. The message box contains a
title, optional text, and buttons.

Synopses: ~

    1.  `success` = |love.window.showMessageBox|(`title`, `message`,
        `type`, `attachtowindow`)
    2.  `pressedbutton` = |love.window.showMessageBox|(`title`,
        `message`, `buttonlist`, `type`, `attachtowindow`)

Variants: ~

    1.  `success` = |love.window.showMessageBox|(`title`, `message`,
        `type`, `attachtowindow`)

    1.          Displays a simple message box with a single 'OK' button.

        Parameters: ~
            • {title} (`string`)
                The title of the message box.
            • {message} (`string`)
                The text inside the message box.
            • {type} (`MessageBoxType`) |love2d-docs-MessageBoxType| (default: `'info'`)
                The type of the message box.
            • {attachtowindow} (`boolean`) (default: `true`)
                Whether the message box should be attached to the love window
                or free-floating.

        Return: ~
            (`boolean`)
                Whether the message box was successfully displayed.
    2.  `pressedbutton` = |love.window.showMessageBox|(`title`,
        `message`, `buttonlist`, `type`, `attachtowindow`)

    2.          Displays a message box with a customized list of buttons.

        Parameters: ~
            • {title} (`string`)
                The title of the message box.
            • {message} (`string`)
                The text inside the message box.
            • {buttonlist} (`table`)
                A table containing a list of button names to show. The table
                can also contain the fields enterbutton and escapebutton, which
                should be the index of the default button to use when the user
                presses 'enter' or 'escape', respectively.
            • {type} (`MessageBoxType`) |love2d-docs-MessageBoxType| (default: `'info'`)
                The type of the message box.
            • {attachtowindow} (`boolean`) (default: `true`)
                Whether the message box should be attached to the love window
                or free-floating.

        Return: ~
            (`number`)
                The index of the button pressed by the user. May be 0 if the
                message box dialog was closed without pressing a button.
-------------------------------------------------------------------------------
                                             *love2d-docs-love.window.toPixels*
|love.window.toPixels|

Converts a number from density-independent units to pixels.

The pixel density inside the window might be greater (or smaller) than the
'size' of the window. For example on a retina screen in Mac OS X with the
highdpi window flag enabled, the window may take up the same physical size as
an 800x600 window, but the area inside the window uses 1600x1200 pixels.
love.window.toPixels(800) would return 1600 in that case.

This is used to convert coordinates from the size users are expecting them to
display at onscreen to pixels. love.window.fromPixels does the opposite. The
highdpi window flag must be enabled to use the full pixel density of a Retina
screen on Mac OS X and iOS. The flag currently does nothing on Windows and
Linux, and on Android it is effectively always enabled.

Most LÖVE functions return values and expect arguments in terms of pixels
rather than density-independent units.

Synopses: ~

    1.  `pixelvalue` = |love.window.toPixels|(`value`)
    2.  `px`, `py` = |love.window.toPixels|(`x`, `y`)

Variants: ~

    1.  `pixelvalue` = |love.window.toPixels|(`value`)

    1.          See function description

        Parameters: ~
            • {value} (`number`)
                A number in density-independent units to convert to pixels.

        Return: ~
            (`number`)
                The converted number, in pixels.
    2.  `px`, `py` = |love.window.toPixels|(`x`, `y`)

    2.          The units of love.graphics.getWidth, love.graphics.getHeight,
        love.mouse.getPosition, mouse events, love.touch.getPosition, and touch
        events are always in terms of pixels.

        Parameters: ~
            • {x} (`number`)
                The x-axis value of a coordinate in density-independent units
                to convert to pixels.
            • {y} (`number`)
                The y-axis value of a coordinate in density-independent units
                to convert to pixels.

        Returns: ~
            1. (`number`)
                The converted x-axis value of the coordinate, in pixels.

            2. (`number`)
                The converted y-axis value of the coordinate, in pixels.
-------------------------------------------------------------------------------
                                           *love2d-docs-love.window.updateMode*
|love.window.updateMode|

Sets the display mode and properties of the window, without modifying
unspecified properties.

If width or height is 0, updateMode will use the width and height of the
desktop.

Changing the display mode may have side effects: for example, canvases will be
cleared. Make sure to save the contents of canvases beforehand or re-draw to
them afterward if you need to.

Synopses: ~

        `success` = |love.window.updateMode|(`width`, `height`,
        `settings`)

Variants: ~

        `success` = |love.window.updateMode|(`width`, `height`,
        `settings`)

        If fullscreen is enabled and the width or height is not supported (see
        resize event will be triggered.

        If the fullscreen type is 'desktop', then the window will be
        automatically resized to the desktop resolution.

        Transparent backgrounds are currently not supported.

        Parameters: ~
            • {width} (`number`)
                Window width.
            • {height} (`number`)
                Window height.
            • {settings} (`table`)
                The settings table with the following optional fields. Any
                field not filled in will use the current value that would be
                returned by love.window.getMode.
                • {fullscreen} (`boolean`)
                    Fullscreen (true), or windowed (false).
                • {fullscreentype} (`FullscreenType`) |love2d-docs-FullscreenType|
                    The type of fullscreen to use.
                • {vsync} (`boolean`)
                    True if LÖVE should wait for vsync, false otherwise.
                • {msaa} (`number`)
                    The number of antialiasing samples.
                • {resizable} (`boolean`)
                    True if the window should be resizable in windowed mode,
                    false otherwise.
                • {borderless} (`boolean`)
                    True if the window should be borderless in windowed mode,
                    false otherwise.
                • {centered} (`boolean`)
                    True if the window should be centered in windowed mode,
                    false otherwise.
                • {display} (`number`)
                    The index of the display to show the window in, if multiple
                    monitors are available.
                • {minwidth} (`number`)
                    The minimum width of the window, if it's resizable. Cannot
                    be less than 1.
                • {minheight} (`number`)
                    The minimum height of the window, if it's resizable. Cannot
                    be less than 1.
                • {highdpi} (`boolean`)
                    True if high-dpi mode should be used on Retina displays in
                    macOS and iOS. Does nothing on non-Retina displays.
                • {x} (`number`)
                    The x-coordinate of the window's position in the specified
                    display.
                • {y} (`number`)
                    The y-coordinate of the window's position in the specified
                    display.

        Return: ~
            (`boolean`)
                True if successful, false otherwise.
-------------------------------------------------------------------------------
                                                *love2d-docs-love.window-types*
The types of |love.window|:

    None

 vim:nospell:ft=help:
